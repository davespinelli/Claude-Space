# Claude Space
Autonomous investing-research + revenue experiment. See PLAN.md. Setup: `python3 -m venv .venv && .venv/bin/pip install pandas numpy yfinance requests`. Daily: `.venv/bin/python research/scan.py && .venv/bin/python paper/ledger.py mark`.

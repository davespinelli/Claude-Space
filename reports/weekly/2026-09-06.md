# Sunday review — 2026-09-06

## 1. Paper performance — 2 NAV rows, too short to measure
`paper/nav.csv` has 2 dated rows after dedupe (09-03, 09-04), both NAV $100,000.00: return +0.00%, max drawdown 0.00%, SPY +0.66% over the same marks → **−0.66pp vs SPY**, which is noise (the 5 opening buys filled 09-04 and the book has not been marked since). Under 5 rows, so: report only.
**Alpaca vs ledger: no divergence.** All 5 rows of `products/bot/orders.csv` match `paper/trades.csv` exactly on date, symbol, side, qty and price (EFA 138 @108.35, VTI 39 @379.73, SPY 19 @770.19, JPM 41 @358.64, IWM 50 @296.01). Ledger cash $26,099.88 reconciles.

## 2. Rules decision — **RULES v1 → v2 adopted** (the one change allowed this week)
Ranked every KEEP-candidate by min(H1,H2) Sharpe subject to its path's drawdown condition, then re-ran the top rows.
- **Top row, idea 171** (COMP top-20 EW monthly, min-half **1.206**, MaxDD −19.6% vs a −20.23% cap): script re-ran cleanly, but sliding the identical month-end schedule 0–7 trading bars, its 4b drawdown pass survives **3 of 8 anchors** (−18.41%..−21.33%) → the pass is an anchor artefact, **disqualified**. The same control on idea 182's book gives **7 of 8**, independently reproducing idea 182B.
- **Idea 115** (min-half 1.17, 4a+4b): its own memo forbids adoption before queue idea 106, still open → passed over.
- **Adopted: idea 94's `EWall + band3-dg`** (min-half **1.1908**, path 4a). Hold every universe.json name inside a 200d ±3% hysteresis band at 0.75/N of NAV, gated-out weight to cash, weekly, no ranking, no vol filter. u56 @10 bps: **8.66% CAGR / 1.2056 Sharpe / −12.05% MaxDD, halves 1.2259/1.1908, OOS 1.2851, 1.77x turnover** against v1's 6.45%/0.6642/−13.83% (halves 0.6409/0.6878, OOS 0.7471). It beats the live book on CAGR, Sharpe **and** MaxDD, in both halves and out of sample — no regression on any axis.
- **Both re-runs were bit-identical to their committed outputs**: every `.csv`/`.md` artefact byte-for-byte unchanged, and the console files differed on 8 and 2 lines respectively, all of them timing or run-date stamps.
- **Verified three ways:** parent script re-run; re-derived from scratch here to every published digit; live implementation checked against the backtest weights on the decision date (same 42 in-band names, same 0.013393 per-name weight, empty symmetric difference). Robustness added by the review: 4a holds **5/5 weekday phases**, **5/10/25/50 bps**, band widths 0/2/3/5/10% (8% fails by 0.6pp).
- **Not adopted: idea 182's R6 top-20 monthly book** (13.61%/1.1557/−18.81%, OOS 1.1695, 4b). Its script re-ran clean (9/9 u56 cells, 400/400 draws, controls 1.7e-17 / 4.4e-16); it lost on min-half Sharpe (1.1017) and carries a worse drawdown than the live book. It stays the standing **4b** candidate for the Tier-3 capital question, where its CAGR matters and v2's does not.
- **Scope, as a limit not a footnote:** v2 fails 4b on the CAGR floor alone (8.66% vs 10.66%), so it is **not** a capital recommendation; it clears the other four 4b bars including OOS Sharpe 1.2851 vs SPY 0.8820. universe.json is current-constituent, so absolute CAGR is survivorship-optimistic.
- Two clauses changed deliberately: v1's **unpriced "hard exit any day" is removed** so the live book matches the priced book, and the 2%-of-NAV drift band became 10% of one position (a 2% band would suppress every 1.34%-of-NAV order).
- Files: RULES.md, baseline.py (`band_state`, `rules_v2_weights`; compare() baselines on v2, v1 kept), bot.py, apply_rules.py, scan.py (`ma200`/`d200`; score marked informational). Both dry-runs green — apply_rules: 42/56 in-band, one position $1,339, no trades on a non-rebalance day; --force-rebalance: 5 trims + 37 buys. Logged in CHANGELOG.md.

## 3. Research hygiene
QUEUE `## Open` holds 89 items (≥6, nothing to backfill); `## In progress` holds one item dated today, not stale. Added 3 ideas this review generated (273 phase-control census over every monthly KEEP claim, 274 is v2 only a de-grossing effect, 275 price the removed daily hard exit).
**Bug fixed:** `research/LEADERBOARD.md` contained an exact duplicate of its first 1,665 lines prepended to itself (rebase artefact). Verified byte-identical prefix, removed; 4,201 → 2,536 lines.

## 4. Deep Value Desk
COVERAGE.md holds 55 verdicts — **2 IDEA, 49 WATCH, 4 PASS** (17 on 09-04, 24 on 09-05, 14 on 09-06). TRACK.md was last rebuilt 09-04 and covers only that cohort: IDEA (2) avg **+2.2%**, vs IWM **+2.2%**; WATCH (15) avg **+0.4%**, vs IWM **+0.4%**; **no PASS section exists** though 4 PASS verdicts now do.
**No hit rate is definable**: 1 trading day of history, IWM and SPY both read +0.0%; LS_TRACK.md says the same. **38 of 55 verdicts are untracked** — `track_picks.py` needs yfinance, which I must not call, so the 6:05pm weekday Action is the thing that has to run.
**Kill criteria: neither IDEA has triggered, and neither can have yet.** STRT (gross margin <15% for two consecutive quarters) and CCSI (Corporate growth <5% YoY for two consecutive quarters) were published 09-04; no filing has landed since and the offline caches end 09-04. First real test: CCSI's Q3 results in early November, STRT on its next 10-Q. Flagged for the next deep dive. No verdicts changed here.

## 5. Revenue — still $0, and the blocker has not moved
LEDGER.md updated with the pipeline, all file-verifiable: **67 proposals drafted, 0 submitted** (every INDEX.md row reads `drafted`); storefront **not live** (`build_site.py` lines 51-52, both Stripe URLs empty TODOs); Fiverr gig **not published** (`orders/` holds only a README); bounties dead. Every paying channel is unreachable by a buyer for the same reason.
**Top 3 actions:** (1) David's MONDAY.md login session — it unblocks all three tiers at once; (2) submit the 67 drafted bids the moment Freelancer/Upwork are logged in; (3) paste the two Stripe Payment Link URLs into `research/build_site.py` so the storefront can take money.
**David must click:** Freelancer.com · Upwork · Fiverr · GitHub · X · Stripe (≈20 min, MONDAY.md). Sep 24 deadline is 18 days out and no channel is live.

# Memo — idea 250's only 4b/4a survivor: `EWall + band3-rw` on B136 (re-confirmation, not a discovery)

1. **What passed.** Equal-weight every name in the panel, gate each name by a 200d MA with a
   ±3% re-entry band, re-normalise survivors back to gross 0.75, weekly, t+1, 10 bps:
   B136 **11.8% / 1.071 / −18.5%**, halves 1.174/0.981, OOS Sharpe 1.078, turnover 4.8x/yr.
2. It clears **both** KEEP paths (4a and 4b) on B136 at **10 and 25 bps** — the only points
   in a 96-point ladder that clear 4a on a large-cap panel — and clears 4b on u56 too
   (12.3% / 1.168 / −17.6%, OOS 1.211).
3. **This is the already-published `EWall + band3-rw` book** (idea 57's ew-band3, priced by
   ideas 84/154/238). Idea 250 re-derives it from a different direction — it is where the
   B136 drawdown cap closes — and adds nothing new to it. No RULES change is proposed here.
4. **It is not what rule 8 chooses.** An IS-Sharpe chooser over the width ladder picks 12%
   on every EWALL0 cell, and 12% fails B136 on drawdown. The 3% width survives only because
   it was pinned by history, not fitted.
5. **It fails SMALL484 outright** at every width (H1, H2, OOS, DD *and* the CAGR floor), so
   it is a large-cap-only book.
6. Exact RULES wording, if a Sunday review ever wants it:
   > *Eligibility:* a name is IN if its close last crossed above 1.03 × its 200-day moving
   > average more recently than it last crossed below 0.97 × that average; otherwise OUT.
   > *Weights:* equal-weight every eligible name, scaled so total gross exposure is 0.75 of
   > capital, remainder in cash. *Cadence:* weekly; decide at Friday's close, trade at the
   > next close. *No ranking, no vol scaler, no vol cap.*
7. **Do not adopt on this evidence.** The 4a pass is against a low-return live book, and the
   4b margins are thin (DD margin +1.73pp at 10 bps, +1.52pp at 25 bps).
8. Survivorship: B136 is a current-constituent list; the drawdown that clears the cap is the
   drawdown of names that survived to 2026.
9. The whole B136 DD margin closes and re-opens across 3% → 8%, so the pass is one grid
   rung wide on either side — cadence/width sensitivity of the kind idea 65 asks for.
10. Recommended disposition: **leave PARKed with the incumbent band3-rw family**; idea 250's
    contribution is the ceiling above it, not this row.

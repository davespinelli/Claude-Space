# MEMO — a rule 8 WINDOW GEOMETRY clause (idea 1039, lane C, 2026-09-16). PROPOSED for Sunday review, NOT applied (PROTOCOL rule 6). No book promoted, no book KEEP claimed.

1. **What it is for.** 1036 published a **0.0191-wide band** (0.0645..0.0836) in the record's K-free null sd across the 16 single ends of the Q16 grid and called it an **END COMPOSITION** channel "of comparable size to END COUNT". This run asks 1039's question — build an end grid whose OOS windows do NOT overlap and see whether the band collapses — and the answer is **NO, it does the opposite**, which makes the composition reading untenable.
2. **The measurement.** Two tuned dials only, the two the queue names: **OVERLAP FRACTION** {0.00, 0.25, 0.50, 0.75, 0.90} x **END GRID = window length** {400, 550, 700} trading days, at a window COUNT held fixed at **K = 6** (a band is max − min and grows with K; 6 x 700 = 4,200 days is the largest disjoint grid this tape admits). All **15** points published, none selected. Controls at every point: placement (4 anchors), panel (U56, B136), cost (0/10/25 bps), and both band conventions.
3. **The answer.** At L = 550 the band runs **0.2382 / 0.1353 / 0.1266 / 0.0809 / 0.0682** over f = 0.00..0.90 — the DISJOINT grid's band is **3.49x** the 90%-overlap grid's (2.21x at L = 400, 4.29x at L = 700). At matched K the record's own Q16 grid bands **0.0193** against **0.2382** for the disjoint L = 550 grid, **12.3x**. Measured cross-window rho moves **−0.073 → +0.705** over the same dial (Q16's is **0.9046**). **Overlap SUPPRESSES a band; it does not create one.**
4. **The yardstick, and the reusable half.** Every band is read against the band the SAME 18 books' own JOINTLY day-resampled paths produce on the SAME windows (1,000 reps; span, length, count and overlap inherited, only real time-variation destroyed). **14 of 15** points sit at or below the 0.95 bar (median percentile **0.335**, median obs/null ratio **0.804**); the record's **Q16_TAIL** band sits at **0.409** of its own null. **H_NULL is recorded FAIL** on the single breach (L = 700, f = 0.25, median **0.970**), where 15 points at a 0.95 bar are expected to breach ~0.75 times.
5. **The length channel.** Length-matching the record's 16 windows (same 16 starts, every window truncated to the shortest, 1,936 days) moves the band from **0.0335 to 0.0469** and 1036's own convention from **0.0191 to 0.0348** — **1.82x**, **H_LEN FAIL**. A large part of the published band is WINDOW LENGTH, and equalising lengths makes it BIGGER because staggered equal-length windows are less nested than tail windows. Both directions are geometry; neither is content.
6. **EXACT CLAUSE WORDING** (to sit beside 1021's SPLIT-POINT BAND and 1036's END COUNT clauses): *"A band published over a grid of windows states the window COUNT, the window LENGTHS and the OVERLAP between consecutive windows, and is published beside the band the SAME books' own resampled paths produce on that SAME geometry. A band is max − min over K correlated draws: on the record's own tape, holding K and length fixed, it runs 0.2382 at zero overlap against 0.0682 at 90% overlap, and the record's Q16 grid bands 0.0193 at the same K. A band that does not exceed its own geometry-matched null is reported as 'inside the geometry's sampling noise', and never as an END COMPOSITION, END PLACEMENT or overlap effect."*
7. **Scope.** Reporting only. It changes no book, no weight, no cadence, and nothing in RULES.md, PROTOCOL.md, scan.py, bot.py or baseline.py, all of which this run left untouched.
8. **Cost of adopting it.** Retrospective: 1036's END COMPOSITION reading of its 0.0191 is withdrawn (its END COUNT clause is untouched and still stands — this run reproduces its band and its rho as gates G6/G7). Prospective: three tokens per published band, plus one resampled null on geometry the run already has in hand.
9. **Rule 8 evidence carried alongside** (PROTOCOL's declared split 2016-12-31, IS 2009–2016 alone, 2017–2026 read ONCE; 3 IS-only choosers x 2 panels x 3 rungs = 18 points, all published). **OOS 4b 18 of 18, OOS 4a 0 of 18.** Best `U56 IS_CAGR → U56-qroll-q0.17-w1008-d0.50` at **15.60% / 1.293 / −15.59%** (10 bps; 0 bps 16.68% / 1.372 / −15.52%) against SPY OOS **15.21% / 0.8711 / −33.72%** and RULES v2 live **8.62% / 1.2007 / −12.05%** (halves 1.232 / 1.176, OOS Sharpe 1.276). No book promoted — every pick is a GRID ladder book the record already holds, and all 18 fail 4a on drawdown against the live book's −12.05%.
10. **Survivorship (rule 9) and reproduction.** U56 and B136 are current-constituent panels, so every LEVEL is optimistic and every 4b count an UPPER bound; the measured object is a RATIO of two bands read off the SAME books, and where the bias does not cancel it raises the Sharpe level, which INFLATES the null and makes H_NULL the EASIER call. Script `research/backtests/2026-09-16_is-the-END-COMPOSITION-BAND-itself-a-RHO-artefact_C.py`, gates **10 of 10 PASS**, hypotheses **0 of 5** — including G6, which reproduces 1036's 0.0645..0.0836 / 0.0191 exactly, and G7, its rho 0.9046 vs published 0.8934.

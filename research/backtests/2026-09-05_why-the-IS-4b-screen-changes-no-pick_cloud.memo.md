# Memo — idea 132: rule 8's IS 4b screen is an abstention rule, not a selector (no book change)

1. **Not a KEEP.** This run selects among existing arms; it promotes nothing. 53 distinct picked arms, 18 pass 4a, 7 pass 4b full-sample and the same 7 pass 4b on the OOS window alone — all pre-existing leaderboard books.
2. **The question is answered: explanation (A).** The screen changes no pick because the IS-Sharpe argmax is already admissible in **100%** of the cells where anything is. Break the selector and picks do move: **12 moved picks, all 12 from non-Sharpe selectors.**
3. **Refinement the queue did not anticipate:** IS-Calmar is equally immune (0 moves, 100% already-admissible). It is the *risk-adjusted-return* selectors that are immune; IS-CAGR (3 moves) and IS-MaxDD (5 moves) are not.
4. **What the screen actually removes** is a whole panel and a whole book, not an overlay: 0 arms admitted in all 6 small-panel cells, in all 4 V1u cells, and in u56/TOP20 @25 bps. Median admitted per cell is **0 of 17**; non-empty in 7 of 18.
5. **The apparent OOS gain is abstention.** Same 216 picks, three readings of S1 − S0 mean OOS Sharpe for the incumbent selector: **+0.327** (idea 129's, averaged over the cells it entered), **+0.055** (fallback to the control in the 11 cells it declines), **+0.000** (paired on the 7 cells where every screen picks).
6. **The CAGR floor is inert here.** S1 and S2 pick the identical arm in every cell for 3 of the 4 selectors; only IS-MaxDD's pick moves.
7. **Proposed PROTOCOL rule 8 wording** (a reporting clause; it changes no selector and no book):
   > Rule 8's in-sample screen is an **abstention** rule: it decides whether a (panel, book, cost) cell is entered, not which arm is chosen — on the published 306-row corpus it changed **0 of 18 picks**. Every screened walk-forward must therefore report (i) admitted arms / total arms per cell, (ii) whether the unscreened argmax was already admissible, and (iii) the screen's OOS advantage **paired** across cells or under an explicit fallback for the cells it declines. An advantage averaged only over the cells a screen chose to enter must not be quoted.
8. **Negative result reported as found:** the incumbent selector is the **worst** of the four on the paired cells (IS-MaxDD 1.052, IS-CAGR 1.047, IS-Calmar 1.040, IS-Sharpe 1.022). At 7 cells this is noise; it is reported, not acted on, and idea 110's pre-registered-grid test is where it should be settled.
9. **Reproduction before anything else:** all 306 of idea 129's rows re-derived to `max|diff| <= 2.2e-16`, engine-equivalence exact on all three panels. The screen under test is the code that produced the result under test.
10. **Caveats:** survivorship on all three current-constituent panels (idea 54); idea 128 — the IS window is too shallow to express a deep drawdown, which biases the screen toward admitting too much (against this run's own P3, which held anyway); t+1 execution only (idea 126); n = 18 cells, 7 paired.

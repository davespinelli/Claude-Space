# PROPOSED PROTOCOL rule 8 clause — LENGTH ADJUSTMENT (proposed, NOT applied; rule 6)

Source: idea 1044, `2026-09-16_is-the-K-FREE-NULL-SD-a-WINDOW-LENGTH-CURVE-with-a-SOLVABLE-EXPONENT_cloud.py`.
Gates 11 of 11 PASS (1036's 0.0645..0.0836 / 0.0191 band and 1039's 0.0335 / 0.0469 reproduced exactly).
Hypotheses 0 of 6 PASS. No book promoted, no book KEEP claimed. RULES.md / PROTOCOL.md / scan.py / bot.py / baseline.py untouched.

**Exact RULES wording proposed:**

> A null sd, spread or band read off windows of UNEQUAL length is never length-adjusted with an
> exponent FITTED on the same tape. On this record's own 18-book GRID pool the fitted exponent
> runs −0.2996 to −0.8465 across the 24 published (panel × rung × anchor) fits, and its own
> geometry-matched null spreads 0.67–0.73 wide at one window per length — seven times the ±0.05
> an exponent claim would need. Where a length adjustment is made at all it uses the A-PRIORI
> sqrt law (sd → sd·√(L/L_ref)), it is published beside the UNADJUSTED number, and it is stated
> that it did not shrink the record's own Q16 band (0.51–1.12× raw, and > 1 at 25 bps on both
> panels). A measured dispersion floor below 0.012 is reported as ZERO: the estimator's own
> zero-dispersion control carries a +0.0115 floor bias at this tape length.

**What the record may no longer say:** that the K-free null sd's length dependence has a solvable
exponent on this tape, or that a published null sd can be length-adjusted before comparison.

**What still stands:** 1039's finding that window LENGTH moves the band 1.82× at fixed content
(reproduced as gate G5); the sqrt law itself is CONFIRMED in expectation — on the zero-dispersion
control, where −0.5 is true by construction, the fitted median b is −0.5011 / −0.5049. The law is
right; the tape cannot measure it. Measured mean pairwise Sharpe-error correlation ρ_e = 0.8917
(C = 27.35 against the independent-error theory value 252.62), against 1036's independently
measured cross-end rho of 0.8934.

**SURVIVORSHIP (rule 9):** U56/B136 are current-constituent lists, so every Sharpe LEVEL is
optimistic. The bias raises C, which FLATTERS H_POW and works AGAINST H_SAT — it cannot create
the failures above.

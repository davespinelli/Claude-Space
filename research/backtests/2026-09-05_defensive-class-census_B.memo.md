# Memo to the Sunday review — idea 133 (lane B, 2026-09-05)

1. **No RULES change is proposed and no book is adopted.** This run tests idea 129's *caveat*, not a
   book. `RULES.md`, `scan.py`, `bot.py` and `baseline.py` are untouched; the standing 4b candidate
   `EWall + vol60-dg` reproduces exactly (u56 11.6%/1.133/−16.9%, broad 12.4%/1.138/−18.7%) and is
   unaffected.
2. **The finding:** the `4b-defensive` class idea 129 proposed has **531 members across six of eight
   books** (2244-row corpus, 306 of which re-run idea 129's grid at 0 mismatches). Only **37 (7.0%)**
   are `EWall + slow trend gate + de-gross`; on the cross-book Pareto front that construction holds
   **2 of 47 seats and ranks 27th by Sharpe**. Its best members are macro-sleeve books (SLV50/SLV25):
   Sharpe up to **1.292**, MaxDD **−13.6%**, OOS Sharpe **1.311**, at native gross 0.74.
3. **Therefore idea 129's memo should be adopted with its caveat DELETED, not weakened.** The class
   is a class, not a book. PROTOCOL should name it, not name `EWall`.
4. **Proposed PROTOCOL rule 4b reporting clause — exact wording** (replaces the sentence idea 129's
   memo proposed; still report-only, no bar is moved, rule 7 stands):

   > An arm that clears 4b's two halves-Sharpe bars, its out-of-sample Sharpe bar and its MaxDD cap,
   > and fails **only** the CAGR floor, is recorded **`4b-defensive`**, not KILL. Such an arm must
   > state, on the same row: its CAGR shortfall in pp/yr against the floor, its **mean gross**, and
   > its **book construction**. `4b-defensive` is a reporting class, not a KEEP path: it confers no
   > eligibility for adoption and no bar is lowered by it.

5. **Why "mean gross" and "book construction" are both required, with the number that forces it:**
   forcing every arm in the corpus to 0.53 mean gross moves TOP20 from 1 class member to 34 and
   TOP40 from 10 to 52 (class size 139 → 278). De-grossing a ranked book *manufactures* membership,
   so a `4b-defensive` row without its gross and its construction is unreadable.
6. **But membership is not merely a gross level.** `V1u` and `TOP5` join at **no** gross level (0 of
   276 rows each — they fail the halves and OOS bars too), while `SLV25`/`SLV50` are members at
   **every** level (73.5/97.1/76.5% and 76.5/76.5/77.9% at native/m53/m75). Class members average
   MaxDD −13.3% at Sharpe 1.12 against non-members' −25.2% at 0.76.
7. **The class is prospectively selectable, which is new.** Rule 8, four selectors fixed in writing
   before any OOS read, 18 cells pooling all books: a selector that screens *for* the class on the
   IS window alone (S3) gives OOS **9.1% / 1.233 / −14.3%**, beating S0 (12.5%/0.958/−23.7%), idea
   129's S1 (13.0%/1.069/−20.9%) and S2 (10.3%/1.192/−16.6%), with the best mean OOS rank (15.9 of
   136) and beating SPY, RULES v1 and its own control in 12/12/11 of its 12 cells. SPY OOS
   15.45%/0.882/−33.72%; RULES v1 OOS 4.86%/0.451/−25.30%.
8. **S3's picks are four different books** (SLV50 ×7, SLV25 ×3, TOP20 ×1, TOP40 ×1, EWall ×0), which
   is the same answer arrived at from the selection side rather than the census side.
9. **Do not read this as a case for the sleeve.** The sleeve's standing is hostage to open ideas 100
   (PARK), 102 (GLD carries 53% of its return), 105 and 106; class members earn 7–9% CAGR against
   SPY's 15.4%; three current-constituent panels (idea 54) flatter the fully-invested controls, so
   the floor's exclusion is understated, not overstated; idea 128's shallow IS window biases every
   IS screen toward over-admission; idea 38 and idea 126 unchanged.
10. **Ask of the review:** adopt the clause in (4) as PROTOCOL text; leave rule 4b's bars where they
    are; leave RULES v1 and the standing candidate alone; and record that idea 129's "may be one
    construction in disguise" caveat has been tested and is false.

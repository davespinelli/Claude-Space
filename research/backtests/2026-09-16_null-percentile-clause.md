# Idea 998 memo — the per-leg null percentile clause + the DEGENERATE NULL defect (PROPOSED for Sunday review, NOT applied; PROTOCOL rule 6)

1. **The question, answered.** Of the record's committed 4b PASSES that name a panel and a book and resolve to a reproducible cell, **11.8% sit BELOW their own cell's null median on OOS Sharpe** (2 of 17 STRICT cells; 7.8% claim-weighted; **7.1%** once the degenerate-null cells are dropped). Median percentile **100.0**. Idea 975-TRANCHE's 0.090-percentile case is real but **atypical** — H_MEDIAN PASS at 0.882 at/above the median. KILL for "the record's 4b passes are routinely inside their own nulls on return".
2. **On DRAWDOWN the sign flips and that is the finding.** The same passes sit at a median percentile of **57.0** (STRICT), **31.5** (WIDE) and **1.0** (STRUCT — the ladder's own 12 structural 4b passes) on MaxDD: 41.2% / 56.1% / **66.7%** of them are worse than their own gross-matched coin flip's median drawdown. A 4b pass buys its CAGR with drawdown a coin flip of the same gross would not have taken.
3. **A null 4b base rate of 0.000 is not one unpassable leg.** Over the 131 zero-base-rate cells of 157, exactly one leg is unpassable in **51 (38.9%, H_ONELEG FAIL)**; 31 cells have four unpassable legs and 29 have five. The unpassable leg is `L_CAGR` in 0.786 of them, `L_DD` and `L_H2` in 0.573, `L_OOS` 0.542, `L_H1` 0.374.
4. **THE DEGENERATE NULL DEFECT (new, and it invalidates a class of published numbers).** **36 of 157 cells have exactly ONE distinct draw out of 200** — every `EWELIG` cell on every panel (12 U56, 12 B136, 12 SMALL663). A holding-count-matched rotating draw has nothing to choose when the book already holds the whole eligible pool: **on `EWELIG` the null IS the book**. Its base rate, its percentiles and any "book vs null" gain quoted on it are vacuous, not merely noisy.
5. **That defect also broke the estimator, and the gate caught it.** With the strict `>` convention G7's self-scored calibration read **median 35.68 / mean 38.41** (3,925 draws) because every tied draw scores 0. Corrected to the standard MID-RANK convention it reads **50.00 / 49.88**. The correction was made because a gate failed; it moves the headline share-below-median from 0.118 to 0.118 (STRICT, OOS Sharpe) — i.e. not at all.
6. **The record cannot be re-adjudicated from its own prose.** **1,977** committed claim units assert a 4b pass; **1,823 (92.2%) name no panel-and-book pair at all**. Of the 154 that do, only **17 distinct cells** survive a strict one-panel-one-book resolution, and **H_REPRO FAILS at 0.294** — 5 of those 17 cells actually pass 4b structurally on this tree at 10 bps.
7. **And the resolution rule moves the answer.** H_CLAIM **FAILS**: STRICT 0.118 vs WIDE 0.287 share-below-median on OOS Sharpe (|d| **0.169** against a 0.10 bar). A published percentile is not a number until the claim's cell is pinned.
8. **Rule 8.** (book, gross) chosen inside each (panel, cadence) on 2009-2016 alone, 2017-2026 read ONCE: **OOS 4b 1 of 9 for all three choosers**, OOS 4a **0 of 27**. Using this idea's own statistic as the selector (`C_ISPCT`, highest IS percentile inside its own null) certifies exactly as often as the level chooser and takes **5.3 pp more drawdown** (median OOS MaxDD −27.24% vs −21.94%). The one pass is `U56 / BAND03 / EXT (1.00) / W`, OOS **12.67% / 1.2755 / −15.91%** against SPY OOS 15.21% / 0.8711 / −33.72% and RULES v2 (live) 9.45% / 1.2762 / −12.05% — idea 972/997's already-published object, not a new one.
9. **PROPOSED PROTOCOL rule 4 reporting clause (exact wording):** *"A 4b verdict quoted against a null is published with (a) the cell it was drawn in — panel, book, gross, cadence and cost rung — (b) the null's number of DISTINCT draws, and (c) the book's mid-rank percentile inside that null on OOS Sharpe AND on MaxDD. A null with fewer than half its draws distinct is reported as DEGENERATE and no base rate or percentile may be quoted from it. A base rate of 0.000 is published with the count of legs whose own pass rate is below 0.05."*
10. **Survivorship (rule 9).** U56 / B136 / SMALL663 are current-constituent lists (SMALL663 additionally drops the 52 `max_1d_move ≥ 1.0` tickers); every LEVEL and every base rate here is optimistic. The measured object is a percentile of a book inside a null drawn from the SAME surviving panel on the SAME tape, so the bias lifts both sides and largely cancels; the rule-8 levels do not have that protection. Gates **9 of 9**, including an exact cross-run of idea 971's committed 900-row ladder (max |d| 2.22e-16) and 0 verdict disagreements against its `p4b_REC_REC5` column.

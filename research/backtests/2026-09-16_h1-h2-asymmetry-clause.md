# Idea 1003 memo — the H1/H2 asymmetry clause (PROPOSED for Sunday review, NOT applied; PROTOCOL rule 6)

1. **What was found.** On idea 970/971's committed 900-row ladder the typical book is EARLY-tilted: the population median of `Sharpe(H2) − Sharpe(H1)` is **−0.2999** pooled (U56 −0.0824, B136 −0.3617, SMALL663 −0.3521), because SPY's own first half is the strong one (H1/H2 Sharpe **0.9588/0.8207** U56, **0.9596/0.8259** B136, **0.9138/0.8344** SMALL663 — H_SPY PASS 3 of 3).
2. **4b's pass set is systematically less early-tilted than the population it is drawn from** — median gap **+0.2294** pooled, **+0.0656** U56, **+0.0415** B136, positive under all four asymmetry statistics (`A_DIFF` +0.2294, `A_MARGIN` +0.2391, `A_RATIO` +0.2474, `A_RANK` +22.50) and at all five cost rungs (+0.2497 / +0.2461 / +0.2294 / +0.2717 / +0.2853 at 0/5/10/25/50 bps). H_LATE PASS.
3. **But the pooled +0.2294 is a composition artefact and must not be quoted alone:** SMALL663 contributes 60 population rows and 0 passes. The honest within-panel numbers are **+0.0656** and **+0.0415**.
4. **The tilt is a property of the BAR, not of the rules.** The gross-matched rotating coin flip's OWN 4b pass set carries the same tilt: **+0.1534** pooled, **+0.0852** U56, **+0.0432** B136 — larger than the books' on U56 and equal on B136 (H_NULL PASS, |d| 0.0760 against a 0.10 bar). Whatever 4b selects for on this axis, a coin flip that clears the same bar is selected for it just as hard.
5. **971's `L_H1` monopoly does NOT generalise to the REC split.** Over the ladder's REC 4b FAIL rows at 10 bps, `L_H1` fails **0.3631** of the time and `L_H2` **0.5952**; as a SOLE binder, `L_H1` 0.0238 vs `L_H2` 0.0119. H_MONO **FAIL**. The monopoly 971 published belongs to the DISJOINT-halves split's destroyed set, not to 4b as written.
6. **Rule 8 prices the selection and it is not free to correct for.** An IS-only chooser preferring BALANCED halves (`C_ISSYM`, smallest |IS_H2 − IS_H1|) certifies **0 of 9** OOS 4b slots against `C_ISSHARPE`'s and `C_IS4B`'s **1 of 9**, and gives up return: median OOS CAGR **7.69%** vs **11.81%** / **10.46%**. H_RULE8 **FAIL**.
7. **The one OOS 4b pass is not new:** `U56 / BAND03 / EXT (gross 1.00) / W`, OOS **12.67% / 1.2755 / −15.91%** against SPY OOS 15.21% / 0.8711 / −33.72% and RULES v2 (live) 9.45% / 1.2762 / −12.05% — idea 972/997's already-published conditional object, reached again from a third direction. OOS 4a **0 of 27**.
8. **PROPOSED PROTOCOL rule 4 reporting clause (exact wording):** *"A 4b verdict is published with the benchmark's own Sharpe in each half and with the book's `Sharpe(H2) − Sharpe(H1)` read against the median of the SAME panel's book population at the same cost rung. Where the benchmark's halves are themselves asymmetric, the pass set is stated to be selected on that axis and the asymmetry gap is quoted WITHIN PANEL; a pooled gap across panels with unequal pass counts is not a permitted form of the statistic."*
9. **What the clause is NOT.** It does not reject 4b, add a leg, or move a bar; the gap is not a capital signal — correcting for it (`C_ISSYM`) loses 4.1 pp of OOS CAGR and the only OOS certification. It is a reporting requirement, which is the only thing this evidence supports.
10. **Survivorship (rule 9).** U56 / B136 / SMALL663 are current-constituent lists (SMALL663 additionally drops the 52 `max_1d_move ≥ 1.0` tickers). Survivorship is itself LATE-sample tilted, so every asymmetry LEVEL here is an upper bound; the measured object is a pass-set-minus-population DIFFERENCE on the same tape, which removes the panel-wide component but not any interaction between survivorship and the 4b bar. Gates 9 of 9, including an exact cross-run of idea 971's committed 900-row ladder (max |d| 2.22e-16) and 0 verdict disagreements against its `p4b_REC_REC5` column.

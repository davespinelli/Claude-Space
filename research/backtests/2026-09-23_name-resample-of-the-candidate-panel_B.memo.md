# idea 2520 — result memo (lane B, run 65, 2026-09-23). NOT a new KEEP-candidate; no RULES change.

1. **The question.** Is the standing 4b KEEP-candidate (idea 2322's CAP2) a property of the RULE, or of the 56 tickers it was scored on? First NAME-dimension resample in the record; every prior bootstrap resamples TIME.
2. **Answer: the RULE — 4b survives on 63–97% of random sub-panels once the cap's breadth linkage is matched** (U56 70/77/93%, B136 63/90/97% at f = 0.25/0.50/0.75, g0.75, 10 bps, 30 md5-seeded draws each). The committed 56 names sit at the 87th/77th/63rd percentile of their own sub-panels — better than typical, not an outlier.
3. **The committed clause verbatim fails on sub-panels for an arithmetic reason, not a name reason.** `min(g/N_in, 2%)` pins gross at `min(g, 0.02 x N_in)` (idea 2506), so a 14-name book cannot exceed 0.28 of NAV. 4b then reads 0/0/10/100% with `L_CAGR` the ONLY failing leg and median risk gross 0.188/0.381/0.559/0.667 in lockstep.
4. **Wording for whoever takes CAP2 to a Sunday review** (this is the memo's one actionable line; it changes nothing today): if the clause is ever to be stated so that it means the same thing on a panel of a different size, it must be written as a GROSS SCHEDULE, not a name cap — *"hold every admitted name at `G_t / N_in` of NAV where `G_t = min(gross, kappa x N_in)` and `kappa = gross / 37.5`"*. On the committed 56-name panel at gross 0.75 this is byte-identical to the live 2% cap; on any other panel it preserves the schedule instead of silently de-grossing.
5. **Rule 8: the panel is not a dial and fitting it loses.** 2,366 of 2,400 IS-only picks beat SPY's OOS Sharpe, but under the breadth-matched cap the fitted picks average OOS Sharpe 1.2534 (U56) / 1.0328 (B136) against the pre-registered full panel's 1.2906 / 1.0391. The panel must stay PRE-REGISTERED.
6. **The finding that was not asked for.** Same-names paired contrast (BAND minus EWALL, gate the only difference): U56 median dSharpe **+0.1010** (348/364 positive), B136 **-0.0252** (82/364). The sign holds at every breadth including f = 1.00. The 200d gate is a Sharpe COST on the broad panel.
7. **What the gate reliably does on BOTH panels:** shallower MaxDD in 726 of 728 (median +9.2 pp) paid for with lower CAGR in 727 of 728 (median -3.4 pp). It is a drawdown-buying device; only on U56 does it also buy Sharpe.
8. **Caveat stated, not buried:** the contrast is NOT exposure-matched — BAND runs mean risk gross 15.6 pp (U56) / 2.6–6.5 pp (B136) below EWALL. Within-panel rank correlation of dSharpe with that gap is only +0.07 / -0.27 (within-cell median -0.16 over 24 cells), so exposure does not explain the panel split, but an exposure-matched re-run is the obvious next idea.
9. **Survivorship (rule 9):** a random sub-panel of current constituents is still a panel of survivors. The pass-rate curve inherits the bias in full. Only the BAND-minus-EWALL contrast is first-order immune.
10. **Verdict: CONFIRM of the candidate's 4b as rule-driven + KILL of the band gate read as a name-SELECTION edge on B136. No new book, no change to RULES.md, scan.py, bot.py or baseline.py.**

- 2026-09-05 (cloud, idea 148 the-no-leverage-ceiling-is-load-bearing) — **ANSWERED: the premise is CONFIRMED but narrower than idea 144 implied. PROTOCOL rule 2's no-leverage clause IS doing part of 4b's adequacy work — and it does so exclusively through the CAGR floor.** Idea 94's harness and idea 144's corpus were imported rather than re-implemented, and four checks passed before any new number was read: `H.run` vs `engine.backtest` **max|diff| = 0.000e+00**, idea 94's published EWall+vol60-dg u56@10bps row (**11.587% / 1.133 / -16.884%**), idea 129/131's POINT census at m=1.00 (**306 rows / 29 pass4b / 27 floor-only KILL**), and idea 144's own floor-exclusion claim (**51 at c=1.00 -> 37 at c=1.30**, exact). 306 books x a 26-point gross family = **7,956 backtests**; two tuned parameters (phi in 7 values, delta in 6), the ceiling swept exhaustively over its legal range and every one of 13 x 42 = 546 verdict sets reported. **(1) THE ANSWER: 57 of 306 4b verdicts (18.6%) are decided by WHERE the ceiling is put rather than by either bar** over c = 0.75 .. 1.3333 (1.3333 x 75% target = 100% gross, which is where rule 2 actually bites — not idea 144's 1.30). 16 books pass at every legal ceiling, 233 at none, 57 flip somewhere. FAMILY-4b passes run **16 -> 73** across the sweep (58 at c=1.00, 72 at 1.30) and the CAGR floor's unique exclusions run **93 -> 36**. **(2) THE MECHANISM IS SINGLE-CHANNEL.** Of the 57 ceiling-decided KILLs at c=0.75, **57 fail the CAGR floor** and **55 fail only the floor**; **0 fail the DD cap at any ceiling** — as they cannot, since the cap tightens with gross while the floor relaxes. Deleting the floor (phi = 0) drives the ceiling-decided count to **0 at all 6 deltas x all 13 ceilings**. The clause has no bite of its own: it is a second, unlabelled setting of the adequacy bar. **(3) IDEA 144's OWN CEILING WAS SAFE BY PLACEMENT, NOT BY CONSTRUCTION**: c=1.30 vs the legal 1.3333 changes **1** verdict, but c=1.00 changes 15 and c=0.75 changes 57, and at c=1.30 the ceiling-decided count ranges **0..5 (median 0)** over the 42-point bar grid against **0..84** at c=0.75. **(4) 4a IS CEILING-BLIND**: 184 of 306 books pass FAMILY-4a at *every* ceiling, so the whole interaction is 4b's. **(5) Rule 8** (choose on 2009-2016, evaluate 2017-2026 untouched, 18 cells): every family ceiling beats the no-screen control by **+0.032** paired OOS Sharpe and the m-pinned screen (idea 146's recommendation) by **+0.010**, best at c=1.20 (**0.710**) — but all sit **0.17-0.20 below SPY's 0.882** (OOS CAGR 6.4-8.9% vs SPY 15.45%; MaxDD -17 to -22% vs -33.7%; RULES v1 4.86%/0.451/-25.3%). Raising the ceiling buys OOS CAGR and pays OOS drawdown at a near-constant rate while moving Sharpe by ~0.002 — the gross dial again, as ideas 66 and 146 found. **Not reproduced and reported as such:** idea 144's 'the m<=1.30 ceiling binds in 54 of 306 books' matches none of three readings tried (admissible set reaches the ceiling **9**; FAMILY verdict differs between c=1.00 and c=1.30 **14**; family-Sharpe argmax at m=1.30 **106**). No RULES change, no new book, no KEEP candidate; one proposed 4b reporting clause in `research/backtests/2026-09-05_no-leverage-ceiling-is-load-bearing_cloud.memo.md`. Survivorship, idea 128's shallow IS window, idea 38's calendar-day index and the two non-scale-free `ebud` arms are carried in the script header and the memo.
- 2026-09-05 (cloud, idea 146 pin-m-or-let-the-screen-choose) — **ANSWERED: the static gross multiplier m should be PINNED at the published gross in PROTOCOL rule 8. Rules unchanged, no new book, no KEEP candidate; one proposed rule-8 clause for the Sunday review.** Idea 94's simulator and idea 144's cell builder were imported rather than re-implemented, so this run adjudicates the same corpus the question was asked about, and four checks passed before any new number was read: `H.run` vs `engine.backtest` **max|diff| = 0.0**, idea 94's published EWall+vol60-dg u56@10bps row (11.587% / 1.133 / -16.884%), **0 of 54** differing S0/S1/S3 picks against idea 144's committed picks file, and idea 144's paired aggregates exactly (S1 0.127/1.022/-0.211, S3 0.138/1.019/-0.228). 306 books x a 25-point gross family = **7,650 runs**; two tuned parameters (the m-rule {point, pin, hi, lo} and the ceiling m_max in {1.00, 1.30}), all swept, all reported; 4b bars left at the published phi=0.70, delta=0.60. **(1) The FAMILY screen is not a screen.** In **7 of 7** entering cells it keeps the arm the pinned screen picks and moves only m, so its exchange rate is the gross ladder's own local slope BY IDENTITY (the matched-DD control on that arm is therefore trivially 0.000, stated rather than presented as a finding). Splitting the screen confirms it: PIN — family admissibility with m forced to 1.00 — equals the incumbent POINT screen in 6 of 7 arms and by 0.001 Sharpe in the seventh, so the admissibility change is a null and the m-rule is the whole story. **(2) On idea 74's axis the trade prices well and is still not worth taking:** realised median **0.668** pp CAGR per pp of |MaxDD| given up, beating the fitted OOS ladder slope 7/7 and the 200d gate's price (median 0.448) 7/7 — but beating the **IS** ladder slope **0 of 7**, i.e. the same move was priced at a median 1.082 pp/pp on the window the screen actually reads, 1.6x what it delivered — while buying **-0.003** OOS Sharpe with **0 of 7** cells improved. **(3) What it spends is exactly what 4b rations:** pin -> hi at m_max=1.30 turns **2 of the 3** pinned 4b passes into failures (broad/EWall@10, u56/EWall@10) for Sharpe changes between -0.015 and 0.000. **(4) Its m is an artifact of the window, not a property of the book:** the within-family IS Sharpe range is 0.002-0.004 over all 25 gross points for the scale-free arm kinds, so the selector's own statistic cannot order m and the IS drawdown cap decides it alone — and that cap is too loose, admitting more gross than the OOS window in **4 of 5** comparable cells (mean +0.100) with the two broad/TOP20 cells admitting 8-11 IS points where the OOS window admits zero (idea 128's shallow-IS-crash finding, now measured on the gross axis). **(5) The opposite m-rule is no better as a rule:** FAM-lo gains +0.007 Sharpe and 3.0pp of shallower drawdown but gives up 1.9pp of CAGR, so hi and lo are a coin flip on Sharpe and a large unmandated risk decision on everything else. Idea 144's family convention **survives for REPORTING** (a rescaled book is the same book) and is barred only from SELECTION. Caveats carried: only 7 of 18 cells enter any screen and the small panel enters none; survivorship on all three current-constituent panels flatters high gross, i.e. the side this run rules out; the two `ebud` arms are not pure exposure rescales (idea 144 Q1) and are flagged in the price table; ideas 38 and 126 carry over; Q3's matched-DD control is hindsight and labelled as such. Script `research/backtests/2026-09-05_pin-m-or-let-the-screen-choose_cloud.py`, note `...result.md`, proposed clause `...memo.md`.
- 2026-09-05 (lane B, idea 90 gross-interval-as-a-pre-registered-KEEP-bar) — **ANSWERED, KILL of the proposal as written: the interval is a real object, its WIDTH is not a robustness statistic, and the thing that actually carries the out-of-sample content is the CELL COUNT, which is already a pass/fail.** Idea 84 showed 4b's drawdown cap and CAGR floor both live on the gross axis and idea 144 proved CAGR and |MaxDD| are monotone along a book's own gross family, so idea 90 asked PROTOCOL to quote the width of each book's 4b-passing interval instead of a single pass/fail. Idea 94's simulator was imported and idea 144's family rebuilt and checked before any new number was read: **7,650 of 7,650 rows, index identical, max|diff| 5.0e-7 on IS_Sharpe** (idea 144's CSV is written to 6 decimals; the bar was 1e-6), **0 pass4a disagreements**, and the census comes back exactly — FAMILY-4b 72 at m<=1.30 / 58 at m<=1.00, POINT-4b 29 at m=1.00 (published 72/58/29). Corpus 3 panels x 3 books x 17 arms x 2 cost rungs = 306 books x 25 gross points = **7,650 backtests**, weekly, t+1, 10 and 25 bps, bars held at the published phi=0.70/delta=0.60; exactly two tuned parameters (w*, k) with **all 52 grid points printed**; the m_max ceiling run as an arm at 1.00 and 1.30. **(1) The object is sound.** The passing set is a single contiguous run in **72 of 72** non-empty books at m<=1.30 and 58 of 58 at m<=1.00, n_runs max 1, with **no exceptions to list**, and contiguity holds for the four arms idea 144 flagged as not scale-free (bud 9/9, dd 8/8). **(2) Census.** Non-empty by cell: u56@10 31/51, u56@25 23, broad@10 14, broad@25 4, and **small@10 0, small@25 0 — 0 of 102 small-panel books have an interval at any gross level on either cost rung**, which is direct support for queue idea 136. Widths median 0.15, IQR 0.10-0.21, max 0.40; mean width orders gate 0.047 > dd 0.038 > bud 0.031 > ctl 0.028 > stop 0.010. The queue's own operational KEEP (non-empty on both large-cap universes at both costs) admits **4 of 51 (book, arm) pairs**, all on EWall, and in all four the **intersection is non-empty**: `vol60-dg` **m in [0.95, 1.05]**, `vol60-rw` [0.90, 0.95], `band3-rw` [1.00, 1.05], `ddctl-8/.5/recover` m=1.15. Two of those are the project's standing cross-cell 4b passers (idea 127), recovered independently and now carrying a sizing RANGE where RULES has a point. **(3) The decisive test kills the width.** Against the binary pass/fail it refines: Spearman(IS width, OOS width) **+0.307 vs +0.296**, Spearman(IS width, OOS Sharpe) **+0.540 vs +0.546 — the binary wins** — while the incumbent IS-Sharpe carries **+0.902**; and within the 67 IS-passing books the grade collapses to +0.177 and **+0.055**, i.e. once a book has an interval, how wide it is says essentially nothing about what happens next. P(OOS non-empty | IS width >= w) is non-monotone (0.478 -> 0.656 -> **0.444** -> 0.000). On cross-cell portability the graded statistic is WORSE than the binary in 2 of 4 cells and tied in a third. The one place grading looks better (w*=0.15 lifts, 0.207->0.422 on broad@10) is a threshold read in-sample over 51 books and is contradicted by both rank correlations and (4). **(4) As a bar it is actively harmful, and the cell count is the real statistic.** Holding k=1, raising w* from 0.05 to 0.30 discards 20 of 31 books and LOWERS admitted-set mean OOS Sharpe **1.022 -> 0.989**; holding w* at its floor, raising k from 1 to 4 lifts it **1.022 -> 1.039 (k=3) -> 1.126 (k=4)** against 0.827 for the unbarred large-cap corpus — and plain POINT-4b in >=3 of 4 cells already reaches 1.126. **(5) Rule 8** (screens read 2009-2016 only, OOS read once, 18 cells): the widest-IS-interval selector is the **second non-inert screen this project has found — 2 of 7 picks move, where the incumbent IS-POINT-4b and idea 144's family screen both move 0** — and the first that does not lose Sharpe: paired on the 7 cells every selector enters, S3/S4 **13.1%/1.0397/-21.0%** vs S0=S1 12.7%/1.0219/-21.1% and S2 13.8%/1.0192/-22.8%, against SPY OOS 15.45%/0.882/-33.7% and RULES v1 4.77%/0.480/-19.7%. But the paired gain is literally one substitution (band3-rw for abs12-rw on u56/EWall@10 +0.171; for vol60-dg on broad/EWall@10 -0.046), so it is not evidence, and the memo recommends AGAINST letting the interval choose the arm or the gross. Every screen enters only **7 of 18** cells, declining all 6 small-panel and all 4 V1u cells — idea 132's "abstention is the screen" reproduced on a sixth bar. **No book is proposed, no rule changes, nothing is promoted.** The memo puts an amended PROTOCOL reporting clause to the Sunday review: quote the NUMBER OF CELLS in which the interval is non-empty and the INTERSECTION [m_lo, m_hi] as the range a candidate can be sized at, and explicitly do NOT quote a single cell's width as a robustness statistic. Caveats carried: survivorship on all three current-constituent panels (idea 54); the shallow IS drawdown window biases every selector identically (idea 128); ideas 38 and 126 carry over; 7 paired walk-forward cells; and every Q3/Q4 number is a large-cap number because the small panel contributes no intervals at all. Script: `research/backtests/2026-09-05_gross-interval-as-a-pre-registered-KEEP-bar_B.py`; result: `.result.md`; memo: `.memo.md`.
- 2026-09-05 (cloud, idea 91 band-width-at-g085) — **ANSWERED, KILL of the gross-shortfall hypothesis; the published 3% band SURVIVES the move to g = 0.85 and should be pinned rather than re-chosen. Rules unchanged, no new book.** Idea 84's script was imported rather than re-implemented, so every number sits on the harness that produced the constant being re-opened: the band-parameterised weight builder reproduces idea 84's three fixed books at **0 differing cells**, engine equivalence is `max|diff| = 0.0`, and idea 84's published rows come back exactly (u56 12.8% / 1.135 / -17.1%, halves 1.11/1.16; broad 12.6% / 1.062 / -18.9%, 1.16/0.97) before anything new was read. 5 bands x 5 gross levels x 2 universes x 2 books x 2 cost rungs = 200 points, all printed; two tuned parameters, both swept exhaustively. **(1) The two dials are orthogonal on Sharpe.** Holding the band fixed, full-sample Sharpe moves at most **0.0048** (median 0.0012) across the whole 0.55-1.00 gross ladder, OOS Sharpe at most 0.0047, and the Sharpe-optimal band is **identical at every g in 8 of 8 cells** (8% in six, 3% in one, 0% in one) — so the premise that the width was chosen at the wrong exposure has no room to be true. **(2) The substitution mechanism is refuted with its sign reversed:** a wider band very slightly LOWERS mean realised gross (0.8375 -> 0.8353 at g = 0.85 going 0% -> 8%) rather than raising it. What it buys is turnover, 9.3 -> 3.7 turns/yr on u56/ew-all@10bps. **(3) The band is nonetheless a genuine instrument:** against a matched-gross control (the unbanded book of the same cell interpolated along its own g ladder to the banded point's realised gross) it wins full-sample Sharpe in **116/144** matchable points and OOS Sharpe in **136/144**; at band 3, +0.052 Sharpe, +0.075 OOS Sharpe, +0.59pp CAGR, 0.59pp shallower DD, -1.97 turns/yr. **(4) 3% is not the Sharpe argmax but is the right pin.** 8% wins Sharpe in 6 of 8 cells and pays for it in drawdown; pooled 4b passes run 0%->5, 2%->9, **3%->11**, 5%->10, 8%->9 (at g=0.85: 2/4/4/4/2). Under rule 8 over all 40 cells, **PIN3 12.3% / 1.054 / -17.7% beats SEL-IS 12.3% / 1.025 / -18.9% and the unbanded control 11.5% / 0.983 / -18.3%**, 4b 11/40 vs 9/40 vs 5/40; the IS window picks 8% in 30 of 40 cells and it does not hold up out of sample, and the IS-4b-screened selector abstains in 29 of 40. Only 4b's LEVEL bars (CAGR floor, DD cap) couple the two dials at all. Caveats carried: survivorship on both current-constituent panels (which flatters the wide-band end this run argues against), idea 38's calendar-day index, idea 126's t+1-only execution, idea 128's shallow IS crash. Script `research/backtests/2026-09-05_band-width-at-g085_cloud.py`, note `...result.md`.
- 2026-09-05 (lane C, idea 144 is-the-ladder-even-a-candidate) — **ANSWERED, KILL of the pre-registered hypothesis; the construction fix survives as a reporting convention, not as a bar deletion.** Idea 131 established that every candidate version of 4b's adequacy bar exists only to keep the static-gross ladder out of the corpus, and that no return- or exposure-LEVEL threshold can do that without discarding real defensive books. Idea 144 tested the construction fix instead of another metric: **a static rescaling of a book is the SAME book**, so the ladder is not a candidate but the interior of each book's own gross family. Idea 94's harness is imported and idea 131's corpus reproduced exactly on all four checks (306 rows / 82 Pareto / 29 pass 4b / 27 floor-only; ladder 97 of 342 floor-only, all at m <= 0.80; IS groups 45/9/252). Corpus = 3 panels x 3 books x 17 arms x 2 cost rungs = **306 books x 25 gross points m in [0.10, 1.30] = 7,650 backtests**, weekly, t+1, no leverage; exactly two tuned parameters (phi, delta) with all 42 grid points reported under both conventions and the m_max ceiling run as an arm. **(1) The hypothesis is dead:** with the ladder dropped, the CAGR floor still uniquely excludes **51 of 306** books (37 at the no-leverage ceiling) and the MaxDD cap **18** (22); the floor is load-bearing at **36 of 42** phi x delta points under BOTH conventions. 4b loses no bar. The one bar that excludes nothing anywhere, under either convention, is the **OOS Sharpe bar** (0 unique exclusions in every cut). **(2) What it does lose is a degree of freedom, provably:** along a family CAGR and |MaxDD| both rise with m, so the cap and the floor can only bind at m* = the largest gross clearing the cap, and the m* rule reproduces the exhaustive family verdict in **306 of 306 books at both ceilings**. Five bars, four degrees of freedom. The closed-form single ratio bar `CAGR/|MaxDD| >= (phi x CAGR_SPY)/(delta x |MaxDD_SPY|)` reproduces it **89.2% / 93.1%** — a good approximation, not a substitute (compounding is not linear in m; the ceiling binds in 54 of 306). The cap's bite falls **62%** at the published coefficients (47 -> 18) and to **1 of 306** at delta = 0.70. **(3) The convention is only true for scale-free instruments,** a limit measured rather than assumed on a split pre-registered from the instruments' units: PURE (ctl/gate/stop, n=234) CAGR monotone 228/234, |MaxDD| monotone 234/234, max Sharpe range along the family **0.0130**; SCALE-DEP (ddctl/ebud, n=72) monotone 27/72 and 35/72, max Sharpe range **0.2924** against a cross-book Sharpe sd of 0.319. An 8%-drawdown trigger at half gross is a different rule, because the book can no longer fall 8%. **(4) Mechanism, which is the transferable part:** of the 72 family admits at m <= 1.30, **29 pass only BELOW** published gross (they failed the DD cap; de-grossing fixes it), **14 only ABOVE** (they failed the CAGR floor — 14 of idea 129's 27 floor-only victims), and 29 include m = 1.00 unchanged. At m <= 1.00 the fix saves **0 of the 27 victims**: de-grossing cannot cure a CAGR floor. **(5) Price:** admissions 29 -> 58 -> 72, both KEEP paths 6 -> 72, and admitted-set OOS quality 1.114 / -18.5% / 13.2% -> 1.087 / -19.1% / 12.9% -> 1.098 / -19.6% / 13.2%; the ungated `control` book is admitted in 5 of 18 cells where POINT-4b admits it in 0, and that concession is the whole cost. **(6) Rule 8 walk-forward** (screens read 2009-2016 only, picks read once on 2017-2026, 7 selectors x 18 cells): the FAMILY screen is **the first screen in this project that is not inert — 7 of 18 picks moved, where POINT-4b with and without the floor moves 0 of 18** (reproducing ideas 131 and 132 on a fourth and fifth bar) — but it buys no Sharpe. Paired on the 7 cells every selector enters: S1 = S2 = 12.7% / **1.022** / -21.1%; S3 (family) 13.8% / 1.019 / -22.8%; S4 (floor deleted) 12.4% / 1.020 / -21.2%; S5 (cap deleted) 10.8% / **1.029** / -18.1%; S6 (Calmar bar) 13.8% / 1.019 / -22.8%. Reference OOS over the 18 cells: SPY 15.45% / 0.882 / -33.7%, RULES v1 4.86% / 0.451 / -25.3%; all family selectors beat SPY on Sharpe in 5 of 7 and RULES v1 in 7 of 7. Letting a screen choose gross is therefore **safe but not valuable**: +1.1pp of OOS CAGR for -1.7pp of extra drawdown and -0.003 Sharpe, with the cap-deleted arm the exact mirror image — a clean out-of-sample confirmation of idea 66 (gross is an exact, Sharpe-neutral lever) on ground idea 66 never touched. **No book is proposed, no rule changes, nothing is promoted.** The memo puts two PROTOCOL clauses to the Sunday review — the same-book convention *restricted to scale-free instruments*, and the cap-plus-floor stated as one test at m* — and recommends AGAINST letting rule 8 choose m. Caveats carried: survivorship (idea 54) flatters exactly the 29 cap-rescued admits; the IS window's shallow SPY drawdown (idea 128) biases all selectors identically; 7 paired walk-forward cells and 11 of 18 cells admitting nothing (all 6 small-panel cells among them); ideas 38 and 126 carry over; the no-leverage ceiling is itself load-bearing (binds 54 of 306, moves the floor's exclusions 51 -> 37) and is now queue idea 148. Script: `research/backtests/2026-09-05_is-the-ladder-even-a-candidate_C.py`; memo: `.memo.md`; result: `.result.md`.
# Strategy Changelog
- 2026-09-03 — Scan v1: composite = mean(pct-rank 12-1 mom, 6m, 3m) × trend factor (0.5 if below 200d) ÷ sqrt(vol20). Note: vol scaling favors low-vol ETFs (SPY/RSP rank high); revisit weighting in first Sunday review. No trades yet — rules v1 pending David's risk answers.
- 2026-09-04 — RULES v1 adopted for the paper book (top-5 risk-adjusted momentum, 200d filter, 15% sizing, Friday rebalance, daily 200d hard exit). First trades expected Fri Sep 4 close.
- 2026-09-03 — Local sprint (6 ideas): dual-momentum, inverse-vol, vol-target, broad-universe, full-exposure all KILL/PARK. macro-trend-ensemble variant B is the first KEEP-candidate (Sharpe/DD better in both halves and OOS; CAGR 5% so it is a defensive sleeve). Key diagnostic: v1 gross exposure is a pure lever (corr 1.00 with 100% version); the CAGR gap to SPY comes from the selection composite, which has no demonstrated cross-sectional edge. Direction for next ideas: (a) core index exposure with trend/drawdown control, (b) ensemble sleeve + momentum sleeve, (c) test whether the composite adds anything over equal-weight of eligible names.
- 2026-09-03 (late) — Local sprint total 9 ideas. Decisive: v1 score has zero cross-sectional IC because /sqrt(vol20) cancels momentum; equal-weighting all eligible names gives Sharpe 1.05 (both halves > SPY). PROTOCOL gained KEEP path 4b (judge vs SPY). Recommendation memo: research/backtests/2026-09-03_RECOMMENDATION.md. Rules unchanged pending Sunday review + idea 28.
- 2026-09-04 (lane A, idea 1 no-vol-scaling) — **KILL** as a v1 replacement: all 12 grid points (vol_scale ON/OFF x n in {3,5,8} x gross in {75%,100%}) fail KEEP path 4a on MaxDD and 4b on the drawdown cap. But the treatment is confirmed decisively — dropping `/sqrt(vol20)` is worth **+10.1%/yr (t 3.33)** at v1's own n=5/75%, and beats v1 on Sharpe at every (n, gross). Mechanism: the scaler cuts the average vol20 of held names from 29.4% to 14.1% and leaves only 31% name overlap; it is a different (low-vol) book, not a risk adjustment. Walk-forward (IS 2009-2016 -> OOS 2017-2026) picks OFF n=3/100%: OOS 31.7%/1.06/-33.1% vs v1 7.8%/0.75/-13.8% and SPY 15.5%/0.88/-33.7% — beats both on Sharpe and CAGR, fails 4b only on drawdown. Nearest miss PARK: OFF n=8/75%, 13.8%/0.93/-17.9%, halves 0.92/0.95, OOS 15.4%/0.98, clears 4b's MaxDD, CAGR and OOS tests and fails on H1 Sharpe alone (0.918 vs SPY 0.957). Edge survives 25 bps, dies at 50. New ideas 38-40 queued.
- 2026-09-04 (lane A) — **DATA BUG, acts on live signals.** `data/prices.csv` is indexed on CALENDAR days from 2014-09-17 (BTC-USD's first date): yfinance returns a 7-day index when crypto is in the ticker list and `cache_prices.py` ffills every equity across weekends. `load_universe()` drops the crypto columns but keeps the rows, so ~30% of post-2014 daily returns are exactly zero and years have 365 rows, not 252. (1) `engine.metrics()` annualizes at 252, so every sandbox/Actions backtest understates CAGR and Sharpe, and only in the second half of the sample — which corrupts the H1/H2 test PROTOCOL rule 4 rests on. v1 reads 4.65%/0.555/halves 0.659-0.452 on the raw cache vs 6.48%/0.666/0.641-0.692 corrected; the corrected figures reproduce the Sep-3 leaderboard row exactly, so the Sep-3 rows (run locally on live yfinance) are sound and only cloud-run rows are affected. (2) It flips verdicts: SPY reads 11.5%/0.78 raw vs 15.3%/0.89 corrected, so 4b's SPY tests are far too easy — OFF n=8/75% *passes* 4b on the raw cache and fails it corrected. (3) `research/scan.py` downloads the universe **including** BTC/ETH and ffills, so the live daily scan runs on the same index: `rolling(200)` spans ~143 trading days and `rolling(20)` ~14. Measured 2015+: the 200d-MA flag is wrong on 7.6% of ticker-days, at least one name is mis-flagged on 95.8% of days (mean 4.2 of 56), full eligibility differs on 8.1% of ticker-days, and vol20 reads a median 0.84x of true — loosening the vol<0.60 gate and mis-scaling the `1/sqrt(vol20)` term. The paper book began trading on these signals today. Fix is queued as idea 38 (INFRASTRUCTURE, do first); PROTOCOL forbids a research script from patching baseline.py/scan.py, so this goes to the Sunday review.
- 2026-09-04 (lane B, idea 40 vol-scaler-replacement) — **KILL** as a capital-worthy (4b) replacement for the per-name vol scaler: 0 of 21 grid points pass. Base book = lane A's no-scaler book (top-n by the v1 composite without `/sqrt(vol20)`, eligible only, 75% gross, weekly); treatment = a book-level control instead of a per-name one; 2 tuned params (n in {3,5,8}, threshold). (1) **The book-level drawdown rule of QUEUE idea 22 is a KILL mechanism, not a mixed result.** A 6-12% drawdown trigger with a new-high reset holds a 15-21%-vol book at half exposure on **52-79% of all days** — permanent de-leveraging, not tail control. It keeps 49-62% of the raw book's CAGR to buy back 23-39% of its drawdown and lowers Sharpe at every one of the 9 (n, D) points. Idea 22 is answered here and needs no separate run. Two points (n=8, D=6% and D=8%) do pass **4a** — 7.1%/0.722/-12.1% and 7.4%/0.700/-13.8% vs v1's 6.5%/0.666/-13.8% — and are precisely the pathology 4b was added to catch: they clear the bar only because the live book is weak, and fail all four 4b tests. Memo recommends the Sunday review record them as a documented 4a pass and **reject** them. (2) **The 200d-breadth gate is nearly free and is the project's nearest 4b miss.** Halving exposure when the fraction of the universe above its own 200d MA falls below B costs ~1pp of CAGR for ~4pp of drawdown, improves MaxDD in 8 of 9 points, and sits at half exposure on only 7-17% of days — against the scaler's cost of 10.1%/yr. It never improves Sharpe (9 of 9 slightly worse): it is a drawdown reducer, not a Sharpe improver. **n=3/B=30% passes 4b's H1 (0.961 vs SPY 0.957), H2 (1.089), OOS Sharpe (1.081 vs 0.884) and CAGR (21.0% vs a 10.7% floor) and fails on MaxDD alone by 0.4pp: -20.6% against a -20.2% cap.** Not tuned further (rule 7). (3) Walk-forward (rule 8, two selection rules fixed before reading any OOS number): the breadth arm's IS-Sharpe pick is that same n=3/B=30%, OOS 23.8%/1.081/-20.6% vs RULES v1 7.8%/0.751/-13.8% and SPY 15.5%/0.884/-33.7%. The 4b-aware selection rule picks **nothing** in the NONE and BREADTH arms — no in-sample point met the in-sample drawdown cap — which is the structural finding: this book cannot be made to satisfy 4b's drawdown test by a book-level exposure gate alone. Diagnosis for the next ideas: **H1 Sharpe against SPY's 0.957 is now the single binding 4b constraint in ideas 24, 25, 28 and 40** — four independent candidates failing the same test points at the 2009-2017 window, not at the strategies. Ideas 41-43 queued. Survivorship caveat (current-constituent universe.json) applies to the 21%/yr figures.
- 2026-09-04 (lane A, idea 2 position-count) — **KEEP-candidate under PROTOCOL path 4b — the project's first.** The question is not "3 vs 5 vs 8": RULES v1 holds the top 5 at a *fixed 15% each*, so its position count and its gross exposure are one dial (n=3 is a 45%-invested book, n=8 a levered 120% one), and ideas 1 and 40 therefore varied two things at once. This run separates them — arm FIXEDW keeps v1's w=15% (n<=6, since n=7 is leverage), arm EQW holds gross constant at 75% with w=0.75/n — and runs both on the v1 composite (ON) and without the `/sqrt(vol20)` term (OFF). Tuned parameter: n only; all 30 points reported. Harness sanity: `ON */n=5` reproduces the v1 baseline exactly and `OFF */n=5` reproduces idea 1's row. **5 of 30 points pass 4b, 2 pass 4a.** (1) The candidate: **top-20 eligible names, equal-weight at 75% gross, ranked without the vol scaler — 12.7%/1.093/-18.3%, halves 1.088/1.103** (SPY 0.957/0.837), and it is the rule-8 walk-forward pick of the 4b-aware selection rule, chosen on 2009-2016 alone, with untouched **OOS 14.4%/1.170/-18.3% vs SPY 15.5%/0.884/-33.7% and RULES v1 7.8%/0.751/-13.8%**. It is *not* the in-sample Sharpe maximum (0.993 vs n=4's 1.101) — it was selected on in-sample drawdown and improved out of sample, the opposite of the overfitting signature; the plain-Sharpe rule picks n=4 and fails on drawdown both in and out of sample. Memo with exact RULES wording: `2026-09-04_position-count.md`. (2) **Position count is what resolves the H1-Sharpe constraint that ideas 24, 25, 28 and 40 all failed on.** The `fail4b` column splits cleanly: n=2-5 fail on MaxDD (too concentrated), **n=6-10 fail on H1 Sharpe alone** (0.892-0.918 vs 0.957 — this is idea 1's nearest miss and idea 40's entire BREADTH arm), n>=12 pass everything. So the answer to queue idea 43 is at least partly "a concentration failure, not a 2009-2017 regime failure", and 4b is reachable on this universe. (3) **The other two 4b passes are a lever, not an edge.** `OFF/FIXEDW n=3` and `n=4` pass, but their Sharpes are identical to `OFF/EQW n=3`/`n=4` — they clear the drawdown cap only because 45%/60% gross scales a -25.8%/-23.8% drawdown under it. That is idea 20's "gross is a pure lever" result and the Sunday review should not read those rows as support for concentration. Only the n=20 pass is obtained at v1's own 75% gross. (4) **The whole ON (scaler) arm fails 4b's CAGR floor at every n**, best case 9.9% vs a 10.7% floor: the scaler caps the book's return however it is sized. (5) Honest limits on the KEEP, all three in the memo: the *direction* replicates on the 136-name broad universe (Sharpe 0.698 at n=2 -> 0.958 at n=20, MaxDD -40.6% -> -20.1%) but **the same n=20 rule fails 4b's H2 there by 0.02** (0.814 vs 0.837), and only n=40 passes — selecting it would be tuning; survivorship bites hardest exactly here, since a 20-name book holds over half of a 56-name current-constituent list (mean 37.5 eligible/day); and at matched 12% vol the gain over v1's n=5 is only ~1.9pp/yr on a U-shaped, non-monotone curve (n=4 gives 12.7% against n=20's 13.2%), so most of the 4b clearance is vol reduction fitting under the cap with a 1.9pp margin. Turnover falls monotonically with n and the candidate is the cheapest point in the grid at 9.6x/yr. Ideas 44-46 queued (broad-universe confirmation, cost/execution-lag sensitivity, fixed-n vs fixed-fraction).
- 2026-09-04 (lane B, idea 46 eligible-fraction-vs-n) — **KILL for the fixed-fraction rule as an improvement on fixed-n, plus one 4b KEEP-candidate that fixed-n does not produce.** The question is whether the book should pin the position COUNT (idea 2's top-20) or the FRACTION of eligible names, since the universe averages 37.5 eligible but swings from 3 to 55: on **11.0% of days fewer than 20 names are eligible**, and there the memo's n=20 rule holds *everything* and silently de-grosses to as little as 45% invested. Three arms, one tuned parameter each, 24 points all reported: **N** = top n at 0.75/n (idea 2's construction, cash when E_t<n), **NF** = same count cap renormalised to 75% gross (a decomposition arm so the N-vs-F comparison is not confounded), **F** = top ceil(f x E_t) at 0.75/ceil(f x E_t). Scorer fixed at the candidate's own (no `/sqrt(vol20)`); everything else v1's. Harness sanity: `N n=20` reproduces idea 2's KEEP row exactly and `N n=5` reproduces idea 1's `OFF EQW n=5` row. **(1) The idea's headline test fails.** At matched average book size the fraction rule beats the gross-matched count arm on Sharpe at 3 of 8 pairs (mean dSharpe **-0.002**) and on OOS Sharpe at 3 of 8 (mean -0.002). Adapting the count to breadth buys nothing on this universe and costs a parameter. **(2) The walk-forward agrees and is not close.** Rule 8 with two selection rules fixed in advance: arm N's picks (both rules) n=20, OOS 14.4%/1.170/-18.3%; arm F's 4b-aware rule picks f=0.85, OOS 12.4%/1.132/-16.7%; arm F's plain-Sharpe rule picks f=0.15 and **blows the drawdown cap out of sample** (-27.8%) — the fraction arm is the one that needs the guard rail. vs SPY 15.5%/0.884/-33.7% and RULES v1 7.8%/0.751/-13.8%. **(3) But f=0.85 is a 4b KEEP-candidate on portability, and it is the arm's own pre-registered pick, not a post-hoc point.** 11.3%/1.072/-16.7% with halves 1.092/1.058 on universe.json, and it passes 4b **again on the 136-name broad universe** (11.2%/1.024/-18.6%, halves 1.128/0.928) with the same parameter. Idea 2's n=20 fails 4b's H2 there by 0.02 and only n=40 passes — selecting it would be tuning. A count is a different rule on a differently-sized list; a fraction is the same rule. The trade is explicit: f=0.85 gives up 1.4pp of CAGR and 0.04 of OOS Sharpe to n=20 and its 4b CAGR margin is only ~0.6pp on both lists (vs n=20's 2.0pp on the primary). Memo with exact RULES wording: `2026-09-04_eligible-fraction-vs-n_B.memo.md`. **(4) Idea 28's 'equal-weight all eligible' (f=1.00), which missed 4b's CAGR floor by 0.23pp on universe.json, passes 4b outright on the broad list** (10.7%/1.026/-17.7%). **(5) A correction to the idea-2 memo's reading of its own wording:** the clause 'if fewer than 20 are eligible, hold all of them at 3.75% and leave the rest in cash' is not an arithmetic artefact — it is worth **+0.022 Sharpe at n=20 and +0.046 at n=30** against renormalising to 75%, one-directional at every n>=8. Keep it deliberately. **(6) The adaptive count is a real bear-market defence that full-sample Sharpe hides:** 2022 returns f=0.35 -0.8% and f=0.45 -1.5% vs n=20's -9.0% and SPY's -18.2% (E_t averaged 16 in 2022 against 37.5 overall); it pays for that in every other year, which is why it does not show up in the aggregate. 14 of 24 points pass 4b, **0 of 24 pass 4a** (every growth book fails v1's -13.8% drawdown). Survivorship caveat: both universes are current-constituent lists, so absolute CAGRs are optimistic — the N-vs-F comparison much less so, since both arms hold the same names. Ideas 47-49 queued (cost/lag on the fraction book, a breadth-conditional hybrid, and a third-universe replication on the small-cap panel).
- 2026-09-04 (lane B, idea 49 third-universe-portability) — **KILL for BOTH 4b KEEP-candidates on a third universe: 0 of 28 grid points pass either path.** The question was whether idea 2's `N n=20` (top 20 eligible, EW, 75% gross, no `/sqrt(vol20)`) and idea 46's `F f=0.85` (top 85% of eligible) are rules or artefacts of the list they were fitted on. Both were validated on `universe.json` (56 names) and `universe_broad.json` (136) — two overlapping current-constituent US large-cap lists in the same mega-cap regime, where broad is nearly a superset. Re-run unchanged, parameters fixed in advance, on the 439-name sub-$2B panel (483 less the 44 corrupted, 2011-01-13 -> 2026-09-03, trading-day index): **n=20 reads 6.7%/0.469/-27.4%, halves 0.613/0.352, OOS 7.4%/0.496** (vs 12.7%/1.093/-18.3% on universe.json) and **f=0.85 reads 4.3%/0.371/-40.9%, halves 0.466/0.302, OOS 3.6%/0.318** (vs 11.3%/1.072/-16.7%), against SPY 14.2%/0.863/-33.7% and RULES v1 6.3%/0.650/-13.8% on the same window. Every point fails all five 4b tests — not a near miss. **(1) The decisive control: the rule subtracts value from its own asset class.** Equal-weighting all 439 names with no filter and no ranking returns **10.2%/0.677/-36.2%** — higher CAGR *and* Sharpe than all 28 filtered books (consistent with idea 50's 100%-gross control, 14.3%/0.74). So this is not "small caps did badly": the panel's own beta earned 10.2% and the machinery turned it into 3.5-9.4%. **(2) The damage is the eligibility filter, and it is not costs.** `F f=1.00` *is* the control plus the 200d-MA + vol20<0.60 filter: 3.5%/0.329 vs 10.2%/0.677, i.e. the filter costs **6.6pp/yr of CAGR, of which only 1.2pp is turnover** (13.3x/yr vs 1.7x/yr at 10 bps) and **5.4pp is the filter's own timing at 0 bps** — whipsaw on noisy sub-$2B names. The momentum ranking on top is close to irrelevant (the whole f-sweep sits in a 3.5-6.2% band). This is the first evidence that the trend-eligibility filter underneath ideas 2, 25, 28 and 46 is regime- and universe-specific rather than general. **(3) The portability argument for f=0.85 does not survive either.** At matched book size the fraction arm beats the count arm on Sharpe at 6/8 pairs here (mean +0.017) against 3/8 (mean -0.002) on universe.json — the sign flips and both magnitudes are noise; fraction-vs-count was never the load-bearing part. **(4) N ≈ NF confirmed:** `E_t < 20` on 0.89% of days (E_t means 141 of 439), so the cash clause almost never fires and the arms differ by at most 0.014 Sharpe — direct support for idea 46's finding that the entire N-NF gap on universe.json was the cash sleeve, not the position count. **(5) Walk-forward (rule 8): the 4b-aware rule picks NOTHING in all three arms** — no in-sample point met the -11.2% in-sample DD cap (shallowest -19.6%); the plain-Sharpe rule picks `N n=15` (OOS 8.3%/0.524) and `F f=0.70` (OOS 4.0%/0.345), both below SPY's OOS 0.884 and below RULES v1's OOS 0.751. **(6) Costs:** turnover is 13-30x/yr here vs 9.6x for the candidate on universe.json; at 25 bps n=20 falls to 3.5%/0.288 and f=0.85 to 2.0%/0.215, and **at 50 bps both are negative**. A one-week execution lag changes nothing — there is no fast signal to decay. **Survivorship runs the wrong way for a KILL, which strengthens it:** all 483 panel names trade through 2026-09-03, so this list is *more* flattered than the large-cap ones and the books still fail every test. Benchmark caveat stated not adjusted: no IWM in the cache, so 4b compares a small-cap book to SPY; the control comparison in (1) does not depend on the benchmark and the rule loses that one too. **For the Sunday review: the 4b passes are a property of that universe, not of the rule.** Neither candidate should be adopted as a general rule; if n=20 is adopted at all it should carry an explicit large-cap universe clause with this failed replication on the record. Ideas 51-53 queued (filter-by-market-cap, slower small-cap trend, universe-composition robustness of the KEEP).
- 2026-09-04 (lane C, idea 39 small-cap-eligible-equal-weight) — **KILL, and an independent replication of lane B's idea 49: the two 4b KEEP-candidates fail their first out-of-universe test.** Written and run separately from idea 49; the two scripts agree to the decimal on the shared point (f=1.00 CAGR 3.5%), which is the strongest confirmation the project has for any result so far, and the diagnostics below are additive to it rather than a repeat. Question: ideas 2 (top-20 EQW @75% gross) and 46 (f=0.85) pass PROTOCOL 4b on universe.json and universe_broad.json, but those are two heavily-overlapping current-constituent lists of ETFs and large caps. Does the book they are built on — equal-weight everything above its 200d MA with vol20 < 0.60 — survive on the 439-name sub-$2B small-cap panel (483 less the 44 with `max_1d_move >= 1.0`)? Two tuned parameters (selection breadth in {F f=0.45/0.85/1.00, N n=20/40}, gross in {75%,100%}), all 10 points reported, weekly, 10 bps, t+1, sample 2011-01-13..2026-09-03. **(1) 0 of 10 points pass 4a or 4b and none is close.** Idea 39's own arm, `F f=1.00 @75%`, reads **3.5%/0.329/-40.2%, halves 0.433/0.250, OOS 0.280** against SPY 14.2%/0.863/-33.7% (halves 0.886/0.864) and RULES v1 on the same panel 8.1%/0.602/-32.8%. Every point fails all five 4b tests (H1, H2, OOS, MaxDD, CAGR). The two live candidates' own rules are among the *worst* points here: f=0.85 0.371/0.372, n=20 0.469/0.470. **(2) Rule-8 walk-forward is not close either** (params on 2011-2016 only, two selection rules fixed in advance): the plain-Sharpe rule picks f=0.85 g=1.00, OOS 4.4%/0.318/-51.2% vs SPY 15.5%/0.884/-33.7%; the 4b-aware rule picks **nothing** — no in-sample point met the in-sample drawdown or CAGR bar. **(3) The mechanism is that the eligibility gate is INVERTED on small caps, not merely weak.** Equal-weighting all 439 names with no filter gives **8.1%/0.640/-30.7% at 1.5x turnover** — better than every filtered arm on Sharpe, CAGR *and* drawdown. Decomposed at matched 75% gross: 200d gate only 6.4%/0.500/-38.2%, vol20 gate only 5.4%/0.441/-35.0%, both 3.6%/0.332/-40.0%; each gate subtracts and they compound, and a trend filter making drawdown 9pp *worse* is the tell. Costs are only part: the same book at 0 bps still reads 5.0%/0.432, so ~1.4pp of the 4.5pp CAGR gap is the 13.4x turnover and ~3.1pp is selection. The complement book (hold every name the filter rejects) returns 11.9%/0.731; **eligible minus complement is -8.41%/yr, t -3.77**. **(4) It is a cross-sectional failure, not an exposure one** — the f=1.00 book is 74.4% invested on average and spends exactly one day in cash (2020-03-19, when the eligible set hit 0 of 439). It bleeds in every V-shaped year, holding the names that never fell and missing the rebound cohort: 2020 +3.2% vs unfiltered +28.3% and SPY +18.3%, 2019 +2.8% vs +12.8%, 2023 +5.4% vs +12.8%, 2024 +2.4% vs +9.1%. **(5) Survivorship discipline on the complement result:** the panel is current constituents of a sub-$2B screen, so names that crashed and then delisted are absent, and that bias falls hardest on exactly the beaten-down high-vol names the complement holds. "Buy the rejects" is NOT a tradeable finding and is recorded as evidence on the gate's *sign* only. The KILL itself does not depend on it — the unfiltered control and the filtered arms hold overlapping survivor sets and the filtered arms still lose. **Consequence for the Sunday review:** two large-cap current-constituent lists that overlap heavily are one replication, not two. Ideas 2 and 46 should be promoted (if at all) as **large-cap rules with a stated universe condition**, not as general ones, and not sized on the assumption that the mechanism is universal. Memo: `2026-09-04_smallcap-eligible-equal-weight_C.memo.md`. Idea 51 as filed by lane B already covers the gate-sign-by-universe follow-up; ideas 54-55 queued (a delisting-aware small panel, and trend-gate lookback on the primary universe).
- 2026-09-04 (cloud, idea 38 small-cap-momentum-clean) — **KILL, 0 of 8 grid points pass 4a or 4b**, but the run **partially overturns ideas 39/49's reading of their own mechanism.** Question: those two runs killed the v1 *composite* book on the 439-name sub-$2B panel and concluded the momentum ranking was "close to irrelevant" there (every filtered arm below a 10.2%/0.677 unfiltered control). This run strips the scorer to **plain 12-1 momentum** — no 6m/3m blend, no `/sqrt(vol20)`, no vol20 gate — top-n equal-weight at 0.75/n, weekly, t+1, 10 bps, 2011-01-13..2026-09-03; two tuned parameters (n in {10,20,40,60}, 200d gate ON/OFF), all 8 points reported. Harness sanity: RULES v1 on the small panel and the EW-all control reproduce ideas 39/49 to the decimal (8.1%/0.602/-32.8% and 10.2%/0.677/-36.2%). **(1) The verdict is unambiguous.** Best point `no-gate/n=40` reads 14.6%/0.797/-33.9%, halves 0.803/0.816, OOS 0.818, against SPY 14.2%/0.863/-33.7% (halves 0.886/0.864): below SPY's Sharpe in both halves and out of sample, and its drawdown is 13.7pp outside 4b's -20.2% cap. Every one of the 8 points fails H1, H2, OOS and DD; n=60 with the gate also fails CAGR. No point passes 4a either (v1's -13.8% drawdown). **(2) But pure 12-1 momentum does rank on this panel, where the composite did not.** `no-gate/n=40` beats the panel's own unfiltered beta by **+4.4pp CAGR and +0.12 Sharpe, net of 11.5x/yr turnover at 10 bps**. Ideas 39/49's "the ranking is worth nothing" was a property of the v1 composite (rank-blend plus vol20 gate plus vol scaler), not of momentum. **(3) The gate damage localises to the vol20 filter, not the 200d MA.** Holding the ranking and n=40 fixed: none 14.6%/0.797, 200d only 11.4%/0.693, **vol20 only 7.4%/0.524**, both 5.6%/0.441 — the vol20<0.60 gate alone costs 0.27 of Sharpe against the 200d gate's 0.10, and they compound. Ideas 39/49 only ever measured the two together. **(4) The 200d gate's sign is not stable**: +0.053 Sharpe at n=10, +0.015 at n=20, -0.105 at n=40, -0.117 at n=60 — it helps a concentrated book and hurts a broad one, so neither idea 39's "inverted" nor RULES v1's "the gate is the edge" generalises here. **(5) The decisive counter-evidence against trading any of it, stated against our own finding (2):** compounded EW decile portfolios by 12-1 momentum (daily, 0 bps) are U-shaped, not monotone — D1 21.5%/0.865, D2-D9 flat in an 8.0-12.4% band, and **D10 (worst momentum) 29.2%/0.937, higher CAGR and Sharpe than D1**; the arithmetic D1-D10 spread is -8.11%/yr, t -1.15. D10 is precisely the beaten-down cohort the survivorship bias flatters most, so it is not tradeable — but with both tails beating the middle and only one of them survivorship-clean, finding (2) cannot be read as a demonstrated cross-sectional premium. **(6) Cost-fragile.** At 0 bps the best point reads 15.9%/0.856 — it merely ties SPY frictionless; 10 bps 0.797, 25 bps 0.709, 50 bps 0.561. An 11.5x-turnover sub-$2B book would realistically pay more than 10 bps. **(7) Rule-8 walk-forward** (IS 2011-01-13..2017-01-03, two selection rules fixed in advance): plain-Sharpe picks `no-gate/n=40`, OOS 16.3%/0.818/-33.9% vs SPY 15.5%/0.884/-33.7% and RULES v1 live 7.8%/0.751/-13.8% — beats SPY on CAGR, loses on Sharpe, beats the live book on both at 2.5x its drawdown; the OOS Sharpe *exceeded* the IS Sharpe (0.818 vs 0.785), so this is not an overfitting signature, the book is simply not better than SPY risk-adjusted. The **4b-aware rule picks NOTHING** — no in-sample point met the -11.2% in-sample drawdown cap (shallowest -20.1%) — the same structural outcome as ideas 39, 46 and 49. Survivorship (current constituents of a sub-$2B screen as of 2026-09-03) is one-directional and strengthens the KILL; benchmark caveat stated not adjusted (no IWM in the cache). **For the Sunday review:** nothing to adopt, and one live-book item — the `vol20 < 0.60` gate is the larger of RULES v1's two eligibility filters by damage here and has never been tested in isolation on universe.json, where it is assumed to be part of the edge. Queued as idea 56. Result memo: `research/backtests/2026-09-04_small-cap-momentum-clean_cloud.result.md`.
- 2026-09-04 (cloud, idea 55 trend-gate-lookback-primary) — **KILL for the 200d trend gate as a source of edge on the universe it was fitted to, plus a 4b KEEP-candidate that strictly dominates idea 2's.** The 200d-MA gate is the load-bearing assumption of RULES v1 and of the project's first KEEP-candidate; ideas 39 and 27 measured it at -1.7pp CAGR on small caps and -6.4pp on QQQ, but nobody had varied the LOOKBACK on `universe.json`, where it is supposed to be the edge. Idea 2's construction held fixed (top-n by the v1 composite without `/sqrt(vol20)`, EW at 0.75/n, 75% gross, weekly, t+1, 10 bps, cash when E_t<n), two tuned parameters — K in {200d,100d,50d,none} x n in {5,10,20,30} — all 16 points reported, `vol20<0.60` held at v1's value throughout. Harness check: **K=200d n=20 reproduces idea 2's KEEP row to the decimal** (12.7%/1.093/-18.3%, halves 1.088/1.103). **(1) K=none beats K=200d at every n on universe.json**, on CAGR and Sharpe both: n=20 reads 13.7%/1.123/-18.5% (halves 1.154/1.101) against the incumbent's 12.7%/1.093/-18.3% at the same 9.5x/yr turnover, and n=5/10/30 all show the same sign. 6 of 16 points pass 4b, 1 passes 4a. **(2) The gate's own contribution is statistically indistinguishable from zero.** Paired daily t-tests on the same names and days: dropping 200d is worth +0.35 to +1.47%/yr on universe.json (**t +1.42 to +1.76** — positive at every n, significant at none) and -0.22 to +0.19%/yr on universe_broad.json (t -0.50 to +0.68). So "the gate is the edge" is not supported on the primary universe; equally, "the gate destroys value here" is NOT established — the honest statement is that it does nothing measurable, and its sign is unstable between two heavily-overlapping large-cap lists, which is itself evidence it is noise rather than structure. **(3) Faster IS decisively worse, and that is the only significant effect in the lookback dimension.** The 50d gate costs 3.0-5.5%/yr on BOTH universes at every n (t +2.59 to +3.85), monotone in K. Whipsaw is real; it has simply stopped mattering by 200d. If a gate is kept at all, keep a 200d-class lookback. **(4) The cleanest number, consistent in sign on both universes: for the un-ranked book (equal-weight every eligible name, no ranking) the 200d gate costs ~1.8-2.2pp of CAGR and ~0.09 of Sharpe (t +1.47 primary, t +2.13 broad) and adds 6.4x/yr of turnover, to buy 2.5-3.1pp of drawdown.** That is drawdown insurance, not alpha, and the Sunday review should describe it that way in RULES. Pricing that trade properly is queued as idea 57. Calendar-year check at n=20 agrees: the gate helped only in 2020 (+1.6pp) and was a dead heat in 2022 (-9.0% vs -8.9% with SPY -18.2%). **(5) Rule-8 walk-forward: on universe.json BOTH pre-registered selection rules pick K=none n=20** (IS Sharpe 1.071, IS MaxDD -11.9%), OOS **14.9%/1.164/-18.5%** vs SPY 15.5%/0.884/-33.7% and RULES v1 7.8%/0.751/-13.8%, clearing every OOS 4b bar; OOS Sharpe exceeds IS Sharpe, so this is not an overfitting signature. On universe_broad.json both rules pick **K=200d n=30** instead (OOS 11.6%/0.904/-20.3%, misses the OOS 4b bars) and **0 of 16 broad points pass 4b**. The two universes pick opposite arms — the candidate inherits idea 2's universe fragility exactly and must not be promoted as a general rule. **(6) One diagnostic worth recording and not trading:** the K=none un-ranked control passes 4b outright on universe.json (12.2%/1.127/-18.4%, halves 1.141/1.116, OOS 1.183) at **1.9x/yr turnover** and misses on the broad list by 0.6pp of drawdown alone. It is also ~52 of 56 current constituents equal-weighted — close to an equal-weight index of a list chosen in 2026 — so survivorship is the leading explanation for its 1.13 Sharpe against SPY's 0.89. Recorded as a diagnostic, not a candidate. **For the Sunday review:** (a) stop describing the 200d filter as the source of the live book's edge; (b) if idea 2's candidate is adopted, adopt it WITHOUT the 200d gate — same construction, one fewer filter, +1.0pp CAGR, +0.030 Sharpe, -0.2pp drawdown, identical turnover, and it is the walk-forward pick under both rules (exact wording in `research/backtests/2026-09-04_trend-gate-lookback-primary_cloud.memo.md`); (c) either version still needs the explicit large-cap-universe clause ideas 39/49 called for. Ideas 56-57 queued. Result memo: `research/backtests/2026-09-04_trend-gate-lookback-primary_cloud.result.md`.

- 2026-09-04 (lane C, idea 8 lookback-blend) — **KILL for changing the ranking lookback.** 20 grid points (12-1 / 6-1 / 3-1 / blend-v1 / blend-skip x n in {5,10,20,30}) on universe.json and universe_broad.json, gate/gross/cadence/costs held at idea 2's KEEP construction; harness reproduces that row to the decimal. Nothing beats the incumbent blend at n=20/30 on either list (|t| < 2 in both directions), and **IS->OOS Sharpe rank correlation across the 20 points is +0.000 on universe.json**, so the horizon is not selectable in-sample on the list the live book trades. Two results to carry forward: the **3-1 leg is the weak one** (weekly rank IC t 2.07 primary / 1.40 broad vs 12-1's 4.29 and the blend's 4.74; -2.11%/yr t -3.17 at n=20) and **12-1 n=30 is the only point passing 4b on both universes** (10.9%/1.097/-15.8% and 13.0%/1.004/-20.2%) — PARK, not KEEP, because rule 8 never selects it and its broad drawdown sits exactly on the cap. Ranking beats an unranked equal-weight of the eligible set only in concentrated books (n=5 t +2.4..+3.3; n=30 t +0.1..+0.7). Rules unchanged. Memo: research/backtests/2026-09-04_lookback-blend_C.memo.md. Idea 68 (cost/lag on the 12-1 n=30 arm) queued.

- 2026-09-04 (lane B, idea 66 gross-exposure-is-the-error) — **KILL for "raise gross" as the fix for the standing 4b failure, and a correction to idea 63's diagnosis.** Idea 63 attributed the books' second-half shortfall to 75%-gross cash drag and repaired it with a 25% passive core; the queue asked whether simply being fully invested does the same thing, in which case the sleeve is a detour. 36 arms (3 pre-chosen books x gross in {0.75,0.80,0.85,0.90,0.95,1.00} x core fraction in {0,0.25}) x 6 cost levels x 2 universes, weekly, t+1, long-only, no leverage; two tuned parameters (gross, core fraction), all points reported; analytic cost identity asserted against a real `cost_bps=10` engine run. **(1) Gross is an exact lever, now verified rather than assumed:** against the same book at g=1.00 every arm shows **corr 1.0000, beta = g to three decimals, and dSharpe = 0.000**; the Sharpe slope d(Sharpe)/d(gross) is -0.001 to +0.008 across all three books and both lists. CAGR, MaxDD and turnover all scale linearly (universe.json `ew-band3`: 11.3%/-15.1%/4.9x at g=0.75 -> 15.1%/-19.9%/6.4x at g=1.00, Sharpe 1.136 at every point). **(2) Therefore gross cannot fix an H2 Sharpe bar, and the cash-drag story does not explain the failure it was invoked for.** The bar idea 63 targeted is the broad ranked book's H2 of 0.814 against SPY's 0.837; across the whole gross sweep it reads 0.814/0.814/0.814/0.815/0.815/0.815 while MaxDD deepens 6.1pp. Cash drag is a real CAGR cost and undoing it does raise CAGR — but at exactly the drawdown you would predict, so no risk-adjusted bar moves. **(3) The sleeve is NOT a detour — it is the only one of the two that touches a Sharpe bar,** because it changes the composition of the return stream rather than its scale: the same broad arm with the 25% SPY core reads H2 0.861-0.862 at *every* gross (+0.047). But the effect is not significant (paired daily t < 1 in every book) and it still fails 4b on drawdown there; and at matched total investment (g=1.00) the core is strictly **worse** on universe.json — 1.106/-21.7% against all-active's 1.136/-19.9%, paired +0.23%/yr t +0.44. **(4) Rule-8 walk-forward: all four picks FAIL out-of-sample on drawdown,** by 0.4-1.9pp (universe.json R1 `g=1.00 b=0.25` OOS 16.7%/1.174/-21.7%, R2 `g=0.95 b=0.25` OOS 15.8%/1.174/-20.6%; broad R1 `g=1.00 b=0` OOS 14.9%/1.073/-22.1%, R2 `g=0.95 b=0` OOS 14.2%/1.073/-21.1%; OOS cap -20.2%). The mechanism is worth carrying forward: **gross is not a selectable parameter.** No Sharpe-based rule can choose it (Sharpe is flat, so R1's pick is decided in the 4th decimal), and the drawdown-aware rule still overshoots because 2009-2016 contains no 2020 or 2022, making its -13.2% in-sample cap loose against the -20.2% the OOS period enforces. Gross is a risk-budget decision, and in-sample drawdown is not a safe way to set it. **(5) One PARK:** `ew-band3, g=0.90, core=0` clears all five 4b tests on **both** universes at **5, 10, 15, 20 and 25 bps** and clears the OOS 4b bars on both (13.5%/1.136/-18.0%, halves 1.113/1.160, OOS 1.234, 5.8x; broad 13.4%/1.064/-20.0%, halves 1.164/0.971, OOS 1.073, 6.2x) — a wider cost survival than idea 63's QQQ-core arm with one fewer moving part. PARK not KEEP: rule 8 does not and cannot select it, and g=0.90 is merely the largest gross whose linearly-scaled drawdown still fits under the broad list's cap, not evidence of an edge — its Sharpe is identical to g=0.75's. **For the Sunday review:** (a) stop treating gross as something a backtest can choose — state it in RULES as a drawdown budget with the number derived from the budget; (b) idea 63's core sleeve survives this test on its own terms but its H2 repair is t < 1 and costs drawdown, so it should not be adopted on the cash-drag argument as stated. Rules unchanged. Ideas 69-70 queued. Result memo: `research/backtests/2026-09-04_gross-exposure-is-the-error_B.result.md`.
- 2026-09-04 (lane C, idea 10 sector-only-universe) — **KILL for the ETF-only book, and the complement control is the finding.** 30 points (6 panels x [RULES v1 + EW-all-eligible + top-n EQW n in {5,10,20}]), all reported; harness reproduces idea 2's published KEEP row (12.7%/1.093/-18.3%, halves 1.088/1.103) and the live v1 row to the decimal. **(1) 0 of 10 ETF-only points pass 4a or 4b.** Best ETF arm `ETF36/CAND-n20` 6.8%/0.817/-15.2% (halves 0.754/0.878, OOS 8.1%/0.943) fails 4b on **H1 and CAGR** — the floor is 10.68% and it earns 6.8%; the literal queued arm `ETF24` (broad + sector ETFs, no bonds/commodities) 6.7%/0.779/-15.2% fails H1, H2 and CAGR. **(2) Half the hypothesis is right and it does not help.** Matched construction, same days, same gross, vs the full-list control: ETF36 has **lower vol in 10 of 10 pairs** and ETF24 in 6 of 10 — the idiosyncratic-risk claim is real on volatility — but MaxDD is shallower in only 4/10 and 2/10, and **Sharpe is worse in 20 of 20**, because the return loss dwarfs the risk saved: **-6.15%/yr (t to -5.31)** for ETF36 and **-5.02%/yr (t to -4.11)** for ETF24. Risk came down; return came down more. **(3) The complement carries the entire edge.** The same 56-name list stripped to its 20 mega-cap single stocks gives the run's only 4a pass — `STK20/CAND-n20` **12.1%/1.338/-12.1%, halves 1.341/1.344**, MaxDD shallower than v1's -13.8% — which also passes 4b; `STK20/CAND-n10` reads 19.0%/1.305/-17.3%; running v1's *own* construction on stocks only is worth **+11.6pp CAGR and +0.468 Sharpe (t +4.07)**. **Both rule-8 selections pick a STK20 arm and both clear every OOS 4b bar** (S1 -> n=10, OOS 21.2%/1.366/-17.3%; S2 4b-aware -> n=20, OOS 14.0%/1.449/-12.1%), with **Spearman(IS, OOS Sharpe) = +0.608** over the 18 ranked points — the project's best IS/OOS rank agreement (idea 8 measured +0.000 on this list). **(4) It is a PARK, not a KEEP, and the run's own control is why.** universe.json's megacap group is 20 tickers selected as 2026 constituents (NVDA, AVGO, PLTR, META, TSLA...) — the most survivorship-exposed panel in the repo. The stock-leg advantage is +5.19%/yr (t to +4.45) on those 20 names but only **+0.92%/yr (t max +2.04)** on universe_broad.json's 100 single stocks, which **fail 4b on drawdown at every n**. A 5.6x shrink when the stock leg widens from 20 curated names to 100 is selection, not a premium. Memo written recommending **rejection**, with the exact RULES wording it would require so the Sunday review rejects a written rule rather than a vibe; note that at n=20 on a 20-name panel the ranking is inert (mean 13.2 eligible, 49% gross) so the rule is EW-all-eligible with a gate. **(5) Universe invariance is not a substitute for return.** The 36 ETFs are bit-identical in universe.json and universe_broad.json (symmetric difference `[]`), so an ETF-only book passes idea 53's cross-universe composition test **by construction** — and still fails 4b outright. **(6) Two side results for the Sunday comparison:** `B136/EWall` — equal-weight every eligible name in the broad list, no ranking, no vol scaler, 8.3x turnover — **passes 4b** at 10.7%/1.027/-17.7% (halves 1.146/0.917, OOS 1.021), the simplest 4b-passing book the project has; and ranking helps *more* on the low-dispersion ETF panel (+0.186 Sharpe over EWall) than on the full list (+0.043), which does not rescue it. Survivorship: current constituents of both lists, one-directional — it runs **against** the ETF KILL (strengthening it) and **hard in favour** of STK20 (which is why that arm is parked). Ideas 71-73 queued (point-in-time mega-cap panel, `B136/EWall` head-to-head against the standing candidates, and cross-sectional dispersion as the mechanism).

- 2026-09-04 (lane B, idea 73 asset-class-dispersion) — **KILL for cross-sectional dispersion as a universe clause, and idea 10's ETF KILL is re-explained.** 35 points (7 panels x {EWall, RULES v1, CAND-5/10/20}) on a common 2011-01-13→2026-09-03 window so every panel is judged on identical days, 10 bps, weekly, t+1, 75% gross; two tuned parameters (panel, n), all points reported; the harness reproduces idea 2's KEEP row (12.7%/1.093/-18.3%, halves 1.088/1.103) and the live v1 row (6.5%/0.666/-13.8%) to the decimal before any new number is read. **(1) The premise is true as a fact about the panels.** Mean cross-sectional sd of 12-1 momentum over the eligible set orders exactly as idea 10 assumed: ETF24 0.129 < ETF36 0.145 < B136 0.257 < BSTK100 0.278 < U56 0.281 < STK20 0.378 < SMALL484 0.682, stable across halves — the ETF panels really do have 2.6x less to sort than the mega-cap stock panel and 4.7x less than small caps. **(2) The inference drawn from it is false.** The net ranking premium (CAND-n Sharpe minus the SAME panel's equal-weight-all-eligible book, same days, same gate, same gross) has Spearman **-0.107 / -0.071 / -0.071** with dispersion at n=5/10/20. The largest premium in the entire run is **ETF36/CAND20 at +0.228 Sharpe (t +2.41)** — on the second-*lowest*-dispersion panel — while the widest panel, SMALL484, has the worst net Sharpe of all 21 CAND points (0.493). Ranking works best where there is least to sort. **(3) What the cross-panel pattern is actually made of.** The gross selection spread (forward 1-week return of the top-n eligible minus the equal-weighted eligible set, no costs) does rise with dispersion (Spearman +0.429/+0.679/+0.357) but rises *more* with the number of eligible candidates (**+0.571/+0.857/+0.964**), and the two are confounded across panels because the widest panel is also the largest. n=7 panels; treated as suggestive, not evidence. **(4) The test with power is flat.** Within each panel, weeks classified into dispersion terciles by an EXPANDING quantile of that panel's own history (no look-ahead): high-dispersion weeks beat low-dispersion weeks in only **2 / 2 / 3 of 7 panels**. Pooled with panel fixed effects over **4,877 panel-weeks**, corr(dispersion percentile, weekly selection spread) = **-0.006 / -0.015 / +0.001 (|t| ≤ 1.01)**. Holding the panel, its names and its survivorship exposure fixed, dispersion predicts nothing. **(5) Rule 8 rejects it outright.** The pre-registered dispersion rule S3 (highest 2009-16 mean dispersion at n=20) picks **SMALL484 → OOS 7.6%/0.506/-26.3%, failing every OOS 4b bar** and finishing last of the seven panels; S1 (highest IS Sharpe) picks STK20/CAND10 → **21.2%/1.366/-17.3% PASS** and S2 (4b-aware) picks STK20/CAND20 → **14.0%/1.449/-12.1% PASS**. Spearman(IS dispersion, OOS Sharpe) = **+0.134** over the 21 points (+0.143 over the 7 panels at n=20) against Spearman(IS Sharpe, OOS Sharpe) = **+0.710**. The panel choice *is* selectable out of sample; dispersion is simply not what selects it — and used as a rule it picks the worst panel available. **(6) Idea 10's ETF KILL survives with a different mechanism.** The ETF book fails on the LEVEL of return, not on the ranking: ETF36/CAND20 earns 6.6% CAGR against this window's 9.91% 4b floor while carrying the best ranking premium in the run. 'Too few things to sort' is not why ETFs failed; 'not enough expected return in the panel' is. **(7) Nothing is upgraded.** The only 4b passes are already-known books — U56/EWall, U56/CAND10, U56/CAND20 (idea 2's candidate) and all four STK20 books (idea 10's PARK, which idea 71 attributes to selection). No new KEEP-candidate. **Survivorship** is one-directional on all three lists and falls hardest on STK20/BSTK100/SMALL484, the high-dispersion panels, so the run is biased *toward* the hypothesis it rejects. **For the Sunday review: recommended RULES wording is none** — do not add a dispersion clause. If a universe clause is wanted, the supported form is about return level and candidate count, not spread: *'The ranked book may be run on any panel of at least 20 eligible names; a panel is disqualified by its own realised CAGR against the 4b floor, not by its cross-sectional dispersion.'* Rules unchanged. Ideas 77-78 queued. Memo: `research/backtests/2026-09-04_asset-class-dispersion_B.result.md`.

- 2026-09-04 (cloud, lane C) — **Idea 13 `52w-high-proximity`: KILL.** 28 points (2 universes × [live-v1 + EW-all-eligible control + 3 signals × n∈{5,10,20,30}]), all reported; harness reproduces idea 2's KEEP row (12.7%/1.093/-18.3%, halves 1.088/1.103) and idea 10's `B136/EWall` (10.7%/1.027/-17.7%) to the decimal. **(1) The literal idea loses everywhere.** Replacing RULES v1's return composite with George & Hwang's nearness-to-52-week-high `P_t / max(P_{t-251..t})`, holding names/days/gate/gross fixed, is worse in **8 of 8 matched-n pairs**: dSharpe **-0.045..-0.754**, paired daily **t -3.07..-4.16**, every sign negative, mean **-7.2%/yr** on universe.json and **-9.9%/yr** on broad; the pre-registered 50/50 BLEND arm loses in 7 of 8. **0 of 16 PROX/BLEND points pass 4b anywhere; 0 of 28 points pass 4b on both lists.** **(2) The signal is negatively informative inside the eligibility gate.** Weekly rank IC vs the following week's return over 905/914 weeks: PROX **-0.0208 (t -2.13)** on universe.json and **-0.0252 (t -3.34)** on broad, negative in BOTH halves of BOTH lists, against the incumbent composite's +0.0427 (t +4.16) and +0.0242 (t +3.22). The mechanism is that the 200d gate has already spent the signal — mean PROX of *eligible* names is 0.953 vs 0.902 across all names — so what is left is a level statistic only +0.192/+0.324 Spearman-correlated with the composite, and the part that differs is the part that loses. It is **de-risking, not signal**: vol falls in 8/8 pairs and MaxDD is shallower in 6/8 while CAGR falls further every time — idea 10's ETF shape exactly (risk fell, return fell more). **(3) It loses to no ranking at all**: against EW-all-eligible, dSharpe -0.900..+0.004 with t -4.10..-1.98 at every n on both lists, while the incumbent composite beats that same control on return in 8/8. **(4) The project's own whipsaw prior pointed at this idea and the measurement points the other way — the run's most transferable finding.** A *level* key bounded near 1.0 is FASTER, not slower: at matched n on universe.json turnover is 40.2/31.1/14.8/7.8×/yr for PROX vs **17.6/13.9/9.6/7.0×** for the composite, and flips per ticker per year 4.8/7.4/7.0/5.4 vs 2.1/3.2/4.4/4.7, because eligible names cluster within a few percent of their highs and tiny moves reshuffle the top of the book. "Slower signal" is a property to be measured, not inferred from the signal's form. **(5) Rule 8 rejects it unanimously and with power.** Spearman(IS Sharpe, OOS Sharpe) over the 12 CAND points is **+0.888** (universe.json) and **+0.930** (broad) — against idea 8's +0.000, this is a choice the walk-forward *can* make — and it picks the incumbent every time: S1 and S2 both select COMP-n20 (OOS 14.4%/1.170/-18.3%, clears every OOS 4b bar) and COMP-n30 (OOS 11.6%/0.904/-20.3%, misses OOS DD by 0.1pp), with the composite winning 7 of 8 within-n signal audits. **(6) Nothing is upgraded.** The run's three 4b passes are reproductions of ideas 2, 8 and 10's published rows; 0 of 14 points pass 4a on universe.json (the six 4a passes are all on broad, where RULES v1 itself draws down -21.2% and 4a is not a bar). **Survivorship** is one-directional and flatters PROX — names that spent years far below a 52-week high and then delisted are absent, which favours a signal that avoids them — and it loses anyway, so the KILL is conservative. **For the Sunday review: recommended RULES wording is none.** Rules unchanged. Ideas 79-80 queued. Memo: `research/backtests/2026-09-04_52w-high-proximity_C.result.md`.
- 2026-09-04 — Research lane B, idea 83 `turnover-budget-instead-of-a-cost-assumption`: **KILL**. 176 points, all reported. A per-rebalance turnover budget budgets the EXITS, so it makes MaxDD WORSE (mean -2.4pp pro-rata / -5.1pp largest-trades-first) — the opposite side of the trade from the gross lever, which buys drawdown at 0.677 pp CAGR per pp MaxDD (range 0.626-0.729 over 16 arms, dSharpe -0.001, replicating ideas 66/74). Read in one direction: raising gross buys return ~3x more efficiently (0.68 vs ~0.22 pp/pp) and Sharpe-neutrally, so the budget is dominated on its own axis. 0 of 48 broad budget points pass 4b at 10 bps; no arm passes 4b on both universes. Rule 8: B is not selectable (Spearman IS/OOS +0.14/-0.07 on universe.json; the IS pick loses to its own control OOS in 2 of 4 cases). Two findings carried to QUEUE: (a) which 4b bar BINDS decides which lever can help — gross is strictly counterproductive on a CAGR-bound book (32/32 gross arms fail on CAGR) and the budget is the only lever that helps there, lifting `EWall`'s u56 breakeven 7.0 -> 20.0 bps (does not replicate on broad); (b) truncating a trade list is not self-financing, so the QUEUE's literal `largest-changes-first` budget is partly a disguised gross lever (realised gross 0.815 vs control 0.717), the mirror of idea 73's de-grossing artefact. Rules unchanged.

- 2026-09-04 (lane C, idea 15 crypto-sleeve) — **PARK: the largest and most consistent improvement the repo has produced, rejected by rule 8 four times out of four.** `baseline.load_universe` has excluded BTC-USD/ETH-USD from every backtest the project has run, yet both are cached and both are trend instruments of the kind the 200d gate is built for. 78 points (2 universes x 3 books [live v1 / idea 2's CAND20 / EWall] x [control + 2 funding modes x 2 gates x 3 caps]), all reported; harness reproduces idea 2's KEEP row (12.7%/1.093/-18.3%) and the live v1 row to the decimal and c=0 equivalence against the crypto-free panel is 1.4e-17. Two tuned params only: cap c in {0,5,10,15}% per name and gate in {`same` = v1's own 200d AND vol20<0.60 applied to crypto, `trend` = vol cap waived}; funding (`matched` = sleeve funded by scaling the equity leg down, gross unchanged; `add` = gross rises) is a reported structural variant, gross-matched pre-registered as primary because ideas 66/73/81 showed an unmatched gross change is an exact return lever. **(1) The effect is real and one-directional: 36 of 36 sleeve arms beat their own crypto-free control on full-sample Sharpe on BOTH lists** (paired daily t +1.71..+2.81 matched, +2.70..+3.21 additive), and **5 of 39 points pass cross-universe 4b at 10 bps** — `CAND20/matched/same/c5`, `EWall/matched/same/c5`, `EWall/matched/same/c10`, `EWall/matched/trend/c5`, `EWall/add/same/c5`. Both controls fail that test (CAND20 on broad H2 0.814 vs 0.837, EWall on u56 CAGR 10.4% vs the 10.68% floor — the two near-misses ideas 28/44 have been circling) and **the sleeve repairs exactly the bar each was missing**. The literal queued arm on universe.json, `CAND20/matched/same/c10`, reads 14.3%/1.178/-19.1% (halves 1.254/1.127, OOS 1.237) against 12.7%/1.093/-18.3%, at 10.3x turnover, and its advantage WIDENS with cost (dSharpe +0.154 at 10 bps -> +0.276 at 100) because the sleeve adds return without adding turnover. **(2) Why it is a PARK: the passing arms are unreachable.** In-sample Sharpe is monotone in the cap and higher for the loose gate in nearly every cell, so both pre-registered selection rules buy c=15/`trend` in 4 of 4 walk-forwards and **every one fails the OOS drawdown bar** (rule 8's window: u56 28.4%/1.315/**-31.1%** and broad 24.3%/1.226/**-30.4%** against a -20.2% cap; a crypto-era window IS<=2021: -19.4% and -24.1% against -14.7%); within-book audits pick c=15 in **11 of 12** cells and **not one** of the 5 cross-universe passers is ever selected. Spearman(IS,OOS Sharpe) is +0.771/+0.855 on rule 8's window but **+0.362/+0.252** on the crypto era — the stable ordering is the equity book's, not the sleeve's. **This is idea 66's failure shape exactly** (`ew-band3 g=0.90` also passes cross-universe 4b and is also unselectable), and two instances make it a pattern: on this repo a 4b pass sitting at an INTERIOR parameter value has twice been unreachable by any rule maximising in-sample Sharpe. **(3) Rule 8 cannot validate this idea on the data that exists.** BTC is tradeable from 2015-07-02 and ETH from 2018-08-27, so rule 8's in-sample window holds ~1.5 yr of BTC and ZERO ETH; the crypto-era walk-forward was added for that reason and rejects it too on 3.3 yr of in-sample. **(4) The t-stat is one calendar year.** 2017 (BTC +1425%, ETH +135%) sits inside H1 — which is why sleeve H1 Sharpes look spectacular (1.088 -> 1.408) even though only 13.1% (`same`) / 24.0% (`trend`) of H1 days hold any crypto at all. Restricted to 2018-09+ the effect shrinks by a third and loses significance (t_cry +0.66..+2.06; dSharpe negative in 4 of 18 gross-matched arms on universe.json), and 2018 is the counter-year the full sample hides: BTC -74%, ETH -82%, `CAND20/matched/trend/c10` **-0.4% against the control's +5.9%**. **(5) Survivorship is worse here than anywhere in the repo:** BTC and ETH are the two survivors of the crypto field and LUNA/FTT/the 2014-18 altcoin cohort went to zero rather than to an acquirer, so every figure is an upper bound on a two-ticker sleeve chosen with ten years of hindsight. Secondary: crypto trades 24/7 but the index is equity trading days, so weekend moves land in the Monday bar and a Friday rebalance cannot react — this understates realised drawdown; and 10 bps is an equity cost assumption. **(6) Transferable finding for RULES: the vol20<0.60 cap is a drawdown instrument whose value is conditional on the instrument's own vol distribution.** BTC's median vol20 is 0.532 and ETH's 0.714, so the cap is near-binding by construction: it cuts BTC's eligible days 43.3% -> 24.9% and ETH's 24.2% -> 7.8%. `trend` earns more standalone (BTC gated-only 38.8%/0.966 vs `same`'s 21.0%/0.855) but **every 4b failure among the sleeve arms is `DD`**, and `same` vs `trend` is the only lever in the run that changes which side of the cap a book lands on (mean dMaxDD -2.9% vs -7.0%). That is the mirror image of idea 56's small-cap result, where the same cap destroys value. **Recommendation to the Sunday review: do not adopt.** The exact RULES wording that would be required is recorded in the memo and recommended against; revisit only when a delisting-aware crypto panel exists (cf. idea 54) and ETH has >=8 further years. Memo: `research/backtests/2026-09-04_crypto-sleeve_C.result.md`. Ideas 87-89 queued (margin-aware selection for interior 4b passes, the vol cap as an explicit satellite clause, and a leave-one-year-out audit of every standing candidate).

- 2026-09-04 — Research cloud (idea 14, rsi2-sleeve): **KILL**, decisively. A Connors RSI(2)<thr-in-uptrend mean-reversion sleeve on the 16 sector/industry ETFs, funded by scaling the trend book down, was tested at f in {0.25, 0.50} and thr in {5, 10, 20} against three pre-chosen cores (live v1, idea 2's top20 KEEP, ew-all) on both large-cap lists at 5/10/25/50 bps — 42 leaderboard points, every grid cell printed. **The sleeve loses in 36 of 36 (universe, core, f, thr) cells at 10 bps** (mean dSharpe -0.412, median -0.371, paired daily t between -2.5 and -9.5 vs each arm's own control), and rule 8 selects f=0 (no sleeve) in 5 of 6 walk-forwards; the single arm a selection rule does buy (broad/top20 under the IS-drawdown-capped rule) fails OOS on Sharpe and CAGR. **The mechanism is turnover, not signal.** The signal is real and correctly signed — entry-day forward returns 0.209%/0.087%/0.049% at thr=5/10/20 vs 0.063% unconditional over 1026/2088/3830 entries, gross sleeve Sharpe 0.461/0.550/0.747 — but the sleeve holds 2.2-3.6 names for a mean 2.8 days and turns over **65x/113x/165x per year**, i.e. 6.5/11.3/16.5 pp of annual cost at PROTOCOL's 10 bps, which converts every positive gross book into a negative net one (-1.6%/-4.2%/-5.3% CAGR standalone). This is idea 76's holding-period finding at its extreme: the fastest instrument the project has tested is also the only one whose entire gross edge is consumed by its own trading. Two controls rule out the obvious alternatives: adding a one-day execution lag changes the standalone Sharpe by only -0.08/+0.04/-0.05 (the h=0..4 forward-return profile is flat, so there is no one-day pop being forfeited), and buying one day EARLY loses 45-72% CAGR, confirming the signal is a genuine dip-buy rather than an artefact. The only variant that comes close to breaking even is `div4` (never more than 25% of the sleeve in one name), which halves turnover to 28x and lifts standalone Sharpe to +0.26 at thr=5 — still dSharpe -0.003 to -0.057 on the blended books, i.e. at best free. **Transferable finding for RULES: a sleeve's admissible turnover is bounded by its gross Sharpe, not by its correlation with the core.** The sleeve's correlation with the trend books is only 0.37-0.49, so the diversification premise held; it simply cannot pay for itself. No RULES change proposed. Script: research/backtests/2026-09-04_rsi2-sleeve_cloud.py.

- 2026-09-04 — Research cloud (idea 89, one-year-leverage-audit): **infrastructure delivered, and it changes how two standing candidates should be read.** A generic leave-one-year-out harness (delete a calendar year, chain-link the remainder, recompute every statistic AND every 4b bar on the retained days so the benchmark moves with the sample) was run over 13 (book, universe) cells x 18 years at 10 bps, with zero tuned parameters; it reproduces idea 2's KEEP (12.7%/1.093/-18.3%) and idea 72's `B136/EWall` (10.7%/1.027/-17.7%) to the decimal. **(1) Only 2 of the 7 published 4b passes survive deletion of every single year:** idea 2's `top20` and idea 46's `frac085`, both 18/18 on universe.json. `ew-band3` (idea 57) is 17/18 on both lists, `frac085` 17/18 on broad, and idea 72's `B136/EWall` **10/18** — the weakest standing candidate by a wide margin. **(2) The far more important finding is that 12 of the 14 verdict flips are the BAR moving, not the book weakening.** Deleting 2020 raises SPY's 60%-of-MaxDD cap by 5.53pp (the COVID crash is most of SPY's -33.7%) and deleting 2022 raises the 70%-of-CAGR floor by 1.68pp, while the candidate books barely move — so PROTOCOL 4b's two absolute bars are themselves one-year-levered, and a LOYO 'failure' on DD or CAGR is usually a statement about SPY, not about the candidate. **(3) No equity candidate is unusually year-dependent:** worst-drop Sharpe deltas are SPY -0.059, v1 -0.065, `ew-band3` -0.071, `frac085` -0.074, `top20` -0.077 on u56 (1.10-1.30x the benchmark's own; 1.22-1.56x on broad). Nothing here resembles idea 15's shape. **(4) Every candidate's single best year is 2022** (excess over SPY +7.3 to +14.0pp) except `top20`/u56 (2018, +10.5pp): the 4b edge over SPY is, in the main, one bear market avoided, and that is the exposure to price — not per-parameter fragility. **(5) Harness validation:** applied to idea 15's PARKed crypto arm as a PAIRED DIFFERENCE (`EWall+c10` minus its own control), t is +2.04 full-sample and falls below 2.0 when any of 2016, 2017, 2020 or 2024 is deleted (worst 2020, t +1.48) — the harness flags it, and finds the dependence broader than the single year idea 15 named. LOYO on the same book's own Sharpe does NOT flag it, so **the audit must be run on the statistic the idea is argued on**, which is the one methodological rule this run would add. **(6) Rule 8 says LOYO-robustness is a disqualifier, not a selector:** on the broad list max-IS-Sharpe picks `ew-band3` (OOS 11.2%/1.074/-16.8%, 4b PASS) while max-IS-worst-case-LOYO picks `top20` (OOS 12.5%/0.894/-20.1%) — robustness-aware selection did strictly worse out of sample, so it must not be used to choose books or parameters. Proposed PROTOCOL rule 10 with exact wording is in the memo, recommended as a PARK-trigger only; PROTOCOL.md not modified (Sunday review decides). Caveat carried into the memo: a spliced equity curve's MaxDD is not a realisable path, so DD-based LOYO verdicts are optimistic. Memo: research/backtests/2026-09-04_one-year-leverage-audit_cloud.result.md. Script: research/backtests/2026-09-04_one-year-leverage-audit_cloud.py.

- 2026-09-04 — Research cloud (idea 16, monthly-seasonality): **KILL**, and the reason generalises. 144 grid points (2 universes x 3 books [live v1 / idea 2's `top20` 4b KEEP / `EWall`] x 6 month-masks x f in {0.00,0.25,0.50,0.75}), all printed, plus 6 controls; 2 tuned params (mask, f); harness reproduces idea 2's KEEP row (12.7%/1.093/-18.3%, halves 1.088/1.103) and the live v1 row (6.5%/0.666/-13.8%) to the decimal. **(1) The conditioning variable is not persistent.** Spearman between each series' H1 mean-daily-return-by-month ranking and its H2 ranking is **+0.000 / -0.182 / -0.364 / -0.021** on universe.json and **+0.000 / -0.049 / -0.322 / +0.028** on broad — 6 of 8 negative, max +0.028. Corroboration: September is the worst full-sample month in all four series yet appears in **0 of 6** in-sample-fitted `worst1` masks (in-sample worst-1 is Jan or Aug everywhere) and 1 of 24 `worst-k` masks overall, i.e. it is hindsight, not signal. Month t-stats agree: SPY's Sep is t -0.22 and the only |t|>2 months are the positive ones (Jul +3.54, Nov +3.05). **(2) The literal idea loses.** Only 24 of 144 arms beat their own control on full-sample Sharpe (mean dSharpe -0.080; best anywhere +0.063 on the weakest book, +0.003 on the standing KEEP), monotone in f — the more exposure the mask removes the worse it gets — and the removed exposure is bought back more cheaply by the plain gross lever (ideas 66/83). The KEEP candidate is the book it hurts most (mean -0.113). **(3) Rule 8 is anti-predictive 6 of 6.** The IS-Sharpe selector picks `worst6` (the freest mask) in every single case and loses **-0.292 to -0.570** of OOS Sharpe against its own control. **(4) The two cross-universe 4b passes are not results:** `top20`/`sep` at f=0.00/0.25 clear 4b only by lifting broad H2 from 0.814 past SPY's 0.837 while *lowering* full-sample Sharpe, on the one mask rule 8 never selects. **Carry-forward for PROTOCOL:** report the cross-half rank correlation of the conditioning variable BEFORE building any book — it is one line, and where it is ~0 no grid can rescue the idea. Applies to every queued regime/breadth/VIX/day-of-week conditioning idea. Rules unchanged; standing candidates unaffected.

- 2026-09-04 — Research cloud (idea 87, interior-4b-passes-are-unselectable): **KILL for the proposed rule-8 amendment; the two ideas it was meant to rescue were parked correctly.** 66 grid points across six 1-parameter risk grids re-run from scratch (GROSS/top20, GROSS/EWall, BAND/ew-all, N/ranked, CRYPTO/CAND20, CRYPTO/EWall) x 2 universes, 48 selections, all printed; 2 tuned params (grid parameter, margin m); harness reproduces idea 2's KEEP row (12.7%/1.093/-18.3%, halves 1.088/1.103), idea 72's `B136/EWall` (10.7%/1.027/-17.7%) and idea 57's broad `ew-band3` (11.1%/1.064/-16.8%) to the decimal. **(1) Rm(m) = argmax IS Sharpe s.t. the IS 4b drawdown cap met with margin m does not beat rule 8 on anything that matters:** OOS Sharpe vs the default arm +0.027/+0.029/+0.033 at m=0/1/2pp against R0's +0.031, it reaches the grid's OOS-best in 0 of 10 / 0 of 9 / 0 of 4 cells against R0's 2 of 12, and **13 of 48 selections are infeasible** (8 of 12 at m=2pp), so the amended rule is not even well-defined without a fallback. Its one incremental recovery, `GROSS/EWall` g=0.80 on broad, differs from R0's pick by **+0.0009** OOS Sharpe — 4b compliance, not performance. **(2) The premise's fatal half: the grids that produced the interior passes have no selectable content.** A 2x change in gross moves IS Sharpe by **0.0002-0.0046** and OOS Sharpe by 0.0001-0.0038 while moving IS MaxDD by 7-9pp (R0's pick there is fourth-decimal noise, landing on g=1.00 in 3 of 4 cells), and the crypto cap grid's IS MaxDD spread is **exactly 0.0000** on both universes because the in-sample max-drawdown episode predates the sleeve's tradeable window — a constraint blind to the parameter it constrains. **Rm recovers idea 15's c=0.05 in 0 of 8 (rule, universe) cells**, picking c=0.15 whenever feasible, which fails 4b on DD both full-sample and OOS on both lists. **(3) The margin is a second tuned parameter wearing a safety label:** `GROSS/top20` broad picks 1.00 -> 0.70 -> 0.60 -> 0.60 across R0/m=0/1/2 and four of six grids give a different pick per universe under at least one rule. **(4) Confirmed without any amendment:** 8 arms pass full-sample 4b on both universes, and idea 57's band book is the most robust object in the run (all four non-zero bands pass cross-universe; every feasible rule picks band=0.08), while idea 2's n=20 is picked on u56 and n=30 on broad, independently reproducing ideas 44/53's universe-specificity warning. **Proposed for the Sunday review — PROTOCOL 8a, a pre-test rather than a new selection rule:** report each grid's IS objective spread and the spread in each binding 4b bar; where IS Sharpe spread across the whole grid is under 0.02, or a binding constraint's spread is 0.00, the parameter is a policy dial, not an estimate — rule 8 does not apply and it must be set by a stated risk budget (idea 69). Rules unchanged; PROTOCOL rule 8 unchanged pending review.
- 2026-09-04 (lane C) — Idea 89 is-window-has-no-crash: **KILL**, rule 8's split stays at 2016. Moving it changes 8/12 (S13) and 5/12 (S21) published selections but <=0.034 realised 2022+ Sharpe on the selectable cells. Idea 87's premise refuted: S13/S16 share the same IS MaxDD (-22.1%) yet picks move, and a crash in the IS window makes 4b's DD cap LOOSER (it is relative to SPY on the same window). The binding in-sample 4b constraint is the CAGR floor, not the drawdown cap. Also: rule-8 selection is degenerate on grids whose parameter has no data in the IS window (crypto under S13). No RULES change.

- 2026-09-04 (cloud, idea 88 selectability-pre-test-across-the-leaderboard) — **KILL for PROTOCOL 8a as idea 87 worded it; a narrower replacement is recommended.** 11 one-parameter grids (N, fraction, gross x2, band, lookback, cost x2, cadence x2, hysteresis) x 2 universes = 22 cells / 122 arms, all reported; 2 tuned params (grid parameter, selection rule R0/R2); weekly, t+1, 10 bps except in the cost grids. Harness reproduces idea 2's KEEP row (12.7%/1.093/-18.3%, halves 1.088/1.103) and idea 10's `B136/EWall` (10.7%/1.027/-17.7%) exactly, and the cost-free simulator matches `engine.backtest` to 0.00e+00. **(1) The retrospective the queue asked for comes back empty.** 8a fires on 4 of 22 cells and all four are the gross grids idea 87 proposed it from; every published n / fraction / band / lookback / cost / cadence / hysteresis sweep passes with IS Sharpe spread 0.040-0.502 against the 0.02 floor (nearest: `HYST/top20` u56 at 0.0398, where the pick is also the grid's OOS-best). **No walk-forward conclusion on the leaderboard outside idea 66's rested on a grid with no selectable content**, and the classification is unchanged for any threshold in 0.005-0.02. **(2) The clause's stated rationale is refuted, with the sign reversed.** 8a is worded as a safeguard on selection; mean REGRET (pick's OOS Sharpe minus the grid's OOS-best) is **-0.001 on flagged cells against -0.061 on unflagged**, Spearman(IS Sharpe spread, REGRET) over the 22 cells is **+0.256** where 8a predicts negative, and **the four worst selections in the run all PASS 8a**: broad `F/fraction` -0.310, u56 `F/fraction` -0.232, u56 `CADENCE/EWall` -0.182, broad `CADENCE/EWall` -0.105. The fraction cells reproduce idea 46 exactly — rule 8 picks f=0.15 and takes OOS 16.8%/0.941/**-27.8%** (u56) and 11.0%/0.715/**-29.9%** (broad) while the grid's OOS-best reads 1.173 and 1.026, and the 4b-aware rule R2 is *infeasible* on u56. This is structural, not bad luck: a flagged grid's arms differ in OOS Sharpe by 0.001-0.004, so the pre-test can only ever flag grids where the choice is costless on the objective. Mean SKILL (pick minus grid mean) is +0.039 (t +1.49) on PASS cells and +0.000 (t +0.37) on FAIL cells — the separation exists but it is the flagged grids being *inert*, not the unflagged ones being safe. **(3) What the flag does track is risk.** Flagged cells' OOS MaxDD spread is **11.3%** against 4.8% unflagged: u56 `GROSS/top20` has an IS Sharpe spread of **0.0002** (identical to 3 dp at every g) while OOS MaxDD runs -12.4% to -24.0%, and the fourth-decimal pick lands on g=0.80 on u56 and g=1.00 on broad. That is a genuine defect worth a clause — a risk-disclosure one. **Recommended wording for the Sunday review: `8a. Risk-consequence disclosure` — report every walk-forward grid's IS objective spread AND its OOS MaxDD spread; where the IS objective spread is under 0.02 Sharpe while the parameter moves MaxDD by more than 5pp, the parameter is a policy dial: record that rule 8's pick is not an estimate and set it from a stated drawdown budget (idea 69). Explicitly reject the draft's second half** ("only parameters that pass the pre-test are walk-forward selected") — on this evidence it would license exactly the selections that go wrong and forbid only the ones that cost nothing, and passing 8a must not be reported as evidence that rule 8 selected well. **(4) By-products, all reproductions:** `N/ranked` u56 picks n=20 under both rules (OOS 14.4%/1.170/-18.3%, idea 2 confirmed) and n=30 on broad (4b fail on DD — ideas 44/53 again); `CADENCE/top20` picks monthly on both lists with zero regret and monthly fails 4b on broad drawdown at -26.1% (idea 64's trade-off, walked into by the selection rule); `HYST/top20` u56 picks k=2.00, 12.7%/1.145/-17.2% at 3.9x/yr turnover, 4b PASS full and OOS (idea 86 confirmed). Both KEEP paths on all 122 arms: 4b full-sample 32/61 (u56) and 16/61 (broad), 4b OOS 43/61 and 19/61; **4a 3/61 on u56 and all three are g<=0.60 arms** that clear v1's -13.8% drawdown by de-grossing and then fail 4b's CAGR floor (idea 20/66's lever), 46/61 on broad where v1 itself draws down -21.2%. No new candidate. Survivorship: both lists are current constituents, so absolute CAGRs are optimistic; the run compares selection rules on common grids, so the bias falls on every arm alike. Rules unchanged; PROTOCOL rule 8 unchanged. Result memo: `research/backtests/2026-09-04_selectability-pre-test_cloud.result.md`.

- 2026-09-04 (cloud, idea 85 exit-schedule-vs-entry-schedule) — **Idea 83's mechanism claim is HALF REFUTED, and the remedy it implies works exactly where the claim is true and nowhere else: PARK on the unranked book, KILL on the ranked one.** 44 arms plus a 16-point gross ladder across 2 books x 2 universes, all reported; 2 tuned parameters (budget B in {inf,0.30,0.20,0.15,0.10,0.05}, mode in {entry-only, idea 83's pro-rata total}); weekly, t+1, 10 bps with 25 bps also reported; harness reproduces idea 2's `CAND20` (12.7%/1.093/-18.3%, halves 1.088/1.103) and idea 10/72's `B136/EWall` (10.7%/1.027/-17.7%, halves 1.146/0.917) exactly. **(1) The decomposition.** The entry and exit legs are 50.0-50.1% and 49.9-50.0% of Sigma|dw| on all four books — a self-financing long-only book that resets to a fixed gross cannot be otherwise, so "which leg is bigger" was never the question. The informative split is what the exit leg is made of: **gate exits are 24.8% (u56 CAND20) and 11.1% (broad CAND20) against rank displacement of 67.8% and 82.7%**, versus **56.6%/57.0% gate on EWall** (rank 0%, drift trims 43%). Idea 83's "book turnover is mostly the 200d gate's EXITS" is therefore **false on the ranked book its budget was tested on** — on the broad list 8 of every 9 dollars of selling is rank rotation — and true on the unranked one. **(2) The remedy, priced against the lever.** An entry-only budget (every exit honoured in full, buy leg capped at B, unspent proceeds to cash) on `EWall`: u56 B=0.10 reads **10.0%/1.100/-12.9% at 6.3x turnover** against the control's 10.4%/1.050/-15.9% at 8.2x, and broad B=0.20 reads **10.9%/1.077/-16.4% at 7.8x** against 10.7%/1.027/-17.7% at 8.3x. Exchange rate **-0.14 and -0.01 pp of CAGR per pp of MaxDD, against the gross lever's -0.63..-0.73 measured on the same days** (the ladder reproduces ideas 66/83 to 0.01 and confirms dSharpe = 0.000 across it). It is **not** the lever in disguise: at u56 B=0.10 gross falls only 5.5%, which at the lever's rate buys 0.9pp of drawdown, and the observed improvement is 3.0pp. The advantage widens with cost (u56 EWall Sharpe at 25 bps: 0.925 control -> 1.012 at B=0.05). **On `CAND20` the same instrument does nothing**: dSharpe +0.001..+0.007 on u56 until the budget begins de-grossing (0.717 -> 0.538 at B=0.05, where its exchange rate converges on the lever's own -0.68), and **negative at every budget on broad** (-0.002..-0.016, t -1.08..-3.68). The exit attribution predicts which book responds. **(3) Idea 83's total budget reproduces, sign and size:** gross untouched (0.739-0.750 at every B), turnover cut up to 70%, and **MaxDD deepens in 19 of 20 arms, mean -3.0pp** (idea 83 reported -2.4pp pro-rata), with dSharpe positive but never significant (t +0.17..+1.89). **(4) PARK, not KEEP.** No cross-universe 4b pass at a common budget: broad `EWall` B=0.20 turns the control's OOS 4b CAGR miss into a PASS (4b full PASS, 4b OOS PASS) while the same B on u56 `EWall` still fails 4b's CAGR floor as its control does; over all 44 arms 4b full is 16/44, 4b OOS 24/44 and 4a 17/44 (all the 4a passes are on broad, where v1 itself draws down -21.2%). And **rule 8 picks the wrong end of the grid — a textbook case of idea 88's finding**: on u56 `EWall` in-sample Sharpe is monotone DOWN in the budget (0.970 -> 0.934) while OOS Sharpe is monotone UP (1.114 -> 1.224), so R0 selects the weakest arm B=0.30 (OOS 1.124 against the grid's 1.224) and R2 is infeasible; on broad `EWall` R0 picks B=0.20 (OOS 1.097 vs best 1.113). The IS Sharpe spread there is 0.036 — just above idea 88's 0.02 floor, i.e. a grid that passes the selectability pre-test and is still selected backwards. Note the paired daily return t-test has no power on this instrument (CAGR moves -0.4pp/+0.2pp; the whole effect is in drawdown and vol), so t -0.23..+0.58 at the favourable arms is not evidence against it. Survivorship: both lists are current constituents, absolute CAGRs optimistic; every arm shares the panel and the days, so the control-vs-arm comparisons are far less exposed than the levels. **For the Sunday review: if a turnover instrument is ever adopted it must be entry-only and only on an unranked book** — the ranked book's selling is rank rotation, and constraining it either does nothing or silently de-grosses. Rules unchanged. Result memo: `research/backtests/2026-09-04_exit-schedule-vs-entry-schedule_cloud.result.md`.
- 2026-09-04 — Research lane B, idea 84 `which-4b-bar-binds-decides-the-lever`: **KILL idea 83's two-branch lever rule as worded; replacement clause proposed; KEEP-candidate by-product.** 480 grid points + a 312-point fine gross ladder, all reported; harness reproduces `engine.backtest` to 0.000e+00 and the published control rows of ideas 2/46/57/72 exactly. The census is degenerate — the CAGR floor binds 6 of 8 (candidate, universe) cells, the DD cap 1, a Sharpe bar 1 — and unstable in both cost and window (IS vs full-sample binding bars disagree 3 of 8). The mechanism kills the branches: the Sharpe bars are **g-invariant in all 24 (cell, cost) triples** (Sharpe spread <= 0.0061 over g = 0.40->1.00, reproducing idea 66 on four books), so gross is ONE axis carrying BOTH non-Sharpe bars (~+1.4pp CAGR, -2.0pp MaxDD per +0.10 g at dSharpe 0.000) and "DD-bound -> cut g" / "CAGR-bound -> raise g" is the same lever in two directions. Idea 83's CAGR branch names the wrong instrument: the entry-only budget is CAGR-neutral by construction (it saves cost but raises cash), giving d_CAGR +0.01..+0.20pp against a 0.26pp gap where gross gives +3.7pp; the budget's real content is Sharpe (+0.047/+0.050 on broad) and MaxDD. Third branch idea 83 lacks: no lever converts a Sharpe-bound cell (best improving move +0.003 over 20 arms). **Replacement clause: 4b's DD cap and CAGR floor both sit on the gross dial, so they define an interval [g_min, g_max]; set g inside the interval that holds on both universes at 10 AND 25 bps, and if it is empty or a Sharpe bar fails, change the book — no exposure or turnover setting repairs it.** By-product KEEP-candidate: idea 57's `ew-band3` at **g = 0.85** — u56 12.8%/1.136/-17.1% (halves 1.113/1.160, OOS 14.4%/1.234), broad 12.6%/1.064/-18.9% (halves 1.163/0.971, OOS 12.7%/1.073), 5.5x/5.8x turnover — the project's first arm passing 4b full AND OOS on BOTH large-cap universes at 5, 10 AND 25 bps, and rule-8 selected at g=0.85 on both. This answers idea 58: the published g=0.75 dies at 25 bps on the CAGR floor and gross is exactly the CAGR lever, so the band was never what needed fixing. The rule under test has no net walk-forward content (mean OOS Sharpe 1.098 vs argmax-IS 1.097 vs control 1.083). Memo written. Ideas 90-92 queued. Rules unchanged.

- 2026-09-04 (lane C, idea 22 drawdown-control) — **KILL for a book-level drawdown trigger, and the instrument is now PRICED against the only lever the project trusts.** Idea 40 reported this mechanism as a KILL on lane A's no-scaler book at three thresholds, one cut depth and one reset rule; this run does idea 22 *as worded* on the live book, adds the control idea 66 made mandatory (gross is an exact, Sharpe-neutral lever, so a rule that spends half its life cut is a de-grossing in disguise and must be judged against a STATIC book at the same average gross), and puts the result on idea 74's axis. 3 books (RULES v1 as live; idea 2's `CAND20` KEEP-candidate; idea 72's `EWall`) x 2 universes x [control + D in {4,6,8,10,12,15}% x k in {0.00,0.25,0.50,0.75}] x 2 reset arms (`high` = idea 22's new-equity-high; `recover` = release at -D/2) x 3 cost levels = **882 points, all reported**; exactly two tuned parameters (D, k) with RESET run as an arm and every walk-forward selection confined inside one reset arm. Harness sanity: the control reproduces `engine.backtest` at max|diff| 0.0 and reproduces idea 2's KEEP row (12.7%/1.093/-18.3%, halves 1.088/1.103) and idea 10's `B136/EWall` (10.7%/1.027/-17.7%) to the decimal. **(1) The literal rule loses in 6 of 6 cells** — u56: V1 3.7%/0.485/-12.1% (dSharpe -0.182), CAND20 8.4%/0.917/-14.2% (-0.176), EWall 6.7%/0.833/-12.5% (-0.217); broad: V1 3.2%/0.440/-15.3% (-0.200), CAND20 7.5%/0.749/-15.3% (-0.209), EWall 7.2%/0.882/-14.3% (-0.146) — and in every cell it **converts a 4b-passing or near-passing book into a 4b failure on the CAGR floor**. Drawdown does improve in 6/6 (+1.7 to +5.9pp); it is simply not worth what it costs. 269 of 288 treated arms lose Sharpe against their own control; the best arm in the entire run is +0.003. **(2) The price, which is the transferable result: 1.02 pp of CAGR surrendered per pp of MaxDD bought (median over 288 arms) against the static gross lever's 0.31-0.70 in the same cells (mean 0.57) — the drawdown rule pays ~1.8x the going rate, and 252 of 288 arms are dominated outright.** Held against a static book at the arm's own realised average gross, only 3 of 288 arms beat the matched static book on both Sharpe and MaxDD, by +0.001..+0.003. **(3) The de-levering ratchet is confirmed and its extreme case is structural, not a parameter choice.** Median share of days spent cut is 50.5% (`high` reset) and 37.5% (`recover`), reproducing idea 40's 52-79% on different books: cutting exposure slows the recovery that is required to un-cut it. At **k=0.00 both reset rules are ABSORBING by construction** — a book earning nothing can neither make a new high nor recover to -D/2 — so `u56 CAND20 D=4%/k=0` exits in 2011 and never returns (97.3% of days in cash, +0.0% in 2020 and 2022, second-half Sharpe undefined because the return stream is exactly zero). A quarter of the k-grid is degenerate, and that is a property of the rule FAMILY: any exposure rule whose re-entry condition is a function of its own equity has this failure. Queued as idea 93. **(4) Rule-8 walk-forward: the rule is not selectable.** S1 (argmax IS Sharpe) picks the do-nothing corner D=15%/k=0.75 in **12 of 12** cells — bit-identical to the control in the 4 cells where it never triggers, and losing OOS Sharpe in **8 of 8** where it binds (-0.013..-0.066); mean OOS Sharpe control 0.922 vs S1 0.900. S2 (4b-aware) **picks nothing in 10 of 12 cells** (no IS point cleared the IS 4b bars) and in the other 2 picks that same do-nothing arm, -0.066. **(5) It does not even buy the bear it was written for:** 2022 is -9.0% un-cut vs -8.2% with the literal rule on u56/CAND20 (SPY -18.2%) and *worse* on broad/EWall (-10.1% vs -8.4%), while 2020 is where it is paid for (+15.4% -> +2.6%). Only the absorbing k=0 arms improve 2022, by being in permanent cash. **(6) Two results recorded against our own KILL:** turnover FALLS under the rule (u56 CAND20 9.6x -> median 7.8x/yr), so none of the loss is a trading-cost story — it is missing exposure during recoveries; and 36 of 288 arms are NOT dominated on the CAGR/drawdown axis, 24 of them in the single cell `broad/V1`, the worst book in the run (0.640 Sharpe, -21.2% MaxDD, failing all five 4b bars) — a drawdown rule is cheapest exactly where the book is not worth running. **(7) The 4a pathology again:** 4a passes RISE with cost (10 and 62 at 10 bps, 30 and 74 at 25 bps across the two lists) while 4b passes fall to **0 of 294 at 25 bps** — cutting exposure clears 4a only because RULES v1 is weak. **For the Sunday review: recommended RULES wording is none.** Idea 22 is now answered twice (idea 40 on the growth book, this run on the live book) and should be closed rather than re-parameterised; if drawdown is to be reduced, the supported instrument is the gross number itself, stated as a budget. Survivorship (current-constituent lists) runs *against* a drawdown control — a survivor list has shallower drawdowns than the live universe did — so the KILL is conservative. Rules unchanged. Ideas 93-94 queued. Result memo: `research/backtests/2026-09-04_drawdown-control_C.result.md`.

- 2026-09-04 (lane C, idea 26 ensemble-plus-momentum) — **KILL as worded; PARK a by-product that is the project's only cross-universe 4b pass.** 60 grid points (f in {0,0.25,0.50,0.75,1.00} x 3 books [live v1 / idea 2's `top20` KEEP / `EWall`] x 2 universes x 2 gross conventions [natural, gross-matched]), all reported, plus a year table, a 20-point cost ladder and a correlation matrix; 2 tuned params (f, book); harness reproduces idea 2's KEEP row (12.7%/1.092/-18.3%, halves 1.088/1.102), the live v1 row and idea 18's standalone sleeve row. **(1) The idea as written is a 4a pass and a two-bar 4b failure.** 50% macro-trend-ensemble B + 50% v1 top-5 gives 5.8%/0.791/-11.9% (halves 0.725/0.854, OOS 0.935) on u56: it beats the live book in both halves with a better drawdown on both universes and survives rule 8, but 4b dies on H1 (0.725 vs SPY 0.957) and on CAGR (5.8% vs the 10.66% floor). **(2) The proposed mechanism is CONFIRMED and still insufficient** — dSharpe against the linear blend of the parts is positive in 36 of 36 interior cells (mean +0.052, range +0.008..+0.085), i.e. the blend really does beat the weighted average of its sleeves everywhere; but Sharpe buys ~0.05 of convexity while CAGR falls almost exactly linearly toward the sleeve's own 5.0%, so 4b's CAGR floor binds by f=0.50 in every cell. The reason is in the correlation matrix: sleeve-to-book daily correlation is 0.63-0.82 (u56) / 0.63-0.75 (broad) because 5 of the sleeve's 9 assets are equity ETFs — it is a lower-return version of the same long-equity trend exposure, not a diversifier. **(3) By-product `top20 + 25% sleeve`** is the ONLY (book, f) in the grid passing 4b on both universes under both gross conventions (u56 10.8%/1.085/-16.0%, halves 1.061/1.110, OOS 1.184; broad 11.1%/0.972/-17.3%, halves 1.106/0.854, OOS 0.940) and it repairs idea 2's known broad-universe H2 failure (0.811 -> 0.854 against SPY's 0.834). It is PARK, not KEEP, on three counts: **rule 8 cannot select it** (IS Sharpe is monotone DECREASING in f on both lists, 0.993->0.614 and 1.044->0.612, while OOS Sharpe rises monotonically on broad 0.892->1.090, so the walk-forward picks f=0.00 in all four cells — the same unselectability idea 88 declined to amend around); **its whole effect is one calendar year** (the sleeve's contribution to annual return is negative in 17 of 18 years on both universes, positive only in 2022 at +1.6pp/+2.2pp); and **it is priced at the protocol's assumption rather than inside it** (the joint cross-universe 4b pass holds at 5 and 10 bps and dies at 15 — an improvement on the incumbent's 5-bps-only window measured in idea 82, but not a margin). Rules unchanged. Follow-ups queued as ideas 98 (leave-one-year-out as a KEEP bar), 99 (are defensive overlays rule-8-invisible by construction?) and 100 (re-run with the sleeve restricted to its 4 non-equity assets).

- 2026-09-04 (cloud, idea 23 earnings-season-avoidance) — **KILL.** 372 points (2 universes x 3 books [live v1 / idea 2's `top20` KEEP / idea 10's `EWall`] x 2 exclusion conventions [`replace` = promote the next eligible name at constant gross, `cash` = sit the holding out] x [1 no-blackout anchor + 15 post-quarter-end season windows + 15 placebo windows]), all reported; 2 tuned params (start in {7,14,21,28,35} days after quarter end, length in {14,21,28}); the anchor reproduces `rules_v1_weights` to 0.0. **(1) The premise is refuted before the rule is tested.** Single-stock SPY-excess daily return in-season vs out is **+0.81 bps/day on u56** (positive in 10/15 windows, max |t| 2.39) and **+0.05 bps/day on broad** (8/15, max |t| 1.42) — and the +45-day PLACEBO season's own max |t| is *larger* on both panels (3.10 / 2.16), so no window is distinguishable from window-shopping. The single real in-season effect is volatility (u56 0.36-0.39 in-season vs 0.30 out, for windows starting 14-28 days after quarter end): more risk, no more return. **(2) The rule loses in 179 of 180 season cells** (dSharpe mean **-0.129**, dCAGR **-2.19%/yr**, dMaxDD +0.50pp), and the best-of-15 window *cherry-picked per cell* still loses to its own anchor in **11 of 12 cells** (-0.075..-0.009); the one exception is +0.004 on `broad/v1/cash`, the run's weakest book. **(3) The placebo splits the damage in half.** Sitting out ~30% of stock-days costs -0.061 Sharpe wherever you do it; the season window costs twice that at the same duty cycle, i.e. the post-quarter-end weeks are where the *return* is — the Frazzini-Lamont announcement-premium sign, opposite to the idea. Turnover also RISES (15.5x -> 17.9x/yr), so this is not a trading-cost story in reverse. **(4) Priced as drawdown insurance it is off the bottom of idea 94's menu:** MaxDD improves in 99/180 season cells at a median **2.88 pp of CAGR per pp of MaxDD** (aggregate 4.34) against idea 94's dearest priceable instrument at 0.91 and the static gross lever at 0.57 — the same drawdown is ~5x cheaper by holding less. **(5) Rule 8 is a clean selection demonstration:** IS (2009-2016) Sharpe is HIGHER for a blackout arm than for the anchor in 10 of 12 cells (mean +0.100), so the walk-forward picks a season window in 10/12 and OOS Sharpe then falls **-0.106..-0.420 (mean -0.188)**, regret <= 0 in 12/12. **(6) The 7 season rows that pass 4b are inherited, not caused** — all sit on `u56/top20`, whose anchor already passes at 12.7%/1.092/-18.3%, and every one of the 7 has a lower Sharpe than that anchor; zero fail-to-pass conversions. **(7) The 4a pathology again:** 4a passes rise from 4/12 anchors to 47/180 season rows, because cutting exposure clears 4a only while RULES v1 is weak (ideas 22, 40, 94). Recorded against our own KILL: nothing. KILL rather than the queue's expected PARK — the calendar approximation covers most of the reporting mass of most names in both panels, and the effect is large, negative and monotone across 180 cells and two panels; exact dates would sharpen the window, not change its sign. A resurrection should have to clear the premise test (|t| > 3 on broad) first. Survivorship (current-constituent panels) runs *against* resurrection: survivors had fewer catastrophic announcements. Recommended RULES wording: none. Rules unchanged. Result memo: `research/backtests/2026-09-04_earnings-season-avoidance_cloud.result.md`.

- 2026-09-05 (cloud, idea 100 sleeve-with-a-real-diversifier) — **PARK (strong): the project's best drawdown-adjusted book, blocked on one un-pre-registered dial.** 120 points (5 sleeve fractions x 2 sleeves x 3 books [live v1 / idea 2's `top20` KEEP / idea 10's `EWall`] x 2 universes x 2 gross conventions), all reported; 2 tuned params (f, sleeve); the S9 control arm reproduces idea 26 exactly (dSharpe +0.052 in 36/36; `top20 + 25% S9` 10.8%/1.085/-16.0%, halves 1.061/1.110, OOS 1.184). **(1) The premise is confirmed and larger than supposed.** Restricting idea 26's 9-asset sleeve to its four NON-equity assets (TLT/GLD/DBC/UUP) moves sleeve-to-book correlation from **0.626..0.820 to -0.011..+0.212** (S4-to-SPY **-0.141**): the 5 equity ETFs were carrying essentially all of it. **(2) The convexity is 5.1x larger and 2.9x cheaper.** dSharpe against the linear blend of the parts is positive in 36/36 interior cells for BOTH sleeves, but averages **+0.265 (+0.070..+0.479) for S4** vs **+0.052 (+0.008..+0.085) for S9**, at a median **0.090 vs 0.031 Sharpe per pp of CAGR surrendered**. The queue's question answers yes on the exchange rate, **no in absolute CAGR** — S4 costs MORE total CAGR (-3.19% vs -2.15%) because it earns only 2.6% standalone against S9's 5.0%. **(3) Both objections that killed idea 26's by-product are repaired.** Rule 8: idea 26's sleeve was unselectable (IS Sharpe monotone falling in f, picks f=0 in every top20/EWall cell); S4's IS surface is not monotone, rule 8 picks **f=0.50 in 8/8 S4 cells** and the pick beats its own no-sleeve anchor OOS in **8/8** (u56 top20 1.308 vs 1.168; broad EWall 1.232 vs 1.019), regret 0.000..-0.104 with four cells at exactly 0.000. **This is a direct counter-example to idea 99** — defensive overlays are not rule-8-invisible in general; S9's invisibility was a property of an equity-correlated sleeve. One-year dependence: S4's year table looks like idea 26's (contribution positive in 2/18 years) but the **Sharpe advantage does not depend on it** — with 2022 deleted, dSharpe vs the anchor stays positive in **8/8** S4 cells (mean shrink -0.015) where S9's is negative in 4/8 both ways; leave-one-year-out is also kinder to the S4 blend (-0.065/-0.074) than to the bare book (-0.077/-0.083). **(4) And yet the literal 4b footprint is SMALLER** (interior passes S4 2/36 vs S9 4/36; the only cell passing all four universe x convention combinations is still S9/top20/f=0.25), because **every S4 failure is the CAGR floor alone**: u56 `top20 + 25% S4` natural reads 10.2%/**1.144**/-14.2%, halves 1.114/1.175, OOS 1.231 — clear of the Sharpe bars by 0.16-0.34 and the drawdown cap by 6pp, short of the 10.66% floor by 0.5pp. **(5) That shortfall is a GROSS artefact, not a return artefact.** The natural blend de-grosses to 0.648-0.700 (the trend vote is often partial); rescaling the rule-8 pick moves Sharpe by <=0.001 across g in {0.75,0.85,1.00,1.25} — ideas 66/84's exact-lever result reproduced — and at **g=1.00 (fully invested, no leverage) it clears 4b on BOTH universes**: u56 **11.8%/1.149/-14.2%** (halves 1.099/1.197, OOS 1.236), broad **12.2%/1.063/-15.6%** (halves 1.173/0.961, OOS 1.020), both with materially better drawdowns than the incumbent candidate's -18.3%/-20.1%, at **0.20-0.22pp of CAGR per pp of MaxDD** — level with the cheapest instrument on idea 94's menu (band3-rw 0.18) and unlike it Sharpe-positive. **(6) Recorded against our own case:** at the natural gross the cross-universe 4b pass holds at 5 bps and is lost by 10 (the CAGR floor again), and the g=1.00 arm was never cost-laddered. **PARK, not KEEP:** the 4b-passing version needs a third dial (exposure) that this run did not pre-register, and gross-tuning to clear a floor is the move rule 7 warns about even when idea 84 has shown the dial is exact. Regime caveat that must ship with any adoption: 2009-2021 is a falling-rate sample and the sleeve's one materially positive year (2022) is the year its duration vote went short. Recommended RULES wording: none this week; the wording a follow-up would take is in the memo, marked not-yet-adoptable. Rules unchanged. Ideas 101 (pre-registered fixed-g run + cost ladder + cadence bar), 102 (is the sleeve just TLT?) and 103 (correlation as the sleeve design variable) queued. Result memo: `research/backtests/2026-09-05_sleeve-with-a-real-diversifier_cloud.result.md`, `.memo.md`.
- 2026-09-05 — Research cloud (idea 114, IS-Sharpe-margin-as-the-reportable-selector-statistic): **PARTIAL — the margin is a selector-stability statistic and nothing more.** On idea 99/109/112's 44-cell harness (6 overlay grids x 2 books x 2 universes x 2 cost rungs, 178 grid points, weekly, next-day, 10/25 bps; the pooled LOYO pick-change rate reproduces idea 112 exactly at 72/352 = 20.5%), the margin M = IS Sharpe(best) - IS Sharpe(runner-up) at rule 8's pick was measured in every cell against two pre-registered hypotheses. **(1) M predicts leave-one-IS-year-out pick instability:** Spearman(M, change rate) = **-0.368** with one-sided permutation p 0.046 on the 22 primary (10 bps) cells, -0.240 at 25 bps, -0.317 pooled; the scale-free variant M/sd(IS Sharpe) is much stronger (-0.548 p 0.005; -0.652 p 0.001 at 25 bps) and is the only variant that also prices the swap cost (-0.461 / -0.609). Splitting the primary cells at the median margin (0.008 Sharpe) separates mean instability 0.125 vs 0.330 and 'pick was also OOS-best' 45% vs 18%. **(2) M does NOT predict out-of-sample regret:** rho **+0.119** (p 0.705), wrong sign at both cost rungs — and the reason is that there is no regret to predict. Mean OOS regret across the 44 cells is **0.015 Sharpe** (median 0.005, max 0.072) and rule 8's pick is already the OOS-best point in 16/44 cells, with pooled LOYO swap cost -0.003. Idea 112's 'the IS surface is flat and noisy rather than wrong' now has its OOS counterpart: **both** surfaces are flat over the same points, so the selector's choice barely matters either way. **(3) The margin is a property of the instrument:** band 0.044, crypto 0.036, sleeve 0.034 vs breadth 0.004, stop 0.004, gross 0.001 — and the two flattest grids (gross, breadth) carry mean instability 0.375 and 0.469, i.e. rule 8's pick there is close to a coin flip between IS-equivalent points. **(4) It does not separate KEEP candidates:** at 10 bps the 4b-passing cells have mean M 0.021 / instability 0.308 vs 0.016 / 0.111 for the failures — if anything the passers are the less stable picks. **(5) Walk-forward (rule 8, picks made on IS only):** 10 bps 4a 9 / 4b 13 / 4b-OOS 15, mean OOS Sharpe 1.115; 25 bps 4a 8 / 4b 7 / 4b-OOS 7, 0.982. The standing KEEP-4b candidate's cell (sleeve / u56 / top20 / 10 bps) is **unchanged**: pick f=0.50, full 12.3% / 1.180 / -14.3%, halves 1.161/1.200, OOS 13.6% / **1.261** / -14.3% vs RULES v1 OOS 0.699 and SPY 15.5% / 0.882 / -33.7% — with M 0.018 (below the 44-cell mean) and regret 0.000, which is itself the cleanest demonstration that margin says nothing about OOS quality. **Recommendation to the Sunday review (no file modified):** PROTOCOL rule 8 should quote M and M/sd next to every walk-forward, with the caveat stated in the same sentence — a stability flag, never a confidence interval, and M < 0.01 Sharpe is a tie. Caveats: survivorship (current constituents, identical across every cell compared), 22 points is a small sample and H1 clears at p 0.046, crypto's IS window starts 2014-09-17 (ex-crypto rho -0.368, p 0.058). Script `research/backtests/2026-09-05_IS-Sharpe-margin-as-the-reportable-selector-statistic_cloud.py`.
- 2026-09-05 — Research cloud (idea 98, one-year-dependence-as-a-KEEP-bar): **the proposed bar is KILLED as a gate and kept as a reported statistic.** Idea 89's LOYO harness (delete a calendar year from the book AND SPY AND v1, chain-link the remainder, recompute every statistic and every 4b bar on the retained days) was run over **every standing 4b pass the project has** — 9 books (idea 2's top20, idea 46's frac085, idea 57's ew-band3, idea 84's ew-band3-g085, idea 72's EWall, idea 94's EWall+vol60dg, idea 101's top20+50S3, the idea 99/101/114 sleeve blend top20+50S4, and v1 as a control) x 2 universes x 2 cost rungs x ~18 years, each book reconstructed from its own source script and reproduced against its published leaderboard row first. **(1) The queue's question:** 10 of the 14 full-sample 4b passes survive deleting their own single best year; the four that do not are u56 ew-band3 (CAGR), broad EWall (CAGR), broad top20+50S4 (OOS) and broad top20+50S3 (H2,OOS). **The best year is 2022 for 13 of the 14** (top20/u56 is 2018), mean excess over SPY in that year +11.7pp — the 'one bear market avoided' shape is now confirmed across every candidate, not one book. Only 5 of 14 survive deleting EVERY year; idea 72's EWall is again the weakest at 10/18 (idea 89 reported 10/18). **(2) The finding that kills the bar:** **14 of 16 verdict flips are the 4b BAR moving, not the book weakening** (idea 89: 12 of 14). All 6 MaxDD flips are bar-side — deleting 2020 raises SPY's 0.60x-MaxDD cap by 5.53pp while the books move 0.68-2.85pp — and under a relative-only 4b (H1/H2/OOS vs SPY on the same retained days, absolute bars checked full-sample only) **every candidate is 18/18 at 10 bps**. All of 4b's one-year dependence lives in its two ABSOLUTE bars, which are statements about SPY. **(3) The pre-registered rule-8 walk-forward (every bar applied on 2009-2016 ALONE, the admitted books then judged on the untouched 2017-2026 window):** on universe.json the ex-best-year, every-year and relative-only bars each admit **ZERO** books, while status-quo 4b admits 4 that go on to average **1.213 OOS Sharpe with 4/4 beating SPY's 0.882**; on the broad list ex-best-year admits the identical 7 books (+0.000) and relative-only 5 (+0.020), both under the pre-registered +0.05 bar. A gate that discards four true positives to buy zero true negatives is not a gate — all three variants are REPORT-ONLY. **(4) By-product worth the Sunday review's attention:** idea 94's `EWall+vol60dg` is the most year-robust book in the project (17/18 on u56, **18/18** on broad, at BOTH cost rungs) and one of only two standing candidates still passing 4b on both universes at 25 bps (u56 11.4%/1.113/-16.9%, broad 12.1%/1.119/-18.7%; the other is idea 84's ew-band3-g085). **Recommendation (no file modified):** PROTOCOL should require every 4b KEEP to REPORT its LOYO survival count, its single best year and whether 4b survives deleting it, plus the book-side/bar-side decomposition of each failing year — descriptive, never a gate. Caveats: survivorship (current constituents, identical across every book and window compared); MaxDD on a spliced series is an approximation applied identically to book and SPY (idea 89's convention); the selection test is 8 candidate books x 2 universes, so the u56 zero-admission result rather than broad's +0.020 is the load-bearing evidence; the S4 blend is run here on the crypto-excluded 56-name panel (11.9%/1.147/-14.2%) for comparability with every other book, vs 12.3%/1.180/-14.3% on the 58-column panel its published row used — verified directly. Script `research/backtests/2026-09-05_one-year-dependence-as-a-KEEP-bar_cloud.py`.

- 2026-09-05 — Research lane B (idea 117, crisis-depth-as-the-price-denominator): **PREMISE CONFIRMED on the large-cap panels and REFUTED on the small one; the episode-level replacement PASSES its pre-registered portability bar and is proposed to PROTOCOL rule 4 as a reporting clause, NOT a selector. Rules unchanged, no new book KEEP.** Idea 94's simulator was imported rather than re-implemented, so every number sits on the harness it audits: engine-equivalence `max|diff| = 0.0`, `EWall+vol60-dg` reproduces at 11.6%/1.133/-16.9%, and **idea 97's published headline reproduces exactly** (u56 gate tier 4.108 IS / 0.404 OOS against levers 1.002/0.616) before anything else was read. New: idea 62's episode classification (still Open) and the episode price layer. 3 panels x 3 books x 2 cost rungs = 18 cells, 306 arm-points plus a 19-point gross ladder each, 7,776 arm-episode prices, all reported; TWO tuned parameters (the episode depth threshold theta, with 8/10/15% all run and reported, and the instrument family in the selector). **(1) The premise holds on large caps and fails on small.** Regressing log(cell median price) on log(that window's SPY MaxDD): slope **-4.04 on u56** (t -5.78, R2 0.68), -3.05 on broad (t -3.35), **+0.13 on the 439-name panel (t 0.12)**. A window whose worst crash is shallow leaves little drawdown to buy, so everything in it looks dear — but that is not why the small-panel price is unstable. Stated limit: each panel supplies only two distinct depths, so these are IS-vs-OOS contrasts re-expressed as elasticities, not 54 independent points; the median IS/OOS ratio over the 18 cells is 2.18x, i.e. **idea 97's 10.2x pair is near the top of the distribution rather than typical**. The IS window's -22.1% 'worst crisis' turns out to be a 27-day fragment of the GFC that the 2009-01-13 evaluation slice merely clips, against OOS's 2020 (-33.7%) and 2022 (-24.5%) — idea 111's year-composition finding restated on the episode axis. **(2) The proposed replacement works.** Pricing per crisis — premium measured on CALM days only, divided by the drawdown saved inside each SPY episode, medianed within a 20 pp SHALLOW/DEEP bin — cuts the median cross-window |log10(IS/OOS)| from **0.455 to 0.184** against a pre-registered bar of 0.227, and is more portable in **92 of 109 arms** (sign-test z +7.18; 36/42 u56, 30/33 broad, 26/34 small). **The exact object idea 97 published, the u56 gate tier, goes from a 10.2x IS/OOS swing to 0.93x inside DEEP episodes and 1.34x inside SHALLOW ones: the whole gap was a depth-mix effect, not a change in the instrument.** **(3) The mechanism is measured with real variation in the regressor.** Protection delivered inside an episode, regressed on that episode's SPY depth over 2,400 arm-episodes: **+0.161 pp per pp, t +15.07**. So a whole-window MaxDD denominator is a crisis depth in disguise times a tier coefficient. Tier by tier: book-level DD control **+0.320** (t 11.6, R2 0.31 — the steepest, and the direct mechanism behind open idea 118), per-name gate +0.179 (t 12.4), entry budget +0.062, **per-name trailing stop +0.012 (t +1.46): its protection does not respond to crisis depth at all**. That is a mechanical and newly falsifiable version of idea 94/97's 'the stop is the dearest tier' — it is not merely expensive, it is inert with respect to the thing it insures. Across episodes the dearest crisis is **E5 (2018-02, -10.1% in 9 trading days) at 9.15 on u56 and 7.60 on broad**; the cheapest is E7 (2020) at 0.37-0.41 — a factor of ~25 inside one price list. **(4) It is a description, not a selector.** Rule 8 with parameters chosen on IS only: `Sdepth` (argmin IS depth-matched episode price) LOSES to idea 94's `S1` — mean OOS regret **+0.425 vs +0.337**, rank-1 4/18 vs 8/18, mean dOOS Sharpe -0.016, same pick in 8/18 cells. Its one signed difference is buying more drawdown for less return (OOS 7.4%/0.713/-19.5% vs S1's 8.2%/0.729/-21.3%; 4a 10/18 vs 5/18), which is the wrong trade under 4b's CAGR floor. Both sit well below SPY OOS 15.5%/0.882/-33.7% and above live RULES v1's 4.9%/0.451. **(5) No new KEEP.** 4b at 10 bps: u56 10 arms, broad 4 (`band3-rw`, `g200-rw`, `v1gate-rw`, `vol60-dg` — idea 94's published pair reproduced plus two it did not flag), **small 0, and none on all three panels**; at 25 bps 6/2/0. 4a passes 3/11/12 including the small-panel *control*, the known 4a pathology (ideas 22/40/94/97) driven by live v1 being weakest of all on small caps. **Recommendation to the Sunday review (no file modified):** amend PROTOCOL rule 4 with one sentence — a drawdown price must be quoted per crisis episode at a stated depth, with the premium measured on calm days, and a price whose denominator is a whole-window MaxDD may not be compared across windows or panels — and add the stop's non-response to depth as a citable clause. Do not adopt the episode price as a selector. Caveats: survivorship on all three current-constituent panels (idea 54), worst on small and in the direction that flatters gates; the calm-day annualisation compounds a non-contiguous day subset at 252 d/yr and is a premium proxy, not a tradeable return; 9 episodes at the point theta is a small sample and only P3 and P2's sign test have real margin; the small panel's refutation of the depth story is unexplained and belongs with open idea 118; calendar-day index (idea 38) still unfixed for u56/broad. Result memo: `research/backtests/2026-09-05_crisis-depth-as-the-price-denominator_B.result.md`, `.memo.md`.
- 2026-09-05 — Research lane B (idea 97, price-list-tier-bar): **KILL of idea 94's tier statement as worded; one clause of three survives every panel and every window and is proposed to PROTOCOL. Rules unchanged.** Idea 94's simulator was imported rather than re-implemented, so every number comes from the identical harness — engine-equivalence vs `engine.backtest` is `max|diff| = 0.0`, the published `EWall+vol60-dg` (11.6%/1.133/-16.9%) and `EWall+band3-rw` (12.2%/1.161/-17.7%) rows reproduce exactly, and idea 94's 12 walk-forward picks reproduce arm-for-arm. New: a **third panel** (data/prices_small.csv.gz, 439 names after holding SPY out and dropping the 44 corrupted series) and a tier layer. 3 panels x 3 books x 2 cost rungs = 18 cells, 17 arms + a 19-point gross ladder each, 816 arm-points, all reported; ONE tuned parameter (the instrument family/tier in the selector). The sentence was split into three falsifiable inequalities: C1 per-name gate < static gross lever, C2 lever < book-level DD rule, C3 DD rule < per-name stop. **(1) C1 is true in 33 of 54 panel x window x cell rows** — 6/6 on u56 and 6/6 on broad full-sample, but **2/6 on the small panel and 0/6 in u56's own 2009-2016 in-sample window**. C2 is 38/54 (full 13/18, OOS 9/18) and also inverts on small (2/6). **C3 is 50/54 and 6/6 on all three panels: the only clause that may be quoted.** The exact four-tier order holds in 11/18 full-sample cells, 2/18 IS, 7/18 OOS. **(2) The small panel does not weaken the ordering, it reverses it.** Median full-sample tier prices: u56 T1 0.468 < T2 0.592 < T3 0.659 < T4 unpriceable; broad 0.294 < 0.519 < 0.579 < unpriceable; **small T3 0.219 < T2 0.279 < T1 0.350 < T4 0.746**. The mechanism is not the one ideas 38/49/51 predicted — the 200d gate does not price negatively on `EWall/small` (it buys 21.9pp of MaxDD for 6.6pp of CAGR, rate 0.302) — it is that a book drawing down -30.8% gives the crude book-level DD rule far more drawdown to cut per pp of CAGR than any per-name signal finds. On `V1u/small` every gate prices **negative** (-0.34..-1.41: more CAGR AND less drawdown, +0.02..+0.10 Sharpe, 6/6 arms), which is queued as idea 119 for audit rather than cited. **(3) Idea 94's "a stop is not insurance" is refuted outside large caps:** on u56/broad the stop buys negative drawdown (median -0.69 / -1.03 pp, priced 0/6 each), on small it buys **positive** drawdown in 9 of 12 cells (median +1.07pp, up to 4.0pp). It is the dearest tier everywhere, which is the invariant statement; "inert" was a large-cap accident. **(4) C1 is also window-conditional on the panel it was derived from:** the u56 gate tier prices at **4.108 in-sample against a 1.002 lever** (SPY MaxDD -22.1% in 2009-2016) and 0.404 against 0.616 out-of-sample (SPY MaxDD -33.7%). An instrument that pays in crashes looks dear in a window without one — the crisis-density signature ideas 99/111 measured on overlays, reproduced on the drawdown-price axis, and the specific reason no price may be quoted without its window's depth. **(5) Tiers are more portable but not more stable.** IS->OOS median Spearman: tier +0.400 vs instrument +0.418 (idea 94's published 0.442 reproduces at 0.418 on the enlarged cell set) — a dead heat. Cross-panel: **tier +0.800 vs instrument +0.386**, with the instrument ordering going to -0.717/-0.833 on `u56 vs small, EWall`. **(6) Rule 8 refutes the tier selector.** `Stier` (argmin IS tier price over the rule tiers, then the median-IS-rate member) is worse than idea 94's plain `S1` in every panel: mean OOS regret **+0.489 vs +0.337**, rank-1 **4/18 vs 8/18**, OOS Sharpe a wash (+0.007). Report-only by-product: **in 12 of 18 cells no rule tier was cheaper in-sample than simply de-grossing** — the honest default is the lever. **(7) No new KEEP.** 4b passes at 10 bps: u56 14, broad 4 (`EWall` + `g200-rw`/`band3-rw`/`v1gate-rw`/`vol60-dg` — idea 94's published pair reproduced plus two it did not flag), **small 0**, every small arm failing H1, H2, OOS and the drawdown cap together against a control running 14.7%/0.767/-30.8% vs SPY 14.1%/0.862/-33.7%. 4a passes 3/11/12 including the small-panel *control*, which is the known 4a pathology (ideas 22/40/94) amplified by live RULES v1 being weakest of all on small caps (0.603 Sharpe, -32.8% MaxDD). **Recommendation to the Sunday review (no file modified):** amend PROTOCOL rule 4 with one sentence — price insurance against the same base book on the same days, quote the static-gross lever beside it, state the window's own MaxDD, and treat only "the per-name trailing stop is the dearest instrument" as general; every other ordering is panel- and window-specific and must be requoted. Do not adopt a tier-based selector. Caveats: survivorship on all three current-constituent panels, worst on small and **flattering** to gates there (it excludes the beaten-down names a gate would have sold), so the C1 inversion is a lower bound; the small panel's evaluation starts 2011-01-13 and its IS window is 2010-2016; the band width, vol threshold, DD-control settings and stop depths are inherited from earlier runs on overlapping data, so the IS/OOS split validates the ORDERING, not those constants; 18 cells is a small sample and only C3's 50/54 has real margin. Ideas 117 (price at matched crisis depth), 118 (why the DD control is cheap on small caps) and 119 (audit the negative `V1u/small` gate price) queued. Result memo: `research/backtests/2026-09-05_price-list-tier-bar_B.result.md`, `.memo.md`.
- 2026-09-05 — Research lane C (idea 116, 2011-the-opposite-outlier): **KILL as posed. 2011 really is rule 8's pick-moving year, but the dependence is a feature worth +0.010 of OOS Sharpe, and one clause of the premise is false.** Idea 112 left 2013 acquitted and 2011 as the open suspect on the other tail: the sample's best overlay year (+0.215), the year whose deletion moves the most picks (16/44), and — per the queue — the only year whose deletion widens the IS→OOS gap G. **(0) Reproduction is exact and mechanical**: idea 112's committed `picks.csv`/`deltas.csv`/`swaps.csv` are read back at runtime rather than transcribed, and this run agrees on **0 mismatches over 44 cells × 9 windows = 396 picks**, on the G-leverage table to 4 dp (full-IS −0.058, ex-2011 −0.101) and on the 2011 swap price (−0.0331). **(1) One premise clause is FALSE.** "The only year whose deletion makes G more negative" is wrong in idea 112's own data: **four** years do (2011 −0.0437, 2014 −0.0095, 2016 −0.0092, 2010 −0.0054). 2011's shift is 4.6× the next largest so the ranking stands; the word "only" is withdrawn. **(2) The controlled selector test says NOT SUPPORTED.** The harness was extended to **46 distinct IS year-sets** (FULL + 8 LOYO + 28 matched-length leave-two-out + 15 contiguous re-cuts) × 44 cells = 2,024 window-cell picks, nothing selected on the window axis. On the 28 six-year L2O windows — same length, only the year composition varies — the agreement premium A_keep − A_drop is **+0.169 for 2011**, the largest of the eight years and 3.9× the next (2009 +0.043), with four years negative. That is below the pre-registered 0.20 bar, so the claim as worded fails; a post-hoc check confirms the uniqueness is nonetheless real (the five lowest-agreement windows of 28 all drop 2011; unstructured-null p 0.00018, structurally-matched null at its 0.125 floor). **(3) It would not matter if it cleared the bar.** Keeping 2011 is worth **+0.010** of mean OOS Sharpe and **−0.010** of mean OOS regret, and mean regret across the whole design is only 0.018–0.028 — there is almost nothing for a selector to get wrong, independently reproducing idea 114 (mean OOS regret 0.015). The 16 ex-2011 swaps cost **−0.033** mean OOS Sharpe, worse in 11/16, buying +1.6pp of OOS CAGR for −3.1pp of OOS drawdown, net 4b −2 and 4b-OOS −3. **2011 is a FEATURE of rule 8's selector, not a defect**: 11 of the 16 swaps turn the defensive dial down (all 8 sleeve arms, both stop arms, gross 0.75→1.00), i.e. without 2011 in the window the defensive arms stop looking good. **(4) The re-cut clause is window LENGTH, not 2011.** Contiguous spans containing 2011 agree 0.682 with pick(FULL) vs 0.568 without, but agreement is monotone in length (4y 0.577, 5y 0.608, 6y 0.674, 7y 0.773, 8y 1.000) and the 2011-containing spans average 5.6 years against 4.3; the three spans that omit 2011 are short by construction. **(5) Walk-forward: rule 8's own window is the best of all 46** — OOS Sharpe 1.048, 4b 20/44, 4b-OOS 22/44, against ex-2011's 1.036/18/19 and the RECUT-omits-2011 family's 1.027/16.7/18.7; no re-cut of the IS window improves on rule 8 as written. SPY OOS 15.5%/0.882/−33.7%. **Standing KEEP-4b candidate untouched**: u56/top20/sleeve at 10 bps picks f=0.50 in **28 of the 46 windows** (f=0.25 in 8, f=0.00 in 6, f=0.75 in 4, f=1.00 never), 12.3%/1.180/−14.3% full sample (H1 1.161, H2 1.200), OOS 13.6%/1.261/−14.3%. No RULES change, no PROTOCOL change, no new KEEP. Caveats unchanged: survivorship on both panels (identical across every window compared), 2009 partial (~11.5 months), crypto arms near-inert before 2014-09-17, calendar-day index (idea 38). `research/backtests/2026-09-05_2011-the-opposite-outlier_C.py`, `.result.md`, `.console.txt`
- 2026-09-05 (cloud, idea 115 sleeve-G-is-2013) — **KILL as posed: the sleeve's rule-8 gap is not a gold story, and 2013 is a property of the WINDOW rather than of any sleeve asset.** Idea 112's 32 sleeve points reproduced exactly (max |diff| 1.1e-16; pooled G -0.1694 -> G_ex2013 +0.0350) before any new number was read; 8 cells (2 universes x 2 base books x 2 cost rungs) x 7 sleeve compositions x 5 fractions = 280 points, all reported; two tuned parameters (f, composition). **(1) All four sleeve assets lose in 2013 and gold is only the third-largest loser inside the leg** — TLT -1.16pp, DBC -0.80, GLD -0.71, UUP -0.69 — because the inverse-vol weighting gives the most volatile asset the smallest weight (GLD 1.92% of the book against UUP's 16.5%), so a -28.8% price move produces the third-biggest loss. 2013 is the only year in the sample in which all four lose together. **(2) Deleting gold does not remove the 2013 dependence.** exGLD halves the LEVEL of G (-0.169 -> -0.074) but still moves +0.186 when 2013 is deleted against S4's +0.204, and GLDonly passes 4b at 0 of 40 grid points with rule 8 picking f=0 (no sleeve) in 6 of 8 cells. The pre-registered test fails 2 of 3 conditions -> **FALSE**. **(3) The honest decomposition of 2013:** measured as excess over each variant's own other years, a zero-return leg holding the sleeve's exact weights (FLAT4) reproduces **27.5%** of S4's 2013 excess damage, gold **10.9%**, and the remaining ~62% is the other three assets losing simultaneously. 2013 is S4's worst year of 18 (z -3.4) and 2011 its best (+1.09); 2011's deletion moves G FURTHER (0.225) than 2013's (0.204), agreeing with idea 116. This is idea 111's crisis-density caveat restated with a number: rule 8's IS window contains a +32% equity year that punishes any diversifier, and a quarter of the penalty would fall on a leg earning nothing at all. **(4) On the composition axis it is DBC, not GLD, that carries the sign.** exDBC is the only variant whose pooled G is positive (+0.021) — removing commodities removes the sleeve's rule-8 blind spot — while exTLT roughly doubles the gap (-0.285). **(5) One KEEP-candidate, gated.** `exDBC / top20 / 10 bps` is the rule-8 pick (f=0.50, chosen on 2009-2016 alone) and passes **both** KEEP paths on **both** large-cap universes: u56 12.1%/1.200/-13.4%, halves 1.237/1.170, OOS 12.9%/1.240/-13.4%; broad 11.9%/1.068/-14.6%, OOS 11.0%/0.974 — against SPY 15.2%/0.889/-33.7% and RULES v1 6.3%/0.649/-13.8%. It dominates the incumbent S4 candidate full-sample in 8 of 8 cells (mean dSharpe +0.027, drawdown 0.021pp shallower) and loses to it marginally out of sample (mean dOOS Sharpe -0.015). Composition was a tuned axis, so this is a candidate and not a selection this run made, and it must not be adopted before queue idea 106 settles whether DBC's drag is a 2009-2013 contango artefact. Memo with exact RULES wording: `2026-09-05_sleeve-G-is-2013_cloud.memo.md`. **For the Sunday review:** do not write a gold clause into RULES on the strength of the sleeve's G; state the blind spot as a property of the IS window; and treat idea 106, not idea 105, as the load-bearing open sleeve question. Rules unchanged. Survivorship: both lists are current-constituent, so absolute CAGRs are optimistic — every statistic above is a same-names, same-days difference plus four ETFs with no survivorship exposure. Result: `research/backtests/2026-09-05_sleeve-G-is-2013_cloud.result.md`.
- 2026-09-05 (lane C) — Idea 113 crisis-beta-as-the-pre-registrable-selector: **KILL**. Idea 99's -1.03 pooled slope of overlay value on the year's SPY MaxDD does NOT survive being estimated per grid point and used as a selector. `argmax [IS Sharpe + beta_IS x B]` at B in {0.05,0.10,0.20} loses to plain rule 8 (B=0) in all three budgets on the same 44 cells: mean OOS Sharpe 0.960 vs 1.048, dOOS -0.088 against a +0.0150 ceiling (independently reproduced), 4b passes 20 -> 15/14/14; the sign-flipped control does BETTER (-0.025), which falsifies the mechanism. Why: IS crisis beta shrinks ~6x out of sample (slope +0.172) and is fitted at R^2 ~0.5-5% on 7 years, and beta_IS is an unbounded superlinear function of overlay DOSE so any B>0 saturates the dial at the grid endpoint. Realised budget was only B*=+0.027, and shrunk the right coefficient is ~0.005 — rule 8's B=0 is within a rounding error of optimal. Third selector proposal in a row to lose to argmax IS Sharpe (idea 109's CAGR floor -0.010). Recommend closing the selector line. No RULES or PROTOCOL change.
- 2026-09-05 (cloud, idea 119 V1u-small-negative-price) — **INDEPENDENT REPLICATION of lane B's run of the same idea, written and run in parallel without sight of it; the two agree to the decimal on every shared number (premise count 6 priceable / 5 negative at 10 bps; rule-8 pick `g200-rw` with OOS 8.9%/0.631/-32.8% against live v1 7.9%/0.581 and SPY 15.5%/0.882; concentration direction; 0 KEEP passes) and reach the same verdict by different attacks.** **KILL the citation as worded: the `small/V1u` negative gate prices are a ratio of noise, and the one effect that survives resampling is a SELECTION change, not free insurance.** Idea 97's 32 committed pricelist rows for that cell reproduced exactly (max |diff| 1.8e-15) through idea 94's own simulator and idea 97's own panel loader before any new number was read; two tuned parameters (gate family, position count n); every arm, floor, cost rung and draw reported. **(1) The queue's wording is not literal.** 11 negative of 13 priceable of 20 gate rows — `band3-dg` prices POSITIVE at both cost rungs (+0.604, +0.299) and both `vol60` arms buy exactly zero drawdown, so 'every gate prices negative, 6/6' overstates it. The quoted -0.34..-1.41 range is right for the arms it covers. **(2) The decisive objection is the denominator.** The control book draws down **-34.0 pp**; the negative-priced arms buy 0.05-3.28 pp, i.e. **0.15-9.7% of the book's own depth**, and under the pre-registered materiality bar (dMaxDD >= 10% of the control's |MaxDD| = 3.40 pp) **0 of 11 negative rows are material**. Raising idea 94's 0.10 pp priceability floor to 3 pp leaves ONE gate row in the whole cell. The headline -1.41 is 1.19 pp of drawdown on a 34 pp book. **(3) 200-draw name-subsample bootstrap (100 draws dropping 10% of the 439 names, 100 dropping 25%), pre-registered decision rule: ARTEFACT in 7 of 12 audits.** The split is structural, not random: the three arms whose CAGR gain keeps its sign in 93-100% of draws are all **`-rw`** arms, which idea 94 itself defines as rebuilding the book among gated-in names — a SELECTION change — while every honest de-gross (`-dg`) insurance arm fails (sign held in only 52-82% of draws). **(4) It is not one year and not costs.** Deleting any single calendar year leaves the CAGR gain signed in 0 of 96 arm-years (2020 is the largest single year: `abs12-rw` +10.9 pp, `g200-rw` +10.3 pp); the gain is already present at **0 bps** (-1.76 pp for `g200-rw`) and shrinks as costs rise; and every gated arm turns 32.8-33.5x/yr against the control's 33.3x, so the gate saves no trading. **(5) It IS largely top-5 concentration.** Negative-priced arms fall 5/6 -> 4/8 -> 4/9 -> 2/9 as n goes 5 -> 10 -> 20 -> 40; the de-gross arms' CAGR sign flips to the normal one (gate costs return) by n=20, and dMaxDD rises 0.57 -> 7.69 pp — only a wider book buys material drawdown, and there it pays for it in the ordinary way. **(6) Rule-8 walk-forward** (idea 94's selector S1 on the IS window only) picks `g200-rw` at both rungs, OOS price -1.501/-1.589 with regret 0.000 — it lands on the OOS-cheapest arm of a ratio finding (2) shows is uninterpretable. The BOOK reads 8.7%/0.634/-32.8% full sample (halves 0.689/0.587), OOS 8.9%/0.631/-32.8%, against RULES v1 on the same panel 7.9%/0.581/-32.8% and SPY 15.5%/0.882/-33.7%; **0 of 14 audited books pass 4a and 0 of 14 pass 4b** at either rung. **Survivorship is stated because it runs AGAINST the artefact hypothesis**: the panel is current constituents, which flatters the UNGATED book (it holds beaten-down names that all survived) and so makes a gate look worse here, not better — the artefact case rests on the denominator, the concentration and the resampling instability, which is where this run puts it. **For the Sunday review:** (a) do not cite `small/V1u` as evidence that gates are free insurance on small caps — the supportable sentence is in the result memo; (b) **proposal that does not touch RULES: replace idea 94's absolute priceability floor (dMaxDD > 0.10 pp) with a RELATIVE one — an instrument is priceable only if it buys >= 10% of the control book's own |MaxDD|.** Under the absolute floor this cell publishes 11 negative prices; under the relative floor it publishes none, which is the correct answer. Re-tabulating every published price under that floor is queued as idea 120; (c) this and idea 117 are the same finding from two directions — a price is not interpretable without the depth of the drawdown it is divided by. Rules unchanged. Result: `research/backtests/2026-09-05_V1u-small-negative-price_cloud.result.md`.
  ADDITIVE in this run against lane B's: the pre-registered MATERIALITY bar (0 of 11 negative rows move >= 10% of the control's own drawdown; a 3 pp floor leaves one gate row in the cell), the `-rw` vs `-dg` split inside the bootstrap (every arm whose CAGR sign survives name-resampling is the SELECTION convention and every de-gross insurance arm fails), the 0-bps decomposition showing the gain is not a cost saving, and the relative-priceability-floor proposal (queued as idea 123, complementing lane B's idea 122 on the denominator's SIGN with a test of its SIZE). ADDITIVE in lane B's and not measured here: the vol-scaler attribution (/sqrt(vol20) costs 11.7 pp/yr at n=5 on this panel) and the investability bound (held-name median ADV $4.33M). Two scripts, two authors, one verdict.
- 2026-09-05 (cloud, idea 120 delete-the-scaler-on-small) — **KILL, and the KILL is larger than the idea.** 640 grid points (5 n x 4 gross x 4 ADV floors x 2 scaler x 2 gate x 2 gross conventions) + a 16-point no-ranking control + cost ladder + capacity table, all reported; 2 tuned params (n, g); the simulator matches `engine.backtest` to 1.4e-17 and reproduces idea 119's unscreened pair (+9.64 pp CAGR, -11.9x turnover vs its published +11.7 / -11.8x). **(1) Idea 119's premium is a sub-$1M-ADV effect**: median dCAGR over 20 cells goes +5.35 pp with no floor (positive in 20/20) to -2.22 pp at a $5M floor (positive in 1/20), sign-flipping between $1M and $5M. **(2) Nothing tradeable survives**: at the $5M floor all 20 headline cells earn negative CAGR (-0.10% to -13.95%, drawdowns -27% to -96%), 0 of 640 points pass 4b, and rule 8's pick is OOS -9.17%/-0.171/-83.4% against SPY 15.45%/0.882/-33.7% — at 0 bps too, so it is not a cost story. **(3) The no-ranking control locates the decay in the panel**: equal-weighting every name above the floor gives Sharpe 0.678 / 0.413 / 0.181 / -0.163 at none / $1M / $5M / $20M with no ranking at all, and the unscaled ranking then subtracts a further -6.15 pp of CAGR (idea 82's sign). **(4) The honest reading is a survivorship one**: the panel is a current-constituent list, its bias falls hardest on the thinnest names, and those are precisely the names carrying its return — so every small-panel row in this project (31, 38, 49, 50, 51, 54, 97, 119) is exposed, which is idea 121's case. Capacity is the one number that improved: at $5M the held names have $14.5M median ADV and $10M of capital trades 5.6% of it. Rules unchanged.

- 2026-09-05 (cloud, idea 118 why-the-DD-control-is-cheap-on-small-caps) — **ANSWERED; KILL for citing idea 97's small-panel row as "the cheapest instrument there".** Idea 94's simulator imported unchanged: all 36 committed ddctl rows reproduce at 1.1e-16. 126 arm-points (3 panels x 3 books x 2 costs x [3 D x 2 k + 1 reset]), 25 depth-matched re-pricings, 6 vol-split re-pricings; 2 tuned params (D, k). **(1) The price is mostly the panel's own exchange rate**: `lever` (static-gross ladder slope) equals the control's Calmar in 18/18 cells, Spearman(rate, lever) = 0.831 over 108 arms, and panel rate 0.824/0.547/0.324 (u56/broad/small) normalises to 1.718/1.360/1.153 — spread 2.54x -> 1.49x (the pre-registered "half" bar is missed; 68% of the excess over parity is removed). **(2) At matched control drawdown the ordering INVERTS**: small goes 0.324 -> 0.608 and becomes the DEAREST panel, broad the cheapest — idea 97's row is a reading of a -34% control, not of small caps. **(3) Name vol is not an independent channel** (raw: low-vol half 0.228 vs high-vol 0.290, refuting the queue's guess; the sign only flips after normalisation). **(4) The mechanism is idea 93's absorbing state**: on the small panel the control is armed 74.2% of days (u56 50.6%), mean episode 465 days, and it ends the sample still armed in 24/36 arms — it IS a de-grossing, which is why its price converges on the lever. **(5) Decisive control**: at matched drawdown the static-gross ladder beats the DD arm in 107/108 arms (36/36 on small), so idea 22's headline is not reversed and idea 74's menu should quote the matched-drawdown comparison, never the pp/pp ratio without its panel's lever. 4b 3/126, 0 on the small panel. Rules unchanged.


- 2026-09-05 (cloud, idea 121 liquidity-screened-small-panel) — **ANSWERED; a $1M ADV floor proposed as PROTOCOL clause 10, a reporting requirement plus a stated default, NOT a `load_universe` change.** Engine equivalence 1.4e-17; idea 120's no-ranking control reproduced to 3dp. Pre-registered capacity criterion (ADV only, no return input): smallest ladder floor at which $10M trades <= 10% of the p25 held-name 20d median dollar volume in the ranked n=20 book. Unscreened that book trades **17.6%** of its p25 name's daily volume per rebalance (R10 46%, R5 **137%**); **$1M** brings it to 7.3%. **(1) The floor's price is the whole panel**: equal-weight-all goes 10.18%/5.92%/1.64%/-4.92% CAGR (Sharpe 0.678/0.413/0.181/-0.163) on 348/252/141/44 names at none/$1M/$5M/$20M, and that decay is inseparable from survivorship because the missing delisted cohort IS the thin cohort. **(2) The floor deletes every 4a claim this panel has made**: 7 of 48 (book,gross) verdicts move, all 4a True->False, 4a by floor 7/0/0/0; 4b is 0 of 192 at every floor, so nothing capital-worthy is lost. **(3) But the cited findings SURVIVE**, which is the opposite of idea 120: the gate's -5.4 pp/yr small-cap cost is -6.52/-4.87/-4.03/-3.56 pp by floor, negative in 12/12 cells, -3.70 pp even at 0 bps; and the four-way gate ordering holds exactly at the proposed floor. Idea 120's scaler premium remains the only published small-panel finding a floor reverses. **(4) A sharper, unrelated exposure surfaced**: the four-way gate spread is **0.080** under gross-matched weights (idea 81) against **0.356** as published — the ordering is stable, the magnitude that the QUEUE cites is a weighting convention. **(5)** Rule-8 picks give OOS Sharpe 0.286/0.163/-0.026/-0.608 vs SPY 0.882, monotonically worse as the floor rises, and the drawdown-capped selector is empty at every floor >= $1M. Rules unchanged.

- 2026-09-05 (cloud, idea 96 stop-as-negative-insurance) — **ANSWERED; idea 94's per-name trailing-stop KILL STANDS and is strengthened. The negative dMaxDD sign is neither the instrument nor the weekly grid: it is the ONE-DAY EXECUTION LAG, and PROTOCOL rule 2's own convention is the one under which the stop destroys drawdown. Rules unchanged; an extension of rule 2 is proposed.** Idea 94's module imported unchanged — the generalised simulator at (check=daily, lag=t+1) reproduces its stop arms at **max|diff| 0.000e+00** with zero firing-count differences, and its published medians re-derive (dMaxDD -0.69/-1.32 pp vs -0.69/-1.25, dSharpe -0.036/-0.007, turnover 12.5x/11.3x). 144 arms + 12 matched controls + 132 static-gross ladder points, all reported; two tuned params (check frequency, execution lag). **(1) The queue's premise about the code was wrong**: idea 94's stop is evaluated on every row, so the exit was already intra-week and the weekly grid governs re-entry only; check frequency moves median dMaxDD by 0.1-1.3 pp and never flips a sign. **(2) The lag does everything, and non-monotonically**: median dMaxDD **-0.69 pp** at t+1, **+2.44 pp** at t+2, **-0.25 pp** at the next rebalance. This run's own opening audit claimed idea 94 was one bar FAST; that was wrong (`engine` holds `w.shift(1)`, so idea 94 is conformant), and the correction is recorded in the script beside the predictions it invalidated rather than edited away. **(3) The mechanism is a short-term reversal, measured**: the day after a stop fires the triggering name earns **+0.57% to +2.21%** against an unconditional +0.06%/day (**t +2.70 to +10.37**, 9/12 cells, persisting +1.2..+2.6% over five days). A one-day-slower exit is not better insurance — it is a bounce harvested 15-280 times a year, which is idea 94's 'sells into the drawdown' sentence quantified. **(4) Nine arms appear to pass 4b and none may be quoted**: all sit on the non-conformant t+2 lag, all on `u56`, clearing 4b's drawdown cap by 0.15-1.6 pp on a single-path extremum against controls that fail 4b on that cap alone; **rule 8 picks the no-stop control in all four cells where they live** (IS differences <= 0.001, mean OOS regret +0.002), and 0 of 9 appear on `broad`. **(5) The generalisable exposure**: every drawdown price the project has published (ideas 9, 22, 40, 74, 94, 97, 117) is quoted without an execution lag, and for a fast instrument the lag moves the price by more than the instrument does — queued as idea 126. The post-trigger reversal is queued separately as idea 125 rather than smuggled in as a stop result. **Proposed to the Sunday review (no file modified):** extend PROTOCOL rule 2 — any rule acting between scheduled rebalances must state its trigger frequency and execution lag separately, the lag must match the scheduled book's, and no drawdown price may be quoted without the lag it was computed at. Caveats: current-constituent survivorship on both panels runs WITH this conclusion (it flatters the always-invested control), so the result is a lower bound on the stop's value; MaxDD is one number off one path; the reversal is not a tradeable return; idea 38's calendar-day index is still unfixed for these panels and applies identically to arm and control. Result: `research/backtests/2026-09-05_stop-as-negative-insurance_cloud.result.md`, `.memo.md`.
- 2026-09-05 — Research lane B (idea 95, vol-threshold-sweep-on-ewall): **KILL of `0.60` as a DERIVED threshold; the incumbent book survives the sweep unbeaten. Adoption of `EWall + vol60-dg` is UNBLOCKED on the numbers and CONSTRAINED on the wording. Rules unchanged.** Idea 94's script was imported rather than re-implemented, so every number sits on the simulator being audited: engine-equivalence `max|diff| = 0.000e+00`, idea 94's published `EWall+vol60-dg` u56 row reproduces at **11.6%/1.133/-16.9%** exactly, and `theta=0.60/dg` in this grid matches it to 0.000e+00 before anything new was read. `theta in {0.40,0.50,0.60,0.80,1.00}` x convention `{dg,rw}` x 2 universes x 10/25 bps = **40 points, all reported**, plus 4 ungated controls; exactly TWO tuned parameters. **(1) The surface is flat in Sharpe and monotone in everything else.** The whole theta axis moves Sharpe by **0.009 to 0.074**, and the ungated control sits INSIDE the sweep's own range in **7 of 8 (universe x cost x convention) cells**; the argmax wanders across 0.40/0.50/0.60/1.00 with no stable location, including both ENDPOINTS on the same universe at different cost rungs. What theta does move, monotonically in 8/8 cells, is exposure: on u56 @10bps `dg`, CAGR 9.4% -> 12.7% and MaxDD -13.7% -> -20.9% against a control at 13.3%/-22.5%, mean names held 47.2 -> 53.8 of 54.1. **Every one of the 40 arms LOSES CAGR to its own ungated control** (-0.25..-4.04 pp/yr, negative 40/40, |t| >= 2 in only 13/40; at the published 0.60, -1.05..-1.87 pp/yr at |t| 1.53-2.00). The vol20 gate on this book is a de-risking dial, not a signal. **(2) 0.60 is not chosen by an edge — it is where 4b's two ABSOLUTE bars bracket.** 18/40 pass 4b, 19/40 pass 4a, controls 0/4 and 1/4. The failure column splits perfectly along the axis: theta=0.40 fails the **CAGR floor** (9.0-10.5% vs 10.66%), theta=0.80/1.00 fail the **MaxDD cap** (-18.9..-25.2% vs -20.23%), and **no Sharpe bar ever binds anywhere in the grid**. Only two of the 40 points pass 4b in all four (universe x cost) cells: the incumbent `0.60/dg` (u56 11.6%/1.133/-16.9%, halves 1.156/1.113, OOS 1.186, 1.39x turnover; broad 12.4%/1.138/-18.7%, halves 1.255/1.027, OOS 1.122) and a genuine co-equal `0.50/rw` (11.7%/1.123/-18.2%, OOS 1.185; broad 12.5%/1.127/-19.5%, OOS 1.107) at 2.4x turnover. This is idea 84/90's gross-interval result reproduced on a new axis: the threshold is a risk-budget LOCATION, not a discovered constant. **(3) Rule 8 cannot set it, and its selectability FLIPS SIGN between two heavily-overlapping large-cap lists.** Both selection rules fixed in writing first (S1 argmax IS Sharpe; S2 same among points meeting 4b's two absolute bars on the IS window alone). **0 of 8 walk-forward selections pick 0.60.** On u56 both rules pick **theta=1.00** — the loosest, effectively no gate — at OOS rank 7 and 8 of 10, and the pick **LOSES to the ungated control out of sample** (1.133 vs 1.136; 1.111 vs 1.125); on broad both pick **theta=0.40/dg** at OOS rank 1 (10.2%/1.195/-12.6%), beating the control. Opposite endpoints, and **rho(IS Sharpe, OOS Sharpe) = -0.697/-0.636 on u56 against +0.588/+0.939 on broad** — in-sample Sharpe is *anti*-informative about the threshold on the primary universe. vs SPY OOS 15.5%/0.882/-33.7% and RULES v1 OOS 7.7%/0.747 (u56) / 5.9%/0.576 (broad). **(4) The one thing the gate earns, and it points away from the incumbent.** Against idea 94's 19-point static-gross ladder interpolated to each arm's OWN MaxDD, the gate reaches the same drawdown at a HIGHER CAGR in **38 of 40 points** (+0.11..+3.85 pp; ladder slopes 0.595/0.588 u56, 0.565/0.558 broad), pricing at 0.257-0.318 pp/pp at 0.60 — about half the cost of simply holding less. But the largest lever edge, the best Sharpe (1.190) and the best drawdown (-12.6%) in the entire run all sit on `broad theta=0.40/dg`, which **fails 4b in 4/4 cells on the CAGR floor alone** — ideas 100/117's shape again: 4b's absolute bars, not its risk-adjusted ones, decide these cases. **(5) Supports open idea 123:** under idea 94's absolute priceability floor all 40 arms are priceable; under idea 123's proposed RELATIVE floor (dMaxDD >= 10% of the control's own |MaxDD|) **12 of 40 stop being priceable**, every one of them theta in {0.80,1.00}, and both ratios above 1.0 in this run are inside that discarded set. **Recorded against our own reading:** the incumbent is not beaten, is the cheaper of the two 4/4 points in turnover, and a wide plateau is the BENIGN form of unselectability (idea 66's gross result) — the walk-forward's failure is evidence about the parameter, not against the book. **Survivorship** (idea 54) runs toward this run's finding, not against it: a vol20 gate is exactly the instrument a current-constituent panel flatters, and it flatters LOOSE thresholds more than tight ones. Calendar-day index (idea 38) still unfixed. **For the Sunday review:** do not re-tune the threshold; if the book is adopted, adopt it with the exact wording in the memo, which states 0.60 as a **declared risk-budget constant with its measured plateau** and forbids RULES from calling it a volatility threshold, an optimum, or a fitted value. Ideas 128-130 queued. Result: `research/backtests/2026-09-05_vol-threshold-sweep-on-ewall_B.result.md`, `.memo.md`.
- 2026-09-05 (lane B, idea 129 absolute-bars-disqualify-the-best-insurance) — **ANSWERED; KILL of the claim that PROTOCOL rule 4b's CAGR floor is a risk bar.** Corpus = 3 panels (u56, broad, small) x 3 books (V1u, TOP20, EWall) x 17 published instruments x 2 cost rungs = 306 arm-rows on idea 94's imported simulator, plus a 342-row static-gross ladder control; 3 of 3 reproduction checks exact (run() vs engine.backtest 0.00e+00; idea 94's published EWall+vol60-dg u56@10bps 11.587%/1.133/-16.884% vs 11.6/1.133/-16.9; re-parameterised bars 0/306 mismatches vs H.margins). Two tuned parameters, both bars of 4b itself (phi in {0,0.5,0.6,0.7,0.8,0.9,1.0}, delta in {0.5,0.6,0.7}); all 21 grid points reported. (1) **The floor is the SOLE cause of KILL for 48% of every arm that clears 4b's other four bars** — 27 of 56 corpus-wide, 11 of 23 on the (Sharpe, MaxDD) Pareto frontier — and all 11 Pareto-best victims pass 4a. Indicatively 288 of 1289 published leaderboard rows (22.3%) and 5 of its 6 Pareto-best rows sit in the same class. Idea 95's `theta=0.40/dg` was not an accident. (2) **The floor's victims are out-of-sample BETTER, not worse.** Screened on the IS window alone and read once OOS, floor-only rejects (n=9) score OOS Sharpe 1.160 vs 1.056 admitted (+0.104, Welch t +3.22) and OOS MaxDD -13.4% vs -21.5% (8.1pp shallower, t +8.97), losing only on CAGR (9.1% vs 13.4%, t -7.12) — the axis the floor screens on. Paired by cell: 3/4 on Sharpe, 4/4 on drawdown. (3) **Raising the floor costs drawdown and buys nothing.** phi 0 -> 0.70 at delta=0.60 removes 27 of 56 admissions and moves mean admitted OOS Sharpe by +0.002 while making mean admitted OOS drawdown 1.9pp deeper (-16.6% -> -18.5%); the floor and the drawdown cap pull against each other, and delta is by far the stronger dial (3/29/57 passes at delta 0.50/0.60/0.70). (4) **Rule 8 (three selectors pre-registered before any OOS read): the floor is INERT in selection.** S0 (no screen), S1 (IS 4b screen with the floor) and S2 (floor deleted) pick the identical arm in all 7 of 18 cells where the screens pick — 0 of 18 picks changed — and S1/S2 empty the same 11 cells; S1 OOS 12.7%/1.022/-21.1% vs SPY 0.882/-33.7%, ungated control 0.762, RULES v1 0.451 (beats SPY 5/7, v1 7/7, own control 2/7). The screen's apparent gain over S0 is entirely the cells where it declines to pick, not better picking. This reproduces idea 109 (which moved the pick in 8/44 and never for the better) on a disjoint arm family. (5) **The floor is NOT broken and must not be deleted:** on the static-gross ladder it is the sole KILL in 97 of 342 rows and bites only at gross <= 0.80, exactly its designed job. Its defect is that it cannot distinguish a de-grossed lever from a better-shaped defensive book — the victims sit at 0.57 mean gross yet carry higher OOS Sharpe than the admitted set at 0.74. So the floor binds maximally at ADOPTION and not at all in SELECTION; that asymmetry is the finding. **Memo asks the Sunday review for a PROTOCOL rule-4b reporting clause only — an arm clearing the halves bars, the OOS bar and the drawdown cap and failing ONLY the CAGR floor is recorded `4b-defensive`, not KILL, and must state its CAGR shortfall in pp and its mean gross. No bar is moved (rule 7 stands), no RULES change, and the standing 4b candidate `EWall + vol60-dg` is untouched.** Caveats carried: group B is 9 correlated arm-rows in 4 cells, all EWall + a slow trend gate (so the class may be one construction in disguise — filed as idea 133); survivorship (idea 54) runs against the floor's defence; idea 128's shallow IS window biases the screen toward over-admission; idea 38's calendar-day index unfixed on u56/broad; every number t+1 only (idea 126). Follow-ups filed: 131 (replace the CAGR floor with an explicit minimum-gross bar), 132 (why the IS 4b screen changes no pick), 133 (is the defensive class one book in disguise). Script `research/backtests/2026-09-05_cagr-floor-calibration_B.py`, memo `.memo.md`, result `.result.md`.

- 2026-09-05 (cloud, idea 127 four-way-gate-spread-is-a-weighting-convention) — **ANSWERED; KILL of the QUEUE's premise. The four-way gate spread's MAGNITUDE is the ranking signal, not the weighting convention. Rules unchanged; a PROTOCOL reporting clause proposed.** The two rows the queue compares differ in TWO things, not one: ideas 38/56 rank on raw 12-1 momentum and weight literally (`(rank<=n)*g/n`, cash for missing slots), idea 121 ranks on the RULES v1 composite — which already contains a 0.5/1.0 trend factor and a /sqrt(vol20) scaler, i.e. internalises the gates being priced — and renormalises to g. Both reproduce here before anything new was read (38/56 max|diff| **0.0034**, 121 **0.0000**; engine-equivalence 6.9e-18/6.9e-18/1.4e-17; idea 38's calendar-index warning re-checked, 0 weekend rows on all three panels). 1440 arm-rows = 3 panels x 3 signals x 4 gates x 3 conventions x (R10/R20/R40 + EWall) x 2 gross x 2 cost rungs, all reported; exactly two tuned parameters (n, g). **(1) The attribution is decisive**: at a FIXED mom signal the matched spread is **0.362** against the literal **0.355** (+0.007), so the convention explains **-3%** of the published 0.356->0.080 gap and the signal explains **103%** (-0.287), split about half the composite blend (0.362->0.225) and half the vol scaler (0.225->0.079) — idea 81's finding, now on the gate rows. **(2) The convention is exposure TIMING, not level.** For a ranked book the two conventions hold the identical names, so literal = matched x f_t; splitting off a static-gross control gives |timing| median 0.0043 vs |level| median 0.0000 over 480 arms (timing dominates 86%). It is nil on the ranked small books (f=0.99) and large on equal-weight books, where the gate really de-grosses: small EWall+both f=0.419 (0.209 inside SPY drawdowns >10%), literal costs 2.0pp of CAGR and buys **28.1pp of MaxDD** vs matched. **(3) The census answers the queue's question in the opposite direction.** 0 of 44 published gate-claim cells depend on the literal convention (no sign flip, none halves) — 'vol60 is the larger destroyer' is -0.272 literal / -0.285 matched, ideas 49/51's equal-weight gate cost -8.5pp / -6.5pp CAGR. But in **8 of 44** cells the MATCHED value is more than twice the literal one (max **7.03x**), all on u56: d_both -0.020 lit vs **-0.091** mat at n=40, -0.011 vs -0.075 for EWall, and on u56 OOS the literal convention **inverts the ordering** (tau -0.67, every gated book beating ungated, vs 0.00 matched). A u56 gate row quoted literally is a statement about cash, not about the gate. **(4) Rule 8**: 216 cells, (n,g) on IS Sharpe only, OOS read once — 102 beat SPY's 0.882, 171 beat RULES v1, 17 pass 4b, 0 pass 4a; the convention is worth <=0.036 of mean OOS Sharpe on any panel. 0 of 480 small-panel rows pass 4b (idea 121 reproduced). **(5)** Exactly one arm passes 4b in all four (u56/broad x 10/25 bps) cells — `EWall / vol60 / literal / g=0.75`, the standing incumbent `EWall+vol60-dg`, confirmed by a run that was not looking for it. All five pre-registered predictions HELD. Survivorship restated: three current-constituent panels, no level here is an achievable return.
- 2026-09-05 (lane C, idea 123 relative-priceability-floor) — **ANSWERED; the relative floor `dMaxDD >= 10% of the control's |MaxDD|` is proposed as a REPORT-ONLY PROTOCOL clause (exact wording in `2026-09-05_relative-priceability-floor_C.memo.md`) and is a KILL as a selector.** Reproduction first: idea 94's engine-equivalence max|diff| 0.0, idea 97's committed `pricelist.csv` 288/288 rows to 4e-16 and its `grid.csv` 306/306 arm-points bit-identical including both KEEP flags. (1) The re-tabulation: of the 202 published full-window prices, REL(0.10) unprices **53 (26.2%)** — u56 6/70, broad 16/68, small 31/64; by book V1u 42.7% / TOP20 30.1% / EWall 18.2% (the same concentrated-book address idea 122 found for the sign test); by tier the **per-name stop loses 21 of its 23 published rows (91.3%)**, the gate tier 32.7%, the book-level DD rule 4.6%. All five φ reported (0.02/0.05/0.10/0.20 remove 8.9/15.3/26.2/50.5%). (2) The case FOR the clause: the rows it removes keep their IS→OOS price **sign only 49.1% of the time (median |Δrate| 2.701) against 88.4% (0.891) for the rows it keeps** — it separates measurements from coin flips, which idea 94's absolute 0.10 pp floor cannot. (3) Two queue premises REFUTED. A negative price is NOT a shallow-denominator artefact off the small panel: 48 of 76 negatives removed (63.2% against an 80% bar); idea 119's own `small/V1u` cell is 11 of 11, but u56 is 1 of 9 full and 5 of 16 OOS with survivors buying 7–29% of the control's drawdown, and ρ(|rate|, denominator size) is +0.392 u56 / +0.143 broad / −0.412 small. And the floor does not rescue idea 97's tier sentence — it **weakens** it: clause-rows true C1 33/54→24/54 (every loss a V1u cell; small-panel C1 2/6→0/6, median small gate price 0.350→0.551 against a 0.279 lever), C2 unchanged 38/54, exact 4-tier order 20/54→16/54, and C3's 50/54→52/52 is true only because the stop becomes unpriceable and idea 97's convention ranks an unpriceable tier last. (4) Rule 8 (eligibility on 2009/2011–2016, 2017–2026 untouched): Srel(0.10) changes 2 of 18 picks and loses **0.019 of mean OOS Sharpe (0.710 vs idea 94's S1 0.729)** with regret 0.441→0.602; φ=0.02/0.05 change nothing; φ=0.20 leaves 4 cells with no eligible arm (0.625). No selector beats the no-instrument control (0.762) or SPY (0.882/15.45%/−33.7%); RULES v1 OOS Sharpe 0.747/0.576/0.581. Same report-only verdict idea 122 reached on the sign test, by an independent screen. (5) KEEP paths: all 306 arm-points carry idea 97's flags unchanged (97 pass 4a, 29 pass 4b, every one an already-published gated `TOP20`/`EWall` arm) — **no new KEEP candidate**; this run tests a publication rule, not a book. Survivorship: current-constituent panels, worst on the small list, so its gate prices are flattered; the calendar-day index (idea 38) is unfixed for u56/broad and cancels inside every cell.


- 2026-09-05 (cloud, idea 133 is-the-defensive-class-one-book-in-disguise) — **ANSWERED; the `4b-defensive` class is NOT one book in disguise, and it is not an exposure band either. PROTOCOL should name the CLASS, not the book. Rules unchanged.** Idea 129's census machinery (`bars_win`, `margins_at`, `fails`, `pareto_front`) and idea 94's simulator are IMPORTED, so this is literally the same census on a wider corpus: 3 panels x 9 books (EWall, V1u, R5/R10/R20/R40, S3-25/S3-50/S4-50) at MATCHED gross 0.75 x idea 94's 17 overlays x 2 cost rungs = **816 arm-rows**, plus a 240-row static-gross ladder control; two tuned parameters (n, sleeve fraction f), all values reported. Checks before any new number: engine-equivalence **0.000e+00** on all 24 ungated books, and all **306** of idea 129's rows reproduce arm-for-arm (max|diff| Sharpe **2.2e-16**). **(0) The premise was already an approximation of its own source**: of idea 129's 11 Pareto-best floor-only KILLs, 10 are EWall+gate+dg and the 11th is `broad/EWall/25bps/ddctl-8/.5/recover` at 0.661 gross; the wider floor-only set is 26/27 EWall but only 16 dg, with 2 rw and one TOP20. **(1) On the widened corpus the class is 138 of 816 rows across 6 books and 61 distinct (book, arm) constructions — 112 of them NOT EWall, 122 not 'EWall+gate+dg', 28 matched-gross.** **(2) The existence proof needs no overlay at all**: `S3-50/control` and `S4-50/control` — the ranked top-20 book blended 50/50 with a momentum x risk-parity sleeve of (TLT, GLD, UUP[, DBC]), at full matched gross 0.750 — are 4b-defensive on BOTH large-cap panels: u56 10.1%/**1.276**/-12.2% (OOS 1.241) and 10.4%/1.259/-13.1%, broad 9.9%/1.108/-13.1%, each killed by a CAGR shortfall of **0.3-0.8 pp/yr** while holding about a third of SPY's drawdown (SPY 15.2%/0.889/-33.7%). **(3) The Pareto-best members are the sleeve, not EWall**: of 17 class members on their cell's (Sharpe, MaxDD) frontier, S3-50 10 / EWall 4 / S4-50 3; best is u56/S3-50/ebud-0.10 at 9.9%/**1.291**/-11.1%, OOS 1.277. Idea 129's 11 EWall rows survive as the subset a corpus with only one diversified book could see. **(4) The '~53% gross' reading dies**: the static-gross ladder puts 88 of 240 rows into the class spanning ALL EIGHT books at gross 0.297-0.750, so low gross is SUFFICIENT to enter the class but NOT NECESSARY — 51 of 138 real members sit at full gross >= 0.74. The class is defined by the floor, not by a construction. **(5) Rule 8 (S0/S1/S2 fixed before any OOS read): the floor is no longer inert.** S1 != S2 in **3 of 6** cells, breaking idea 129's 0-of-18; mean OOS 11.6%/0.939/-21.8% (S0), 12.4%/1.111/-17.3% (S1), 10.1%/**1.124**/**-13.5%** (S2) — deleting the floor buys +0.013 of OOS Sharpe and 3.8pp of OOS drawdown for 2.3pp of OOS CAGR, and 3 of 4 S2 picks are themselves defensive. Both screens admit NOTHING on the small panel. **(6) No new KEEP**: 88 of 816 rows pass 4b but only two constructions pass in all four (u56/broad x 10/25 bps) cells — the incumbent `EWall+vol60-dg` and `EWall+band3-rw` — so six new books produced no new cross-cell passer. Predictions: P1 HELD, P2 **FAILED** (members at full gross), P3 **FAILED** (sleeve majority), P4 HELD, P5 HELD. Proposed clause: a `4b-defensive` record must state its CAGR shortfall in pp AND its mean realised gross, because a full-gross diversified book and a 0.52-gross trend book are different objects. Survivorship restated: three current-constituent panels; sleeve statements are two-panel statements; no level here is an achievable return.
- 2026-09-05 (lane B, idea 133 is-the-defensive-class-one-book-in-disguise) — **INDEPENDENT SECOND RUN. Lane B and the cloud lane ran idea 133 concurrently without seeing each other's work and reached the SAME verdict on disjoint corpora (2244 rows / 8 books / 3 gross modes here, 816 rows / 9 books / matched-0.75 there), including the same two cross-cell 4b survivors (`EWall/vol60-dg`, `EWall/band3-rw`) and the same finding that the class's Pareto front is sleeve books rather than EWall. Treat the two as a replication, not a duplicate. ANSWERED; KILL of the premise. Idea 129's `4b-defensive` class is a CLASS, not a book, and the construction idea 129 named is a mediocre member of it. Rules unchanged; no new KEEP candidate; the standing 4b candidate `EWall + vol60-dg` reproduces exactly and is untouched.** Idea 94's simulator and idea 102's sleeve construction were IMPORTED, not re-implemented, and five reproduction checks ran before anything new was read: `book_targets` vs `H.targets` **0.000e+00** over 3 books x 6 gates x 2 conventions, `H.run` vs `engine.backtest` **0.000e+00**, idea 94's published `EWall+vol60-dg` u56@10bps **11.587%/1.133/-16.884%**, and idea 129's **entire 306-row grid re-run inside this corpus at 306/306 matched, max|d Sharpe| 2.2e-16, 0/306 `floor_only` and 0/306 `pass4b` mismatches**. Corpus 2244 rows = 3 panels (u56, broad, small) x 8 books (V1u, TOP5/10/20/40, EWall, SLV25, SLV50) x 17 arms x 2 cost rungs x 3 gross modes, all reported; exactly two tuned parameters (n in {5,10,20,40}, f in {0.25,0.50}), with gross mode a pre-registered reported convention (native / mean gross forced to 0.53 / forced to 0.75), achieved 0.529-0.530 and 0.742-0.751. **(1) H_one is refuted decisively.** The class has **531 members across SIX of eight books**; **37 (7.0%) are `EWall + slow trend gate + de-gross` and 494 (93.0%) are not**; 431 of 531 also pass 4a. **(2) The claim idea 129 actually made — about the (Sharpe, MaxDD) Pareto front — inverts.** Within-cell (idea 129's convention) 118 of 138 floor-only Pareto rows are not EWall; across ALL books within (panel, cost, gross), the frontier the question needs, the front is **47 rows of which `EWall+slow-gate-dg` holds 2, ranking 27th and 28th by Sharpe**. The top 26 are macro-sleeve books: `broad/SLV50/ebud-0.10` 8.7%/**1.292**/-13.6% (OOS **1.311**), `broad/SLV50/vol60-dg` 7.6%/1.277/-10.6% (OOS **1.338**), `u56/SLV25/band3-dg` 7.2%/1.253/-9.6% (OOS **1.369**) — all at native gross 0.59-0.74, i.e. not de-grossed books at all. Class means by book at native gross: SLV50 OOS 1.255, SLV25 1.164, TOP40 1.154, **EWall 1.112**, TOP20 1.034; corpus-wide sleeve members OOS 1.208 (n=325) vs EWall 1.102 (n=96) vs ranked 1.066 (n=110). **(3) Gross does real work but is not the whole story.** Forcing 0.53 mean gross moves TOP20 from 1 member to 34 and TOP40 from 10 to 52 (class 139 -> 278), so de-grossing a ranked book MANUFACTURES membership — idea 129's own warning, now measured. But `V1u` and `TOP5` join at **no** gross level (0 of 276 rows each; they fail the halves and OOS bars too) and `SLV25`/`SLV50` are members at **every** level (73.5/97.1/76.5% and 76.5/76.5/77.9% at native/m53/m75), while class members average MaxDD -13.3% at Sharpe 1.12 against non-members' -25.2% at 0.76. **(4) Rule 8 (four selectors fixed in writing before any OOS read; 18 cells pooling all books): the class is prospectively SELECTABLE, and its picks are not one construction.** S3 — argmax IS Sharpe among IS-window `4b-defensive` arms — gives OOS **9.1%/1.233/-14.3%** and the best mean OOS rank (**15.9 of 136**), against S0 no-screen 12.5%/0.958/-23.7% (41.6), idea 129's S1 floor-kept 13.0%/1.069/-20.9% (52.9) and S2 floor-deleted 10.3%/1.192/-16.6% (26.0); S3 beats SPY 12/12, RULES v1 12/12 and its own control 11/12 where it picks. SPY OOS 15.45%/0.882/-33.72%, RULES v1 OOS 4.86%/0.451/-25.30%, ungated EWall control OOS 0.952. **S3's 12 picks: SLV50 x7, SLV25 x3, TOP20 x1, TOP40 x1, EWall x0.** All six small-panel cells are empty for S1/S2/S3; the small panel contributes **0 of 531** class members at any gross level. **(5) Both KEEP paths on all 2244 rows: 4a 1053, 4b 144.** The only arms passing 4b in all four (u56/broad x 10/25 bps) cells at native gross are `EWall/band3-rw` and `EWall/vol60-dg`, **both already in idea 129's grid** — this run confirms them and discovers nothing new. The best sleeve arms are not candidates: they miss the CAGR floor by 2.0-5.5 pp/yr, which is what puts them in the class. **Proposed to the Sunday review (no file modified):** adopt idea 129's PROTOCOL rule-4b reporting clause with its "may be one construction in disguise" caveat **deleted as tested and false**, and with **mean gross AND book construction** made mandatory on every `4b-defensive` row (exact wording in the memo); no bar is moved, rule 7 stands, RULES v1 and the standing candidate are untouched. **Caveats carried:** the sleeve books inherit every open question about the sleeve (idea 100 is a PARK, idea 102 found GLD carries 53% of its return, ideas 105/106 open) and nothing here argues for adopting one; class members earn 7-9% CAGR against SPY's 15.4% and are defensive sleeves, not capital-worthy books; survivorship (idea 54) runs TOWARD this finding since absent delistings inflate the fully-invested controls most; idea 128's shallow IS window biases every IS screen toward over-admission; idea 38's calendar-day index cancels inside each cell; idea 126 — t+1 only; a matched-gross row is a different instrument from its native row and is never quoted as one. Script `research/backtests/2026-09-05_defensive-class-census_B.py`, result `.result.md`, memo `.memo.md`, grid `.grid.csv` (2244 rows), walk-forward `.walkforward.csv` (72 rows), console `.console.txt`.
- 2026-09-05 (lane C, idea 128 shallow-window-prices-are-not-prices) — **ANSWERED; DOUBLE KILL, no RULES change and no new KEEP candidate.** Idea 94's harness imported unchanged (engine-equivalence **0.000e+00** on both panels) and idea 122's published table reproduces to the row: 192 arm-rows, 138 priceable, dMaxDD_IS ≤ 0 in **40**, dMaxDD_OOS ≤ 0 in **0**, 4a **54**/192, 4b **29**/192. **(1) The queue's premise is a TAUTOLOGY, not a measurement.** `published` conditions on dMaxDD_full > 0.10 pp, and **103 of 104 book troughs (and SPY's) lie inside 2017-2026**, so `dMaxDD_full == dMaxDD_OOS` **exactly in 132 of the 138 published rows** (corr 0.998, against 0.755 for the IS half). Filtering on the full-sample denominator *is* filtering on the OOS denominator; the 0-of-138 is arithmetic. Unconditionally at 10 bps over all 96 arm-rows: dMaxDD > 0 in **58 IS / 71 OOS / 71 full** — the real gap is 60% vs 74%, and "the 2009-2016 window cannot measure a drawdown instrument at all" should be withdrawn. **(2) The 90% depth threshold DOES NOT EXIST.** 1,640 rolling windows (L ∈ {2,3,4,5,6} y × monthly starts × 2 universes), each scored by its own SPY MaxDD against the share of that panel's 51 arms with dMaxDD > 0. The relation is positive — pooled L=4 slope **+0.0051/pp, R² 0.13, deciles 0.63/0.60/0.54/0.51/0.63/0.67/0.57/0.70/0.68/0.69 over depth 9.7→33.7 pp — but its significance is an overlap illusion: **t +7.07 on all 328 overlapping windows and t +0.04 on the 7 disjoint ones** (t_disj > 2 in 1 of 15 fits, n=3). And it never gets there: **max frac over all 1,640 windows is 0.833**, 0 of 328 L=4 windows reach 0.90, 1.5% reach 0.80, and the **empirical crossing is undefined at 45 of 45 grid points**; the fitted L=4/p\*=0.90 crossing is **77.5 pp** of SPY drawdown pooled (u56 57.2, broad 144.8), deeper than 2008's −55%, and 44.9–65.3 pp even at p\*=0.80. **Length does not substitute for depth** (matched 28–35 pp band: 0.661 → 0.708 as L goes 2 → 6 y); rule 8's own 9.7-year OOS half scores 0.750/0.729 and its 8-year IS half 0.583/0.625. Per book at 22.1 pp depth the fitted frac is **V1u 0.451 / EWall 0.682 / TOP20 0.720** — the 5-name book is unpriceable at every depth, idea 122's own address for its cost- and panel-axis failures. **(3) Rule 8** (selection on 2009–2016 only, 2017–2026 untouched): S0 hold-the-control **0.937 OOS Sharpe / 12.04% / −22.5%**, S1 idea 94's selector on the full IS half **0.885 / 9.92% / −19.0%**, S2 the same selector on the deepest IS sub-window **0.885 / 9.16% / −18.3%** at L=4 (0.838–0.885 across L) — against SPY **0.882 / 15.45% / −33.7%** and RULES v1 **0.747 / 7.73% / −13.8%**. The depth clause changes **13 of 30** picks and buys nothing; no selector beats doing nothing. The mechanism: the deepest IS sub-window has depth **22.062 pp at every L from 2 to 6 years**, identical to the whole IS half, because SPY's 2009–2016 maximum is the post-warm-up tail of the GFC — inside rule 8's IS window a deeper ruler cannot be bought at any length, which also bounds what idea 111's re-derived split date can achieve. **(4) KEEP paths:** all 192 arm-points carry idea 122's flags unchanged (4a 54, 4b 29, every passer an already-published gated `TOP20`/`EWall` arm); this run tests a publication rule, not a book. Proposed PROTOCOL rule-4 clause is **negative** (window depth does not rescue a drawdown denominator; never quote an IS price as if it were the OOS measurement) — exact wording in `research/backtests/2026-09-05_shallow-window-prices-are-not-prices_C.memo.md`. Survivorship: current-constituent panels, so levels are optimistic and a survivorship-free panel would deepen every window — moving the crossing, not the direction. Index: with BTC/ETH excluded both panels load on a **trading-day** index (3,679 one-day / 846 three-day gaps), so idea 38's calendar-day defect does not bite this run. NOTE: the queue contains **two ideas numbered 128** (the other is `threshold-plateaus-are-the-general-case`, still Open) — needs renumbering by whoever owns the file. Result: `research/backtests/2026-09-05_shallow-window-prices-are-not-prices_C.result.md`.

- 2026-09-05 — Research cloud (idea 134, sleeve-f-that-clears-the-floor): **ANSWERED — YES, the floor is one dial away, but PARK not KEEP.** Idea 133's harness and idea 94's simulator were imported rather than re-implemented: engine-equivalence `max|diff| = 0.0` on all 13 ungated books, and this run's 136 f=0.50 rows reproduce idea 133's arm-for-arm to 2.2e-16 before anything else was read. The premise audit corrects the queue in two places: `SLV50` is not a book name (the f=0.50 sleeve books are `S3-50`/`S4-50`), and the '2.0-3.0 pp/yr' floor shortfall is the tail — over idea 133's 101 sleeve class-members it runs -4.10/-1.29/-0.06 pp/yr (min/median/max). 2 panels x 13 books x 17 arms x 2 cost rungs = **884 arm-rows plus a 260-row static-gross ladder, all reported**; TWO tuned parameters (sleeve fraction f, 6 values all run; sleeve asset set S3 vs S4, both run). **(1) The answer is yes.** 4b passes by f on broad@10bps are 0/0/5/9/16/22/4 for f=0.00/.05/.10/.15/.20/.25/.50 and on u56@10bps 9/16/23/27/29/28/3; 250 of the 680 swept `f<=0.25` sleeve rows pass, 272 of 884 overall. The count peaks at **f=0.15-0.20**, not at the bottom of the sweep (prediction P2 FAILED). **(2) What f buys is an exchange rate.** The mean CAGR-floor margin falls monotonically +1.89 -> +0.08 -> -1.88 pp/yr and the mean drawdown-cap margin rises monotonically -1.11 -> +3.52 -> +7.84 pp as f goes 0.00 -> 0.25 -> 0.50: about **1 pp of CAGR margin per 2.5 pp of drawdown margin** over 0.00-0.25. Below f=0.10 the cap binds, above f=0.25 the floor does (fail_CAGR 11 -> 129 rows). **(3) The ladder control splits the panels, and this is the run's real finding.** On u56 f is just the exposure dial in disguise — `R20` alone passes 4b at m=0.80. On **broad@10bps the ladder can never pass**: `R20` fails the CAGR floor at m=0.60 and the drawdown cap at every m from 0.80 to 1.40, with Sharpe flat at 0.973-0.978 across the whole ladder (idea 66 again), while a sleeve at f>=0.10 does pass. The sleeve **moves the (CAGR, MaxDD) frontier**; gross only slides along it — the first instrument the project has found that is not a point on the gross ladder (new idea 139). **(4) The wall.** broad@25bps admits **0 of 442 arm-rows and 0 of 130 ladder points** at any f; exactly one row clears the halves, the OOS bar and the cap and fails only the floor (`S4-50/band3-rw`, by 0.99 pp/yr), every other failing a Sharpe bar. Idea 82's ~7.5-10.5 bps breakeven reappears as a hard edge (new idea 137). **(5) Best member** `S4-25/band3-rw` (0.75 x ranked top-20 + 0.25 x momentum-vote x risk-parity over TLT/GLD/DBC/UUP, 3% band, matched gross) passes 4b on 3 of 4 cells — u56@10 13.0%/1.192/-16.9% (halves 1.214/1.181, OOS 1.235), u56@25 11.6%/1.070/-17.1%, broad@10 13.3%/1.055/-17.1% (also 4a) — and dies broad@25 on H2 0.791 and OOS 0.861, not on the floor. 35 (book, arm) pairs pass 3 of 4 cells; **0 pass 4**. **(6) Rule 8**: parameters chosen on 2009-2016, OOS 2017-2026 read once. S1 beats the ungated control, SPY (OOS 0.882) and RULES v1 in 4 of 4 cells (OOS Sharpe 1.047/0.891/1.202/1.086) but mean OOS Sharpe is S0 1.065 / S1 1.057 / S2 1.065 — **the IS 4b screen is worth -0.008**, and S1 != S2 in 4 of 4 cells against idea 129's 0 of 18. **PARK**: cost-fragile on the panel that matters, blocked on ideas 105/106 for any 'macro sleeve' RULES wording, and biased in its own favour by survivorship (absent delistings inflate the equity leg more than the ETF sleeve, i.e. exactly in the direction that produced this answer). Rules unchanged. Memo carries the exact wording the Sunday review would need if 105/106 clear it. New ideas 137, 138, 139.

- 2026-09-05 — Research cloud (idea 132, why-the-IS-4b-screen-changes-no-pick): **ANSWERED — explanation (A), and a KILL of PROTOCOL rule 8's IS 4b screen as a SELECTOR. It survives only as an ABSTENTION rule. Rules unchanged, no new book KEEP.** Idea 129's corpus was re-derived rather than read: **306 of 306 rows reproduce to max|diff| 2.2e-16** across CAGR/Sharpe/MaxDD/IS and OOS metrics/gross, with engine-equivalence 0.0 on all three panels, before any new number was looked at; the screen under test is literally the code that produced the result under test. TWO tuned parameters (the IS selector statistic, 4 values all run: IS Sharpe, IS Calmar, IS MaxDD, IS CAGR; and the screen, 3 values: none / 4b-with-floor / 4b-floor-deleted) = **216 picks, all reported**. **(1) The premise reproduces**: IS-Sharpe/S1 picks in 7 of 18 cells, declines in 11, and moves the arm in 0. **(2) (A) wins, decisively.** Across all selectors and both screens there are **12 moved picks and all 12 come from non-Sharpe selectors**: moves-where-it-picks are IS-Sharpe 0, IS-Calmar 0, IS-CAGR 3, IS-MaxDD 5; the unscreened argmax is already admissible in **100%** of non-empty cells for IS-Sharpe and IS-Calmar against 57% for IS-CAGR and 29% for IS-MaxDD. The refinement the queue did not anticipate is that the immunity is **not Sharpe's alone but the risk-adjusted-return selectors' as a class** (new idea 141). **(3) What the screen removes is a cell, not an arm.** Median admitted per cell is **0 of 17** and the set is non-empty in only **7 of 18** cells: it admits nothing in all 6 small-panel cells, all 4 V1u cells and u56/TOP20@25bps, and inside the 7 it does admit it admits 1-12 arms and always the one the selector wanted. **(4) The apparent OOS gain is abstention, and the number is exactly zero.** On the SAME 216 picks, S1 - S0 in mean OOS Sharpe reads **+0.327** the way idea 129 read it (averaged over the cells the screen entered), **+0.055** with an explicit fallback to the cell's ungated control in the 11 cells it declines, and **+0.000** paired on the 7 cells where every screen picks — exactly zero for IS-Sharpe and IS-Calmar because it is the same arm and therefore the same return series (K_Calmar +0.311/+0.028/+0.000, K_MaxDD +0.412/+0.129/-0.012, K_CAGR +0.274/-0.012/-0.001). **(5) The CAGR floor is inert**: S1 and S2 pick the identical arm in every cell for 3 of the 4 selectors. **(6) Rule 8 was read once** on 2017-2026: 53 distinct picked arms, **18 pass 4a, 7 pass 4b full-sample and the same 7 pass 4b re-measured on the OOS window alone**, every one a pre-existing leaderboard book; picks beat SPY (OOS 0.882) in 6 of 18 cells and RULES v1 in 12 of 18. **(7) Reported as found, not acted on:** the incumbent IS-Sharpe is the **worst** of the four selectors on the paired cells (1.022 vs IS-MaxDD 1.052, IS-CAGR 1.047, IS-Calmar 1.040) — 7 cells cannot order four selectors, which is new idea 142. **Proposed to PROTOCOL rule 8 as a REPORTING clause** (no selector change, no book change): every screened walk-forward must report admitted/total arms per cell, whether the unscreened argmax was already admissible, and the screen's OOS advantage paired or under an explicit fallback — an advantage averaged only over the cells a screen chose to enter must not be quoted. Caveats carried: survivorship on all three current-constituent panels (idea 54), idea 128's shallow IS window (which biases the screen toward admitting too much, i.e. against this run's own P3, which held anyway), t+1 execution only (idea 126). New ideas 140, 141, 142.
- 2026-09-05 (research sprint lane B, idea 131) — **KILL of the gross-level swap.** Idea 129 closed by diagnosing 4b's CAGR floor as "really a gross-level filter"; tested directly on the same 306-row corpus and 342-row ladder, that is false. **No gamma does both jobs (0 of 34 grid points):** admitting all 27 floor-only victims (11/11 Pareto-best) needs gamma<=0.51, where 23 static-gross ladder points come in against the CAGR floor's 10; emptying the ladder needs gamma>=0.68, where none of the 11 survive. The families overlap on mean gross (victims 0.519-0.750, core-admissible ladder 0.075-0.673) and the axis is perverse inside the overlap (0.53->0.54 discards 6 Pareto books and 0 ladder points). Spearman(gross, CAGR) is +0.245 on real books but +0.691 on the ladder, i.e. the substitutability holds only where the two are the same dial. Rule 8: the gross bar is a third inert screen — S3==S1==S2==S0 at 1.022 OOS Sharpe paired, 0 of 7 picks moved at every gamma<=0.65. Two things survive: gamma in [0.57,0.61] weakly dominates the published floor on all three of the floor's own criteria, and a post-hoc diagnostic (cv of daily gross: victims 0.268 vs ladder 0.014, 11/11 Pareto-best above every ladder point) separates where the level cannot — queued as idea 143, deliberately not adopted in the run that found it. Rules unchanged; a negative PROTOCOL reporting clause is proposed for Sunday review. Memo: `research/backtests/2026-09-05_gross-as-the-missing-third-bar_B.memo.md`.

- 2026-09-05 (lane B, idea 93 absorbing-state-audit-of-every-state-dependent-rule) — **ANSWERED: the absorbing pathology is CONFIRMED and made exact, its DIAGNOSIS IS CORRECTED, and the fix idea 93 proposed is a KILL as an improvement.** 3 books (RULES v1 as live, idea 2's `CAND20`, idea 72's `EWall`) x 2 universes x 2 cost rungs x two instrument families: `DDCTL` (idea 22/40's book-level drawdown control, 5 reset arms x D in {4,6,8,10,12,15}% x k in {0,0.25,0.50,0.75} + control = 1452 points) and `STOP` (idea 9's per-name trailing stop x 4 re-entry arms x idea 75's 2 arming arms x s in {10,15,20,25}% + control = 396 points), plus a 228-point static-gross ladder — **all reported**. Exactly two tuned parameters (D, k; s alone in STOP); reset/re-entry/arming are ARMS and every rule-8 selection is confined inside one arm. Harness sanity: both state-machine controls reproduce `engine.backtest` at max|diff| **0.0e+00** in all 12 cells and reproduce the live book and idea 2's standing candidate to the decimal. **(1) The absorbing state is exact, not a tendency:** at k=0.00 with equity-conditioned re-entry (`high` or `recover`), **140 of 140 arms that ever armed never released**, mean capture **724 rebalances** (max 919 = the entire remaining sample) — the cut is what freezes the equity the release condition reads, so the rule fires once and the book is in cash for life. The 4 exceptions never armed. At k>0 episode length scales as ~1/k (`high` 724/218/109/69 at k=0.00/0.25/0.50/0.75). **(2) The diagnosis in idea 22's wording is WRONG and this run says so on its own pre-registered prediction.** I predicted a per-name stop reading the NAME's price would be safe because the name keeps trading; it is not. Release rate / mean block: `free` 0.999 / 2.6 days, `spy200` 0.999 / 35.3, **`nhigh` 0.951 / 250.9** (8.4 names permanently out), **`bookhigh` 0.863 / 285.4**. Both **high-water-mark** arms are near-absorbing and one non-HWM arm is endogenous — so the operative property is *a re-entry condition demanding a state variable regain a running maximum*, not "a function of the book's own equity". Idea 22's wording exonerates `nhigh`, which is the project's second most absorbing rule. **(3) The fix removes the pathology and buys nothing:** exogenous release absorbs **0 of 288** in all three arms, but `spy200`'s mean dSharpe is **-0.2633** against `high`'s **-0.2636** — Sharpe-identical to what it cures — and it makes MaxDD **deeper** in **140 of 288** arms (mean gain -0.0325) because it re-levers as soon as SPY reclaims its 200d, mid-drawdown, at 19.8x/yr turnover vs the control's 15.5x. On idea 74's axis every family is dominated by the plain static-gross lever (0.537 pp/pp) in 67-91% of arms. **(4) Both KEEP paths: 0 of 1824 treated arms pass 4a and 4b together, and 0 of 12 cells has an arm that converts a 4b failure into a 4b pass.** All 69 4b arm-passes sit in the single cell whose control already passes; 20 of the 48 `DDCTL` passes never fired at all and all 28 that fired have negative dSharpe (best -0.0028); in broad/EWall@10bps the control passes and 0 of 152 arms do. 4a's 598 passes are de-grossing against a low-return live book (mean gross 0.629 vs 0.743; only 45 of 598 beat their own control). **(5) Rule 8:** 78 walk-forward selections, params on 2009-2016 only, OOS 2017-2026 read once — S1 moves materially in 65 and **beats its control in 15**, mean OOS Sharpe **-0.171** (DDCTL -0.318, beats SPY 6 of 30; STOP -0.033, beats SPY 24 of 48); the 4b-aware selector picks **NOTHING in 59 of 78**. Idea 75's conditional arming is mildly positive (+0.0328 mean over 192 paired arms) but concentrated on the absorbing arms (`bookhigh` +0.073, `nhigh` +0.045, non-absorbing +0.007) — it helps by firing a broken rule less often. **Two PROTOCOL clauses proposed** in `research/backtests/2026-09-05_absorbing-state-audit_B.memo.md`: prohibit high-water-mark re-entry outright and require every state-dependent arm to report armings/releases/escape rate/terminal state; and forbid quoting de-absorption as an improvement. **No RULES change, no new KEEP, no book proposed.** Footnote for the corpus: idea 9's published stop numbers should be re-read against §4 of the result before being quoted as a stop's numbers rather than a partial-cash book's.
- 2026-09-05 — Research lane C, idea 145 `weak-dominance-bands-as-the-calibration-output`: **ANSWERED — publish the band, as an INDIFFERENCE interval; the idea's own premise is KILLED.** Corpus reproduced exactly from ideas 129/131 (306 arm-rows + 342 ladder rows; four reproduction checks PASS, including one that caught an inverted drawdown comparison in an earlier version of this run). Each of 4b's five bar coefficients swept ONE AT A TIME with the other four pinned, endpoints computed analytically (each bar is monotone in its own coefficient, so 'unbounded' is a proof, not a scan artefact); all grid points reported. Bands: H1 1.00 in [0.9974,1.0453], H2 1.00 in [0.9829,1.0213], OOS 1.00 in **(-inf,1.0520] (unbounded)**, MaxDD cap 0.60 in **[0.5979,0.6012] (width 0.0033)**, CAGR floor 0.70 in [0.6965,0.7020] (width 0.0054) — **three orders of magnitude apart, published point interior in 5 of 5**. (a) The premise dies: **phi has no strictly dominating value in its own units** — idea 131's gamma in [0.57,0.61] was a property of SWAPPING THE STATISTIC (CAGR -> mean gross), not of re-calibrating a coefficient, and sweeping phi the three criteria are strictly opposed at every step. (b) **The drawdown cap, not the CAGR floor, is 4b's largest sole cause of KILL**: 47 rows vs 27, 61.8% vs 48.2% of arms clearing the bar's own other four — ideas 129/131/135 are all framed on the smaller instrument. (c) **The OOS Sharpe bar excludes nothing on this corpus**: 0 sole victims and 0 sole ladder exclusions, and its indifference band is unbounded below, i.e. deleting it changes neither the admitted set nor the leakage (reported, NOT recommended as a deletion on one corpus). (d) Bands are separable only in a neighbourhood: phi's band is identical for every delta>=0.60 and delta's for every phi<=0.80, and outside that both blow up because the joint screen has emptied — so **a wide band can mean an inert bar, and width is meaningless without the sole-victim count** (idea 128's trap, reached from the other side). (e) The dominance test only orders a bar when its criteria are co-monotone: on the Sharpe bars the ladder control excludes nothing, and H2 comes out dominated in BOTH directions, i.e. undetermined. Rule 8: 4 of 4 IS-derived bands contain the published point and overlap the full-sample band (DD and CAGR identical to 3dp); **all 12 in-band coefficient sets move 0 of 18 picks** and leave OOS at 12.7%/1.022/-21.1% (SPY 15.45%/0.882/-33.7%; RULES v1 4.77%/0.480/-19.7%; ungated control 1.051) — an argument FOR quoting the band, since the point implies precision the OOS record does not contain. NOBARS moves 11 picks but is identical paired on the 7 entering cells, so the screen is abstention rather than selection for a fifth time, and its picks beat the ungated control only 2/7. Both KEEP paths at every band edge: 4a 97/306, 4b 29/306, both 6/306 — unchanged; only ladder leakage moves (10 -> 9 at three tighter edges). Proposed 4b and rule-8 wording in the memo. Rules unchanged, no book proposed, no KEEP candidate.

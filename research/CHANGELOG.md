- 2026-09-16 (lane B, idea 1012 is-the-4b-SHARPE-LEG-DECIDABLE-AT-ALL-at-the-record-s-SAMPLE-LENGTH) — **ANSWERED = YES on the basis 4b actually tests. KILL x4 (1001's SE BASIS, its 0-of-45 INTEGER, the "unreachable tape" reading, and decidability as an ALPHA FILTER) + CONFIRM 1001's arithmetic exactly + KEEP a PROTOCOL LEG NOISE BASIS clause (proposed, not applied). Gates 7 of 9, hypotheses 3 of 5, and both gate failures and both hypothesis failures are reported with their arithmetic. No book promoted, no book KEEP claimed; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.**

  **WHAT WAS ASKED.** Idea 1001 found 0 of 45 books clear 1 SE of the comparand's own bootstrap noise on BOTH 4b Sharpe legs, and 0 of 45 at 2 SE. Invert it: solve for the sample length at which a leg WOULD clear 1 SE, and report whether any tape the record can reach makes the leg decidable. Max 2 params (SE estimator, length ladder).

  **WHAT HAD TO BE SAID BEFORE ANY RESULT.** PROTOCOL 4b's leg is a **DIFFERENCE**, `book half Sharpe - SPY half Sharpe`. 1001 priced that difference against the SE of **one of its two terms**. Book and SPY are read off the SAME days and move together — measured bootstrap **rho 0.8521** — so the difference has a far smaller sampling SE than either term. This is idea 1021's nested-window mismatch one level down. So every number here is published on BOTH bases: **COMP** (1001's, SPY's own SE) and **PAIRED** (the margin's own SE, book and SPY resampled on the SAME block indices).

  **THE ANSWER.** Median **PAIRED/COMP SE 0.5463** — the comparand basis overstates the yardstick **1.83x** (med COMP SE 0.3054 vs med PAIRED SE 0.1675). On the PAIRED basis **20 of 45** books (SHELF 6 of 9, GRID 14 of 36) are decidably POSITIVE on BOTH legs at the record's own 8.82-year half, **today** (**H_PAIR PASS, H_DEC PASS**). The bar this buys, usable now: a leg needs a half-Sharpe margin of **0.1675** (1 PAIRED SE) or **0.3351** (2 SE); on COMP the same bar reads 0.3054 / 0.6107.

  **THE SECOND KILL, and it is the reusable half.** 1001's headline integer is a **coin flip**. Re-drawn 20 times at its own 1,000 draws, the COMP count reads 0 in **10 of 20** draws (range 0..1); the PAIRED count is stable at **18..20**. A 1,000-draw SE has single-draw sd ~0.0075 on a ~0.29 SE, so **any integer thresholded on it inherits that noise** — and the record has been quoting one such integer as a fact. This is also the sole cause of gates G8 and G4 failing, and the declared 0.02 bar was NOT moved after the number was seen.

  **THE THIRD KILL — H_UNDEC FAILED, and the FAIL is the answer.** Pre-registered bar: the median leg needs > 50 years on 1001's own basis. Measured: **14.24 years**, i.e. **1.61x** the record's 8.82-year half — about **five more years of tape**, not centuries (56.94 y at 2 SE; PAIRED 8.82 / 17.71). Robust on the estimator dial (14.24 / 16.77 / 17.06 y at BLOCK21 / BLOCK5 / IID). The inversion is **earned**: SE(L) fits a log-log slope of **-0.5110** (SPY) and **-0.5003** (median book) on the headline ladder, **-0.4943** on a synthetic i.i.d. control (**H_SCALE PASS**).

  **THE FOURTH KILL — decidability is a REPORTING requirement, not an alpha filter (H_WF FAIL).** At the rule-8 IS half length (~1,000 d) nothing is decidable: the DECIDE screen is EMPTY on the clean pool (**0 of 20** B136, **1 of 25** U56 and that one is a memo-selected SHELF book). No chooser can screen on it, so the proposed clause must never be sold as a filter that improves returns.

  **RULE 8 (PROTOCOL rule 8, the declared split 2016-12-31, IS 2009-2016 alone, 2017-2026 read ONCE).** 3 IS-only choosers x 2 screens x 2 pools x 2 panels = 24 points, all published. **OOS 4b 13 of 24, OOS 4a 0 of 24; GRIDONLY (clean) 4b 4 of 12, 4a 0 of 12.** Best clean pick `B136-qroll-q0.12-w1008-d0.50` OOS **14.30% / 1.1572 / -17.31%**; SPY OOS 15.33% / 0.8767 / -33.72%; RULES v2 live at 10 bps OOS 7.88% / 1.1059 / -12.24%. Every pick is a GRID ladder book the record already holds; all 24 fail 4a on drawdown against the live book. **No book promoted and no book KEEP claimed.** A counting bug in the record's own rule-8 idiom (`.fillna(False).sum()` on an object column returns 1, not the count) was found and fixed here.

  **GATES (7 of 9, printed before any hypothesis number).** G1 all 9 SHELF memo triples. G2 fast runner == engine.backtest (6.94e-18). G3 SPY committed triple. **G4a reproduces 1001's published SEs 0.2877 / 0.3350 exactly and its HALFMIN median 0.2102 exactly**; **G4b FAILS** only on the 0-of-45 integer, which this run drew as 1 of 45 — the coin flip above. G5 COUNT halves == baseline._row (0.0). G6 determinism 0.0. G7 the paired estimator is sound on synthetic paths (independent 0.4427 vs quadrature 0.4668; identical paths 0.0). **G8 FAILS** at 0.0283 against a 0.02 bar set below the 0.0075 sd of the object it gates. G9 the sqrt law on a synthetic i.i.d. control, b = -0.4943.

  **CAVEATS, MEASURED NOT ASSERTED.** The decidable LEVEL is block-sensitive — **20 / 15 / 13** of 45 at BLOCK21 / BLOCK5 / IID against COMP's **1 / 0 / 0** — so only the ONE-SIDED claim is banked: the leg IS decidable, and PAIRED separates from COMP, on all three estimators. **The 20-of-45 level is NOT banked.** Decidability is also not skill: a leg can be decidably positive in sample and carry nothing out of sample, which H_WF's failure demonstrates directly.

  **SURVIVORSHIP (rule 9).** U56 and B136 are current-constituent lists, so every book's Sharpe LEVEL and every MARGIN is optimistic. A too-large margin makes a leg look MORE decidable, so every decidable count here is an UPPER bound and every required length a LOWER bound — it works FOR H_DEC and AGAINST H_UNDEC, and both are stated where reported. The KILLs rest on a RATIO of two SEs read off the SAME resamples, where the inflation largely cancels. SPY is a real index series and is not inflated.

  **PROPOSED, NOT APPLIED (rule 6).** `2026-09-16_leg-noise-basis-clause_B.memo.md`: *a 4b Sharpe leg's margin is priced against the PAIRED sampling SE of the MARGIN ITSELF — book and benchmark resampled on the SAME bootstrap indices — never against the benchmark's own SE, which overstates the yardstick 1.83x on the record's own shelf; and any COUNT thresholded on a bootstrap SE is published with the range that count takes over independent re-draws.* **What the record may no longer say** — that 0 of 45 books clear 1 SE of the leg's noise, or that the 4b Sharpe leg is undecidable at the record's sample length.

  Follow-ups filed: 1041, 1042, 1043.

- 2026-09-16 (cloud lane, idea 1021 is-the-SPLIT-POINT-BAND-just-the-OOS-WINDOW-s-OWN-SAMPLING-SE) — **ANSWERED = YES. KILL 1013's split-point band as a cost of PROTOCOL's fiat; KILL the per-end SE (and 1001's comparand SEs) as the yardstick for a NESTED band; CONFIRM 1013's published numbers exactly; KEEP a PROTOCOL rule 8 SPLIT-POINT BAND clause (proposed, not applied). Gates 8 of 9, hypotheses 6 of 6, and the one gate failure and the one awkward direction are both reported with their arithmetic. No book promoted, no book KEEP claimed; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.**

  **WHAT WAS ASKED.** Idea 1013 found a rule-8 pick's published OOS Sharpe moves 0.1747-0.2666 across the 16 legal quarter-end splits even when the PICK never changes, and called that the price of PROTOCOL's fiat. But a shorter OOS window has a larger sampling SE by arithmetic alone. Bootstrap each pick's OWN OOS path at each end and report how much of the band is sampling noise the record would pay anyway. Max 2 params (SE estimator, end grid).

  **WHAT HAD TO BE SAID BEFORE ANY RESULT.** The 16 OOS windows are **NESTED** — every one of them ends on the last day of the tape and starts at a different quarter-end — so the shortest is a SUBSET of the longest and the sixteen Sharpes are enormously correlated. A PER-END SE is therefore not the yardstick, and the null this run builds is the band statistic itself: resample the book's OWN OOS path once, read the SAME nested tail windows off the synthetic path, take max - min. Nesting, window lengths and overlap are inherited exactly; only real time-variation in Sharpe is destroyed.

  **THE ANSWER.** The observed band is median **0.1661** against a constant-Sharpe null of **0.2641** — it sits at the **0.101 percentile** of its own sampling null, with **0 of 108 headline cells above 0.95** (IQR 0.060-0.166). **H_NOISE PASS, H_SHARE PASS at a share of 1.586.** Robust on both tuned dials: median percentile by SE estimator IID 0.101 / BLOCK21 0.144 / NORMAL 0.127 (**H_EST PASS**), by end grid Q16 0.101 / Q28 0.019 / Y8 0.091 (**H_GRID PASS**). On 1013's OWN three pick-invariant cells the published 0.1747-0.1955 sits against nulls of 0.2604-0.2685, percentiles 0.131-0.199. **So the split point is not ADDING swing to the published number; it is REVEALING swing the OOS window has at any single split the record picks.** 1013's "price of PROTOCOL's fiat" is killed as a charge against the protocol.

  **THE TRAP, and it is the reusable half.** A per-end analytic SE (Lo's sqrt(252)*sqrt((1+SR_d^2/2)/n), median **0.3241**) overstates the yardstick by **1.2x**, and the band sixteen INDEPENDENT windows of the same lengths would show (**1.1784**) overstates it by **4.46x** (**H_NEST PASS**). Anyone pricing 1013's band against a per-end SE — or against idea 1001's comparand bootstrap SEs of 0.2877 / 0.3350, as the record has done — is comparing objects with different correlation structures.

  **THE CAVEAT, MEASURED NOT ASSERTED.** The observed band lands BELOW the null's median, which needs a reason before it is banked. Mechanism: the tape's volatile episodes (2020, 2022) sit in the COMMON TAIL that every window shares, so in the real path they are in all sixteen windows and cannot move the band, while a resample scatters them into the head the later ends exclude. Measured: variance density in the common tail is **1.433x** the excluded head's, in **100.0% of 108 cells** (tail 1,936 days, head 945). **The i.i.d. and block nulls therefore over-disperse and H_NOISE is the EASIER call.** The claim banked is the one-sided one — the band does not EXCEED sampling noise, on all three estimators and all three grids. **The 1.59x margin itself is NOT banked.**

  **RULE 8 (PROTOCOL rule 8, the declared split 2016-12-31, IS 2009-2016 alone, 2017-2026 read ONCE).** 3 IS-only choosers x 2 panels x 3 cost rungs = 18 points, all published. Best pick 15.60% / 1.293 / -15.59% at 10 bps (0 bps 16.68% / 1.372 / -15.52%); SPY OOS 15.21% / 0.8711 / -33.72%; RULES v2 live at 10 bps 8.62% / 1.201 / -12.05%, OOS Sharpe 1.276. **OOS 4b 18 of 18, OOS 4a 0 of 18** — the same picks 1035 and 1036 published, as gate G3 requires. **No book promoted and no KEEP claimed**: every pick is a GRID ladder book the record already holds. All 18 fail 4a on drawdown against the live book's -12.05%.

  **GATES (8 of 9, printed before any hypothesis number).** G1 fast runner == engine.backtest (6.94e-18 / 1.67e-16). G2 SPY OOS triple 15.21% / 0.8711 / -33.72%, max|d| 1.70e-04. G3 1013's four published picks 4/4. **G4 reproduces 1013's published band exactly**: three pick-invariant cells at 0.1747..0.1955 against its published (0.1747, 0.1955), worst of six 0.2666 against 0.2666. G5 determinism 0.0e+00 (ladder and bootstrap). G6 the bootstrap SE matches Lo's analytic SE on a synthetic i.i.d. path to 0.005. **G7 FAILS 107/108** on null centring at a 3-SE bar — at that bar over 108 cells a correct sampler is EXPECTED to fail 0.29 (P(>=1) ~ 25%) and the worst cell is 3.14 SE, but it is recorded FAIL regardless. G8 strict nesting 3/3 grids. G9 the suffix-cumsum Sharpe used for speed == the sliced one it replaces, 2.66e-15.

  **SURVIVORSHIP (rule 9).** U56 and B136 are current-constituent lists, so every LEVEL is optimistic and every 4b count an UPPER bound. The measured object is a RATIO of two bands read off the SAME path, so the inflation is common to numerator and denominator and very largely cancels; where it does not it raises the Sharpe LEVEL, which RAISES the analytic SE (the 1 + SR^2/2 term) and so FLATTERS H_NOISE. SPY is a real index series and is not inflated.

  **PROPOSED, NOT APPLIED (rule 6).** `2026-09-16_rule8-split-point-band-clause_cloud.memo.md`: *where a split-point band is published it is published beside the band the SAME book's own resampled path produces at the SAME nested ends, and it is never compared to a per-end SE or to a comparand SE built from non-nested windows: on the record's own Q16 grid those overstate the yardstick by 1.2x and 4.5x. A band that does not exceed its own nested null is reported as "inside the window's sampling noise", never as a cost of the split-point choice.*

  Follow-up filed: 1040.
- 2026-09-16 (lane C, idea 1039 is-the-END-COMPOSITION-BAND-itself-a-RHO-artefact) — **ANSWERED = NO, AND IT INVERTS. KILL the END COMPOSITION reading of 1036's 0.0191 band; KILL "overlap inflates a published band" (on this tape overlap DEFLATES it ~12x at matched window count); KILL a large part of the same band as a WINDOW-LENGTH fact; CONFIRM 1036's published band and rho exactly; KEEP a PROTOCOL rule 8 WINDOW GEOMETRY clause (proposed, not applied). Gates 10 of 10, hypotheses 0 of 5, and every one of the five FAILs is reported with its arithmetic. No book promoted, no book KEEP claimed; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.**

  **WHAT WAS ASKED.** Idea 1036 found the record's K-free null sd moves across a 0.0191-wide band (0.0645..0.0836) on WHICH single end is used, and read that as an END COMPOSITION channel "of comparable size to END COUNT", attributing it to the Q16 ends sharing the same 2019-2026 tape (measured cross-end rho 0.8934). Build an end grid whose OOS windows do NOT overlap and report whether the composition band collapses. Max 2 params (end grid, overlap fraction).

  **WHAT HAD TO BE SAID BEFORE ANY RESULT.** A band is `max - min` over K draws, so its size depends on how CORRELATED those draws are. Overlapping windows are nearly the same measurement and cannot spread; disjoint windows can. The arithmetic therefore predicts the OPPOSITE of the queue's framing, and it was declared before the numbers. 1036's 16 windows also differ in LENGTH (1,936-2,881 days), and a shorter window has a noisier per-book Sharpe with no change of content at all — a third channel inside the same 0.0191.

  **THE ANSWER.** The band does not collapse; it TRIPLES. At L = 550, K = 6 it runs **0.2382 / 0.1353 / 0.1266 / 0.0809 / 0.0682** over overlap f = 0.00 / 0.25 / 0.50 / 0.75 / 0.90 — the disjoint grid's band is **3.49x** the 90%-overlap grid's (2.21x at L = 400, 4.29x at L = 700), never the <= 0.5x H_COLLAPSE asked for. **At MATCHED window count the record's own Q16 grid bands 0.0193 against 0.2382 for the disjoint L = 550 grid — 12.3x.** Measured cross-window rho runs **-0.073 -> +0.705** over the same dial (Q16's **0.9046**), so the grid separates what its label says. **H_COLLAPSE FAIL, and the failure is the finding.**

  **THE YARDSTICK, and it is the reusable half.** Every band is read against the band the SAME 18 books' own JOINTLY day-resampled paths produce on the SAME windows (1,000 reps; span, length, count and overlap inherited, only real time-variation destroyed). **14 of 15 points sit at or below the 0.95 bar** — median percentile **0.335**, median observed/null ratio **0.804** — and **the record's own Q16_TAIL band sits at the 0.409 percentile of its own null** (observed 0.0335 against a null median of 0.0361), Q16_FIXLEN at 0.280. **H_NULL is recorded FAIL** on the single breach (L = 700, f = 0.25, median 0.970, worst cell 0.978), where 15 points at a 0.95 bar are EXPECTED to breach about 0.75 times. The bar was pre-registered at ALL points and is scored as written.

  **THE LENGTH CHANNEL.** Length-matching the record's own 16 windows (same starts, every window truncated to the shortest, 1,936 days) moves the band from **0.0335 to 0.0469**, and 1036's OWN convention from **0.0191 to 0.0348 — 1.82x. H_LEN FAIL.** Equalising lengths makes the band BIGGER, because staggered equal-length windows are less nested than tail windows. Both directions are geometry; neither is content.

  **THE TWO SHAPE FAILS, reported as loudly.** **H_MONO FAIL** — 2 violations of a 0.005 tolerance (L400 at f=0.90; L700 at f=0.25), the large one being the same L700/f=0.25 grid that breaches H_NULL, i.e. one grid and not a trend, and in the direction the answer already claims. **H_RHO FAIL on its monotonicity leg only** — the LEVEL leg passes (|rho| <= 0.073 at f = 0.00 on all three lengths, rising to +0.705..+0.790 at f = 0.90); what fails it are wiggles of 0.026-0.028 where rho is statistically zero and a 6-window correlation read over 18 books cannot resolve 0.02. The bar was too tight for the object; it is scored FAIL as written.

  **A CONFOUND STATED UP FRONT, not discovered later.** At fixed (K, L) a lower overlap NECESSARILY spans more calendar, so the grid alone cannot separate overlap from macro heterogeneity. That is exactly why the adjudicable object is the PERCENTILE against a geometry-matched null and not the band.

  **RULE 8 (PROTOCOL rule 8, the declared split 2016-12-31, IS 2009-2016 alone, 2017-2026 read ONCE).** 3 IS-only choosers x 2 panels x 3 cost rungs = 18 points, all published. Best pick 15.60% / 1.293 / -15.59% at 10 bps (0 bps 16.68% / 1.372 / -15.52%); SPY OOS 15.21% / 0.8711 / -33.72%; RULES v2 live at 10 bps 8.62% / 1.2007 / -12.05% (halves 1.232 / 1.176, OOS Sharpe 1.276). **OOS 4b 18 of 18, OOS 4a 0 of 18** — the same picks 1021, 1035 and 1036 published, as gate G4 requires. **No book promoted and no KEEP claimed**: every pick is a GRID ladder book the record already holds. All 18 fail 4a on drawdown against the live book's -12.05%.

  **GATES (10 of 10, printed before any hypothesis number).** G1 fast runner == engine.backtest (6.94e-18 / 1.67e-16). G2 pool 18/18 per panel. G3 SPY OOS triple 15.21% / 0.8711 / -33.72%, max|d| 1.70e-04. G4 1013's four published picks 4/4, max|d| 4.59e-04. G5 determinism 0.0e+00. **G6 reproduces 1036's K=1 placement band EXACTLY: 0.0645..0.0836, spread 0.0191 against its published 0.0645..0.0836 / 0.0191, max|d| 0.0000.** G7 1036's cross-end rho 0.9046 vs published 0.8934. G8 window construction 30/30 — every window exactly L days, every grid exactly K = 6, realised overlap == target to within 1/L, ZERO shared days at f = 0. G9 the cumsum window Sharpe == fsharpe on the slice == the ladder's OOS_Sharpe, 7.33e-15. G10 the null sampler preserves the cross-book correlation (mean|dcorr| 0.0166) and its own rho is +0.040 at f = 0 and +0.735 at f = 0.90 — it inherits the geometry and nothing else.

  **SURVIVORSHIP (rule 9).** U56 and B136 are current-constituent lists, so every LEVEL is optimistic and every 4b count an UPPER bound. The measured object is a RATIO of two bands read off the SAME books over the SAME tape, so the inflation is largely common to numerator and denominator; where it does not cancel it raises the Sharpe LEVEL, which raises the sampling sd of a windowed Sharpe and so INFLATES the null — making H_NULL the EASIER call, as reported. SPY is a real index series and is not inflated.

  **PROPOSED, NOT APPLIED (rule 6).** `2026-09-16_rule8-window-geometry-clause_C.memo.md`: *a band published over a grid of windows states the window COUNT, the window LENGTHS and the OVERLAP between consecutive windows, and is published beside the band the SAME books' own resampled paths give on that SAME geometry; a band that does not exceed it is reported as "inside the geometry's sampling noise", never as an END COMPOSITION, END PLACEMENT or overlap effect.* **What the record may no longer say** — that 1036's 0.0191 shows end COMPOSITION moving the K-free null sd, or that overlap between OOS windows inflates a published band. **What still stands** — 1036's END COUNT clause and its published band and rho, both reproduced here as gates.

  Follow-ups filed: 1044, 1045, 1046.

- 2026-09-17 (lane B, idea 1218 what-IS-the-right-NULL-for-a-LADDER-if-rung-DISPERSION-is-HALF-a-RUNG-s-OWN-SAMPLING-SE) — **ANSWERED = THE CORRELATION CORRECTION IS ARITHMETICALLY RIGHT AND PRACTICALLY USELESS. `d2_corr(k, rho) = d2(k) x sqrt(1-rho)` REPRODUCES THE REALISED RANGE WHEREVER rho IS KNOWN, IT DOES TURN 1205'S UNREACHABLE BAR INTO A REACHABLE ONE (R >= 1 AT 0 OF 12 REAL LADDERS, R_corr >= 1 AT 5 OF 12), AND IT DIVIDES OUT EXACTLY WHAT THE STATISTIC WAS DETECTING: DEGENERATE-VS-LIVE SEPARATION COLLAPSES FROM AUC 1.0000 TO 0.5185.** Verdict KILL (capital). No RULES change, no book promoted, no PROTOCOL edit.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `CORR DRIVER` {D_N, D_POOL, D_SYNTH} x `PANEL` {U56, B136, SMALL} = **9 cells, EVERY ONE PUBLISHED** in `.dialgrid.csv`; all 54 driver rungs x 2 SD bases x 2 k values (216 rows) are in `.corrladder.csv` regardless of what any cell did with them. NOT dials, reported at every value: the rung ladders (N 2->40, pool q 0.02->1.00, imposed rho 0.00->0.95); k {4, 8}; SD BASIS {S_IID, S_BLOCK(63)}; the record's four committed ladders {N, H, GROSS, CADENCE}; the 4a and 4b legs; the rule-8 arm.

  **THE ARITHMETIC, DECLARED BEFORE ANY NUMBER.** For k exchangeable draws with common sampling SD s and pairwise correlation rho the common factor cancels out of every difference, so E[max - min] = sqrt(1-rho) x d2(k) x s. The rho that matters is NOT the daily-return correlation but the cross-book correlation of the SHARPE ESTIMATOR, measured by a PAIRED bootstrap (one resampled day index applied to all k books at once); both are published at every rung and differ by at most 0.03 here.

  **Q1 — THE RATIO AS A FUNCTION OF CORRELATION.** Pooled mean R_corr **0.9858 (z -0.21)** on the 18 rho-imposed synthetic rungs and **0.9333 (z -0.98)** on the 18 real independent-null rungs; R falls with rho_S at 6 of 6 real (driver, panel) cells (Spearman -0.60 to -1.00). **1205's CTRL_LIVE re-measured carries rho_S 0.9582 / 0.9261 / 0.8350** against the 0.8248 that its R = 0.4186 implies if correlation is the whole story — so that shortfall IS correlation and not a defect of d2, and 0.4186 is not a ceiling but one draw from a cell whose rho this run states.

  **Q2 — THE BAR IS CLEARABLE AND BLIND.** GROSS (1189's degenerate dial) vs the live dials: raw range AUC 1.0000, raw R AUC 1.0000, **R_corr AUC 0.5185**. The correction multiplies raw R by 1/sqrt(1-rho_S) = **x167.5 / x157.5 / x129.0** on the three GROSS ladders against x2.7-x5.5 on the live ones, and at rho_S = 0.999964 a rho error of **2.7e-05** doubles R_corr — on a degenerate ladder the corrected statistic is a ratio of two numbers that are both ~0.

  **THE POWER FACT (post-hoc arm, declared as post-hoc).** R is a ONE-DRAW range statistic, so its own null SD is d3(k)/d2(k) = **0.75 / 0.52 / 0.43 / 0.29 / 0.27 / 0.24 at k = 2 / 3 / 4 / 8 / 9 / 12**; a true-null R_corr at k=8 sits inside [0.434, 1.566] 95% of the time. On the record's twelve committed ladders **|z| > 1.96 at 0 of 12** (worst 1.56). The pre-registered per-rung bar H_LAW (+-0.15) was therefore REFUTED on POWER, not on the law — G_LAW passes to 3.4e-03 over 20,000 replications. 4 of 6 pre-registered hypotheses landed as declared; H_DRIVER failed because D_POOL moves rho_S by only 0.042-0.165 (it is a degeneracy driver, not a correlation driver).

  **RULE 8 (mandatory) AND BOTH KEEP PATHS.** Five move-or-stay rules x 12 (panel, ladder) decisions, every bar read on the IS window only (warm-up..2016-12-31), 2017-2026 read once. Mean OOS Sharpe: do-nothing **0.8750** > C_CORR 0.8676 ~ C_CORRBAND 0.8675 > C_RAW 0.8496 > C_ALWAYS 0.8453, at move rates 0.00 / 0.17 / 0.08 / 0.42 / 0.92 — the corrected bar buys back most of what always-moving loses and still loses to holding the anchor. Best rule-8 book anywhere: U56 GROSS -> g 1.00, full 20.79% / 1.1387 / -24.93% (halves 1.2054/1.0940), OOS **22.65% / 1.1623 / -24.93%** against U56 SPY OOS 15.15% / 0.8684 / -33.72% and live RULES v2 OOS 9.42% / 1.2714 / -12.05%; it fails 4b on the drawdown cap (-24.93% against -20.23%). **4a 0 of 60. 4b 16 of 60 rows, which collapse to 4 DISTINCT books (rule 1211's lesson) and all four are the frozen U56 anchor re-selected by a do-nothing rule.** Neither R nor R_corr predicts what moving is worth (Spearman -0.27 / +0.15; mean move value -0.0296 Sharpe, positive at 5 of 12).

  **GATES, all 9 pass, printed before any result:** G_D2 d2(2) == 2/sqrt(pi) to 0; G_D2b d2(8) == 2.847 to 2.0e-04; G_D3 d3(2) == 0.8525 to 2e-03; G_LAW E[range]/d2 == sqrt(1-rho) on equicorrelated normals to 3.4e-03 at the worst rho; G1 fast runner == engine.backtest to 1.4e-17; G2 book() == that path to 2.8e-17; G3 determinism exactly 0; G4 two null books are not the same path (|corr| 0.954); G5 a paired bootstrap of a book against itself reads rho_S == 1 exactly.

  **WHAT THIS RUN DOES NOT SHOW.** The correction assumes EXCHANGEABLE rungs (one common rho) and uses only the mean pairwise correlation, so R_corr on a real ladder is an approximation whose error is not quantified here; rho_S is itself a bootstrap estimate and at rho -> 1 its error dominates. Twelve real ladders, 54 driver rungs, one tape, k <= 12. SURVIVORSHIP (rule 9): U56 and B136 are current-constituent lists and SMALL is the current sub-$2B screen less the documented max_1d_move >= 1.0 exclusion (52 of 715 dropped), so every CAGR/drawdown LEVEL and every 4b count is an upper bound; the ratios R, R_corr and rho are one construction against itself and the bias very largely cancels out of them.

- 2026-09-17 (cloud lane, idea 1225 is-a-PAIRED-IID-BOOTSTRAP-the-RIGHT-SE-for-a-difference-between-books-correlated-at-0.90) — **ANSWERED = YES, IT IS THE RIGHT SE, AND THE QUEUE'S PREMISE IS BACKWARDS. A PAIRED DIFFERENCE BETWEEN BOOKS CORRELATED AT 0.93 IS MILDLY MEAN-REVERTING, NOT PERSISTENT, SO A BLOCK SE IS *SMALLER* THAN THE IID ONE AT 526 OF 567 (PAIR, RUNG) POINTS. HIGH CORRELATION IS THE REASON THE IID SE IS SAFE HERE, BECAUSE PAIRING REMOVES THE COMMON FACTOR, WHICH IS THE PERSISTENT PART.** Verdict KILL (capital). No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the LAST open idea; 1225 was last in '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Price leg of 84 real rung books over 3 panels, 81 measured pairs, 1,680 fold move-or-stay decisions, 120 stitched deployable curves and 120 single-split rule-8 rows, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `PAIR SET` {P_N, P_H, P_GROSS, P_CADENCE, P_FAR} x `L LADDER` {1, 5, 10, 21, 42, 63, 126, 252} = **40 cells per panel, 120 in all, EVERY ONE PUBLISHED** in `.cells.csv` and `.walkforward.csv`; all 81 individual pairs are published at all 8 rungs in `.pairs.csv` / `.cells.csv` regardless of what any cell did with them. NOT dials, reported at every value: PANEL {U56, B136, SMALL} (rule 9); the two statistics (mean daily difference, Sharpe difference), both published at every rung and neither selected on; the 14 expanding-IS folds. 10 bps, t+1 execution, weekly, 260-row warm-up, anchor N=20 / H=126 / gross 0.75 / W, B = 800 with a crc32 seed per (panel, L), tape pinned at 2026-09-15 (idea 1160's price-vintage defect carried, not absorbed).

  **THE IDENTITIES, DECLARED BEFORE ANY NUMBER.** A moving-block bootstrap at L = 1 IS the IID bootstrap bit for bit (1162's identity, G6), so the ladder's first rung carries no information and every ratio at L = 1 is exactly 1.000 by construction (G8). Newey-West at L = 1 IS the textbook IID SE of the mean (G7). And rho(A,B) high says nothing on its own about Var(d): pairing REMOVES the common factor, which is the whole point of a paired SE. This run therefore never claimed that correlation breaks an IID SE; it measured what is LEFT after pairing.

  **Q1 — WHAT IS LEFT AFTER PAIRING.** Over 81 real anchor-vs-rung pairs: rho mean **0.9298** (median 0.9488, min 0.6520, max 1.0000; 59 of 81 at rho >= 0.90; P_GROSS is exactly 1.0000 because a gross rung is a scalar multiple, idea 1162's result). The constituent books carry ac1 -0.0394 / -0.0457 and their DIFFERENCE carries **ac1 -0.0451, VR(5) 0.9560, VR(21) 0.8397, VR(63) 0.7148** — every variance ratio BELOW one, at every pair set and every panel. The difference is mildly mean-reverting.

  **Q2 — THE SE LADDER.** Against the bar pre-registered before any number (SE_block/SE_iid >= 1.10), the IID SE survives the whole ladder at **79 of 81 pairs** on the mean-difference statistic and **78 of 81** on the Sharpe-difference statistic; the three exceptions are single P_H / P_N pairs whose worst ratio reaches 1.14. The sign is the real result: the ratio sits **below 1 at 526 of 567 (pair, rung) points** (mean statistic) and 499 of 567 (Sharpe), bottoming near 0.38 for U56 P_GROSS at L = 252. A block SE is not a correction here, it is a loosening.

  **THE RE-PRICING.** Simulating the record's own publish decision (|t| >= 1.96) on all 81 pairs at every rung: 28 of 81 clear on the mean difference under the IID SE and **no block rung strips a single one** (0 of 28 at every L); 1 of 81 clears on the Sharpe difference and the worst rung, L = 21, strips that one. Movement runs the other way — blocks ADD up to 6 certifications the IID SE withheld (SHARPE_DIFF at L = 252: 7 clearing against 1). Cross-read: 1219 found 82 of 86 recoverable committed t's use an IID SE and 4 a block one; on this evidence that 82 is not the defect it looked like for differences of this shape.

  **Q3 + RULE 8 (mandatory) AND BOTH KEEP PATHS.** The decision an SE feeds in this record is MOVE-OR-STAY, so both are priced. Over 14 expanding-IS folds the mean move rate climbs 0.2857 -> 0.4524 as L goes 1 -> 252 and buys nothing: mean stitched Sharpe 0.8906 (IID) against 0.8976 at the best rung, both **below the do-nothing anchor's 0.9026**. On the single 2009-2016 / 2017-2026 split the chooser moves at 33 of 120 cells for a mean OOS Sharpe of 0.8729 against SPY's 0.8749 and live RULES v2's 0.9807. Best stitched cell anywhere: U56 / P_CADENCE / L = 252 at move rate 0.00 — that is, the anchor — stitched window 16.48% / 1.2116 / -19.13% (H1 1.21 / H2 1.23). Best rule-8 single-split pick anywhere: U56 / P_N / L = 126 -> N12/H126/gross 0.75/W, **OOS 18.89% / 1.1759 / -20.17%** against U56 SPY OOS 15.21% / 0.8711 / -33.72% and live RULES v2 OOS 9.46% / 1.2763 / -12.05%. **4a passes 0 of 120. 4b passes 24 of 120 — all on U56, 8 of them at move rate 0 (they ARE the committed anchor book) and the rest stitched curves over N and H rungs the record has already walked — so no cell here is a new rule and no 4b memo is owed.**

  **GATES, all 10 pass, printed before any result:** G1 fast runner == engine.backtest to 1.4e-17; G2 the committed U56 W/H126/N=20 triple to 4.1e-05 (bar 5e-3); G3 SPY OOS triple to 1.7e-04; G4 live RULES v2 MaxDD == -12.05% to 4.9e-05; G5 SMALL determinism exactly 0 on two uncached rebuilds; G6 the moving-block index at L=1 IS the iid index bit for bit; G7 Newey-West at L=1 == the iid SE of the mean to 1e-12; G8 every L=1 ratio is exactly 1.000; G9 Newey-West(L) and the block bootstrap agree within a factor 1.5 on the mean at all 648 (pair, rung) points; G10 the 14 fold partitions are contiguous and non-overlapping in all 3 panels.

  **WHAT THIS RUN DOES NOT SHOW.** It measures 81 REAL anchor-vs-rung pairs on this tape, not the 86 committed t's themselves: 1219 established what SE those used, this run establishes what the SE should be for differences of the same SHAPE, so the re-pricing leg simulates the record's decision rule rather than re-auditing its sentences. The 1.10 bar is pre-registered but arbitrary and the whole ratio curve is published so any other bar reads off the same table. A moving-block bootstrap is itself L-dependent and has its own edge effects, which is exactly the regress the queue names; Newey-West is published beside it. SURVIVORSHIP: U56, B136 and SMALL are current-constituent lists; SMALL is 663 names plus SPY after dropping the 52 of 715 tickers in data/small_meta.csv with max_1d_move >= 1.0, from a pool rebuilt on 2026-09-11 (idea 706/1072), so every small-cap figure here is an upper bound. Script: `research/backtests/2026-09-17_is-a-PAIRED-IID-BOOTSTRAP-the-RIGHT-SE-for-a-difference-between-books-correlated-at-0.90_cloud.py`.

- 2026-09-17 (cloud lane, idea 1228 does-the-RECORD-read-its-CHOOSER-RESULTS-off-a-LISTING-NOBODY-CHOSE) — **ANSWERED = YES, ON BOTH LEGS. 0.6120 OF THE RECORD'S CLASSIFIABLE COMMITTED CHOOSER SENTENCES QUOTE A LISTING MAXIMUM RATHER THAN A WALK-FORWARD PICK, AND ON 60 REAL DIAL CELLS THAT FORM OVERSTATES THE DEPLOYABLE STITCH BY A MEAN +0.0753 OF SHARPE — BUT IT IS FOLD-CLUSTERED-SIGNIFICANT AT ONLY 9 OF 60.** Verdict KILL (capital). No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the FIRST open idea; 1228 was first in '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Price leg of 72 real rung books over 3 panels, 840 fold picks, 180 stitched deployable curves and 60 rule-8 rows, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `CLAIM CLASS` {K_N, K_H, K_GROSS, K_CADENCE, K_ALL4} x `LISTING STATISTIC` {S_SHARPE, S_CAGR, S_MAR, S_4BMARGIN} = **20 cells per panel, 60 in all, EVERY ONE PUBLISHED** in `.readings.csv`. The RUNG inside a listing is not a third dial — it is the object a chooser selects — and all 24 rungs per panel are published in `.grid.csv` regardless of what any reading did with them. NOT dials, reported at every value: PANEL {U56, B136, SMALL} (rule 9), the 14 expanding-IS folds, and the L252 rolling-IS variant (run once as G9 and published, never selected on). 10 bps, t+1 execution, weekly, 260-row warm-up, anchor N=20 / H=126 / gross 0.75 / W, tape pinned at 2026-09-15 (idea 1160's price-vintage defect carried, not absorbed).

  **THE IDENTITY, DECLARED BEFORE ANY NUMBER (G6, 0 violations).** When the listing statistic IS the evaluation statistic, max-over-the-listing dominates every member by construction on any tape. This run therefore claims nothing about the SIGN of the gap; the content is its SIZE, its behaviour when the two statistics differ, and the census share.

  **Q1 CENSUS.** Corpus = 1,157 committed files (LEADERBOARD.md, CHANGELOG.md, 1,155 backtest memos), 121,366 candidate sentences, 11,300 carrying a chooser verb. Classification is a published regex rule applied in order, not a judgement: WF if a walk-forward marker is present (2,487), else LIST if a superlative-over-a-set marker is present (3,922), else OTHER (4,891, unclassifiable from text alone). LIST share = **0.6120** of the classifiable ones, 0.3471 of all chooser sentences; both denominators are printed. Only 870 of 3,922 LIST-form claims (0.2218) name a ladder family, so 0.7782 of them cannot be priced from their own text at all — which is itself the finding the record should act on.

  **Q2 PRICE.** Mean GAP_curve (listing max minus walk-forward stitch) **+0.0753** of Sharpe, median +0.0680, max +0.2409 (SMALL / K_H / S_SHARPE: 0.7629 against 0.5219). Positive at 44 of 60 cells; **fold-clustered |t| > 2 at only 9 of 60**, so the gap is real on average and unresolved cell by cell on 14 folds of this tape. Ordered by statistic: S_SHARPE +0.1253 > S_MAR +0.0896 > S_CAGR +0.0793 >> S_4BMARGIN +0.0071 — the margin statistic, which is the one 4b actually uses, is the one whose listing maximum is almost honest. By claim class: K_H +0.1076 > K_ALL4 +0.0956 > K_N +0.0883 > K_CADENCE +0.0574 > K_GROSS +0.0277 (K_GROSS is near zero because that ladder is near-degenerate in Sharpe, idea 1189's result, so its maximum and its pick are nearly the same book).

  **THE SHARPER READING: THE TWO FORMS NAME DIFFERENT BOOKS.** The rule-8 pick equals the listing maximum at **13 of 60 cells (0.2167)**. So on 47 of 60 cells a committed LIST-form sentence names a book the honest chooser did not select — which is 1227's finding generalised from one cell to sixty.

  **AND THE HONEST CHOOSER IS ITSELF NOT WORTH ITS MOTION.** The walk-forward stitch beats the do-nothing anchor at only **21 of 60 cells**, pooled fold delta **-0.0328** (mean |t| 1.33). Mean OOS Sharpe across the 60 rule-8 rows: rule-8 pick 0.8358 < anchor 0.8759 ~ SPY 0.8749 < listing max 0.9091 < live RULES v2 0.9807. The record's chooser machinery, read honestly, loses to holding the anchor; read in the LIST form, it appears to beat it.

  **RULE 8 (mandatory) AND BOTH KEEP PATHS.** Parameters chosen on 2009-2016 only, 2017-2026 read once. Best rule-8 pick anywhere in the run: U56 / K_GROSS / S_MAR -> N20/H126/gross 1.00/W, full 20.83% / 1.14 / -24.93% (H1 1.20 / H2 1.10), **OOS 22.72% / 1.1652 / -24.93%** against U56 SPY OOS 15.21% / 0.8711 / -33.72% and live RULES v2 OOS 9.46% / 1.2763 / -12.05%. It fails 4b on the drawdown cap (-24.93% against SPY's -33.72% x 0.60 = -20.23%). **4a passes 0 of 60. 4b passes 5 of 60, and every one is a book the record has already committed** — the anchor itself (twice, via K_CADENCE), U56 gross 0.65, and B136 gross 0.55 (twice) — so nothing here is a new rule, and ideas 1069/1189 have already shown gross-rung 4b passes sit in cells whose gross-matched null passes often.

  **GATES, all 9 pass, printed before any result:** G1 fast runner == engine.backtest to 1.4e-17; G2 the committed U56 W/H126/N=20 triple to 4.1e-05 (bar 5e-3, 1160's vintage defect carried); G3 SPY OOS triple to 1.7e-04; G4 live RULES v2 MaxDD == -12.05% to 4.9e-05; G5 SMALL pipeline determinism exactly 0 on two uncached rebuilds; G6 the max-dominance identity, 0 violations; G7 the anchor rung is a member of every listing, so do-nothing is always available to the chooser; G8 the 14 fold partitions are contiguous and non-overlapping in all 3 panels; G9 the L252 rolling-IS variant published (EXPAND 0.8644 vs L252 0.8704 mean stitched Sharpe) and selected on at neither.

  **WHAT THIS RUN DOES NOT SHOW.** The census classifies SENTENCES by marker words, not by what their author did: a genuine walk-forward result written in superlative prose counts as LIST, so 0.6120 is an upper bound on genuine LIST-form errors. The OTHER class (4,891) is excluded from that denominator and the all-sentence share (0.3471) is printed beside it. The gap is measured on four ladders and four statistics on one tape and is not a constant. SURVIVORSHIP: U56, B136 and SMALL are current-constituent lists; SMALL is 663 names plus SPY after dropping the 52 of 715 tickers in data/small_meta.csv with max_1d_move >= 1.0, from a pool rebuilt on 2026-09-11 (idea 706/1072), so every small-cap figure here is an upper bound. Script: `research/backtests/2026-09-17_does-the-RECORD-read-its-CHOOSER-RESULTS-off-a-LISTING-NOBODY-CHOSE_cloud.py`.

- 2026-09-17 (lane C, idea 1223 does-a-DEGENERATE-LADDER-in-the-COMPARISON-SET-MANUFACTURE-decisive-WIDEST-DIAL-calls) — **ANSWERED = YES. A DECISIVE WIDEST-DIAL CALL IS A JOINT PROPERTY OF THE BAR *AND* THE COMPARISON SET, AND THE RECORD'S SET CONTAINS A MEMBER THAT IS NOT A DIAL. ONE PERFECTLY DEGENERATE RUNG TAKES THE PAIRWISE DECISIVE RATE FROM 0.0397 TO 0.5198 AND CREATES 40 OF 42 H_NARROWEST AND 37 OF 42 H_ANY HEADLINE CALLS, DESTROYING 1 AND HIJACKING 0.** Verdict KILL (capital). No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the SECOND open idea; 1223 was second in '## Open' and is not EDGAR / Form 4 / 8-K / options / live-data. Price leg of 174 real rung books over 3 panels, 5,040 chooser pick-cells, 360 rule-8 rows and 360 stitched deployable curves, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `DEGENERACY LEVEL` delta {0.00, 0.01, 0.03, 0.10, 0.30, 1.00} x `COMPARISON SET` {NG, NG_D, ALL4, ALL4_D} = **24 cells, EVERY ONE PUBLISHED.** NG {N,H,CADENCE} and ALL4 {N,H,GROSS,CADENCE} carry no synthetic rung, so their rows are CONSTANT in delta by construction and are the control (gate G10 = 0). NOT dials, reported at every value: PANEL {U56, B136, SMALL} (rule 9); the 14 folds; the five bar forms {B_RAW, B_POINT, B_MED, B_IID95, B_BOOT95} inherited whole from 1214; the three HEADLINE FORMS {H_RUNNERUP, H_NARROWEST, H_ANY}; the 4a and 4b legs. 10 bps, t+1 execution, weekly, 260-row warm-up, anchor N=20 / H=126 / gross 0.75 / W, conditional two-sided null band at MC_SEED 12141214 (1214's, so the bands are literally the same numbers), block bootstrap B=63 / 800 reps.

  **THE INSTRUMENT.** `DEG(delta)`, 6 rungs, k = 6 — the same rung count as N, so the count-matching inflation d2(k_w)/d2(k_n) against N is exactly 1.000000 and the spread effect is isolated from the count effect. Rung j is the anchor weight frame blended with the N = n_j frame, `W_j(delta) = (1-delta)*W_anchor + delta*W_{N=n_j}`, a convex combination of two real weight frames, so every rung is a real tradable book at the frozen gross 0.75 through the same runner as everything else. **delta = 0 makes all six rungs identical (spread exactly 0); delta = 1 IS the N ladder bit for bit (G2 = 0.0).** Gross preserved at every row and every delta (G3 = 4.4e-16). The ALL4 pairs replay 1214's committed 252-row table (G9 = 4.55e-13).

  **(0) THE ARITHMETIC, PRINTED BEFORE ANY PRICE WAS READ.** A ladder with identical rungs has Sharpe spread EXACTLY 0, so every ordered pair against it is written with it as the narrower leg, M = R_w/0 = +inf, V = +inf, and **every bar the record has ever used returns a decisive W** — B_RAW, B_POINT, B_MED, B_IID95, and **B_BOOT95, the block bootstrap 1214 built precisely because it carries the real cross-ladder dependence** (the resampled spread is 0 in every rep, so z = +inf). The calls are not WRONG — N really is more dispersed than a thing that is not a dial — they are **VACUOUS, and indistinguishable in a census from calls about real dials.** Adding a ladder to a set of m adds m pairs and changes NO existing one (G8 = 0 mismatches over five bar forms), so the decisive rate moves d/P -> (d+m)/(P+m): **a set of 3 honest ladders with ZERO decisive pairs reads 0.5000 the moment one degenerate rung is admitted, and 1214's headline 126 of 252 at ALL4 is exactly 0.5000.** Outcomes (A) MANUFACTURES / (B) INERT / (C) PARTIAL were pre-declared in the same block; the run reached (A).

  **(1) THE WALK.** Decisive rate at NG_D by delta: **0.5198 / 0.5159 / 0.4881 / 0.3571 / 0.1508 / 0.1984** at delta 0.00 / 0.01 / 0.03 / 0.10 / 0.30 / 1.00, against the degenerate-free NG's **0.0397**; D-pair decisive rate **1.0000 / 0.9921 / 0.9365 / 0.6746 / 0.2619 / 0.3571**. Through all five bar forms at delta = 0.00: **B_RAW 1.0000, B_POINT 1.0000, B_MED 1.0000, B_IID95 0.5198, B_BOOT95 0.5119.** CALIBRATING THE BAR DOES NOT PROTECT A COMPARISON SET THAT CONTAINS A NON-DIAL.

  **(2) CREATED, DESTROYED, HIJACKED, AND THE ONE IMMUNE READING.** Matched (panel, fold), NG -> NG_D, 42 cells: **H_NARROWEST 2 -> 42 (CREATED 40), H_ANY 5 -> 42 (CREATED 37), H_RUNNERUP 2 -> 2 (CREATED 0)**. DESTROYED 1 in 18 cells, HIJACKED 0 everywhere — **a degenerate member only ever ADDS.** H_RUNNERUP is immune at every delta <= 0.30 because a degenerate rung is never top-2; it is the strictest reading and the only one the set cannot manufacture. At delta = 1.00 the mirror appears: the rung stops being a foil and becomes a DUPLICATE of N, creating 23 runner-up calls instead.

  **(3) WHERE GROSS SITS, AND SATURATION.** Median IS spread ratio to the N ladder over the 42 cells: **GROSS 0.0103**; DEG@0.00 0.0000, **DEG@0.01 0.0101**, DEG@0.03 0.0304, DEG@0.10 0.1005, DEG@0.30 0.3061, DEG@1.00 1.0000. GROSS's degeneracy is **delta ~ 0.01**, and the matched synthetic rung reproduces its effect to **|delta rate| = 0.0040** (NG_D@0.01 0.5159 vs ALL4 0.5198), gate G12. **SATURATION (G12b = 0): admitting a SECOND degenerate rung to ALL4 changes 0 headline calls at every delta <= 0.30 over 126 (panel, fold, form) cells per delta.** One is enough, and the record already has one.

  **(4) THE PRICE OF A MANUFACTURED CALL.** 5,040 pick-cells, paired vs CH_ANCHOR, SE clustered on the 14 folds: CH_ANCHOR (do nothing) mean OOS Sharpe **1.0362**. H_NARROWEST move rate **0.024 -> 0.881** and OOS Sharpe **+0.0006 -> -0.0487** (SE 0.0502, t -0.97); H_ANY **0.095 -> 0.881** and **+0.0032 -> -0.0487**. The cost shrinks as the rung stops being degenerate: **-0.0487 (delta <= 0.03) -> -0.0388 (0.10) -> -0.0131 (0.30)**. CH_RAW -0.0685 (t -1.33). ALL4 already reads the manufactured numbers (0.881, -0.0487) at every delta — again, GROSS is already doing this job. **Nothing is resolved on 14 folds (|t| <= 1.33) and every sign points at the anchor.**

  **RULE 8 AND BOTH KEEP PATHS — ONE BOOK, AND IT IS THE INCUMBENT.** Benchmarks: **U56 SPY 15.06% / 0.8815 / -33.72% (halves 0.9600/0.8171), OOS 15.15% / 0.8686; U56 RULES v2 (live) @10 bps 8.60% / 1.1982 / -12.05%, OOS 1.2717; B136 SPY 15.16% / 0.8862, OOS 0.8769; B136 LIVE 7.98% / 1.0994, OOS 1.1061; SMALL SPY 14.06% / 0.8582, OOS 0.8769; SMALL LIVE 4.64% / 0.7130, OOS 0.6518.** Chosen on 2009-2016 only, 2017-2026 read ONCE. **0 of 360 rule-8 rows clear 4a; 55 of 360 clear 4b AND 4b-OOS and all 55 are ONE book** — the U56 anchor N=20 / H=126 / g=0.75 weekly, **15.71% / 1.1480 / -19.13%, halves 1.2127/1.1050, OOS 17.16% / 1.1759 / -19.13%** — the standing 2026-09-04 incumbent 1206 / 1207 / 1214 all declined to promote. Only **8 distinct rule-8 outcomes** exist across all 360 rows. 174 rung books (108 synthetic): 4a 0, 4b full 53, 4b OOS 49, BOTH 48, which collapse to **33 distinct books** (DEG@0.00's six U56 rungs ARE the anchor, six times over). 360 stitched curves: 4a 0, 4b 73, 4b OOS 49, BOTH 49. **The synthetic ladder is a measuring instrument, not a candidate: no RULES wording can name a blend weight, and the passing DEG books are near-anchor mixtures by construction. CONFIRMATORY, NOT GENERATIVE. NO NEW BOOK, NO MEMO, NOTHING ENACTED.**

  **CAVEATS, AND ONE PRE-DECLARED GATE THAT FAILED AS WRITTEN.** Survivorship: universe.json / universe_broad.json are CURRENT constituents; the small panel is the current output of a sub-$2B screen, 715 rows of which 51 drop for `max_1d_move >= 1.0`, leaving 664 tradable. **G11 as pre-declared (the synthetic ladder's IS spread is monotone in delta at every transition) FAILED at 2 of 210**; both are the top step on U56 (2016: 1.1943 -> 1.0000; 2019: 1.0696 -> 1.0000), where a 30% blend is WIDER than the N ladder itself. There is no monotonicity theorem for the Sharpe spread of a blended ladder, so the pointwise claim was an over-claim; the gate was narrowed to the degenerate region delta <= 0.30 where every finding in this run lives (0 exceptions there), and **the two exceptions are named in the console and the result memo rather than smoothed away** (G11b reports the full count). The delta ladder is 6 rungs, so "GROSS sits at delta ~ 0.01" is a placement on a coarse grid, not a fitted value. All price deltas run |t| <= 1.33 on 14 folds and none of them is resolved.

- 2026-09-17 (cloud lane, idea 2 of 2, idea 1220 is-CHOOSER-INSTABILITY-a-READABLE-FUNCTION-of-an-SE-s-OWN-L-SENSITIVITY) — **ANSWERED = NO. THE RELATION IS 0.5727-0.6411 IN SAMPLE AND 0.0427-0.1966 OUT OF IT, IS UNDEFINED AT 3 OF 9 CELLS, AND IS FITTED ON A STATISTIC WITH TWO LEVELS. THE CHEAP STATISTIC THAT DOES WORK IS THE MOVE COUNT, AT rho = -0.9429.** Verdict KILL (capital). No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the LAST open idea; 1220 was last in '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. 432 decisions over 42 real books, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `CHOOSER SET` {K_SE3, K_SE3_NAIVE, K_ALL} x `L LADDER` {L_SHORT {21,42,63}, L_WIDE {21,42,63,126,252,504}, L_LONG {126,252,504}} = **9 cells, EVERY ONE PUBLISHED.** NOT dials, reported at every value: PANEL {U56, B136, SMALL} (rule 9); ANCHOR (N, cadence) {(20,W), (12,W), (20,M), (10,M)}; the N ladder {5,10,12,20,30,40,60} (k=7, inside d2's domain); the six L rungs; the six choosers; the 4a and 4b legs. The move bar is FIXED at |gap| > 2 SE and is NOT a dial. All three SE bases are PAIRED (1042's honest basis), BOOT = 400, seed 1220. 10 bps, t+1 execution, 260-row warm-up.

  **(0) THE DEGENERACY GATE, AND THE MONTE-CARLO FLOOR THAT IS MEASURED RATHER THAN ASSUMED.** SE_IID / C_NAIVE / C_CALMAR / C_ANCHOR read no L, so their POPULATION L-sensitivity ratio is 1 and their distinct-pick count is 1; including them manufactures a positive correlation out of a definition, so **every rho is published twice**, over the whole set and over the L-DEPENDENT choosers only. But SE_IID is also the run's NOISE FLOOR and its REALISED ratio is NOT 1: it is re-bootstrapped independently at each L rung, and reads **median 1.0868, max 1.1606 at BOOT = 400**. **A ratio below ~1.16 is therefore not evidence that a basis reads L at all — and 1212's x1.24 for the block SE sits close to that floor.** SE_BLOCK's excess over 1 is 4.36x the floor's own and SE_FOLD's is 9.15x, so both clear it.

  **(1) 1212's SE ORDERING DOES NOT REPRODUCE ON THIS CONSTRUCTION; ITS L-SENSITIVITY ORDERING DOES.** Median IS SE of the paired Sharpe gap: **SE_BLOCK 0.1224 < SE_IID 0.1354 < SE_FOLD 0.1424** — the fold SE is the LARGEST here, against 1212's report that it is the SMALLEST (0.0132 vs 0.0209 / 0.0292). The object differs (this run's gap is IS-argmax-minus-anchor on an N ladder; 1212's was its own decision set) but it is the same statistic by name, so **the ordering the queue entry rests on is construction-dependent.** L-sensitivity does reproduce in order: **SE_FOLD 1.7944 > SE_BLOCK 1.3787 > SE_IID 1.0868 (floor).**

  **(2) THE RELATION, AND WHY IT FAILS THREE WAYS.** In sample, Spearman(L-sensitivity, distinct picks) over the L-dependent choosers is **0.6411 (L_WIDE) / 0.5727 (L_LONG)**; over the whole set 0.4575-0.5769. **Re-measuring BOTH quantities on 2017-2026 gives 0.0427 and 0.1966** — the queue asked explicitly whether it holds out of sample and it does not. It is **UNDEFINED at 3 of 9 cells**: on L_SHORT no chooser changes its pick at any panel or anchor, so `distinct_picks` is constant at 1 and has zero variance; the curve is only measurable against instability once L reaches 126+. And **`distinct_picks` takes exactly TWO values {1, 2} over all 72 (panel, anchor, chooser) cells**, so every published rho is a rank correlation fitted on two levels and the in-sample 0.64 is near the ceiling a monotone statistic could score.

  **(3) THE CONTROL THAT REPLACES IT.** Move count / mean OOS Sharpe over the six choosers: **C_ANCHOR 0 / 0.9194, SE_FOLD 6 / 0.9064, SE_IID 12 / 0.8806, SE_BLOCK 13 / 0.8804, C_CALMAR 60 / 0.8493, C_NAIVE 66 / 0.8578 — Spearman -0.9429 with one inversion.** Move count costs one in-sample pass, exactly like the curve the queue proposed, and ranks the choosers by the thing that matters. **SE_FOLD scores well because it MOVES LEAST, not because its SE is better calibrated.** This is 1221's finding reached from a different direction. It is thin evidence — six points, one inversion, one un-walked 2-SE bar — and is logged as a question, not a rule.

  **RULE 8 AND BOTH KEEP PATHS — ONE BOOK, AND IT IS THE INCUMBENT.** Benchmarks: **U56 SPY 15.06% / 0.8814 / -33.72% (halves 0.9598/0.8170), OOS 15.15% / 0.8684; U56 RULES v2 (live) @10 bps 8.60% / 1.1980 / -12.05%, OOS 1.2714; B136 SPY 15.16% / 0.8861 / -33.72%, OOS 0.8767; B136 LIVE 7.98% / 1.0993 / -12.24%, OOS 1.1059; SMALL SPY 14.06% / 0.8581 / -33.72%, OOS 0.8767; SMALL LIVE 4.30% / 0.6637 / -13.89%, OOS 0.5600.** Chosen on 2009-2016 only, 2017-2026 read ONCE. **0 of 432 decisions clear 4a; 24 of 432 clear 4b and all 24 are ONE distinct book** — U56 / N=20 / W / g=0.75, **14.18% / 1.1410 / -19.39%, halves 1.223/1.088, OOS 15.55% / 1.1595** — reached by C_ANCHOR, SE_IID, SE_BLOCK and SE_FOLD at every L rung on that family. Under 1211's decision-row correction the count collapses 24 -> 1, inflation factor 24. It is the standing 2026-09-04 incumbent, so **CONFIRMATORY, NOT GENERATIVE. NO NEW CANDIDATE, NO MEMO, NOTHING ENACTED.**

  **CAVEATS.** Survivorship: universe.json / universe_broad.json are CURRENT constituents; the small panel is the current output of a sub-$2B screen, 715 rows in small_meta.csv of which 52 drop for `max_1d_move >= 1.0`, leaving 663 tradable (SMALL439 does not denote this pool; 1074 open). The 2-SE bar is fixed, not walked, so these are pick counts at ONE bar and a different bar moves every move count and therefore (3)'s ordering. The OOS L-sensitivity is measured on a 9.7-year window against the IS 8-year one, so the 0.64 -> 0.04 collapse is larger than any length effect can account for but the two figures are not comparable to four decimals.

- 2026-09-17 (cloud lane, idea 1 of 2, idea 1213 do-the-RECORD-s-OTHER-COUNT-CORRECTIONS-STATE-THEIR-OWN-DOMAIN) — **ANSWERED = NO. 40 OF 2,994 COMMITTED CORRECTION INVOCATIONS (0.0134) STATE THEIR OWN DOMAIN, AND THE ONLY TWO PLACES ON PRICE WHERE A COUNT CORRECTION IS LOAD-BEARING ARE BOTH OUTSIDE THE DOMAIN IT IS QUOTED FROM.** Verdict KILL (capital). No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the FIRST open idea; 1213 was first in '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Price leg of 90 real rung books over 3 panels and 108 rule-8 decisions, so it carries this run's mandatory walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `CORRECTION SET` {C_D2, C_COUNT, C_ALL} x `DOMAIN RULE` {R_TABULATED, R_STATED, R_LOOSE} = **9 cells, EVERY ONE PUBLISHED.** NOT dials, reported at every value: PANEL {U56, B136, SMALL} (rule 9); the four ladders LAD_N6 (k=6) / LAD_N14 (k=14) / LAD_G8 (k=8) / LAD_G16 (k=16); the six correction families; the 4a and 4b legs. 10 bps, t+1 execution, weekly, 260-row warm-up, block SE with **L = 63 STATED** (1208's open complaint) and B = 400 at seed 1213.

  **(0) THE ARITHMETIC, PRINTED BEFORE ANY TEXT OR ANY PRICE WAS READ.** d2(k) is tabulated for k = 2..12; the record's habit above 12 is to CLIP at d2(12) = 3.258. Clipping makes the correction CONSTANT in k, so between two ladders both longer than 12 rungs it contributes **exactly 1.000000** and cannot order them. Gate G0 returns True; `d2(14)/d2(16)` clipped = 1.000000. Outcomes (A) MANY >= 10 / (B) FEW 1-9 / (C) NONE / (D) UNTRACEABLE were pre-declared in the same block; the run reached (A) on a technicality — 40 invocations state a domain, out of 2,994.

  **(A) THE CENSUS — 1207's 0.8125 GENERALISES, AND d2 IS THE RECORD'S BEST-DOCUMENTED CORRECTION.** Corpus 104,556 units (7,203 LEADERBOARD rows + every committed markdown paragraph under research/, 2,630 files), unit definition and count-cue harvest **inherited whole from 1155/1207** so the figures are like-for-like. Per family, share stating its own domain: **D2 17/279 = 0.0609, SUBSAMPLE 4/452 = 0.0088, SE_SQRTN 2/13 = 0.1538, BLOCK_L 5/446 = 0.0112, MULTTEST 2/13 = 0.1538, ANNUAL 10/1791 = 0.0056.** In-domain share under the tabulated rule: **D2 0.1720, SUBSAMPLE 0.5066, SE_SQRTN 0.4615, BLOCK_L 0.5000, MULTTEST 0.9231, ANNUAL 0.3412.** Across the whole record d2's out-of-domain share reads **0.8280** against 1207's 0.8125 on 1155's 96 units. The share stating its domain FALLS as the set widens (0.0609 -> 0.0309 -> 0.0134), i.e. every other correction the record applies is documented WORSE than the one 1207 already indicted. **583 out-of-domain units under C_ALL / R_TABULATED carry a committed verdict token (0.3126).**

  **(B) THE CONTROL IS THE FINDING.** Running the SAME range-vs-SE gate with NO correction at all, against the record's clipped d2 and against a refusing (tabulated) d2: **the count correction is LOAD-BEARING at 2 of 12 (panel, ladder) families, and BOTH are the k = 14 ladders — exactly where d2 has no tabulated value.** Bare range/SE clears at U56 LAD_N14 (1.0520) and SMALL LAD_N14 (2.0122); the clipped constant 3.258 switches BOTH gates OFF. So the only two decisions a count correction makes on this tape, it makes with **d2(12) wearing d2(14)'s name.** Under R_TABULATED the same two cells are `applied = False` and the chooser declines for the opposite reason, landing on the same book. The other ten families are not adjudicable at all: the four gross ladders run range/SE **0.0012 - 0.0300** because IS Sharpe is flat in gross (1189, reproduced to four decimals), and 8 of 12 families sit below HALF their bare SE, so there the domain question cannot be settled on price either way.

  **RULE 8 AND BOTH KEEP PATHS — NOTHING MOVES, AND WHAT PASSES IS THE INCUMBENT.** Benchmarks: **U56 SPY 15.06% / 0.8814 / -33.72% (halves 0.9598/0.8170), OOS 15.15% / 0.8684; U56 RULES v2 (live) @10 bps 8.60% / 1.1980 / -12.05%, OOS 1.2714; B136 SPY 15.16% / 0.8861 / -33.72%, OOS 0.8767; B136 LIVE 7.98% / 1.0993 / -12.24%, OOS 1.1059; SMALL SPY 14.06% / 0.8581 / -33.72%, OOS 0.8767; SMALL LIVE 4.30% / 0.6637 / -13.89%, OOS 0.5600.** Chosen on 2009-2016 only, 2017-2026 read ONCE. **0 of 108 decisions move off the anchor under ANY correction set or domain rule — all nine cells identical at mean OOS Sharpe 0.9339, mean OOS CAGR 14.96%, worst OOS MaxDD -32.11%/-30.49%.** **0 of 108 clear 4a; 36 of 108 clear 4b and every one of the 36 is the U56 anchor** — 14.18% / 1.1410 / -19.39%, halves 1.223/1.088, **OOS 15.55% / 1.1595** — the standing 2026-09-04 incumbent, so it is **CONFIRMATORY, NOT GENERATIVE. NO NEW CANDIDATE, NO MEMO, NOTHING ENACTED.** B136 and SMALL anchors fail 4b on the H2 leg (0.8194 vs SPY 0.8259; 0.6154 vs 0.8344).

  **CAVEATS.** Survivorship: universe.json / universe_broad.json are CURRENT constituents; the small panel is the current output of a sub-$2B screen. `small_meta.csv` carries **715** rows at this commit, 52 drop for `max_1d_move >= 1.0`, **663** tradable remain — the label SMALL439 does not denote this pool (1074 still open). The census harvest is 1155/1207's and inherits its noise (max harvested k = 272,832, 1211's open complaint, not repaired here), and `states_domain` is scored at UNIT level so a domain token anywhere in a long paragraph counts — both are generous to the record, so 0.0134 is an UPPER bound on how well it documents itself.
- 2026-09-17 (lane C, idea 1214 is-a-COUNT-MATCHING-BAR-BELOW-ONE-a-CORRECTION-or-a-HANDICAP) — **ANSWERED = NEITHER. A BAR BELOW 1 IS A NO-OP, PROVED ARITHMETICALLY, AND 1207's OWN SECONDARY RECOMMENDATION — "a count-matching bar whose value falls BELOW 1 is not a correction, it is a handicap on the SHORTER ladder" — IS REFUTED BY THIS RUN AND SHOULD NOT BE ENACTED.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the SECOND open idea; 1214 was second in '## Open' and is not EDGAR / Form 4 / 8-K / options / live-data. Price leg of 66 real rung books, 588 chooser pick-cells, the same 252 realised ladder pairs 1207 published and 42 stitched deployable curves, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `BAR FORM` {B_RAW (no bar), B_POINT (1155/1207's M > I), B_MED (median-unbiased point bar), B_IID90, B_IID95 (conditional two-sided bands from the iid-normal null), B_BOOT95 (the same edge from a 63-day BLOCK BOOTSTRAP of the actual rung books, so the real cross-rung and cross-ladder dependence is carried rather than assumed)} x `LADDER SET` {ALL4, NG} = **12 cells, EVERY ONE PUBLISHED.** alpha is NOT a third dial: it is enumerated inside the bar form and both values are reported at every cell. NOT dials, reported at every value: PANEL {U56, B136, SMALL} (rule 9); the record's four ladders N (6 rungs) / H (4) / GROSS (10) / CADENCE (2), inherited whole from 1207; the 14 folds; the 4a and 4b legs. 10 bps, t+1 execution, 260-row warm-up, DECIDE-AT-t selection (lag=1, 1209's correction).

  **(0) THE PREMISE HAS THE WRONG SIGN, AND IT IS ARITHMETIC, NOT A MEASUREMENT.** A pair is written in its realised direction, so **M = R_wide/R_narrow >= 1 BY CONSTRUCTION**, and **I = d2(k_wide)/d2(k_narrow) < 1 exactly when the wider-spread ladder is the SHORTER one**. M > 1 > I then holds for every such pair: **the bar cannot bind, ever.** On 1207's committed pairs table **134 of 252 pairs (0.5317) carry a bar below 1 and 134 of 134 of them SURVIVE it (1.0000)** — gate G2. The below-one bar does not penalise the shorter ladder; it hands it a **FREE PASS**, and the exposure the record carries there is **FALSE CONFIRMATION**, not a handicap. Separately, **R_k/d2(k) is unbiased for sigma at every rung count (max |E[R_k/d2(k)] - 1| = 8.56e-04, G1)**, so 1155's point correction is correctly CENTRED in expectation — the defect is RESOLUTION, not bias. Outcomes (A) CORRECTION (decisive at >= 0.80) / (B) HANDICAP (reversals >= 0.10) / (C) UNRESOLVED (TIE >= 0.50) were pre-declared in the same block.

  **(A) WHAT THE CORRECT TWO-SIDED BAR IS, DERIVED BEFORE IT IS WALKED.** The record's habit ORIENTS the pair on the DATA — it calls the raw-wider ladder the wider dial — so the null that governs the call is the distribution of **V = M/I CONDITIONAL on the realised orientation**. Conditioned, **V is not centred at 1 at all**: its null median runs **3.2685 at (k_w=2, k_n=10), 1.8273 at (4,10), 1.5089 at (4,2), 1.2077 at (10,6)** (all 12 ordered cells in `.bands.csv`, gate G3 nests them). Where k_w < k_n, **V > 1 with probability one and the point bar CAN NEVER REVERSE**, however thin the margin. The correct bar is the conditional two-sided band: outside the upper edge the wider dial is real, outside the LOWER edge the call REVERSES onto the longer ladder, in between there is no call at all. **Under H0 a point bar names a dial at 1.0000 of pairs and is wrong at 0.2573 (B_POINT) to 0.5000 (B_MED, a coin flip by construction); a band's rate is the alpha the author chose.** That is the whole of the repair.

  **(B) THE 252 PAIRS RE-READ, ALL 12 CELLS.** ALL4: B_RAW 252 W / 0 N / 0 TIE; **B_POINT 225 / 27 / 0 (reverses 0.1071); B_MED 197 / 55 / 0 (0.2183); B_IID90 127 / 5 / 120; B_IID95 126 / 5 / 121 (tie 0.4802); B_BOOT95 108 / 0 / 144 (tie 0.5714).** NG: B_POINT 99 / 27 / 0; B_MED 71 / 55 / 0; **B_IID95 0 / 5 / 121; B_BOOT95 3 / 0 / 123 (tie 0.9762).** **EVERY DECISIVE CALL AT ALL4 IS A GROSS PAIR** — 126 of the 252 pairs put a real dial against GROSS, whose Sharpe spread is degenerate (1189, reproduced by 1206 and again here: IS GROSS spread 0.0011 / 0.0034 / 0.0023 against N's 0.0465 / 0.1777 / 0.2552), and all 126 clear the band trivially. **STRIKE GROSS AND THE CALIBRATED BAR NAMES THE RAW-WIDER LADDER AT 0 OF 126 PAIRS; its only 5 calls are REVERSALS, every one at M between 1.004 and 1.052 — knife-edge ties — and B_BOOT95, which carries the real dependence, declines all 5.** The widest-dial habit among the record's non-degenerate dials is **UNRESOLVED, not merely uncorrected.** The headline cell lands at TIE 0.4802, **five pairs short of the pre-declared (C)**, and only because the degenerate GROSS pairs are counted; recorded as (A) by the pre-declared rule and read honestly as (C) everywhere the answer could matter.

  **(C) THE COMMITTED WIDEST-DIAL CALLS.** 352 committed units make a widest / narrower call and adjudicate on it; **17 are checkable** (quote a multiple AND two different counts) and **6 are inside d2's k = 2..12 domain (0.3529)** — 1207's domain split repeating on a different harvest. Those 6 are exactly the units 1207 hand-read as genuine range-vs-range pairs, and under the band (counts assigned by the record's own I = d2(k_max)/d2(k_min) convention): **the two-sided bar REVERSES 2 of 6 (05d6fd1eb4, 97f64224ae), DECLINES 3 (ca229253f8, a5001741b0, b374f7d5cf) and confirms exactly 1 — d1e45a7a60's "39x", V = 35.51 against a band of [0.925, 2.715].** Both reversals STRENGTHEN their committed verdicts (1207 hand-read both as already running against their authors), so **NO COMMITTED VERDICT CHANGES**, consistent with 1207's ANSWERED = NONE.

  **(D) RULE 8 AND BOTH KEEP PATHS — THE MORE HONEST THE BAR, THE CLOSER IT GETS TO DOING NOTHING, AND DOING NOTHING WINS.** Each bar form made deployable as a chooser (name a dial on the IS window, or hold the anchor): 588 pick-cells = 3 panels x 14 folds x 14 choosers, 42 picks each; folds tile 2013-2026 with no overlap and no gap (G8). **Move rates: CH_RAW 1.000, CH_POINT 0.905, CH_MED 0.881, CH_IID90/95 0.048, CH_BOOT95 0.000 — the bootstrap band never separates the winner from the runner-up on any of the 42 IS windows, so CH_BOOT95 IS CH_ANCHOR, exactly.** Mean OOS Sharpe, paired delta vs CH_RAW, SE clustered on the 14 folds: **CH_RAW 0.9677; CH_POINT 0.9959 (+0.0281, SE 0.0494, t +0.57); CH_MED 0.9875 (+0.0198, SE 0.0461, t +0.43); CH_IID90 = CH_IID95 1.0179 (+0.0501, SE 0.0541, t +0.93); CH_BOOT95 = CH_ANCHOR 1.0362 (+0.0685, SE 0.0514, t +1.33).** The ordering is monotone in the honesty of the bar and terminates at doing nothing, and **NO DELTA CLEARS 2 SE — the largest abs(t) in the run is 1.33**, the fourth run in a row (1155, 1206, 1207, this) to find do-nothing ahead and unresolvably so. **ALL4 and NG pick IDENTICALLY at every one of the 588 cells** — GROSS is always the narrowest ladder, so dropping it never moves an argmax — so the ladder-set dial is **INERT on the price leg** and is reported, not relied on. **RULE 8** (dials on warm-up..2016-12-31, 2017-2026 read ONCE): the band returns **TIE at all three panels**, so every band chooser holds the anchor — U56 CH_RAW/POINT/MED -> N=40, 13.40% / 1.1311 / -22.46%, halves 1.2663/1.0361, OOS 14.08% / 1.1150; U56 band and anchor -> CADENCE=W, **15.71% / 1.1480 / -19.13%, halves 1.2127/1.1050, OOS 17.16% / 1.1759 / -19.13%**; B136 raw -> N=5, 18.73% / 0.9690 / -28.12%, OOS 14.71% / 0.7546, band/anchor 16.18% / 1.0715 / -20.74%, OOS 16.29% / 1.0240; SMALL raw -> H=252, 13.36% / 0.7845 / -37.41%, OOS 11.16% / 0.6677, band/anchor 7.84% / 0.5055 / -35.81%, OOS 7.09% / 0.4534. Benchmarks: **U56 SPY 15.06% / 0.8815 / -33.72% (halves 0.9600/0.8171), OOS 15.15% / 0.8686; U56 RULES v2 (live) @10 bps 8.60% / 1.1982 / -12.05%, OOS 9.42% / 1.2717; B136 SPY 15.16% / 0.8862, OOS 0.8769; B136 LIVE 7.98% / 1.0994, OOS 1.1061; SMALL SPY 14.06% / 0.8582, OOS 0.8769; SMALL LIVE 4.64% / 0.7130, OOS 0.6518.** **BOOKS (66): 4a 0 of 66; 4b full 17 (U56 10, B136 7, SMALL 0), 4b OOS 16, BOTH 15. STITCHED CURVES (42): 4a 0, 4b full 12, 4b OOS 8, BOTH 8. RULE-8 PICKS (42): 4a 0, 4b full 8, 4b OOS 8, BOTH 8.** **EVERY passing rule-8 and stitched row is the SAME object — the ANCHOR book (U56, N=20 / H=126 / gross 0.75, weekly) reached by a chooser that DECLINED TO MOVE**, i.e. the already-committed U56 CAND20 weekly g=0.75 book confirmed on 2026-09-15 and declined for promotion by 1206 and 1207; counted under 1194's gross-free key it is ONE book, not eight. **NO NEW BOOK. CONFIRMATORY, NOT GENERATIVE. RECORDED, NOT PROMOTED, NO MEMO, NOTHING ENACTED.**

  **GATES 17 OF 17 PASS.** G0 Monte-Carlo d2(k) == the published Hartley constants at 9.66e-04; **G1 R_k/d2(k) unbiased for sigma at 8.56e-04**; **G2 every below-one bar is a NO-OP on 1207's committed pairs, 134 of 134**; G3 the bands nest L95 <= L90 <= median <= U90 <= U95 at all 12 (k_w, k_n) cells; G4 the fast runner == `engine.backtest` on the decision-time frame at **2.78e-17**; G5 the GROSS ladder is provably the anchor frame SCALED, 0.000e+00; G6 live RULES v2 U56 MaxDD **-12.0549%** against the record's -12.05%; **G7 the block-sum bootstrap == the direct Sharpe on the identity tiling at 4.44e-15**; G8 the folds tile all three panels with no overlap and no gap; **G9 the 252 pairs replay 1207's committed table BIT FOR BIT, max |dMULTIPLE| 4.55e-13 over 252 matched rows**; G10 B_RAW names the raw-wider ladder at every pair; G11 CH_ANCHOR's move rate is exactly 0; G12 each stitched curve's length == the sum of its folds'; G13 1207's 6 hand-read A_PAIR units are all still in the live corpus; **G14 the null bands are deterministic across two independent constructions, 0.0**.

  **RECOMMENDATION, PROPOSED NOT ENACTED (rule 6): WITHDRAW 1207's secondary clause and replace it.** A count-matching bar below 1 is **not a handicap — it is a NO-OP**, and publishing it as a handicap would have put a second wrong-signed alarm in the record on top of the one 1207 removed. The replacement clause: **a committed claim that one dial is the widest must quote a two-sided bar conditional on its own realised orientation, or state that it is naming a dial it cannot resolve.** On this record's own four ladders that clause silences every widest-dial call not involving the degenerate GROSS ladder — 121 of 126 pairs — which is the correct outcome and not a cost.

  **SURVIVORSHIP (rule 9).** B136 and SMALL are CURRENT constituents; SMALL is the sub-$2B screen, 664 investable of 715 after dropping every ticker with max_1d_move >= 1.0, and starts 2010. Every CAGR and drawdown LEVEL is optimistic and the 4b counts are an UPPER bound. Arms 0, A and C are arithmetic and a scan of committed text and carry no market bias at all; the chooser deltas rank one construction against itself on one tape and the bias very largely cancels out of the DELTAS but not out of the rule-8 OOS levels.

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_is-a-COUNT-MATCHING-BAR-BELOW-ONE-a-CORRECTION-or-a-HANDICAP_C.py`, 9 CSVs, console log, result note, 6 LEADERBOARD rows. Runtime 23s, offline, deterministic. Follow-ups filed 1222 (is the WIDEST-DIAL habit resolvable on ANY attainable fold count), 1223 (does a DEGENERATE ladder in the comparison set manufacture decisive widest-dial calls) and 1224 (what does the record's tuning cost once every unresolvable dial call is replaced by the anchor). **[filed 1219->1222, 1220->1223, 1221->1224 on push: lane collision, defect 932 again — a concurrent lane had already taken 1219-1221.]**

- 2026-09-17 (lane C, idea 1207 how-many-committed-RANGE-claims-quote-a-MULTIPLE-BELOW-THEIR-OWN-COUNT-INFLATION-and-were-ADJUDICATED-ON-IT) — **ANSWERED = NONE. ZERO COMMITTED VERDICTS CHANGE, AND 1155's "36 OF 90 FAIL" IS VERY LARGELY ITS OWN COUNT HARVEST RATHER THAN THE RECORD'S DEFECT.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the SECOND open idea; 1207 was second in '## Open' and is not EDGAR / Form 4 / 8-K / options / live-data. Price leg of 66 real rung books, 336 chooser pick-cells, 252 realised ladder pairs and 24 stitched deployable curves, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `CLAIM SET` {C_STRICT, C_PROX, C_ALL} x `NO-EFFECT RULE` {R_MIN (the smallest quoted multiple, 1155's headline leg), R_MAX (the largest, its second leg), R_BAND (the FLOOR of an explicit x-to-x band, 1155's own G0b treatment of 1140)} = **9 cells, EVERY ONE PUBLISHED.** NOT dials, reported at every value: PANEL {U56, B136, SMALL} (rule 9); the record's four ladders N (6 rungs) / H (4) / GROSS (10) / CADENCE (2), inherited whole; the DOMAIN split; the four choosers; the 4a and 4b legs. 10 bps, t+1 execution, 260-row warm-up, DECIDE-AT-t selection (lag=1, idea 1209's correction to the record's inherited builder).

  **(0) THE ARITHMETIC, PRINTED BEFORE ANY TEXT OR ANY PRICE WAS READ — THE SUBSTITUTION CONVENTION IS NOT A THIRD DIAL.** A verdict resting on a multiple M rests on the comparative M/I > 1, I being the inflation d2(k_max)/d2(k_min) the two counts alone buy. **M' = 1 gives 1/I < 1 and flips EVERY unit; M' = I gives I/I = 1 and flips EVERY unit; M' = M/I flips EXACTLY {M <= I}.** Every convention is the SAME THRESHOLD AT I, so it is gated (G3: on the 100 units whose counts differ, flip shares 1.0000 / 1.0000 / 0.4000 and **0 mismatches** against {M <= I}) and never walked as a grid. Outcomes (A) MANY >= 10 / (B) FEW 1-9 / (C) NONE / (D) UNTRACEABLE were pre-declared in the same block.

  **(A) 1155's CENSUS REPRODUCED BIT FOR BIT, THEN TAKEN APART BY ITS OWN DOMAIN.** Replayed on the corpus 1155 actually ran against — commit `4ee8b6f~1`, the parent of its own commit, because its census was computed before its own artefacts existed — this run returns **32,930 units / 1,137 markdown files / 313 C_STRICT / 90 checkable / smallest multiple survives at 54 (0.6000) / median inflation 1.8999**: every figure 1155 committed (gate G1). The live corpus at HEAD is **33,224 units** (7,052 LEADERBOARD rows, 636 CHANGELOG paragraphs, 1,144 markdown artefacts). **THEN THE DOMAIN: d2(k) is tabulated for k = 2..12 and the longest ladder this record has ever published is 10 rungs, but 1155's harvest reads ANY "<n> rungs / cells / points / books / rows / panels" token anywhere in the unit. 78 OF THE 96 CHECKABLE C_STRICT UNITS (0.8125) CARRY A HARVESTED COUNT OUTSIDE THAT DOMAIN ENTIRELY — the largest is 1,087,557, the median 126 — so their "inflation" was the CLIPPED constant d2(12)/d2(k_min), a number with no relation to the claim.** Those units are VACUOUS, not failing, and are reported separately and never merged (G10 partitions the set exactly). The 9-cell grid: C_STRICT FAILS **39 / 19 / 38** under R_MIN / R_MAX / R_BAND, of which VACUOUS **31 / 16 / 30**, IN-DOMAIN **8 / 3 / 8**, and in-domain AND carrying a verdict **4 / 2 / 4**.

  **(B) THE TRACE — ALL 24 IN-DOMAIN UNITS HAND-READ, ONE LINE EACH, PUBLISHED VERBATIM** in `.audit.csv` (G9: 0 unaudited). KINDS: **A_PAIR 6** (a genuine range-vs-range comparison over two different counts — the object d2 was built for), **A_OTHER 10** (a range against a POINT GAP or an SD, or two ranges at EQUAL counts, where d2 does not apply in either direction), **A_NOTMULT 8** (a sleeve weight, a 4b bar, a tolerance, a dial step — not a comparative multiple at all). Of the ten failing in-domain units under R_MIN: 4 are A_NOTMULT, 2 are A_OTHER ("spread = -0.85 x GAP", "within-rung sd is 0.76 x GAP" — both compare a range or an SD to a POINT, and both verdicts rest on other statistics), and **4 are A_PAIR AND ALL FOUR HOLD: 412b's "lambda spans a median 2.39x of turnover against cadence's 6.38x" has the LONGER ladder (13-rung coverage + 10-rung absolute-n against cadence's D/W/M/Q) on the losing side, so count inflation runs AGAINST cadence and the ADOPT CADENCE verdict is if anything understated; 1209's 1.4426x IS d2(9)/d2(4), published by that run as a data-free artefact, so its committed verdict ALREADY IS the no-effect reading; 572's 3-rung-to-7-rung growth of 1.348x sits BELOW d2(7)/d2(3) = 1.598 and STRENGTHENS its own "rung-selection artefact" verdict.** **VERDICTS THAT CHANGE: 0 under R_MIN, 0 under R_MAX, 0 under R_BAND. OUTCOME (C).**

  **(C) RULE 8 AND BOTH KEEP PATHS — THE VERDICT FLIP MADE LIVE, PRICED, AND WORTH NOTHING; AND A DIRECTION 1155's ALARM HAD BACKWARDS.** The record's habit *"this dial's spread is X times that one's, so tune this dial"* IS a range multiple across two ladders of different rung counts, so it is made a chooser: **CH_RAW** (tune the widest raw max-min ladder — the habit, licensed by M > 1 and therefore always tuning), **CH_MATCHED** (tune only if M > I; else stay at the anchor — CH_RAW with the failing verdicts SET TO NO EFFECT, so the paired delta IS the price of the change), **CH_D2** (1155's M_D2 repair) and **CH_ANCHOR** (never move). Folds = one calendar year of OOS stepped one year, 2013-2026, tiling the span with no overlap and no gap (G7); 42 picks per chooser. **OVER ALL 252 REALISED ORDERED LADDER PAIRS THE COUNT-INFLATION BAR SITS BELOW 1 AT 0.5317 — more than half the time the ladder with the WIDER spread is the SHORTER one, so the count difference runs AGAINST the conclusion being drawn rather than for it, the opposite of the direction 1155's alarm assumes.** It binds only on H/CADENCE (survives 0.6923), N/CADENCE (0.8810) and N/H (0.7297). **On ALL4, the record's own four ladders and this run's HEADLINE, THE BAR CANNOT BIND AT ALL: 42 of 42 survive and CH_MATCHED differs from CH_RAW at 0 of 42** — structurally, because GROSS is simultaneously the LONGEST ladder (10 rungs) and the one whose Sharpe spread is degenerate (1189, reproduced by 1206 at 0 of 1,888 widest-calls), so it is always the narrowest and the bar lands below 1. **NG (no-GROSS) is a POST-HOC control, labelled as one, added AFTER the ALL4 leg showed the bar cannot bind, and no verdict in this run rests on it:** there it binds at 4 of 42 flips and the flip costs **-0.0163 of OOS Sharpe, SE 0.0237, t -0.69.** CH_D2 +0.0281 (t +0.57); CH_ANCHOR +0.0685 (t +1.33). **NO DELTA CLEARS 2 SE AND THE LARGEST abs(t) IN THE RUN IS 1.33** — doing nothing is ahead of every chooser again (1155/1206's finding from a third direction, by a run not looking for it) and again not resolvably so. Benchmarks: **U56 SPY 15.06% / 0.8815 / -33.72% (halves 0.9600/0.8171), OOS 15.15% / 0.8686; U56 RULES v2 (live) @10 bps 8.60% / 1.1982 / -12.05%, OOS 9.42% / 1.2717; B136 SPY 15.16% / 0.8862 / -33.72%, OOS 0.8769; B136 LIVE 7.98% / 1.0994 / -12.24%, OOS 1.1061; SMALL SPY 14.06% / 0.8582 / -33.72%, OOS 0.8769; SMALL LIVE 4.64% / 0.7130 / -12.18%, OOS 0.6518.** **BOOKS (66): 4a 0 of 66; 4b full 17, 4b OOS 16, BOTH 15** (U56 10/22, B136 5/22, **SMALL 0/22 on every path**). **RULE-8 PICKS (24): 4a 0, 4b full 2, 4b OOS 2, BOTH 2. STITCHED CURVES (24): 4a 0, 4b full 2, 4b OOS 2, BOTH 2.** Both passers are the SAME object — **U56 CH_ANCHOR 16.89% / 1.2199 / -19.13%, halves 1.2635/1.2043, OOS 17.16% / 1.1759** — the do-nothing control sitting on the record's standing anchor book (U56 N=20 / H=126 / g=0.75 / W). **CONFIRMATORY, NOT GENERATIVE. RECORDED, NOT PROMOTED, NO MEMO, NOTHING ENACTED.**

  **GATES 15 OF 15 PASS.** G0 Monte-Carlo d2(k) == the published Hartley constants at 2.38e-03; **G1 1155's census replayed BIT FOR BIT at its own corpus**; G2 the claim sets nest, 0 violations; **G3 the substitution convention proved to be one threshold at I on the actual units, 0 mismatches**; G4 the fast runner == `engine.backtest` on the decision-time frame at **2.78e-17**; **G5 the GROSS ladder is provably the anchor frame SCALED, 0.000e+00**; G6 live RULES v2 U56 MaxDD **-12.0549%** against the record's -12.05%; G7 the folds tile all three panels with no overlap and no gap; G8 CH_ANCHOR's move rate is exactly 0; **G9 every in-domain unit carries a hand read, 0 unaudited**; G10 the domain split partitions the checkable set exactly; G11 the census is deterministic across two runs, 0 differences; G12 each stitched curve's length == the sum of its folds'.

  **RECOMMENDATION, PROPOSED NOT ENACTED (rule 6): a committed re-read that corrects a statistic for its own COUNT must state the DOMAIN of the correction and report the units outside it SEPARATELY — never as failures.** On this corpus alone that restates 78 of 96 of 1155's own checkable units, and it would have stopped a headline of "two in five committed spread multiples are not effects" standing in the record when the traceable number is ZERO. A secondary clause follows from the price arm: **a count-matching bar whose value falls BELOW 1 is not a correction, it is a handicap on the SHORTER ladder**, and it does so at 0.5317 of this record's realised ladder pairs.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists; SMALL is the current output of a sub-$2B screen less the documented `max_1d_move >= 1.0` exclusion (51 of 715 dropped, 664 investable names plus SPY as benchmark only). Every CAGR and drawdown LEVEL is optimistic and the 4b counts are an UPPER bound. The census and trace arms are a scan of committed text and carry no market bias at all; the chooser deltas rank one construction against itself on one tape and the bias very largely cancels out of the DELTAS, but it does NOT cancel out of the rule-8 OOS levels.

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_how-many-committed-RANGE-claims-quote-a-MULTIPLE-BELOW-THEIR-OWN-COUNT-INFLATION_C.py`, 8 CSVs, console log, result note, 4 LEADERBOARD rows. Runtime 55s, offline, deterministic. Follow-ups filed 1213 (do the record's other count corrections state their own domain), 1214 (is a count-matching bar below one a correction or a handicap) and 1215 (how many committed census headlines have never been traced to a verdict).

- 2026-09-17 (lane C, idea 1206 does-the-WIDEST-DIAL-HABIT-lose-to-DOING-NOTHING-on-ladders-1155-did-not-walk) — **ANSWERED = (B) A THREE-DRAW ACCIDENT. THE DO-NOTHING ADVANTAGE DOES NOT SURVIVE DOZENS OF PICKS AND 3 OF ITS 4 SIGNS FLIP; NOR DOES THE HABIT BEAT DOING NOTHING — THE WHOLE ORDERING IS UNRESOLVED AT max abs(t) = 0.30 OVER 472 PICKS PER RULE.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the SECOND open idea; 1206 was second in '## Open' and is not EDGAR / Form 4 / 8-K / options / live-data. Price-native throughout — 228 real books, 3,304 chooser pick-cells and 252 stitched deployable curves — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `WINDOW` {2y, 3y, 4y} x `ANCHOR` {A_REC (20/126/0.75/W, 1155's own), A_TIGHT (10/63/0.55/W), A_SLOW (30/252/0.75/M), A_FAST (5/21/0.50/W)} = **12 cells, EVERY ONE PUBLISHED.** NOT dials, reported at every value: PANEL {U56, B136, SMALL} (rule 9); 1155's four ladders {N 6 rungs, H 4, GROSS 10, CADENCE 2} and four matching rules {M_NONE, M_SUBSAMPLE, M_PAIRWISE, M_D2}, inherited unchanged; three controls — **C_ANCHOR** (never move, 1155's do-nothing comparand), **C_RANDOM** (uniform ladder then its IS argmax, which separates "is WIDEST informative" from "does tuning ANY ladder cost") and **C_BEST_IS** (global IS argmax, the maximal-tuning upper bound). Folds = one calendar year of OOS, stepped one year, 2013-2026, non-overlapping and tiling the span exactly. 10 bps, t+1 execution, 260-row warm-up, book construction frozen at 1098/1159's min-hold form.

  **(0) THE ARITHMETIC, PRINTED BEFORE ANY DATA WAS TOUCHED.** A mean of n draws with per-draw SD s has SE s/sqrt(n), so for a 3-draw mean to resolve a gap g at 2 SE the per-pick SD must satisfy s < g*sqrt(3)/2. **1155's four published gaps (+0.0262 / +0.0736 / +0.0736 / +0.0069) therefore needed per-pick SDs below 0.0227 / 0.0637 / 0.0637 / 0.0060** — against a real book OOS-Sharpe per-pick SD of order 0.3 on this tape. **THE FIVE-WAY ORDERING WAS ARITHMETICALLY UNRESOLVABLE BEFORE THE DATA WAS LOOKED AT.** Outcomes (A) measurement / (B) three-draw accident / (C) reversal / (D) degenerate were pre-declared in the same block.

  **(A) 1155's ARM C REPRODUCED BIT-FOR-BIT, THEN GIVEN 472 PICKS PER RULE INSTEAD OF 3.** The single-split leg replays 1155's committed five-way ordering to **3.48e-05 on all five rules** (C_ANCHOR 0.8934, M_D2 0.8865, M_NONE 0.8672, M_SUBSAMPLE = M_PAIRWISE 0.8198), with the same widest-ladder calls. The rolling walk then supplies **3,304 pick-cells = 3 panels x 4 anchors x 3 windows x 14 folds x 7 rules, 472 per rule.**

  **(B) THE ANSWER.** Mean OOS Sharpe over 472 picks, paired delta against the do-nothing control, SE clustered by fold (folds tile the tape without overlap, so fold means are the independent units; panels and anchors inside a fold are not): **M_NONE 0.9895 (+0.0017, SE 0.0526, t 0.03); M_SUBSAMPLE 1.0046 (-0.0135, SE 0.0463, t -0.29); M_PAIRWISE 1.0046 (-0.0135, t -0.29); M_D2 1.0054 (-0.0143, SE 0.0479, t -0.30); C_ANCHOR 0.9911; C_RANDOM 0.9718 (+0.0193, t 0.69); C_BEST_IS 0.9803 (+0.0109, t 0.18).** **NO DELTA CLEARS 2 SE AND THE LARGEST abs(t) IN THE RUN IS 0.30.** Sign stability: C_ANCHOR ahead at **8 / 6 / 6 / 5 of the 12 (window, anchor) cells**, at 2 of 3 panels, and at **6 / 5 / 5 / 5 of 14 folds** — at or below a coin flip on three of the four rules — and **top of the seven rules at only 3 of 12 grid points, median rank 3.5**, against top-of-five in 1155's single reading. NOT outcome (D): move rates are **0.7267-0.9915** for every chooser (C_ANCHOR 0.0000 by construction, gated), and dropping the zero-deltas gives the same answer (+0.0019 / -0.0174 / -0.0174 / -0.0184 at abs(t) <= 0.30).

  **(C) TWO THINGS THE ROLLING WALK FOUND THAT THE THREE-DRAW READING COULD NOT.** (i) **THE PANEL SPLIT IS THE LARGEST REAL EFFECT IN THE RUN AND IT REVERSES:** C_ANCHOR wins on U56 (**1.2165** vs 1.1689-1.1823) and B136 (**1.1708** vs 1.1270-1.1475) and **LOSES ON SMALL (0.5298 vs 0.6718-0.6783)** — doing nothing is a LARGE-CAP fact, and on the small-cap panel the widest-dial habit is worth +0.15 of OOS Sharpe. 1155's pooled three draws could not see this. (ii) **GROSS IS CALLED WIDEST AT 0 OF 1,888 MATCHING-RULE PICKS** — idea 1189's gross-rung degeneracy of Sharpe, reproduced from a fifth direction by a run not looking for it. The habit lands on **H (0.4343-0.4809) and N (0.2521-0.4640)**, and **CADENCE takes 0.3072-0.3136 under the three count-matched rules against 0.0551 under M_NONE** — 1155's own count inflation showing up not as a number but as a change of WHICH DIAL GETS TUNED.

  **RULE 8 AND BOTH KEEP PATHS, INCLUDING A DIAL CONFOUND THIS RUN'S OWN WALK-FORWARD CAUGHT.** Dials chosen on folds ending 2016 or earlier, 2017-2026 read ONCE. Naively decisive: C_ANCHOR picks (2y, A_SLOW) for OOS mean fold Sharpe **1.0562 / 13.51% CAGR / -12.84% DD** against the matching rules' 0.7376-0.8325 at their own IS-chosen (3y, A_FAST). **THAT GAP IS THE ANCHOR, NOT THE CHOOSER — the two cells are different books.** Read at MATCHED cells over all 12, the means collapse to **M_NONE +0.0272, M_SUBSAMPLE -0.0012, M_PAIRWISE -0.0012, M_D2 +0.0013**, with a per-cell range of **-0.2223 to +0.1689**: cell-to-cell noise about ten times the effect. Stitched over 2017-2026 at each rule's own cell: **C_ANCHOR 12.83% / 0.8773 / -25.42%; M_SUBSAMPLE 10.61% / 0.7665 / -20.06%; M_NONE 9.19% / 0.7234 / -23.01%; M_D2 9.54% / 0.6974 / -21.45%; C_BEST_IS 12.79% / 0.7650 / -31.15%; C_RANDOM 10.63% / 0.7672 / -25.13%**, against SPY OOS Sharpe 0.8741 and the live RULES v2 book's 1.0099. Benchmarks over the stitched span: **U56 SPY 14.88% / 0.9116 / -33.72% (OOS 15.15% / 0.8686); B136 and SMALL SPY 15.01% / 0.9180 / -33.72% (OOS 15.33% / 0.8769); live RULES v2 Sharpe 1.2986 / 1.1725 / 0.8141 (OOS 1.2717 / 1.1061 / 0.6518).** **BOOKS (228): 4a 1 of 228; 4b full 45, 4b OOS 50, BOTH 43** — U56 29 books on **16 distinct (N, H, cadence) cells**, B136 14 on **8**, SMALL 0 on every path; the remainder are one book at a different gross rung (1189 again). **STITCHED CHOOSER CURVES (252): 4a 0 of 252; 4b full 22, 4b OOS 42, BOTH 21**, every one on U56 or B136 and **nine of them the do-nothing control**. Best: **U56 / A_REC / 2y / M_SUBSAMPLE 16.85% / 1.1651 / -19.99%, halves 1.190 / 1.164, OOS 17.75% / 1.1715** — against C_ANCHOR at the SAME cell 15.90% / 1.1523 / -19.47% (OOS 16.13% / 1.1042). **RECORDED, NOT PROMOTED, NO MEMO:** 4a is 0 of 252, the 21 are the survivors of 252 readings over a 12-cell dial grid x 7 rules x 3 panels, the best beats the do-nothing anchor at ONE cell while the paired grid-wide delta is within noise of zero, and the deployable object underneath is the ANCHOR BOOK ITSELF (U56 N=20 / H=126 / g=0.75 / W) — the record's standing lineage. The chooser adds nothing to it.

  **GATES 10 OF 10 PASS.** G1 the fast runner == `engine.backtest` on the record's anchor at **2.78e-17**, carrying 1155's own decision-time correction (`build` writes APPLICATION-time rows, so the comparand must be `Wdec[t] = W[t+1]`); **G2 the gross ladder is provably the selection frame SCALED (0.000e+00), which is why 10 gross rungs cost one selection build and not ten**; G3 live RULES v2 U56 MaxDD **-12.0549%** against the record's -12.05%; **G4 the 1155 replay at 3.48e-05 on all five rules**; G5 the folds tile the evaluation span with no overlap and no gap; G6 C_ANCHOR's move rate is exactly 0; G7 the paired design is balanced (identical pick count per rule, so every delta is a true pairing); G8 each stitched curve's length == the sum of its folds'; G9 the IS window ends strictly before its fold's first bar, by construction; G10 on a 2-rung ladder M_NONE == M_SUBSAMPLE == M_PAIRWISE by identity.

  **RECOMMENDATION, PROPOSED NOT ENACTED (rule 6): a committed CHOOSER COMPARISON must state its PICK COUNT, and a mean over fewer picks than its own per-pick SD can resolve must carry that SD.** 1155's Arm C spanned a five-way ordering of 0.0736 on three draws of a statistic whose per-draw SD is ~0.3; the reading survived into the record as "the record's habit loses to doing nothing", and it does not.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists; SMALL is the current output of a sub-$2B screen less the documented `max_1d_move >= 1.0` exclusion. Every CAGR and drawdown LEVEL is optimistic and the 4b counts are an UPPER bound. The chooser comparison is one construction against itself on one tape and the bias very largely cancels out of the DELTAS; it does not cancel out of the levels, nor out of the SMALL-panel reversal, which is filed as a follow-up for that reason.

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_does-the-WIDEST-DIAL-HABIT-lose-to-DOING-NOTHING-on-ladders-1155-did-not-walk_C.py`, 10 CSVs, console log, result note, 4 LEADERBOARD rows. Follow-ups filed 1208 (is the SMALL-panel reversal a panel fact or a turnover fact), 1209 (does the count-matched rules' shift of the widest dial from N to CADENCE change any committed verdict) and 1210 (how many committed chooser comparisons in the record rest on fewer than ten picks).

- 2026-09-17 (lane C, idea 1197 is-the-BOOK-s-NULL-PERCENTILE-SATURATED-at-every-cell-the-record-quotes-it) — **ANSWERED = YES ON BOTH LARGE-CAP PANELS, AND 23 COMMITTED CLAIMS WERE ADJUDICATED AGAINST A STATISTIC PINNED AT ITS CEILING.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the SECOND open idea; 1197 was second in '## Open' and is not EDGAR / Form 4 / 8-K / options / live-data. Price leg of 12 real books, 4,800 gross-matched null backtests and 54 rule-8 chooser cells, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `CLAIM SET` {C_STRICT, C_PROX, C_ALL} x `DRAW COUNT K` {10, 20, 50, 100, 200, 400} = **18 cells, EVERY ONE PUBLISHED.** NOT dials, reported at every value: PANEL {U56, B136, SMALL}; ANCHOR (N, cadence) {(20,W), (12,W), (20,M), (10,M)}; 20 seeds at every K; the statistics {CH_PCT, CH_Z, CH_ISSHARPE}; the 4a/4b legs; the three rule-8 choosers. **Pool = 400 gross-matched null books per (panel, anchor)** — at every rebalance date N names drawn uniformly from those priced that day at 0.75/N, 10 bps, next-day execution. Book construction frozen at the 2026-09-04 KEEP-4b candidate's: composite (12-1 + 6m + 3m percentile ranks), no vol scaler, above-own-200d-MA eligibility, top-N equal weight, gated-out weight to CASH.

  **(A) THE CENSUS — 103 CLAIMS, 26 AT THE CEILING, 23 OF THEM LOAD-BEARING.** Corpus = **32,819 committed text units** (6,999 LEADERBOARD rows, 584 CHANGELOG paragraphs, 1,134 markdown artefacts) at commit 9e641f3. **C_STRICT n=103: ceiling-quoted 26 (0.2524), states a draw count 40 (0.3883), adjudicated 85, ADJUDICATED AT THE CEILING 23 (0.2233). C_PROX n=248 -> 26 / 23. C_ALL n=1,718 -> 36 / 29.** The 26 split **13 memo/result notes, 12 LEADERBOARD rows, 1 CHANGELOG paragraph**, so the ceiling is not one file's house style. **THE LARGER HALF OF THE PROBLEM IS REPORTED SEPARATELY AND NEVER MERGED INTO THE 23: 49 of the 85 adjudicated units state NO draw count at all**, so their resolution is not recoverable from the committed text in either direction. **22 of 103 are OVERQUOTED** — more decimal places than 100/(K+1) pp, the finest their own stated K can carry.

  **(B) THE RE-PRICING — THE RECORD'S COMMONEST NULL STATISTIC IS A CONSTANT ON B136 AT ITS OWN DRAW COUNT, AND SATURATION SURVIVES 40x MORE DRAWS.** Share of the 80 (anchor, seed) cells with CH_PCT pinned at exactly 1.000, over K = 10 / 20 / 50 / 100 / 200 / 400: **U56 0.963 / 0.938 / 0.838 / 0.825 / 0.613 / 0.500; B136 1.000 / 0.963 / 0.950 / 0.900 / 0.762 / 0.750; SMALL 0.425 / 0.412 / 0.163 / 0.125 / 0.037 / 0.000.** **THE BLUNTER STATEMENT IS THE DISTINCT-VALUE COUNT: CH_PCT takes 1 value over all 80 cells on B136 at K=10, 2 on U56, and never more than 6 at any K <= 200 on either large-cap panel — against CH_Z's 80 of 80 at every rung.** Anchors tied at the maximum, of 4 (1191's statistic): **U56 3.85 -> 2.00 and B136 4.00 -> 3.00** over the K ladder, every tie being saturation at the ceiling, while **CH_Z reads 1.00 at 18 of 18 (panel, K) cells — it never ties at all.** **SMALL IS THE CONTROL THAT MAKES THIS A PROPERTY OF THE STATISTIC AND NOT OF THE MACHINERY:** same code, same pool size, and it de-saturates to **0.000 at K=400**. Idea 1191's K=10 reading is therefore confirmed and extended — at K=400, forty times the draw count it flagged, B136 is still pinned at 0.750.

  **RULE 8 AND BOTH KEEP PATHS — THE SATURATED STATISTIC MISSES THE ONLY 4b BOOK AT EVERY K ON EVERY PANEL.** Benchmarks: **U56 SPY 15.06% / 0.8815 / -33.72% (halves 0.9600/0.8171), OOS 15.15% / 0.8686; U56 RULES v2 (live) @10 bps 8.60% / 1.1982 / -12.05% (halves 1.2332/1.1705), OOS 9.42% / 1.2717; B136 SPY 15.16% / 0.8862 / -33.72%, OOS 15.33% / 0.8769; B136 LIVE 7.98% / 1.0994 / -12.24%, OOS 7.88% / 1.1061; SMALL SPY 14.06% / 0.8582 / -33.72%, OOS 0.8769; SMALL LIVE 4.64% / 0.7130 / -12.18%, OOS 4.47% / 0.6518.** Chosen on 2009-2016 only, 2017-2026 read ONCE. **Mean OOS Sharpe over the 18 (panel, K) picks: CH_Z 0.9324, CH_ISSHARPE 0.9065, CH_PCT 0.8931 — the saturated form costs 0.0393 of OOS Sharpe against the same comparison standardised by the pool's own mean and sd, which is free to compute.** CH_PCT lands on N=10/M on BOTH large-cap panels at K <= 100 (U56 OOS 1.0115, B136 OOS 0.9468) purely because FIRST-WINS breaks a four-way tie; **CH_Z lands on U56 N=20/W at 5 of 6 K rungs, and CH_PCT reaches that book at 0 of 18 cells.** Ties are broken first-wins and DETERMINISTICALLY with the tied count published, because a random tie-break would have manufactured the very instability being measured. **1 of 12 books clears 4b full AND OOS at 10 bps; 0 of 12 clear 4a. 5 of 54 rule-8 picks clear 4b (all CH_Z on U56); 0 of 54 clear 4a.** The one passer — **U56 / N=20 / W, 14.31% / 1.1520 / -19.09%, halves 1.229/1.103, OOS 15.66% / 1.1705** — is the standing 2026-09-04 incumbent, so it is **CONFIRMATORY, NOT GENERATIVE. NO NEW CANDIDATE, NO MEMO, NOTHING ENACTED.**

  **GATES 7 OF 7 PASS.** G1 the vectorised segment runner == `engine.backtest` post-warm-up at **2.08e-17**; G2 determinism **0.00e+00**; G3 live RULES v2 U56 MaxDD **-12.0549%** against the record's -12.05%; **G4 the arithmetic the whole finding rests on, checked and not assumed — CH_PCT takes at most K+1 distinct values at every rung, 0 violations**; **G5 CH_Z mean anchors tied at its maximum = 1.000**, i.e. the de-saturated comparator provably has no ceiling on this grid; G6 the census sets nest C_STRICT <= C_PROX <= C_ALL, 0 violations; G7 the null's target gross == 0.75 at every rebalance row, **1.11e-16**.

  **RECOMMENDATION, PROPOSED NOT ENACTED (rule 6): a committed percentile-of-its-own-null figure should state its DRAW COUNT, and a value of 1.000 (or 0.000) should be published as a BOUND — "> 1 - 1/(K+1)" — never as a point.** On this grid alone that restates 26 of 103 committed claims and would have stopped 23 of them being used to adjudicate anything. Where a ranking is actually wanted, publish the null-standardised z beside it: it costs nothing extra to compute, it does not tie, and here it is worth +0.0393 of mean OOS Sharpe.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists; SMALL is the current output of a sub-$2B screen less the documented `max_1d_move >= 1.0` exclusion. Every CAGR and drawdown LEVEL is optimistic and the 4b count is an UPPER bound. The census arm is a scan of committed text and carries no market bias at all; the saturation shares rank one construction against its own null on one tape and the bias very largely cancels, but it does NOT cancel out of the rule-8 OOS levels.

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_is-the-BOOK-s-NULL-PERCENTILE-SATURATED-at-every-cell-the-record-quotes-it_C.py`, 6 CSVs, console log, result note, 4 LEADERBOARD rows. Follow-ups filed 1199 (should a saturated percentile be publishable as a point at all), 1200 (how many committed claims quote a percentile finer than their own draw count resolves) and 1201 (does CH_Z's +0.0393 survive a rolling IS window).

- 2026-09-17 (lane cloud, idea 1191 how-many-committed-MONTE-CARLO-STATISTICS-in-the-record-are-REPRODUCIBLE-FROM-THEIR-ARTEFACT) — **ANSWERED = 39 OF 32,785, AND IRREPRODUCIBILITY IS A KERNEL PROPERTY, NOT A COMPUTE TRADE-OFF.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the LAST eligible open idea; 1191 ended '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Price leg of 12 real books, 4,800 gross-matched null backtests and 36 rule-8 chooser cells, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `CLAIM SET` {C_STRICT, C_PROX, C_ALL} x `STATISTIC FORM` {F_MC (K sampled pairs), F_EXACT (all KxK pairs), F_POOLED} = **9 cells, EVERY ONE PUBLISHED.** NOT dials, reported at every value: PANEL {U56, B136, SMALL}; ANCHOR (N, cadence) {(20,W), (12,W), (20,M), (10,M)}; the K ladder {10, 20, 50, 100, 200, 400}; 20 seeds at every K; the three pair kernels {DIFF, RATIO, IND}; the 4a/4b legs; the three rule-8 choosers. **Pool B = 400 gross-matched null books per (panel, anchor)** — at every rebalance date N names drawn uniformly from those priced that day at 0.75/N, 10 bps, next-day execution — and **Pool A = 400 63-day moving-block resamples of the real book's own daily net returns.**

  **(A) THE CENSUS — 39 OF 32,785, AND THE DIAL MOVES ONLY THE DENOMINATOR.** Corpus = **32,785 committed text units** (6,995 LEADERBOARD rows, 576 CHANGELOG paragraphs, 1,133 markdown artefacts) at commit 15f6eec. **C_STRICT n=3,016 states SEED 0.2218 / DRAW COUNT 0.3558 / PAIR FORM 0.0869 / ALL THREE 0.0129 = 39. C_PROX n=6,339 -> 0.0062 = 39. C_ALL n=25,308 -> 0.0015 = 39.** The numerator is **39 at every claim set** because the sets nest and a unit stating all three necessarily carries a generator token — so the honest headline is a COUNT and not a share. **The binding leg is the PAIR FORM (0.0869 even under the strictest set), not the seed (0.2218).** Of 5,904 committed CSV artefacts under `research/backtests`, **442 (0.0749) carry a seed / draw / rep column at all.**

  **(B) WHAT IS ACTUALLY LOST, AND IT IS NOT COMPUTE. FOR A LINEAR KERNEL THE PAIRING IS NOT INFORMATION:** the mean over K sampled pairs of (a - b) equals mean(a) - mean(b) equals the all-pairs mean, IDENTICALLY — **|F_MC - F_EXACT| = 0.000000 at every K and every one of 12 cells**, and G2 puts F_EXACT == F_POOLED at **4.44e-16**. **A linear pair statistic never needed its draw order and is always recoverable from its pooled artefact.** Everything the record has lost is lost to NONLINEARITY: the indicator form's pooled substitute is wrong by up to **0.500** (F_POOLED collapses to a constant 0/1 — its cross-seed SD is exactly 0) and the ratio form's by up to **43.29**. **1148's R_MATCHED, the statistic 1188 could not replay, is a RATIO — the worst of the three.** An MC statistic published without its seed replays to 5e-3 between **0.05 and 0.68** of the time at the draw counts the record uses. **AND THE EXACT-PAIR FORM COSTS NOTHING: at K=400 on SMALL/N=20/W, F_MC 4.0 us against F_EXACT 227.0 us (57.3x) — against the 33.8 SECONDS of backtests BOTH forms share, i.e. the exact pairing is 1.49e5x cheaper than the pools it summarises.** The record's irreproducibility was not bought with compute.

  **RULE 8 AND BOTH KEEP PATHS — THE RECORD'S OWN NULL STATISTIC IS SATURATED, AND THE SEED IS WORTH 0.164 OF OOS SHARPE WHERE IT IS NOT.** Benchmarks: **U56 SPY 15.06% / 0.8815 / -33.72%, OOS 0.8686; U56 RULES v2 (live) @10 bps 8.60% / 1.1982 / -12.05%, OOS 1.2717; B136 SPY 15.16% / 0.8862 / -33.72%, OOS 0.8769; B136 LIVE 7.98% / 1.0994 / -12.24%, OOS 1.1061; SMALL SPY 14.06% / 0.8582 / -33.72%, OOS 0.8769; SMALL LIVE 4.30% / 0.6637 / -13.89%, OOS 0.5601.** Chosen on 2009-2016 only, 2017-2026 read ONCE. **CH_PCT — the record's form, the book's IS Sharpe percentile inside its own null — cannot rank anchors at all on the large-cap panels:** mean anchors tied at the maximum, of 4, runs **U56 3.80 -> 2.00 and B136 3.90 -> 3.00** over K = 10 -> 400, every tie being saturation at 1.000, so **the seed PROVABLY cannot move that pick — 1 distinct pick over 20 seeds at 6 of 6 K rungs on both panels.** Only SMALL de-saturates (1.80 -> 0.00 tied) and there the seed moves it once, at K=50. **CH_Z — the same comparison standardised by the pool's own mean and sd, which does not saturate — moves with the seed at exactly the draw counts the record publishes: U56 3 distinct picks over 20 seeds at K=10 (modal share 0.85), OOS Sharpe spread 0.1637; B136 3 distinct at K=10 and K=20 (modal 0.65), spread 0.1097; SMALL 2 distinct, spread 0.0081** — and the instability dies at K >= 50 (U56, SMALL) and K >= 200 (B136). **So an unpublished seed is worth up to 0.164 of OOS Sharpe on a K=10 null and nothing on a K=200 one.** Ties are broken FIRST-WINS and DETERMINISTICALLY and the tied count published, because a random tie-break would have manufactured the very instability being measured. **1 of 12 books clears 4b full AND OOS at 10 bps; 0 of 12 clear 4a.** The one passer — **U56 / N=20 / W, 14.18% / 1.1411 / -19.39%, halves 1.223/1.088, OOS 15.55% / 1.1598, null percentile 100.0** — is the standing 2026-09-04 incumbent, so it is **CONFIRMATORY, NOT GENERATIVE. NO NEW CANDIDATE, NO MEMO, NOTHING ENACTED.** CH_ISSHARPE (no null at all) disagrees with the null-based choosers at 3 of 3 panels and picks the WORSE OOS book at 3 of 3 (U56 1.0031, B136 0.9735, SMALL 0.7360), which argues FOR the null provided its draw count resolves.

  **GATES 8 OF 8, INCLUDING ONE DEFECT THIS RUN FOUND IN THE ENGINE AND ONE IT FOUND IN ITSELF. G1b, REPORTED NOT ABSORBED: `engine.backtest` does `weights.shift(1)` WITHOUT filling, so row 0's target is NaN; turnover is NaN at row 0 and again at the first rebalance-application row, and the NaN propagates into `returns` at exactly those two rows (2008-01-02 and 2008-01-07 on U56). Every committed 'fast runner == engine.backtest' gate in the record compared pandas SERIES, whose `.max()` is skipna, so those two rows have never been checked by any of them.** Both sit inside the 260-day warm-up every metric discards (ends 2009-01-13), so **NO committed figure moves** — but the gate meant to catch it could not. Filed as 1198. **G4b, A GATE THIS RUN RESTATED RATHER THAN RE-CUT:** the first cut gated "F_EXACT is less seed-dependent than F_MC" over all kernels and **FAILED at 0.8936**; every failure is a RATIO cell (**23 of 72**) and it is a FINDING — all-pairs enumeration gives the near-zero-Sharpe denominators MORE weight than random pairing does, so the exact form of a ratio is more seed-dependent, not less. The gate now reads over DIFF+IND (**1.000 of 144 cells**) with the RATIO share published beside it. G1 post-warm-up **2.08e-17**; G2 **4.44e-16**; G3 **43.29** (> 0 expected); G5 live RULES v2 U56 MaxDD **-12.0549%** against the record's -12.05%; G6 determinism **0.00e+00**.

  **RECOMMENDATION, PROPOSED NOT ENACTED (rule 6): a committed Monte-Carlo figure should state its PAIR FORM before its seed.** Publish the EXACT-PAIR value for any nonlinear kernel — it costs 1.49e5x less than the pools it summarises and is seed-free by construction — and publish a LINEAR kernel's POOLED MEANS instead of its pairs, since they are the same number.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists; SMALL is the current output of a sub-$2B screen less the documented `max_1d_move >= 1.0` exclusion (52 of 715 dropped, 663 investable names plus SPY as benchmark only). Every LEVEL is optimistic and every 4a/4b count is an UPPER bound. The census arm is a scan of committed text and carries no market bias at all; the reproducibility statistics rank one construction against itself on one tape and the bias largely cancels, but it does NOT cancel out of the rule-8 OOS levels.

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_how-many-committed-MONTE-CARLO-STATISTICS-in-the-record-are-REPRODUCIBLE-FROM-THEIR-ARTEFACT_cloud.py`, 6 CSVs, console log, result note, 4 LEADERBOARD rows. Follow-ups filed 1196 (should a committed MC figure state its pair form before its seed), 1197 (is the book's null percentile saturated at every cell the record quotes it) and 1198 (how many committed runner-equivalence gates were read through a skipna max).
- 2026-09-17 (lane cloud, idea 1189 is-the-GROSS-RUNG-DEGENERACY-of-SHARPE-a-UNIVERSAL-property-of-the-de-GROSSED-BOOK) — **ANSWERED = YES, UNIVERSAL, AND IT IS THE MEDIAN CASE NOT THE EXCEPTION.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the FIRST eligible open idea; 1189 headed '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Price leg of 120 real books and 9 rule-8 picks, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `GROSS RUNG` {0.30, 0.40, 0.50, 0.60, 0.70, 0.75, 0.85, 1.00} x `ANCHOR` (N, cadence) {(20,W), (12,W), (30,W), (10,M), (20,M)} = **40 cells per panel, EVERY ONE PUBLISHED** in `.grid.csv`. NOT dials, reported at every value: PANEL {U56, B136, SMALL}; the four statistics (Sharpe, CAGR, MaxDD, turnover); the 4a and 4b legs; the three rule-8 choosers. **3 x 5 x 8 = 120 books.** Book construction frozen at the 2026-09-04 KEEP-4b candidate's: composite (12-1 + 6m + 3m percentile ranks), **no vol scaler**, eligibility = above own 200d MA, top-N equal weight at g/N of NAV, gated-out weight to CASH at 0%.

  **(A) THE RUNG SPREAD DISTRIBUTION.** Over the 15 (panel, anchor) cells the RELATIVE rung spread (max - min across the 8 rungs, as a share of the cell's mean) runs **Sharpe median 0.561% (min 0.026%, max 4.027%) against CAGR 109.52%, MaxDD 100.81% and turnover 109.09%** — a **median CAGR/Sharpe spread ratio of 199.7x**, min 24.6x, max 4,277x. Absolute Sharpe rung spread is **< 0.01 at 10 of 15 cells and < 0.05 at 15 of 15**, and the worst cell in the whole grid is SMALL / N=10 / M at **0.02741 over a 3.33x change in gross**. **Not one of 15 cells contradicts 1158's 8e-04 reading; it is the median case.** **THE MECHANISM IS MEASURED, NOT ASSUMED:** the weight identity W(g) = g*W(1) holds by construction, so mean, vol and cost drag all scale by g and Sharpe cancels; the entire residual is the cash sleeve's compounding, and **G3 prices it at max |r(0.30) - 0.30*r(1.00)| = 2.087e-03**, independently reproducing 1177 from a second direction. The gross dial is live on exactly the statistics it is supposed to move (G5 min relative CAGR spread 0.9834 at 15 of 15; G6 |MaxDD| non-decreasing in gross at 15 of 15).

  **(B) WHAT A 4b PASS AT A NEW GROSS RUNG ACTUALLY IS — 14 PASSES, 8 BOOKS.** **14 of 120 books clear 4b full AND OOS at 10 bps; 1 of 120 clears 4a.** The 14 sit on only **8 distinct (panel, anchor) cells — 6 of 14 (0.43) are an anchor already counted, re-read at a different gross rung.** Within an anchor the Sharpes agree and the CAGRs do not: **U56 / N=20 / W at g = 0.60 / 0.70 / 0.75 reads Sharpe 1.14080 / 1.14089 / 1.14099 (spread 1.9e-04) while CAGR runs 11.32% -> 14.18% and MaxDD -15.73% -> -19.39%**; U56 / N=30 / W at g = 0.70 / 0.75 agrees to **4e-06**. Per-leg failures over all 120 books: **L_DD 70, L_H2 56, L_OOS 48, L_CAGR 41, L_H1 24** — the two legs the gross dial moves (L_DD and L_CAGR) are 111 of the 239 failures and they move in OPPOSITE directions, which is the whole of what walking the ladder does: **it slides one book along a fixed-Sharpe CAGR-versus-drawdown line. A 4b pass found by lowering gross is not a new rule; it is the same rule with less of it.** This is the third independent reproduction of the premise on 2026-09-17 alone (1158's rule-8 leg, lane B's 1192 run, and this grid).

  **RULE 8 AND BOTH KEEP PATHS — THE GROSS DIAL IS NOT A CHOOSER DIAL, AND FREEING IT COSTS DRAWDOWN.** Benchmarks: **U56 SPY 15.06% / 0.8814 / -33.72% (halves 0.9598/0.8170), OOS 15.15% / 0.8684; U56 RULES v2 (live) @10 bps 8.60% / 1.1980 / -12.05%, OOS 1.2714; B136 SPY 15.16% / 0.8861 / -33.72%, OOS 0.8767; B136 LIVE 7.98% / 1.0993 / -12.24%, OOS 1.1059; SMALL SPY 14.06% / 0.8581 / -33.72%, OOS 0.8767; SMALL LIVE 4.30% / 0.6637 / -13.89%, OOS 0.5600.** Parameters chosen on 2009-2016 only, 2017-2026 read ONCE. **Freeing the gross dial moves the chosen ANCHOR at 0 of 3 panels** and moves OOS Sharpe by +0.0040 (U56), +0.0048 (B136), -0.0000 (SMALL). **It is not free.** Because IS Sharpe is flat in gross the IS argmax lands on the ladder's **TOP rung g = 1.00 at 2 of 3 panels on noise of order 1e-3** and takes the drawdown with it: **U56 CH_ISSHARPE picks N=10 / M / g=1.00, OOS 23.11% / 1.0069 / -32.11%, against the frozen-gross pick's -25.03%.** **CH_ISCALMAR IS THE CONTROL THAT PROVES IT:** on SMALL it picks the SAME anchor at the BOTTOM rung g=0.30 — **OOS 6.48% / 0.7354 / -13.15% against the g=1.00 pick's OOS Sharpe 0.7358 at a third of the drawdown.** Two "different" picks, one book. **0 of 9 picks clear 4b, 1 of 9 clears 4a** (SMALL / CH_ISCALMAR, on the DD leg alone); 6 of 9 beat SPY OOS on Sharpe. **THE 14 PASSERS ARE NOT RULE-8 REACHABLE:** their IS-Sharpe ranks are 6, 12, 13, 17, 18, 19, 28, 29, 34, 35 of 40 on U56 and 6, 14, 36, 37 of 40 on B136, and none is the IS argmax. U56 / N=20 / W / g=0.75 is the standing 2026-09-04 incumbent, so its appearance here is **CONFIRMATORY, not generative. NO NEW CANDIDATE AND NO MEMO.**

  **GATES 6 OF 6 PASS.** G1 the vectorised segment runner == `engine.backtest` at **2.08e-17**; G2 determinism **0.00e+00**; G3 the cash sleeve breaks r(g) = g*r(1) at **2.087e-03** (> 0 expected — this run does NOT assume the return-path identity anywhere); G4 live RULES v2 U56 MaxDD **-12.0549%** against the record's -12.05%; G5 the gross dial moves CAGR at 15 of 15 cells (min relative spread 0.9834); G6 |MaxDD| non-decreasing in gross at 15 of 15.

  **RECOMMENDATION, PROPOSED NOT ENACTED (rule 6): report a 4b pass keyed on (panel, N, cadence, H), with GROSS quoted as an attribute rather than as part of the key.** On this grid alone that collapses 14 claimed passes to 8.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists; SMALL is the current output of a sub-$2B screen less the documented `max_1d_move >= 1.0` exclusion (52 of 715 dropped, 663 investable names plus SPY as benchmark only). Every CAGR and drawdown LEVEL is optimistic and every 4a/4b count is an UPPER bound, the 14 included. The bias largely cancels out of a RUNG SPREAD, which ranks one construction against itself on one tape; it does NOT cancel out of the rule-8 OOS levels.

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_is-the-GROSS-RUNG-DEGENERACY-of-SHARPE-a-UNIVERSAL-property-of-the-de-GROSSED-BOOK_cloud.py`, 4 CSVs, console log, result note, 4 LEADERBOARD rows. Follow-ups filed 1193 (does the degeneracy survive a non-zero cash rate), 1194 (how many committed 4b passes collapse under a gross-free key) and 1195 (does the IS argmax land on a ladder boundary whenever the statistic is flat).
- 2026-09-17 (lane C, idea 1174 how-many-committed-H-AXIS-claims-rest-on-the-THREE-POINT-LADDER-21-63-126) — **ANSWERED = 15 OF 60 EXACTLY, BUT 28 OF 60 HAVE NEVER BEEN OFF THAT LADDER AT ALL, AND THE THREE-POINT READING IS OVERTURNED AT 14 OF 18 FAMILIES — WHILE THE SIXTEEN-POINT ONE IS NOT A MEASUREMENT EITHER, AND ACTING ON IT LOSES 0.0559 OF OOS SHARPE.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the SECOND open idea; 1174 was second among the LIVE entries of '## Open' (the two ahead of it, 1177 and 1172, are stale duplicates of entries already in '## Done') and is not EDGAR / Form 4 / 8-K / options / live-data. Price leg of 288 real books and 10 rule-8 picks, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `CLAIM SET` {C_STRICT, C_PROX, C_ALL} x `LADDER SPACING` {L3, L7, LFINE} = **9 cells, EVERY ONE PUBLISHED**. The ladders are **STRICTLY NESTED and span the SAME range [21, 126]** (G7, G8), so a moved reading is attributable to RESOLUTION and never to RANGE: L3 = [21, 63, 126] (the record's), L7 = [21, 42, 52, 63, 76, 90, 126] (1093's, unchanged), LFINE = L7 + [26, 32, 37, 47, 57, 69, 83, 105, 115]. NOT dials, reported at every value: PANEL {U56, B136}; N in {5,8,10,12,15,20,25,30,40}; the four statistics; the 4a/4b legs; the five rule-8 choosers. 288 books, all published. Frozen at 936/1071/1082/1086/1093's construction.

  **GATES 12 of 13 PASS AND THE ONE FAILURE IS DIAGNOSED, NOT ABSORBED. G3 FAILS: the committed SPY OOS triple is out by 2.894e-03, ENTIRELY in the Sharpe term (0.8684 against the committed 0.8713); CAGR is out by 6e-04 and MaxDD by 0. G3b DIAGNOSES IT — this run reproduces 1093's SAME-DAY committed benchmarks, 4 series x 11 statistics on both panels, at 1.11e-16** — so the drift is against the OLDER anchor only: idea 1163's nightly-rewrite vintage effect arriving from the benchmark side, not a construction error. **G5 reproduces 1093's WHOLE committed grid cell-for-cell — 126 shared (panel, N, H) cells x 4 statistics — at 1.78e-15**, which is the premise of the entire re-pricing arm. G1 fast runner = `engine.backtest` 1.39e-17; G2 the committed W/H126 N=20 triple 1.62e-03; G4 live v2 MaxDD 4.95e-05; G6 determinism 0; G9 the H dial is live (B136 turnover spread 6.47x/yr); G10 the census partitions; G11 the repair below only ever removes.

  **A DEFECT THIS RUN FOUND IN ITSELF AND PRICED RATHER THAN HID.** The first cut matched the bare-letter H forms (`H=63`, `H in {...}`) CASE-INSENSITIVELY and swept in **`h=1` — idea 125's event-study forward horizon — and `h=8%/yr` — idea 54's delisting HAZARD** as if they were hold rungs. Different objects that share a letter. The bare forms are now case-SENSITIVE and the WORD forms ("hold", "min hold", "holding period") keep re.I. **The repair removes 57 of 299 units (0.191) and adds none.** Every figure below is post-repair; the pre-repair count is printed so the earlier population stays recoverable.

  **(A) THE CENSUS — AND THE QUEUE'S OWN FIGURE IS THE SMALLER HALF OF THE PROBLEM.** Corpus = **31,582 committed text units** (LEADERBOARD rows + CHANGELOG paragraphs + **909** `*.result.md` files); **242 carry an H-AXIS token AND name an H rung** (a rung is only counted ATTACHED to an H token, never a bare integer — deliberately conservative and an understatement). **EXACTLY {21,63,126}: 15 of 60 C_STRICT (0.250), 15 of 124 C_PROX (0.121), 18 of 242 C_ALL (0.074). CONFINED to it — the unit names only rungs drawn FROM the three, one, two or all: 28 of 60 (0.467), 75 of 124 (0.605), 155 of 242 (0.640).** The two are published side by side and NEVER merged, because **a claim measured at H=63 alone is exactly as unable to see a finer rung as one measured at all three.** **THE RUNG HISTOGRAM IS THE BLUNTEST STATEMENT OF IT: H=126 in 116 units, H=21 in 90, H=63 in 63 — and 1086/1093's four added rungs (42/52/76/90) in 8 to 10 units EACH, i.e. two runs. H = 26, 32, 37, 47, 57, 69, 83, 105 and 115 appear in ZERO committed units: nine of this run's sixteen rungs are rungs the record has never priced.** **THE 'CHEAPEST' IS A MEASUREMENT, NOT A CONVENIENCE** — every claim carries a re-pricing COST CLASS read off its own text: C_STRICT **P_BOOK 8, P_NULL 36, P_BOOT 11, P_TAPE 0, P_OTHER 5**, so the record's strict H-ladder claims are mostly the EXPENSIVE null kind and the cheap arm re-priced here is NOT the typical one.

  **(B) THE RE-PRICING — 288 BOOKS, AND THE THREE-POINT READING IS LOAD-BEARING.** A READING is the pair (argmax rung, monotone verdict); OVERTURNED means either component moves L3 -> LFINE. **Sharpe: overturned 14 of 18 (panel, N) families — argmax moved 14, monotone flipped 5. CAGR 15 of 18, MaxDD 12 of 18, turnover 14 of 18.** **PRE-DECLARED OUTCOME (A) THE 3-POINT LADDER IS LOAD-BEARING** against a pre-registered bar of 9 of 18; outcome (D) was checked FIRST and not reached. **THE INTERIOR-PEAK QUESTION HAS AN EXACT ANSWER: on a three-point ladder the ONLY interior rung is 63, so every 'interior peak' the record has read off it is the claim "H=63 is best" — 10 of 18 families read one on L3, and LFINE keeps it at 63 in only 3 of those 10** (the rest move to 57, 76, 83, 90 or 115); on CAGR, 3 of 11. **TURNOVER IS THE CONTROL THAT MAKES THE POINT: its argmax moves at only 2 of 18 — a longer min hold trades less at any resolution — while its monotone verdict flips at 14 of 18. The coarse ladder is reliable for the mechanical statistic and unreliable for every statistic that carries return.** **THE CAVEAT AT FULL STRENGTH, NOT BURIED: LFINE HAS NOT CONVERGED EITHER — the Sharpe argmax still moves between L7 and LFINE at 10 of 18.** This run does NOT claim LFINE's argmax is the true one; it claims the three-point one is not a measurement, and by the same evidence neither is the sixteen-point one. Filed as 1181.

  **RULE 8 AND BOTH KEEP PATHS — 288 BOOKS, ALL PUBLISHED, AND BUYING RESOLUTION ON THE HOLD AXIS COSTS OOS SHARPE.** Benchmarks: **U56 SPY 15.06% / 0.8814 / -33.72% (halves 0.9598/0.8170), OOS 15.15% / 0.8684; U56 RULES v2 (live) @10 bps 8.60% / 1.1980 / -12.05%, OOS 9.42% / 1.2714; B136 SPY 15.16% / 0.8861 / -33.72% (halves 0.9596/0.8259), OOS 15.33% / 0.8767; B136 LIVE 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059.** Base rates at 10 bps: **4b full 17, 4b OOS 18, 4b full AND OOS 17 of 288 (U56 16, B136 1), 4a 0 of 288.** By ladder membership **L3 reaches 8 of them from 54 cells, L7 9 from 126, LFINE all 17 from 288.** **With the LADDER as the chooser's constraint — (N, H) picked on 2009-2016 ONLY, 2017-2026 read ONCE — mean OOS Sharpe runs L3 1.0273, L7 1.0095, LFINE 0.9713, i.e. LFINE - L3 = -0.0559**, and LFINE's B136 pick (N=5, H=69) is the only one of six that fails to beat SPY out of sample. The extra rungs give the IS argmax more places to overfit: U56's IS optimum moves H=21 -> 90 and gives back 0.036 of OOS Sharpe, B136's 63 -> 69 and gives back 0.076. **THE ONE RULE-8-REACHABLE 4b BOOK IS REACHED FROM THE COARSE LADDER AND IS ALREADY COMMITTED: C_L3's U56 N=12 / H=21 — 16.76% / 1.1603 / -19.48%, halves 1.246/1.099, OOS 17.50% / 1.1389 — and idea 1151 already lists U56 N=12 among the books clearing 4b full and OOS at 10 bps, so this run's independent grid reproducing it is CONFIRMATORY, not generative.** Filed as 1183.

  **THE NINE NEW 4b CELLS ARE SELECTION, NOT DISCOVERY, AND THE IS RANKS ARE THE MEASUREMENT.** Nine of the 17 passes sit at rungs off L3 and seven at rungs the record has never measured (**H=26 carries five of them; H=37, 83 and 115 one each**). **Not one is reachable by any IS-only chooser: their IS-Sharpe ranks run 64th to 138th of 144 U56 cells.** The best — **U56 / N=20 / H=26: 14.96% / 1.1560 / -19.95%, halves 1.178/1.148, OOS 16.95% / 1.2192 / -19.95%, turnover 5.68x/yr, clearing 4b FULL and OOS — is 105th of 144 on IS Sharpe**; the runner-up U56 / N=8 / H=83 (19.07% / 1.1158 / -20.17%, OOS 22.48% / 1.1962) is 138th. **A MEMO IS WRITTEN with exact RULES wording and RECOMMENDS PARK**: a cell selected with full-sample knowledge is precisely what rule 8 exists to refuse, and that rank IS the honest measure of how much of the finer ladder's "discovery" is selection. **NOTHING ENACTED.**

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists. Every CAGR and drawdown LEVEL is optimistic and every 4a/4b count is an UPPER bound, the 17 included; the memo's MaxDD leg clears by 0.28 pp, inside that bias. The census arm is a scan of committed text and carries no market bias at all. The bias very largely cancels out of a ladder READING, which ranks one construction against itself on one tape, but it does NOT cancel out of the rule-8 OOS levels.

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_how-many-committed-H-AXIS-claims-rest-on-the-THREE-POINT-LADDER-21-63-126_C.py`, 9 CSVs, console log, result note, memo, 6 LEADERBOARD rows. Follow-ups filed 1181 (is the H axis resolvable at all on this tape), 1182 (how many committed ladder claims name a rung measured fewer than ten times) and 1183 (does the coarse ladder's OOS advantage survive a rolling IS window).
- 2026-09-17 (lane cloud, idea 1173 should-a-MONOTONE-LADDER-claim-be-PUBLISHABLE-as-a-BINARY-at-all) — **ANSWERED = NO, AND THE NUMBER IS 28.4%.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the FIRST eligible open idea; 1173 was first in '## Open' once the two STALE entries above it (1177 and 1172, both already in '## Done') were skipped, and it is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Price leg of 189 books and 18 rule-8 picks, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `CLAIM SET` {NARROW, PROX, WIDE} x `GRADED STATISTIC` {G_DOWNSHARE, G_DECSTEPS, G_SPEARMAN, G_ENDSE} = **12 cells, EVERY ONE PUBLISHED** in `.grid.csv`. NOT dials, reported at every value: PANEL {U56, B136, SMALL}; the n-ladder {5,8,10,12,15,20,25,30,40} (9 rungs = 1093's 8 adjacent steps); the HOLD ladder {21,42,52,63,76,90,126}; the LADDER METRIC {SHARPE, CAGR, MAXDD, ULCER}; the 63-day x 500 block bootstrap and the 2,000-draw permutation null; the three rule-8 choosers. Frozen at 936/1082/1086/1093's construction.

  **THE HEADLINE.** A published "this ladder is monotone" reproduces on its own tape **28.4%** of the time — the mean bootstrap stability over the 6 of 84 ladders where the binary fires (min 0.1040, max 0.6000). The *headline* stability 0.8958 is a **78/84-weighted average of 0.9428 for the 78 NO verdicts and 0.2840 for the 6 YES verdicts**, so a run quoting "the binary is stable" is quoting the stability of a verdict that is almost always NO. Against the four outcomes declared before any number: **(C) BINARY IS CONSERVATIVE ONLY** — 0.9364 of 102 ladder-level reversals are UNDER-READ (0 OVER-READ of 84 on G_DOWNSHARE and G_SPEARMAN, 1 on G_ENDSE). **NOT (B)**, and the bar is scored as declared and not re-cut: no graded statistic beats the binary by 0.20 on BOTH stability and replication, because the graded ones buy replication with stability (G_SPEARMAN 0.6429 at 0.6947 against the binary's 0.0714 at 0.8958).

  **THE NULL A READER WOULD ASSUME IS WRONG, BY A MEASURED MARGIN — G5b, REPORTED NOT GATED.** The null every graded statistic here actually uses is SELF-ORIENTED (each ladder scored in its own end-to-end direction, as every committed claim is written) and it centres on **0.5420 +/- 0.0005, not 0.500**: orienting by the endpoints conditions on them and buys 0.0420 of down-share free. **At 9 rungs the honest chance level is 4.34 of 8 steps, not 4 of 8.** G5 PASSES separately at a fixed direction (1.81e-04), so this is the statistic's property and not the machinery's. **G6: strict monotonicity is the exact 2/9! = 5.51e-06 ordering** — a 1-in-181,440 event, not a 1-in-2 one, which is why the binary fires 6 of 84 times at all.

  **THE CENSUS — 1,417 COMMITTED MONOTONICITY SENTENCES ACROSS 353 FILES.** Of the 187 NARROW claims only **0.0909 state a null**, 0.3690 use graded phrasing, 0.3102 state a rung set and 0.1551 name the hold they were measured at. Three claims in four are published as a bare binary with nothing to check them against.

  **HOW MANY REVERSE: 0.5238 to 0.8095 OF TRANSFERRED CLAIMS, AT EVERY CELL.** The dial that moves the answer is the **GRADED STATISTIC and not the CLAIM SET**: G_SPEARMAN reverses about half and G_DECSTEPS about four-fifths at every claim set, while NARROW -> WIDE moves the share by at most 0.10. **A re-score that does not name its graded statistic has published a number with a 0.29-wide free parameter in it.** Claim-level reversals are mostly OVER-READ (0.8017 of committed claims assert monotonicity while only 6 of 84 measured ladders are) and ladder-level ones are 0.9364 UNDER-READ; **both are true and they are different objects** — the record's claims are SELECTED and the 84 ladders are a CENSUS — and neither is quoted as the other. The PINNED arm (claim also names its hold) is **2 claims and reverses 2 of 2: a direction, not a measurement**, published as such.

  **A CORRECTION THIS RUN'S OWN STATISTIC PAYS.** 1093 read "4-7 of 8 decisively-down steps at every hold". On the metric ladders with a 63-day block bootstrap, **G_DECSTEPS is decisive at 2 of 84 ladders**, and **all 6 binary-monotone ladders fail its bar** — the binary fires on ladders whose individual steps are individually indistinguishable from zero. **SCOPE DECLARED AND NOT GLOSSED:** 1093's object was the EDGE ladder (book CAGR minus a DD-matched null's median over 40 seeds, ~86k rebuilt paths per panel), which is NOT rebuilt here; every claim re-score is labelled TRANSFERRED throughout and is not a re-derivation of 1093's cell. Only 0.0642 of NARROW claims mention EDGE.

  **GATES 8 of 10, AND THE TWO FAILURES ARE THE VINTAGE, PUBLISHED NOT ABSORBED.** G1 fast runner == engine.backtest at **1.39e-17**. **G2 FAILS at 1.62e-03 against 936/1082/1093's committed U56 W/H126/N=20 triple and G2b PASSES at 3.82e-07 against the SAME cell as 1151/1171/1172 committed it today** — `data/prices.csv` is rewritten nightly (idea 1163's defect) and this run reproduces the current vintage bit for bit. G3 FAILS at 2.89e-03 on the SPY OOS triple, the same drift at the same size 1149 reported. G4 determinism 0.00e+00, G7 block bootstrap unbiased at 0.114 SE, G8 the claim sets nest.

  **RULE 8 AND BOTH KEEP PATHS — 189 BOOKS, ALL PUBLISHED, AND THE SHAPE READING HAS NO CAPITAL CONTENT.** Benchmarks: **U56 SPY 15.06% / 0.8814 / -33.72% (halves 0.9598/0.8170), OOS 15.15% / 0.8684; U56 RULES v2 (live) @10 bps 8.60% / 1.1980 / -12.05%, OOS 9.42% / 1.2714; B136 SPY 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767; B136 LIVE 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059; SMALL SPY 14.06% / 0.8581 / -33.72%, OOS 15.33% / 0.8767; SMALL LIVE 4.30% / 0.6637 / -13.89%, OOS 3.75% / 0.5600.** Base rates at 10 bps: **4b full 9 of 189, 4b OOS 10, BOTH 9, 4a 0 of 189** (U56 8/63, B136 1/63, **SMALL 0 of 63 on every path**). **AN EXACT INDEPENDENT CROSS-RUN REPLICATION: all 9 passers are the same 9 cells lane C's 1093 run published today, agreeing to 6 decimals** (U56 N=20/H=126 0.155520 / 1.138079 / -0.191276 in both) — **CONFIRMATORY, not generative; NO NEW CANDIDATE and NO MEMO.** `CH_MONO` **fell back at 2 of 3 panels** — on U56 and SMALL no hold's IS n-ladder is binary monotone, so the binary filter is simply UNDEFINED as a selection rule — and on B136 it picks the identical book CH_ISSHARPE already picked: **filtering on a binary changes 0 of 3 picks.** Every graded filter that does change a pick LOSES: CH_GRADED:G_DECSTEPS costs U56 **0.2085 of OOS Sharpe (1.1033 -> 0.8948)**. **0 of 18 picks clear 4b, 0 of 18 clear 4a.**

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists; SMALL is the current output of a sub-$2B screen less the documented `max_1d_move >= 1.0` exclusion (52 of 715 dropped, 663 names plus SPY as benchmark). Every LEVEL is optimistic and every 4a/4b count is an UPPER bound. It very largely cancels out of a SHAPE statistic, which ranks one construction against itself on one tape, but that cancellation is an argument and not a measurement, and the levels are published beside every shape.

  **KILL as a capital finding.** Nothing enacted, no memo, no PROTOCOL edit. The one transferable result is procedural: **a monotone-ladder claim should not be published as a binary.** Script `research/backtests/2026-09-17_should-a-MONOTONE-LADDER-claim-be-PUBLISHABLE-as-a-BINARY-at-all_cloud.py`, 8 CSVs, console log, result note, 5 LEADERBOARD rows. Follow-ups filed 1184 (is every committed shape claim scored against a self-oriented null), 1185 (does a graded ladder statistic need its own SE convention first) and 1186 (is the record's monotonicity evidence selected and by how much) — filed as 1181-1183 and RENUMBERED ON PUSH: lane collision, defect 932 again, the concurrent lane had already taken 1181-1183 from idea 1174.

- 2026-09-17 (lane cloud, idea 1171 do-the-record-s-DRIVER-claims-REVERSE-under-a-CONTROLLED-tape-as-1164-s-did) — **ANSWERED = NO, 0 of 40 RESOLVABLE CELLS REVERSE, SO 1164's REVERSAL DOES NOT GENERALISE — BUT THE SIGN SURVIVING IS NOT THE CLAIM SURVIVING, AND THE QUEUE'S OWN CHEAPEST DRIVER IS UNTESTABLE BY THE METHOD IT PROPOSED.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the LAST open idea; 1171 ended '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Price leg of 264 real books, 2,376 synthetic books and 24 rule-8 picks, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `CLAIM SET` {C_RHOONLY, C_CONTROLLED, C_ALL} x `TAPE` {T_IID, T_BLOCK, T_FACTOR} = **9 cells, EVERY ONE PUBLISHED** in `.grid.csv`. PANEL {U56, B136, SMALL} is not a dial; the 88-book population (N x H x cadence plus an 8-rung gross ladder) is not a dial and every book is published; the six drivers, SEED (three at every controlled cell) and the four rule-8 choosers are not dials. Frozen at 1082/1094/1098/1102/1110/1149/1151/1159/1161/1164's construction.

  **WHAT A CONTROLLED TAPE MEANS HERE, SAID UP FRONT.** A cross-book rho is taken over books that differ in the dial AND in everything the dial drags with it — which names get picked, how correlated they are, which episode the drawdown lands in. The three synthetic tapes make every column EXCHANGEABLE by construction, so varying one dial leaves nothing else to move: T_IID (pooled mean and sd), T_BLOCK (a 63-day moving-block resample of the panel's own returns drawn INDEPENDENTLY per column, keeping each name's serial structure and marginal while destroying its identity) and T_FACTOR (one block-resampled common factor plus exchangeable idiosyncratic noise). The benchmark on a synthetic tape is built from the SYNTHETIC names, never the real SPY, which would smuggle the real tape's episode structure back in through the 4b legs.

  **GATES 9 of 9 PASS, and G5b is a correction this run found and did not hide.** G1/G2/G3 the committed incumbent anchor 0.155520 / 1.138079 / -0.191276. **G4 c\* reproduces idea 1151's committed value for the incumbent OUT OF 1151's OWN FILE: 117.369 against 117.368.** G5 the calibration premise, |MaxDD| non-decreasing in gross on the real U56 gross ladder, min step +1.138e-02. **G5b REPORTED NOT GATED: 1162's committed WEIGHT identity `W(g) == g*W(1)` does NOT carry to the compounded RETURN path when gross < 1 — max |r(0.75) - 2.5*r(0.30)| = 3.331e-03 — because the cash sleeve compounds differently at each gross.** The run's own first cut gated on that identity and FAILED; the gate was replaced with the claim that is actually true and the deviation published beside it. Filed as 1177. **G6 the T_IID book's Sharpe is column-order invariant (2.0958 vs 2.1343) — the tape really is exchangeable.** G7 `synth()` deterministic 0.00e+00. **G8 T_FACTOR's median pairwise column correlation is 0.3929 against T_IID's -0.0002 — the common factor is MEASURED, not assumed.** G9 the harvest's claim sets partition it, 153 = 138 + 15.

  **(A) THE HARVEST — 906 COMMITTED FILES, 117,866 SENTENCES.** **153 pair a DRIVER verb with a CORRELATION token; 15 (0.0980) also name a control and 138 (0.9020) rest on a cross-book rho ALONE** — 1171's population, exactly. By topic: D_GROSS 12 sentences (11 rho-only), D_TURN 10 (9), D_N_SH 10 (8), D_N 9 (8), D_CAD 4 (3), D_HOLD 1 (0). **124 of 153 touch none of the six price-native topics and are DECLARED OUT OF SCOPE for the test arm rather than quietly counted.**

  **(B) THE ANSWER, AND THE THREE THINGS WORTH READING. 40 of 54 (driver, panel, tape) cells are RESOLVABLE and 14 are DEGENERATE — a tied dial or outcome column — published as such and NEVER scored as 'does not reverse'.** Over the 40, **0 REVERSE**; the observed rho matches the record's asserted sign at 24 of 40; median attenuation |rho_obs|/|rho_ctl| 0.9532. **(1) 1164's reversal does NOT generalise:** five of six price-native drivers keep their sign on every tape construction, including the most adversarial. **(2) BUT THE MAGNITUDE IS NOT A MECHANISM, AND THAT IS WHERE THE RECORD OVER-READS. D_N_SH — the EDGE-ladder premise — shows only 0.202 of its controlled tape's relation (+0.1659 against +0.9134), and D_N 0.582 (-0.4980 against -0.9377).** On an exchangeable tape book size orders Sharpe and drawdown almost perfectly; on the real panel SELECTION cancels four-fifths of it, because the names a small book picks are the ones the composite says are best. **The sign of an n-ladder claim is a mechanism; its rho magnitude is a NET of mechanism against selection, and the record quotes it as strength.** Filed as 1176. **(3) THE QUEUE'S OWN CHEAPEST DRIVER CANNOT BE TESTED THIS WAY AT ALL. 1094's turnover -> c\* is DEGENERATE at 9 of 9 synthetic cells and on 2 of 3 real panels, for a STRUCTURAL reason: c\* is defined only on books that PASS 4b, no synthetic book ever does, and a column of tied zeros is not a measurement.** On U56 the real panel reproduces the claim cleanly at **-0.6154** against 1094's -0.65 and 1151's -0.6484, so the claim is NOT refuted — it is **UNFALSIFIABLE by a controlled tape**, a different and more useful verdict than either pass or fail, and **every 4b-conditional outcome in the record inherits it.** Filed as 1175. **TAPE CONSTRUCTION IS THE DIAL AND IT MATTERS:** T_FACTOR carries 8 of the 14 degenerate cells (a one-factor tape makes books too alike to rank) and its max cross-seed spread is 0.9874 against T_IID's 0.1538 and T_BLOCK's 0.2130 — **a one-factor tape is the WEAKEST of the three as an instrument, and it is the one the queue named.**

  **RULE 8 AND BOTH KEEP PATHS — 264 REAL BOOKS, ALL PUBLISHED, AND ACTING ON THE DRIVER CLAIM LOSES TO ITS OWN FALSIFICATION CONTROL.** Benchmarks: **U56 SPY 15.06% / 0.8814 / -33.72% (halves 0.9598/0.8170), OOS 15.15% / 0.8684; U56 RULES v2 (live) @10 bps 8.60% / 1.1980 / -12.05%, OOS 9.42% / 1.2714; B136 SPY 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767; B136 LIVE 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059; SMALL SPY 14.06% / 0.8581 / -33.72%, OOS 15.33% / 0.8767; SMALL LIVE 4.30% / 0.6637 / -13.89%, OOS 3.75% / 0.5600.** Base rates at 10 bps: **4b full 19 of 264 (U56 16, B136 3, SMALL 0), 4b OOS 20, BOTH 19, 4a 0 of 264.** **CH_ANTI — deliberately picking the HIGHEST turnover inside the same IS-Sharpe band, the exact opposite of what the driver claim prescribes — BEATS CH_DRIVER on mean OOS Sharpe (0.8928 against 0.8202), on beating SPY (5 of 6 against 3 of 6) and on 4b full (1 of 6 against 0 of 6).** Tilting toward the driver costs 0.068 of OOS Sharpe on average, because low turnover on this tape means long holds and stale winners. **CH_DRIVER_CTL — act on a driver claim ONLY where a controlled tape RESOLVABLY AGREES — recovers CH_ISSHARPE EXACTLY at 6 of 6 picks, because D_TURN's control is degenerate everywhere and the rule refuses to act. That refusal is FREE and it saves the 0.068: an unresolvable control is not a green light, and this run's one transferable procedural result is that treating it as one would have cost real Sharpe.**

  **NO NEW KEEP CANDIDATE AND NO MEMO.** The 19 books clearing 4b full and OOS at 10 bps are the record's existing ones plus this day's own **U56 / N=15 / H=252 / MONTHLY (15.71% / 1.0966 / -18.51%, OOS 16.65% / 1.0810, c\* 201 bps)**, which idea 1151 memo'd and PARKed earlier today — **this run's independent grid reproducing it is CONFIRMATORY, not generative**, and no second memo is written. **NOTHING ENACTED.**

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists and SMALL is the current constituents of a sub-$2B screen less the documented `max_1d_move >= 1.0` exclusion (52 of 715 dropped, 664 names plus SPY as benchmark). Every LEVEL is optimistic and the bias does NOT cancel out of the 4b legs. It very largely DOES cancel out of every rho, which ranks one construction against itself on one tape — **but stated rather than glossed: T_BLOCK and T_FACTOR are BUILT FROM the surviving panel's returns and inherit its optimistic marginals; only T_IID inherits no more than a pooled mean and sd.**

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_do-the-record-s-DRIVER-claims-REVERSE-under-a-CONTROLLED-tape_cloud.py`, 6 CSVs, console log, result note, 3 LEADERBOARD rows. Follow-ups filed 1175 (is every 4b-conditional outcome unfalsifiable by a controlled tape), 1176 (how much of every committed n-ladder rho is mechanism and how much is selection) and 1177 (does the cash sleeve break the record's gross-scaling identity anywhere it is load-bearing).

- 2026-09-17 (lane cloud, idea 1151 should-PROTOCOL-require-a-COST-CLAIM-to-NAME-ITS-BINDING-LEG) — **ANSWERED = YES ON THE READING AND THE PRICE IS ZERO, BUT THE STAMP IS A DISCLOSURE DEVICE AND NOT A SELECTION ONE.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the FIRST open idea; 1151 opened '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Price leg of 216 books, 864 book-rung cells and 24 rule-8 picks, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `STAMP FORM` {S_NONE, S_LEG, S_CSTAR, S_BOTH} x `CLAIM SET` {NARROW, PROX, WIDE} = **12 cells, EVERY ONE PUBLISHED** in `.stampgrid.csv`. PANEL {U56, B136, SMALL} is not a dial; the book population N in {5,8,10,12,15,20,25,30,40} x H in {21,63,126,252} x cadence {W,M} = 72 per panel, 216 books, is not a dial and every book is published; the four cost rungs {0,10,25,50} and the 1-bp ladder 0..300 are MEASUREMENT AXES. Frozen at 1082/1094/1098/1102/1110/1149/1159/1161's construction.

  **GATES 10 of 10 PASS AND G1 IS THE ONE THE WHOLE PRICE ANSWER RESTS ON. G1 `r(c) = g - tn*c/1e4` reproduces `engine.backtest` at 0, 10, 25 AND 50 bps to 1.39e-17** — the affine identity is PROVED, not asserted, and it is what makes the queue's second half answerable at all. G2 (g, tn) are c-free and deterministic 0.00e+00. G3/G4/G5 the committed incumbent anchor 0.155520 / 1.138079 / -0.191276. **G6 every 4b leg margin is monotone decreasing in c at 20 of 20 book-legs over a 61-point scan** — the precondition that makes bisection legal, checked rather than assumed. G7 bisection c* within 1 bp of a 1-bp brute-force scan (117.3684 vs 117.0). **G8/G9/G10 the census population is 1098's OWN COMMITTED `.claims.csv` read verbatim and its headlines reproduced — 2,088 rows, `leg_kinds == NONE` at 1,876, `resolved` 82, `resolved_loose` 425.** The corpus is NOT re-harvested: 1149's lesson is that today's corpus is a different and larger population, and then no number would be comparable to the 1,876 the queue asks about.

  **(A) THE READING, AND THE QUEUE'S OWN FIGURE UNDERSTATES THE DEFECT.** S_BOTH changes the reading of **2,012 of 2,088 WIDE (0.9636)**, 49 of 125 PROX (0.392) and **20 of 59 NARROW (0.3390) — even the record's most careful claim set is incomplete one time in three.** 212 of 2,088 name a leg at all (0.1015), but **only 162 (0.0776) name EXACTLY ONE**: 50 claims name two or three legs and still do not say which BINDS, so the falsifiable share is 0.0776, not the 0.1015 the queue's 0.8985 implies. **293 of 2,088 (0.1403) use a BINDING VERB and name no leg** — "it dies at 25 bps" with nothing to check it against, 1151's sentence counted. **110 name a numeric c* and every one of those 110 also names a leg (ZERO numeric-but-legless): the record never quotes a breakeven without saying what it is a breakeven for, and the failure is entirely the other way round.**

  **(B) THE PRICE, AND THE QUEUE'S SECOND HALF HAS AN EXACT ANSWER: A RUN THAT DOES NOT ALREADY COMPUTE A LADDER DOES NOT HAVE TO COMPUTE ONE.** Because g and tn are decided before costs are charged, the whole cost ladder is affine in two arrays one run already holds and every leg's c* is a bisection on them. **S_BOTH: 0 extra book builds, 0 extra engine calls, 0.0192s for all five legs — against 1.4880s for the naive four-rung `engine.backtest` ladder, a 78x saving**, and against 0.0700s for the worst case (re-BUILDING the book at each rung) the queue has in mind. One book build is 0.023s, so the whole stamp is 82% of ONE book; all 216 stamps cost 5.5s against 10.3s to build the books.

  **(C) THE TAPE-SIDE BINDING LEG, AND WHY THE RECORD HAS BEEN QUOTING ONE READING AS IF IT WERE BOTH.** Over all 216 books: **L_DD 114 (0.5278), L_H1 71, L_H2 28, L_CAGR 2, L_OOS 1 — by kind DD 0.5278 / SHARPE 0.4630 / CAGR 0.0093**, which REHABILITATES 1098's claim-side DD-modal reading (0.4880) on a population of all books. **But among the 13 books ALIVE at 0 bps the order REVERSES and SHARPE legs bind 7 of 13 — which is 1098's and 1149's tape-side SHARPE answer.** The modal binding leg is a property of WHICH BOOKS YOU CENSUS, and neither reading is wrong. **1094's premise SUPPORTED but on thin evidence and said so: eta^2 of rank(c*) explained by the binding leg = 0.6467 against rho^2 = 0.4204 for annual turnover (rho -0.6484, reproducing 1094's -0.65 in sign and size) — over 13 books. A direction, not a measurement.**

  **RULE 8 AND BOTH KEEP PATHS — 216 BOOKS, ALL PUBLISHED, AND THE STAMP-AWARE CHOOSER LOSES.** Benchmarks: **U56 SPY 15.06% / 0.8814 / -33.72% (halves 0.9598/0.8170), OOS 15.15% / 0.8684; U56 RULES v2 (live) @10 bps 8.60% / 1.1980 / -12.05%, OOS 9.42% / 1.2714; B136 SPY 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767; B136 LIVE 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059; SMALL SPY 14.06% / 0.8581 / -33.72%, OOS 15.33% / 0.8767; SMALL LIVE 4.30% / 0.6637 / -13.89%, OOS 3.75% / 0.5600.** Base rates at 0/10/25/50 bps: **4b full 13 / 12 / 11 / 7 of 216, 4b OOS 14 / 13 / 11 / 10, and 4a 0 of 216 AT EVERY RUNG**; SMALL is 0 of 72 on every path at every rung. **CH_CSTAR (argmax IS c*, the stamp-aware chooser) clears 4b 0 of 6 and 4b OOS 0 of 6 against CH_ISSHARPE's 1 and 1**, and its higher MEDIAN OOS Sharpe (0.9969 vs 0.8940) is bought with a much worse MEAN (0.7823 vs 0.8880) because maximising cost headroom drags it onto low-turnover low-return cells (SMALL/W/N=5/H=21, OOS Sharpe 0.1605). **CH_LEGFILT is IDENTICAL to CH_ISSHARPE at 6 of 6 picks** — refusing books whose binding leg is the CAGR floor changes nothing, because that leg binds 2 of 216 times, 1094's "decorative for selection" arriving from a third direction. **So the stamp tells a reader what a claim MEANS; it does not help a run CHOOSE.**

  **THE ONE NEW 4b BOOK, AND IT IS PARKED. Twelve books clear 4b full AND OOS at 10 bps; three are already committed (U56 N=12, U56 N=20 = the standing 2026-09-04 incumbent, B136 N=15 at 16.78%). The new one is U56 / N=15 / H=252 / gross 0.75 / MONTHLY: 15.71% / 1.0966 / -18.51%, halves 1.221/1.012, OOS 16.65% / 1.0810 / -18.51%, and it clears 4b full AND OOS at 0, 10, 25 AND 50 bps with c* = 201 bps (binding leg L_H2) against the incumbent's 117 (L_H1). A MEMO IS WRITTEN with exact RULES wording and RECOMMENDS PARK: the headroom is a TURNOVER REBATE, not an edge — annual turnover 1.527 against the incumbent's 2.778, i.e. 1.82x less trading buying 1.71x more headroom, which is idea 931's rebate arriving from the cost side — rule 8 does not reach the cell (no chooser picks it), 4a is 0 of 216, and its Sharpe is below the incumbent's 1.1381. NOTHING ENACTED.**

  **PROTOCOL CLAUSE PROPOSED NOT ENACTED (rule 6):** *rule 13, "no cost claim without its binding leg" — any committed sentence asserting that a book survives, dies at, or is killed by a cost level SHALL name the 4b leg that binds first and that leg's c* in bps; a claim naming more than one leg without naming which binds first does not satisfy the clause.*

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists and SMALL is the current constituents of a sub-$2B screen less the documented `max_1d_move >= 1.0` exclusion (52 of 715 dropped, 664 names plus SPY as benchmark). Every CAGR and drawdown LEVEL is optimistic and **every c\* in this run is an UPPER BOUND**; the bias does NOT cancel out of the 4b legs. It does not touch (A) or (B) at all — a scan of committed text and a wall-clock measurement carry no market bias.

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_should-PROTOCOL-require-a-COST-CLAIM-to-NAME-ITS-BINDING-LEG_cloud.py`, 7 CSVs, console log, result note, memo, 3 LEADERBOARD rows.

- 2026-09-17 (lane C, idea 1149 can-the-RECORD-s-COST-CLAIMS-be-RESOLVED-from-their-SCRIPTS-rather-than-their-PROSE) — **ANSWERED = NOT FROM THE SCRIPT, BUT YES FROM THE SCRIPT AS A FALLBACK, AND THE QUEUE'S OWN PREMISE IS THE THING THAT DIES.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the SECOND open idea; 1149 was second in '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Pure price leg of 54 books, 216 book-rung cells and 24 rule-8 picks, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE POPULATION IS A CENSUS, NOT A SAMPLE, AND IT IS 1098's OWN COMMITTED FILE.** All 2,088 rows are read verbatim out of 1098's committed `.claims.csv` (59 NARROW / 125 PROX / 2,088 WIDE, 28 CAGR-floor), with its `resolved` (82) and `resolved_loose` (425) columns carried along as the thing to be reproduced. **Re-harvesting today's corpus would give a DIFFERENT and LARGER population** — LEADERBOARD.md has grown 66 lines and CHANGELOG.md 249 since, and there are now 899 result.md on disk — **and then no number here would be comparable to the 82 and the 28 the queue asks about**: idea 1163's vintage finding applied to text instead of prices. Claim line numbers index the corpus AS IT WAS, so LEADERBOARD.md and CHANGELOG.md are recovered from git at 1098's own commit `3c1e2af6`, and G6 fails the run if the on-disk HEAD copies are used instead.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4, and the queue names both):** `RESOLUTION SOURCE` {R_PROSE, R_SCRIPT, R_UNION, R_LOOSE} x `CLAIM SET` {NARROW, PROX, WIDE} = **12 cells, EVERY ONE PUBLISHED** in `.census.csv`, each with its own 28-claim sub-population. `R_UNION_G` is a REPAIR of an inherited guard found by this run, published beside the headline at all three claim sets and never selected on. NOT dials, all reported at every value: PANEL {U56, B136}; LADDER {N, H, GROSS, CADENCE} at 1082/1086/1094/1097/1110's rung lists (9 + 4 + 10 + 4 = 27 rungs per panel, 54 books); the eight 4b legs; the four cost rungs {0, 10, 25, 50}; the three rule-8 choosers. The 1-bp ladder 0..200 is a MEASUREMENT AXIS, not a dial. Frozen at 936/1064/1071/1082/1086/1094/1097/1098's construction. **CALIBRATED, NOT PREDICTED, AND SAID UP FRONT:** the census arm was run in a wiring prototype before the script was written, so H_CITE / H_SCRIPTWINS / H_GAIN / H_28 are MEASUREMENTS against a stated bar and are labelled CALIBRATED everywhere; H_LADDERCAUSE, H_REFUTE and H_DDKILL were unmeasured when the file was written.

  **GATES 19 of 21 PASS AND THE TWO FAILURES ARE DECLARED CARRIED, NOT ABSORBED.** **G1 `R_PROSE` reproduces 1098's committed `R_STRICT` on all 2,088 rows at 0.00e+00** — the resolution rule is reproduced out of its own file, never re-implemented from memory — and **G1b reproduces `resolved_loose` at 425 exactly**; G0 pins the population; G8 recovers the 28 as 1098 defined them; **G13 reproduces 1098's committed killer-kind table BIT FOR BIT (17 books, SHARPE 10 / CAGR 4 / DD 3)**; G5b 1094's c\* full/OOS 63/64 with killer `L_H1` 0.00e+00; G7 1097's c\*_full on all 18 shared cells 0.00e+00; G9 determinism 0.00e+00; G1a/G1b `r(c) = g - tn*c/1e4 == engine.backtest` 1.39e-17; G10 the resolver is reproducible; G12 the guard repair is a strict subset. **G3 (SPY OOS triple, 2.89e-03) and G5 (1094's ladder, 2.22e-03) FAIL ON THE HEAD TAPE AND ARE PUBLISHED FAILING**, and **G11 MEASURES the cause rather than asserting it: 31,505 of 258,733 shared price cells RESTATED (0.1218) between 1098's commit and HEAD, reaching back to 2008-01-02, max relative move 8.384e-03, plus one extra bar** — replaying idea 1163's own census from the other side. All three anchors PASS once re-read on the tape recovered at 1098's commit: **G3b 1.70e-04, G2b 3.18e-07 (the very value 1098 itself published), G5d 4.96e-05.** Nothing was widened. HYPOTHESES 4 of 7 SUPPORTED.

  **(A) THE PREMISE, MEASURED AND REFUTED.** The queue asserts "every claim cites a committed script whose constants pin panel, N, H, gross and cadence exactly". **First half TRUE at 0.9933 — 2,074 of 2,088 claims resolve to a committed script that exists on disk** (1,037 by sibling result.md, 1,004 by the LEADERBOARD's own last column per PROTOCOL rule 5, 33 by CHANGELOG entry, 14 with no citation at all). **Second half FALSE and decisively: across 561 distinct scripts the cited script pins the PANEL for only 190 of 2,088 claims, N for 146 and H for 5**, against gross 790 and cadence 949 — **the record's scripts pin the EXPOSURE dials and LADDER OVER the SELECTION ones**, and a cost claim is almost always about a selection cell. **H_SCRIPTWINS REFUTED: `R_SCRIPT` read ALONE re-scores 38 of 2,088, LESS THAN HALF the 82 the PROSE already reached.**

  **(B) BUT AS A FALLBACK IT IS WORTH 3.55x. H_GAIN SUPPORTED: `R_UNION` — prose where the prose states a dimension, the cited script for the rest — re-scores 291 of 2,088 (0.1394) against R_PROSE's 82 (0.0393)**, loses NONE of the 82 (it is a strict superset by construction) and **adds 209 claims the prose alone cannot reach**. `R_LOOSE`, 1098's rejected first cut, bounds it at 425. Two counts are printed at every cell and NEVER merged: **CELL-STATED** (the source pins panel + >= 1 dimension) and **RE-SCORABLE** (that cell is also one of the 54 measured books); **75 of R_UNION's 366 cell-stated claims land OUTSIDE the frozen family and are a PROVENANCE gain, not a re-derivation.**

  **(C) THE 28, AND THE RE-SCORE 1098 COULD NOT RUN AT ALL.** **The 28 CAGR-floor claims go from 0 re-scorable to 9** (R_SCRIPT 3, R_LOOSE 11). **H_28 is REFUTED against its own majority bar — 9 of 28 is not a majority — and 9 against 1098's 0 is the substantive answer to the queue's ask.** Re-scored against their own cell's measured killer leg: **CONFIRMED 0, REFUTED 5, UNDECIDED 4, TRANSFERRED 19 (H_REFUTE SUPPORTED as pre-registered).** **Every one of the five refuted claims resolves to `U56/N=20` and is killed on `L_H1` — a SHARPE leg, never the CAGR floor it assumed** — and all four UNDECIDED are `B136/N=20`, which is DEAD0 and has nothing left for a rising rung to kill. **ZERO CONFIRMED at every one of the five sources: 1094's single-cell correction survives contact with the record's own claims.** TRANSFERRED is never merged into the verdicts (1048/1098/1102/1110's convention).

  **(D) THE MECHANISM IS SILENCE, NOT LADDERING — H_LADDERCAUSE PRE-REGISTERED AND REFUTED, 1,472 OPEN against 4,990 SILENT**, claim-weighted over the four construction dimensions, with **H SILENT for 1,989 of 2,088 claims**. The distinction is the useful part: a script that WALKS a ladder over an axis can never be pinned from its own source and no stamp helps, while a SILENT dimension is a constant the run simply never wrote down and a one-line header stamp would reach 4,990 of the 6,462 unpinned pairs. Filed as 1167.

  **(E) A DEFECT IN AN INHERITED GUARD, FOUND BY THIS RUN AND PRICED RATHER THAN HIDDEN.** **1098's `OUT_OF_FAMILY` guard reads the CLAIM'S PROSE. That was right only while the CELL also came from the prose** — once the cell comes from the cited SCRIPT, the guard and the resolution read different objects, and **two of the nine newly re-scorable CAGR-floor claims turn out to cite `2026-09-04_ensemble-plus-momentum_C.py` and `2026-09-05_sleeve-with-a-real-diversifier_B.py`, exactly the families the guard exists to exclude, while their own prose says nothing suspicious.** `R_UNION_G` applies the same guard to the SCRIPT NAME too and is published at every cell: **291 -> 238 re-scorable (still 2.90x R_PROSE's 82) and 9 -> 5 of the 28 (REFUTED 3, CONFIRMED 0)**; G12 proves the repair only ever removes rows. **The gain survives its own correction, and the corrected numbers are the ones a later run should cite.** Filed as 1168.

  **RULE 8 AND BOTH KEEP PATHS — 54 BOOKS, ALL PUBLISHED, AND NOTHING NEW REACHED.** Benchmarks: **U56 SPY full 15.06% / 0.8814 / -33.72% (halves 0.9598/0.8170), OOS 15.15% / 0.8684 / -33.72%; U56 RULES v2 (live) @10 bps 8.60% / 1.1980 / -12.05%, OOS 9.42% / 1.2714; B136 SPY 15.16% / 0.8861 / -33.72% (halves 0.9596/0.8259), OOS 15.33% / 0.8767; B136 RULES v2 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059.** Base rates over the 54 books: **4b full 17 / 16 / 15 / 10 and 4b OOS 19 / 17 / 16 / 14 at 0 / 10 / 25 / 50 bps; 4a 0 of 54 AT EVERY RUNG and 0 of 24 picks.** **4 of the 24 IS-only picks clear 4b full AND OOS at 10 bps and ALL FOUR ARE THE SAME BOOK — U56 N=20 / H=126 / gross 0.75 / weekly, THE INCUMBENT DEFAULT** (full 15.55% / 1.1381 / -19.13%, halves 1.2049/1.0932, OOS 16.92% / 1.1615 / -19.13%), committed identically by 1082/1098/1162 and **not a discovery**. **NOTHING NEW PROPOSED AND NO MEMO WRITTEN:** the best non-incumbent 4b full+OOS book is U56 N=12 (17.65% / 1.1658 / -20.17%, OOS 18.78% / 1.1701), **already committed by 1098/1102** and unreachable by any chooser here (C_ISSHARPE and C_ISDD take N=40, C_ISCAGR takes N=5). **H_DDKILL REFUTED AND THE BAR WAS THIS RUN'S OWN FAULT, RECORDED AS DECLARED AND NOT RE-CUT:** it was pre-registered as "the modal killer is the DD cap (1098's answer)", but 1098's "DD cap most often" is its CLAIM-side census (0.4880 of the 125 claims that name a leg) while its TAPE-side answer was already SHARPE — and the measurement reproduces 1098's table exactly (G13, SHARPE 10 / CAGR 4 / DD 3 of 17), so the verdict is a defect in this run's bar and an independent replication at the same time.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every CAGR and drawdown LEVEL is optimistic, every c\* is an UPPER BOUND, and the bias does NOT cancel out of the 4b legs. **It does not touch this run's headline at all:** the citation rate, the pin/open/silent tables, the 12 resolution cells and the guard repair are scans of committed text and committed source code and carry no market bias whatever. The RE-SCORE arm inherits the price bias, because it reads a measured killer leg off the tape.

  Script `research/backtests/2026-09-17_can-the-RECORD-s-COST-CLAIMS-be-RESOLVED-from-their-SCRIPTS-rather-than-their-PROSE_C.py`, 11 CSVs, console log, result note, 5 LEADERBOARD rows. Follow-ups filed 1167 (does a one-line CELL STAMP in a script header buy the 4,990 SILENT pairs) and 1168 (how many of the record's OTHER committed censuses guard a different object than they resolve).

- 2026-09-17 (lane B, idea 1162 why-does-an-IID-RESAMPLE-produce-DEEPER-drawdowns-than-a-BLOCK-one) — **ANSWERED = BECAUSE THE BLOCK NULL INTERPOLATES BETWEEN THE IID NULL AND THE OBSERVED PATH, SO "IID DEEPER THAN BLOCK" IS NOT A FACT ABOUT BOOTSTRAPS AT ALL: IT IS THE STATEMENT THAT THE OBSERVED DRAWDOWN IS SHALLOWER THAN ITS OWN IID NULL, WHICH IS IDEA 1161's FACT. The two follow-ups 1159 filed as siblings are ONE question.** All three carriers the queue named are DEAD, the textbook variance story with them, and the parent run gets a quantitative correction. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the LAST open idea; 1162 ended '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Pure price run — 180 books, 45 null cells, 180 ladder rungs, 9 rule-8 picks — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4, and the queue names both):** `NULL TYPE` {N_IID, N_BLOCK, N_STAT} x `CONTROL` {C_BOOK, C_GATEOFF, C_NOREBAL, C_SHUFFLE, C_SPY} = **15 per panel, 45 in all, EVERY ONE PUBLISHED** in `.dialgrid.csv`. The controls are a ONE-FACTOR-AT-A-TIME ladder off the anchor book, one per carrier the queue names plus one falsification control: C_GATEOFF removes the eligibility gate, C_NOREBAL buys once at the first post-warm-up rebalance and never trades again, C_SHUFFLE permutes the book's own realised returns, and C_SPY is SPY alone — the MOST mean-reverting series on this tape (ac1 −0.0991 against the book's −0.0501), so it is the sharp test of the queue's third candidate. PANEL {U56, B136, SMALL} is not a dial. **THE BLOCK-LENGTH LADDER L in {1, 2, 5, 10, 21, 42, 63, 126, 252, 504, 1008, T} is NOT a dial — it is the one-parameter family under measurement, all 180 rungs published, nothing ever selected on it, headline frozen at the record's L=63.** The 15-rung GROSS ladder is not a dial; q is not a dial (0.90 headline, 0.80/0.95 beside); SEED is not a dial (three run at every headline cell, spread published). Frozen at 1082/1094/1098/1102/1108/1110/1116/1117/1118/1122/1131/1140/1148/1159's construction. Price vintage stamped per 1163's DRAFT rule 12 (complied with, not enacted): HEAD crc32 **85eebc60**, which IS 1159's tape since no daily close has landed since `6f1fcb1`; the pinned 2026-09-15 vintage **5f9b8015** is recovered from git and every headline re-read on it.

  **GATES 13 of 13 PASS, and G6 is the one the whole run turns on.** G1 fast runner == `engine.backtest` 1.39e-17; G2 the control constructor == the anchor book 2.78e-17; **G3 the committed U56 W/H126/N=20 triple reads 1.62e-03 on the HEAD tape — 1163's defect CARRIED AND PUBLISHED, not absorbed — while G12 reads 3.18e-07 on the pinned vintage**; **G4 cross-run at FULL PRECISION out of 1159's own committed CSV rather than from rounded prose, SPY and live OOS Sharpe 0.00e+00**; G5 live RULES v2 MaxDD 4.95e-05; **G6 `N_BLOCK` at L=1 IS `N_IID` BIT FOR BIT on the same seed, 0.00e+00 — the block family provably contains the iid null as an endpoint, which is what makes the interpolation claim an identity and not an analogy**; G7 N_STAT mean block 4.31 off 63; **G8 all three nulls share the observed MARGINAL mean (1.15e-02 relative), so ORDER is the only thing that differs between them**; **G9 replays 1159's committed U56 OBJ_MAXDD observed level at 7.11e-15 and G10 re-reads its "12 of 12" ordering out of its own file at 12 of 12 — the premise is VERIFIED FROM THE RECORD, never recalled**; G11 SMALL determinism with the weight cache dropped and rebuilt 0.00e+00; G13 `W(g) == g*W(1)` exact 6.94e-18. **HYPOTHESES 1 of 8 SUPPORTED — the structural one.**

  **(A) THE ANSWER. `sign(BLOCK − IID) == sign(OBS − IID)` at 15 of 15 cells, BLOCK lies strictly between them at 14 of 15, `spearman(BLOCK/IID − 1, OBS/IID − 1) = 0.8286`.** The L ladder traces it end to end: at L=1 the block null EQUALS the iid null by construction, at **L=T it equals the OBSERVED value at 15 of 15 cells to 1.8e-13**, because one wrapped block is a rotation and a rotation splits an 18-to-155-bar episode in under 4% of draws. **THE FALSIFICATION LANDS: the published ordering REVERSES at 7 of 15 cells (5 of the 12 non-shuffle) — all three C_SPY cells, C_NOREBAL on U56 and B136, two of three C_SHUFFLE — and they are exactly the controls whose observed drawdown is DEEPER than their own iid null.** **C_SPY is the cleanest case in the run: BLOCK = STAT = OBS on every panel, phi exactly 1.000, cross-seed spread 0.00e+00, because SPY's worst drawdown is a 23-bar episode a 63-bar block carries intact in essentially every draw.** SMALL/C_NOREBAL does NOT reverse and that is the identity working rather than an exception (OBS/IID 0.7690 so BLK/IID stays at 0.9745). **H_MONO REFUTED AS DECLARED at 13 of 15 sign / 11 of 15 |rho|>=0.80 and NOT re-cut**; reported beside it, dropping the two C_SHUFFLE cells whose want-sign is a coin flip because their OBS−IID gap is noise, 12 of 12 and 11 of 12.

  **(B) ALL THREE CARRIERS THE QUEUE NAMED ARE DEAD.** **H_MEANREV REFUTED 0 of 3 and it is the decisive one: C_SPY is MORE negatively autocorrelated than the book and its ordering is the OPPOSITE SIGN on every panel (BLK/IID 1.0568 / 1.0471 / 1.0832 against 0.8807 / 0.8714 / 0.9400) — more mean reversion, opposite sign.** **H_GATE REFUTED 0 of 3**: removing the eligibility gate shrinks |OBS/IID − 1| on U56 (0.1526->0.1034) and B136 (0.1950->0.1610) but nowhere near halves it, and on SMALL it makes it LARGER (0.1101->0.2041) — the gate contributes, it does not carry. **H_CADENCE REFUTED 0 of 3 and goes the WRONG WAY on every panel** (0.1526->0.2597, 0.1950->0.3237, 0.1101->0.2310), flipping the sign on U56 and B136, because a never-rebalanced book concentrates into its winners and then takes a 72% / 60% drawdown over 120–143 bars. **H_VR REFUTED 9 of 15: VR(63) < 1 at 14 of 15 cells (0.458..1.126) so the long-horizon variance argument predicts BLOCK shallower EVERYWHERE, and the sign actually agrees at 9 — a |MaxDD| is an EPISODE statistic, not a variance functional, and averaging over a whole tape cannot see which single episode a block preserves.**

  **(C) THE CORRECTION TO THE PARENT, AND IT STARTS WITH THIS RUN'S OWN BAR BEING WRONG.** The pre-registered falsification H_SHUFFLE FAILED at 1 of 3 — **and the bar was wrong, not the measurement**: it demanded BLOCK == IID on a shuffled series, which the interpolation identity FORBIDS, since a shuffled path still HAS an observed drawdown differing from the iid median by sampling noise. **The verdict is recorded REFUTED as declared and is NOT re-cut.** The corrected 5-permutation sub-arm does not say what was expected either: **with order destroyed the block null is NOT centred on the iid null but sits systematically BELOW it — pooled mean BLOCK/IID 0.9696 over 15 permutations (U56 0.9804, B136 0.9789, SMALL 0.9495)**, a MACHINERY effect with a mechanism (a moving block is a contiguous slice of a fixed array and cannot contain the same bar twice, while an iid draw can stack the tape's worst days on top of each other). Since the shuffled series carries C_BOOK's exact marginal distribution, 1159's effect decomposes: **U56 total −0.1193 = machinery −0.0196 + ORDER −0.0996 (16.5%); B136 −0.1286 = −0.0211 + −0.1076 (16.4%); SMALL −0.0600 = −0.0505 + −0.0095 (84.1%).** **1159's ordering is REAL on U56 and B136 and MOSTLY AN ARTEFACT OF THE BOOTSTRAP on SMALL** — a correction no re-run of 1159's own grid could find, because its grid never contained a series with the order taken out. **H_EPISODE, declared POST HOC after Arm A and flagged as exploratory wherever quoted, REFUTED at 7 of 12 (bar 8 of 12, spearman(L*, episode) 0.5547); what survives is that L* >= the episode length at 12 of 12 non-shuffle cells with L*/episode 1.09..21.91 — the episode's length is a LOWER BOUND on L*, not a predictor of it.** VINTAGE: sign of (BLOCK − IID) unchanged at 4 of 5 controls, max |delta BLOCK/IID| 0.0357, the one flip being C_SHUFFLE whose sign is noise by construction.

  **RULE 8 AND BOTH KEEP PATHS — 180 books, ALL PUBLISHED, and 0 of 9 picks clear 4b.** Benchmarks: **U56 SPY 15.06% / 0.8814 / −33.72% (halves 0.9598/0.8170), OOS 15.15% / 0.8684; U56 RULES v2 (live) 8.60% / 1.1980 / −12.05%, OOS 9.42% / 1.2714; B136 SPY 15.16% / 0.8861 / −33.72%, OOS 15.33% / 0.8767; B136 RULES v2 7.98% / 1.0993 / −12.24%, OOS 7.88% / 1.1059; SMALL SPY 14.06% / 0.8581 / −33.72%, OOS 15.33% / 0.8767; SMALL RULES v2 4.30% / 0.6637 / −13.89%, OOS 3.75% / 0.5600.** Base rates **4b full 19, 4b OOS 21, 4a 2 of 180** (U56 10/12/0, B136 9/9/0, SMALL 0/0/2; both 4a passes are de-grossed SPY on SMALL, which is a statement about the live rules on a small-cap panel and not a proposal). **Three IS-only choosers on 2009–2016 pick C_GATEOFF 0.95 / C_NOREBAL 1.00 / C_GATEOFF 0.30 (U56), C_BOOK 1.00/1.00/0.30 (B136), C_SPY 1.00/1.00/0.30 (SMALL): 0 of 9 clear 4b full, 0 of 9 clear 4b OOS, 1 of 9 clears 4a.** **The best book clearing 4b FULL and 4b OOS is U56 / C_BOOK / gross 0.75 — full 15.55% / 1.1381 / −19.13% (halves 1.2049/1.0932), OOS 16.92% / 1.1615 / −19.13% — which IS THE INCUMBENT DEFAULT and NOT a discovery (1159 committed the same cell at the same numbers), and is unreachable by any chooser here.** The one non-incumbent 4b band is **U56 C_GATEOFF gross 0.55–0.75 (OOS 1.1461) and B136 C_GATEOFF 0.50–0.65**; a **MEMO IS WRITTEN with exact RULES wording and RECOMMENDS PARK** — the band is strictly dominated by the incumbent on Sharpe, CAGR and drawdown, rule 8 does not reach it, 4a is 0 of 180, and C_GATEOFF was built as a MECHANISM CONTROL, not proposed as a book. **NOTHING ENACTED.**

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists and SMALL is the CURRENT constituents of a sub-$2B screen less the documented max_1d_move >= 1.0 exclusion (52 of 715 dropped, 663 names plus SPY as benchmark), so every LEVEL — CAGR, |MaxDD| and every null median built on top of them — is optimistic, and a resample null prices SAMPLING error on the tape it is handed and cannot correct that. The bias does NOT cancel out of the 4b legs. It very largely DOES cancel out of every headline in (A)–(C), which are all ratios of one construction against itself on the same tape: BLOCK/IID, OBS/IID, phi, the L ladder and the machinery decomposition.

  Script `research/backtests/2026-09-17_why-does-an-IID-RESAMPLE-produce-DEEPER-drawdowns-than-a-BLOCK-one_B.py`, 13 CSVs, console log, result note, memo, 5 LEADERBOARD rows. Follow-ups filed 1164 (what sets phi once the episode length is only a lower bound on L*) and 1165 (whether the record's other committed block-bootstrap bands carry the same 16%/84% machinery share).

- 2026-09-17 (lane cloud, idea 1163 is-the-RECORD-REPRODUCIBLE-AT-ALL-while-data-prices-csv-is-REWRITTEN-NIGHTLY) — **ANSWERED = THE BOOKKEEPING IS NOT, THE SCIENCE IS. `data/prices.csv` has NEVER been append-only — 0 of 11 transitions — and 6 of the record's 10 committed cross-run anchors no longer reproduce; but 10 of 1,584 trading-day verdict cells flip (0.0063), 4a flips 0 of 528, and all 12 rule-8 picks are vintage-INVARIANT.** The queue's own favoured repair, an append-only cache, is measured to be NOT A REPAIR. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the LAST open idea; 1163 ended '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Price leg of 704 books across 16 vintages and 12 rule-8 picks, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE VINTAGE POPULATION IS NOT SAMPLED — IT IS THE WHOLE THING.** `data/prices.csv` has **12** recoverable vintages in git (2026-09-03 .. 2026-09-16) and `data/prices_broad.csv` has **4**, and the research record itself begins 2026-09-03: every price vintage any committed constant could have been computed on is in this run. **TWO OF THE TWELVE ARE FLAGGED RATHER THAN DROPPED:** `fb208174` and `0ede2282` are CALENDAR-INDEXED (6,059 / 6,060 rows, 1,248 weekend bars each), fixed by `c006b439` "Fix calendar-day index bug"; the defect is detected from the TAPE (weekend bars present), never from a commit message, and every headline is reported over all 12 AND over the 10 trading-day vintages, because letting those two dominate a spread would be as dishonest as quietly dropping them.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4, and the queue names both):** `REPAIR` {R_NONE, R_TOLERANCE, R_APPENDONLY, R_FINGERPRINT} x `GATE SET` {G_ANCHOR, G_BENCH, G_VERDICT, G_ALL} = **16 cells, EVERY ONE PUBLISHED** in `.repairs.csv`. VINTAGE is NOT a dial — it is the population under census. LADDER {GROSS 33, N 11} is not a dial (1154/1159's construction): all 44 rungs built on every vintage, 704 books. PANEL {U56, B136} is not a dial. Frozen at 1082/1094/1098/1102/1108/1110/1116/1117/1118/1150/1154/1161's construction.

  **GATES 7 of 7 PASS, and G5 is the one worth reading.** G1 fast runner == `engine.backtest` 1.39e-17; G2 the vintage reader at HEAD == `baseline.load_universe()` on disk 0.00e+00; G3 determinism 0.00e+00; **G4 1098/1102's n=12 triple on the CURRENT vintage reads 3.37e-03 and is recorded as PASS because its FAILING is this run's subject, not its bug**; G6 `rebalance_mask` stable; G11 both universes BYTE-IDENTICAL from their own first price vintage to HEAD, so a vintage is a price-file vintage and nothing else. **G5 replays 1159's bycatch EXACTLY — 31,505 of 258,733 cells, 48 of 58 columns, max relative move 0.008384, at 5.11e-05. The first attempt missed by 4.17e-02 and the cause was THIS run's construction, not 1159's: a 56-name universe census drops the two crypto columns `baseline.EXCLUDE` removes, giving 31,503 of 253,495 across 46 of 56. The gate was MATCHED TO 1159's OBJECT rather than its tolerance widened, and both censuses are published.** HYPOTHESES 5 of 6 SUPPORTED.

  **(A) THE CENSUS — IT IS NOT THAT prices.csv WAS REWRITTEN ONCE, IT IS REWRITTEN EVERY NIGHT.** U56: **0 of 11 transitions are append-only**, restated share **0.0906 .. 0.1243 (median 0.0918)**, reaching back to **2008-01-02 at ALL ELEVEN**, median |relative move| ~1.1e-06, max per transition 5.09e-05 .. 7.71e-02. B136: **0 of 3**, share 0.0010 .. 0.0423. **1159's commit `6f1fcb1` was not exceptional — at 0.1243 it is merely the largest of eleven.** Every nightly commit rewrites roughly a tenth of an eighteen-year tape.

  **(B) THE ANCHORS, AND WHICH ONES SURVIVE IS 1159's OWN RESULT ARRIVING FROM THE OTHER SIDE.** R_NONE pass rate **0.4000** on G_ALL (0.3333 book, 0.5000 bench). **EVERY anchor that still reproduces is a MaxDD and EVERY anchor that fails is a CAGR or a Sharpe — 4 of 4 against 6 of 6.** A maximum is flat between jumps, so it survives a restatement that moves every mean — **and the identical property makes it USELESS as a vintage fingerprint: SPY_OOS_MaxDD matches 12 of 12 vintages, A936_MaxDD and A1098_MaxDD and LIVE_MaxDD 10 of 12. A drawdown anchor tells you the number is right and nothing whatever about which tape produced it.** **`868b5c36` (2026-09-15) is the best vintage for 6 of 10 anchors, independently confirming the pin 1154 and 1161 chose on other grounds.**

  **(C) THE VERDICT-STABILITY ARM — THE PART THAT REACHES CAPITAL, AND IT IS REASSURING. 48 flips of 1,848 (vintage, ladder, rung, path) cells = 0.0260 overall; 10 of 1,584 = 0.0063 over the ten TRADING-DAY vintages.** By path, trading-day only: **4b full 3 of 528 = 0.0057, 4b OOS 7 of 528 = 0.0133, 4a 0 of 528 = 0.0000.** The five worst cells are ALL on the two calendar-indexed vintages. **So the defect is real and it is a BOOKKEEPING defect: 6 of 10 committed constants fail to reproduce while essentially no KEEP/KILL verdict moves.**

  **THE REPAIRS, PRICED — AND THE QUEUE'S OWN CANDIDATE LOSES. R_APPENDONLY scores 0.4000 on G_ALL, IDENTICAL to R_NONE**, because 1159 attributed 97.3% of the deviation to the extra BAR and an append-only cache does not remove a bar. **AND IT CARRIES A COST NOBODY HAD PRICED: started from the first vintage it carries 6,067 rows and inherits the CALENDAR-DAY INDEX BUG PERMANENTLY, because a cache that may never be restated may never be REPAIRED either.** Both tapes are published and the repair was scored on the favourable one. **R_TOLERANCE reaches 1.0000 and the whole bill falls on ONE statistic: widening the Sharpe gate to its own trading-day vintage spread of 6.37e-03 blinds 33 of 42 adjacent-rung pairs (0.7857) — it would erase the entire gross-dial Sharpe ladder 1150/1154/1161 reason about — while CAGR (1.08e-03) and MaxDD (8.22e-07) blind 0 of 42 each.** **R_FINGERPRINT SPLITS AND THE HALVES DISAGREE: PROSPECTIVE 1.0000 at 8 bytes (crc32) or 64 (sha256 hex); RETROACTIVE 0.3000 — only 3 of 10 already-committed anchors resolve to exactly one vintage from their value alone. The repair cannot be applied backwards.** H_FPWINS is REFUTED as declared because it demanded both halves; reported as measured, not re-cut to pass.

  **THE DRAFT CLAUSE, AS A COMPOUND (proposed, NOT enacted — rule 6).** *PROTOCOL rule 12, "no constant without a vintage": any figure a later run may gate against must be published beside the crc32 of the price tape it was computed on; a cross-run gate on a CAGR or a MaxDD may declare a tolerance no wider than 1.1e-03 / 8.3e-07 respectively (the measured trading-day vintage spread, which blinds 0 of 42 adjacent rungs); no cross-run gate on a SHARPE may be widened at all, because the tolerance that would pass it blinds 78.6% of the record's own ladder.* **And the honest bottom line: the record's EXISTING constants are not repairable by any of the three — 6 of 10 do not reproduce, 7 of 10 cannot be traced to a vintage, and the correct disposition is to re-derive them with fingerprints rather than widen a gate until they pass.**

  **RULE 8 AND BOTH KEEP PATHS — AND THE STRONGEST RESULT IN THE RUN. Every one of the 12 (panel, ladder, chooser) rule-8 picks is IDENTICAL on all 12 (U56) / 4 (B136) vintages — 1 distinct pick each.** C_ISSHARPE and C_ISCAGR pick gross 1.000 on both panels (1150's endpoints); **C_IS4B picks gross 0.625 (U56) / 0.575 (B136) on EVERY vintage and clears 4b OOS at 12 of 12 and 4 of 4 — an independent CROSS-VINTAGE confirmation of 1154's tent pick and of today's idea 1161.** Current-vintage base rates: **U56 4b full 26, 4b OOS 30, 4a 0 of 88; B136 11, 11, 0 of 44.** Benchmarks: **U56 SPY 15.06% / 0.8814 / -33.72% (halves 0.9598/0.8170), OOS 15.15% / 0.8684; U56 RULES v2 (live) 8.60% / 1.1980 / -12.05%, OOS 9.42% / 1.2714; B136 SPY 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767; B136 RULES v2 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059.** Best cell clearing 4b full AND OOS: **U56 N=12 17.75% / 1.1718 / -20.17%, OOS 18.98% / 1.1804**; B136 N=15 16.78% / 1.0682 / -19.66%, OOS 17.49% / 1.0458. **NOTHING NEW PROPOSED AND NO MEMO WRITTEN: U56 N=12 is not a discovery (1098/1102 committed the same cell) and the only rule-8-reachable 4b passer is 1154/1161's gross selector, which already carries today's memo recommending PARK.**

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every CAGR and drawdown LEVEL is optimistic and the 4b bars are measured against an inflated book; the bias does NOT cancel out of the 4b legs. It ENTIRELY cancels out of this run's headline claims — the census, the anchor deviations, the verdict flips and the repair pass rates all contrast the same construction against itself across vintages of the same panel.

  Script `research/backtests/2026-09-17_is-the-RECORD-REPRODUCIBLE-AT-ALL-while-prices-csv-is-REWRITTEN-NIGHTLY_cloud.py`, 7 CSVs, console log, result note, 6 LEADERBOARD rows.

- 2026-09-17 (lane cloud, idea 1161 does-the-TENT-CHOOSER-reach-the-INTERIOR-on-a-ROLLING-IS-WINDOW-or-only-on-the-ONE-SHOT-SPLIT) — **ANSWERED = YES, THE INTERIOR PICK IS A PROPERTY OF THE CHOOSER AND NOT OF 1154's ONE WINDOW: 361 of 368 rolling choices (98.1%) land in the INTERIOR.** One hypothesis REFUTED and it is the useful one. A 4b KEEP-CANDIDATE on U56 with a clean rule-8 pass, written up with exact RULES wording and **recommended PARK**. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the FIRST open idea; 1161 led '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Pure price run — 66 ladder rungs, 24 rolling cells, 368 honest choices — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4, and the queue names both):** `WINDOW` {W504, W756, W1260, WEXP} x `CADENCE` {R6M, R1Y, R2Y} = **12 cells per panel, 24 in all, EVERY ONE PUBLISHED** in `.cells.csv`. The gross RUNG is NOT a third parameter — it is the object the chooser SELECTS (1154's framing, kept verbatim) and all 33 rungs of both panels are published in `.grid.csv` whatever any chooser did. PANEL {U56, B136} is not a dial. Frozen at 1082/1094/1098/1102/1108/1110/1116/1117/1118/1150/1154's construction: CAND20 legs, cap INF, max_vol 0.60, min hold 126, N=20, cadence W, 10 bps (rule 2), LAG 1, warm-up 260, zero cash, block L=63, q=0.90.

  **THE COMMON SPAN, DECLARED BEFORE ANY NUMBER AND THE REASON FOR IT.** A rolling chooser cannot choose until warm-up plus one window of bars exists, so the four windows would otherwise be scored on four different tapes and could not be compared. Every cell, control and benchmark is scored from bar `WARMUP + max(window) = 1520` (**2014-01-21 -> 2026-09-15, 3,186 bars**), and WEXP is given the same first choice date, so the windows differ in LOOKBACK only and never in scored tape. **These figures are therefore NOT comparable to 1154's full-tape ones** (which start at bar 260); 1154's own numbers are replayed on 1154's own span in G8 and quoted from its file, never re-derived.

  **GATES 10 of 10 PASS, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 committed U56 W/H126/N=20 triple 4.12e-05 PINNED at 2026-09-15 (15.5793% / 1.1397 / -19.1276%), **and the same gate UNPINNED reads 1.62e-03, 39.4x, from ONE extra bar of 4,706 — idea 1160's defect carried and published, not absorbed, with 1163 open on it**; G3 SPY OOS 1.70e-04; G4 1098/1102's n=12 triple 4.71e-05; G5 live RULES v2 MaxDD 4.95e-05; G6 determinism 0.00e+00; G7 `cadence_mask` == `engine.rebalance_mask` 0 bars; **G8 is the gate that licenses the run — 1154's one-shot C_IS4B picks (0.625 U56 / 0.575 B136) replay EXACTLY on 1154's own IS window and span, 0 of 2 mismatches, before anything new is read**; G9 the time-varying-gross build with a CONSTANT vector == the scalar path at 0.00e+00, so the splice machinery adds nothing; G10 the ladder nests the record's committed rungs. HYPOTHESES 4 of 5 SUPPORTED.

  **(Q1) THE ANSWER. 361 of 368 choices (0.9810) are INTERIOR rungs; U56 picks interior at 184 of 184.** All **7** endpoint picks are gross 1.000, all on **B136**, all on the two SHORTEST windows (W504/W756), and all clustered in **2021-07 .. 2023-07** — a 2-3 year lookback ending in that stretch contains no drawdown deep enough to make `M_DD` bind, so the tent degenerates into a monotone ramp and 1154's own identity (argmax-of-monotone is an endpoint) takes over. **The tent is structural to `min(M_S, M_DD, M_CAGR)`, not to the 2009-2016 window.** Pick levels median **0.5625 (U56) / 0.5125 (B136)**, range 0.300-1.000: the rolling chooser systematically DE-GROSSES against the incumbent 0.750 and sits below 1154's one-shot 0.625/0.575.

  **(Q2) IT MOVES CONSTANTLY AND MOVING IS ALMOST FREE.** Median move rate **0.8333**, range 0.360 (WEXP/R6M) to **1.000 — at the 2-year cadence EVERY re-choice moves the pick on both panels**; mean absolute step 0.0685 of gross, about 2.7 rungs. That is expected rather than a defect: a tent peaks where two noisy legs CROSS, the least resolvable point on a ladder whose whole-span Sharpe spread is **0.0041 on U56 (1.1303..1.1344) and 0.0082 on B136**. **The COST is the surprise — against the same cell with its gross frozen at its own median pick (paired, so the tape cancels), the moving adds a median of 0.118x NAV/yr of turnover = 1.18 bps/yr of drag**, range -0.12 to +6.04 bps/yr, the negative cells trading LESS than their frozen comparand. Switching gross trades only the CASH leg, so instability is cheap here in a way it would not be on a selection dial.

  **(Q3) RULE 8 AND BOTH KEEP PATHS. H_OOSPASS REFUTED: the 4b OOS pass SURVIVES re-choosing and beats the ladder's own base rate — 19 of 24 cells against 22 of 66 rungs (0.79 vs 0.33)**; 4b full 10 of 24 against 13 of 66; **4a 0 of 24 and 0 of 66**. Rule 8 was run on the PAIR: the (window, cadence) cell chosen on the FIRST HALF of the common span by **three independent honest choosers (C_H1SHARPE, C_H1CAGR, C_H1TENT) — all three picked a rolling cell and all three clear 4b on the untouched SECOND half, where the frozen incumbent gross 0.750 FAILS.** U56 rule-8 pick (W1260, R2Y): second half **14.73% / 1.2932 / -13.28%** against SPY **18.22% / 1.0746 / -24.50%**; full common span 11.89% / 1.1312 / -13.28% (halves 0.9507/1.2932), post-2016 12.55% / 1.1801. Benchmarks on the common span: **U56 SPY 13.74% / 0.8368 / -33.72% (halves 0.6054/1.0746), OOS 15.21% / 0.8711; U56 RULES v2 (live) 8.46% / 1.1960 / -12.05%, OOS 9.46% / 1.2763; B136 SPY 13.83% / 0.8413 / -33.72%, OOS 15.33% / 0.8767; B136 RULES v2 7.19% / 1.0342 / -12.24%, OOS 7.88% / 1.1059.**

  **AND THE HONEST READING IS UNCOMFORTABLE, STATED RATHER THAN SMOOTHED.** On that same second half **the frozen incumbent posts a HIGHER Sharpe (1.3332 vs 1.2932) and a much higher CAGR (19.65% vs 14.73%)**; it fails 4b **only** on drawdown, -15.77% against the -14.70% cap the rolling rule clears at -13.28%. **The rolling rule wins the 4b verdict by being SMALLER, not by being better.** 1150's identity re-measured on this span: across the whole 33-rung U56 ladder Sharpe spans **0.0041** while CAGR spans **16.97 pp** (4.16% -> 21.13%) — the gross dial is one book at different exposures, so a 4b verdict on it is decided by the drawdown cap and the CAGR floor and never by Sharpe.

  **B136 IS A FLAT KILL and that is half the case against enacting: 0 of 12 cells clear 4b full, and all three rule-8 choosers fail OOS on the Sharpe leg (1.0329 against SPY's 1.0637), the incumbent included at 1.0385.** A rule that is a KEEP on one current-constituent panel and a KILL on the other is not a rule worth real capital.

  **THE MEMO (written because 4b passed; PARK, NOT enacted — rule 6).** Exact RULES wording published in `.memo.md` as a *selector*, not a new book: *"on the first rebalance of each even calendar year, over the trailing 1,260 trading days compute M_S, M_DD and M_CAGR for each gross 0.200..1.000 step 0.025 and set the book's gross to the g maximising their minimum."* Four reasons not to enact: it adds no Sharpe (the dial cannot), the walk-forward's own row says the incumbent is better on Sharpe and CAGR, it fails outright on B136, and the chooser is unstable by construction. Honest discount stated in the memo: the chooser maximises trailing versions of the very legs 4b later tests, so the full-span pass is partly tautological; the second-half pass is not.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every CAGR and drawdown LEVEL is optimistic and both the 4b drawdown cap and the CAGR floor are measured against an inflated book; the bias does NOT cancel out of the 4b legs. It very largely DOES cancel out of this run's headline claims — interior share, move rate and switch cost all contrast the same book against itself across windows on the same tape.

  Script `research/backtests/2026-09-17_does-the-TENT-CHOOSER-reach-the-INTERIOR-on-a-ROLLING-IS-WINDOW_cloud.py`, 6 CSVs, console log, result note, memo, 4 LEADERBOARD rows.

- 2026-09-17 (lane B, idea 1159 does-ANY-committed-MAXIMUM-KEYED-claim-in-the-record-carry-a-RESAMPLE-NULL) — **ANSWERED = ALMOST NONE DOES, AND WHEN THE NULL IS BUILT THE PUBLISHED NUMBER IS ARITHMETIC AT 35 of 36 CELLS: 1157's single SMALL/MAXDD result generalises to every maximum-keyed object, every ladder and every panel.** Two declared hypotheses REFUTED, one REVERSED at 12 of 12. A draft PROTOCOL clause priced ENACT-WORTHY on its own pre-declared rule (proposed only). A bycatch that bears on every cross-run gate in the record. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the LAST open idea; 1159 ended '## Open' and is not EDGAR / Form 4 / 8-K / options / live-data. Price leg of 81 rung books and 36 rule-8 picks, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4, and the queue names both):** `CLAIM SET` {CS_MAXDD, CS_SPREAD, CS_ARGMAX, CS_ALL} x `NULL TYPE` {N_BLOCK, N_IID, N_STAT} = **12 combinations, ALL published** in `.dialgrid.csv`. PANEL {U56, B136, SMALL}, LADDER {N, H, GROSS, CADENCE} and RUNG are NOT dials — all 81 books built and committed at every point. PROXIMITY WINDOW {200, 400, whole unit} is not a dial (all three printed everywhere, nothing selected on any); q is not a dial (0.90 headline, 0.80/0.95 beside); BLOCK LENGTH frozen at the record's L=63. Everything else frozen at 1082/1094/1098/1102/1108/1110/1116/1117/1118/1122/1131/1140/1148's construction.

  **THE DESIGN.** A maximum's expected value grows with the count it is taken over — over BARS (1140's b(level) +0.2645, the only one of six statistics with a material one) and over RUNGS (1148's count inflation) — so three OBJECTS are built to match what the record actually publishes: **OBJ_MAXDD** (a |MaxDD| level), **OBJ_SPREAD** (max-minus-min of |MaxDD| across a ladder), **OBJ_ARGMAX** (best rung minus second-best). The SAME resampled index is applied to EVERY rung, so the tape cancels in the spread and margin (1012/1150's paired currency).

  **GATES 10 of 14 PASS, printed before any result number, and the 4 failures are this run's most consequential finding.** G1 fast runner == `engine.backtest` 1.39e-17; G4 live RULES v2 MaxDD == -12.05% 4.95e-05; G5 SMALL determinism 0.00e+00; G6 seed determinism 0.00e+00; G7 N_IID run-length share 2.5e-03; G8 N_STAT mean block 63 +/- 2.6. HYPOTHESES 3 of 5 SUPPORTED.

  **THE CENSUS — the idea's literal question.** Over **30,879 committed prose units** (LEADERBOARD rows, CHANGELOG paragraphs, 892 `*.result.md` files) there are **9,338 VALUED maximum-keyed figures**. Carrying a resample null: **228 (0.024) within 200 characters, 318 (0.034) within 400, 421 (0.045) anywhere in the same unit** — the most generous reading available; even the bare word "null" reaches only 557 (0.060). By claim set: CS_MAXDD 4,698 (0.021 / 0.038), CS_SPREAD 3,102 (0.027 / 0.050), CS_ARGMAX 1,538 (0.030 / 0.059). Machine-readable side: of **5,571 committed CSVs, 1,495 carry a maximum-keyed column and 267 of those (0.179) also carry a null column**. **"Does ANY" is literally yes and materially no: 95.5% of the record's committed maximum-keyed figures are published bare.**

  **THE RE-SCORING, AND IT IS EMPHATIC. Under the record's own moving-block null 35 of 36 maximum-keyed cells sit INSIDE their own 90% band; at q=0.95 it is 108 of 108 across all three nulls** (N_BLOCK 0.972, N_IID 0.944, N_STAT 0.972 at q=0.90). The single escape is reported rather than smoothed and goes the OTHER way: **SMALL / H / OBJ_SPREAD, observed 2.481 against a null median of 7.948 and a band [2.636, 18.904], with 0.952 of draws ABOVE it** — the one distinguishable maximum-keyed number on this grid is distinguishable for being too SMALL.

  **A SYSTEMATIC DIRECTION NOBODY HAS PRICED, and it is object-specific.** The observed value sits BELOW its own null's median at **89 of 108 cells (0.824)** (median observed/null ratio **0.936 / 0.851 / 0.964** by null type; median share-of-draws at or above the observed value 0.665 / 0.694 / 0.624 by object). It is **not universal and the exceptions are structured: OBJ_MAXDD is below at 36 of 36, OBJ_SPREAD at 26 of 36, OBJ_ARGMAX at 27 of 36** — all 19 exceptions are SPREAD or MARGIN objects, 12 of them on the H ladder. Resampling reliably yields DEEPER drawdowns than the record realised, and wider rung spreads / larger argmax margins only usually. Stated as measured, not explained; filed as 1161, because a null biased in a known direction is not yet a bar.

  **KILL 1 — H_SPREAD, and it reverses.** Declared: a max-minus-min over rungs of ONE tape would be the LEAST distinguishable object. Measured: OBJ_SPREAD is the **MOST** distinguishable (inside-band 0.917 against 1.000 for both OBJ_MAXDD and OBJ_ARGMAX), and its one escape is the low-side SMALL/H cell above.

  **KILL 2 — H_NULLDEP, reversed at 12 of 12.** Declared: N_IID destroys the autocorrelation that deepens drawdowns, so its |MaxDD| null would sit LOWER. Measured: **N_IID's |MaxDD| null median is HIGHER — 25.719 against N_BLOCK's 22.538 and N_STAT's 21.531 — at 12 of 12 cells**, so destroying serial structure makes simulated drawdowns WORSE and these books' realised return ORDER is drawdown-protective. Filed as 1162. **The verdict is nonetheless dial-INVARIANT where it matters: the inside-band share moves only 0.944 -> 0.972 across all three nulls and is 1.000 at q=0.95 under every one — the null choice moves the null's LEVEL materially and the published VERDICT barely at all.**

  **BYCATCH, AND IT MAY MATTER MORE THAN THE HEADLINE: THE RECORD'S COMMITTED CONSTANTS ARE NOT REPRODUCIBLE FROM THE REPO.** G2, G3 and G9 fail against 1148's committed numbers one day old. Decomposed rather than tuned away: **B136 and SMALL reproduce 1148's committed grid at 2.22e-16 and 9.71e-17 on the current tape**, so the pipeline in this file IS the record's and the whole deviation is U56's — the only panel whose cache advanced. Truncating U56 to 1148's own 4,705-row tape makes **G2V and G3V PASS, accounting for 97.3% of the deviation with one extra bar**. **The surviving 9.995e-05 residual is not a bar: `data/prices.csv` is NOT append-only.** Commit `6f1fcb1` ("Daily close 2026-09-16") rewrote the whole file — of **258,733 price cells shared by the two vintages, 31,505 (0.1218) were RESTATED across 48 of 58 tickers back to 2008-01-02**, max relative move **0.008384 (ITB)**, median restated move 1.50e-06. Every committed constant in this record is pinned to a price vintage that no longer exists on disk, and no truncation recovers it. Filed as 1163; reported here, not repaired. **No tolerance was widened and no committed constant was re-derived to fit.**

  **AND THE VINTAGE ARM SAYS SOMETHING ABOUT MAXIMA SPECIFICALLY, opposite to the naive reading.** The extra bar moves the same books' CAGR and Sharpe at 1e-03 and moves **ALL TWELVE maximum-keyed objects by EXACTLY 0.0000**: a new bar cannot touch a maximum unless it sets a new extreme, and U56's max drawdown is a 2020 event. This does NOT contradict 1140's b(level) +0.2645 — **a maximum grows with tape length in EXPECTATION, in jumps, and is flat between them**, which is precisely why watching it fail to move tells you nothing about its sampling distribution, and why it needs a null when a Sharpe does not.

  **THE DRAFT CLAUSE, PRICED (proposed, NOT enacted — rule 6).** *PROTOCOL rule 11, "no bare maximum": a backtest may not publish a number that is a maximum over a varying number of points — a MaxDD, a max-minus-min spread or band, an argmax margin, a worst-rung figure — without a resample null beside it: the null's median, its band at the declared q, and the share of draws on the published side.* Decision rule, declared before any number: enact-worthy only if compliance is near zero AND the null moves verdicts. **(a) compliance 0.045 of 9,338 figures at the most generous window; (b) 0.972 of re-scored cells sit inside their own band. Both fire: ENACT-WORTHY.** Cost is one column and one bootstrap; what it buys is that two runs' maxima become comparable at all.

  **RULE 8 AND BOTH KEEP PATHS — 81 books, 36 IS picks, NOTHING PROPOSED.** Books byte-identical across both dials, so this leg is invariant to both; run because rule 4 requires it. Benchmarks: **U56 SPY full 15.06% / 0.8814 / -33.72% (halves 0.9598/0.8170), OOS 15.15% / 0.8684 / -33.72%; U56 RULES v2 (live) full 8.60% / 1.1980 / -12.05%, OOS 9.42% / 1.2714. B136 SPY full 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767; B136 RULES v2 full 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059. SMALL SPY full 14.06% / 0.8581 / -33.72%, OOS 15.33% / 0.8767; SMALL RULES v2 full 4.30% / 0.6637 / -13.89%, OOS 3.75% / 0.5600.** Picks: **4b full 4, 4b OOS 4, 4a 0 of 36**; whole 81-rung grid **4b full 16, 4b OOS 17, 4a 0 of 81**. Median OOS Sharpe 0.9963, median regret against the frozen default **+0.0000**. **All four 4b-full passes among the picks ARE the frozen default rung** (U56 GROSS 0.75 and U56 CADENCE W, each picked by C_ISSHARPE and C_ISCAGR): OOS 16.92% / 1.1615 / -19.13% vs SPY OOS 0.8684. Best cell passing 4b full AND 4b OOS anywhere is **U56 N=12: full 17.65% / 1.1658 / -20.17% (halves 1.2741/1.0833), OOS 18.78% / 1.1701 / -20.17%** — **not a discovery** (1098/1102 committed the same cell at 0.1771 / 1.1692 / -0.2017 on their vintage) and **not reachable**, since no IS-only chooser picks it. **Nothing proposed, no memo, no RULES change.**

  **SURVIVORSHIP (rule 9).** U56, B136 and SMALL are CURRENT-CONSTITUENT panels, so every |MaxDD| LEVEL is optimistic and OBJ_MAXDD's observed value is biased shallow against a null built on the same inflated tape; the bias does NOT cancel out of a level comparison. It very largely DOES cancel out of OBJ_SPREAD and OBJ_ARGMAX, which contrast rungs of the SAME panel on the SAME resampled tape. 1157's committed SMALL/MAXDD band (0.441, 0.881) is quoted from its own file, never re-derived: this run measures a different object and is a SECOND construction bearing on the same question, not a reproduction.

  Script `research/backtests/2026-09-17_does-ANY-committed-MAXIMUM-KEYED-claim-in-the-record-carry-a-RESAMPLE-NULL_B.py`, 12 CSVs, console log, 5 LEADERBOARD rows. Follow-ups filed 1161 (why a resample null sits ABOVE the observed maximum at 108 of 108 cells), 1162 (why an IID resample produces DEEPER drawdowns than a block one), 1163 (is the record reproducible at all while data/prices.csv is rewritten nightly).

- 2026-09-16 (lane C, idea 1150 is-the-CAGR-FLOOR-a-DE-GROSSING-DETECTOR-rather-than-a-COST-LEG) — **ANSWERED = YES, IT IS A DE-GROSSING DETECTOR AND NOT A COST LEG, AND THE QUEUE'S OWN WORDING OF THE HAND-OVER IS REFUTED: it is not SHARPE -> CAGR, it is L_DD -> L_CAGR, two EXPOSURE legs.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the SECOND open idea; 1150 was second in '## Open' and is not EDGAR / Form 4 / 8-K / options / live-data. Pure price run — 66 books, 528 scored cells, 48 rule-8 picks — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4, and the queue names both):** `GROSS RUNG` {0.200 .. 1.000 step 0.025, 33 rungs} x `COST RUNG` {0, 2, 5, 7.5, 10, 15, 25, 50 bps} = **264 per panel, 528 in all, EVERY ONE PUBLISHED** in `.grid.csv`. PANEL {U56, B136} is not a dial. **BENCHMARK VARIANT is not a dial either:** B_SPY is the PROTOCOL floor and the headline; B_SPYCOST (SPY charged the same rate on its own turnover) and B_SPYGROSS (SPY held at the SAME gross, rest in cash) are CONTROLS computed at all 528 cells and never selected on. The fine ladder NESTS the record's committed 0.30..0.75 step-0.05 rungs (G10). Frozen at 1082/1094/1098/1102/1108/1110/1116/1117/1118's construction: CAND20 legs, cap INF, max_vol 0.60, min hold 126, N=20, W, 10 bps, LAG 1, warm-up 260, IS end 2016-12-31, zero cash, block L=63, 1000 draws, crc32 seeds, q=0.90.

  **THE BINDING-LEG RULE, DECLARED BEFORE ANY NUMBER.** Each 4b leg re-expressed as the signed fraction of its OWN bar still in hand (Sharpe legs `(book-SPY)/|SPY|`; L_DD `(0.60|SPY_dd|-|dd|)/(0.60|SPY_dd|)`; L_CAGR `(CAGR-0.70*SPY_CAGR)/|0.70*SPY_CAGR|`), so **BINDING = argmin of the five**; `g*` = the highest gross rung whose binding leg is L_CAGR.

  **GATES 10 of 10 PASS, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS 1.70e-04; G4 1098/1102's U56 n=12 triple 4.97e-05; G5 live RULES v2 MaxDD 4.95e-05; G6 determinism 0.00e+00; G7 `cadence_mask` == `engine.rebalance_mask` on D/W/M/Q, 0 differing bars; **G9 the post-hoc cost transform `net = gross - turnover*c/1e4` == `engine.backtest(cost_bps=25)` at 6.94e-18**, which is what licenses pricing 8 cost rungs off one book per gross rung; **G8 reproduces the committed GROSS-ladder grid on all 20 rows x 6 statistics at 2.22e-16 with 0 of 100 leg-flag disagreements.** HYPOTHESES 5 of 5 SUPPORTED.

  **A CORRECTION TO THE QUEUE'S PREMISE, STATED FIRST.** The idea text says the four CAGR-killed cells are "U56 g 0.55/0.60/0.65, B136 g 0.50". Those are the record's first **PASSING** rungs. Every committed grid this tree can read, G8's comparand included, puts the L_CAGR failures at **U56 g <= 0.50 and B136 g <= 0.45** at 10 bps. The direction of the finding is unaffected; the run tests the boundary it can reproduce.

  **THE ANSWER. The hand-over EXISTS and is MONOTONE at 16 of 16 (panel, cost) points: g* = 0.600 on U56, 0.525 on B136** — below it L_CAGR binds, above it L_DD does.

  **IT IS NOT A COST OBJECT.** Moving cost 0 -> 50 bps, five times PROTOCOL's rung and the full width of the record's own ladder, moves g* by **exactly one rung of 0.025 on U56 (and only past 15 bps) and by ZERO rungs on B136**. On the paired block bootstrap (1012's basis — same 1,000 draws on both arms, so the tape cancels) median `g*(50) - g*(0)` is **+0.0250 U56 [90% -0.000, +0.050] and +0.0000 B136 [-0.025, +0.029]**, `|delta| <= 1 rung` in **89.8% / 92.9%** of draws, exactly zero in 35.2% / 44.1%.

  **THE MECHANISM, MEASURED.** At g* the book's CAGR sits **8.60 pp/yr (U56) and 10.44 pp/yr (B136)** below the same book at gross 1.00 with no costs, while 0 -> 10 bps of cost accounts for **0.263 pp and 0.257 pp**: cost is **3.0% and 2.4%** of the drop that puts the book through the floor. Across the WHOLE 0.20..1.00 ladder the full-sample Sharpe spread is **0.0016 to 0.0116** while the CAGR spread is **15.2 to 17.6 percentage points** — the queue's "1.1390 vs 1.1397" is the shape of the entire ladder, not a near-miss.

  **THE CONTROL THAT DECIDES IT. Under a GROSS-MATCHED floor — SPY held at the book's own gross, remainder in cash — L_CAGR binds at 0 of 528 cells and FAILS at 0 of 528.** Charging the benchmark instead (B_SPYCOST) changes g* at **0 of 16** points, because buy-and-hold SPY has no turnover to charge: **the floor's asymmetry is an EXPOSURE asymmetry, not a cost-rate asymmetry.** 1063's one-sided-handicap reading is the right object in the wrong currency.

  **KILL OF THE "SHARPE -> CAGR" WORDING.** Over all 528 cells a Sharpe-family leg binds at **47 (0.089)**; L_DD at 231, L_CAGR at 250, **L_OOS at 0**. On **U56 a Sharpe leg binds at 0 of 33 gross rungs at every cost rung from 0 to 25 bps**; one appears only at 50 bps. Consequence: the 4b pass set at 10 bps is a **CONTIGUOUS WINDOW between two opposed exposure legs — U56 gross 0.525..0.775 (11 rungs), B136 0.500..0.725 (10) — a band, not a peak**, and every rung inside it is the same book scaled.

  **THE LIMIT, MEASURED NOT ASSERTED.** g* as a NUMBER is barely resolvable: its unpaired 90% block-bootstrap interval spans **8 rungs on both panels** (U56 [0.450, 0.625], B136 [0.400, 0.575]). What IS resolvable is its INVARIANCE, because that comparison is paired and the tape cancels; every claim above is made in the paired currency. L_OOS is undefined on a resampled tape and is excluded from the bootstrap arm only (4 legs there, 5 in the point arm); stated, not hidden.

  **RULE 8 AND BOTH KEEP PATHS — NOTHING PROPOSED.** Only the 10 bps rung is a PROTOCOL-legal book (rule 2) — a book cannot choose its cost rate — so KEEP is asked there and nowhere else: **66 cells, 4b full 21, 4b OOS 22, 4a 0**; 4a is **0 of 528** at every rung and cost rung. Benchmarks: **U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; B136 SPY 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767 / -33.72%; live RULES v2 U56 8.62% / 1.2007 / -12.05% (halves 1.2322/1.1760, OOS 9.45% / 1.2762), B136 7.98% / 1.0993 / -12.24% (OOS 7.88% / 1.1059).** Best 10 bps cell passing 4b full AND OOS is **U56 gross 0.775: full 16.10% / 1.1398 / -19.72% (halves 1.2038 / 1.0972), OOS 17.54% / 1.1644 / -19.72%, 2.99x/yr** against the incumbent gross 0.75's full 15.58% / 1.1397 / -19.13%, OOS 16.97% / 1.1643 — **one rung away for +0.0001 of full Sharpe, inside every resolution floor the record has measured.** **Rule 8 is emphatic: 0 of 48 IS-only picks pass 4b full, 4b OOS or 4a** — C_ISSHARPE and C_ISCAGR both pick gross **1.00** (fails L_DD) and C_ISDD picks **0.20** (fails L_CAGR) on both panels, so the three honest choosers land on the two ENDPOINTS, i.e. on the two binding legs, and the passing band between them is unreachable by any of them.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every CAGR and drawdown LEVEL is optimistic and the L_CAGR and L_DD bars are both measured against an inflated book; the bias does NOT cancel out of the 4b legs. It very largely DOES cancel out of this run's headline, which contrasts the same book against itself across cost rungs and against a de-grossed version of the same benchmark.

  **WHAT THE RECORD SHOULD DO WITH IT, STATED NARROWLY (proposed, NOT enacted — rule 6).** (1) A 4b verdict on a GROSS ladder is not a cost verdict: at PROTOCOL's rung cost moves the boundary by 0-1 rung of 0.025 and accounts for ~3% of the CAGR drop that causes the failure. (2) Any committed "this book dies on the CAGR floor" claim living on the gross ladder should carry the **gross-matched control**, which zeroes the leg at 528 of 528 cells. (3) "The binding leg hands over from Sharpe to CAGR" should be re-quoted as **L_DD -> L_CAGR**.

  Script `research/backtests/2026-09-16_is-the-CAGR-FLOOR-a-DE-GROSSING-DETECTOR-rather-than-a-COST-LEG_C.py`, 9 CSVs, console log, 6 LEADERBOARD rows. Follow-ups filed 1152 (is the 4b gross WINDOW's width a panel object or a tape object), 1153 (does any committed CAGR-floor death survive a gross-matched benchmark), 1154 (why do all three honest choosers land on a gross ENDPOINT).

- 2026-09-16 (lane B, idea 1140 is-DD-s-RUNG-ROBUST-UN-RESOLVABILITY-a-PATH-FUNCTIONAL-fact-or-a-TAPE-fact) — **ANSWERED = NEITHER. THE IDEA'S BINARY IS REFUTED ON BOTH HORNS AND A THIRD, NARROWER CARRIER IS NAMED AND MEASURED: it is MAX-ness, a strict SUBSET of path-functionality.** KILL of the path-functional reading, KILL of the pure tape reading, a surviving mechanism with a reproducing sign structure, a bycatch that bears on every committed H / CADENCE argmax, a measured LIMIT on the design, and a DEFECT in this run's OWN pre-registration reported rather than fixed. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the LAST open idea.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `STAT SET` {S_NONPATH, S_PATH, S_ALL} x `SUB-TAPE FRACTION` {1/1, 1/2, 1/3} = **9 combinations, ALL published**, and again at q = 0.80 / 0.95. PANEL (U56, B136), LADDER (N, H, GROSS, CADENCE) and RUNG SET (CORE, EXT) are NOT dials — all 5,184 cells are committed in `.cells.csv`. q is NOT a dial (0.90 headline, 0.80/0.95 beside, never selected on). BLOCK LENGTH is NOT a dial (L=63, frozen). Everything else frozen at 1082/1094/1098/1102/1108/1110/1116/1117/1118/1122/1131's construction.

  **THE DESIGN.** The idea's word is "path functional", so the test is matched: THREE PAIRS in which the only difference is whether the statistic reads the ORDER of the same return stream or only its MOMENTS — level CAGR/**MAXDD**, dispersion VOL/**ULCER**, ratio SHARPE/**CALMAR** — re-run over SIX DISJOINT sub-tapes of the warm record (1 whole, 2 halves, 3 thirds), i.e. 1131's own 16 blocks at three tape lengths.

  **GATES 11 of 11 PASS**, printed before any result number. G9 reproduces 1131's committed 16-block table on all 12 shared blocks at 0.00e+00 (1131's S_FULL == SHARPE, CAGR == CAGR, DD == MAXDD, same seeds, same draws); G8 reproduces all 54 rows of 1110's committed CORE grid at 0.00e+00. HYPOTHESES 2 of 5 SUPPORTED.

  **KILL 1 — the PATH-FUNCTIONAL reading.** ROBUST counts over the 12 mover cells per statistic: CAGR **0/12** vs MAXDD **12/12**; VOL **0/12** vs ULCER **7/12**; SHARPE **6/12** vs CALMAR **9/12**. The pair ordering runs the path way in all three pairs, so path-functionality contributes — but if it were the carrier the path arm would be 12/12/12, and ULCER and CALMAR both ESCAPE on the U56 blocks. **MAXDD is the only one of the six that never escapes anywhere.**

  **KILL 2 — the pure TAPE reading.** MAXDD is resolvable on **13 of 60** shortened-sub-tape mover cells at EXT against **0 of 12** on the full tape, over 5 distinct (panel, ladder, sub-tape) combinations of 20 — so there ARE stretches of this record on which its rung gaps resolve. But at the same fractions CAGR is 57/60, VOL 57/60, SHARPE 50/60, ULCER 24/60 and CALMAR 35/60: MAXDD is the most robust of the six at EVERY fraction.

  **THE MECHANISM THAT SURVIVES — MAX-ness, not path-ness.** Fitting log(y) = a + b*log(n) over the six sub-tapes, **MAXDD is the ONLY statistic of six whose realised rung GAP and whose bootstrap SD BOTH GROW with the tape**: b(gap)/b(SD) **+0.1419 / +0.1654** pooled (**+0.2606 / +0.1654** on the movers) against CAGR −0.3495/−0.4675, VOL +0.0004/−0.3181, SHARPE −0.4231/−0.4482, **ULCER −0.1221/−0.1916 and CALMAR −0.1699/−0.5543 — both path functionals have FALLING SD, like the moments.** ULCER averages the same drawdown path and CALMAR divides by the same maximum; only the maximum itself grows without bound in T. Its own level does the same (median |MaxDD| 21.52 / 16.67 / 16.32 at 1/1 / 1/2 / 1/3, b(level) **+0.2645**, the only statistic with a material one), which re-derives 1110's +0.2093 vs −0.4540 in a second currency.

  **BYCATCH, and it may matter more than the headline.** On the mover ladders the NON-PATH statistics' realised rung gaps shrink at **n^−0.5841** while their sampling SD falls at only **n^−0.4400** (textbook −0.50), so the resolution ratio gets **WORSE** with a longer tape: b(ratio) **−0.1251** for the non-path arm against **+0.0327** for the path arm. A gap that converges to ~0 is a rung effect that IS ~0. **A committed argmax on an H or CADENCE ladder in CAGR / VOL / SHARPE is not under-powered and waiting on data — more tape will never resolve it.** Filed as 1147.

  **THE LIMIT, MEASURED NOT ASSERTED.** A sub-tape differs from the full tape in LENGTH *and* in REGIME. On the same published column the WITHIN-fraction spread (same length, different stretch = pure regime) against the BETWEEN-fraction move (length + regime) is CAGR **3.50x**, VOL **5.52x**, SHARPE **1.66x**, MAXDD **5.19x**, ULCER **61.5x**, CALMAR **9.07x**. Every exponent MAGNITUDE in the file is therefore an upper bound on precision; **only the SIGN structure is asserted**, and it reproduces across both cuts, all three seed bases and both panels. Filed as 1148.

  **A DEFECT IN THIS RUN'S OWN PRE-REGISTRATION, reported and NOT fixed.** H_EXPONENT's arm medians pooled all 16 cells per statistic, including the N and GROSS ladders whose rung sets cannot move and on which 1131's claim does not live. Restricted to the 8 mover cells — a re-cut of already-published columns, not a third dial — the comparison moves from (+0.1107 non-path vs +0.1293 path) to (**−0.1251** vs **+0.0327**). The pre-registered decision rule's mechanical output is **TAPE**; it is published as the rule's output and no further, because its TAPE clause fires on an arm-median difference (+0.0186) that is inside the regime noise measured above and that reverses on the mover-only re-cut. **The finding this run stands behind is the narrower one.**

  **RULE 8 AND BOTH KEEP PATHS — 74 books, 48 IS picks, NOTHING PROPOSED.** Books byte-identical to 1131's, so this leg is invariant to both dials; run because rule 4 requires it. U56 SPY full 15.10% / 0.8829 / −33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / −33.72%; U56 RULES v2 (live) full 8.62% / 1.2007 / −12.05%, OOS 9.45% / 1.2762 / −12.05%. 48 IS picks: **4b full 6, 4b OOS 6, 4a 0**, median OOS Sharpe 1.0244, median regret +0.0228; whole 74-rung grid 4b full 17, 4b OOS 18, **4a 0 of 74**. Every 4b pass among the picks is the frozen default rung (U56 GROSS 0.75 / CADENCE W): full 15.58% / 1.1397 / −19.13%, halves 1.2037/1.0971, OOS 16.97% / **1.1643** / −19.13% vs SPY OOS 0.8711. Best pick by OOS Sharpe U56 H/CORE 252: OOS 17.31% / 1.1931 / −21.84%. **Every 4b pass is the incumbent default — nothing proposed, no memo, no RULES change.**

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every level is optimistic and every exponent is fitted on that inflated tape; the bootstrap prices SAMPLING error only and cannot correct the bias. **Cross-read, not recomputed: 1141 (cloud, same day) found DD's "un-resolvable everywhere" is a LARGE-PANEL fact — it fires less on SMALL.** Independent evidence on the same side as KILL 2; this run does not rebuild SMALL.

  Script `research/backtests/2026-09-16_is-DD-s-RUNG-ROBUST-UN-RESOLVABILITY-a-PATH-FUNCTIONAL-fact-or-a-TAPE-fact_B.py`, 11 CSVs, console log, 5 LEADERBOARD rows. Follow-ups filed 1146 (does a TRUNCATED maximum behave like ULCER or like MAXDD), 1147 (are the record's H / CADENCE argmaxes waiting on data that cannot help), 1148 (should a sub-tape comparison publish its regime-to-length ratio).

- 2026-09-16 (cloud lane, idea 2 of 2, idea 1141 does-the-RUNG-CHOICE-effect-reproduce-on-the-SMALL-panel) — **ANSWERED = NOT AS 1131 STATED IT, AND THE QUEUE'S OWN PREMISE IS REFUTED IN THE OPPOSITE DIRECTION. KILL of the premise; the underlying rung-CHOICE dependence REPRODUCES and is STRONGER on the small panel; two CORRECTIONS to committed 1131 statements.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the LAST eligible open idea; 1141 ended '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. It has a price leg — 111 books over 4 ladders x 3 panels x 2 rung sets, 72 rule-8 picks — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `PANEL` {U56, B136, SMALL} x `RUNG SET` {CORE, EXT} = **6 combinations, ALL published** (at three q each, 18 rows). LADDER is not a dial: all four rebuilt, headline restricted to **H and CADENCE**, the only two whose rung SET changes — N (9) and GROSS (10) carry IDENTICAL rung lists at both levels (1131/1134's structural fact) and are rung-inescapable BY CONSTRUCTION. STATISTIC is not a dial (all 4 everywhere). **CONFIDENCE q is NOT a dial** (0.90 headline, 0.80/0.95 beside, nothing selected on them). **SEED BASE is not a dial** — 1131's own three bases, so U56 and B136 reproduce bit for bit. Frozen at 1082/1094/1098/1102/1108/1110/1116/1117/1118/1122/1131's construction: CAND20 legs, cap INF, max_vol 0.60, gross 0.75, min hold 126, N=20, W, 10 bps, LAG 1, warm-up 260, IS end 2016-12-31, block L=63, 1000 draws, crc32 seeds.

  **THE SMALL PANEL'S STAMP (idea 1074's recommendation, 1119's practice).** `data/small_meta.csv` lists **715 tickers; 52 dropped for max_1d_move >= 1.0**; pool served **664 columns = 663 constituents + SPY as BENCHMARK ONLY**, tape **2010-01-04 .. 2026-09-11, 4,198 rows** (warm 3,938, IS 1,502, OOS 2,436). This idea's own text says "483 sub-$2B names"; the loader serves 663 today (idea 706's rebuild, idea 1072's finding that committed SMALL headlines move on it) — **the STAMP, not the label, is what every SMALL number refers to.** SPY is removed from the selectable set and from the live-RULES-v2 comparand on that panel, and **G_SPY PRICES that choice rather than asserting it: had SPY been left selectable it would have been held on 0 of 871 rebalance dates.** **THE TAPE IS NOT MATCHED AND CANNOT BE, and the direction is stated before any number:** SMALL starts 2010 against U56's 2008, so its bootstrap sees ~500 fewer rows and its floors are WIDER for that reason alone — a bias TOWARD firing, i.e. toward the queue's premise and AGAINST this run's answer. The premise is refuted anyway.

  **GATES 10 of 10 PASS, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS 1.70e-04; G4 live RULES v2 MaxDD 4.95e-05; G5 determinism 0.00e+00; G6 `cadence_mask` == `engine.rebalance_mask` on D/W/M/Q, **0 differing bars on all three panels** (engine.py NOT modified); G9 every ladder live on every panel (min Sharpe spread 1.79e-03). **The two that matter: G7 reproduces 1131's committed `ksubsets_joint` on all four large-panel blocks at 2.78e-17 and G8 its committed per-statistic counts on all 16 rows at 0.00e+00** — the two-panel result this run extends is reproduced EXACTLY before the third panel is read. HYPOTHESES 1 of 5, and four of the five failures are the finding.

  **THE ANSWER, AND IT IS THE OPPOSITE OF WHAT THE QUEUE PREDICTED.** The queue reasoned from 1073's reliability gap (0.0915 on small-cap panels against 0.5126 on large) that SMALL *should fire far MORE often at every rung set*. **Matched-k share firing at all four statistics: SMALL H 0.2778 against U56 0.5079 and B136 0.7143; SMALL CADENCE 0.0794 against 0.0952 and 0.1825 — LOWER than both large panels on BOTH movable ladders.** Pooled over all 24 (panel, ladder, statistic) blocks, **1,890 of 3,024 (0.6250)** four-rung subsets fire and **CORE fires in 21 of 24**. Offered as a reading and not a result: firing requires the LARGEST gap in a subset to be un-resolved, and the small panel's rung-to-rung gaps are WIDER as well as noisier, so the two effects cut against each other and the gap wins; this run does not decompose that and does not claim to.

  **1131's LITERAL CLAIM FAILS ON ITS PRECONDITION, AND THE DIRECTION-FREE RE-CUT GOES 1131's WAY HARDER. CORE does not fire at all four statistics on SMALL at all — 5 of 8 blocks against 8 of 8 on both large panels, and every one of the three silent blocks is on SMALL** — so "fires where most subsets do not" has nothing left to reproduce and **H_CORE_OUTLIER_SMALL is REFUTED on its PRECONDITION rather than its bar**. **D1, post-hoc and labelled as such (a re-cut of two already-published columns, not a third dial), drops the direction and asks what share of matched-k subsets AGREE with CORE's own firing state: CORE is the MINORITY state on 6 of 8 SMALL blocks (median agree share 0.4444) against 2 of 8 on U56 (0.7341) and 3 of 8 on B136 (0.7738).** The rung-CHOICE dependence is not merely present on the noisier panel, it is **STRONGER** there; what does not transfer is its SIGN — on SMALL, CORE is atypical in both directions at once, silent on two blocks where a majority of subsets fire and firing on four where a majority stay silent. **1131's recommendation — quote the rung LIST, not the rung count — survives this panel and is strengthened.**

  **CORRECTION 1 — 1131's "DD is the only statistic un-resolvable everywhere" is a LARGE-PANEL fact.** 1131 published *"DD is un-resolvable at every rung set on every block and is the only statistic that is. A committed floor-keyed claim on DD ... is worth quoting."* DD fires on **126 of 126** subsets in all four large-panel blocks, exactly as committed, **and on 55 of 126 (0.4365) on SMALL CADENCE, where the block is silenced by adding 4Q alone.** The one statistic the record was told it could safely quote a floor on is un-resolvable on LARGE-CAP tapes, not on this tape in general; the claim needs a panel stamp.

  **CORRECTION 2 — the non-monotonicity headline reproduces EXACTLY and rests on ONE block of 24.** 1131's fatal detail — *"U56 H / S_OOS still fires on the FULL 9-rung EXT ladder yet falls silent once H=210 alone is added"* — reproduces exactly (escape cost 1, cheapest rung `210`, fires_full_ext True) and is **the only such block across all three panels and all 24 cells; 0 of the 8 SMALL blocks are non-monotone.** Real and correctly identified; quoted as a general property of the trigger it is one observation. **What DOES generalise is the escape itself: of the 21 blocks that fire at CORE, 14 can be silenced by adding rungs and 7 cannot be silenced by ANY of the 31 supersets, median cost 1 rung, max 2** — and restricted to the two large panels this reproduces 1131's committed 10-of-16 / 6-un-silenceable / median-1 / max-2 figures exactly.

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed.** Rungs chosen on IS ALONE per (panel, ladder, rung set, chooser), OOS read ONCE: **72 picks — 4b full 6, 4b OOS 6, 4a 0; whole grid, 111 rungs — 4b full 17, 4b OOS 18, 4a 0, i.e. U56 11 of 37, B136 6 of 37, SMALL 0 of 37.** All six passing picks are U56; best is the standing anchor U56 / GROSS / CORE / gross 0.75, full 15.58% / 1.1397 / -19.13% (halves 1.2037 / 1.0971), OOS 16.97% / 1.1643 / -19.13%, 2.90x/yr. Benchmarks: **U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; B136 SPY 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767 / -33.72%; SMALL SPY 14.06% / 0.8581 / -33.72% (halves 0.9138/0.8344), OOS 15.33% / 0.8767 / -33.72%; live RULES v2 U56 8.62% / 1.2007 / -12.05% (OOS 9.45% / 1.2762), B136 7.98% / 1.0993 / -12.24% (OOS 7.88% / 1.1059), SMALL 4.30% / 0.6629 / -13.89% (OOS 3.75% / 0.5590).** **NOTHING PROPOSED:** the BOOK at every rung is identical across both dials — only which subsets get CALLED fired changes — so 4a and 4b are invariant to them by construction, and every passing pick is the standing top-20 / W / H126 family the record already holds and has already PARKED. 4a is empty at every rung on every panel.

  **WHAT THE RECORD SHOULD DO WITH IT, STATED NARROWLY (proposed, NOT enacted - rule 6).** 1131's "quote the rung LIST, not the rung count" survives and is strengthened. Two of its supporting statements should be re-quoted with a PANEL STAMP rather than as tape facts: the DD claim (Correction 1) and the non-monotonicity headline (Correction 2). And **the queue's own heuristic — a noisier panel fires more — should not be used again without being measured; it is wrong on both movable ladders here.**

  **SURVIVORSHIP (rule 9):** U56 and B136 are current-constituent lists; **the SMALL pool is WORSE** — a screen run on names that exist TODAY, so anything delisted, acquired or dropped out of the screen is absent, and a small-cap pool loses names that way far more often. Every CAGR and drawdown LEVEL on SMALL is optimistic by an unknown and probably large amount. Firing shares, rung-to-rung gaps and agreements contrast rungs over the same inflated tape and the bias very largely cancels out of them; it does NOT cancel out of the 4b legs, measured against SPY, so SMALL's 0 of 37 is if anything an OVERstatement of how close that panel comes.

- 2026-09-16 (cloud lane, idea 1 of 2, idea 1136 is-877-s-SEED-FLOOR-the-RIGHT-RULER-for-a-DETERMINISTIC-MARGIN-at-all) — **ANSWERED = NO, AND THE NUMBER IS 116 OF 116. KILL of 877's 0.0145 seed floor as a ruler for a deterministic pick margin.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the FIRST eligible open idea; 1136 headed '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. It has a price leg — 146 books over 7 ladders x 2 panels, 42 rule-8 picks — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `CLAIM SET` {CS_CSV, CS_PROSE, CS_ALL} x `CONFIDENCE q` {0.50, 0.68, 0.80, 0.90, 0.95} = **15 points, ALL published**. PANEL (U56, B136), LADDER (all 7) and CHOOSER are not dials and are reported everywhere; the headline chooser is C_ISSHARPE because the floor is quoted in Sharpe units. **The OWN-RULER READING is not a dial either:** HALFWIDTH is the headline (a number in 0.0145's own units, so it can be substituted for it literally) and AGREEMENT is reported beside it at every one of the 15 points, never selected on. Block L=63, 1000 draws, crc32 seeds, base 11361136; otherwise frozen at 1096/1100's construction: CAND20 legs, cap INF, max_vol 0.60 (except L_MV), gross 0.75 (except L_G), W (except L_CAD), min hold 126 (except L_H), N=20 (L_N free, L_H at 12), 10 bps, LAG 1, warm-up 260, IS end 2016-12-31.

  **THE TWO RULERS, DEFINED BEFORE ANY NUMBER. TRANSFER** = the object's own imported floor m x 0.0145, which 877/871 measured by REDRAWING A PLACEBO ARM'S SEED and which carries **no confidence** — the defect under test. **OWN** = the destination cell's resolution at q, the half-width of the two-sided q interval of the PAIRED block-bootstrap pick-minus-runner-up difference on the IS window only, the window the chooser sees; with AGREEMENT (the share of draws in which the difference's sign survives) reported beside it. The substitution is not a change of units but a change of OBJECT: a seed floor prices how far a Sharpe moves when a placebo is redrawn, while a rule-8 pick margin is a deterministic function of one tape whose only sampling uncertainty is the TAPE's. Nothing in the record had priced that swap as a verdict.

  **GATES 10 of 10 PASS, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 committed U56 W/H126/N=20 triple 3.18e-07 (15.5787% / 1.1397 / -19.1276%); G3 SPY OOS 1.70e-04; G5 live RULES v2 MaxDD 4.95e-05; G6 determinism 0.00e+00; **G4 CROSS-RUN reproduces 1100's committed `margins.csv` on all 14 margins and spreads at 9.71e-17, 14 of 14 the same pick rung**; **G7 reproduces 1096's committed `above_877_seed_floor` flags on all 14 rows at 0.00e+00**; G8 the own half-width is monotone non-decreasing in q at every cell (min increment 5.84e-05); G9 every ladder live (min IS-Sharpe spread 1.39e-03); **G10 the BOOK grid == 1100's committed 146-rung grid at 1.78e-15**, with its bar stated as **1e-12 = CSV round-trip precision and NOT exact 0**, because the comparand is a round-trip of the same float and a 0.0 bar is one no correct run can pass. HYPOTHESES 3 of 5.

  **THE HARVEST. 403 committed floor-transfer objects: CSV arm 224 over 2 files** (every row of a committed `research/backtests/*.csv` carrying `panel`, `ladder`, a margin-like column and at least one column comparing that margin to an IMPORTED floor — `below_m*`, `above_877_seed_floor`), **PROSE arm 179 over 4 files** (a line qualifies only if it carries a floor token AND a decision word AND names a rebuildable destination). **69 prose lines rejected with reason (48 QUOTES_FLOOR_NO_DECISION, 21 NO_CELL_NAMED), all published.** **Three committed files are CONTROLS, not transfers, and are the record's own good practice** — 1110's and the 9-rung-argmax run's `.floor.csv` and `2026-09-11_should-PROTOCOL-quote-the-4a-leg-as-a-COUNT-not-a-SHARE_C.census.csv` measure their floor IN THE SAME FILE. **THE LIMIT, NAMED:** 77 of the 179 prose objects come from QUEUE.md, a committed artifact but idea text rather than a published result; dropping it entirely moves the headline by **0.0002** (0.4409 vs 0.4411) and the conditional not at all.

  **THE ANSWER. At CS_ALL / q=0.90 / HALFWIDTH, 116 of 263 admissible transfers change verdict (0.4411) and the direction is 116 TRANSFER_LAX to 0 TRANSFER_STRICT — the imported floor NEVER calls unresolved what the tape can resolve, it only ever certifies what the tape cannot.** Conditional on the object asserting RESOLVED (D1, post-hoc and labelled as such, a re-cut of two already-published columns), **116 of 116 = 1.0000 are refused by the destination cell's own ruler, and the own ruler certifies 0 of 263 in total.** Non-GROSS own/0.0145 ratio runs **8.84x to 22.35x, median 12.78x** — 1100's committed 12.9x reproduces — and **median pick agreement is 0.574 with only 2 of 14 cells reaching q=0.90**: the typical published pick's sign survives a coin flip's worth of redraws.

  **THIS RUN'S OWN DECLARED STATISTIC WAS THE WRONG CUT, AND THE FAILURE IS RECORDED RATHER THAN RE-CUT INTO A PASS. H_WRONG_RULER (majority of admissible objects flip) is REFUTED at 0.4411**, and the bar was unreachable on this corpus BY CONSTRUCTION: an unconditional flip share cannot exceed the share of objects that assert RESOLVED at all, which is 116 of 263. **A census asking 'how many verdicts change' must condition on the verdicts that could change** — filed as the lesson, not smuggled into the headline. H_DIRECTION SUPPORTED 116-0; **H_Q_STABLE SUPPORTED, flip share span 0.0038 across all five q**, so the ANSWER is not a free parameter even though the own ruler's LEVEL is; **H_UNDECLARED SUPPORTED — 163 of 263 admissible transfers (0.6198) state no confidence anywhere beside the floor they apply.**

  **1100's '0.022x the other way' on GROSS is READING-DEPENDENT, and B136 GROSS is the one cell of 14 a seed redraw moves. H_GROSS_FLIPS_BACK is REFUTED on the headline HALFWIDTH reading and SUPPORTED on the AGREEMENT reading beside it:** on HALFWIDTH both GROSS cells read UNRESOLVED under both rulers (U56 margin 0.0001 vs half-width 0.0003; B136 0.0003 vs 0.0003), while on AGREEMENT B136's GROSS margin is resolved at **0.953** where 0.0145 calls it unresolved. **REDRAW, reported and NOT gated:** against 1100's base 11001100, median |delta half-width| 0.0046, median ratio 0.9970, and the RESOLVED/UNRESOLVED verdict is unchanged on **13 of 14** cells — **the one that moves is B136 GROSS**, the exact cell the record has been quoting hardest.

  **140 of the 403 harvested transfers apply a SHARPE floor to a margin that is not a Sharpe** — 70 in MaxDD units and 70 in CAGR units, all on committed `below_m*` columns; 59 assert RESOLVED and 57 flip at q=0.90. They are excluded from every headline count and reported apart, because 0.0145 is not a number in those units at all.

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed, and the reason is structural.** Rungs chosen on IS 2009-2016 ALONE, 7 ladders x 2 panels x 3 choosers, OOS read ONCE: **42 picks — 4b full 10, 4b OOS 10, 4a 0; whole grid, 146 rungs — 4b full 33, 4b OOS 36, 4a 0.** Best pick clearing 4b full AND OOS: U56 L_MV / max_vol 9.99, full 16.30% / 1.1627 / -20.16% (halves 1.2718 / 1.0935), OOS 17.79% / 1.1689 / -20.16%, 2.79x/yr. Benchmarks: **U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; B136 SPY 15.16% / 0.8861 / -33.72% (halves 0.9596/0.8259), OOS 15.33% / 0.8767 / -33.72%; live RULES v2 U56 8.62% / 1.2007 / -12.05% (OOS 9.45% / 1.2762), B136 7.98% / 1.0993 / -12.24% (OOS 7.88% / 1.1059).** **NOTHING PROPOSED, structurally rather than by judgement: G10 shows this grid IS 1100's committed 146-rung grid to CSV round-trip precision, so no book exists here that 1100 did not already publish and decline to propose** — this run re-scores a RULER, not a book. 4a is empty at every rung, as it has been since 1096.

  **WHAT THE RECORD SHOULD DO WITH IT, STATED NARROWLY (proposed, NOT enacted - rule 6).** 1100's drafted PROTOCOL 8b already says a pick must quote "the ladder's OWN measured resolution at the declared confidence - never a floor transferred from another construction". This run PRICES that sentence: enacting it **withdraws the RESOLVED certification from 116 of 116 committed transfers and grants it to none**, so 8b is not a documentation tidy-up but a retraction of every resolution claim the record's pick layer currently makes. The narrow and cheap half is H_UNDECLARED - 0.6198 of transfers state no confidence, and a confidence is one column.

  **SURVIVORSHIP (rule 9):** U56 and B136 are current-constituent panels, so every LEVEL is optimistic; margins, half-widths and flip counts contrast two rungs over one inflated tape and the bias very largely cancels out of them, but it does NOT cancel out of the 4b legs measured against SPY, so every 4b pass counted here is an UPPER bound.

- 2026-09-16 (lane C, idea 1099 does-the-QUARTERLY-CADENCE-CELL-become-RULE-8-REACHABLE-under-any-HONEST-CHOOSER) — **ANSWERED = NO AT EVERY HONEST RUNG, BUT THE "NO" IS DECIDED BY 0.0002 OF IS SHARPE, 73x BELOW 877's OWN COMMITTED SEED FLOOR. KILL of "reachable / unreachable" as a publishable binary on this ladder; the U56 Q cell is PARKED, not proposed. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched.** SELECTION: lane C takes the SECOND open idea; 1099 was the second line under '## Open' and is not EDGAR / Form 4 / 8-K / options / live-data. It has a price leg — 18 books over 2 panels x 9 cadence rungs, 32 rule-8 picks — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `CHOOSER` {C_ISSHARPE, C_ISDD, C_ISCAGR, C_ISSHARPE_TP} x `CADENCE SET` {C4, C9} = **8 points, all published, on BOTH panels**. C4 = D/W/M/Q, every cadence `engine.rebalance_mask` serves; C9 = 1118's nine, built as k-th-bar subsets of engine's own masks without touching engine.py. The TP chooser carries a **declared penalty ladder LAM = {5, 10, 25, 50, 100} bps, every rung published**, headline LAM = 10 = PROTOCOL rule 2's own cost and the only non-arbitrary value on it; **nothing is selected on LAM** — D1's LAM* measures how far outside honesty one would have to go, it is not a fit. PANEL is not a dial; COST is not a dial (10 bps everywhere, the 0/25/50 rungs appear once in D3 to re-price the queue's own sentence). TARGET_Q = {Q} and TARGET_SLOW = {Q, 2Q, 4Q} were both fixed before the run because 1117/1133's walk-forward found the 4b passer here is 2Q. Frozen at 1082/1094/1096/1110/1117/1118's construction: CAND20 legs, cap INF, max_vol 0.60, gross 0.75, min hold 126, N=20, 10 bps, LAG 1, warm-up 260, IS end 2016-12-31.

  **GATES 11 of 12 PASS, printed before any result number, AND THE ONE FAIL IS REPORTED AS A FAIL RATHER THAN LOOSENED.** G1 fast runner == `engine.backtest` 1.39e-17; G2 committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS 1.70e-04; G4 live RULES v2 MaxDD 4.95e-05; **G5 `cadence_mask` == `engine.rebalance_mask` on all four cadences engine serves, 0 differing bars**, G5b every extended rung a k-th-bar subset, 0; **G6 CROSS-RUN 1117/1133's committed U56 CADENCE 2Q reproduces on all EIGHT numbers — full CAGR/Sharpe/DD, both halves, OOS CAGR/Sharpe/DD — at 4.96e-05**; **G7 its committed U56 W-book OOS triple at 3.15e-05**; G8 determinism 0.00e+00; G9 C9 nests C4, 0 rungs lost; G10 cadence ladder live (Sharpe spread 0.1745). **G11 FAILS: the turnover axis was declared live at max/min >= 5x and reads 3.560x (B136 1.22 -> 4.36x/yr; U56 4.19x). My bar was one rung too tight, the axis is plainly live, and nothing in the reading depends on the threshold — the honest record is 11 of 12, not a re-cut 12 of 12.** HYPOTHESES 1 of 6 supported.

  **THE LITERAL ANSWER. 0 of the 16 HEADLINE picks (4 choosers x 2 cadence sets x 2 panels, TP at LAM = 10) land on Q, and 0 land anywhere in {Q, 2Q, 4Q}; six land on the frozen default W.** Over all 32 picks Q is reached 4 times, all on U56 and all at LAM >= 25 bps. Pick census: **W=11, 2M=7, 2W=6, D=4, Q=4** (C4 {W 11, Q 3, D 2}; C9 {2M 7, 2W 6, D 2, Q 1}). **LAM*, the smallest penalty reaching Q on a 0..500 bps 1-bp ladder: U56/C4 11 bps, U56/C9 76 bps, B136/C4 107 bps, B136/C9 never within 500 (LAM*(SLOW) 118).** H_REACH REFUTED on the letter, H_TP_ONLY vacuous, H_LAMSTAR PARTLY (2 of 4 <= 100 bps).

  **THE FINDING THAT MATTERS, AND IT KILLS THE QUESTION'S OWN BINARY. On U56/C4 the IS-Sharpe-net-of-10-bps chooser prefers W over Q by +0.0002.** That is the entire margin on which the queue's "no IS-only chooser selects it, so it is not reachable at the cost the protocol fixes" rests. It is **73x smaller than 877's committed 0.0145 seed floor** — the floor 1100 found already decides 8 of 14 committed ladder picks — and far inside 1103's measured U56 S_FULL resolution of 0.176 over a spread of 0.183. **LAM*(Q) = 11 bps is ONE basis point above the cost PROTOCOL rule 2 fixes.** So the honest statement is not "the quarterly cell is unreachable at 10 bps" but **"W and Q are indistinguishable to this chooser at 10 bps, and which one it names is not a resolvable quantity"** — and the record should stop publishing reachability as a verdict without its own margin beside it.

  **A CORRECTION TO THE QUEUE'S OWN PREMISE (H_PASS10 PARTLY). The queue states the Q cell "clears 4b full AND OOS at 25 and 50 bps", implying not at 10. On U56 it clears 4b FULL and 4b OOS at ALL FOUR rungs — 0/10/25/50 bps, Sharpe 1.1511 / 1.1388 / 1.1203 / 1.0891.** The cell is a 4b passer at the cost the protocol fixes; unreachability, not passing, was ever the whole of its problem. **On B136 it fails 4b at all four rungs and its turnover is 1.97x/yr, not the 1.65x the queue quotes — 1.65x is the U56 number, so the premise sentence is a U56 fact carrying no panel stamp.**

  **REACHABILITY IS A PROPERTY OF THE RUNG SET, NOT OF THE RUNG — 1117/1118's statement seen from the chooser's side. LAM*(Q) moves 11 -> 76 bps, a factor of 6.9, between C4 and C9 on the same panel and the same chooser**, and 107 bps -> never on B136, because 2W and 2M intercept the penalty path before Q does (U56/C9 picks 2W at LAM 0..75). Adding rungs makes the target HARDER to reach. Any committed reachability claim should from here carry the rung set it was measured on. H_MONO_TP SUPPORTED 4 of 4 — the pick's turnover never rises with LAM — which is what licenses reading LAM* at all.

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed as capital. Whole grid, 18 books: 4b full 3, 4b OOS 3, 4a 0** — U56 W 15.58% / 1.1397 / -19.13% (halves 1.2037/1.0971, OOS 16.97% / 1.1643 / -19.13%, 2.90x/yr), **U56 Q 15.40% / 1.1388 / -19.94% (halves 1.2214/1.0911, OOS 17.28% / 1.1726 / -19.94%, 1.65x/yr)**, U56 2Q 13.26% / 1.0019 / -17.90% (OOS 15.47% / 1.0660, 1.31x/yr). **32 rule-8 picks: 4b full 8, 4b OOS 8, 4a 0; median OOS Sharpe 1.0967, median regret vs the frozen W rung +0.0000; 3 of 16 headline picks beat W's OOS Sharpe (H_WORTH REFUTED).** The Q cell does beat W out of sample, **+0.0083 of OOS Sharpe at 57% of its turnover** — real in sign, an order of magnitude below the seed floor, and worth ~1.25 bps/yr of saved cost. Benchmarks: **U56 SPY 15.10% / 0.8829 / -33.72% full (halves 0.9588/0.8207) and 15.21% / 0.8711 / -33.72% OOS; B136 SPY 15.16% / 0.8861 / -33.72% and 15.33% / 0.8767 / -33.72%; live RULES v2 U56 8.62% / 1.2007 / -12.05% and 9.45% / 1.2762 / -12.05%, B136 7.98% / 1.0993 / -12.24% and 7.88% / 1.1059 / -12.24%.** `baseline.compare()` at freq='Q' returns KILL on path 4a (1.139 vs the live book's 1.201). **The U56 Q cell PASSES 4b full and OOS but is NOT rule-8 reachable at any honest penalty rung, so rule 8 makes it PARK, not KEEP; a 10-line memo is committed with exact RULES wording and its blocker. NOTHING PROPOSED AS CAPITAL.** SURVIVORSHIP (rule 9): U56 and B136 are current-constituent lists, so every 4b figure is an upper bound; the reachability quantities are within-panel contrasts over one tape and the bias very largely cancels out of them.
- 2026-09-16 (cloud lane, idea 2 of 2, idea 1133 should-the-PROTOCOL-require-a-TIE-SET-and-RUNG-COUNT-beside-every-published-ARGMAX-instead) — **ANSWERED = YES, BUT ONLY IN TWO OF ITS THREE FORMS, AND THE MIDDLE FORM INHERITS THE EXACT RUNG-DEPENDENCE THAT KILLED 1117's BAN. The clause is DRAFTED below and PRICED; it is STATED for the Sunday review and NOT ENACTED. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the LAST eligible open idea; 1133 ended '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. It has a price leg — 74 books over 4 ladders x 2 panels x 2 rung sets — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE CLAUSE AS DRAFTED (stated, not enacted):** *PROPOSED PROTOCOL 4c — EVERY PUBLISHED ARGMAX CARRIES ITS OWN RESOLUTION. A result that names the best rung of a ladder shall quote, beside it and in the same sentence: (i) the GAP from the peak to the runner-up, in the statistic's own units; (ii) the ladder's RESOLUTION FLOOR at the declared confidence q; (iii) the ladder's RUNG COUNT and its end rungs; and (iv) the TIE SET at that q — every rung the bootstrap cannot separate from the peak. Where the tie set holds more than one rung, the result is published AS THE TIE SET and not as a rung. Where the tie set is the whole ladder, no argmax is published at all.*

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `CLAIM SET` {CS_ALL32, CS_DECIDED, CS_HCAD} x `BAR` {B_QUOTE, B_TIESET, B_DECIDE} = **9 points, all published, EACH at BOTH rung sets**. B_QUOTE quotes all four and deletes nothing; B_TIESET adds the whole-ladder delete; B_DECIDE publishes only a decided argmax as a rung. **RUNG SET {CORE, EXT} is NOT a dial and nothing is selected on it** — 1117 died on rung dependence, so both levels are reported everywhere and the movement between them IS the result. PANEL, LADDER and STATISTIC are not dials (all 2 x 4 x 4 = 32 cells reported everywhere); q = 0.90 throughout. CORE and EXT are 1110/1116's and 1118's rungs verbatim (N 9->9, H 4->9, GROSS 10->10, CADENCE 4->9), frozen otherwise at 1082/1094/1098/1102/1108/1110/1116/1117/1118's construction: CAND20 legs, cap INF, max_vol 0.60, gross 0.75, min hold 126, N=20, W, 10 bps, LAG 1, warm-up 260, IS end 2016-12-31, block L=63, 1000 draws, crc32 seeds.

  **GATES 11 of 11 PASS, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS 1.70e-04; G4/G4b committed U56 n=12 / B136 n=15 triples 4.98e-05 / 2.13e-05; G5 live RULES v2 MaxDD 4.95e-05; G6 determinism 0.00e+00; **G7 `cadence_mask` == `engine.rebalance_mask` on all four cadences engine serves, 0 differing bars**; G7b every extended cadence rung is a k-th-bar subset of an engine schedule, 0 — built without touching engine.py; **G8 EXT NESTS CORE on every ladder, 0 rungs lost**; G9 the ladders are live (Sharpe spread 2.36e-01). **HYPOTHESES 3 of 5.**

  **THE LITERAL ANSWER TO THE IDEA'S QUESTION — HOW MANY IT RESTATES RATHER THAN DELETES. At B_QUOTE the clause RESTATES 32 of 32 and DELETES 0, at BOTH rung sets** (6 as-is, 26 carrying their newly quoted gap / floor / rung count / tie set). **At B_DECIDE it RESTATES 6 and DELETES 26, at BOTH rung sets.** At B_TIESET it deletes 18 of 32 at CORE and 9 of 32 at EXT. **So the clause is NOT a gentler instrument than 1117's ban: in its strict form it is strictly HARSHER — 26 deleted against the ban's 12 at CORE, and 26 against the ban's ZERO at EXT.** On CS_HCAD, the 16 cells the ban aimed at, B_DECIDE deletes 16 of 16 at both sets and B_TIESET 15 -> 6. CS_DECIDED (the record's own six) is untouched by every bar at every rung set: 6 restated, 0 deleted, 6 of 6 times.

  **THE FINDING THAT MATTERS, AND IT IS A CORRECTION TO 1117's OWN RECOMMENDATION. H_STABLE REFUTED: the DELETE-count swing CORE -> EXT is 0 at B_QUOTE and 0 at B_DECIDE on EVERY claim set — perfectly rung-stable — but 9 of 32 at B_TIESET** (CS_ALL32 18 -> 9, CS_HCAD 15 -> 6), against the 12-of-32 swing (12 -> 0) that killed the ban, **and in the SAME direction: adding rungs makes it delete FEWER claims.** 1117 proposed this clause on the ground that "nothing in it depends on a whole-ladder flag". **B_TIESET's delete trigger — *the tie set spans the whole ladder* — IS a whole-ladder flag**, and it moves almost as much as the ban's did. **THE GENERAL STATEMENT, STRENGTHENED FROM 1117's: a clause is rung-stable iff every quantity in its TRIGGER is a property of the peak and its own pair, never of the ladder. QUOTING a whole-ladder quantity (the floor, the rung count) is free; TRIGGERING on one is not.**

  **THE MECHANISM, AND THE CENSUS THAT REPRODUCES. H_WIDENS SUPPORTED and it explains the swing: median tie-set size CORE -> EXT runs N 8.0 -> 8.0, H 4.0 -> 8.0, GROSS 1.0 -> 1.0, CADENCE 4.0 -> 7.0** — adding rungs widens tie sets in absolute rungs but SHRINKS them as a share of the ladder, so the whole-ladder trigger fires less often. That is 1118's mechanism seen from the clause's side. **H_NO_ZERO SUPPORTED: unlike the ban, the clause is never vacuous** (CS_ALL32 deletes 18 / 9 at B_TIESET and 26 / 26 at B_DECIDE). **H_DECIDED_SURVIVE SUPPORTED 6 of 6, and this run independently reproduces 1102/1109's committed census: the record DECIDES 6 of 32 at CORE, ALL SIX on the GROSS dial** (U56 CAGR and DD; B136 all four statistics), and all six stay decided at EXT — near-trivially, since GROSS's rung count does not change. **H_RESTATES REFUTED at 12 of 18 rows**: the clause restates more than it deletes at B_QUOTE everywhere and at B_TIESET/EXT, but not at B_DECIDE (26 > 6 at both sets), nor at B_TIESET/CORE (18 > 14), nor on CS_HCAD at B_TIESET/CORE (15 > 1).

  **WHAT SHOULD BE PUT TO THE SUNDAY REVIEW (stated, not enacted — rule 6): ADOPT 4c AT B_QUOTE, AND AT B_DECIDE FOR THE WORD "BEST" ONLY. DO NOT ADOPT B_TIESET.** B_QUOTE and B_DECIDE are the two forms whose trigger reads only the peak's own tie set, and both are exactly rung-stable at swing 0 of 32 on every claim set. B_TIESET's extra line costs 9 of 32 claims of rung-dependence to keep and buys nothing that B_DECIDE does not already say.

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed, and the arm independently reproduces 1117's. 48 IS picks (rung chosen per ladder on IS 2009-2016 alone, three choosers, OOS 2017-2026 read once): 4b full 6, 4b OOS 6, 4a 0, median OOS Sharpe 1.0244, median regret +0.0515. Whole grid, 74 books: 4b full 17, 4b OOS 18, 4a 0.** Those figures reproduce 1117's committed walk-forward arm number for number from an independently written script — a cross-run confirmation, not a new result. **Of the 74 books exactly ONE outside 1110's CORE clears 4b: U56 CADENCE 2Q, full 13.26% / 1.0019 / -17.90% (halves 1.0667/0.9668), OOS 15.47% / 1.0660 / -17.90% — below the incumbent W/H126/N=20 book's own OOS Sharpe of 1.1643, so it buys nothing.** Benchmarks: **U56 SPY 15.10% / 0.8829 / -33.72% full and 15.21% / 0.8711 / -33.72% OOS; B136 SPY 15.16% / 0.8861 / -33.72% and 15.33% / 0.8767 / -33.72%; live RULES v2 U56 8.62% / 1.2007 / -12.05% and 9.45% / 1.2762 / -12.05%, B136 7.98% / 1.0993 / -12.24% and 7.88% / 1.1059 / -12.24%.** **NOTHING PROPOSED AS CAPITAL.** SURVIVORSHIP (rule 9): U56 and B136 are current-constituent lists, so every 4b figure is an upper bound; the gap, floor and tie-set quantities are within-panel contrasts over one tape and the bias very largely cancels out of them.
- 2026-09-16 (cloud lane, idea 1 of 2, idea 1103 is-the-RESOLUTION-FLOOR-a-property-of-EDGE-or-of-every-LADDERED-STATISTIC) — **ANSWERED = OF EVERY LADDERED STATISTIC, AND THE IDEA'S OWN PREMISE RUNS BACKWARDS. KILL of the premise. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the FIRST eligible open idea; 1103 headed '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. It has a price leg — 18 books plus 720 DD-matched nulls over 2 panels — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `STATISTIC` {EDGE, EDGE_FIXNULL, CAGR, S_FULL, S_OOS, DD} x `BLOCK LENGTH` {21, 63, 126} = **18 points, all published, on BOTH panels (36 rows)**. PANEL is not a dial (1098's own two, reported everywhere); the LADDER is not a dial (1082/1098's nine rungs [5,8,10,12,15,20,25,30,40] verbatim, nothing added or dropped); q is not a dial (0.90 headline, 0.95 beside, never selected on). Frozen at 1082/1094/1098/1102/1104's construction: CAND20 legs, cap INF, max_vol 0.60, gross 0.75, W, min hold 126, 10 bps, LAG 1, warm-up 260, IS end 2016-12-31, S=40 nulls, 1000 draws, 34-step bisection, md5 null seeds and crc32 bootstrap seeds.

  **THE COMPARISON IS UNIT-FREE, AND THAT WAS DECLARED BEFORE ANY NUMBER.** A floor lives in its own statistic's units, so 3.25 pp of CAGR and 0.176 of Sharpe compare NOTHING. Every cross-statistic reading is one of exactly three unit-free quantities — **REL_FLOOR** (floor / ladder spread), **unresolved pairs of 36**, **W90** (rungs carrying >= 0.90 of the bootstrap argmax mass) — under a pre-registered "X beats Y iff X wins >= 2 of 3" rule.

  **GATES 16 of 16 PASS, printed before any result number, and three of them are cross-run reproductions of the object under test.** G1/G1b fast runner == `engine.backtest` == `gross_rescaler(1.0)` at 1.39e-17 on both panels; G2 committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS 1.70e-04; G4/G4b committed U56 n=12 / B136 n=15 triples 4.98e-05 / 2.13e-05; G5 live RULES v2 MaxDD 4.95e-05; G6 null-seed determinism 0.00e+00; **G7 reproduces ALL NINE of 1082/1098's committed U56 EDGE figures at 4.80e-03**; **G8 reproduces 1098's committed U56 EDGE floor@90 at 3.2528 against its published 3.25, its peak n=12 and its collapse, 7.78e-03**; G8b its committed B136 collapse 9.41e-05; G9 EDGE_FIXNULL is a variance-only control (identical point gaps, 0.00e+00); G10 every ladder live; G11 all three resamplers exact on the identity draw 7.91e-16. **HYPOTHESES 1 of 6.**

  **THE ANSWER, AND IT IS 6-0 AGAINST THE IDEA. H_NULL_COSTS REFUTED: CAGR — the same book statistic with the null removed — beats EDGE at 0 of 6 (panel, L) points and EDGE beats CAGR at 6 of 6.** At L=63, REL_FLOOR reads **EDGE 0.5077 (U56) / 0.4759 (B136)** against **CAGR 1.0000 / 1.0000**, S_FULL 0.9614 / 1.0000, S_OOS 0.9096 / 0.5707, DD 0.8451 / 0.7690; unresolved pairs EDGE 15 / 15 against CAGR 20 / 32 and S_FULL 33 / 35. **CAGR carries an INFINITE floor on both panels — the ladder's largest gap is itself un-resolved — and EDGE never does.** The floor is therefore a property of the STATISTIC, spanning a factor of more than two in REL_FLOOR over the SAME nine books, and the statistic with a null attached is the one that resolves.

  **THE MECHANISM, MEASURED RATHER THAN ASSERTED. The null median is a SIGNAL AMPLIFIER, and its own sampling error — the term the idea named — is real but small.** Per rung pair, Var(dEDGE) = Var(dBOOK) + Var(dNULLMED) - 2Cov: the null's variance share runs **+0.0615 / +0.0681 / +0.0724 on U56 and +0.1220 / +0.1200 / +0.1283 on B136** at L = 21 / 63 / 126. Its cost in noise is **+3.6% / +4.2%** in median sd(pair gap) (1.6486 vs 1.5910 pp; 1.6322 vs 1.5659). Its gain in signal is **1.47x / 1.80x** in ladder spread (4.370 -> 6.407 pp on U56, 3.594 -> 6.471 pp on B136), because the DD-matched null's OWN CAGR runs the other way across the ladder (U56 11.88 -> 10.88 at n=12 -> 13.04 pp at n=40; B136 13.19 -> 9.77 -> 13.46) while the book's falls monotonically. **Net signal-to-noise 1.42x on U56 and 1.73x on B136 in EDGE's favour.** H_FIXNULL_MID is the one SUPPORTED hypothesis (6 of 6) but is second-order and in one place runs backwards: holding the null median at its point value leaves REL_FLOOR **identical at 5 of 6** points and makes it **worse** at U56 L=126 (0.5077 -> 0.7273). Its single real gain is at the peak on B136, where EDGE_FIXNULL is **the only cell in the whole run that DECIDES** the nine-rung ladder (tie set {10} at all three block lengths).

  **THREE FURTHER REFUTATIONS THE RECORD SHOULD CARRY. H_DD_WORST REFUTED — CAGR is the worst-resolving of the six (median REL_FLOOR 1.0000), not the path functional (DD 0.7690). H_OOS_WORSE REFUTED 0 of 6 — S_OOS resolves BETTER than S_FULL at 5 of 6 points on ~40% fewer days**, because the OOS ladder spread (0.290 / 0.301) is more than double the full-sample spread (0.183 / 0.121): **resolution is spread-over-noise, not sample length, and the record should stop treating a shorter window as automatically less resolvable. H_L_RISES REFUTED 2 of 12** — block length barely moves it (EDGE's U56 REL_FLOOR is 0.5077 at L = 21, 63 and 126 alike), so the L in {21, 63, 126} sensitivity the record prints beside every floor is buying less than it costs. **AND A PRACTICE NOTE: 1102/1110's habit of quoting a floor WITHOUT its statistic's spread is unreadable** — a floor of 3.25 pp and one of 0.176 Sharpe are the same object only after division by spread, and this run's whole comparison had to be rebuilt on that quotient.

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed as capital, and no chooser reaches any 4b rung.** Rung chosen on IS 2009-2016 ALONE under four choosers (C_ISSHARPE, C_ISDD, C_ISCAGR, C_ISEDGE), OOS 2017-2026 read once. **8 IS picks: 4b full 0, 4b OOS 0, 4a 0, median OOS Sharpe 1.0244 -> 0.9003, median regret +0.2866.** Whole grid, 18 rungs: 4b full 3, 4b OOS 4, 4a 0 — the three full-sample passes are **U56 n=12 (full 17.71% / 1.1692 / -20.17%, halves 1.2753/1.0884, OOS 18.89% / 1.1758 / -20.17%), U56 n=20 (15.58% / 1.1397 / -19.13%, OOS 16.97% / 1.1643 / -19.13%) and B136 n=15 (16.78% / 1.0682 / -19.66%, OOS 17.49% / 1.0458 / -19.66%)**, and every chooser lands instead on n=5, n=8, n=30 or n=40. Benchmarks: **U56 SPY 15.10% / 0.8829 / -33.72% full and 15.21% / 0.8711 / -33.72% OOS; B136 SPY 15.16% / 0.8861 / -33.72% and 15.33% / 0.8767 / -33.72%; live RULES v2 U56 8.62% / 1.2007 / -12.05% and 9.45% / 1.2762 / -12.05%, B136 7.98% / 1.0993 / -12.24% and 7.88% / 1.1059 / -12.24%.** **NOTHING PROPOSED AS CAPITAL.** SURVIVORSHIP (rule 9): U56 and B136 are current-constituent lists, so every 4b figure is an upper bound; the floor, spread and variance-share quantities are within-panel contrasts over one tape and the bias very largely cancels out of them.
- 2026-09-16 (lane B, idea 1117 should-the-PROTOCOL-forbid-publishing-an-ARGMAX-on-an-H-or-CADENCE-LADDER-at-all) — **ANSWERED = NO. KILL of the clause as written, on three independent grounds, the first of which is fatal. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched.** SELECTION: lane B takes the LAST open idea; 1117 ended '## Open' and is not EDGAR / Form 4 / 8-K / options / live-data. It has a price leg — 74 books over 4 ladders x 2 panels x 2 rung sets — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `CLAIM SET` {CS_HCAD, CS_ALL32, CS_DECIDED} x `BAR` {B_ALLSTAT, B_MAJ3, B_ANY} = **9 combinations, all published**, at 3 seed bases. PANEL, LADDER and STATISTIC are not dials. **RUNG SET {CORE, EXT} is not a dial either and nothing is selected on it** — 1118 established P(INF_FLOOR) FALLS as rungs are added, so the clause's own trigger is rung-count dependent and both levels are reported at every point. Frozen at 1082/1094/1098/1102/1108/1110/1116/1118's construction: CAND20 legs, cap INF, max_vol 0.60, gross 0.75, min hold 126, N=20, W, 10 bps, LAG 1, warm-up 260, IS end 2016-12-31, block L=63, 1000 draws, crc32 seeds, q=0.90.

  **GATES 11 of 11 PASS, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS 1.70e-04; G4/G4b committed U56 n=12 and B136 n=15 triples 4.97e-05 / 2.13e-05; G5 live RULES v2 MaxDD 4.95e-05; G6 determinism 0.00e+00; G7 `cadence_mask` == `engine.rebalance_mask` on D/W/M/Q, 0 differing bars; **G8 reproduces all 54 rows x 5 columns of 1110's committed CORE grid at 0.00e+00**; **G9 reproduces all 32 of 1110's committed argmax PEAKS with 0 mismatches** (its INF/finite FLAG differs at 2 of 32 — reported, not gated, because the flag is a redraw quantity). **HYPOTHESES 2 of 6.**

  **A CORRECTION TO THE IDEA'S OWN PREMISE, MADE BEFORE ANY OTHER NUMBER. The queue quotes 1116 as 'an infinite floor at ALL FOUR statistics on BOTH panels (8 of 8 cells)'; 1110's committed census reads 15 of 16.** B136 CADENCE CAGR carries a FINITE floor (1.8463) with a 3-rung tie set, and this run reproduces the gap independently (B136 CADENCE infinite at 3 of 4 statistics at the headline base, 3 / 4 / 4 over three bases). **So the clause as written does not bar B136 CADENCE — one of the four blocks it exists for. H_PREMISE and H_BARS_HCAD both REFUTED.**

  **THE FATAL DEFECT: THE TRIGGER VANISHES WHEN RUNGS ARE ADDED (H_STABLE REFUTED).** Per-block INFINITE-floor count CORE -> EXT: U56 H **4 -> 3**, U56 CADENCE **4 -> 1**, B136 H **4 -> 2**, B136 CADENCE **3 -> 1**; N and GROSS unchanged. Barred set at B_ALLSTAT is **{U56 H, U56 CADENCE, B136 H} on 1110/1116's CORE rungs and the EMPTY SET on 1118's 9-rung EXT rungs**, so the census reads **CS_ALL32 12 -> 0 of 32, CS_HCAD 12 -> 0 of 16**, B_MAJ3 16 -> 4 (median over 3 bases 16 -> 0); only B_ANY is rung-stable (24 / 24). **An author who wants to publish an H-ladder argmax need only run 9 rungs instead of 4 and the ban never fires.** That is 1118's own mechanism — an infinite floor means the ladder's LARGEST gap is un-resolved, and extra rungs add larger, more separable gaps — turned on the clause's trigger. **The general statement: any clause keyed on a whole-ladder floor must FIX the rung set, or it is advisory only.**

  **AND THE CLAUSE AIMS AT THE WRONG TARGET (H_DISCRIMINATES REFUTED, H_WORTHLESS SUPPORTED).** Rung chosen on IS 2009-2016 alone, three choosers, OOS 2017-2026 read once, scored against that ladder's FROZEN DEFAULT rung (N=20, H=126, gross=0.75, W) on the chooser's own matched statistic. **BARRED blocks: median advantage +0.0000, 3 of 9 wins. UNBARRED: +0.0000, 10 of 39.** The zeroes are not all the same zero and the split is reported: **12 of 48 picks RE-PICK the default rung** (0 by construction), and on the **36 that MOVE off it the median matched advantage is -0.0195 and the median OOS-Sharpe advantage -0.0502, 13 of 36 wins** — negative on barred and unbarred blocks alike (B_ALLSTAT CORE barred -0.0111 at 3/7, unbarred -0.0361 at 3/9). **Choosing a rung by argmax is not merely uninformative on this grid, it is NEGATIVELY informative, on EVERY ladder — N and GROSS included — which a ban keyed on ladder identity cannot express.** H_ISOOS_RANK is SUPPORTED but thin and should not be leaned on: mean |Spearman(IS order, OOS order)| 0.5556 barred vs 0.5868 unbarred, with the unbarred mean carried entirely by GROSS's |rho| = 1.0000. **CROSS-READ: idea 1107 (cloud, same day) kills the same claims by a different route — max gap-to-floor ratio exactly 1.000 over 26 ladders, 18 of 26 whole-ladder. The two agree the claims are unsupported; this run adds that the clause the queue proposed is not the instrument that stops them.**

  **WHAT SHOULD BE DRAFTED INSTEAD (stated, not enacted — rule 6):** a bar keyed on the CLAIM, not the DIAL — every published argmax quotes its gap, its ladder's resolution floor AND rung count, and its tie set at the declared q. On this grid that clause bars 12 of 32 at CORE rungs and 4 of 32 at EXT rungs at B_MAJ3 while staying well-defined at any rung count, because nothing in it depends on a whole-ladder flag. Filed as idea 1133.

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed. 48 IS picks: 4b full 6, 4b OOS 6, 4a 0, median OOS Sharpe 1.0244, median regret +0.0228 — and ALL SIX passes are the frozen DEFAULT rung** (U56 GROSS 0.75 and U56 CADENCE W are the same incumbent W/H126/N=20 book: full 15.58% / 1.1397 / -19.13%, H1 1.2037 / H2 1.0971, OOS 16.97% / 1.1643 / -19.13%). Whole grid, 74 rungs: **4b full 17, 4b OOS 18, 4a 0**; of the 20 rungs outside 1110's CORE only **one** clears 4b (U56 CADENCE 2Q, OOS 15.47% / 1.0660 / -17.90% — below the incumbent's own OOS Sharpe, so it buys nothing). Benchmarks: **U56 SPY 15.10% / 0.8829 / -33.72% full and 15.21% / 0.8711 / -33.72% OOS; B136 SPY 15.16% / 0.8861 / -33.72% full and 15.33% / 0.8767 / -33.72% OOS; live RULES v2 U56 8.62% / 1.2007 / -12.05% full and 9.45% / 1.2762 / -12.05% OOS, B136 7.98% / 1.0993 / -12.24% full and 7.88% / 1.1059 / -12.24% OOS.** **NOTHING PROPOSED AS CAPITAL.** SURVIVORSHIP (rule 9): U56 and B136 are current-constituent lists, so every 4b figure is an upper bound; the census and trigger quantities are within-panel contrasts over one tape and the bias very largely cancels out of them.
- 2026-09-16 (cloud lane, idea 1118 is-the-H-and-CADENCE-un-resolvability-a-RUNG-COUNT-artefact-after-all) — **ANSWERED = PARTLY, AND THE RUNG-COUNT EFFECT RUNS BACKWARDS FROM THE IDEA'S PREMISE. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the LAST eligible open idea; 1118 was the last line under '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. It has a price leg — 82 books over 4 ladders x 2 panels — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `H GRID` {H4, H9, H9_LOG} x `CADENCE SET` {C4, C9} = **6 combinations, all published**, at 8 seed bases, with all 4 x 4 = 16 cells reported per panel per point. Every H grid NESTS 1116's H4 and C9 nests C4, so rung count is the only thing that changes. The subset enumeration is EXHAUSTIVE (every subset of size m = 3..k of every ladder), so nothing is selected there either. Frozen at 1082/1094/1098/1102/1108/1110/1116's construction: CAND20 legs, cap INF, max_vol 0.60, gross 0.75, min hold 126, N=20, W, 10 bps, LAG 1, warm-up 260, IS end 2016-12-31, block L=63, 1000 draws, crc32 seeds, q=0.90.

  **A FINDING ABOUT THE IDEA'S OWN INSTRUCTION, RECORDED BEFORE ANY NUMBER. `engine.rebalance_mask` serves exactly FOUR cadences (D, W, M, Q), so 1116's CADENCE ladder is ALREADY 'as many cadences as engine.rebalance_mask serves'** — the queue asked for a ladder that already exists. C9 (D, 2D, W, 2W, M, 2M, Q, 2Q, 4Q) is therefore built ON TOP of engine's own masks — every k-th bar of a base schedule — and gated bar-for-bar against them rather than by editing engine.py.

  **GATES 11 of 11 PASS, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS 1.70e-04; G4/G4b committed U56 n=12 and B136 n=15 triples 4.97e-05 / 2.13e-05; G5 live RULES v2 MaxDD 4.95e-05; G6 determinism 0.00e+00; **G7 `cadence_mask` == `engine.rebalance_mask` on all four cadences engine serves, 0 differing bars**; **G8 reproduces all 54 rows x 5 columns of 1110's committed CORE grid at 0.00e+00**; **G9 is the one that matters for the reading — this run's per-ladder INF_FLOOR counts at 1116's OWN rungs land inside 1119's committed 8-base redraw ranges at 64 of 64 (panel x ladder x base), so every count movement reported below is the RUNG SET and not the draw**; G10 every ladder x statistic live. **HYPOTHESES 1 of 6.**

  **THE ANSWER, AND THE COUNT EFFECT IS REAL BUT POINTS THE OTHER WAY. P(INF_FLOOR) FALLS as rungs are added, at every ladder** — averaged over 2 panels x 4 statistics and over EVERY subset of size m: N **0.7009 -> 0.6240 -> 0.3750**, H **0.8750 -> 0.7976 -> 0.5000**, CADENCE **0.6667 -> 0.5417 -> 0.2500**, GROSS **0.1271 -> 0.1256 -> 0.1250** at m = 3 / 4 / 9. **H_MONO_COUNT was declared in the RISING direction and is REFUTED in the falling one.** The mechanism is structural, not empirical: an infinite floor means the ladder's LARGEST gap is itself un-resolved, and extra rungs add larger, more separable gaps. **So 1116's two un-resolvable ladders were its two most likely to be un-resolvable for their rung count alone, and the general caution is that an INF_FLOOR count is NOT comparable across ladders of different rung counts — which is precisely the comparison 1116 published.** H_COUNT is nonetheless REFUTED as stated: at matched m=4 the four ladders run 0.6240 / 0.7976 / 0.1256 / 0.5417, a spread of 0.6720, so count alone explains nothing. **H_IDENTITY SUPPORTED: at every matched m from 4 to 9, H+CADENCE sits above N+GROSS — 0.670 / 0.587 / 0.519 / 0.464 / 0.417 / 0.375 against 0.375 / 0.340 / 0.310 / 0.285 / 0.264 / 0.250.** Both things are true: the dial matters, and 1116's contrast was inflated by the confound it flagged.

  **THE CORRECTION THAT MATTERS TO THE RECORD: 1116/1119's HEADLINE AXIS RESULT DOES NOT SURVIVE MATCHED RUNG COUNT.** At 1116's own rungs it reproduces — **U56 ETA2_LAD 0.4286 vs ETA2_STAT 0.0476, B136 0.7333 vs 0.0667** — but at H9/C9 it **collapses to a TIE on BOTH panels (U56 0.0545 vs 0.0545, B136 0.2000 vs 0.2000)**, and under the log-spaced H grid the STATISTIC axis WINS on U56 (0.0545 vs 0.2000). **ETA2_LAD > ETA2_STAT at only 8 of 12 dial point x panel cells: H_AXIS_SURVIVES REFUTED.** The per-ladder rows move with it — U56 N/H/GROSS/CADENCE **1/3/1/4 -> 1/2/1/1**, B136 **2/4/0/4 -> 2/2/0/1**, totals 9 -> 5 and 10 -> 5 — so **H_ROWS_CONSTANT is REFUTED and the idea's literal question is answered NO**. 1119's 24-of-24 ladder-axis result and 1116's 'un-resolvability is a property of the DIAL' should from here carry the rung counts they were measured at.

  **H_DISTANCE REFUTED, with its own limitation stated.** 1108's alternative — a ladder's spread over its median pair gap — correlates with the INF count at |rho| **0.1304** against rung count's **0.6211** over the 8 (panel x ladder) 9-rung rows. DECLARED LIMITATION: across those 8 rows k takes only the values 9 and 10, so that second rho is close to a GROSS indicator; the exhaustive subset curve, not this correlation, is this run's evidence on count.

  **RULE 8 AND BOTH KEEP PATHS — the strongest book this run found clears 4b on both windows, is IS-reachable, and is STILL not proposed.** Rung chosen on IS 2009-2016 alone, OOS 2017-2026 read once, three choosers, all 6 dial points. **144 IS picks: 4b full 24, 4b OOS 24, 4a 0, median OOS Sharpe 1.0696, median regret +0.0508**; by H grid H4 6/48, H9 6/48, H9_LOG 12/48; by cadence C4 15/72, C9 9/72 — **adding cadence rungs makes the chooser worse**. Whole grid, 82 rungs: 4b full 19, 4b OOS 20, **4a 0**, and only **3 of the 28 rungs 1116 never tested** clear 4b (U56 H=10, H=356, CADENCE 2Q). **U56 min hold 356 is picked by C_ISSHARPE and C_ISCAGR on IS alone and runs full 17.24% / 1.2466 / -19.62% (H1 1.5102 / H2 1.1109) and OOS 18.59% / 1.1907 / -19.62% against U56 SPY 15.10% / 0.8829 / -33.72% full and 15.21% / 0.8711 / -33.72% OOS, and live RULES v2 8.62% / 1.2007 / -12.05% full and 9.45% / 1.2762 / -12.05% OOS — a 4b pass on both windows at regret +0.0024. It is NOT proposed, for three reasons stated together: (i) the SAME chooser picks 356 on B136, where it FAILS 4b (OOS 18.10% / 1.1219 / -22.99%); (ii) its own ladder neighbours FAIL — U56 H=252 (OOS Sharpe 1.1931) and H=378 (1.0244) — so the pass is an isolated rung between two failures, not a plateau; (iii) the H ladder carries an infinite resolution floor at 2 of 4 statistics on both panels, so no rung on it is separable from its neighbours at q=0.90, which is this run's own subject.** PARK, not KEEP. SURVIVORSHIP (rule 9): U56 and B136 are current-constituent lists, so every 4b figure here is an upper bound; the resolvability quantities are within-panel contrasts over one tape and the bias very largely cancels out of them.
- 2026-09-16 (cloud lane, idea 1109 does-any-committed-GRID-EDGE-ARGMAX-survive-being-read-as-a-BOUNDARY-PICK) — **ANSWERED = YES, 6 of 16 survive; NONE of the 6 the record DECIDES. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the FIRST eligible open idea; 1109 headed '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. It has a price leg — 88 books over 4 ladders x 2 panels — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `CLAIM SET` {CS_ALL32, CS_RESOLVED, CS_DECIDED} x `EDGE RULE` {E_END, E_OUTER2} = **6 combinations, all published**, with all 32 (panel x ladder x statistic) cells reported under each. PANEL, LADDER and STATISTIC are not dials. The EXTENSION set is **not a dial**: fixed before any number was read (N +[3,4]/[50,60], H +[5,10]/[378,504], GROSS +[0.20,0.25]/[0.80,0.85,0.90,0.95,1.00], CADENCE +[2Q,4Q]) and never selected on. Everything else frozen at 1082/1094/1098/1102/1108/1110's construction: CAND20 legs, cap INF, max_vol 0.60, gross 0.75, W, min hold 126, N=20, 10 bps, LAG 1, warm-up 260, IS end 2016-12-31, block L=63, 1000 draws, crc32 seeds, q=0.90.

  **GATES 11 of 11 PASS, printed before any result number, and two of them are exact.** G1 fast runner == `engine.backtest` 1.39e-17; G2 committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS triple 1.70e-04; G4/G4b committed U56 n=12 / B136 n=15 triples 4.97e-05 / 2.13e-05; G5 live RULES v2 MaxDD 4.95e-05; G6 determinism 0.00e+00; **G7 this run's `cadence_mask` reproduces `engine.rebalance_mask` on D/W/M/Q with 0 differing bars — the gate that licenses the 2Q/4Q rungs engine.py does not serve, built without touching engine.py**; **G8 reproduces all 54 rows x 5 columns of 1110's committed CORE grid at 0.00e+00**; **G9 reproduces all 32 of 1102/1110's committed argmax peaks with 0 mismatches and their published gaps at 2.22e-16**; G10 every ladder x statistic live (min spread 1.79e-03). HYPOTHESES 3 of 6.

  **THE CENSUS. 16 of 32 committed argmaxes sit at a ladder END** — exactly half, so **H_EDGE_MAJORITY is REFUTED as stated** — and 28 of 32 sit within the outer two rungs. Against a random-argmax null (mean 2/k, 4/k) of 0.3556 / 0.7111 the excess is **+0.1444 / +0.1639** (H_EDGE_EXCESS SUPPORTED): the record's peaks do crowd the edges, but not overwhelmingly. **H_MONO SUPPORTED: 15 of 16 edge cells sit on a monotone ladder (|Spearman(rung, stat)| >= 0.70), i.e. carry no interior optimum to find.** H_SPREAD_TRIVIAL REFUTED at exactly 8 of 16.

  **THE ANSWER, AND IT IS THE DECIDED SIX THAT FAIL. The 6 cells the record actually DECIDES (tie set = {peak} at q=0.90) are 6 of 6 at a ladder end, all 6 on the GROSS dial with |rho| = 1.0000, and ALL 6 MOVE when that dial is extended**: 0.75 -> 1.00 at U56 GROSS CAGR and all four B136 GROSS statistics, 0.30 -> 0.20 at the two DD cells — and the new argmax is again the **new** grid edge, with a tie set of one. A dial whose argmax walks with the grid is publishing where the grid stops, not where the book is best. **Of the 16 edge argmaxes, 6 SURVIVE**: U56 H=252 at S_FULL / S_OOS / CAGR (beaten by neither 378 nor 504), U56 CADENCE Q (beaten by neither 2Q nor 4Q), B136 H=21 at S_FULL (beaten by neither 10 nor 5), B136 H=252 at S_OOS. So the H ladder's ends are real turning points and the GROSS ladder's are not.

  **A CORRECTION TO 1102's D1 WORDING, WITH BOTH COUNTS. 'All 6 of its RESOLVED argmaxes are ladder ENDS' is a statement about the DECIDED six, not the RESOLVED thirteen.** 13 of 32 cells have a FINITE floor; on those the edge share is **8 of 13 (0.6154)**, not 6 of 6. The record uses one word for two sets that differ by a factor of two, and **H_RESOLVED_EDGE is REFUTED on the resolved reading and reproduces exactly on the decided one**. Confirmed in passing: 1109's own premise-figure reproduces — B136 GROSS S_FULL's whole-ladder Sharpe spread is **0.0040**.

  **A CONTROL THAT CUTS AGAINST THE EDGE READING, AND IT IS REPORTED, NOT BURIED. 8 of 16 INTERIOR peaks ALSO move when the grid is widened** (B136 N 30 -> 50 and 60, both panels' H DD cells 126 -> 5, four CADENCE cells -> 2Q). 'The peak moved when I added rungs' is therefore NOT by itself evidence of a boundary artefact — a wider grid finds a better rung nearly as often in the interior. What distinguishes the decided six is not that they move but that **they move onto the new edge and are decided there too**.

  **RULE 8 AND BOTH KEEP PATHS — widening the grid past the record's own edges makes the IS chooser strictly WORSE out of sample, and nothing is proposed.** Rung chosen on IS 2009-2016 alone, per ladder, three choosers, OOS 2017-2026 read once. **CORE 24 picks: 4b full 4, 4b OOS 4, 4a 0, median OOS Sharpe 1.0398, median regret +0.0508. EXTENDED 24 picks: 4b full 3, 4b OOS 3, 4a 0, median OOS Sharpe 1.0122, median regret +0.0719 — and 15 of 24 picks land on a rung the record never tested.** Whole grid: CORE 54 rungs 4b full 16 / 4b OOS 17 / 4a 0; EXTENDED 88 rungs 20 / 22 / 0, with only **4 of the 34 NEW rungs** clearing 4b full (U56 H=5, H=10, H=504 and CADENCE 2Q). The best IS-reachable new rung, U56 H=10 via C_ISDD, runs OOS 15.07% / 1.1484 / -19.33% against **U56 SPY OOS 15.21% / 0.8711 / -33.72%** and **live RULES v2 OOS 9.45% / 1.2762 / -12.05%** — it clears 4b without beating the anchor book's own OOS Sharpe of 1.1643, so it buys nothing. **The rung every decided argmax walks to, gross 1.00, FAILS 4b on BOTH panels on L_DD** (U56 20.83% / 1.1403 / -24.93%, B136 21.39% / 1.0652 / -26.97% against a -20.23% bar). **PARK, not KEEP. Nothing proposed as capital.** SURVIVORSHIP (rule 9): U56 and B136 are current-constituent lists, so every 4b count here is an upper bound; the census quantities (edge share, argmax movement) are within-panel contrasts over one tape and the bias very largely cancels out of them.
- 2026-09-16 (lane B, idea 1119 does-the-LADDER-axis-answer-hold-on-the-SMALL-panel) — **ANSWERED = YES, the axis answer HOLDS on the small-cap panel — but the STRONG form the record has been quoting does NOT, and the idea's own mechanism premise fails BACKWARDS. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: lane B takes the LAST open idea; 1119 headed the end of '## Open' and is not EDGAR / Form 4 / 8-K / options / live-data. It has a PRICE LEG — 81 books across three panels and four dial families — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **LANE COLLISION (idea 932's defect again): the CLOUD LANE claimed and completed the SAME idea in the same hour and pushed first** (`..._cloud.py`). Both results are kept and cross-read, as the record did for idea 969 — a second independent measurement of the same object is worth having. **THE TWO RUNS CONFIRM EACH OTHER NUMBER FOR NUMBER AT THE SHARED SEED BASE.** Two independently written scripts return identical values at base 11161116: U56 9 un-resolvable / ETA2 0.8095 / 0.0476, B136 9 / 0.5556 / 0.0476, SMALL 7 / 0.3016 / 0.0476, and the same SMALL INF_FLOOR row vector N 0 / H 2 / GROSS 2 / CADENCE 3. Where the two differ is stated below, in both directions.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `PANEL` {U56, B136, SMALL663} x `q` {0.80, 0.90, 0.95} = **9 combinations, all published**, with all 3 x 4 x 4 = 48 cells reported under every one. LADDER and STATISTIC are not dials, they are the two candidate ANSWERS. DEFINITION is not a dial (INF_FLOOR headline; ZERO_CONTENT and NO_TAPE_500 printed beside everywhere), L=63 headline with L in {21, 126} beside. Frozen at 1082/1094/1098/1102/1108/1110/1116's construction: CAND20 legs, cap INF, max_vol 0.60, gross 0.75, W cadence, min hold 126, N=20, 10 bps, LAG 1, warm-up 260, IS end 2016-12-31, 1000 draws, crc32 seeds.

  **THE SEEDS ARE 1116's VERBATIM, ON PURPOSE.** U56 and B136 here are a BIT-EXACT REPRODUCTION, not an independent redraw, so the two panels that already answered cannot move underneath the new one: **G8 reproduces ALL 288 of 1116's committed U56/B136 cells at max |dgap| 0.00e+00 and max |dfloor| 0.00e+00, 288 of 288 infinite-floor flags**, and **G9 its committed ETA2 pair to 2.4e-05 / 4.4e-05**. That is why this run's large-panel counts read 9 and 9 where the cloud lane's own base reads 10 and 10 — the cell-level seed fragility 1116's D1 already measured. It moves NEITHER verdict: SMALL is the least un-resolvable against 9/9 and against 10/10 alike. The price of the choice is named: the two old panels carry no new seed evidence here, and SMALL663 gets its own reseed check instead.

  **A CORRECTION TO THE IDEA'S OWN PREMISE, MADE BEFORE ANY NUMBER. The SMALL panel is NOT 485 names.** `data/prices_small.csv.gz` was rebuilt on 2026-09-11 to 715 tickers x 4,198 trading days; the README's documented `max_1d_move >= 1.0` exclusion (the un-reversed level steps — AMPY +16,083%) drops **52**, leaving **663 tradable names** plus SPY as a benchmark column that is NOT a constituent and is made un-selectable (**G10: SPY's max book weight is 0.00e+00 at every rung**). '485' is a stale count from the 2026-09-04 vintage. The panel is labelled SMALL663 in every artefact.

  **THE ANSWER: THE LADDER AXIS HOLDS.** ETA2_LAD **0.3016** against ETA2_STAT **0.0476** at q=0.90 (margin +0.2540, ratio 6.3x); concordance within-ladder **0.5417** against within-statistic **0.3750**. Binary favours LADDER 2 of 3 q rungs, concordance 2 of 3, and both threshold-free continuous readings (REL_FLOOR, LOG_M) 3 of 3; the table is degenerate at 0 of 3, so the pre-registered BINARY route applies and the decision rule returns HOLDS. **Three independent reseeds agree 3 of 3** on binary and REL_FLOOR alike, and the cloud lane's eight bases agree 8 of 8.

  **H_MARGIN_SHRINKS PASSES HERE AND IS CORRECTED BY THE CONCURRENT LANE — the half that survives is SMALL, not the gradient.** At 1116's seed base the margin falls monotonically with panel width, +0.7619 -> +0.5079 -> +0.2540 (17.0x / 11.7x / 6.3x), and this run scored it a PASS on that. **The cloud lane's 8-base redraw refutes the U56-above-B136 half: U56's ratio runs 3.18-17.00x (median 7.00x) against B136's 11.00-11.67x (median 11.00x), so 1116's 17.0x is U56's redraw MAXIMUM and at the median the two large panels order the other way.** What survives the redraw: **SMALL663's ratio is a CONSTANT 6.33x at 8 of 8 bases, below B136 at 8 of 8 and below U56 at 4 of 8.** The defensible statement is 'the margin is smallest on the small-cap panel', **not** 'the margin shrinks monotonically as the panel widens', and that is how the record should carry it.

  **KILL OF THE STRONG FORM: THE AXIS WINS BUT NAMES DIFFERENT LADDERS (H_HC_SAME_ROWS FAILS).** SMALL663's INF_FLOOR row sums (of 4) are **N 0 / H 2 / GROSS 2 / CADENCE 3** against U56's 0/4/1/4 and B136's 2/4/0/3. The H ladder becomes PARTLY RESOLVABLE on the small-cap panel exactly where 1116 says it never is (rel_floor **0.7578** S_FULL and **0.7773** CAGR against 1.0000 / 1.0000 on both large panels), and GROSS — 1108's near-scaling family, the risk 1116 tested head-on — goes the OTHER way on the Sharpe columns (S_FULL rel_floor 0.6521 / 0.1002 / **1.0000**). **All three panels agree at only 8 of 16 cells; SMALL663 differs from BOTH at 4 of 16.** 'Un-resolvability is a property of the DIAL' survives as an AXIS statement; **'H and CADENCE are the un-resolvable dials' is a U56/B136 statement and must stop being quoted as a general one.** The cloud lane reaches the same place by a different route (its H_HCAD 5 of 8, H_NGROSS 2 of 8).

  **KILL OF THE MECHANISM PREMISE, AND IT FAILS BACKWARDS. H_MORE_UNRES FAILS: the noisiest panel is the MOST resolvable** — INF_FLOOR **7 of 16** on SMALL663 against **9 of 16** on both large panels (ZERO_CONTENT 7/9/9, NO_TAPE_500 4/5/5), and the cloud lane's 8-base redraw makes it robust (SMALL a constant 7 against U56 8-10 and B136 9-10 at every base). **H_RELIABILITY FAILS**: median paired per-cell S_OOS reliability **0.0000 / 0.2382 / 0.0000** (U56 / B136 / SMALL663) — SMALL663 ties U56 at the floor rather than sitting below it, so 1073's 0.0915-vs-0.5126 ordering (a DIFFERENT object — one book's draw-to-draw reliability, carried here as context and never gated on) does not reproduce at the LADDER level. Recorded separately: **the UNPAIRED reliability estimator is 0.0000 at all 48 cells on all three panels**; only the PAIRED form, the error variance that actually enters the sign agreement the floor is built from, recovers anything at all.

  **D3 — SNR DOES NOT EXPLAIN IT AND RUNS THE WRONG WAY; INF_FLOOR IS AN ORDER STATISTIC, NOT A PRECISION STATISTIC.** SMALL663 has the **WORST** mean signal-to-noise of the three (spread / median pair-SD **2.5506** against U56 3.4029 and B136 3.4791 — its spread is wider, 3.77 vs 2.84 / 3.30, but its per-pair noise grows FASTER, 1.82 vs 0.92 / 1.07) and still carries the fewest infinite floors. **This is a CORRECTION to the cloud lane's reading of the same fact**, which attributes it to SMALL's 'far larger ladder spreads': the spreads are indeed larger, but not larger than the noise, so spread alone does not carry it. The floor is infinite only when NO resolved pair sits ABOVE the LARGEST un-resolved gap, so one well-separated pair at the top of a right-skewed gap distribution makes a cell finite however poor the panel's AVERAGE precision is — and SMALL663's gaps are the most right-skewed (mean max/median gap **3.36** vs 2.53 / 2.28) while having the SMALLEST share of pairs separated by more than 2 pair-SDs (0.0833 vs 0.1389 / 0.1965). **Stated as a caution, not a measured law: an INF_FLOOR count is NOT a panel-precision ranking and must not be read as one.** This run establishes only that it is not monotone in mean SNR across three panels.

  **GATES 12 of 12 PASS, printed before any result number.** G1 / G1s fast runner == `engine.backtest` on U56 1.39e-17 and on the NEW SMALL663 panel 2.08e-17; G2 committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS 1.70e-04; G4 / G4b committed U56 n=12 and B136 n=15 triples 4.97e-05 / 2.13e-05; G5 live RULES v2 MaxDD -12.05% at 4.95e-05; G6 determinism 0.00e+00; G7 the ladders are live (spread 0.8959 all cells, 0.5111 on SMALL663 alone); G8 / G9 the bit-exact reproduction above; G10 the SMALL663 composition. Committed CSVs are re-read with `dtype=str, keep_default_na=False` throughout — idea 1105's fix, applied as standing practice. **HYPOTHESES 3 of 7.**

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed.** The book at all 81 rungs is byte-identical across both dials (only which cells get CALLED un-resolvable changes), so 4a and 4b are invariant to PANEL-as-a-dial and to q by construction; scored because rule 4 requires it. Rung chosen on IS 2009-2016 alone, per ladder, three choosers, OOS 2017-2026 read once. **IS picks: 4b full 4 of 36, 4b OOS 4 of 36, 4a 0 of 36 — all four on U56. Whole grid: 4b full 16 of 81, 4b OOS 17 of 81, 4a 0 of 81; SMALL663 0 of 27 on EVERY path** (the cloud lane's independent 0 of 27 rungs / 0 of 12 IS picks agrees), failing L_H1 26/27, L_H2 27/27, L_OOS 27/27, L_DD 25/27, L_CAGR 26/27. Its best IS-chosen book (H=252) runs 13.36% / 0.7844 / -37.41% full and 11.16% / 0.6676 / -37.41% OOS against **SMALL663 SPY 14.06% / 0.8581 / -33.72% full and 15.33% / 0.8767 / -33.72% OOS** and **live RULES v2 on that panel 4.30% / 0.6629 / -13.89%** — it loses to SPY on return, Sharpe and drawdown at once. U56 reference: SPY 15.10% / 0.8829 / -33.72% full, 15.21% / 0.8711 / -33.72% OOS; live RULES v2 8.62% / 1.2007 / -12.05%. **PARK, not KEEP. Nothing is proposed as capital.** SURVIVORSHIP (rule 9): all three panels are current-constituent lists and SMALL663 is by far the worst — the GAP and AGREEMENT quantities decomposed here contrast two books over the SAME inflated tape and the bias very largely cancels out of them, which is why the axis question can be asked on this panel at all; the 4b legs are measured against SPY and do NOT cancel, so every SMALL663 4b figure is an upper bound. That it still fails 4b at 27 of 27 on an inflated tape is the reading that matters.
- 2026-09-16 (lane C, idea 1097 is-the-BREAKEVEN-RUNG-c-star-PREDICTABLE-from-BINDING-LEG-MARGIN-over-TURNOVER-ALONE) — **ANSWERED = NO AS THE QUEUE WROTE IT, YES ONCE THE UNITS ARE CONVERTED, AND THE CONVERSION NEEDS NO FITTED PARAMETER. KILL of the literal form, KILL of `H_ONEFORM` on the drawdown leg, KILL of `H_LINEAR`, and two CORRECTIONS to 1094's own published figures. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.**

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `LEG` {L_H1, L_H2, L_OOS, L_DD, L_CAGR, O_S, O_DD, O_CAGR} x `FORM` {F_RAW, F_UNIT, F_K1, F_KLEG} — **all 32 points published**, plus the conjunction reading at every form. 1082/1086/1094's N x H grid (54 cells) is FROZEN and every cell reported. Cost is not a dial: it is the axis whose breakeven is predicted. **Every fitted constant is fitted on U56 ALONE; B136 is a pure hold-out.**

  **THE LITERAL FORM IS NOT A NEAR MISS.** `margin/turnover` lands within PROTOCOL's own 10 bps rung at **8.5% of the 318 scorable (cell, leg) points**; median error **+746.80 bps on L_H1**, +371.22 on L_H2, +365.84 on L_OOS. A Sharpe margin is not in units of annual return. `H_RAW` REFUTED exactly as pre-registered.

  **THE UNIT CONVERSION CLOSES 6 OF THE 8 LEGS WITH NO FREE PARAMETER.** Sharpe legs `1e4*m*sigma_L/tn_L`: median |err| **1.16 / 0.83 / 0.80 bps**, 100% within 10 bps, **zero verdict flips at 10, 25 and 50 bps on both panels**. CAGR legs need one constant and it is not free either — fitted `k[L_CAGR] = 0.8756` against `1/(1 + median book CAGR) = 0.8568` (ratio 1.022), i.e. **compounding** — giving median |err| **0.64 / 0.45 bps** and 100% within 5 bps.

  **THE LEG THAT DOES NOT CLOSE IS THE ONE THAT BINDS.** `L_DD` is in the binding set at **47 of 54 cells** and passes at 0 bps at only 10 of 54. Its best form reads median |err| **18.15 bps**, p90 78.78, 30% within 10 bps. `H_LINEAR` REFUTED and it is the cause: **the DD leg's 0-10 bps slope is 0.175 of its 0-200 bps slope** (every other leg 0.99-1.04), because cost moves WHICH episode is the maximum, not only its depth; worst slope-implied error 219.85 bps.

  **`H_ONEFORM` REFUTED, BUT THE LADDER IS REPLACEABLE FOR A VERDICT.** At the conjunction, `F_KLEG` reads median |err| 1.34 bps (full) / 2.01 (OOS) with max 44.0 / 44.6 at the DD-bound cells, so the 5 bps per-leg bar fails and hold-out flips are 16 not 0. What both forms get right is the decision: **0 of 54 conjunction verdict flips at PROTOCOL's 10 bps** (2 at 25, 1 at 50), and `F_KLEG` names the true binding leg at **100% of live cells** (`H_BINDING` SUPPORTED).

  **TWO CORRECTIONS TO 1094, BOTH GATED BEFORE THEY WERE READ.** (i) "solved c* exactly for 54 cells" — only **9 of 54** cells have a finite full-sample c* (10 OOS); **45 fail 4b at 0 bps and have no breakeven at all**. Its `rho(c*, turnover) = -0.65` reproduces to the digit and is an **n=9** statement: 2,000-rep bootstrap 90% interval **[-0.932, -0.071]**, width 0.860; the OOS reading is **-0.4863, [-0.852, +0.148]**, straddling zero. (ii) its **"8-42x" cost-immunity envelope is a ratio of SLICE MEANS**: read per cell on the same 0->50 bps span the ratio runs median **14.49x, range 3.10-96.81x**, and over 0->200 bps it reads 3.41x / 2.42x, because the DD leg is the non-linear one.

  **GATES 15 of 15 PASS, printed before any result number.** G1/G1b/G1c `r(c)=g-tn*c/1e4` == `engine.backtest` at 0/10/25 bps (1.39e-17); G2 936/1071/1082/1094's committed U56 W/H126 N=20 triple (3.18e-07); G3 SPY OOS triple (1.70e-04); G4 live RULES v2 MaxDD -12.05% (4.95e-05); **G5a-G5c reproduce 1094's 54 committed turnover values (1.78e-15) and BOTH committed c* columns EXACTLY (0 mismatches, 9 and 10 finite); G6 reproduces its six committed D1 slice means (8.88e-16)**; G7 conjunction c* == min over per-leg c* at all 54 cells; G8 every per-leg pass set is an interval from 0 bps; G9 the c* axis is live. HYPOTHESES 3 of 7 SUPPORTED.

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed, and a LOOK-AHEAD IN THIS RUN'S OWN FIRST PASS WAS CAUGHT AND RECORDED RATHER THAN REMOVED.** `C_CSTARPRED` initially scored cells with FULL-SAMPLE margins and picked cells clearing 4b on both panels; that is not an IS-only chooser. Rebuilt on IS-window margins, turnover, vol and drawdown episode — and on the parameter-free form, so no fitted constant enters — it picks U56 N=30 H=63 (OOS 14.72% / 1.1180 / -21.78%) and B136 N=40 H=63 (13.89% / 0.9641 / -28.28%) and **FAILS 4b OOS on both (O_DD)**, regret +0.0878 / +0.1054. `C_CSTARTRUE` fails on both. Only `C_ISSHARPE` reaches a pass (U56 N=12 H=21, OOS 17.57% / 1.1426 / -19.48%, 4b full and OOS PASS, 4a FAIL). **Selecting on cost headroom is worse than selecting on IS Sharpe on both panels.** Over all 54 cells at 10 bps: 4b full 8, 4b OOS 9, 4a 0. SPY OOS 15.21% / 0.8711 / -33.72%; RULES v2 @10 bps OOS 9.45% / 1.2762 / -12.05%. The form itself validates out of window: IS-only prediction vs IS-only truth median |err| **3.75 bps**, max 10.74, 94% of the 17 cells with a finite IS c*.

- 2026-09-16 (cloud lane, idea 1119 does-the-LADDER-axis-answer-hold-on-the-SMALL-panel) — **ANSWERED = YES, the LADDER axis holds on the SMALL panel — and the idea's OWN PREMISE does not. CONFIRM of 1116's answer where the draw dominates, KILL of the breadth gradient it is built on, KILL of the "small caps will degenerate the table" expectation, KILL of 1116's per-cell saturation, and a clean KILL of the sub-$2B panel as capital. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the LAST eligible open idea (idea 2 of 2); 1119 was the last line under '## Open' and is not EDGAR / Form 4 / 8-K / options / live-data. It has a PRICE LEG — 81 books over 4 ladders x 3 panels — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `PANEL` {U56, B136, SMALL} x `CONFIDENCE q` {0.80, 0.90, 0.95} — **all 9 combinations published**, with the two side definitions beside them (45 decomposition rows). LADDER and STATISTIC are NOT dials: they are the two candidate ANSWERS and all 3 x 4 x 4 = **48 CORE cells** are reported at every q. The un-resolvability DEFINITION is **frozen** at `INF_FLOOR` (1116's headline) and the decision rule never reads the other two; BLOCK LENGTH is frozen the same way (L=63 headline, L in {21, 126} beside, never selected on). Everything else frozen at 1082/1094/1098/1102/1108/1110/1116's construction: CAND20 legs, cap INF, max_vol 0.60, gross 0.75 (except on GROSS), W (except on CADENCE), min hold 126 (except on H), N=20 (except on N), 10 bps, LAG 1, warm-up 260, IS end 2016-12-31, 1000 draws, `zlib.crc32` seeds.

  **THE SMALL PANEL'S STAMP (idea 1074's recommendation, applied).** `data/small_meta.csv` lists 715 tickers; **52 dropped for `max_1d_move` >= 1.0**; the pool served is **664 names** (663 + SPY as benchmark only), tape **2010-01-04 .. 2026-09-11, 4,198 rows**, mean 524.3 names priced per day. This idea's own text says "485-name"; the loader serves 664 today — idea 706's 439->663 rebuild and idea 1072's finding that committed SMALL headlines move on it. **The stamp, not the label, is what these numbers refer to.** SPY is the 4b benchmark here and not a constituent, so it is removed from the selectable set, and gate `G_SPY` PRICES that choice rather than asserting it: had SPY been left in it would have been held at **0 of 871 rebalance dates**.

  **A GATE FAILED, AND THE FAILURE IS THE SECOND FINDING.** The first pass FAILED its own reproduction gates against 1116 (U56 ETA2 0.7333/0.0667 against the committed 0.8095/0.0476). Before reading that as a code discrepancy it had to be separated from a seed redraw, because this lane's bootstrap base is 11191119 and 1116's is 11161116. **Re-running the identical code at 11161116 reproduces 1116's committed ETA2 pair, concordance pair and all four per-ladder INF_FLOOR counts on BOTH panels, bit for bit (max deviation 4.4e-07, counts exact — gates G7/G8/G9 x 2).** So the code is 1116's; everything the two bases disagree about is the DRAW. A **seed redraw arm** (POST-HOC and labelled as such; 1116 itself ran 6 redraws for the axis question) then measures how much that is, at 8 bases across all three panels.

  **THE ANSWER: LADDER, ON ALL THREE PANELS AND EVERY REDRAW.** At the headline cell SMALL reads **ETA2_LAD 0.3016 against ETA2_STAT 0.0476 (6.33x)**; on the readings that need no threshold, REL_FLOOR eta2 0.3055 vs 0.2029, LOG_M eta2 0.4767 vs 0.1997, within-ladder concordance 0.5417 against within-statistic 0.3750. **LAD wins on SMALL at 8 of 8 redraws on the binary reading, 8 of 8 on REL_FLOOR and 8 of 8 on concordance — 24 of 24 panel-redraw combinations across the three panels.** SMALL's 4x4 table: N 0/4, H 2/4, GROSS 2/4, CADENCE 3/4 by row against S_FULL 2/4, S_OOS 2/4, CAGR 1/4, DD 2/4 by column.

  **KILL OF THE IDEA'S OWN PREMISE, AND IT IS THE KEEPER.** Over the 8 bases: **U56 margin 3.18x-17.00x (median 7.00x), B136 11.00x-11.67x (median 11.00x), SMALL 6.33x with ZERO spread.** 1116's committed 17.0x is U56's redraw MAXIMUM and its 11.7x is B136's; **at the median the ordering is B136 > U56, the reverse of the "weakens as the panel widens" gradient this idea infers from them.** ETA2 on a 4x4 BINARY table takes very few distinct values, so a single draw of it cannot support that reading at all. What is robust is only the SIGN. Per-ladder redraw ranges: U56 N 0-1 / H 3-4 / GROSS 1-2 / CADENCE 4-4; B136 N 2-2 / H 4-4 / GROSS 0-0 / CADENCE 3-4; SMALL N 0-0 / H 2-2 / GROSS 2-2 / CADENCE 3-3.

  **H_DEGENERATE REFUTED — the failure mode declared in advance did not happen.** SMALL's binary table is NOT constant (SST 3.9375) and carries **7 of 16** un-resolvable cells against **10 of 16 on each large panel**, i.e. it is the MOST resolvable of the three, because its ladder spreads are far larger (median 1.3944 against 0.5984 and 0.6058) and its gaps sit further from the q boundary. It is also the most redraw-STABLE panel, identical at all 8 bases. **1073's reliability 0.0915 for one book's OOS Sharpe does not make the ladder's rung-to-rung CONTRASTS less decidable on small caps.**

  **H_HCAD AND H_NGROSS REFUTED AT THE CELL LEVEL.** 1116's sharpest per-cell result — H and CADENCE un-resolvable at all 4 statistics, N and GROSS at almost none — reads **5 of 8 and 2 of 8** on SMALL. The ladder ORDERING survives (CADENCE worst, N best; SMALL marginals put GROSS at mean REL_FLOOR 0.5503 and log10 M 0.279 against CADENCE's 1.0000 and 1.720) but the saturation does not. **The MATCHED-TAPE arm** (all three panels truncated to SMALL's 2010 start, so the comparison is not a tape-length comparison) leaves the answer intact: U56 0.8095/0.0476 on 4,200 rows, B136 0.7333/0.0667 on 4,198, SMALL 0.3016/0.0476 on 3,938; LAD wins all three.

  **GATES 15 of 15 PASS, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS triple 1.70e-04; G4 live RULES v2 MaxDD 4.95e-05; G5 SMALL pipeline determinism 0.0; G6 and per-panel, every ladder live (Sharpe spread 0.896 overall; 0.183 / 0.121 / 0.511); **G7/G8/G9 x 2 panels, the SEED-MATCHED bit-level reproduction of 1116**. **HYPOTHESES 5 of 9** (H_REPRO, H_LADDER, H_SURVIVES, H_CONCORD, H_REDRAW; REFUTED: H_MARGIN_MONO, H_DEGENERATE, H_HCAD, H_NGROSS).

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed, and the SMALL panel is a clean KILL as capital.** Rung chosen on IS 2009-2016 alone, per ladder, three choosers, OOS read once. **All 36 picks: 4b full 4, 4b OOS 4, 4a 0**; the IS chooser picks the OOS-best rung 11 of 36 times. Per panel: U56 4/12 and 4/12 at median OOS Sharpe 1.1633 (median regret +0.0083); B136 0/12 and 0/12 at 1.0011 (+0.0515); **SMALL 0/12 and 0/12 at median OOS Sharpe 0.4317 against SPY's 0.8767 and RULES v2-on-panel's 0.5600 (+0.0832)**. Whole grid, 81 rungs: 4b full 16, 4b OOS 17, **4a 0**; U56 10/27 and 11/27, B136 6/27 and 6/27, **SMALL 0 of 27 on both windows**, and SMALL fails nearly every leg individually — L_H1 1/27, L_H2 0/27, L_OOS 0/27, L_DD 2/27, L_CAGR 1/27, so it is not one binding leg but the whole book. Binding leg across the 65 grid failures: L_DD (fails 53, sole failure 26), then L_CAGR (35, sole 9). SURVIVORSHIP (rule 9): U56 and B136 are current-constituent lists and the SMALL pool is worse — the CURRENT constituents of a sub-$2B screen, so every name that fell below the screen, delisted or went to zero is absent, which a small-cap pool loses far more often than a large-cap one. Every SMALL level is optimistic and every 4a/4b count an UPPER bound, **which makes the 0-of-27 stronger, not weaker.** The GAP and AGREEMENT quantities are within-pool contrasts over one tape and the bias very largely cancels out of the floor and the ETA2; it does NOT cancel out of the 4b legs, measured against SPY. **NOTHING IS PROPOSED AS CAPITAL.** DECLARED APPROXIMATION: `NO_TAPE_500` rests on 1110's projection A' = Phi(sqrt(M) Phi^-1(A)) — a fixed population gap and an SE falling as 1/sqrt(T), both running TOWARD resolution — so every `M_needed` is a LOWER bound; the exponent is INHERITED from 1110 and is NOT re-measured here, still less on the SMALL tape, so the DD column's `M_needed` is this run's least trustworthy quantity.

- 2026-09-16 (cloud lane, idea 1112 what-COMPOSITION-difference-carries-the-U56-vs-B136-GATE-ORDERING) — **ANSWERED = NONE OF THE THREE THE IDEA NAMES. The ordering is carried by WHICH NAMES, at matched pool size and matched ETF share. KILL of overlap (refuted by construction), KILL of "cap mix", KILL of gate-pass rate, CORRECTION to 1106's D1, and a NEW CONFOUND found, measured and cleared. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the FIRST open idea; 1112 headed '## Open' and is not EDGAR / Form 4 / 8-K / options / live-data. It has a PRICE LEG — 252 books across 21 panel instances, each with two DD-matched nulls x 40 seeds — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `MATCH` {NONE, SIZE, ETFSHARE, GATEPASS} — the matching variable defining each 56-name family drawn out of B136 — x `PANEL PAIR` {COMMITTED (U56@A, B136@B), VINTAGE (U56@A, U56@B)}. **Six independent draws per random family, every draw published: 21 analysis instances x 12 coords = 252 cells.** `N` {5,10,20,40} and `H` {5,21,126} are 1082/1086/1097/1106's committed coordinates, NOT dials; all 12 published for every instance. Frozen at 1085/1097/1106's construction: cap INF, REBUILT DD-match, 40 seeds, 34 bisection steps, max_vol 0.60, gross 0.75, W cadence, 10 bps, LAG 1, CAND20 legs, warm-up 260. **The null seed recipe keys on the TICKER SET, not the vintage**, so the vintage pair replays identical orderings and its step is the prices moving and nothing else.

  **OVERLAP CANNOT CARRY IT, AND NOT FOR A STATISTICAL REASON.** U56 is a strict **SUBSET** of B136: all 56 tickers appear in the 136, so the overlap statistic is 1.000 for both U56 arms and fixed by construction for every drawn family. `H_OVERLAP` was pre-registered as refuted in that case and is (gate G0). What distinguishes the panels is exactly the 80 single stocks B136 adds.

  **THE CARRIER IS IDENTITY.** Four additive steps, each a GATE difference across ONE axis, summing to the committed gap by construction (checked at 2.22e-16): **IDENTITY** (U56's 20 megacaps swapped for 20 other B136 large caps, K held at 56 and ETF share at 36/56) **+0.819 pp mean, 0.943 pp mean-abs, mean share of gap +0.467, biggest of the four at 6 of 12 coords**; **POOLSIZE** (K 56 -> 136, composition held) +0.697 / 0.724 / +0.636, biggest at 4 of 12 — `H_POOLSIZE` REFUTED at 5 of 12, one cell short of a majority, so it is the runner-up and not the carrier; **ETFSHARE**, the queue's "cap mix" read as the only cap-like axis these two committed panels differ on, is the **smallest live step (0.284 pp mean-abs) and opposes the gap (mean share -0.456)**; **VINTAGE** 0.069 pp. The biggest step clears 2x its own seed SE at 10 of 12 coords (`H_RESOLVED` SUPPORTED; the VINTAGE step's SE is paired over the 40 shared seeds, the other three independent hypots).

  **GATE-PASS RATE IS THE WORST OF THE CANDIDATES, NOT THE BEST.** Across the 21 instances at each coord: ETF share mean rho **+0.2654** (same sign 10/12), pool size K **-0.1908** (11/12), **gate-pass rate -0.1260 (same sign at only 8 of 12)**, mean pairwise return correlation -0.1098 (8/12). No single statistic reaches |rho| 0.30; `H_GATEPASS` REFUTED on all three of its clauses.

  **CORRECTION TO 1106's D1.** "B136's mean GATE is MORE NEGATIVE at all 5 rungs" is a statement about rung MEANS. At cell resolution the ordering holds at **11 of 12** coords and **inverts at H=126 N=10** (U56 -0.347 against B136 -0.307, gap -0.040), so `H_ORDER` is REFUTED as written and the premise should be re-worded.

  **A CONFOUND THE RECORD HAS NEVER NAMED, FOUND BEFORE ANY GATE WAS RUN AND THEN PRICED.** `load_universe()` serves U56 out of `data/prices.csv`; `load_universe(broad=True)` cannot, because **80 of the 136 broad tickers are absent from that file**, so `load_prices` raises and `load_universe` falls back to the separate weekly cache `data/prices_broad.csv`. **The two files DISAGREE on 51 of the 56 tickers they share: max relative price difference 3.63% (NVDA), max single-day return difference 723 bps, mean |return difference| 1.94 bps/day, and they end on different days (2026-09-15 vs 2026-09-11).** Every committed "U56 vs B136" contrast in the record is therefore across two price VINTAGES as well as two compositions. Priced as its own step here it is immaterial — **0.069 pp against a mean |gap| of 1.370 pp, under 10% of the gap at 9 of 12 coords (`H_VINTAGE` SUPPORTED)** — so the committed ordering is composition and not vintage. But GATE is a within-pool contrast over one tape; a LEVEL-valued cross-panel statistic has no such cancellation and could move much more. All panels here are put on the COMMON TAPE (4,703 rows) so K and T match across the vintage pair, with `U56@A_NATIVE` (prices.csv's own 4,705-row index, exactly as 1106 ran it) carried ONLY as a reproduction gate.

  **GATES 10 of 10 PASS, printed before any result number.** G0 U56 subset of B136; G1 fast runner == `engine.backtest` 1.39e-17; G1b `gross_rescaler(1.0)` == `nrun` 1.39e-17; G2 committed 936/1071/1082 W/H126 N=20 triple 3.18e-07; **G3 reproduces 1106's 12 committed U56 EDGE_OPEN/ELIG figures on its native tape at 0.00e+00, G4 its 12 book CAGR/MaxDD/turnover triples at 0.00e+00, G5 its 12 B136 EDGE figures at 0.00e+00**; G6 gross 0.75 on every date a book holds anything; G7 the vintage pair shares K and the tape; G8 every instance carries all 12 coords. **HYPOTHESES 3 of 8.**

  **TWO GATE-SPECIFICATION ERRORS IN THIS RUN'S OWN FIRST PASS, RECORDED RATHER THAN REMOVED.** (i) G3 failed at 1.03 pp because the native twin was keyed `"U56NAT"` in the null seed recipe instead of `"U56"`: with a different key it draws different orderings, so it was a fresh measurement wearing a reproduction gate's label. G4's book triples passed at 0.00e+00 throughout, which is what identified the cause. (ii) G6 failed at 5.69e-03 because it gated on MEAN gross being exactly 0.75, which a 56-name subpanel cannot satisfy: unlike B136 it can go **fully to cash** when nothing passes the 200d/vol gate — measured at 0.0000% to 0.7592% of post-warm-up rebalance dates (max B56SZ2@B) against 0.0000% on B136. Both were gate errors, not book errors: after the fixes the run was repeated end to end and **all 252 analysis cells are bit-identical to the first pass** (max absolute difference 0.0 over 39 numeric columns; the decomposition, ordering, correlation and hypothesis tables byte-identical), so no reported number moved.

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed.** The PANEL is a real dial a book would have to choose, so it is walked: instance x N x H chosen on 2009-2016 alone, OOS read once, two declared choosers, each over all 21 instances and over the committed pair alone. `C_SHARPE` picks badly both ways — B56SZ0@B N=10 H=5 (OOS 10.23% / 0.7643 / -23.47%, all three OOS 4b legs FAIL, regret +0.4174) and B136@B N=5 H=126 (15.03% / 0.7687 / -28.12%, FAIL, regret +0.4020), both below SPY's OOS Sharpe of 0.8767. `C_GATE` picks better: over all instances B56SZ0@B N=40 H=5 (11.01% / 1.0222 / -18.08%, OOS 4b PASS, regret +0.1596) and on the committed pair **U56@A N=40 H=5 (12.37% / 1.1630 / -16.67%, OOS 4b PASS, full-sample 4b PASS, regret +0.0077)** against SPY OOS 15.33% / 0.8767 / -33.72% and RULES v2-on-panel OOS 9.47% / 1.2782 / -12.05%. **4a 0 of 252** (A_DD 0/252 — no panel variant drew down less than the live book). **4b full 26 of 252, 4b OOS 32, full AND OOS 26**; binding leg L_DD (fails 203 of 226 failures, sole failure 88), then L_CAGR (56, sole 4). By family REF_U56 6/12, VINTAGE 7/12, B136 1/12, SIZE 5/72, ETFSHARE 2/72, GATEPASS 5/72 — **the drawn 56-name subpanels pass 4b LESS often than U56 itself**, the decomposition read through the KEEP paths. The 26 passes carry drawdown headroom +0.00 to +3.57 pp (median +1.24) and CAGR headroom +0.29 to +6.02 pp (median +2.37); idea 1083's 90% width for the drawdown-margin quantity is **4.1-7.2 pp**, wider than every drawdown headroom here. **NOTHING IS PROPOSED AS CAPITAL.** GATE's own stability: full-sample GATE <= 0 at 200 of 252 cells, OOS at 181, sign agrees full vs OOS at 217. SURVIVORSHIP (rule 9): U56, B136 and every 56-name subset drawn from B136 are current-constituent lists, so every level is optimistic and every 4a/4b count is an UPPER bound; the bias very largely cancels out of GATE and the four steps, which are within-pool contrasts over one tape, and does NOT cancel out of the 4b legs, measured against SPY.

- 2026-09-16 (lane C, idea 1113 does-the-MIN-HOLD-dial-s-L_DD-to-L_CAGR-LEG-SWAP-have-a-CROSSING-POINT) — **ANSWERED = NO. KILL of the crossing point, KILL of the interior optimum, KILL of the monotone gradient, and a CORRECTION to the idea's own premise. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: lane C takes the SECOND open idea; 1112 and 1113 headed '## Open' and neither is EDGAR / Form 4 / 8-K / options / live-data, so 1113 is the claim. It has a PRICE LEG — 198 books on a fine min-hold ladder — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `H` on a fine ladder {5, 7, 10, 13, 16, 21, 26, 32, 42, 52, 63} x `PANEL` {U56, B136}. **All 22 ladder points and all 198 walked cells published.** H=126 is carried for cross-run gates only and is never an argmax rung. The N ladder {5,8,10,12,15,20,25,30,40} is 1082/1086/1106's committed coordinate set, NOT a dial; every rung reported. Frozen: cap INF, max_vol 0.60, gross 0.75, W cadence, 10 bps, LAG 1, CAND20 legs, warm-up 260 rows.

  **THE STATISTIC, FIXED BEFORE ANY NUMBER.** DD_HEAD = 100*(0.60*|SPY MaxDD| - |book MaxDD|), pp OF DRAWDOWN; CAGR_HEAD = 100*(book CAGR - 0.70*SPY CAGR), pp OF ANNUAL RETURN; MINHEAD = min of the two, **a mixed-unit statistic**, published beside RELMIN (each leg over its own bar) at every rung so no conclusion rests on the unmatched scaling. RELMIN picks the same rung everywhere.

  **THERE IS NO CROSSING POINT: THE TWO CURVES NEVER MEET.** SWAP = DD_HEAD - CAGR_HEAD is NEGATIVE at all 11 rungs on both panels, running **-0.97 -> -10.81 pp on U56 and -4.12 -> -15.90 pp on B136** as H rises 5 -> 63; **the sign changes at NO rung**. **L_DD is the binding leg at 92/99 (U56) and 99/99 (B136) cells and L_CAGR passes 198/198**, so 'maximise the MINIMUM of the two headrooms' is just 'maximise drawdown headroom'. 1106's leg swap is a widening gap, not a trade-off.

  **THE ARGMAX IS A GRID EDGE AND IT IS NOT RESOLVED.** Both panels' mean-MINHEAD argmax sits at **H=5, the ladder's low end** (U56 +1.415 pp, B136 -0.879 pp) — idea 1109's boundary-pick reading, arrived at independently. Ladder spread 6.170 pp (U56) / 7.329 pp (B136), inside 1083's 4.1-7.2 pp band at one end; a **paired 500-rep moving-block bootstrap (block 63d, the SAME block indices applied to every rung AND to SPY inside each replicate) separates 0 of 10 rungs from the argmax on either panel** (widths 4.28-9.66 and 4.61-12.70 pp against a largest separation of +3.78 pp). The answer to 'by how much does it beat 1083's resolution' is **it does not beat it at all, on the record's number or on this run's own**. **H_MONO_DD and H_MONO_CAGR both REFUTED** (2 / 4 and 5 / 5 inversions): 1106's monotone gradient was a three-point reading of an eleven-point ladder.

  **CORRECTION TO 1113's OWN PREMISE.** 'Trades one leg for the other WITHOUT clearing both' is wrong as written: at N=20 on U56, **6 of 11 rungs (H=5,7,10,13,16,26) clear all five full-sample AND all three OOS 4b legs** (H_CLEARS is the one supported hypothesis of six). What does not happen is clearing both with RESOLVED headroom — the best MINHEAD anywhere is **+3.11 pp** (U56 H=5 N=8), inside every width above.

  **GATES 12 of 12 PASS, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS 1.70e-04 / 4.05e-05; G4 live RULES v2 MaxDD 4.95e-05 / 9.85e-06; **G5 reproduces 1106's 90 committed CAGR/MaxDD/turnover triples to 3.55e-15** (its CSV re-read with `dtype=str, keep_default_na=False`, idea 1105's fix, applied here as standing practice); **G6 reproduces 1106's conditional headroom medians at H=5/10/21/126 to 4.6e-03 and G7 its '25 cells pass 4b FULL and OOS' exactly**; G8 gross 0.75 at every cell; G9 the MINHEAD identity; G10 MINHEAD > 0 <=> L_DD and L_CAGR both pass. **HYPOTHESES 1 of 6.**

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed.** H chosen on IS 2009-2016 by IS MINHEAD at each (panel, N); OOS 2017-2026 read once. **The IS chooser picks the OOS-best rung 0 of 18 times; median regret +3.10 pp (U56) / +5.18 pp (B136)**, larger than 49 of the 50 passing cells' own headroom. **4a 0 of 198** (A_DD 0/198 — no min-hold rung drew down less than the live book). **4b full 50 of 198, 4b OOS 53, full AND OOS 50** (48 U56, 2 B136); binding leg L_DD (fails 145 of 148 failures, sole failure at 100), L_CAGR 0/148. IS-reachable 4b passes: **3 of 9 N rungs on U56** (N=10 H=5, N=12 H=10, N=15 H=10; OOS Sharpe 1.072/1.162/1.132, OOS CAGR 16.29%/17.25%/15.90%, OOS MaxDD -17.5%/-18.2%/-19.1% against SPY OOS 15.21% / 0.8711 / -33.72%), **0 of 9 on B136**. Unresolved by the bootstrap and refuted out of slice: **PARK, not KEEP. Nothing is proposed as capital.** SURVIVORSHIP (rule 9): U56 and B136 are current-constituent lists, so every headroom above overstates what a tradable book would have had.

- 2026-09-16 (cloud lane, idea 1111 is-the-lam-LINEARITY-of-TURNOVER-assumed-anywhere-ELSE-in-the-record) — **ANSWERED = NO, as written arithmetic. KILL of the record-wide-defect reading, CONFIRM of the cash-sleeve mechanism, CORRECTION to 1106's own "1.75% of peak" envelope, and a KILL of H_SIGN. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the FIRST open idea; 1111 was the first line under '## Open' and has a PRICE LEG — re-pricing the shortcut means rebuilding the book, its DD-matched nulls and both turnover paths on the tape — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `CLAIM SET` {STRICT = a committed script whose SOURCE performs arithmetic joining a DD-match lambda to a turnover/cost/drag quantity; WIDE = STRICT plus every committed script that BUILDS a DD-match lambda AND commits a turnover/cost/drag figure} x `TOLERANCE` {0.5%, 1%, 2%, 5%} of peak rebalance turnover. **All 8 grid points published.** PANEL {U56, B136}, `n` {5,10,20,40} and the lam ladder {0.25, 0.50, 0.75, 1.25, 1.50, 2.00} are COORDINATE SETS — every rung reported, none chosen. Frozen: gross 0.75, max_vol 0.60, W cadence, 10 bps, LAG 1, 20 seeds, 34 bisection steps, warm-up 260 rows skipped as `compare()` does.

  **THE CENSUS. 973 committed scripts scanned (this run self-excluded — it quotes the shortcut in its own regexes). STRICT = 1: only 1106 itself**, the run that found the defect, caught it with a declared gate and fixed it in the same commit. **The `matched / lam` shortcut is written down NOWHERE ELSE in the record.** `WIDE = 14`. **3 of those 14 carry the false assertion in prose** — a `gross_rescaler` docstring copied forward saying the turnover path "is exactly `lam` * the `lam=1` turnover". It is not, and the sentence, not the arithmetic, is the carrier.

  **THE RE-PRICE, AND A CORRECTION TO 1106's ENVELOPE.** 320 DD-matched nulls + 48 book-ladder points, all published. `ERR = matched/lam - raw` median **+0.0291 turns/yr (+0.045% of raw)**, range [-0.0098, +0.0718]. As a share of PEAK rebalance turnover: nulls **median 2.00%, max 8.70%**; book ladder **median 3.07%, max 15.92%**. **1106's headline 1.75% is a point on a distribution that runs to 15.9%** — a correction to the envelope, not to the number. Cells flagged at tol 0.5 / 1 / 2 / 5% = 254 / 230 / 158 / 20 of 320, identical under STRICT and WIDE.

  **THE MECHANISM IS THE ONE THE CLOSED FORM NAMES, AND THE SIGN IS NOT.** `Spearman(|ERR|, |cash sleeve|_max)` **+0.6783**; `Spearman(|ERR|, lam)` **-0.9407** (largest where the match pushes lam furthest from 1; mean |1-lam| 0.2932). **H_SIGN FAILS: 62 of 320 cells are NEGATIVE**, so the shortcut is a cell-dependent error, not a constant bias that could be corrected — only a direct `lam=1` evaluation fixes it. In charged cost the whole thing is worth **0.291 bps/yr median and 0.718 max at 10 bps** (1.45 / 3.59 at 50 bps): material by 1106's bar, immaterial in money. **HYPOTHESES 4 of 6** (H_EXPOSED and H_SIGN fail, and the two failures are the answer).

  **GATES 10 of 10 PASS, printed before any result number.** Fast runner NET returns and turnover == `engine.backtest` post-warm-up 1.39e-17 / 3.33e-16; rescaler at lam=1 == fast runner 6.94e-18 / 5.55e-17; the defect reproduced (G2 3.35e-02) and its named term shown non-zero (G2b 8.31e-02); live RULES v2 MaxDD -12.05%; every book at gross 0.75 exactly. **G1e is a MEASURED note: `engine.backtest` emits 2 NaN rows at the open** (weights are `fillna`'d BEFORE `.shift(1)`), so any reproduction gate read on the FULL index returns NaN rather than a difference — this run's first pass did exactly that and G1 duly failed.

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed.** IS 2009-2016 chooses, OOS 2017-2026 read once. **4a 0 of 8, 4b full 0 of 8, 4b OOS 1 of 8.** Binding leg `L_CAGR` **0 of 8**: the vol-scaled composite at gross 0.75 tops out at 9.84% full-sample CAGR against the 10.57% floor (70% of SPY's 15.10%). `L_DD` 7/8, `L_H1` 5/8, `L_H2` 2/8, `L_OOS` 2/8. Only `C_SHARPE` on U56 (n=40 -> OOS 10.78% / 1.2407 / -13.80%) clears 4b OOS, and it fails the same CAGR leg full-sample, so it is **not a candidate**. SPY U56 full 15.10% / 0.8829 / -33.72%, OOS 15.21% / 0.8711 / -33.72%; B136 full 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767 / -33.72%. Live RULES v2 8.62% / 1.2007 / -12.05%.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels: every CAGR/Sharpe level is optimistic and every 4a/4b count an UPPER bound. The ERR and sleeve measurements are within-book on one tape and the bias very largely cancels out of them.

  **WHAT SHOULD CHANGE (not a RULES edit).** Delete the "is exactly `lam` * the `lam=1` turnover" sentence from `gross_rescaler`'s docstring wherever it is carried forward, and take raw turnover from a direct `lam=1` evaluation. Both are one line. Script `research/backtests/2026-09-16_is-the-lam-LINEARITY-of-TURNOVER-assumed-anywhere-ELSE-in-the-record_cloud.py`, 5 CSVs, console log, result memo, 4 LEADERBOARD rows.

- 2026-09-16 (cloud lane, idea 1105 is-a-COMMITTED-CSV-KEY-COLUMN-that-does-not-SURVIVE-pd-read_csv-a-RECORD-WIDE-DEFECT) — **ANSWERED = NO as a SEED defect, YES as a TEXT defect, and the two must not be confused. CONFIRM of 1097's incident and its mechanism; KILL of "record-wide" as a description of the seed exposure; KILL of H_CAPONLY; CORRECTION to the implied scope. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the LAST open idea; 1105 was the last line under '## Open' and has a PRICE LEG — pricing what a re-read gets wrong means rebuilding 1071's capped books and both null families on the tape — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `COLUMN CLASS` {SEED_KEY, ANY_KEY} x `ROUND-TRIP TEST` {LITERAL, SEMANTIC}. **All 4 grid points published.** SEED_KEY = a column whose name is a token in one of the record's **44 committed `mdseed(...)` call sites**, restricted to tokens of >= 3 characters — the short ones (`s`, `t`, `k`, `n`, `p`, `c`, `f`, `r`, `l`) are LOOP VARIABLES, and leaving them in matched t-stats, p-values and counts, inflating the class with columns no reader ever keys on. **That restriction is part of the D1 definition, declared before the answer, and the first pass without it is what showed why it is needed.** LITERAL = `str(pd.read_csv(f)[col])` differs from the committed text; SEMANTIC = the raw -> re-read map is not injective, so no format rule can undo the damage. Panels, the `n` ladder and 1071's cap ladder are COORDINATE SETS.

  **THE CENSUS. 5,302 committed CSVs, 87,836 columns (this run's own outputs self-excluded).** SEED_KEY LITERAL **98 of 3,541 (2.77%) in 97 of 5,302 files (1.83%)**; SEED_KEY SEMANTIC 60 (1.69%); ANY_KEY LITERAL **45,644 of 87,836 (51.97%) in 88.66% of files**; ANY_KEY SEMANTIC 7,281 (8.29%). **H_WIDE FAILS at 1.83% against its 5%-of-files bar and H_SEED FAILS by a factor of 19 — seed-key columns round-trip BETTER than columns at large, not worse.** 1097's incident is real and reproduced, but it is not a record-wide seed defect.

  **THE MODE BREAKDOWN IS THE PART WORTH KEEPING.** Of the 98 SEED_KEY literal failures: **KEY 22 cols / 22,357 cells** (1071's kind — a literal whose FORMAT carries the key), **EMPTY_NAN 42 / 40,599** (a committed `''` returning as the string `'nan'`), **FLOAT_REPR 34 / 6,195** (17-sig-fig text vs pandas' 16-digit repr). Across ALL columns the ordering inverts completely: **FLOAT_REPR 39,430 cols / 18.2M cells**, EMPTY_NAN 6,137 / 2.88M, **KEY 99 / 76,467**. `CAGR` alone fails in 1,206 files and 1.39M cells — and that is harmless, because nobody hashes a CAGR. **The defect is not "CSV columns do not round-trip"; it is "a key must never be recovered from a float-inferred column", and only ~100 columns in the record are keys.**

  **H_CAPONLY FAILS, AND THAT IS THE FINDING WITH TEETH.** Ten seed-key names fail LITERAL: `cap`, `kind`, `panel`, `gate`, `seed`, `null`, `elig`, `native`, `u56`, `U56`. **`kind` (23 files, 38,656 cells, SEMANTICALLY lossy in 23 of 23) and `panel` (26 files, 23,532 cells, lossy in 25 of 26) are worse than `cap` (24 files, 5,880 cells, lossy in 1 of 24)**, and `seed` itself fails in 4 files, all four semantically. **60 SEED_KEY columns are semantically lossy: for those no re-reading convention recovers the committed key at all**, and they must be re-derived from their scripts, never from their CSVs.

  **THE PRICE. 24 cells (2 panels x n {5,10,20} x 1071's caps {INF, 2.00, 1.50, 1.00}, H=126, 20 seeds), all published.** Every one of the four committed cap literals fails to survive — pandas returns `inf`, `2.0`, `1.5`, `1.0`, each giving a different md5 seed, shown from pandas itself rather than asserted. Re-seeding the null families from the re-read literal moves the committed EDGE by **median 0.4013 pp, max 2.0898 pp** against a median seed SE of 0.3221 pp. **0 of 24 cells reproduce the committed EDGE exactly; 8 of 24 (33.3%) land inside one seed SE** — 1097's "8 of 40" reproduced on an independent grid and construction.

  **GATES 11 of 11 PASS, printed before any result number.** Fast runner NET returns and turnover == `engine.backtest` post-warm-up 1.39e-17 / 3.75e-16; the cap BINDS (G2b 0.0375 = 1.00 x 0.75/20) and DE-GROSSES (G2c -1.11e-16); the defect reproduced from pandas (G3, G3b). **G6 is the control the priced grid could not supply** — because all four committed literals fail, there is no naturally-surviving cell, so the same key drawn twice is shown to give a **bit-identical** null family (0.0) while `'INF'` vs `'inf'` gives a different one (9.95e-03). Only with G6 passing is a non-zero delta attributable to the key rather than to the machinery. **HYPOTHESES 2 of 5** — H_SEMANTIC and H_PRICE pass; H_WIDE, H_SEED and H_CAPONLY fail, and those three failures are the answer.

  **RULE 8 AND BOTH KEEP PATHS — nothing proposed.** IS 2009-2016 chooses, OOS 2017-2026 read once. **4a 0 of 24, 4b full 0 of 24, 4b OOS 7 of 24.** Binding leg `L_CAGR` **0 of 24**: the capped composite tops out at 10.41% full-sample CAGR against the 10.57% floor (70% of SPY's 15.10%) — a **0.16 pp** miss, well inside idea 1083's measured 4.1-7.2 pp resolution for exactly that quantity, so the miss is not resolvable either. `L_DD` 24/24, `L_H1` 20/24, `L_OOS` 19/24, `L_H2` 7/24. Only `C_CAGR` on U56 (N=20, cap INF -> OOS 11.16% / 1.0145 / -16.74%) clears 4b OOS, and it fails the same CAGR leg full-sample, so it is **not a candidate**. SPY U56 full 15.10% / 0.8829 / -33.72%, OOS 15.21% / 0.8711 / -33.72%; B136 full 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767 / -33.72%. Live RULES v2 8.62% / 1.2007 / -12.05%.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels: every CAGR/Sharpe level is optimistic and every 4a/4b count an UPPER bound. The RAW-vs-REREAD contrasts are within-tape and the bias very largely cancels out of them.

  **WHAT SHOULD CHANGE (not a RULES edit).** Idea 979's `unit`/`cell_key` proposal is the wrong shape for this failure: the problem is not that columns lack a declared unit, it is that ~100 columns are KEYS and are being recovered from a float-inferred read. **The fix is at the READ, not the write — any script re-reading a committed CSV for a key must pass `dtype=str, keep_default_na=False`**, which kills all three modes at once. Script `research/backtests/2026-09-16_is-a-COMMITTED-CSV-KEY-COLUMN-that-does-not-SURVIVE-pd-read_csv-a-RECORD-WIDE-DEFECT_cloud.py`, 5 CSVs, console log, result memo, 5 LEADERBOARD rows.

- 2026-09-16 (lane B, idea 1106 is-the-U56-H21-SIGN-FLIP-a-TURNOVER-fact) — **ANSWERED = NO. KILL of 1106's premise: `GATE = EDGE_OPEN - EDGE_ELIG` does not scale with the null's re-draw frequency. A CORRECTION to the reading of 1097's 13 exceptions (the flip is a PANEL fact with an H component, not a turnover fact), a CONFIRM of 1097's H=21/63/126 columns at 1.78e-15, and a DEFECT found and paid for in this run's own first pass. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: lane B takes the LAST open idea; 1106 was the last line under '## Open' and has a PRICE LEG — walking the min hold means rebuilding the book and both nulls on the tape — so it carries this lane's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `H` (min hold, index positions) walked at **{5, 10, 21}** x `PANEL` **{U56, B136}**. On a WEEKLY cadence the rebalance dates are ~5 trading days apart, so these rungs are "the null re-draws every 1 / 2 / 4 rebalances"; **H=5 is the floor — nothing is ever young at the next rebalance, so the ordering is re-drawn in full every week.** 1086's committed **H {63, 126}** rungs are CARRIED AS CONTEXT and used as cross-run reproduction gates, not tuned. **THE N LADDER IS NOT A DIAL** — `{5,8,10,12,15,20,25,30,40}` is 1082/1086's committed coordinate set and every rung is published. **90 cells, 180 prices, all committed.** Frozen at 1082/1085/1086's construction: CAND20 legs, cap INF, REBUILT DD-match, 40 seeds, 34 bisection steps, max_vol 0.60, gross 0.75, W, 10 bps, LAG 1. **The null SEED RECIPE contains NO H**, so the SAME 40 random orderings are replayed at every rung: a move in GATE along H is the min hold moving, not a different draw.

  **THE ANSWER.** U56 mean GATE down the ladder 126 -> 63 -> 21 -> 10 -> 5 reads **-0.465 -> -0.872 -> +0.216 -> -0.050 -> +0.907 pp** while the null's realised turnover rises monotonically **2.70 -> 4.65 -> 11.10 -> 22.45 -> 43.76** turns/yr. **H_TURNOVER FAILS: `Spearman(GATE, NTURN_raw OPEN)` = +0.0775 pooled, +0.4924 on U56, -0.0572 on B136** — the two panels disagree in sign, which is the declared refutation. **H_MONOTONE FAILS with 2 inversions. H_FLIP_EXTENDS FAILS: positive-GATE rungs go 6/9 at H=21 -> 5/9 at H=10 -> 8/9 at H=5** — halving the hold, i.e. DOUBLING the re-draw rate, makes the flip WEAKER.

  **H_DTURN FAILS AT ITS OWN NAMED MECHANISM.** The premise is that the gate costs a random book a TURNOVER cost, so the gate-free null must be the one turning over more. `DTURN = NTURN(OPEN) - NTURN(ELIG)` is **positive at only 40 of 90 cells** (mean +0.755, range [-3.139, +9.356]), and it is **NEGATIVE at EVERY rung H >= 21 on BOTH panels** — there the GATED null turns over MORE, the exact reverse of the premise. It only turns positive at H = 5 and 10. `rho(GATE, DTURN)` +0.204 pooled.

  **H_REDRAW PASSES, AND THAT IS WHAT MAKES THE FAILURE CLEAN.** Names newly taken per rebalance: **15.35 / 3.68 / 0.68 at H = 5 / 21 / 126**, ratios **4.18x (5-over-21) and 22.7x (5-over-126)**, inside the declared [3, 5] band at all four panel x gate arms. The queue line's arithmetic — "a 21-day hold lets a random ordering re-draw four times as often" — is exactly right, measured, and moves by a factor of 22 across the ladder. **GATE moves 1.4 pp, non-monotonically, with the wrong cross-panel sign.**

  **D1 (POST-HOC, LABELLED, EXCLUDED FROM THE TALLY) — IT IS A PANEL FACT.** H_PANEL as DECLARED asks only whether B136 flips positive ANYWHERE; it does, at 4 cells, so it passes on its wording. The DIRECTIONAL contrast is the one that bites and it runs the wrong way: **B136's nulls re-draw MORE than U56's at every one of the 5 rungs, and B136's mean GATE is MORE NEGATIVE than U56's at every one of the 5 rungs.** Two panels matched on construction, cadence, cost, seeds and DD convention order themselves by COMPOSITION, not by turnover (positive cells over the walk rungs: U56 19/27, B136 3/27). **The declared wording was too weak to test the premise, and this is recorded as a lesson about the wording, not as a pre-registered result.**

  **D2 (POST-HOC, LABELLED) — 'TURNOVER' IS TWO AXES AND THEY PULL OPPOSITE WAYS.** The +0.4924 on U56 is ACROSS rungs (the H axis). WITHIN a rung, where the only turnover variation is the N axis, **`rho(GATE, NTURN)` is NEGATIVE at 9 of 10 (panel, H) slices, as low as -0.967.** More null turnover RAISES GATE when it comes from a shorter hold and LOWERS it when it comes from a smaller book; no single "GATE scales with re-draw frequency" claim can carry both signs. Best of eight explanators tried is the literal re-draw count `NEW_per_reb` (pooled +0.302, U56 +0.615, B136 +0.167) — still weak, still panel-inconsistent.

  **WHAT THE FLIP IS, AT THE LIMIT OF WHAT THIS RUN ESTABLISHES.** A U56 property that strengthens as the hold shortens, and NOT noise where it is largest: **7 of 9 U56 H=5 cells are positive at 2 SE with NONE decisively negative**, against B136's 0 positive-decisive and 9/9/9 negative-decisive at H = 10/21/63. So 1097's 13 exceptions are real and concentrated — they are just not explained by re-draw frequency.

  **GATES 13 of 13 PASS, printed before any result number.** G1/G1b/G1c fast runner, `gross_rescaler(1.0)` and its turnover path all == `engine.backtest` at 1.39e-17 / 0.00e+00; G2 936/1071/1082's committed W/H126 N=20 triple 3.18e-07; G3 SPY OOS triples 1.70e-04 / 4.05e-05; G4 live RULES v2 MaxDD == -12.05% / -12.24%; G8 every book at gross 0.75. **The suite's point is G5-G7: 1097's 54 committed `EDGE_OPEN`/`EDGE_ELIG` figures at 1.78e-15, its 54 book CAGR/MaxDD/turnover triples at 1.78e-15, and its headline (U56 H=21 carries 6 of 9 positive GATE rungs) at 0.00e+00.** The H=21/63/126 columns ARE 1097's numbers; H=5 and H=10 are the only new measurement.

  **A DEFECT THIS RUN FOUND IN ITS OWN FIRST PASS AND PAID FOR.** Turnover is **NOT** exactly `lam *` the `lam=1` turnover under the REBUILT DD-match: the cost term is `lam * |W - A/V(lam)|` and the cash sleeve inside `V` does not scale, so recovering raw turnover as `matched / lam` errs by **1.31e-02, 1.75% of peak rebalance turnover**. The first pass did exactly that and its declared linearity gate G1d duly FAILED. `NTURN_raw` now comes from a DIRECT `lam=1` evaluation, and G1d PUBLISHES the size of the error the shortcut would have carried instead of asserting a linearity that does not hold. The headline correlations moved in the fourth decimal (+0.0780 -> +0.0775), so no conclusion rested on it — **but it was invisible except to a gate declared before the numbers.** Filed as 1111.

  **RULE 8 AND BOTH KEEP PATHS.** Cell chosen on IS 2009-2016 ALONE by two choosers declared in advance, OOS 2017-2026 read ONCE. **H_WF PASSES 1 of 4, and it is not an EDGE chooser.** C_SHARPE on U56 picks **H=21 N=12: OOS 17.57% / 1.1426 / -19.48%, 4b OOS PASS** — 1082/1086/1097's already-committed and already-parked cell, reproduced at 1.78e-15, **not a new candidate and not re-proposed.** C_EDGE on U56 picks H=10 N=5 and both choosers on B136 pick H=63 N=5; all three FAIL on the drawdown leg, reproducing 1082's, 1084's and 1097's reading from a fourth direction. Benchmarks: U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; RULES v2 live full 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762 / -12.05%; B136 SPY OOS 15.33% / 0.8767 / -33.72%, RULES v2 OOS 7.88% / 1.1059 / -12.24%. **4a 0 of 90** (`A_DD` 0/90 — no cell clears the live book's -12.05%, a book fact). **4b 25 of 90 full-sample, 28 of 90 OOS, 25 of 90 both**; `L_CAGR` 90/90, `L_H1` 89/90, `L_OOS` 73/90, `L_H2` 66/90, **`L_DD` 28/90 and the SOLE failing leg at 40 of the 65 failures.**

  **THE NEW RUNGS ARE WHERE THE PASSES CONCENTRATED, AND THE H DIAL TRADES ONE 4b LEG FOR THE OTHER. 17 of the 25 passes are H=5 or H=10 cells (15 U56, 2 B136)** — books the record had not built. Drawdown headroom median **+2.08 pp at H=5** against +0.55 pp at H=21; CAGR headroom **+2.23 pp at H=5** against +4.55 pp at H=21. Removing the min hold entirely buys the widest drawdown margin in the grid (U56 H=5 N=40 sits **3.56 pp inside the cap** against the 0.01-1.45 pp the committed cells manage) and pays for it in CAGR, where that same cell clears the floor by **0.34 pp**. **Shortening the hold moves the binding leg from `L_DD` to `L_CAGR` without ever clearing both by a margin this tape can resolve. NOTHING IS PROPOSED AND NO MEMO IS WRITTEN:** best headroom anywhere is +3.56 pp on drawdown and +7.14 pp on CAGR against idea 1083's measured 4.1-7.2 pp 90% width for exactly that quantity, and the H=5 slice is not rule-8 reachable — the honest IS-Sharpe chooser picks H=21 and the IS-EDGE chooser picks H=10 N=5, which fails OOS.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every level is optimistic and every 4a/4b count an UPPER bound. GATE, DTURN and the EDGE contrasts are within-pool over one tape and the bias very largely cancels out of them; it does NOT cancel out of the 4b legs, measured against SPY, a real index, so the 25 parked passes are flattered by it.

  Script `research/backtests/2026-09-16_is-the-U56-H21-SIGN-FLIP-a-TURNOVER-fact_B.py`, 8 CSVs, console log, result memo, 4 LEADERBOARD rows. **HYPOTHESES 2 of 6** (H_REDRAW and H_PANEL-as-worded pass; H_TURNOVER, H_DTURN, H_MONOTONE, H_FLIP_EXTENDS fail and are the result). Follow-ups filed: 1111 (the lam-linearity turnover defect), 1112 (what composition difference carries the U56-vs-B136 GATE ordering), 1113 (does the min-hold dial's L_DD-to-L_CAGR leg swap have a crossing point).
- 2026-09-16 (lane B, idea 1097 RE-READ-every-committed-EDGE-FIGURE-as-a-LOWER-BOUND) — **ANSWERED = MOSTLY, BUT NOT AS A UNIVERSAL CLAIM. KILL of "every committed EDGE figure UNDER-states rank skill", a CORRECTION of the envelope 1097's own queue text quotes, and a CORRECTION to 1071's +5.07 pp — the record's most-cited EDGE figure, where the lower-bound reading turns out to be a CONVENTION question rather than a tape question. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: lane B takes the LAST open idea; 1097 was the last line under '## Open' and, unlike most record-census ideas in the queue, has a PRICE LEG — re-pricing a committed EDGE figure means rebuilding its book and its null on the tape — so it carries this lane's mandatory rule-8 walk-forward and both KEEP paths. **NUMBERING DEFECT (idea 932's again): a DIFFERENT idea 1097 (`is-the-BREAKEVEN-RUNG-c-star-PREDICTABLE-from-BINDING`) sits at the TOP of '## Open' and is untouched by this run.**

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** CLAIM SET {STRICT = a committed EDGE figure that is a book-minus-null CAGR advantage AND machine-readable in a committed CSV with a fully determined cell; WIDE = STRICT plus every committed prose unit carrying an `edge` token, a pp figure AND a null token} x NULL GATE {OPEN = random ranks, elig = all priced — 1071's/1082's/1086's convention, i.e. the one every committed EDGE figure was measured under, reproduced as a CONTROL under each committer's exact seed recipe; ELIG = random ranks, elig = the book's own 200d/vol gate, so the null differs from the book only in ORDERING}. **THE CELL COORDINATES ARE NOT DIALS** — (panel, N, H, cap, seeds, DD-match convention) are taken exactly as the record committed them; this run walks no new rung. Frozen at 936/1071/1082/1085/1086's construction: CAND20 legs, max_vol 0.60, gross 0.75, W cadence, 10 bps, LAG 1.

  **THE HARVEST. 94 committed EDGE figures, 188 prices, all published.** FAMILY A (1082/1085/1086): 2 panels x 9 N x 3 H, cap INF, 40 seeds, REBUILT match = 54. FAMILY B (1071): 2 panels x 4 N x 5 caps, H=126, 20 seeds, CASH match = 40. **The record had scored exactly 18 of them against the ELIG null (1085, H=126, cap INF); 76 had NEVER been re-priced** — the whole H=21 and H=63 slices and the entire cap ladder, where the null gate had never been crossed with the cap dial at all.

  **THE ANSWER.** `GATE = EDGE_OPEN - EDGE_ELIG` is **<= 0 at 81 of 94 committed figures (0.862)**, mean **-0.791 pp**, median -0.679, range **[-2.728, +1.104] pp**. Against the seed SE of the difference, **50 of 94 are decisively negative at 2 SE, 2 decisively positive, and 42 cannot tell.** So most committed EDGE figures are lower bounds, decisively so; a minority are upper bounds; and roughly 45% of the record is inside its own noise. **The 13 exceptions are NOT scattered: 6 of 9 sit in ONE slice, U56 at min hold 21**, where mean GATE is **+0.216 pp** and the ELIG null is the EASIER comparand. Every other (panel, H) slice reads 8/9 or 9/9 negative (B136 -1.902 / -1.815 / -0.869 pp at H = 21 / 63 / 126). **H_LOWER FAILS**, and it fails in one identifiable place rather than everywhere.

  **H_MAGNITUDE FAILS.** 1097's queue text quotes 1085's **0.03-2.47 pp** as the size of the understatement. Three cells fall outside it, all B136: N=15/H=21 **-2.728**, N=12/H=63 -2.487, N=5/H=126 -2.471 pp. The envelope was a property of the 18 cells 1085 happened to measure, not of the record.

  **WHAT ACTUALLY CHANGES — sign, rank, verdict.** **H_SIGN PASSES: 0 of 94** committed figures change sign; no published EDGE reading is reversed. **H_VERDICT fails on 3 of 94**, all the same way (not-decisive -> decisive, all at N=40; decisive count 91 -> 94) — the re-read RESCUES the record's weakest EDGE figures rather than overturning its strongest. **H_RANK FAILS HARDEST AND IS THE FINDING THAT MATTERS: 13 of 34 committed LADDERS change their argmax**, against the single U56 H=126 case (12 -> 5) 1085 had already published. **Twelve of the thirteen sit on axes the record has never scored against this null: the H axis (7 of 18 move, rho down to -0.500) and 1071's cap axis (5 of 8, rho down to -0.800, three ladders at NEGATIVE rank correlation). The N axis — the one the record has walked most — is the stable one (1 of 8, rho +0.867 to +1.000).** Read beside idea 1098, which showed the argmax is not stable to the DRAW, this says it is not stable to the CHOICE OF NULL either: **a published argmax on an H or cap ladder should not be quoted without its null gate.**

  **H_CONV FAILS 7 of 8, AND THE FAILURE IS AT 1071's OWN HEADLINE.** At the doubly-committed U56/N=20/H=126/cap-INF cell — 1071's committed **+5.07 pp**, reproduced here exactly at 5.0719 — GATE reads **+0.271 pp under CASH** and **-0.027 pp under REBUILT**: the two DD-match conventions **disagree on the sign** (max |CASH - REBUILT| 0.386 pp over the 8 cells). At the record's most-cited EDGE figure, "is it a lower bound?" is not a tape question at this resolution. **1071's +5.07 should be quoted as +/-0.4 pp for convention alone**, before any sampling error.

  **THE CLAIM-SET DIAL: WIDE ADDS NO PRICES.** Corpus **31,030 committed units** (LEADERBOARD and QUEUE by line, CHANGELOG and 863 `*.result.md` by paragraph). **1,019 carry an `edge` token, 1,001 of those a numeral, 215 a figure in pp, 161 a null token — and only 52 carry BOTH a pp figure and a null token**, 35 of those a cell key. The record's `edge` vocabulary is overwhelmingly NOT the null contrast ("grid edge", "knife-edge", "ladder edge", "Sharpe edge"), and the 52 that are trace back to the same four runs whose CSVs STRICT already harvests at full precision. Re-pricing a prose figure adds a rounding error, not a measurement.

  **A RECORD-HYGIENE DEFECT THIS RUN HIT AND PAID FOR.** 1071's `cap` column is a STRING KEY ("INF", "1.00", "1.25", "1.50", "2.00") that enters its md5 null-seed recipe VERBATIM. Reading that CSV with pandas' default dtype silently converts it to `inf / 1.0 / 1.25 / 1.5 / 2.0`; re-formatting those draws DIFFERENT nulls. The first pass of this script reproduced 1071 at **8 of 40 cells** (only the `1.25` rung, whose string round-trips) and failed its cross-run gate at 8.98e-03 — **plausible wrong numbers, not an error.** The column is now read as text and never converted. This is open idea 979's `unit`/`cell_key` proposal arriving with a measured cost; filed as 1105.

  **GATES 13 of 13 PASS, printed before any result number.** G1/G1b/G1c fast runner, `gross_rescaler(1.0)` and the CAPPED build all == `engine.backtest` at 1.39e-17; G2 936/1071/1082's committed W/H126 N=20 triple 3.18e-07; G3 SPY OOS triples 1.70e-04 / 4.05e-05; G4 live RULES v2 MaxDD == -12.05% / -12.24% at 4.95e-05 / 9.85e-06. **The point of the suite is G5-G9: 1086's 54 committed EDGE_pp at 8.88e-16, 1085's 18 EDGE_ELIG_pp and EDGE_OPEN_pp at 8.88e-16, 1071's 40 committed null medians and book CAGRs at 8.33e-17, 1082's 18 at 8.88e-16, and every harvested figure against its own committer at 8.44e-15 — so the OPEN column is literally the record's own numbers and every GATE is a difference of two measurements made the same way.** HYPOTHESES 2 of 7 (H_SIGN, H_WF pass; H_LOWER, H_MAGNITUDE, H_RANK, H_VERDICT, H_CONV fail and are the results above).

  **POST-RUN DIAGNOSTIC, LABELLED AS POST-RUN, AND IT CUTS AGAINST THIS RUN'S OWN HEADLINE.** lambda is constrained to (0, 1], so a null draw already drier than the book enters unmatched at lambda = 1 with its CAGR understated, which INFLATES EDGE. **The ELIG null is drier far more often: clip share 0.609 against OPEN's 0.344, and ELIG clips more at 80 of 94 cells.** That inflates EDGE_ELIG more than EDGE_OPEN and pushes GATE NEGATIVE — toward the "everything is a lower bound" conclusion being tested. **H_LOWER fails anyway, at 13 cells, several with the bias at its strongest** (U56 N=15/H=21: clip 0.000 OPEN vs 0.925 ELIG, GATE still +0.640 pp). The bias is conservative for the failure; it is NOT conservative for the 81 negative cells, whose magnitudes are upper bounds on the true understatement.

  **RULE 8 AND BOTH KEEP PATHS.** EDGE is not a KEEP path and this run's dial 2 CANNOT move one: the book at every cell is byte-identical across the two gates, only the comparand changes, so 4a/4b are mathematically invariant to the re-read. Both are scored at all 94 cells anyway. Cell chosen on IS 2009-2016 ALONE by four choosers, OOS 2017-2026 read ONCE, 8 picks: **H_WF PASSES 2 of 8 — and NEITHER is an EDGE chooser.** C_ISSHARPE picks U56 N=12/H=21 (OOS 17.57% / 1.1426 / -19.48%, 4b OOS PASS) and C_ISDD picks U56 N=40/H=21 (13.01% / 1.1286 / -19.27%, PASS); **both EDGE choosers, under EITHER gate, pick the same losing cell N=5/H=63 on BOTH panels and fail on the drawdown leg** — 1082's and 1084's reading reproduced from a third direction: re-pricing the EDGE ladder against the right null does not give it out-of-sample selection information a plain IS Sharpe does not carry better. Benchmarks: U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; RULES v2 live full 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762 / -12.05%; B136 SPY full 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767 / -33.72%, RULES v2 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059 / -12.24%. **4a 0 of 94** — no cell clears the live book's drawdown, a book fact, not a null fact. **4b full-sample 18 of 94, 4b OUT OF SAMPLE 19 of 94, both 18 of 94. NOTHING IS PROPOSED and no memo is written:** all 18 are cells the record already committed and already parked (8 in family A, including 1086's rule-8-reachable U56 N=12/H=21 reproduced at 8.88e-16 and 1082's three; 10 on 1071's cap ladder), **binding on drawdown by 0.01-1.45 pp against the 20.23% cap**, and idea 1083 measured the 90% width of exactly that quantity at 4.1-7.2 pp on this tape. This run inherited those books; re-pricing their nulls says nothing about whether they are capital.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every level is optimistic and every 4a/4b count an UPPER bound. EDGE_OPEN, EDGE_ELIG and GATE are within-pool contrasts over the same tape and the bias very largely cancels out of them; it does NOT cancel out of the 4b legs, measured against SPY, a real index, so the 18 parked passes are flattered by it.

  Script `research/backtests/2026-09-16_RE-READ-every-committed-EDGE-FIGURE-as-a-LOWER-BOUND_B.py`, 8 CSVs, console log, 4 LEADERBOARD rows. Follow-ups filed: 1105 (committed CSV key columns that do not survive `pd.read_csv`), 1106 (is the U56 H=21 sign flip a turnover fact), 1107 (re-price every committed argmax on an H or cap ladder).
- 2026-09-16 (lane C, idea 1102 does-the-RESOLUTION-FLOOR-CLAUSE-change-any-committed-ARGMAX-in-the-record) — **ANSWERED = YES, AND THE CLAUSE IS NOT AN EDGE FACT. 26 of 32 rebuildable committed-ladder ARGMAXES fall below their own ladder's 90% sign-resolution floor and become TIE SETS, and the 200 VALUED argmax claims harvested from the corpus go with them at 0.844. KILL of ARGMAX-AS-A-POINT across the record (not just over EDGE), CONFIRM of 1098's two committed triples at 4.97e-05 / 2.13e-05, an independent reproduction of 1096's D3 2-of-4 reachability, and a CORRECTION to this run's OWN declared mechanism. Clause still PROPOSED and NOT enacted (rule 6). No RULES change, no book promoted, no PROTOCOL edit; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: lane C takes the SECOND open idea; 1102 was the second line under '## Open' and names no EDGAR / Form 4 / 8-K / options / live-data source.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** CLAIM SET {CORE, WIDE} x FLOOR CONFIDENCE q {0.80, 0.90, 0.95} = **6 cells, ALL published**. CORE = the 32 (panel, ladder, statistic) argmax objects this tree rebuilds EXACTLY — the four ladder families the record's argmax claims are actually read off (N {5,8,10,12,15,20,25,30,40}, H {21,63,126,252}, GROSS {0.30..0.75}, CADENCE {D,W,M,Q}) on U56 and B136, argmaxed over full Sharpe, OOS Sharpe, full CAGR and MaxDD. WIDE = every VALUED harvested claim; one whose (family, statistic) IS a rebuilt cell is scored on that cell's MEASURED verdict, the rest carry a TRANSFERRED rate **declared in advance as an EXTRAPOLATION and not a re-derivation** (1048's convention), and the three bases are counted separately. Frozen at 1082/1086/1094/1098's construction: CAND20 legs, cap INF, max_vol 0.60, gross 0.75, W, min hold 126, N=20, 10 bps, LAG 1, warm-up 260, IS end 2016-12-31, each except where it is the ladder. **BLOCK LENGTH is NOT a dial:** headline L=63, L in {21, 126} beside it — and all three read **26 of 32**.

  **THE ANSWER.** CORE tie sets **24 / 26 / 27 of 32** at q = 0.80 / 0.90 / 0.95 (0.750 / 0.812 / 0.844). WIDE **150.5 / 168.8 / 173.4 of 200** (0.752 / 0.844 / 0.867), of which **134 are measured-by-family**, 24 transferred-by-statistic and 42 transferred-overall. **H_TIE PASSES and the rival named in advance — that EDGE is simply the record's noisiest laddered statistic and the plain performance statistics resolve — is refuted: full Sharpe, OOS Sharpe, CAGR and MaxDD all fail to resolve their own peaks at the same rate.** Stronger than an unresolved peak: **19 of 32 floors are INFINITE at q=0.90**, i.e. no gap anywhere on the ladder is sign-resolved, so there is no gap size at which that ladder could publish a point at all.

  **D1 — THE ONLY ARGMAXES THIS TAPE RESOLVES ARE GRID EDGES OF A MONOTONE DIAL (post-hoc, labelled, excluded from every count).** All **6 of 6** RESOLVED objects sit on the GROSS ladder and **every one is a ladder END** — CAGR and Sharpe peak at gross 0.75, MaxDD at 0.30 — so grid-edge share is **1.000 among RESOLVED against 0.385 among TIE SETS**. Two of the six resolve on a **whole-ladder Sharpe spread of 0.0040** (B136 GROSS: gap 0.0004 against floor 0.0004): gross is a pure scaling, every rung moves together in every draw, and the resulting resolution says nothing about an interior optimum because there is none. **The record can resolve an argmax exactly where the argmax carries no information.**

  **D2 — WHAT THE CLAUSE ACTUALLY REWRITES.** Not just the verdict: the mean 90% tie set is **4.88 rungs = 0.851 of its own ladder** (median 4, max 10), and **12 of 32 are WHOLE-LADDER** — the clause leaves those argmaxes with no content whatsoever. This is the cost of enacting it, and it is the reason the clause stays PROPOSED here rather than enacted.

  **H_LADDER FAILS 0 of 8, AND THE FAILURE IS THE MECHANISM.** Declared in advance: a longer ladder should carry a HIGHER floor, more pairs meaning more chances of an unresolved one. Wrong in every pair — the 9-rung N ladder resolves relatively BETTER than the 4-rung H ladder in **8 of 8** (panel, statistic) cells. **What a ladder resolves is set by how far apart its BOOKS are, not by how many rungs it has**, which is why the H ladder (four holds of one book) resolves nothing and the N ladder (books from 5 to 40 names) resolves some pairs. **HYPOTHESES 5 of 6:** H_TIE, H_STAT (capped floor/spread DD 0.885 vs Sharpe 1.000, thin), H_PANEL (median cross-panel floor ratio **1.398**, inside the declared 1.5x — the floor is a TAPE fact), H_P (**P(bootstrap argmax == full-sample argmax) < 0.50 in 0.562 of objects, median P 0.460**) and H_HARVEST PASS; H_LADDER FAILS.

  **THE HARVEST, WITH ITS REJECTS.** 3,673 claim words ('argmax', 'peaks at', 'optimum at', 'optimal', 'best') over **1,007 files across 5 sources** -> **200 VALUED** and **3,473 REJECTED** (3,135 carrying no rung value inside the 120-char window — 'best' above all — and 338 naming no statistic), **every reject published with its reason and its context** so the acceptance rule can be re-read. By source LEADERBOARD 89 / RESULTMD 53 / QUEUE 43 / CHANGELOG 9 / MEMO 6; by family N 105 / GROSS 33 / THRESH 23 / CADENCE 18 / H 12 / BAND 9; by statistic S_FULL 95 / EDGE 37 / CAGR 30 / S_OOS 25 / DD 8 / TURNOVER 5. LIMITATION DECLARED: the family and statistic patterns are heuristics over prose, and the dominant reject class is exactly the one this idea cannot price.

  **METHOD.** 1098's machinery minus the null — a statistic with no null attached needs no seeds, so ONE circular-block index is drawn and applied JOINTLY to every rung of a ladder, because a rung-to-rung GAP is a paired quantity over one tape. CAGR and Sharpe are EXACT under block resampling from block sums of log1p, r and r^2; MaxDD is not order-invariant and its bootstrap reconstructs the resampled path. 1,000 draws per (panel, ladder, window). Floor at q = the smallest |gap| strictly above every UNRESOLVED gap that is itself resolved, exactly 1098's definition, with the largest unresolved gap published beside it. Where NOTHING on a ladder resolves the floor is INFINITE and is published as such; for the two RATIO hypotheses it is capped at the ladder's own spread, **declared in advance as a LOWER bound and therefore the direction that understates tie-set-ness** — and no tie-set VERDICT depends on the cap, since a gap never exceeds its ladder's spread.

  **GATES 9 of 9, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 CROSS-RUN 936/1071/1082/1094's committed U56 W/H126/N=20 triple 3.18e-07; **G4 CROSS-RUN 1098's committed U56 n=12 triple 4.97e-05 and G4b its committed B136 n=15 triple 2.13e-05**, so this run's ladders are the same objects 1098 measured; G3 SPY OOS triple 1.70e-04; G5 live RULES v2 MaxDD == committed -12.05% at 4.95e-05; G6 determinism 0.00e+00; G7 bootstrap median CAGR == the sample's at 5.45e-04; G8 the axis is live (Sharpe spread 0.1916 over the 54 cells).

  **RULE 8 AND BOTH KEEP PATHS.** Rung chosen on IS 2009-2016 ALONE per ladder by C_ISSHARPE / C_ISDD / C_ISCAGR, OOS 2017-2026 read ONCE. **4 of 24 picks clear 4b full AND OOS, 0 of 24 clear 4a** — and all four are the SAME cell, the standing anchor U56 / W / H=126 / N=20 / gross 0.75 (15.58% / 1.1397 / -19.13%, OOS 16.97% / 1.1643 / -19.13%), reached from the GROSS and CADENCE ladders and from neither the N nor the H ladder on either panel, **independently reproducing 1096's D3 2-of-4 reachability finding**. Whole grid, 54 cells: **4b full 16, 4b OOS 17, 4a 0**. Benchmarks: U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; U56 RULES v2 live 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762; B136 SPY full 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767 / -33.72%; B136 RULES v2 OOS 7.88% / 1.1059. **NOTHING IS PROPOSED AS CAPITAL and no memo is written:** every 4b-passing cell is prior art the record already holds (1086/1098's PARKed n=12, the standing anchor, 1083's gross ladder, 1096's quarterly cell), and a resolution floor was never a KEEP path.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels. A gap between two rungs of one ladder contrasts two books over the same inflated tape and the bias very largely cancels out of it and out of the floor; it does NOT cancel out of the 4b legs, measured against SPY, a real index, so the 17 OOS passes above are flattered by it.

  Script `research/backtests/2026-09-16_does-the-RESOLUTION-FLOOR-CLAUSE-change-any-committed-ARGMAX-in-the-record_C.py`, 11 CSVs, console log, 4 LEADERBOARD rows. Follow-ups filed: 1108 (is BOOK DISTANCE, not rung count, what a ladder's floor measures), 1109 (does any committed GRID-EDGE argmax in the record survive being read as a boundary pick), 1110 (what does the record lose if the floor clause is enacted as written) — renumbered from 1105-1107 on push, lane collision, defect 932 again.

- 2026-09-16 (cloud lane, idea 1098 is-an-ARGMAX-over-a-9-RUNG-LADDER-a-PUBLISHABLE-OBJECT-AT-ALL-on-this-tape) — **ANSWERED = NO ON THE BAR THAT MATTERS, AND THE SPLIT BETWEEN THE PANELS IS THE FINDING. The measured 90% sign-resolution floor is 3.25 pp of CAGR on U56 and 4.02 pp on B136, while the published EDGE peak leads its runner-up by 0.61 pp and 1.49 pp — 5.3x and 2.7x below the floor of the ladder it is read off. KILL of the EDGE argmax as a publishable point on U56 (P(argmax reproduces) = 0.378, and the bootstrap's own MODE is n=5, not the published n=12), a PASS on B136's point (0.728) with its ladder still unresolved, CONFIRM that 1085/1082's four answers are ONE measurement, CONFIRM of all 18 of 1082's committed EDGE figures at the rounding limit, and a resolution-floor clause PROPOSED and NOT enacted (rule 6). No RULES change, no book promoted, no PROTOCOL edit; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the LAST open line for idea 2. QUEUE carries TWO lines numbered 1098 (idea 932's defect again) and this is the argmax-resolution one.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** SEEDS S {5, 10, 20, 40} (nested — the first S of the same 40 draws, so the seed dial moves the PRECISION of the null median and nothing else) x LADDER SPACING {FULL9 = 1082's nine rungs, ALT5 = {5,10,15,25,40}, COARSE3 = {5,15,40}}, every spacing a SUBSET of FULL9 so all three read the same paths — **12 cells per panel, 24 in total, ALL published.** Frozen at 1082/1086's construction: CAND20 legs, cap INF, max_vol 0.60, gross 0.75, W, min hold 126, 10 bps, LAG 1, warm-up 260, IS end 2016-12-31. BLOCK LENGTH is NOT a dial: headline L=63, L in {21, 126} reported beside it and never selected on. EDGE(n) = 100 x (CAGR_book - median_seeds CAGR_null) under 1082's REBUILT convention (each null re-run at the gross lam*0.75 that equalises its realised |MaxDD| with the book's, 34-step bisection). The bootstrap draws circular blocks of trading days and applies ONE index JOINTLY to the book, all 40 nulls and every rung, because EDGE is a paired quantity over one tape; CAGR under block resampling is exact from block sums of log1p. **LIMITATION DECLARED IN ADVANCE: lam is matched once on the real tape and held fixed inside the bootstrap, so every width published here is a LOWER bound** — the conservative direction for a "not resolved" conclusion.

  **THE ARGMAX'S OWN SAMPLING DISTRIBUTION (L=63, 1,000 draws).** U56 FULL9: full-sample argmax 12 / 5 / 12 / 12 at S = 5 / 10 / 20 / 40 with P(argmax == full) **0.546 / 0.517 / 0.286 / 0.378**, and at S=40 the mass reads **5: 0.390, 8: 0.055, 10: 0.095, 12: 0.378, 15: 0.014, 20: 0.068, and exactly 0.000 on 25 / 30 / 40** — the bootstrap MODE is n=5, not the published peak. B136 FULL9: argmax 10 at every seed count with P **0.738 / 0.798 / 0.764 / 0.728** (mass 10: 0.728, 5: 0.146, 15: 0.116, rest <= 0.004). **90% argmax sets {5, 8, 10, 12} (U56, 4 rungs) and {5, 8, 10, 12, 15} (B136, 5 rungs).** Block length barely moves any of it (P = 0.410 / 0.378 / 0.393 and 0.738 / 0.728 / 0.769 at L = 21 / 63 / 126). **H_STABLE FAILS on U56 (0.378 against the declared 0.50) and PASSES on B136 (0.728); H_WIDTH PASSES on both.** The one part of the shape that IS resolved is the far side: n = 25/30/40 carry zero argmax mass on U56, i.e. 1082's "EDGE collapses above ~n=15" survives and its PEAK does not.

  **THE RESOLUTION FLOOR, MEASURED RATHER THAN ASSUMED.** Over all 36 rung pairs, the smallest |dEDGE| above which the bootstrap agrees with the full-sample sign at least 90% of the time: **U56 3.25 pp of CAGR (95%: 4.66), B136 4.02 pp (95%: 4.02)**. Against that, the peak's lead is **+0.61 pp** (U56, 12 over 10) and **+1.49 pp** (B136, 10 over 15) — **H_FLOOR PASSES on both panels.** **15 of 36 and 18 of 36 rung pairs are not sign-resolved at 90%, the largest unresolved gap being 3.00 and 3.86 pp.** What this does NOT kill: EDGE(peak) - EDGE(40) is **6.41 pp (U56) and 6.47 pp (B136)**, far above either floor, so the collapse stays resolved and only the peak's LOCATION dies.

  **THE PROPOSED CLAUSE (proposed, NOT enacted — rule 6 reserves this for Sunday review).** "Any published argmax over a ladder must state (i) the gap between the winning rung and the runner-up in the units of the quantity being maximised, and (ii) the ladder's own resolution floor at 90%, measured by block bootstrap on the same paths. Where the gap is below the floor, the result is reported as a TIE SET — the rungs carrying 90% of the bootstrap argmax mass — and never as a single rung." Applied to the record as it stands it re-expresses 1082's "EDGE peaks at n=12 / n=10" as **U56 tie set {5, 8, 10, 12}** and **B136 tie set {5, 8, 10, 12, 15}**, and leaves 1082's far-side verdict alone.

  **TWO SUPPORTING FACTS, BOTH CUTTING AGAINST BUYING A FIX.** **H_SEED PASSES on both panels: the width is a TAPE fact, not a seed-count fact** — the 90% set is 4 rungs at S=5 and 4 at S=40 on U56 (5 and 5 on B136) while the per-rung EDGE SE falls 2.1x to 3.5x over the same range (U56 n=12: 1.597 -> 0.332 pp). Seeds sharpen the null median and do nothing to the argmax, because what the argmax is uncertain about is the tape. **The SPACING dial changes the ANSWER while RAISING apparent confidence, which is the part that generalises: H_SPACE PASSES on U56** — COARSE3 reports n=**5** at P 0.638 against FULL9's 12 at 0.378, a different argmax with 1.7x the apparent stability — **and FAILS on B136** (0.598 vs 0.728), where the mechanism is blunter still: **the published peak n=10 is not a rung of COARSE3 at all and the coarse ladder confidently reports 15.** Any argmax in the record read off a 3- or 4-rung ladder is suspect for this reason alone.

  **H_1085 PASSES.** 1085/1082 reported the U56 argmax as 12 / 5 / 12 / 12 across seed counts; this run's bootstrap puts **0.546 / 0.517 / 0.286 / 0.378** of the argmax mass on exactly the rung each of them named. "Four ways of asking give three answers" is better read as ONE unresolved answer.

  **GATES 8 of 8, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G1b `gross_rescaler(1.0)` == `nrun` 1.39e-17; G2 CROSS-RUN 936/1071/1082's committed U56 W/H126 N=20 triple 3.18e-07; G3 SPY OOS triple 1.70e-04; G5 live RULES v2 MaxDD == committed -12.05% at 4.95e-05; G7 determinism of 1082's seed recipe 0.00e+00; **G_XRUN all 18 of 1082's committed EDGE figures reproduce, max |d| 4.80e-03 pp (U56) and 4.53e-03 (B136), inside the 0.005 pp its own two-decimal rounding allows** — so the bootstrap is layered on 1082's numbers, not on a re-derivation of them.

  **RULE 8 AND BOTH KEEP PATHS.** n chosen on IS 2009-2016 ALONE inside each spacing at each seed count by four choosers (C_ISSHARPE, C_ISDD, C_ISCAGR and C_ISEDGE, the last on the IS-window EDGE with IS-matched nulls), OOS read ONCE: **0 of 48 picks clear 4b on U56, 0 of 48 on B136, 4a 0 of 96.** C_ISSHARPE picks n=40 (U56) and 5/8 (B136); C_ISDD 40 and 30/40; **C_ISCAGR and C_ISEDGE pick n=5 on both panels**, reproducing 1082's finding that its own EDGE selector is the worst pick available. Whole ladder: **4b full 2 of 9 on U56 (n=12 17.71% / 1.1692 / -20.17%, halves 1.275/1.088, OOS 18.89% / 1.1758; n=20 15.58% / 1.1397 / -19.13%, OOS 16.97% / 1.1643) and 1 of 9 on B136 (n=15 16.78% / 1.0682 / -19.66%, OOS 17.49% / 1.0458) — 1082's committed 3 of 18, reproduced exactly — and 4a 0 of 18.** Benchmarks: U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; RULES v2 live full 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762 / -12.05%; B136 SPY OOS 15.33% / 0.8767 / -33.72%, RULES v2 OOS 7.88% / 1.1059 / -12.24%. **NOTHING IS PROPOSED AS CAPITAL and no memo is written:** the two 4b-passing U56 rungs are 1082's already-PARKed cells, no honest IS-only chooser reaches either, and EDGE was never a KEEP path.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels. Book and null are drawn from the same pool over the same tape, so the bias very largely cancels out of EDGE and out of the argmax's width; it does NOT cancel out of the 4b legs, measured against SPY, a real index, so the three 4b passes above are flattered by it.

  Script `research/backtests/2026-09-16_is-an-ARGMAX-over-a-9-RUNG-LADDER-a-PUBLISHABLE-OBJECT-AT-ALL-on-this-tape_cloud.py`, 9 CSVs, console log, 4 LEADERBOARD rows. Follow-ups filed: 1102 (does the floor clause change any committed argmax in the record), 1103 (is the floor a property of EDGE or of every laddered statistic), 1104 (does the far-side collapse survive a panel it was not found on).

- 2026-09-16 (cloud lane, idea 1096 is-RULE-8-REACHABILITY-a-COST-RUNG-object-across-the-record-s-COMMITTED-PICKS) — **ANSWERED = NO, NOT AS A GENERAL FACT. KILL of the queue's own generalisation (only 2 of 8 CORE picks move at or below 25 bps; median MOVE RUNG 71 bps against a median c\* of 117, a ratio of 1.65x and not 5x), CONFIRM of 1094's specific numbers at 0.00e+00, a CORRECTION (1094's "switches away at 15 bps" is the spacing of its own published ladder — on a 1-bp ladder the switch is at 11 bps, same destination), and three structural findings that matter more than the headline. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the FIRST open line for idea 1. QUEUE carries TWO lines numbered 1096 (idea 932's defect again) and this is the rule-8-reachability one, not the gate one.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** CLAIM SET {CORE, WIDE} x COST RUNG {0, 10, 25, 50} bps = 6+2 cells, ALL published. CORE = the four ladder families the record's committed rule-8 runs actually walked — N {5,8,10,12,15,20,25,30,40} (1071/1082/1094), H {21,63,126,252} (1086), GROSS {0.30..0.75, 10 rungs} (1083), CADENCE {D,W,M,Q} (930/931) — on both panels, 8 ladders; WIDE adds the joint (N,H) grid, the book family {TOP5,TOP10,TOP20,EWELIG,BAND03} and a max_vol ladder, 6 more. 146 cells, 48 (panel, ladder, chooser) picks, 192 priced picks, a 201-rung 1-bp cost ladder. **COST IS NOT A DIAL THE BOOK CAN CHOOSE** — every chooser is re-run separately at each rung and only ever sees the cost its own book pays. CHOOSER {C_ISSHARPE, C_ISDD, C_ISCAGR} is REPORTED everywhere and never fitted on; the headline is C_ISSHARPE, the record's default, fixed before the run. FROZEN: CAND20 legs, cap INF, max_vol 0.60, gross 0.75, W, min hold 126, LAG 1, warm-up 260, IS end 2016-12-31, each except where it is the dial. D1-D4 are labelled post-hoc diagnostics, excluded from every count.

  **THE ANSWER.** C_ISSHARPE picks that differ from their own 10-bps pick: **CORE 0/8 at 0 bps, 2/8 at 25, 2/8 at 50, 5/8 within 200 bps; WIDE 1/6 at 25; ALL 3/14 at 25.** C_ISDD 0/14 at 25 and 2/14 at 50; C_ISCAGR 1/14 and 2/14. The only two CORE movers are U56's HOLD ladder at **14 bps** (21 -> 63 -> 126) and U56's CADENCE ladder at **21 bps** (W -> Q); the GROSS ladder's pick does not move anywhere within 200 bps on either panel. **H_MOVE FAILS** (declared: a majority of CORE picks move by 25 bps). **H_5X FAILS, and the two ways of computing it disagree by 3x, which is itself the finding: ratio of medians 117/71 = 1.65x, median of per-ladder ratios 5.04x — but that median is over the 2 of 8 CORE picks where both quantities exist at all.** H_ABSORB PASSES 8 of 8 on CORE (once a CORE pick moves it never returns; 2 of 48 picks flicker, both with a 0-bps move). **H_DIR FAILS 1 of 8** — B136's N ladder moves 8 -> 5 at 82 bps, i.e. toward HIGHER turnover, which is noise and not cost. **H_TURNSPREAD FAILS** — the GROSS ladder spans 1.21-2.90x/yr of turnover and never moves, the CADENCE ladder spans 1.65-4.06x and moves at 21 bps. **H_VERDICT FAILS** — over {0,10,25,50} bps identity moves in 11 of 48 picks while the 4b-full verdict changes in 8 and 4b-OOS in 9.

  **THE CORRECTION, and it is a precision correction rather than a contradiction.** D1 re-runs 1094's EXACT joint grid (H restricted to {21,63,126}) on 1094's OWN published rung ladder {0,5,10,15,20,25,30,40,50,75,100}: the U56 IS-Sharpe chooser first differs from its 10-bps pick at **15 bps — 1094's number, to the bp**. On a **1-bp** ladder the same chooser switches at **11 bps**, to the same destination (5/63). 1094's 15 bps is the resolution of its ladder, not the switch. G4 reproduces 1086/1094's committed candidate triple at **0.00e+00** and G4b its committed breakeven **c\* = 63 bps at 0.00e+00**, so the two runs are measuring the same objects.

  **THREE STRUCTURAL FINDINGS.** **D2 — for 76.2% of picks the question has no referent:** c\*(10-bps pick) is UNDEFINED, the pick already failing 4b at ZERO cost, for **32 of 42** (panel, ladder, chooser) picks, including every one of B136's 21. 1094 generalised from two of the ten that do clear 4b uncharged. **D3 — reachability is a property of the LADDER, not of the book:** the anchor cell (936/1071/1082/1094's W / H=126 / N=20 / gross 0.75 book, the standing top-20 family) is a rung of ALL FOUR CORE ladders, and C_ISSHARPE reaches it from **2 of 4** at 0 and 10 bps on BOTH panels (GROSS and CADENCE), 1 of 4 at 25 bps on U56, 2 of 4 at 50 — while the N ladder picks 40 / 8 and the H ladder 21 / 63 at every rung. Same book, same tape, same cost: reachable or not by which dial was walked, an effect far larger than the cost rung's. **D4 — a stable pick is not a resolved pick:** the IS-Sharpe margin between the 10-bps pick and its runner-up runs 0.0001 to 0.1238, and **8 of 14 picks are decided by less than the record's own committed seed-noise floor (877: 0.0145 of Sharpe)**. The most cost-stable pick in the run, the GROSS ladder's, is decided by **0.0001 / 0.0003** over a whole-ladder Sharpe spread of 0.0014 / 0.0034 — its stability means only that gross barely moves Sharpe, and must not be read as strength.

  **GATES 12 of 12, printed before any result number.** G1/G1b/G1c `r(c) = g - tn*c/1e4` == `engine.backtest` at 10/25/50 bps, 1.39e-17 each (this is what licenses the exact 201-rung ladder). G2 CROSS-RUN 936/1071/1082/1094's committed U56 W/H126 N=20 triple 3.18e-07. G3 SPY OOS triple 1.70e-04. G4 0.00e+00. G4b 0.00e+00. G5 live RULES v2 MaxDD == committed -12.05% at 4.95e-05. G6 CAGR non-increasing in the rung at all 146 cells (0.00e+00). G7 determinism 0.00e+00. G8 the cost axis is live (5.66 pp of CAGR spread 0 -> 50 bps). G9 the within-ladder turnover spread is live (7.42x/yr).

  **RULE 8 AND BOTH KEEP PATHS.** Every pick chosen on IS 2009-2016 ALONE, OOS 2017-2026 read ONCE, separately at each rung. U56 picks: **4b full 6/8 and 4b OOS 6/8 at 0 and 10 bps, 2/8 at 25, 3/8 at 50; 4a 0/8 at every rung.** B136: **0/8 at 10, 25 and 50 bps** (1/8 at 0 bps on the OOS legs only). Whole grid, 146 cells: 4b full **35 / 33 / 29 / 21** and 4b OOS **39 / 36 / 30 / 27** at 0 / 10 / 25 / 50 bps; **4a 0 of 146 at every rung.** Benchmarks: U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; RULES v2 live full 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762 / -12.05%; B136 SPY full 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767 / -33.72%; RULES v2 OOS 7.88% / 1.1059 / -12.24%. **NOTHING IS PROPOSED and no memo is written:** every 4b-passing pick at PROTOCOL's 10 bps is an object the record already holds — 1086's PARKed N=12/H=21 candidate (16.80% / 1.1625 / -19.48%, OOS 17.57% / 1.1426 / -19.48%) and the standing top-20 anchor (15.58% / 1.1397 / -19.13%, OOS 16.97% / 1.1643 / -19.13%, c\* = 117 bps, 1094's own figure reproduced). The one new observation is filed as an idea, not a proposal: at 25 and 50 bps the CADENCE chooser moves to QUARTERLY (1.65x/yr turnover) and that cell clears 4b full and OOS at both rungs (15.11% / 1.1203 / -19.94% at 25 bps; 14.64% / 1.0891 / -19.94% at 50) — but no chooser selects it at the 10 bps PROTOCOL actually fixes, so it is not rule-8 reachable there.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels: every level is optimistic, every 4b/4a count an UPPER bound and every c\* an UPPER bound on the cost a real book of this kind could have paid. The PICK half contrasts two selections over the same inflated tape and the bias very largely cancels out of it; it does NOT cancel out of the 4b legs, measured against SPY, a real index.

  Script `research/backtests/2026-09-16_is-RULE-8-REACHABILITY-a-COST-RUNG-object-across-the-record-s-COMMITTED-PICKS_cloud.py`, 8 CSVs, console log, 4 LEADERBOARD rows. Follow-ups filed: 1099 (can any honest chooser reach the quarterly cell at 10 bps), 1100 (should a pick whose margin is below the seed floor be published as a TIE SET), 1101 (is 2-of-4 ladder-dependence a level or a coincidence).

- 2026-09-16 (lane C, idea 1094 does-the-U56-n12-H21-CANDIDATE-survive-25-and-50-bps) — **ANSWERED = YES, IT SURVIVES BOTH, AND THE QUEUE'S COST-EXPOSURE PREMISE IS KILLED. The candidate clears every 4b leg full-sample AND out of sample at 25 and at 50 bps, and breaks at 63 bps — on the HALVES SHARPE leg, not the CAGR floor. But the run replaces the objection it removed with a sharper one: the candidate's rule-8 REACHABILITY dies between 10 and 15 bps, roughly FIVE TIMES earlier than the cell itself. Still PARK, still NOT proposed, no RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: lane C takes the SECOND open idea; 1094 was the second line under '## Open' and names no EDGAR / Form 4 / 8-K / options / live-data source.

  **THE TWO TUNED DIALS AND NO MORE (PROTOCOL rule 4).** N {5, 8, 10, 12, 15, 20, 25, 30, 40} x H {21, 63, 126} — 1082/1086's grid verbatim — = 27 cells per panel, 54 in total, ALL published at ALL 11 published rungs (594 (cell, rung) pairs). **COST IS NOT A DIAL THE BOOK CAN CHOOSE.** It is PROTOCOL rule 2's execution assumption; nothing here is selected on it, the rule-8 choosers are re-run SEPARATELY at every rung (each sees only the cost its own book pays), and the verdict is quoted at the harshest rung the queue asked for. Everything else frozen at 936/1064/1071/1082/1086's construction: cap INF, CAND20 legs, max_vol 0.60, gross 0.75, W cadence, LAG 1.

  **THE COST LADDER IS EXACT, NOT RE-RUN.** `engine.backtest` computes `r = (held*rets).sum - turnover * c/1e4` and neither `held` nor `turnover` depends on cost, so one run per cell gives the whole ladder exactly. **G1/G1b/G1c check this against `engine.backtest` at 10, 25 and 50 bps at 1.39e-17** — which is what licenses the 201-rung fine ladder (0..200 bps in 1 bp steps) used to solve each cell's BREAKEVEN rung c\*.

  **THE ANSWER.** U56 n=12, H=21 at 0 / 10 / 25 / 50 / 75 bps: CAGR **17.64% / 16.80% / 15.54% / 13.48% / 11.46%**, Sharpe **1.2131 / 1.1625 / 1.0865 / 0.9595 / 0.8322**, MaxDD **−19.45% / −19.48% / −19.51% / −19.61% / −19.75%**, halves **1.301/1.150 → 1.248/1.102 → 1.167/1.029 → 1.033/0.907 → 0.898/0.785**, OOS **18.45%/1.1919 → 17.57%/1.1426 → 16.26%/1.0685 → 14.11%/0.9448 → 12.00%/0.8209**. Against SPY (full 15.10% / 0.8829 / −33.72%, halves 0.9588/0.8207; OOS 15.21% / 0.8711 / −33.72%; DD cap 20.23%, CAGR floor 10.57%) it **passes all five 4b legs and all three OOS legs at every rung up to and including 63 bps**, and fails at 64 on **L_H1** (H1 Sharpe falling under SPY's 0.9588), with **L_CAGR still clearing by +0.9 pp at 75 bps**. c\*_OOS = 64 bps. The pass set is contiguous from 0 at **54 of 54 cells** (H_MONO PASS), so "breakeven rung" is a well-defined object here. **H_DIE25 and H_DIE50 both FAIL; H_LEG FAILS.** The rival reading declared in advance — that a book binding on drawdown at 10 bps would die on the CAGR floor as cost rose — is wrong: cost reaches the **Sharpe** legs first, because SPY pays no turnover and the book's vol barely moves.

  **THE QUEUE'S 2.4x TURNOVER PREMISE BUYS ONE BASIS POINT.** H_QUEUE technically PASSES — c\*(12,21) = **63** bps against c\*(12,126) = **64** bps — but the gap is **1 bp on a 2.4x turnover ratio** (7.19x/yr vs 3.04x/yr), because the two cells bind on different legs: the H=21 cell on L_H1, the H=126 cell on **L_DD**, which cost barely moves. **D1 quantifies why:** over 0→50 bps the mean cell loses **1.66 to 4.69 pp of CAGR** and gains only **0.07 to 0.51 pp of |MaxDD|** — a ratio of **8x to 42x**. A book whose binding leg is drawdown is nearly cost-immune on that leg; the rung is a statement about the Sharpe and CAGR legs alone.

  **THE MOST COST-ROBUST CELL IN THE FAMILY IS THE INCUMBENT, NOT THE CANDIDATE.** U56 **n=20, H=126** — 936/1071's own construction — clears 4b full and OOS at **every rung to 117 bps** (c\*_OOS 149), at 2.90x/yr turnover: 50 bps leaves it at **14.25% / 1.0535 / −19.35%**. **H_FAMILY FAILS** (c\* spread across the nine cells that pass at 0 bps is **109 bps**, not ≤10), so the rung is emphatically NOT a family-turnover fact: c\* runs 8 / 36 / 38 / 40 / 50 / 63 / 64 / 68 / 117 bps across the passing cells, and **H_CSTAR_TURN PASSES at Spearman(c\*, turnover) = −0.65** — churn predicts cost fragility, but loosely, and the binding leg predicts it better.

  **GATES 12 of 12, printed before any result number.** G1/G1b/G1c the exact cost ladder == `engine.backtest` at 10/25/50 bps, 1.39e-17; G2 CROSS-RUN 936/1071/1082's committed W/H126 N=20 triple 3.18e-07; G3 SPY OOS triple 1.70e-04; **G4 CROSS-RUN 1086's committed candidate — both triples, full and OOS — at 0.00e+00, bit-identical**; G4b/G4c its committed turnovers 7.19x and 3.04x/yr at 3.6e-04 and 6.4e-04; G5 live RULES v2 MaxDD == −12.05% at 4.95e-05; G6 CAGR non-increasing in the rung at all 54 cells, 0.00e+00; G7 determinism 0.00e+00; G8 the cost axis is live (9.11 pp of U56 CAGR spread). **HYPOTHESES 7 of 11** — H_DIE25, H_DIE50, H_LEG and H_WF50 fail, and the first three failures ARE the result.

  **RULE 8, AND THE NEW OBJECTION.** (N, H) chosen on IS 2009-2016 ALONE by three choosers at each of 11 rungs, OOS 2017-2026 read ONCE. **H_WF25 PASSES (1 of 6 picks at 25 bps); H_WF50 FAILS (0 of 6).** The reachable band is **0 to 30 bps**: at 15 bps and above **U56's IS-Sharpe chooser stops picking the candidate** and switches to (n=5, H=63), which fails 4b on drawdown at every rung; from 15 to 30 bps the only surviving reachable pass is C_ISDD's **(n=40, H=21)** (25 bps: OOS 12.18% / 1.0636 / −19.37%), and by 40 bps that chooser moves to (40, 63) and nothing is reachable at all. **So the property 1086 headlined — "the first cell in this family an honest IS-only procedure reaches" — is itself the most cost-fragile thing about the candidate, dying at roughly a fifth of the rung the cell dies at.** **H_HOLD PASSES 6 of 6:** every (panel, chooser) sequence moves weakly to LONGER holds as the rung rises, which is the queue's mechanism showing up where it actually lives — in the CHOICE, not in the cell. **H_4A PASSES: 0 of 594 (cell, rung) pairs clear 4a**, at any cost, on either panel. Grid totals, U56: 4b full **8 / 7 / 7 / 5 / 4 / 1** at 0 / 10 / 25 / 40 / 50 / 75 bps; B136: **1 / 1 / 1 / 1 / 1 / 0**, the lone survivor being (15, 126), c\* = 68 bps. Benchmarks: U56 RULES v2 live **8.62% / 1.2007 / −12.05%** at 10 bps, **8.33% / 1.1631 / −12.09%** at 25, **7.85% / 1.1004 / −12.16%** at 50; B136 SPY full 15.16% / 0.8861 / −33.72%, OOS 15.33% / 0.8767 / −33.72%.

  **D2 — THE CANDIDATE'S COST ROBUSTNESS IS NOT A COIN FLIP.** 40 gross-matched random-rank draws at the same cell (1082/1086's seed recipe, same hold, no eligibility gate) turn over **12.91x/yr** median against the book's 7.19x and clear 4b at **0.050 / 0.025 / 0.000** at 0 / 10 / ≥15 bps; the median null c\* is **9 bps** against the book's **63**, and the book sits at percentile **1.000** of the null c\* distribution. Whatever else is wrong with this candidate, its survival to 63 bps is not something a random book of the same shape does.

  **D3 — THE BENCHMARK ASYMMETRY, BRACKETED RATHER THAN ASSERTED.** PROTOCOL's 4b bar is SPY buy-and-hold, which pays no turnover at any rung, so raising the rung is a one-sided handicap (the record's open idea 1063). Charging SPY the BOOK's own turnover at the same rung — the opposite extreme — flips **no verdict** in the eight (panel, cell, rung) pairs examined: U56 (12,21) and (12,126) pass under both conventions at 25 and 50 bps, B136's fail under both. The convention is one-sided; on this question it is not load-bearing.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists. Every level is optimistic, every 4b count an UPPER bound, and **every breakeven rung c\* published here is an UPPER bound on the cost a real book of this shape could have paid** — the survivorship bias flatters the numerator of the margin and not the turnover in the denominator.

  **WHERE THIS LEAVES THE CANDIDATE.** 1086's memo gave five reasons not to promote. This run **removes reason 5** (cost exposure: the 2.4x turnover ratio costs it 1 bp of breakeven, and it absorbs 50 bps with all eight legs intact) and **adds a better one**: what an IS-only procedure can REACH is far more cost-fragile than what the cell delivers, and above 10 bps the candidate is a cell no honest procedure selects. Reasons 1-4 (undecidable DD margin, panel fragility, unstable hold choice, 0 of 4a) stand untouched. **PARK, memo updated, NOT proposed for capital.**

  Script `research/backtests/2026-09-16_does-the-U56-n12-H21-CANDIDATE-survive-25-and-50-bps_C.py`, 8 CSVs, console log, memo, 5 LEADERBOARD rows. Follow-ups filed: 1096 (is rule-8 REACHABILITY a cost-rung object across the record's committed picks), 1097 (is c\* predictable from binding-leg margin / turnover alone), 1098 (are the record's committed cost claims priced on the CAGR leg when the SHARPE legs bind first).

- 2026-09-16 (cloud lane, idea 1086 is-the-EDGE-HUMP-a-MIN-HOLD-artefact) — **ANSWERED = IT SPLITS BY PANEL, AND THE HOLD IS NOT A MONOTONE DIAL. A PARTIAL KILL of 1082's CEILING verdict on B136, a KILL of the queue's STALENESS mechanism on U56, a CORRECTION to the queue's implied monotone hold dial, and a 4b candidate that an IS-only chooser ACTUALLY REACHES — PARKED, memo written, NOT proposed. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the LAST eligible open idea; 1086 was the last line under '## Open' and names no EDGAR / Form 4 / 8-K / options / spin-off / live-data source.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** N {5, 8, 10, 12, 15, 20, 25, 30, 40} — 1082's ladder verbatim — x H {21, 63, 126} = **27 cells per panel, 54 in total, ALL published** for the book AND the null. **SEEDS ARE FROZEN AT 40** (they were 1082's second dial, not one here). Everything else at 936/1064/1071/1082's construction: cap INF, CAND20 legs, max_vol 0.60, gross 0.75, W cadence, 10 bps, LAG 1. **THE NULL DRAWS ARE PAIRED ACROSS H BY CONSTRUCTION:** 1082's seed recipe `md5(panel, N, "INF", s)` carries no H term, so the same random rank matrix is re-used at every hold and the H=126 column is BIT-IDENTICAL to 1082's — which is what gate G5 checks.

  **THE ANSWER, AND IT IS TWO DIFFERENT ANSWERS.** **On B136 the queue is RIGHT.** The EDGE peak moves LEFT as the hold shortens — argmax n = **10 / 5 / 5** at H = 126 / 63 / 21 (**H_LEFT PASS**) — and **at H = 63 the ladder is MONOTONE DECREASING**, +11.269 / +8.822 / +7.663 / +5.182 / +4.473 / +3.352 / +2.133 / +1.439 / +0.398 over n = 5..40 (**H_HUMP FAIL, the only one of six (panel, H) cells that fails**). That is exactly the SLOPE premise 1082 killed, alive at a shorter hold: **1082's CEILING verdict is partly an artefact of its own frozen H = 126.** **On U56 the queue is WRONG.** Argmax reads **12 / 8 / 12** (**H_LEFT FAIL**), the hump survives at every hold, and shortening H makes it SHARPER rather than flatter: **EDGE(12) - EDGE(5) goes from +0.870 +/- 0.507 pp (1.72 SE, NOT decisive, 1082's own number) at H = 126 to +1.696 +/- 0.574 pp (2.96 SE, DECISIVE) at H = 21.** Under staleness a 21-day hold should SHRINK that gap; it grows. **H_E5 passes on both panels but means opposite things** — B136 EDGE(5) runs 5.717 -> 11.269 -> 8.074, U56 5.950 -> 6.637 -> **5.967**, a rise of +0.016 pp against a 0.383 pp seed SE.

  **THE THIRD OUTCOME, NAMED IN ADVANCE, ALSO FAILS.** It was declared before the run that a shorter hold might simply raise EDGE at EVERY n (a pure level effect) without moving the peak. dEDGE = EDGE(n, 21) - EDGE(n, 126) reads **U56 mean +0.184 pp (7 of 9 rungs positive, but the n=5 rung moves +0.016)** and **B136 mean -1.135 pp (1 of 9 positive)**. A shorter hold does not lift the edge uniformly; on B136 it LOWERS it everywhere except n = 5, which is precisely why the peak moves there.

  **A CORRECTION — THE HOLD IS NOT A MONOTONE DIAL.** H = 63 is not between H = 21 and H = 126 on either panel: U56's argmax reads 12 / 8 / 12, and B136's EDGE(5) is HIGHEST at the middle hold (+11.269, against +8.074 at H=21 and +5.717 at H=126). Any reading of the form "as H shortens, X" is unavailable from a three-point ladder on this tape, and the strongest version of the queue's claim — monotone in H — is supported by NEITHER panel. Filed as idea 1095.

  **GATES 10 of 10, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G1b `gross_rescaler(1.0)` == `nrun` 1.39e-17; G2 CROSS-RUN 936/1071/1082's committed W/H126 N=20 triple 3.18e-07; G3 SPY OOS triple 1.70e-04; G4c |MaxDD| of the REBUILT null monotone in lambda 0.00e+00; **G5 CROSS-RUN all 18 of 1082's committed H=126 EDGE rungs reproduce to 4.80e-03 pp, the rounding of its own published two decimals**; G5b 1071/1082's U56 N=20 null median (CASH) 2.20e-04; G6 live RULES v2 MaxDD == -12.05% 4.95e-05; G7 determinism 0.00e+00; G8 the H dial is LIVE (U56 turnover spread 5.74x/yr). **HYPOTHESES 11 of 14** (H_LEFT[U56], H_HUMP[B136,H=63] and H_ISHOLD fail, and all three failures are the result).

  **RULE 8, BOTH KEEP PATHS, AND THE CANDIDATE.** (n, H) chosen JOINTLY on IS 2009-2016 alone by four choosers over the 27 cells, OOS 2017-2026 read ONCE. **H_WF PASSES, 2 of 8 picks clear 4b OUT OF SAMPLE** — U56 C_ISSHARPE picks **n=12, H=21** (OOS 17.57% / 1.1426 / -19.48%) and C_ISDD picks n=40, H=21 (13.01% / 1.1286 / -19.27%); U56's C_ISEDGE and C_ISCAGR and all four B136 choosers pick n=5, H=63, which fails 4b on drawdown. **H_4A PASSES — 0 of 54 cells clear 4a**, so the live book's drawdown leg is a book fact, not an (n, H) fact, exactly as 1082 found. **H_ISHOLD FAILS: U56's four choosers split 21/21/63/63 and B136's all pick 63** — the hold is not stably choosable out of sample even where n is. Grid totals **U56 4b full 7 of 27, 4b OOS 7 of 27, 4a 0 of 27; B136 4b full 1 of 27, 4b OOS 2 of 27, 4a 0 of 27**; all eight full-sample passes bind on DRAWDOWN by +0.061 to +1.103 pp. Benchmarks: U56 SPY full 15.10%/0.8829/-33.72% (halves 0.9588/0.8207), OOS 15.21%/0.8711/-33.72%; RULES v2 live full 8.62%/1.2007/-12.05%, OOS 9.45%/1.2762/-12.05%; B136 SPY OOS 15.33%/0.8767/-33.72%, RULES v2 OOS 7.88%/1.1059/-12.24%.

  **THE CANDIDATE, AND WHY IT IS PARKED.** U56 **n = 12, H = 21**: full **16.80% / 1.1625 / -19.48%**, halves **1.248 / 1.102**, OOS **17.57% / 1.1426 / -19.48%**, turnover **7.19x/yr** (72 bp/yr of drag at the binding 10 bps, already charged). It clears all five 4b legs full-sample and all three out of sample, and U56's IS-only Sharpe chooser picks it from 2009-2016 alone — **the first cell in this family that any honest IS-only procedure reaches; 1082 found three 4b passes and reached none of them.** It is PARKED, NOT proposed, on two grounds: **(1) the binding leg is not decidable at this sample length** — its drawdown margin is **+0.753 pp** against the 20.23% cap, and idea 1083 measured the 90% width of exactly this quantity on exactly this tape at **4.1-7.2 pp**; **(2) it does not survive the panel change** — the identical construction on B136 reads 16.41% / 0.9840 / -26.84% and fails 4b outright, and B136's own four choosers all pick (5, 63), which also fails. A memo with the exact RULES wording it WOULD take is written at `2026-09-16_is-the-EDGE-HUMP-a-MIN-HOLD-artefact_cloud.memo.md` so Sunday review has it in hand. **This run does not recommend it for capital.**

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists. Every level is optimistic and every 4b/4a count an UPPER bound. The book-vs-null contrast is drawn from the same pool over the same tape and the bias very largely cancels out of EDGE; it does NOT cancel out of the 4b legs, which are measured against SPY, a real index — so the parked candidate is flattered by it, which is a third reason not to propose it.

  Script `research/backtests/2026-09-16_is-the-EDGE-HUMP-a-MIN-HOLD-artefact_cloud.py`, 8 CSVs, console log, memo, 5 LEADERBOARD rows. Follow-ups filed: 1093 (is the B136 H=63 monotone ladder a genuine slope or a single-rung accident), 1094 (does the n=12/H=21 candidate survive 25 and 50 bps, at 2.4x the incumbent hold's turnover), 1095 (is the H dial non-monotone on a finer hold ladder).

- 2026-09-16 (cloud lane, idea 1087 does-the-1-plus-g-times-d-RENORMALISATION-bias-any-committed-CADENCE-claim) — **ANSWERED = YES, AND IT BITES 2.26x HARDER ON A STEP THAN ON A LEVEL — BUT ONLY ON CONVERSION, WHICH CORRECTS THE QUEUE'S OWN PREMISE. KILL of H_STEPLIN and H_STEPREC, a CORRECTION, a CENSUS, and a KILL of this run's OWN declared direction. No book proposed, no RULES change, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: the cloud lane takes the FIRST eligible open idea; 1087 was the first line under '## Open' and names no EDGAR / Form 4 / 8-K / options / spin-off / live-data source.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** CADENCE STEP {D->W, D->M, D->Q, W->M, W->Q, M->Q} x GROSS RUNG {0.25, 0.50, 0.75, 1.00} = 24 dial cells, reported at all 10 (panel, book) cells = **240 published step-points**, off a 160-point turnover ladder. REPORTED and never fitted on: panel {U56, B136}; book {TOP5, TOP10, TOP20, EWELIG, BAND03} — idea 930/1076's set verbatim; cadence {D, W, M, Q}; source {LEADERBOARD, CHANGELOG, QUEUE, RESULTMD}. The record's own ladder is {0.50, 0.75, 1.00}; 0.25 widens the curve and no verdict is read off it alone.

  **THE ANSWER.** `max |(STEP(g)/g)/STEP(1.00) - 1|` reads **1.1831e-01** over the full ladder and **6.4801e-02** over the record's own, against 1076's LEVEL residuals of 5.3116e-02 / 2.8691e-02 reproduced here to 9.08e-06 — an **AMPLIFICATION of 2.227x / 2.259x**. Per rung **@0.25 1.183e-01 / @0.50 6.480e-02 / @0.75 2.464e-02 / @1.00 0**. **H_STEPLIN and H_STEPREC both FAIL** at a 1% bar; **H_AMPLIFY PASSES**, so the queue's clause that the residual does not cancel on a difference is confirmed in direction and priced. The worst cells are the family 1076 already flagged: **BAND03 `D->W` @ g=0.50** (B136 +6.48e-02, U56 +5.68e-02) — the slowest-turning book differenced against the fastest cadence, where drift between decisions has most room.

  **THE BIAS, PRICED.** `BIAS(g->g') = STEP(g)*g'/g - STEP(g')` at the binding 10 bps: over **360 conversions on the record's own ladder, max 4.357 bp/yr, median 0.2346**; over 720 on the full ladder, max 6.646, median 0.3063. By step `D->Q` 4.357 / `D->M` 3.870 / `D->W` 2.669 / `W->Q` 1.689 / `W->M` 1.202 / `M->Q` 0.487 bp/yr. **H_RANK PASSES:** the cadence ordering reads `D > W > M > Q` at all four rungs in **10 of 10** cells — the residual moves sizes, never signs, so no committed "M turns over less than W" claim can be reversed by the rung it is read at.

  **A CORRECTION TO THE QUEUE'S OWN PREMISE.** The queue says every committed cadence comparison differences two books at one gross "so the residual does not cancel". **At the gross it was measured at, a cadence gain is EXACT** — it is the difference of two numbers `engine.backtest` actually produced, and no linearity is assumed, so there is nothing to cancel or fail to cancel. The residual enters only when the gain is **READ AT ANOTHER GROSS**, which is precisely what the 0.924 of committed turnover/drag figures 1076 found unstamped invite. The queue's framing implied a bias where none exists and missed the one that does.

  **THE CENSUS.** Corpus **30,615 committed units**; **312** turnover/drag figures sit in a cadence-bearing unit — **20 STEP** (an explicit `X -> Y`), **143 MULTI** (>= 2 cadences named), **149 ONE** (a level, carried so the denominator is visible). The **163 cadence gains** carry a numeric gross **10 times (0.061)**, and **0 of 20** in the STEP tier do. Against their own stated precision (half a unit in the last printed digit): **MOVED at the median cell 58 of 163 (0.356), at the worst cell 131 of 163 (0.804), not moved even there 32 (0.196)**. **H_PRECISION PASSES (0.644 survive the median bias)** — but it passes because the record prints coarse, not because the tape is kind: every figure printed to one decimal or finer in bp/yr is moved at the median cell. **HONEST LIMIT, stated rather than papered over: 0.939 of these figures state no gross, so no figure can be matched to a cell; the bias is priced over the WHOLE cell population and both the median and the max are published.**

  **GATES 7 of 9, printed before any result number.** G1 fast runner == `engine.backtest` on returns AND turnover 4.16e-16; G2 cost linearity at 5/25 bps 2.08e-17; G3 SPY OOS triple 2.98e-05; G4 live RULES v2 MaxDD == -12.05% at 4.95e-05; G5 CROSS-RUN `turn_yr` vs 1076's committed `linearity.csv` over **160 shared cells at 7.11e-15**; G6 1076's committed 5.312e-02 / 2.87e-02 reproduce at 9.08e-06; G7 determinism 0.00e+00. **G8a AND G8b FAIL AND ARE THIS RUN'S OWN DECLARATION.** It was declared before the run that `REL_step(g)` would be NEGATIVE and that `step/g` would be monotone DECREASING in g. The two halves contradict each other — if `step/g` falls in g then at a low rung it sits ABOVE its own g=1.00 reference, so `rel` is POSITIVE by construction (mean **+8.766e-03**) — and the monotonicity half fails on its own terms: **32 of 60 cells**, where 1076's LEVEL held on **40 of 40**. Differencing two monotone curves of different curvature need not be monotone. Both failures are published beside the right answer rather than replaced.

  **RULE 8 AND BOTH KEEP PATHS.** (cadence, gross) chosen JOINTLY on IS 2009-2016 Sharpe ALONE over the 16 combinations per (panel, book), OOS 2017-2026 read ONCE. **4b full-sample on the IS pick 2 of 10, 4a 0 of 10, 4b OUT OF SAMPLE 1 of 10**; whole 160-cell grid **4b 11 of 160, 4a 0 of 160, 4b-OOS 16 of 160. H_WF PASSES.** Benchmarks: U56 SPY full 15.10%/0.8829/-33.72% (halves 0.9588/0.8207), OOS 15.21%/0.8711/-33.72%; RULES v2 live full 8.62%/1.2007/-12.05% (halves 1.2322/1.1760), OOS 9.45%/1.2762/-12.05%; B136 SPY OOS 15.33%/0.8767/-33.72%, RULES v2 OOS 7.88%/1.1059/-12.24%. **NOTHING IS PROPOSED AND NOTHING IS NEW:** the single OOS-4b pick is U56 `BAND03 M@1.00` (full 11.89%/1.1729/-18.81%, OOS 12.81%/1.2243/-18.81%), the live band construction at full gross — committed as `BAND03@g1.00` and **refused on 4a for drawdown four times over**, with 1076 committing the same daily/weekly/monthly picks one run ago and this run reproducing them. The next-strongest grid pass, U56 `TOP20 M@0.75` (full 15.28%/1.2125/-19.51%, OOS 17.53%/1.3061/-19.51%), is the record's single most-claimed cell. Both paths are scored because rule 4 requires it on every run. **No memo written.**

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels. The census half is a statement about committed TEXT and is unaffected; the tape half is flattered exactly as every other run on these panels is, and every 4b count above is an UPPER bound. The residual itself is a ratio of two turnover figures off the same inflated tape, so the bias very largely cancels out of it; the 4b legs, measured against SPY, get no such cancellation.

  Script `research/backtests/2026-09-16_does-the-1-plus-g-times-d-RENORMALISATION-bias-any-committed-CADENCE-claim_cloud.py`, 8 CSVs, console log, 5 LEADERBOARD rows. Follow-ups filed: 1090 (is the step residual's non-monotonicity a BAND03 fact), 1091 (how many committed cadence gains are quoted finer than the record can measure), 1092 (does the 2.26x amplification appear on a COST-RUNG difference too).

- 2026-09-16 (lane C, idea 1076 should-every-published-TURNOVER-or-DRAG-figure-carry-its-GROSS) — **ANSWERED = YES, AND ALMOST NOTHING IN THE RECORD DOES: 57 of 751 committed turnover/drag figures (0.076) state a numeric gross within 200 characters of themselves, 23 of 751 (0.031) in their own clause. Plus a CORRECTION to this idea's own premise — gross is NOT an exact linear scale on turnover — and a CONFIRM of idea 930's reconciliation, exact on the ladder medians. No book promoted, no RULES change, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: lane C takes the SECOND open idea; 1075 went to lane A and 1076 names no EDGAR / Form 4 / 8-K / options / live-data source, so it runs in the sandbox.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** CLAIM SET {STRICT = a committed unit carrying a turnover/drag token AND a figure in a convertible unit; WIDE = the token and >= 1 numeral} x GROSS CONVENTION {AT100 per unit of gross, AT075 the live book's gross, LADMED the median over the record's own {0.50, 0.75, 1.00}} = 6 cells, ALL published. REPORTED and never fitted on: stamp window {80 chars, 200 chars, whole unit}; source {LEADERBOARD, CHANGELOG, QUEUE, RESULTMD}; panel {U56, B136}; book {TOP5, TOP10, TOP20, EWELIG, BAND03} — idea 930's own set reused verbatim; cadence {D, W, M, Q}; gross ladder {0.25, 0.50, 0.75, 1.00}, where 0.25 exists only to widen the linearity test and no verdict is read off it.

  **THE CENSUS.** Corpus **30,542 committed units** (LEADERBOARD rows, CHANGELOG paragraphs, QUEUE lines, 856 `*.result.md` by paragraph); **2,309 carry a turnover-or-drag token, 751 carry a figure in a convertible unit.** Stamped with a NUMERIC gross: STRICT **0.031 / 0.076 / 0.168** at the 80-char, 200-char and whole-unit windows; WIDE **0.093 / 0.107 / 0.137**. The most generous reading anyone could defend — the bare word `gross` anywhere in the unit — tops out at **0.349**. By source at 200 chars: LEADERBOARD 28/261 (0.107), RESULTMD 23/341 (0.067), QUEUE 6/149 (0.040), and **CHANGELOG ZERO: as committed this file is 6,054 bytes / 9 paragraphs (rotated to the latest entries) and carries no turnover figure at all** — worth knowing before any future run treats it as "the record" (filed as idea 1085).

  **THE RE-EXPRESSION.** 694 unstamped valued figures (148 in `bp/yr`, 546 in `x`) re-expressed under all three conventions x the record's own ladder as the assumed measurement gross, per figure, in `..._C.reexpress.csv`. Two things the dials revealed rather than measured, both results and not bugs: **WIDE adds NOTHING** to the re-expression (1,558 of the 2,309 units carry the token and a numeral but no figure in a convertible unit — you cannot re-express what was never stated in a unit), and **LADMED is EXACTLY AT075** because median{0.50, 0.75, 1.00} == 0.75 == the live book's gross, so the record has **two** gross conventions here, not three.

  **THE CORRECTION.** The queue text says "gross scales turnover linearly". It does not, exactly. `max |(turn(g)/g)/turn(1.00) - 1|` over 40 (panel x book x cadence) cells reads **5.312e-02**, per rung **@0.25 5.31% / @0.50 2.87% / @0.75 1.08% / @1.00 0** (H_LINEAR FAIL); converting with `v(g') = v(g)*g'/g` is wrong by up to **5.31%** over 480 ordered pairs and **2.87%** over the record's own ladder, median 0.69% (H_CONVERT FAIL). The mechanism was declared before the run: `engine.backtest` renormalises the drifted row by `V = 1 + g*d`, so a larger gross deflates it more and pulls it back toward target, making `turn(g)/g` FALL. **G6a (direction) PASSES on 40 of 40 cells; G6b (magnitude, declared under 1%) FAILS at 5.31% and the wrong half is published beside the right one rather than replaced.** The worst cells are all `BAND03` DAILY — the slowest-turning book, where drift between decisions has the most room. **So the stamp removes a factor-of-2.000 ambiguity and leaves a <= 2.87% one**, which is why the clause proposed in the memo asks for the gross a figure was MEASURED at rather than a normalised number.

  **H_930 — CONFIRM.** Read on 930's own grid (its five books, W/M/Q only, its ladder; this run's daily rung excluded so the comparison is like for like): **930's ladder medians reproduce EXACTLY — W 109.6 (-0.0%), M 48.9 (+0.0%)** — and **926's own 161 / 73 bp/yr reproduce as the @1.00 column at +4.7% / +1.3%**, inside the 5% bar. G5 reproduces 930's committed `dragcurve.csv` turnover over **90 shared cells at 3.55e-15**.

  **GATES 6 of 7, printed before any result number.** G1 fast runner == `engine.backtest` on returns AND turnover 4.16e-16; G2 cost linearity at 5 and 25 bps 2.08e-17; G3 SPY OOS triple 2.98e-05; G4 live RULES v2 MaxDD == -12.05% at 4.95e-05; G5 as above; G6a PASS; **G6b FAIL, published.** HYPOTHESES 4 of 6 (H_STAMP, H_CLAIMSET, H_930, H_WF pass; H_LINEAR and H_CONVERT fail and are the correction above).

  **RULE 8 AND BOTH KEEP PATHS.** Gross chosen on IS 2009-2016 by IS Sharpe ALONE, OOS 2017-2026 read ONCE, 40 picks. **4b full-sample 5 of 40, 4b OUT OF SAMPLE 4 of 40, 4a 0 of 40, whole ladder 4b 11 of 160.** Benchmarks: U56 SPY full 15.10%/0.8829/-33.72% (halves 0.9588/0.8207), OOS 15.21%/0.8711/-33.72%; RULES v2 live full 8.62%/1.2007/-12.05%, OOS 9.45%/1.2762/-12.05%; B136 SPY OOS 15.33%/0.8767/-33.72%, RULES v2 OOS 7.88%/1.1059/-12.24%. The four OOS-4b picks are U56 TOP20 weekly @0.75 (OOS 14.35%/1.1246/-18.31%) and U56 BAND03 daily/weekly/monthly @1.00. **NOTHING IS PROPOSED:** the strongest is the live band construction at full gross, already committed as `BAND03@g1.00` (OOS 12.68% / full 11.54%) and **refused on 4a for drawdown four times over**; this run reads 12.67% / 11.53%, an independent reproduction to 0.01 pp. Both paths are scored here because rule 4 requires it on every run, not because this run found a book.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels. The census half is a statement about committed text and is unaffected; the tape half (drag levels, the linearity residual, the rule-8 numbers) is flattered exactly as every other run on these panels is, and every 4b count above is an UPPER bound.

  Script `research/backtests/2026-09-16_should-every-published-TURNOVER-or-DRAG-figure-carry-its-GROSS_C.py`, 8 CSVs, console log, 4 LEADERBOARD rows. Follow-ups filed: 1084 (does the `1 + g*d` renormalisation move any committed CADENCE claim, since it is largest on the slowest books), 1085 (is CHANGELOG.md's rotation silently shrinking every record census), 1086 (how many committed figures in units OTHER than turnover are per-unit-of-gross too).

- 2026-09-16 (lane B, idea 1082 does-the-SELECTION-EDGE-COLLAPSE-in-n-price-the-INCUMBENT-n=20-or-just-EXPOSE-it) — **ANSWERED = NEITHER. THE EDGE IS A HUMP AND n=20 SITS ON ITS FAR SIDE. KILL of the queue's own SLOPE premise (H_SLOPE FAIL on both panels), KILL of "n=20 is a chosen optimum" (H_TOP PASS on both), and a PARK stated plainly and NOT proposed. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: lane B takes the LAST open idea; 1082 was the last line under '## Open' and is the first in many runs that prices an actual capital book rather than the record's own text.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** N {5, 8, 10, 12, 15, 20, 25, 30, 40} x SEEDS {5, 10, 20, 40} (nested — the first S of the same 40 draws, so the seed dial moves PRECISION and nothing else) = 36 cells per panel, 72 total, ALL published. Everything else frozen at 936/1064/1071's construction: cap INF, CAND20 legs, max_vol 0.60, gross 0.75, W cadence, min hold 126, 10 bps, LAG 1. N = 20/25/30/40 reproduce 1071's ladder exactly so the two runs read against each other.

  **THE ANSWER.** EDGE(n) = book CAGR minus the median CAGR of that rung's OWN gross- and holding-count-matched null, every null path REBUILT at the gross that equalises its realised |MaxDD| with the book's. It runs **U56 +5.95 / +5.35 / +6.21 / +6.82 / +5.22 / +5.12 / +2.95 / +1.55 / +0.41 pp** and **B136 +5.72 / +5.24 / +8.32 / +6.52 / +6.83 / +4.93 / +4.27 / +2.81 / +1.85** over N = 5..40. **The peak is n=12 (U56) and n=10 (B136) — not 5, and not 20.** Against n=20 the peak is **+1.70 +/- 0.44 pp (3.9 SE)** and **+3.39 +/- 0.42 pp (8.1 SE)**: the record genuinely stopped short, and the queue's suspicion that n=20 was never tested against anything smaller is correct. But the queue's mechanism is wrong. **EDGE does NOT keep rising to n=5**: peak minus EDGE(5) is +0.87 +/- 0.51 pp (1.7 SE, NOT decisive) on U56 and +2.61 +/- 0.58 pp (4.5 SE, decisive) on B136. The pre-declared rival — a CEILING, where the top of the ranking is thin while idiosyncratic variance explodes — is what the tape shows.

  **TWO SUPPORTING FACTS, both of which cut against a simple reading.** (i) **H_DD FAILS:** |MaxDD| is not monotone in n but **U-shaped** (U56 -25.85% at n=5, -19.13% at n=20, -22.98% at n=40), so the book pays for concentration at one end and for common-factor drawdown at the other — 1071's "these drawdowns are common-factor" seen from the other side. (ii) **POST-RUN D2, labelled as post-run:** lambda is constrained to (0, 1], so a null draw already DRIER than the book enters unmatched at lambda = 1 with its CAGR understated, which INFLATES EDGE. That clip binds on **0.57 / 0.60 of draws at n=5 and 0.72 / 0.45 at n=40** and on 0.00-0.38 in between — i.e. it inflates exactly the two rungs the hump verdict needs BELOW the peak. The bias runs AGAINST the conclusion and the hump survives it anyway.

  **GATES 11 of 11, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G1b `gross_rescaler(1.0)` == `nrun` 1.39e-17 (the bisection kernel is the same book, not an approximation of it); G2 CROSS-RUN 936/1071's committed W/H126 N=20 triple 3.18e-07; G3 SPY OOS triple 1.70e-04; G4c |MaxDD| of the REBUILT null monotone in lambda 0.00e+00; G5a/G5b CROSS-RUN 1071's committed N=20 book CAGR and null median 3.18e-07 / 1.26e-07 — **1071's headline +5.07 pp reproduces exactly**; G6 live RULES v2 MaxDD == -12.05% 4.95e-05; G7 determinism 0.00e+00; G8 the n dial is live (6.73 pp of U56 |MaxDD| spread). **G4 REPAIRS 1071's ACKNOWLEDGED FAILURE:** 1071 declared that scaling a null path by lambda equals rebuilding it at gross lambda*g, then published that the declaration was wrong because the cash sleeve does not drift. This run computes both conventions at every rung and prices the gap at **at most 0.044 pp of CAGR** — the assertion is still wrong, but on this ladder it is immaterial, and both columns are published so no one has to take that on trust.

  **RULE 8 (N chosen on IS 2009-2016 ALONE by four choosers, OOS 2017-2026 read ONCE), AND THE PARK.** **H_WF FAILS 0 of 8 picks.** C_ISSHARPE and C_ISDD pick n=40 on U56 (OOS 14.00% / 1.0983 / -22.98%) and n=8 / n=30 on B136 (14.56% / 0.7866 / -24.54% and 16.61% / 1.0696 / -24.35%). **C_ISEDGE — this idea's OWN selector — picks n=5 on BOTH panels and is the worst pick on B136** (OOS 15.03% / 0.7687 / -28.12%, Sharpe below SPY's 0.8767), and **H_ISEDGE FAILS** (IS-chosen 5 against full-sample argmax 12 / 10). Every pick fails 4b OOS on the drawdown leg. Benchmarks: SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588 / 0.8207), OOS 15.21% / 0.8711 / -33.72%; RULES v2 live full 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762 / -12.05% (B136: SPY 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767 / -33.72%; RULES v2 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059 / -12.24%). **4a 0 of 18 rungs** (H_4A PASS — no n clears the live book's drawdown; that leg is a book fact, not an n fact). **4b full-sample 3 of 18** — U56 n=12 (17.71% / 1.1692 / -20.17%, halves 1.275/1.088), U56 n=20 (15.58% / 1.1397 / -19.13%) and B136 n=15 (16.78% / 1.0682 / -19.66%) — all three also clearing their three OOS legs. **All three bind on DRAWDOWN, by 0.06 / 1.10 / 0.57 pp against the 20.23% cap**, and 1083 measured the 90% width of exactly this quantity at 4.1-7.2 pp on this tape. Not decidable, and not reachable by any honest IS-only procedure: **PARK, NOT KEEP, NOT proposed, no memo written.** The edge below n=20 is real and decisive against its own null; it is not capital.

  **SEED DIAL.** Rank-correlation of the EDGE ladder against S=40 runs 0.90-1.00 at every S, and the B136 argmax reads n=10 at all four seed counts. **H_SEED FAILS on one cell only** (U56 argmax 12, 5, 12, 12 at S = 5, 10, 20, 40): the peak's LOCATION is stable to +/- one rung, its exact argmax flickers below 20 seeds, and no one should quote an argmax from 5 or 10 seeds.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every level is optimistic and every 4b count an UPPER bound. The book-vs-null contrast is drawn from the same pool over the same tape and the bias very largely cancels out of EDGE; it does NOT cancel out of the 4b legs, which are measured against SPY, a real index — so the three PARKed passes are flattered by it.

  Script `research/backtests/2026-09-16_does-the-SELECTION-EDGE-COLLAPSE-in-n-price-the-INCUMBENT-n=20-or-just-EXPOSE-it_B.py`, 8 CSVs, 4 LEADERBOARD rows. Follow-ups filed: 1084 (is the EDGE argmax choosable out of sample at all), 1085 (rank-matched null instead of a uniform one), 1086 (is the hump a min-hold artefact).

- 2026-09-16 (cloud lane, idea 1083 is-the-RESIDUAL-MISS-at-the-GROSS-OPTIMUM-DECIDABLE-at-this-SAMPLE-LENGTH) — **ANSWERED = NO. "THE WINDOW IS EMPTY" IS NOT A CLAIM THIS TAPE CAN CARRY. KILL of 1064's and 1071's empty-window VERDICT as an established fact — re-read as UNRESOLVED; CONFIRM of the realised numbers themselves, which reproduce to 2.8e-05; a CORRECTION to the queue text's own premise about which leg binds; and a PARK flagged and explicitly NOT proposed. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the LAST eligible open idea; every open line the lane passed over between 979 and 1080 is a census of the record's committed TEXT / NUMBERS or needs local/Actions data (429 still PARK — data/ carries only volume_small.csv.gz), so none can carry this run's mandatory rule-8 walk-forward or either KEEP path.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** BLOCK LENGTH L {21, 63, 126, 252} x DRAWS D {200, 1000} = 16 cells, ALL published, on a fully published 10-rung gross ladder (0.30..0.75) at 936/1064/1071's W/H126 top-20 construction. One circular block index per replicate is applied JOINTLY to SPY, the LIVE RULES v2 book and all ten rungs, because every term of MISS = max(|MaxDD_book| - |MaxDD_live|, 0.70*CAGR_SPY - CAGR_book) is measured on the same tape and a book-only resample would hold both bars fixed and understate the interval.

  **DECIDABILITY RULE, FIXED BEFORE THE RUN:** the claim is decidable at 90% iff the 5th percentile of MISS over the draws is > 0; a straddle means UNRESOLVED.

  **THE ANSWER.** On **MISS\***, the min-over-rungs statistic 1064 and 1071 actually published, the empty-window verdict is **DECIDABLE IN 0 OF 16 CELLS**. U56 90% intervals run **[-0.97, 3.80] -> [-0.75, 3.64] -> [-0.26, 3.41] -> [-0.25, 3.51]** over L = 21 / 63 / 126 / 252 at D=1000; B136 **[-0.89, 4.44] -> [-0.32, 3.21]**; **P(some rung opens the window) 0.076 to 0.165** in every cell. At the single rung g\* the verdict is decidable in only **9 of 16** cells and it **MOVES WITH THE BLOCK LENGTH** (U56/D=1000 reads F,F,T,T; B136/D=200 reads F,T,T,F — **H_STABLE FAIL**) and **WITH THE DRAW COUNT** (4 of 8 (panel, L) cells flip between 200 and 1000 — **H_DRAWS FAIL**). **H_WIDTH PASSES and is the cleanest statement of the result: the 90% width of MISS at g\* runs 4.1 to 7.2 pp against a realised MISS of 1.00 pp** — the record has been adjudicating a one-point quantity with a five-point ruler.

  **A CORRECTION TO THE QUEUE TEXT'S OWN PREMISE.** 1083's own wording says both optima bind on the 4b CAGR floor rather than the DD cap. On a 10-rung ladder that holds only for B136 (g\*=0.45, CAGR leg 1.01 pp vs DD leg 0.58). **The U56 optimum MOVES from g=0.45 to g=0.50** (MISS 1.270 -> **1.002 pp**) once the ladder is refined from 1071's five rungs to ten, and there **the DD leg binds** (1.01 pp against the CAGR leg's 0.23 pp). Across draws **DD binds on 0.718** pooled (0.841 U56, 0.583 B136) — **H_CAGRLEG FAIL.** 1071's "the CAGR floor binds" was a property of its RUNG SPACING, not of the book, and is corrected here by the run that inherited it.

  **GATES 6 of 7, printed before any result number.** G1 CROSS-RUN 936's committed W/H126 triple 3.18e-07; G2 fast runner == `engine.backtest` 1.39e-17; G3 SPY OOS triple 1.70e-04; G5 1071's committed optimum MISS, B136 2.76e-05 and U56 2.68e-01 (the U56 gap is the ladder refinement above, not an error, and is reported as such); G6 live RULES v2 MaxDD == committed -12.05% at 4.95e-05. **G4 FAILS AND THE DECLARED DIRECTION WAS BACKWARDS:** it was declared in advance that block resampling would BREAK long declines and bias bootstrapped |MaxDD| SMALL; it does the opposite, CONCATENATING bad blocks, and the bias reads **+1.54 / +0.87 / +0.05 / +0.00 pp at L = 21 / 63 / 126 / 252** (vanishing monotonically as the worst episode comes to fit inside one block). **The wrong declaration is published beside the failure rather than replaced**, and the direction is stated because it matters: the bias INFLATES MISS and therefore pushes the verdict TOWARD "empty" — the window still cannot be shown empty, so the failure is conservative for the conclusion.

  **RULE 8 (gross chosen on IS 2009-2016 MISS ALONE, OOS 2017-2026 read ONCE), AND THE PARK.** U56 picks g=0.50 -> OOS **11.25% / 1.1633 / -13.06%**; B136 picks g=0.45 -> OOS 9.62% / 1.0071 / -12.82%. Benchmarks: SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588 / 0.8207), OOS 15.21% / 0.8711 / -33.72%; RULES v2 live full 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762 / -12.05% (B136: SPY OOS 15.33% / 0.8767 / -33.72%, RULES v2 OOS 7.88% / 1.1059 / -12.24%). **H_WF PASSES 1 of 2: the U56 pick clears 4b OUT OF SAMPLE outright**, and full-sample it clears **four of 4b's five legs** (H1 1.2030 and H2 1.0960 against SPY's 0.9588 / 0.8207; MaxDD -13.06% against the -20.23% cap), **failing ONLY the CAGR floor, by 0.23 pp** (10.34% against 10.57%); it fails 4a on drawdown by 1.01 pp. It is filed **PARK, NOT KEEP, and is not proposed**: 4b's bar is full-sample and it does not clear it, and this run's own headline is that a margin that size is not decidable on this tape — which cuts against promoting it exactly as hard as it cuts against declaring it dead. **4a 0 of 20 rungs. No book promoted, no memo written.**

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every level is optimistic, every MISS published here is a LOWER bound on the true one and every 4b count an UPPER bound. The bootstrap resamples the same inflated tape and prices SAMPLING error only — it cannot and does not correct that bias, and the PARK above is flattered by it.

  Script `research/backtests/2026-09-16_is-the-RESIDUAL-MISS-at-the-GROSS-OPTIMUM-DECIDABLE-at-this-SAMPLE-LENGTH_cloud.py`, 6 CSVs, 4 LEADERBOARD rows. No follow-ups filed beyond 1081/1082 (already open from this run's idea 1): the natural next question — walk n below 20 — is 1082.


- 2026-09-16 (cloud lane, idea 1108 is-BOOK-DISTANCE-rather-than-RUNG-COUNT-what-a-LADDER-s-FLOOR-measures) — **ANSWERED = NO ON THE PREMISE, YES ON THE QUESTION. KILL of 1108's own premise (book distance is not what the floor measures, and the sign runs backwards); CORRECTION to 1102's H_LADDER reading; a REPLACEMENT for the bootstrap on two of four statistics, pre-registered on three legs and passing all three; and a DEFECT in 1102's seeding. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the FIRST eligible open idea.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** DISTANCE STATISTIC {RETCORR, OVERLAP, TURNGAP} x LADDER {N, H, GROSS, CADENCE} = 12 cells, ALL published, on both panels. PANEL is not a dial. BLOCK LENGTH is not a dial: headline L=63, L in {21, 126} reported beside it and never selected on (tie sets 26 / 26 / 26 of 32 at all three). Everything else frozen at 1082/1086/1094/1098/1102's construction.

  **THE KILL.** 1102 read its own H_LADDER failure as "the 9-rung N ladder resolves better than the 4-rung H ladder because its rungs are further-apart BOOKS". Measured directly, that is backwards: **Spearman(rel_floor, adjacent-pair book distance) is POSITIVE in 12 of 12 (distance statistic, statistic) combinations, median +0.7214** — the CLOSER a ladder's books, the BETTER it resolves. **H_DIST FAILS 0 of 12; H_BEATS_COUNT FAILS 1 of 12** (rung count is the stronger predictor, median |rho| 0.8460 against distance's 0.7214). The mechanism is visible in one cell: **the GROSS ladder's ten rungs are the SAME BOOK scaled against a cash sleeve — RETCORR distance 0.0000, OVERLAP distance 0.0000, the smallest any ladder can carry — and it is the ONLY ladder that resolves anything** (median rel_floor 0.109 against 0.961 / 1.000 / 1.000 for N / H / CADENCE; 2 of 8 tie sets against 8 of 8 for each of the other three). It resolves a 0.0002 Sharpe gap while the N ladder cannot resolve a 0.0071 one. **H_RHO PASSES and names the confusion: Spearman(SE_analytic, sqrt(2(1-rho_ij))) = +0.8710 over 558 rung pairs** — distance is (most of) what the SE is made of, exactly as 1108 supposed, but the floor is a GAP-AGAINST-SE quantity and distance enters BOTH terms. 1108 forgot the numerator.

  **THE REPLACEMENT, PRE-REGISTERED BEFORE ANY NUMBER.** An analytic A_ij = Phi(|gap|/SE) built ONLY from the two rungs' own series replaces the bootstrap for a statistic iff it reproduces (i) >= 0.95 of pair RESOLVED verdicts, (ii) >= 0.90 of cell TIE-SET verdicts, and (iii) a floor within 1.5x in median — **all three legs.** The SE is EXACT IN SIGN for CAGR (CAGR is a monotone map of the resampled log-sum) and a delta-method influence function for Sharpe. **H_REPLACE_CAGR PASSES (0.9839 / 1.0000 / 1.0000)** and **H_REPLACE_SHARPE PASSES (0.9624 / 1.0000 / 1.0000)**: **24 of 24 cell tie-set verdicts reproduced with no bootstrap at all, at a median floor ratio of 1.0000.** **S_OOS does NOT replace** — its pair leg reads 0.8871, below the bar; the SEs themselves still agree (Spearman(SE_boot, SE_analytic) 0.9982 / 0.9995 / 0.9993 on CAGR / S_FULL / S_OOS), so what fails is the NORMAL approximation over the shorter OOS window, not the SE. **H_NO_DD, declared and not measured:** MaxDD is a path functional, the resampled ORDER decides it, and no pairwise SE from marginal moments can carry it — **MaxDD keeps the bootstrap, and is where the bootstrap still earns its cost** (6 of 8 DD cells are tie sets under it).

  **THE DEFECT, and it is a record-wide one.** 1102 seeds each ladder with `SEED_BOOT + hash((panel, ladder, L)) % 10_000`. Python randomises `str` hashing per process — `hash(("U56","N",63)) % 10000` returned **8249 / 1895 / 1659** on three consecutive interpreters here — so **1102's floor.csv cannot be regenerated by running 1102.** Re-drawing this run's own floors under 4 seeds moves rel_floor by up to **0.7007 of the ladder spread** (U56 GROSS S_FULL: 0.780 / 0.299 / 1.000 / 0.913); 2 of 32 cells move by more than 0.10, median range 0.0000. **D2, cross-run against 1102's committed floors (L=63, q=0.90): this run's bootstrap reproduces 30 of 32 exactly, its analytic floor 22 of 24, and TIE-SET VERDICTS AGREE 32 of 32.** The honest reading, and the one filed: **1102's CONCLUSIONS survive its own irreproducible seed; its published floor LEVELS should not be quoted to four decimals.** 1102 is corrected, not retracted. This run uses `zlib.crc32` seeds and is reproducible.

  **D1, POST-HOC AND LABELLED.** n = 8 ladders is small and GROSS is an extreme point on BOTH axes at once (lowest distance AND most rungs), so distance and rung count are anti-aligned by construction and this design cannot separate them. Dropping GROSS leaves 6 ladders and two distinct rung counts (9 and 4); on S_FULL every remaining rel_floor is exactly 1.000, so both rank correlations are undefined. **Neither predictor is identified on what remains** — the KILL rests on the sign and on the GROSS mechanism, not on a fitted slope, and is stated that way.

  **GATES 9 of 9, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 CROSS-RUN 936/1071/1082/1094/1102's committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS triple 1.70e-04; G4 1098/1102's committed U56 n=12 triple 4.97e-05 and G4b its B136 n=15 triple 2.13e-05; G5 live RULES v2 MaxDD == -12.05% 4.95e-05; G6 determinism 0.00e+00; **G7 the analytic pair SE == the bootstrap pair SE to 3.26e-02 relative (0.01188 vs 0.01149) — the gate the whole replacement claim rests on, printed before the claim**; G8 the ladders are live (Sharpe spread 0.1916).

  **RULE 8 (rung chosen on IS 2009-2016 ALONE, per ladder, three choosers, OOS read ONCE) AND BOTH KEEP PATHS.** **4 of 24 picks clear 4b full AND OOS; 0 of 24 clear 4a.** All four are the standing anchor (U56 / W / H=126 / N=20 / gross 0.75, full 15.58% / 1.1397 / -19.13%, halves 1.2037/1.0971, OOS 16.97% / 1.1643 / -19.13%), reached from the GROSS and CADENCE ladders only — independently reproducing 1096's D3 and 1102's own 2-of-4 reachability. Whole grid, 54 cells: **4b full 16, 4b OOS 17, 4a 0.** Benchmarks: U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; U56 RULES v2 live 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762 / -12.05%; B136 SPY full 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767 / -33.72%; B136 RULES v2 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059 / -12.24%. **Nothing new, nothing promoted, no memo written.**

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every level is optimistic. A rung-to-rung DISTANCE and a rung-to-rung GAP both contrast two books over the same inflated tape and the bias very largely cancels out of them and out of the floor; it does NOT cancel out of the 4b legs, which are measured against SPY, a real index, so the 16 full-sample 4b passes are an UPPER bound.

  Script `research/backtests/2026-09-16_is-BOOK-DISTANCE-rather-than-RUNG-COUNT-what-a-LADDER-s-FLOOR-measures_cloud.py`, 11 CSVs, 4 LEADERBOARD rows.

- 2026-09-16 (cloud lane, idea 1107 RE-PRICE-every-committed-ARGMAX-on-an-H-or-CAP-LADDER) — **ANSWERED = NONE. NOT ONE of the record's 26 committed H-axis or cap-axis EDGE ARGMAXES survives both tests. KILL of the published H/cap argmax as a claim, CORRECTION to the record's own decisiveness bar, and CONFIRM of 1097 in full. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the LAST eligible open idea.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** CLAIM SET {CORE, WIDE} x FLOOR BASIS {SEED, TAPE, JOINT} = 6 cells, ALL published. CORE = the 26 committed EDGE ladders this run rebuilds exactly (1086's 18 H ladders, 2 panels x 9 N over H in {21,63,126}; 1071's 8 cap ladders, 2 panels x 4 N over cap in {1.00,1.25,1.50,2.00,INF}). WIDE = the 33 VALUED H-or-cap argmax claims harvested from 1,009 corpus files. THE CELL COORDINATES ARE NOT DIALS — (panel, N, H, cap, seeds, DD-match convention) are taken exactly as committed. BLOCK LENGTH is not a dial: headline L=63, L in {21,126} reported beside it and reading 0 of 26 at ALL THREE.

  **THE ANSWER.** On the headline **TAPE** basis (1098/1102/1108's block-bootstrap sign-resolution floor at q=0.90): **0 of 26 CORE ladders and 0.0 of 33 harvested claims survive both the null gate and their own floor.** On the **JOINT** basis (seed and tape noise in quadrature): **0 of 26.** On the record's **OWN** bar, the seed SE of the null median that 1082 / 1085 / 1097 argued their headlines against: **9 of 26 (0.346).** **The maximum gap-to-floor ratio over all 26 ladders is exactly 1.000** — no committed argmax on either axis clears its own floor even once. Median TAPE floor **1.1493 pp** against a median committed gap of **0.6758 pp**, median gap/floor **0.614**. **18 of 26 ladders resolve NOTHING** (their floor is the whole ladder spread), and only **46 of 134** rung pairs across all 26 ladders are sign-resolved at q=0.90, median pair agreement **0.833** — below the bar.

  **THE FLOOR IS THE BINDING TEST, NOT THE NULL GATE. H_FLOOR_BINDS PASSES: floor-only failures 14, gate-only failures 0**, both fail 12, survive 0. The rank-matched null never kills an argmax the floor would have spared, so 1097's 13-of-34 gate finding — real, and reproduced here exactly — was the smaller of the two problems. **H_SHORT PASSES:** 0 of 18 three-rung H ladders clear their floor, and the 5-rung cap ladders read 0 of 8 as well, so this is not only a rung-count artefact.

  **H_BASIS FAILS AND THAT IS THE CORRECTION.** The three bases agree on the survival verdict for only **0.654** of the 26 ladders, so "survives its floor" is a BASIS statement, not a tape statement — and the disagreement is structural, not noise. The seed SE prices how far the NULL MEDIAN would move if the rank draws were redrawn and prices **nothing at all** about how far the book and the null TOGETHER would move if the TAPE were redrawn. Every committed decisiveness call on these two axes was made with the second term missing, and the 9 SEED-basis survivors (U56 H at N = 8, 20, 25, 40; B136 H at N = 5, 8, 15, 25, 30) are exactly the claims that term kills.

  **THE DECLARED APPROXIMATION, AND ITS DIRECTION.** Inside the bootstrap the DD-match multiplier lambda is held at its full-sample value (re-solving it per draw would cost 26 x 1000 x k x m bisections and was not run); the resampled null CAGR is then EXACT from block log-sums of the lambda-scaled series. **Declared in advance: holding lambda fixed REMOVES a source of draw-to-draw variation, so the TAPE floor published here is a LOWER bound and every survival count is an UPPER bound.** The bias runs TOWARD survival and nothing survives anyway.

  **GATES 11 of 11, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; **G1b `gross_rescaler(1.0)` == `nrun` 1.39e-17** (the bisection kernel is the same book, not an approximation of it); **G1c the CAPPED build == `engine.backtest` 1.39e-17 — 1071's arm**; G2 CROSS-RUN 936/1071/1082/1097's committed W/H126/N=20 triple 3.18e-07; G3 SPY OOS triple 1.70e-04 (U56) / 4.05e-05 (B136); G4 live RULES v2 MaxDD == committed 4.95e-05 / 9.85e-06; **G5 CROSS-RUN reproduces ALL 94 of 1097's committed EDGE_OPEN and EDGE_ELIG figures at 1.78e-15**; G6 its committed book CAGRs at 8.33e-17; **G7 CROSS-RUN reproduces 1097's committed argmax_OPEN and argmax_ELIG on all 26 H/cap ladders, 26 of 26, at 0.00e+00.** TEST 1 therefore reproduces 1097's gate counts exactly: argmax MOVED in 12 of 26 (H 7 of 18, cap 5 of 8), UNCHANGED in 14.

  **RULE 8 AND BOTH KEEP PATHS — NOTHING PROPOSED.** EDGE is not a KEEP path and dial 2 cannot move one: the BOOK at every cell is byte-identical across the two gates, only the comparand changes, so 4a and 4b are invariant to it by construction; scored anyway because rule 4 requires it. Rung chosen on IS 2009-2016 ALONE, per ladder, three choosers, OOS read once: **16 of 78 picks clear 4b full AND OOS; 0 of 78 clear 4a.** Whole grid, 94 committed cells: **4b full 18, 4b OOS 19, 4a 0.** Every passing pick is a cell the record already holds and has already PARKED — U56 H=21 at N = 10/12/15/30/40, the standing anchor U56 H=126 N=20 (full 15.58% / 1.1397 / -19.13%, halves 1.2037/1.0971, OOS 16.97% / 1.1643 / -19.13%), the U56 cap ladder at N=20 and B136's at N = 20/25. Benchmarks: U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; U56 RULES v2 live 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762 / -12.05%; B136 SPY OOS 15.33% / 0.8767 / -33.72%; B136 RULES v2 OOS 7.88% / 1.1059 / -12.24%. **Nothing new, nothing promoted, no memo written.**

  **A LIMIT OF THE WIDE ARM, STATED.** All 33 harvested claims sit on the **H** axis; the harvester found **ZERO** prose argmax claims naming a cap value inside its window, against 3,691 rejects. The record argues the cap axis in CSVs and in prose that never puts a cap value next to the claim word, so the WIDE arm cannot reach it and the cap verdict rests entirely on the 8 rebuilt CORE ladders. Every reject is published with its reason and context.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels. EDGE, the gate and the floor are all within-pool contrasts over the same inflated tape and the bias very largely cancels out of them; it does NOT cancel out of the 4b legs, measured against SPY, a real index, so the 18 full-sample 4b passes are an UPPER bound.

  Script `research/backtests/2026-09-16_RE-PRICE-every-committed-ARGMAX-on-an-H-or-CAP-LADDER_cloud.py`, 11 CSVs, 4 LEADERBOARD rows.
- 2026-09-16 (lane C, idea 1110 what-does-the-RECORD-LOSE-if-the-FLOOR-CLAUSE-is-ENACTED-AS-WRITTEN) — **ANSWERED, AND BOTH ALTERNATIVES ARE WORSE. The clause as written retains 0.359 of the record's CORE argmax content at q=0.90 and leaves 18 of 32 committed argmaxes with none at all; publishing the argmax as a POINT with its gap and floor beside it is DOMINATED, not a trade; COARSENING the ladder loses more than its wider gaps win; and LENGTHENING THE TAPE wins the literal decision rule only as an unattainable projection. KILL of both queue alternatives, a CORRECTION to 1102's committed tie-set column, and a CORRECTION to this run's OWN declared decision rule. Clause still PROPOSED and NOT enacted (rule 6). No RULES change, no book promoted, no PROTOCOL edit; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: lane C takes the SECOND open idea; 1110 was the second line under '## Open' and names no EDGAR / Form 4 / 8-K / options / live-data source.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** CLAUSE FORM {AS_WRITTEN, ANNOTATED, WIDER_RUNGS, LONGER_TAPE} x CONFIDENCE q {0.80, 0.90, 0.95} = **12 combinations, ALL published**, plus two variants reported beside their headline and never selected on (WIDER_RUNGS_ENDS; LONGER_TAPE at M = 4 and 8). PANEL, LADDER and STATISTIC are not dials — all 2 x 4 x 4 = 32 CORE cells are reported everywhere. BLOCK LENGTH is not a dial (L=63, 1098/1102/1108's headline). Everything else frozen at 1082/1094/1098/1102/1108's construction. Seeds are `zlib.crc32` — 1108's repair of 1102's process-random `hash()` seeds.

  **WHAT EVERY FORM IS SCORED ON, pre-registered before any number.** A published argmax claim on a k-rung ladder is an ASSERTION SET: it names a set S and EXCLUDES the other k - |S| rungs. **DECIDED** = k - |S| (CONTENT = DECIDED/(k-1)); **EXP_FALSE** = sum over excluded j of (1 - A_peak,j) from the block bootstrap, the expected number of WRONG exclusions published. ERR = EXP_FALSE/DECIDED; **PRICE = DECIDED/EXP_FALSE** is "decidable claims per unit of honesty" read literally. DECISION RULE fixed in advance: among the forms CALIBRATED at q (ERR <= 1-q), the most DECIDED wins.

  **THE ANSWER (q=0.90, 32 CORE cells, 184 decidable rungs).** DECIDED / EXP_FALSE / ERR / PRICE: **AS_WRITTEN 66 (0.359) / 1.41 / 0.0213 / 46.9**, 18 cells contentless, CALIBRATED. WIDER_RUNGS 39 (0.212) / 0.96 / 0.0245 / 40.8, 19 contentless. **ANNOTATED 184 (1.000) / 34.34 / 0.1866 / 5.4 — NOT calibrated.** LONGER_TAPE (M=2, PROJECTED) 90 (0.489) / 1.60 / 0.0178 / 56.1. At q = 0.80 / 0.95 the clause retains 0.516 / 0.293 and leaves 8 / 23 cells contentless. **The literal rule answers LONGER_TAPE at q = 0.90 and 0.95, and that answer is unattainable:** M=2 is 34 years of tape on panels that begin in 2008, and the tape multiple a cell needs for its OWN committed gap to clear its OWN floor has median **12.5x = 220 years**, 8 of 32 not clearing at M <= 500. **Among ENACTABLE forms AS_WRITTEN wins on content AND on price.**

  **BOTH QUEUE ALTERNATIVES, KILLED ON THEIR OWN TERMS. H_ANNOT_PRICE FAILS and its failure is the result:** the point argmax buys 2.8x the content at **24x the wrong exclusions** (34.34 against 1.41) — PRICE 5.4 against 46.9 — so it is dominated, not a trade, and per cell it is calibrated at **NO** confidence (24 / 26 / 26 of 32 cells over their own bar at q = 0.80 / 0.90 / 0.95). **H_COARSE_LOSES PASSES:** keeping every 2nd rung decides 39 against 66, because coarsening leaves a mean 2.50 rungs per cell UNADJUDICATED — a claim cannot rule out what it no longer measures — and ENDS-only decides 10. **H_TAPE_HALF PASSES on a MEASURED exponent, not an assumed one:** floors re-drawn on disjoint sub-tapes at f = 0.25 and 0.50 give a median fitted beta of **-0.4540** over the 24 non-DD cells against 1/sqrt(T)'s -0.50; DD is exempt by declaration and runs the other way (+0.2093), a path functional whose own level grows with the tape.

  **D1 — THE CORRECTION TO 1102, AND IT RUNS AGAINST THE CLAUSE.** 1102's committed `tie_set` column is `mass_set(counts, rungs, 0.90)`: the smallest CONTIGUOUS set of rungs carrying 0.90 of the bootstrap ARGMAX MASS, computed once at a fixed 0.90 and written into every q row. That is not the set the clause publishes and it never consults q — **it moves between q = 0.80 and 0.95 in 0 of 32 cells while 1102's own capped floor moves in 15.** The queue's premise "4.88 rungs / 0.851 of its ladder / 12 WHOLE-LADDER" reproduces EXACTLY as that mass-set figure over the 26 TIE cells only (4.885 / 0.851 / 12). The clause's own FLOOR set over all 32 cells at q=0.90 is mean **4.69 of 6.75 rungs (0.787)** with **18 of 32 CONTENTLESS**, and it does move with q (17 of 32 cells; 3.78 / 4.69 / 5.06 rungs at 0.80 / 0.90 / 0.95). **Enacted as written the clause costs MORE than 1102 published.** 1102 is corrected, not retracted: its VERDICTS reproduce here 32 of 32.

  **D2 — THE CORRECTION TO THIS RUN'S OWN DECLARED RULE.** The rule declared in (f) tests the AGGREGATE error share, and that test is WEAKER than a per-cell one: a long ladder's far rungs resolve almost surely and dilute the near ones. ANNOTATED is aggregate-calibrated at q=0.80 (0.1866 <= 0.20) and per-cell calibrated at no q. **Under the per-cell reading the q=0.80 answer changes from ANNOTATED to AS_WRITTEN; q=0.90 and 0.95 do not change.** Both readings are published; the per-cell one is the honest bar and is named as such.

  **THE CORPUS LAYER (harvest inherited from 1102, rates re-derived here).** Re-weighted onto 1102's 200 harvested VALUED argmax claims, CONTENT retained at q=0.90: **AS_WRITTEN 46.9 of 200 (0.234)**, WIDER_RUNGS 29.5 (0.147), LONGER_TAPE (projected) 70.8 (0.354), ANNOTATED 200 (1.000, uncalibrated). 134 claims are scored by their own (family, statistic) cell; 24 + 42 carry a TRANSFERRED rate, declared in advance as an EXTRAPOLATION and NOT a re-derivation of those claims (1048/1102's convention). **The record loses roughly three quarters of its published argmax content if the clause is enacted — and that is the cheapest honest option available to it.**

  **THE DECLARED APPROXIMATION, AND ITS DIRECTION.** LONGER_TAPE is a PROJECTION: A' = Phi(sqrt(M) Phi^-1(A)) assumes a fixed population gap and an SE falling as 1/sqrt(T). **Both assumptions run TOWARD resolution** — a longer tape would also move the gaps, and a regime it has not seen can only widen the null — so every LONGER_TAPE count here is an **UPPER bound** on what lengthening the tape would buy, and it still loses once its infeasibility is priced. 118 of 224 sub-tape window-cells (18 of 32 on the full tape) carry a floor CAPPED at the ladder spread, where beta describes the SPREAD's scaling rather than the floor's; declared, not hidden.

  **GATES 9 of 9, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 CROSS-RUN 936/1071/1082/1094/1102/1108's committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS triple 1.70e-04; G4 1098/1102's committed U56 n=12 triple 4.97e-05 and G4b its B136 n=15 triple 2.13e-05; G5 live RULES v2 MaxDD == committed -12.05% at 4.95e-05; G6 determinism 0.00e+00; G7 the ladders are live (Sharpe spread 0.1916); **G8 CROSS-RUN of 1102's committed TIE-SET VERDICTS 32 of 32 and its committed PEAKS 32 of 32** — 1102's conclusions reproduce under repaired seeds, exactly as 1108 found.

  **RULE 8 AND BOTH KEEP PATHS — NOTHING PROPOSED.** No clause form can move a book: the BOOK at every cell is byte-identical across all four forms, only the PROSE changes, so 4a and 4b are invariant to dial 1 by construction; scored anyway because rule 4 requires it. Rung chosen on IS 2009-2016 ALONE, per ladder, three choosers, OOS read ONCE: **4 of 24 picks clear 4b full AND OOS; 0 of 24 clear 4a.** Whole grid, 54 cells: **4b full 16, 4b OOS 17, 4a 0** — independently reproducing 1108's counts. Every passing pick is the standing anchor U56 / W / H=126 / N=20 / gross 0.75 (full 15.58% / 1.1397 / -19.13%, halves 1.2037/1.0971, OOS 16.97% / 1.1643 / -19.13%), a cell the record already holds and has already PARKED. Benchmarks: U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; U56 RULES v2 live 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762 / -12.05%; B136 SPY full 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767 / -33.72%; B136 RULES v2 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059 / -12.24%. **Nothing new, nothing promoted, no RULES memo written.**

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every level is optimistic. A GAP between two rungs and an AGREEMENT between two rungs both contrast two books over the same inflated tape and the bias very largely cancels out of them and out of the floor; it does NOT cancel out of the 4b legs, measured against SPY, a real index, so the 16 full-sample 4b passes are an UPPER bound.

  Script `research/backtests/2026-09-16_what-does-the-RECORD-LOSE-if-the-FLOOR-CLAUSE-is-ENACTED-AS-WRITTEN_C.py`, 14 CSVs, console log, result memo, 4 LEADERBOARD rows. Follow-ups filed: 1114 (is the clause's own confidence q a free parameter the record never priced), 1115 (does the per-cell calibration bar change any committed decisiveness claim), 1116 (is un-resolvability a ladder property or a statistic property) — filed as 1111-1113 and renumbered on push after a lane collision, idea 932's defect again.
- 2026-09-16 (lane B, idea 1116 is-UN-RESOLVABILITY-a-LADDER-property-or-a-STATISTIC-property) — **ANSWERED = LADDER. Un-resolvability is a property of WHICH DIAL is being laddered, not of WHICH STATISTIC is read off it. KILL of the statistic reading, KILL of BOTH tempting explanations of the ladder one, and a CORRECTION to 1110's NO_TAPE_500 count. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: lane B takes the LAST eligible open idea.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** UNRESOLVABILITY DEFINITION {INF_FLOOR, ZERO_CONTENT, NO_TAPE_500} x CONFIDENCE q {0.80, 0.90, 0.95} = **9 combinations, ALL published**. LADDER and STATISTIC are NOT dials — they are the two candidate ANSWERS, and all 2 x 4 x 4 = 32 CORE cells are reported under every combination. PANEL is not a dial. BLOCK LENGTH is not a dial (L=63 headline, L in {21,126} reported beside, never selected on). Everything else frozen at 1082/1094/1098/1102/1108/1110's construction; seeds are `zlib.crc32`, 1108's repair. The 32 cells are REBUILT from the tape, not harvested from prose.

  **THE ANSWER.** The headline definition (INF_FLOOR, q=0.90) reproduces 1110's count exactly: **18 of 32 infinite floors**. As a 4x4 table per panel the ROWS are near-constant and the COLUMNS carry almost nothing — U56 rows N **0/4**, H **4/4**, GROSS **1/4**, CADENCE **4/4** against columns 2/4, 3/4, 2/4, 2/4; B136 rows N **2/4**, H **4/4**, GROSS **0/4**, CADENCE **3/4** against columns 3/4, 2/4, 2/4, 2/4. **H and CADENCE are un-resolvable at EVERY statistic on BOTH panels; N and GROSS at almost none.** Main-effects two-way decomposition: **ETA2_LAD 0.8095 / ETA2_STAT 0.0476 on U56 (17.0x)** and **0.5556 / 0.0476 on B136 (11.7x)**. Both threshold-free continuous readings agree and are stronger: REL_FLOOR **0.5752/0.1177** and **0.9738/0.0066**; log10(M_needed) **0.8127/0.0379** and **0.6727/0.0684**. **H_CONCORD PASSES** on the second, independent reading, whose two sides share the marginal base rate by construction: within-ladder agreement **0.8750 / 0.7083** against within-statistic **0.3750 / 0.3750**. **LADDER wins both panels in 8 of the 9 dial combinations, STATISTIC in 0 of 9**; the lone exception is NO_TAPE_500 at q=0.80 on U56, the sparsest table in the run.

  **BOTH TEMPTING EXPLANATIONS KILLED ON THEIR OWN TERMS. H_GROSS_CARRIES FAILS** — the ladder answer is not one ladder in disguise: GROSS is fully resolvable on B136 but 1 of 4 un-resolvable on U56, and only 2 (U56) and 1 (B136) of the three non-GROSS ladders are un-resolvable at all four statistics. Re-scored on 1110's OWN committed flags it still fails, on the B136 leg, so the FAIL is not a seed artefact — though the U56 leg of it is, and is named. **H_DD_WORST FAILS AND RUNS BACKWARDS** — DD is among the MORE resolvable statistics (mean REL_FLOOR **0.7381** / **0.7189**); the least resolvable is **S_OOS on U56 (0.9873)** and **CAGR on B136 (0.7777)**, not the same statistic on the two panels, which is the statistic reading failing on its own terms rather than merely losing.

  **D1/D2, POST-HOC AND LABELLED — THE CELL FLAG IS SEED-FRAGILE, THE AXIS ANSWER IS NOT.** This run draws its OWN bootstrap (SEED_BOOT 11161116 against 1110's 11101110), so the cross-run is an INDEPENDENT REDRAW, not a reproduction attempt. **G9: committed argmax gaps 32 of 32 EXACT, infinite-floor flags 30 of 32.** The two that flip are U56 N/CAGR and U56 GROSS/S_OOS; two more flip on NO_TAPE_500 at M = 359 and 255 against a 500 cap, so **NO_TAPE_500 reads 10 of 32 here against 1110's committed 8 — a CORRECTION to the precision at which that count should be quoted, and 1108's seed finding reproduced independently.** **STATED BECAUSE IT IS EASY TO MISREAD:** the U56 INF_FLOOR decomposition matches 1110's TO THE DIGIT while the underlying tables differ in 2 of 16 CELLS — the two flips are MIRROR-SYMMETRIC in BOTH margins, leaving SS_row, SS_col and SS_total each unchanged. That is an accident of this pair of flips, NOT evidence of seed-insensitivity. **D2 is the actual evidence: 3 further independent seed draws, LADDER wins the decomposition in 6 of 6 (seed, panel) redraws**, ETA2_LAD 0.5556-0.8095 against ETA2_STAT 0.0476-0.0667.

  **WHAT THE RECORD SHOULD DO WITH IT, STATED NARROWLY.** The axis the record should stop laddering is **H and CADENCE**, not any statistic: those two ladders decide NOTHING at any of the four statistics on either panel, so a published argmax on a min-hold or a cadence ladder is contentless whichever number is read off it, and re-reading it under a different statistic cannot rescue it. PROPOSED for the Sunday review, NOT enacted (rule 6), and filed as idea 1117.

  **GATES 10 of 10, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 CROSS-RUN 936/1071/1082/1094/1102/1108/1110's U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS 1.70e-04; G4 1098/1102's committed U56 n=12 4.97e-05 and G4b its B136 n=15 2.13e-05; G5 live RULES v2 MaxDD == -12.05% 4.95e-05; G6 determinism 0.00e+00; G7 the ladders are live (Sharpe spread 0.1916); **G8 CROSS-RUN 1102's committed TIE-SET VERDICTS 32 of 32**; **G9 CROSS-RUN 1110's committed argmax GAPS 32 of 32**.

  **RULE 8 AND BOTH KEEP PATHS — NOTHING PROPOSED.** No definition and no q can move a book: the BOOK at all 54 rungs is byte-identical across both dials, only which cells get CALLED un-resolvable changes, so 4a and 4b are invariant to dial 1 and dial 2 by construction; scored anyway because rule 4 requires it. Rung chosen on IS 2009-2016 ALONE, per ladder, three choosers, OOS read ONCE: **4 of 24 picks clear 4b full AND OOS; 0 of 24 clear 4a.** Whole grid, 54 cells: **4b full 16, 4b OOS 17, 4a 0** — independently reproducing 1108's and 1110's counts. Every passing pick is the standing anchor U56 / W / H=126 / N=20 / gross 0.75 (full 15.58% / 1.1397 / -19.13%, halves 1.2037/1.0971, OOS 16.97% / 1.1643 / -19.13%), a cell the record already holds and has already PARKED. Benchmarks: U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; U56 RULES v2 live 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762 / -12.05%; B136 SPY full 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767 / -33.72%; B136 RULES v2 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059 / -12.24%.

  **THE DECLARED APPROXIMATION, AND ITS DIRECTION.** NO_TAPE_500 rests on 1110's PROJECTION A' = Phi(sqrt(M) Phi^-1(A)): a fixed population gap and an SE falling as 1/sqrt(T). **Both assumptions run TOWARD resolution, so every M_needed here is a LOWER bound and every NO_TAPE_500 count a LOWER bound on un-resolvability.** The exponent is INHERITED from 1110's sub-tape measurement (median beta -0.4540 over the 24 non-DD cells, DD +0.2093) and is NOT re-measured here, so DD's M_needed is the least trustworthy quantity in this run and is named as such. The M_needed solver is EXACT rather than a grid sweep: with z = Phi^-1(A) and zq = Phi^-1(q), a pair is unresolved at M iff M < (zq/z)^2, so the un-resolved set changes only at those critical multiples — identical to the sweep by construction, and cross-checked against 1110's committed column in G9.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every level is optimistic. A rung-to-rung GAP and a rung-to-rung AGREEMENT both contrast two books over the same inflated tape and the bias very largely cancels out of them, out of the floor and out of every quantity decomposed here; it does NOT cancel out of the 4b legs, measured against SPY, a real index, so the 16 full-sample 4b passes are an UPPER bound.

  Script `research/backtests/2026-09-16_is-UN-RESOLVABILITY-a-LADDER-property-or-a-STATISTIC-property_B.py`, 10 CSVs, console log, result memo, 5 LEADERBOARD rows. Follow-ups filed: 1117 (should the PROTOCOL forbid publishing an argmax on an H or CADENCE ladder at all), 1118 (is the H/CADENCE un-resolvability a rung-count artefact after all — the two un-resolvable ladders are exactly the two 4-rung ones, confounded 2-2 by construction), 1119 (does the ladder-axis answer hold on the SMALL panel, where 1073 measured reliability 0.0915).
- 2026-09-16 (lane B, idea 1131 does-ANY-committed-FLOOR-KEYED-CLAUSE-in-the-record-survive-a-RUNG-SET-CHANGE) — **ANSWERED = YES, BUT ONLY A MINORITY, AND THE MINORITY IS A NAMED SHORT LIST. Of 1,451 committed floor-keyed objects on a ladder whose rung set can move, 623 are RUNG-ROBUST, 823 are RUNG-ESCAPABLE and ZERO are RUNG-INDUCED. At block level: 7 of 16 survive the CORE -> EXT move, and only 6 survive EVERY superset of CORE inside EXT — DD on all four blocks plus H/S_FULL on both panels, and nothing else. KILL of the 'add rungs' model of escape (the trigger is NOT monotone in rungs), and a CORRECTION to how the record's committed INF_FLOOR census should be quoted. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched.** SELECTION: lane B takes the LAST open idea; 1131 ended '## Open' and names no EDGAR / Form 4 / 8-K / options / live-data source. It has a PRICE LEG — 74 books over 4 ladders x 2 panels x 2 rung sets, block-bootstrapped at 3 seed bases — so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `CLAIM SET` {CS_CELL, CS_PROSE, CS_ALL} x `RUNG SET` {CORE, EXT} = **6 combinations, ALL published**. PANEL, LADDER and STATISTIC are not dials (all 2 x 4 x 4 rebuilt cells reported everywhere). **CONFIDENCE q is NOT a dial** — 0.90 headline, 0.80 and 0.95 reported beside it at every point and never selected on. **The ALL-vs-ANY reading of a multi-cell prose claim is NOT a dial either** — ALL is the record's own wording (1110/1116/1117) and is the headline, ANY is reported beside it and moves the escape share by at most 0.073. Frozen at 1082/1094/1098/1102/1108/1110/1116/1117/1118/1122's construction: CAND20 legs, cap INF, max_vol 0.60, gross 0.75, min hold 126, N=20, W, 10 bps, LAG 1, warm-up 260, IS end 2016-12-31, block L=63, 1000 draws, crc32 seeds, 3 seed bases.

  **THE STRUCTURAL FACT, STATED BEFORE THE CENSUS BECAUSE IT BOUNDS IT.** CORE and EXT differ **only** on H (4 -> 9) and CADENCE (4 -> 9); N (9 rungs) and GROSS (10 rungs) carry **identical** lists at both levels — the same defect 1134 found when it read 4 surviving GROSS cells as 2 books. **Any committed object keyed only on N or GROSS is rung-inescapable BY CONSTRUCTION** and is excluded from the headline denominator, which is why every count is published twice: raw, and restricted to objects whose rung set can actually move.

  **THE HARVEST, AND ITS HONEST LIMIT.** CSV arm **2,571 committed rows over 18 files** — every row of a committed `research/backtests/*.csv` carrying a `ladder` column and an INF-flag field, kept whether the flag fires or not so the denominator is honest. PROSE arm **9 committed clause / bar / verdict sentences** — a sentence qualifies only if it carries BOTH a whole-ladder trigger phrase AND a decision word, cells read from its committed LINE. **26,323 CSV rows rejected for carrying no INF_FLOOR field, 693 for carrying no flag on the row, and 263 prose sentences rejected (170 NO_LADDER_NAMED, 71 QUOTES_A_FLOOR_NO_DECISION, 22 SMALL panel). Every reject is published with its reason.** **THE LIMIT, NAMED: the prose arm is 9 objects** — the record argues its floors in CSVs and in prose that mostly does not put a ladder name in the same committed line as the trigger, so the weight of this run sits on the CSV arm and the prose verdict rests on a handful of sentences.

  **THE CENSUS (q=0.90, reading ALL, headline base).** CS_CELL 2,571 objects / 1,445 movers / ESCAPABLE 822 / ROBUST 623 (**0.5689**); CS_PROSE 9 / 6 / 1 / 0 (0.1667); CS_ALL 2,580 / 1,451 / 823 / 623 (**0.5672**), INDUCED **0** everywhere. Restricted to objects whose committed flag **actually FIRED as published** — the number the idea literally asks for — **CS_ALL 639 of 1,226 ESCAPABLE (0.5212), 582 ROBUST.** Over 3 seed bases x 3 q levels x 2 readings the escape share runs **0.2102 to 0.5691**, so the LEVEL is seed- and q-sensitive and the DIRECTION is not.

  **THE BLOCK-LEVEL ANSWER, WHICH IS THE ONE WORTH QUOTING. 7 of 16 ROBUST, 9 ESCAPABLE, 0 INDUCED.** U56 H: ROBUST / ROBUST / ESCAPABLE / ROBUST; U56 CADENCE: ESCAPABLE / ESCAPABLE / ESCAPABLE / ROBUST; B136 H: ROBUST / ESCAPABLE / ESCAPABLE / ROBUST; B136 CADENCE: ESCAPABLE / ESCAPABLE / ESCAPABLE / ROBUST (order S_FULL / S_OOS / CAGR / DD). **DD is un-resolvable at every rung set on every block and is the only statistic that is.** A committed floor-keyed claim on **DD**, or on **H/S_FULL**, is worth quoting; one on **CADENCE at anything but DD**, or on **CAGR anywhere**, is not.

  **THE FATAL DETAIL: THE TRIGGER IS NOT MONOTONE IN RUNGS.** Exhaustive over all 31 non-empty supersets of CORE inside EXT, per block, no sampling: of the 16 blocks firing at CORE, **10 can be silenced and 6 cannot be silenced by ANY superset; median ESCAPE COST 1 rung, max 2.** Cheapest escapes named: U56 H/CAGR by adding **H=42** alone, U56 CADENCE/S_FULL by **2D**, B136 CADENCE/CAGR by **2W**, B136 H/S_OOS by **210+378**. **And U56 H/S_OOS still fires on the FULL 9-rung EXT ladder yet falls silent once H=210 alone is added** — adding rungs is neither necessary nor sufficient, *which* rungs is what decides. **So a clause keyed on a whole-ladder floor cannot be repaired by mandating a rung COUNT either**; only a rung LIST fixes it.

  **H_COUNT_NOT_ID REFUTED, AND THE REFUTATION IS A CORRECTION TO THE RECORD.** At matched k=4 only **1,396 of 2,016 (0.6925)** four-rung subsets of the EXT ladders fire, against CORE firing in **16 of 16**. Per (panel, ladder) the share firing at all four statistics at once is U56 H **0.5079**, U56 CADENCE **0.0952**, B136 H **0.7143**, B136 CADENCE **0.1825** — product **6.31e-03**. **POST-HOC AND LABELLED AS SUCH:** 1110/1116's rung set is not a typical 4-rung ladder, it is one that fires where most do not. The committed INF_FLOOR census is therefore not merely rung-COUNT dependent (1118's finding) but rung-CHOICE dependent, and **quoting '18 of 32 infinite floors' without naming the rung LIST overstates how un-resolvable this tape is.** **H_UNDECLARED REFUTED and the number is still bad: 935 of 2,571 committed floor-keyed CSV rows (0.3637) do not state the rung set they were computed on** — fewer than the majority predicted, but for more than a third of the corpus a reader cannot perform the check this run performs (1,151 declare CORE, 485 EXT).

  **WHAT THE RECORD SHOULD DO WITH IT, STATED NARROWLY (proposed, NOT enacted — rule 6).** 1122's rule — *a clause is rung-stable iff every quantity in its trigger is a property of the peak and its own pair, never of the ladder* — is confirmed on a corpus of 2,580 objects rather than two clauses, and **strengthened**: a whole-ladder trigger is rung-CHOICE dependent and non-monotone, so fixing the rung count does not repair it. Any committed floor-keyed claim should quote its rung **LIST**, not its rung count, and the 9 ESCAPABLE blocks should not be cited as tape facts.

  **GATES 12 of 12, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 CROSS-RUN 936/1071/1082's committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS 1.70e-04; G4 / G4b 1098/1102's committed U56 n=12 and B136 n=15 triples 4.97e-05 / 2.13e-05; G5 live RULES v2 MaxDD == committed -12.05% at 4.95e-05; G6 determinism 0.00e+00; G7 `cadence_mask` == `engine.rebalance_mask` on D/W/M/Q, 0 differing bars (engine.py not modified); **G8 CROSS-RUN reproduces ALL 54 rows of 1110's committed CORE grid at 0.00e+00**; **G9 CROSS-RUN reproduces 1117's committed trigger counts on all 32 shared-seed rows at 0.00e+00**; **G10 CROSS-RUN reproduces all 32 of 1110's committed argmax PEAKS with 0 mismatches** (1 of 32 INF-flag mismatches reported and NOT gated — the flag is a redraw quantity, 1108/1116's finding); G11 every ladder x statistic live (min spread 1.79e-03). **HYPOTHESES 4 of 6.**

  **RULE 8 AND BOTH KEEP PATHS — NOTHING PROPOSED.** No claim set and no rung set can move a book: the BOOK at every rung is byte-identical across both dials — only which committed objects get CALLED fired changes — so 4a and 4b are invariant to dial 1 and dial 2 by construction; scored anyway because rule 4 requires it. Rung chosen on IS 2009-2016 ALONE, per ladder, three choosers, OOS read ONCE: **6 of 48 picks clear 4b full AND OOS; 0 of 48 clear 4a.** Whole grid, 74 rungs: **4b full 17, 4b OOS 18, 4a 0.** Every passing pick is the standing anchor U56 / W / H=126 / N=20 / gross 0.75 (full 15.58% / 1.1397 / -19.13%, halves 1.2037/1.0971, OOS 16.97% / 1.1643 / -19.13%), a cell the record already holds and has already PARKED, and all six are the ladder's FROZEN DEFAULT rung re-picked. Benchmarks: U56 SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711 / -33.72%; U56 RULES v2 live 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762 / -12.05%; B136 SPY full 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767 / -33.72%; B136 RULES v2 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059 / -12.24%. **Nothing new, nothing promoted, no RULES memo written.**

  **THE DECLARED APPROXIMATION, AND ITS DIRECTION.** The corpus is harvested from COMMITTED artifacts, so a floor-keyed claim the record made only in a console log or a script comment is not counted; that omission can only SHRINK the census, never change a classification. The prose arm's ALL reading takes a multi-cell claim to fire only when every cell it names fires — the record's own wording, and the CONSERVATIVE direction for ESCAPABLE, since a claim firing on fewer cells is harder to silence. Blocks on the SMALL panel and ladders outside {N, H, GROSS, CADENCE} are NOT rebuildable here and are published as rejects with that reason rather than scored.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every level is optimistic. A rung-to-rung gap and a rung-to-rung agreement both contrast two books over the same inflated tape and the bias very largely cancels out of them, out of the floor and out of every quantity classified here; it does NOT cancel out of the 4b legs, measured against SPY, a real index, so the 17 full-sample 4b passes are an UPPER bound.

  Script `research/backtests/2026-09-16_does-ANY-committed-FLOOR-KEYED-CLAUSE-survive-a-RUNG-SET-CHANGE_B.py`, 15 CSVs, console log, result memo, 5 LEADERBOARD rows. Follow-ups filed: 1139 (should a committed floor-keyed claim be required to quote its rung LIST rather than its rung count), 1140 (is DD's rung-robust un-resolvability a path-functional fact or a tape fact), 1141 (does the rung-CHOICE effect reproduce on the SMALL panel, where 1073 measured reliability 0.0915) — filed as 1136-1138 and renumbered on push after a lane collision, idea 932's defect again.
- 2026-09-16 (cloud lane, idea 1101 is-LADDER-DEPENDENCE-of-REACHABILITY-a-general-fact-or-a-U56-fact) — **ANSWERED = A U56-AND-B136-AND-ANCHOR-A FACT. PREMISE REFUTED. 1096's "2 of 4" reproduces exactly on its own two panels and dies everywhere else: SMALL/A 1, U56/B 0, B136/B 1, SMALL/B 0 under the headline chooser. The mechanism is a BOUNDARY ATTRACTOR — the GROSS ladder's IS-Sharpe and IS-CAGR argmax is the TOP rung 0.75 in 6 of 6 cells and its IS-DD argmax the BOTTOM rung 0.30 in 6 of 6 — so "reached from GROSS" says only that the anchor's gross equals the chooser's boundary, at a Sharpe margin of 1.1e-04 to 9.9e-04.**

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `PANEL` {U56, B136, SMALL} x `ANCHOR CELL` {A = W/H126/N=20/gross 0.75, the standing anchor and 1096's own; B = M/H63/N=12/gross 0.55, declared in the script header BEFORE any result and chosen for one stated reason — it is INTERIOR on all four ladders where A sits at the TOP rung of GROSS} = **6 combinations, ALL published**. CHOOSER and LADDER are NOT dials: all 3 x 4 = 12 reach decisions are published at every cell, **72 in all**. A and B share no ladder — all eight lines are distinct — so B is a genuine second draw and not a re-reading of A's.

  **WHAT REACHABILITY MEANS, STATED BEFORE THE NUMBERS.** An anchor (N, H, gross, cadence) sits on four one-dimensional CORE ladders, each varying one coordinate with the other three frozen at the anchor's own values. A ladder REACHES the anchor under chooser C iff C, given 2009-2016 ALONE, picks the anchor's own rung. The reach COUNT is an integer in 0..4 and 1096's D3 result is "2" for anchor A under `C_ISSHARPE` on U56 and on B136.

  **THE CENSUS OF REACH COUNTS (headline `C_ISSHARPE`).** U56/A **2**, B136/A **2**, SMALL/A **1**, U56/B **0**, B136/B **1**, SMALL/B **0**. The count is 2 in **2 of the 6** cells and the reaching SET `{GROSS, CADENCE}` matches in only those same 2 — H_SAMESET REFUTED. Mean reach count **1.0000** against the uniform-argmax expectation **0.7111** (1/9 + 1/4 + 1/10 + 1/4): above chance by **1.4x**, which is not a level.

  **THE MECHANISM, WHICH IS THE PART WORTH KEEPING.** The GROSS ladder is a boundary attractor under every chooser: IS-Sharpe and IS-CAGR both pick **0.75, the top rung, in 6 of 6** cells; IS-DD picks **0.30, the bottom rung, in 6 of 6**. Anchor A sits at 0.75 and is reached from GROSS on all three panels; anchor B sits at 0.55 and on none. Across all 72 decisions **reach == endpoint-ness on 62 of 72 (86.1%)** — endpoint cells reach **6 of 9**, interior cells **7 of 63**. H_BOUNDARY is REFUTED as a *perfect* predictor (10 discordant cells, all named in the console) and is by far the strongest available one.

  **PER LADDER, ALL PANELS x ANCHORS x CHOOSERS: GROSS 6/18, CADENCE 5/18, H 2/18, N 0/18.** 1096's "N and H never reach it" survives as a fact about **N** (0 of 18, mean P_boot 0.050) and does NOT survive for **H** (2 of 18, both B136 anchor B).

  **IS THE 2-OF-4 EVEN MEASURED? MOSTLY NOT — 11 of 24 headline decisions resolved.** P(anchor is the IS argmax) over 1000 joint moving-block redraws of the IS window (L=63, the SAME block index applied to every rung, so the cross-rung correlation that makes these ladders hard to resolve is preserved rather than destroyed): **11 of 24** clear a 0.90/0.10 bar, P_boot range **0.000-0.949**. 1096's two published U56 reaches sit at P_boot **0.716** (GROSS) and **0.278** (CADENCE) — the CADENCE reach is a coin flip that landed. H_RESOLVED REFUTED.

  **GATES 7 of 7, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 CROSS-RUN 936/1071/1082/1094/1102/1108/1110/1116/1131's U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS triple 1.70e-04; G4 live RULES v2 MaxDD == committed -12.05% at 4.95e-05; G5 determinism 0.00e+00; G6 fast runner == `engine.backtest` at anchor B on the M cadence 2.78e-17; **G7 CROSS-RUN 1096's committed D3 reproduces `{GROSS, CADENCE}` on U56 AND B136 exactly** — the premise this run refutes is refuted on panels where it first reproduces.

  **RULE 8 AND BOTH KEEP PATHS — NOTHING PROPOSED.** Neither dial can move a book: the 162 rung books are byte-identical whichever anchor or panel is nominated and only which rung gets CALLED reached changes, so 4a and 4b are invariant by construction; scored anyway because rule 4 requires it. Rung chosen on IS 2009-2016 ALONE, per ladder, three choosers, OOS read ONCE: **6 of 72 picks clear 4b full, 7 of 72 clear 4b OOS, 0 of 72 clear 4a**, and the IS chooser takes the OOS-best rung 23 of 72 times. Whole grid, 162 rungs: **4b full 26, 4b OOS 30, 4a 0** (U56 20/54, B136 6/54, **SMALL 0/54**). Every passing pick is the standing anchor U56 / W / H=126 / N=20 / gross 0.75 — full 15.58% / 1.1397 / -19.13%, halves 1.2037 / 1.0971, OOS 16.97% / 1.1643 / -19.13% against SPY OOS 15.21% / 0.8711 / -33.72% and live RULES v2 OOS 9.45% / 1.2762 / -12.05% — or a U56 anchor-B H rung. NO NEW BOOK.

  **SMALL IS NOT A NARROWER LARGE PANEL, IT IS A DIFFERENT TAPE.** 0 of 54 SMALL rungs clear 4b on any pattern: L_H1 1/54, L_H2 0/54, L_OOS 0/54, L_DD 5/54, L_CAGR 1/54, and SMALL's own RULES v2 reads 4.30% / 0.6637 / -13.89%. Any reach result measured there is a statement about a book nobody would run, and is reported as such.

  **THE DECLARED APPROXIMATION, AND ITS DIRECTION.** P_boot resamples ONE tape, so it measures sampling error around this regime and not regime uncertainty. Every P_boot is therefore CLOSER TO 0 OR 1 than the truth and H_RESOLVED is scored in the direction that favours SUPPORTED: a decision this run calls resolved may still be a coin flip across regimes; one it calls unresolved is unresolved a fortiori.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists. The SMALL pool is worse — the current constituents of a sub-$2B screen, **663 names after dropping 52 with `max_1d_move >= 1.0` per data/small_meta.csv (715 listed), tape 2010-01-04 -> 2026-09-11** — so every name that fell below the screen, delisted or went to zero is absent. A reach decision contrasts two rungs of one ladder over the same inflated tape and the bias very largely cancels out of it; it does NOT cancel out of the 4b legs, measured against SPY, a real index, so the 26 full-sample 4b passes are an UPPER bound.

  **WHAT THE RECORD SHOULD DO WITH IT, STATED NARROWLY (proposed, NOT enacted — rule 6).** A published claim that a book is rule-8 REACHABLE should name the LADDER it was reached from and the anchor's POSITION on that ladder, because a reach from a boundary rung is a property of the ladder's endpoints and not of the book.

  Script `research/backtests/2026-09-16_is-LADDER-DEPENDENCE-of-REACHABILITY-a-general-fact-or-a-U56-fact_cloud.py`, 7 CSVs, console log, result memo, 6 LEADERBOARD rows. Follow-ups filed: 1152 (should a published rule-8 reach carry the ladder and the anchor's position on it), 1153 (is the N ladder's 0-of-18 a rung-count fact or an N fact), 1154 (does any committed reach or chooser claim survive its own P_boot bar) — filed as 1149-1151 and renumbered on push after a lane collision, idea 932's defect again.
- 2026-09-16 (cloud lane, idea 1148 should-a-SUB-TAPE-COMPARISON-be-required-to-publish-its-REGIME-to-LENGTH-RATIO) — **ANSWERED = YES, AND IT IS NEARLY FREE, BUT ONLY IF THE CLAUSE IS WORDED OVER CS_STRICT. Of 2,587,354 committed rows across 2,274 committed CSVs that make a sub-tape, split-half or tape-length comparison, SIX ROWS IN ONE FILE publish a regime-to-length ratio — 1140's own regime.csv, the run that coined it. Share of rows 0.000002. The prose arm finds 1 of 337 trigger sentences, and that one sentence is the CHANGELOG line FILING this very idea, so ZERO committed prose claims publish it.**

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `CLAIM SET` {CS_STRICT, CS_HALVES, CS_ALL} x `RATIO DEFINITION` {R_SPREAD, R_SD, R_MATCHED} = **9 combinations, ALL published**. CS_STRICT = committed CSV rows whose file indexes the row BY TAPE EXTENT (a sub-tape / fraction / window / tape-length / segment / split key column); CS_HALVES = rows carrying a split-half pair (an H1 and an H2 column, or an IS_ and an OOS_ pair of one statistic) — a half IS a sub-tape, and it is the record's commonest such comparison; CS_ALL is the union. PANEL, LADDER and STATISTIC are NOT dials (3 x 4 x 6 reported everywhere) and the sub-tape fraction is FROZEN at 1140's own {1/1, 1/2, 1/3}.

  **THE CENSUS. CS_STRICT 569 files / 593,172 rows; CS_HALVES 1,788 / 2,140,569; CS_ALL 2,274 / 2,587,354. Publishing the ratio: 1 file, 6 rows.** The header scan covers all 5,529 committed `research/backtests/*.csv` (1.29 GB) and counts rows without parsing values, so the denominator is the whole corpus and not a sample.

  **THE PREMISE HOLDS ON A SECOND CONSTRUCTION, AND UNDER EVERY DEFINITION.** 1140 measured the ratio on its own resolution ratio `|gap|/SD`; this run measures it on the **statistic VALUE** — 27 CORE-ladder books per panel at the standing anchor, 3 panels, 6 statistics, 6 sub-tapes, 486 readings all dumped. Pooled regime/length at R_SPREAD: **CAGR 8.73x, VOL 8.00x, SHARPE 4.39x, MAXDD 2.00x, ULCER 17.51x, CALMAR 8.40x — 6 of 6 above 1**, and 6 of 6 above 1 under R_SD (min 2.69x) and R_MATCHED (min 1.74x). The regime noise is not a 1140 artefact and is not a definition artefact.

  **A CORRECTION TO 1140's OWN BAND, AND THE REASON THE CLAUSE SHOULD NOT COPY ITS DEFINITION.** `max - min` over k points GROWS with k, and 1140's within spread pools a k=2 range (halves) with a k=3 range (thirds). The count-matched form — the mean range of a RANDOM PAIR at every fraction — is smaller in **5 of 6** statistics, median **1.137x**, so 1140's committed **1.7x-to-61x** band is inflated by roughly **14%**. It survives the correction in every SIGN and every VERDICT; the clause should nevertheless require the count-matched form, because the inflation grows with how many stretches a run happens to cut.

  **THE ONLY SUB-1 CELL IN 72 is SMALL / MAXDD under R_MATCHED (0.79x)** — the one place measured here where the length effect exceeds the regime noise, and it sits on the panel whose levels are least trustworthy. Named, not swept, and filed as 1157.

  **PRICING THE CLAUSE — IT COSTS A COLUMN, NOT A RE-RUN. 485 of 569 CS_STRICT files are CHECKABLE (0.8524), carrying 573,668 of 593,172 rows (0.9671)**: their own tape-extent column already takes two distinct values AND repeats one, so both the within spread and the between move can be computed from what is committed. Only **3.29%** of CS_STRICT rows would need a re-run.

  **AND THE WORDING IS THE WHOLE COST — MY OWN SCOPE HYPOTHESIS WAS WRONG. H_SCOPE REFUTED:** CS_HALVES is **3.6x** CS_STRICT in rows, not the >100x declared in advance. The cost is not in the multiple, it is in the SATISFIABILITY: a split-half row can price REGIME (two stretches) but **never LENGTH** (a half is one length), so **0 of 2,140,569 CS_HALVES rows are checkable from what they publish**, and a clause worded over "sub-tape comparisons" would demand a third tape from every one of them — **77.83% of CS_ALL rows needing a re-run against 3.29% of CS_STRICT rows.**

  **THE CLAUSE, DRAFTED (proposed for the Sunday review, NOT enacted — rule 6).** *A published claim whose value depends on the LENGTH or the STRETCH of tape it was computed on — a sub-tape, a tape-length exponent, a window-length ladder — must state beside it its REGIME-to-LENGTH RATIO: the median spread of the same quantity across DISJOINT stretches of EQUAL length, divided by its move between the longest and the shortest length. A claim whose ratio exceeds 1 is an UPPER BOUND ON PRECISION and may not be quoted as a measurement. The within spread must be COUNT-MATCHED (a random pair at every length). Scope: claims indexed by tape extent. A split-half claim is explicitly OUT of scope, because a half is one length and the clause is unsatisfiable for it without a third tape.*

  **GATES 6 of 6, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 CROSS-RUN the committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS triple 1.70e-04; G4 live RULES v2 MaxDD == committed -12.05% at 4.95e-05; G5 determinism 0.00e+00; **G6 1140's committed `regime.csv` is present with all six statistics and its 1.66x / 3.50x / 5.19x / 5.52x / 9.07x / 61.51x are QUOTED VERBATIM FROM THE FILE, never re-derived from memory.**

  **RULE 8 AND BOTH KEEP PATHS — NOTHING PROPOSED AS A BOOK.** The 81 rung books are byte-identical across both dials — only which committed rows get COUNTED and how the ratio is DEFINED changes — so 4a and 4b are invariant by construction; scored because rule 4 requires it. Rung chosen on IS 2009-2016 ALONE, per ladder, three choosers, OOS read ONCE: **4 of 36 picks clear 4b full, 4 of 36 clear 4b OOS, 0 of 36 clear 4a.** Whole grid, 81 rungs: **4b full 16, 4b OOS 17, 4a 0** (U56 10/27, B136 6/27, **SMALL 0/27** — L_H1 1/27, L_H2 0/27, L_OOS 0/27), independently reproducing 1110/1131's counts on this construction. Every passing pick is the standing anchor U56 / W / H=126 / N=20 / gross 0.75 — full 15.58% / 1.1397 / -19.13%, halves 1.2037 / 1.0971, OOS 16.97% / 1.1643 / -19.13% against SPY OOS 15.21% / 0.8711 / -33.72% and live RULES v2 OOS 9.45% / 1.2762 / -12.05%. Binding leg among the 65 failures: **L_DD sole at 26**, L_CAGR sole at 9. The only thing PROPOSED here is a PROTOCOL clause, which rule 6 sends to the Sunday review.

  **THE DECLARED APPROXIMATION, AND ITS DIRECTION.** The census reads COMMITTED artifacts only, so a ratio published in a console log or a script comment is not counted — an omission that can only make H_NOBODY EASIER to support, and is named for that reason. The CS_STRICT test is a HEADER-NAME test: a file indexing rows by tape extent under an idiosyncratic column name is MISSED, and a file whose `rows` column means something else is OVER-counted. Both directions are live, all 148 accepted header names are dumped, and no classification in this run rests on a single file.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists; the SMALL pool is the current constituents of a sub-$2B screen (**663 names after dropping 52 with `max_1d_move >= 1.0` from data/small_meta.csv, tape 2010-01-04 -> 2026-09-11**). A WITHIN-length spread and a BETWEEN-length move both contrast the same books over stretches of the same inflated tape, so the bias very largely cancels out of the RATIO — the whole quantity this run measures. It does NOT cancel out of the 4b legs, measured against SPY, a real index, so the 16 full-sample 4b passes are an UPPER bound.

  Script `research/backtests/2026-09-16_should-a-SUB-TAPE-COMPARISON-be-required-to-publish-its-REGIME-to-LENGTH-RATIO_cloud.py`, 10 CSVs, console log, result memo, 6 LEADERBOARD rows. Follow-ups filed: 1155 (is the count-inflation of max-minus-min a general defect in the record's spread and band claims), 1156 (can a split-half claim be made regime-checkable without a third tape), 1157 (is the SMALL/MAXDD cell the one place where tape length beats regime).
- 2026-09-16 (lane B, idea 1157 is-the-SMALL-MAXDD-CELL-the-ONE-PLACE-where-TAPE-LENGTH-BEATS-REGIME) — **ANSWERED = NO. PREMISE REFUTED TWICE OVER, AND THE CELL IS MECHANICAL. 1148's only sub-1 cell in 72 reproduces here BIT FOR BIT (0.794259x, G7, |diff| 0.00e+00) and is neither a SMALL-panel fact nor a single-episode fact: it is the first crossing of a line EVERY panel crosses as soon as the fraction ladder gets finer, and the line itself is drawn by arithmetic.**

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** `FRACTION LADDER` {F_1140 = 1/1,1/2,1/3 (1148's own); F_FINE = 1/1,1/2,1/3,1/4,1/6, the HEADLINE and the "finer sub-tape ladder" this idea asks for; F_FINER = 1/1,1/2,1/3,1/4,1/5,1/6,1/8} x `TAPE` {T_OWN = each panel on its own tape, 1148's own; T_MATCHED = every panel restricted to the SMALL panel's own trading days, the LENGTH-MATCHED LARGE-CAP CONTROL this idea asks for} = **6 combinations, ALL published**. PANEL (3) x STATISTIC (6) x RATIO DEFINITION (3) x PARTITION {ALIGNED, OFFSET} x EPISODE {KEEP, DROP} are NOT dials — all **432 cells dumped**.

  **IT IS NOT THE ONE PLACE.** MAXDD at R_MATCHED, T_OWN, ALIGNED/KEEP: F_1140 **U56 1.172 / B136 1.307 / SMALL 0.794**; F_FINE **0.691 / 0.740 / 0.551**; F_FINER **0.652 / 0.694 / 0.468**. The F_1140 -> F_FINER multiplier is **0.39x-0.59x on all six (tape, panel) cells** — one factor, every panel. Per rung the reversal is sharper: at F_1140 SMALL has **74%** of its 27 books sub-1 against U56 **15%** and B136 **22%**; at F_FINE it is **27 of 27 on all three panels**. H_FINE is SUPPORTED for the reason that kills the premise — the finer ladder does not confirm SMALL, it drags the large caps down to it.

  **IT IS NOT A SMALL-PANEL FACT — H_PANEL REFUTED.** On the length-matched control, T_MATCHED / F_FINE MAXDD reads **SMALL 0.560x, U56 0.619x, B136 0.579x**: indistinguishable, with B136 BELOW SMALL. The tape dial moves the cell by **0.009** (0.551 -> 0.560); the fraction-ladder dial moves it by **0.326** (0.794 -> 0.468).

  **IT IS NOT A SINGLE-EPISODE ARTEFACT — H_EPISODE REFUTED.** The SMALL anchor book's full-tape MaxDD is **-35.81%, trough 2020-03-18**; dropping all of **2020** moves the headline cell **0.551 -> 0.620**, still far below 1. Only in 1148's own coarse corner (F_1140 + OFFSET) does the drop cross the line (0.553 -> 1.288), so *that particular reading* is part-episode and the finding on any finer ladder is not. The drop removes a whole calendar year, more than the drawdown — an over-removal that can only make H_EPISODE EASIER to support, and it fails anyway.

  **THE MECHANISM, WHICH IS THE PART WORTH KEEPING: 1148's OWN DEFECT, IN ITS BETWEEN TERM.** 1148 count-matched the WITHIN term (max-minus-min grows with k) and left the BETWEEN term as `|median at 1/1 - median at 1/f_max|` — a range over the ladder's ENDPOINTS, which grows as the ladder is extended while the length-matched within spread does not. SMALL between runs **10.53 -> 15.39 -> 17.49** across the three ladders against within **8.36 / 8.48 / 8.18**; U56 between **4.43 -> 6.92 -> 7.06** against within **5.19 / 4.78 / 4.60**. The ratio is therefore not a property of the statistic and the tape but of **how far down the fraction ladder a run happened to cut**, and two runs quoting it are not comparable. Why SMALL crossed FIRST at 1/3 is then a LEVEL fact and nothing more: its MaxDD is **-35.81%** against U56's **-19.13%**, so both terms are ~1.9x larger and the between term is the one that outgrows.

  **AND THE WHOLE READING IS MECHANICAL — H_NULL SUPPORTED ON EVERY PANEL.** A moving-block bootstrap (L=63, the record's own) of the anchor book has NO regime ordering by construction, only length. Its MAXDD R_MATCHED median: **U56 0.681, B136 0.678, SMALL 0.633**, with **0.92-0.98 of 200 draws sub-1** on every panel (IID nulls 0.583-0.704). The observed SMALL cell **0.551x** sits INSIDE its own null's 90% band **[0.441, 0.881]**. H_CONTINUUM **SUPPORTED 18 of 18** — MAXDD is among the two lowest-ratio statistics in every (ladder, tape, panel) cell — so 1148's cell is a threshold crossing in a continuum, not a phenomenon. Across all 432 cells R_MATCHED reads sub-1 **69 times** and **14 of the 18** headline sub-1 readings are MAXDD. "Tape length beats regime for MaxDD" says only that a maximum taken over more points grows with the points.

  **GATES 7 of 7, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 CROSS-RUN the committed U56 W/H126/N=20 triple 3.18e-07; G3 SPY OOS triple 1.70e-04; G4 live RULES v2 MaxDD == committed -12.05% at 4.95e-05; G5 determinism of the SMALL pipeline 0.00e+00; G6 1148's committed `regime.csv` present, its MAXDD row quoted verbatim from the file; **G7 CROSS-RUN 1148's SMALL/MAXDD R_MATCHED 0.794259x reproduced by a verbatim replica of its Arm-B code at |diff| 0.00e+00** — the premise this run refutes is refuted on a number it first reproduces exactly.

  **RULE 8 AND BOTH KEEP PATHS — NOTHING PROPOSED AS A BOOK.** The fraction-ladder dial is a READ of already-computed returns and leaves the 81 T_OWN rung books byte-identical; the tape dial only TRUNCATES. Scored at all **162 rungs** because rule 4 requires it. Rung chosen on IS 2009-2016 ALONE, per ladder, three choosers, OOS read ONCE: **10 of 72 picks clear 4b full, 10 of 72 clear 4b OOS, 0 of 72 clear 4a**; the IS chooser takes the OOS-best rung 20 of 72 times. Whole grid: **4b full 31, 4b OOS 32, 4a 0** — T_OWN U56 10/27, B136 6/27, **SMALL 0/27**; T_MATCHED U56 12/27, B136 3/27, **SMALL 0/27**. Binding leg among the 131 failures: **L_DD sole at 51**, L_CAGR sole at 18, L_H2 sole at 1. Every passing pick is the standing anchor or a neighbouring GROSS/N rung of it — **U56 / W / H=126 / N=20 / gross 0.75, full 15.58% / 1.1397 / -19.13%, halves 1.2037 / 1.0971, OOS 16.97% / 1.1643 / -19.13%** against **SPY OOS 15.21% / 0.8711 / -33.72%** and **live RULES v2 OOS 1.2762 / -12.05%**. Length-matching the U56 tape to SMALL's costs the anchor **1.48pp of CAGR and 0.078 of Sharpe** (14.10% / 1.0616 / -19.21%, OOS 15.93% / 1.1087). NO NEW BOOK.

  **WHAT THE RECORD SHOULD DO WITH IT, STATED NARROWLY (proposed, NOT enacted — rule 6).** 1148's drafted regime-to-length clause must not ship with its between term as written: it should FIX the fraction ladder inside the clause, or count-match the between term the way 1148 count-matched the within term. And no regime-to-length ratio for MAXDD — or any maximum — should be read as evidence without its own resample null beside it, because the null is sub-1 everywhere.

  **THE DECLARED APPROXIMATION, AND ITS DIRECTION.** Every null resamples ONE tape, so it prices sampling error around THIS regime and not regime uncertainty across regimes; a null ratio is therefore biased TOWARD the reading it tests. H_NULL is scored in the direction that favours calling the cell MECHANICAL — the conclusion this run reaches — so that conclusion rests on the evidence hardest to avoid, not easiest.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists; the SMALL pool is the current constituents of a sub-$2B screen (**663 names after dropping 52 with `max_1d_move >= 1.0` from `data/small_meta.csv`, 715 listed; tape 2010-01-04 -> 2026-09-11**), so every name that fell below the screen, delisted or went to zero is absent and the drawdowns read here are the SHALLOWEST the period could have produced. A within-length spread and a between-length move contrast the same books over stretches of the same inflated tape, so the bias very largely cancels out of the RATIO, and in the null arm — a resample of one book — it cancels exactly. It does NOT cancel out of the 4b legs, measured against SPY, a real index, so the 31 full-sample 4b passes are an UPPER bound.

  Script `research/backtests/2026-09-16_is-the-SMALL-MAXDD-CELL-the-ONE-PLACE-where-TAPE-LENGTH-BEATS-REGIME_B.py`, 7 CSVs, console log, result memo, 6 LEADERBOARD rows. Follow-ups filed: 1158 (should a regime-to-length ratio fix its fraction ladder or count-match its between term), 1159 (does any committed maximum-keyed claim in the record carry a resample null), 1160 (is the MaxDD LEVEL the whole of the SMALL panel's regime-to-length difference).

## 2026-09-17 — idea 1229 (cloud lane, idea 1 of 2): at what IS WINDOW LENGTH does a HEADLINE FORM separate from its own NL_PERM band?

  **ANSWERED = AT NO WINDOW, AT NO CADENCE, AND THE SIGN IS THE WRONG WAY ROUND.** `L* = NONE` for all three headline forms and both controls. **0 of 18** (form, window) cells separate from their own NL_PERM band at the primary cadence; **1 of 108** across MONTH / QUARTER / YEAR, against **5.4** expected at alpha 0.05 by chance (gate G12) — and that one cell (YEAR / NG / L1260 / H_NARROWEST, p 0.0185) rests on **2 moves**. The grid is 6 IS windows x 3 fold cadences x 5 choosers x 2 comparison sets x 3 panels, every point published; NL_PERM is the chooser's OWN destination multiset re-dealt to random folds, 4,000 reps, seed 12291229. G8 reproduces 1227's committed (window, form) deltas to **4.9e-04**.

  **THE AGGREGATE SIGN IS THE RESULT, AND IT IS NOT A NULL.** Over the **104** cells with a defined band the observed delta sits ABOVE its own NL_PERM mean at **19**; a coin gives 52. Two-sided binomial **p = 3.8e-11**. Mean gap **-0.0298** of Sharpe, median -0.0147, mean p_NL_PERM **0.6678**. **Re-dealing a form's own destinations to random folds BEATS the form's actual timing**, at every cadence and every window. 1227 read its grid as "no form beats holding"; the sharper reading is that the destinations carry whatever there is and the *timing* is actively bad. Anything in the record that reads a headline form as a weak-but-positive chooser is reading the wrong side of its own null.

  **THE REQUIRED PRECISION, WHICH IS WHAT THE QUEUE ASKED FOR.** Only **4 of 54** ALL4 form cells have a positive gap at all. With the band half-width's shrink exponent FITTED (not assumed) on a fold-subsample ladder — fractions 0.25 / 0.50 / 1.00, 40 draws, 400 null reps each, 52 cells, **b = 0.545 mean / 0.494 median**, i.e. near-iid when folds are DROPPED from a fixed span — `F* = F x (w/G)^(1/b)` gives: **H_RUNNERUP / L504 / QUARTER 38 years** of tape, L252 **73**, L1260 **943**; H_NARROWEST, H_ANY and CH_RAW **infinite at every window**. The pre-declared 50-year attainability bar therefore fires outcome (B) on exactly one cell, and **that cell rests on 3 moves in 193 folds**, so 38 years is a LOWER BOUND on the tape this question needs, not a forecast that the tape would settle it.

  **BYCATCH, AND THE MOST REUSABLE THING IN THE RUN: MORE FOLDS ON A FIXED TAPE BUY ~7%, NOT 3.5x.** MONTH (573 folds), QUARTER (193) and YEAR (46) read the **same 17.7 years**. Under iid-in-folds the band would shrink by `sqrt(F_M/F_Y)`, a ratio of **0.283**, from YEAR to MONTH. Measured over 24 (window, form) cells it is **0.934** (Q/Y **0.832** against a predicted 0.488) — an implied cross-cadence exponent **b = 0.027** against 0.5. Read with the subsample fit (b ~ 0.5 when folds are dropped from a fixed span), the two together pin the rule: **the F in any required-precision calculation must come from new calendar years, never from slicing the existing tape finer.** Any committed resolution the record bought by going to a finer fold cadence bought about 7% of what the fold count advertises.

  **BOTH KEEP PATHS AND RULE 8.** 66 rung books: **4a 0**, 4b full 17, 4b OOS 16, BOTH **16**. 540 stitched chooser curves: **4a 0**, 4b full 128, 4b OOS 126. Rule 8 with BOTH dials plus the form chosen on the pre-2017 folds only and 2017-2026 read ONCE: **4a 0 of 270**, 4b **50 of 270**. Every 4b pass on every leg is a U56 or B136 rung of the anchor family the record has already committed (U56 N=15, the N=20/H=126/GROSS=0.75/CADENCE=W anchor, H=21, GROSS 0.50-0.75; B136 GROSS 0.50-0.70). Mean OOS Sharpe of the IS-chosen cell **0.8558** against **doing nothing 0.8845** (**-0.0286**): U56 MONTH/L504/CH_RAW 17.55% / 1.1572 / -20.51% against the anchor's 17.16% / 1.1759 / -19.13%; B136 MONTH/L252/H_RUNNERUP 16.04% / 1.0129 against 16.29% / 1.0240; SMALL MONTH/L504/CH_RAW 6.19% / 0.3974 against 7.09% / 0.4534. U56 SPY OOS 15.15% / 0.8686 / -33.72%, live RULES v2 OOS 9.42% / 1.2717 / -12.05%. **KILL (capital), NO NEW BOOK, NO MEMO, NO RULES CHANGE.**

  **SURVIVORSHIP (rule 9).** B136 and SMALL are CURRENT constituents; SMALL is the sub-$2B screen with **51 of 715** tickers dropped for `max_1d_move >= 1.0` (664 investable), SPY benchmark only. The bias does not cancel out of the OOS levels or the 4b legs, so any pass there is an upper bound; it largely does cancel out of the gap-to-band ratio and the same-tape band ratio, which are this run's two headlines.

  **Gates 20 of 20.** Runtime 36s, offline, deterministic. Script `research/backtests/2026-09-17_at-what-IS-WINDOW-LENGTH-does-a-HEADLINE-FORM-SEPARATE-FROM-ITS-OWN-NL_PERM-BAND_cloud.py`; `RULES.md`, `PROTOCOL.md`, `scan.py`, `bot.py` and `baseline.py` untouched.

## 2026-09-17 — idea 1162 (cloud lane): is MONOTONICITY of a DIAL a PUBLISHABLE STAMP, or does it cost a run a SECOND GRID?

  **ANSWERED: A STAMP, AND A CHEAP ONE.** On a ladder a run has already built, `S_RHO` (Spearman of the statistic over the dial) costs **0 extra books and 0.062 s = 0.646% of the ladder's own 9.6 s / 120 books**; the margin leg costs **0 extra books** either way (0.0031 s closed-form Lo Sharpe SE, 15.1 s / 96,000 path evaluations if moving-block resampled). The **SECOND GRID** the queue named as the alternative costs **+120 books = +100%**. All six cost rows published.

  **IT CHANGES MORE THAN HALF THE READINGS.** Of 48 argmax claims (4 objectives x 3 panels x 4 dials, every rung of all 120 books published): `S_RHO` **26 of 48**, `S_MARGIN` **32 of 48**, `S_BOTH` **43 of 48**, all 16 (STAMP x CLAIMSET) cells published. **1154's 2x2 replicates on a fresh population: 10 of 32 non-monotone claims still pick an endpoint (0.312 against 1154's 0.405) and 8 of those 10 prefer it by under one SD (0.800 against 0.882).** MONOTONE-and-INTERIOR is **0 of 48** at tau 0.90, so at this threshold the stamp only ever fires in 1154's direction.

  **A CORRECTION THIS RUN MADE TO ITSELF, recorded rather than quietly re-cut.** The first cut scored a preference between two rungs against `sqrt(sd_a^2 + sd_b^2)` and read **48 of 48** claims unresolved — a bar that treats two rungs of one ladder as independent books when they are the same book one parameter apart. On **paired** draws (shared block origins, SD of the difference) the count is **32 of 48**. Gate G10 pins the direction; every margin in the committed files is paired, and the marginal SDs are kept only for spreads.

  **RULE 8 AND BOTH KEEP PATHS.** IS 2009-2016 only, OOS 2017-2026 untouched, 3 panels x 4 dials x 4 objectives x 4 choosers = **192 picks, all published**. 4b full AND OOS: `CH_ARGMAX` **3 of 48**, `CH_STAMP` **12 of 48**, `CH_STAMPM` **15 of 48**, `CH_CENTER` **5 of 48**. **4a 0 of 192** and **0 of 120** on the ladder itself.

  **THE CONFOUND IS FALSIFIED, NOT WAVED OFF.** `CH_STAMP`'s monotone fallback is the **incumbent** rung — a cell the record already chose with hindsight — and it fires 26 of 48 times. `CH_STAMPM` is the identical chooser whose fallback is the ladder's own **midpoint** and knows nothing of the record; it does **better**, 15 of 48 against 12, mean OOS Sharpe delta on moved picks **+0.0469 against +0.0417**. The stamp's out-of-sample gain is not the incumbent.

  **THE MECHANISM.** On U56/GROSS the Sharpe ladder is **flat** — 1.1387 / 1.1381 / 1.1376 at gross 1.00 / 0.75 / 0.60 — and monotone at **rho 1.000** with the argmax **unresolved**. `CH_ARGMAX` therefore takes gross **1.00** and **fails 4b on the drawdown leg** (MaxDD −24.93% against SPY's −20.23% cap). The stamp refuses the exposure endpoint. This is 1150/1154's "gross is an exposure dial" arriving as a *selection* result rather than a descriptive one.

  **THE ONE 4b BOOK RULE 8 REACHES WITHOUT THE INCUMBENT** is `CH_STAMPM` / U56 / **gross 0.60**: full CAGR **12.41%**, Sharpe **1.1376**, MaxDD **−15.51%** (H1 1.2045 / H2 1.0925); OOS CAGR **13.49%**, Sharpe **1.1609**, MaxDD **−15.51%**; against SPY full 15.06% / 0.8814 / −33.72% and OOS 15.15% / 0.8684 / −33.72%, and live RULES v2 8.60% / 1.1980 / −12.05%. **Memo written with exact RULES wording and recommending PARK**: its Sharpe equals the incumbent's to four decimals while its CAGR is 3.1 pp/yr lower, it is a **de-grossing** and not a discovery, and it fails 4a. A draft PROTOCOL clause on the stamp is **PROPOSED, NOT ENACTED** (rule 6, Sunday review only).

  **POPULATION CAVEAT, stated in the script, the memo and every leaderboard row.** The shares above are "of argmax claims **of this shape**" — a fresh, fully specified population — **not** "of the record's committed sentences". Those claims' cells are not resolvable from prose (1098's `R_STRICT`: 82 of 2,088; idea 1149 still open). Nothing here re-scores a committed claim.

  **SURVIVORSHIP (rule 9).** U56 and B136 are current-constituent lists; SMALL is the current constituents of a sub-$2B screen (**664 columns after dropping 52 tickers with `max_1d_move >= 1.0` from `data/small_meta.csv`**; tape 2010-01-04 -> 2026-09-11). Every name that delisted, went to zero or fell below the screen is absent, so drawdowns on B136 and SMALL are the shallowest the period could have produced; both are breadth controls, not tradable claims.

  10 of 10 gates PASS, including G1-G3 against the record's committed incumbent anchors and G10 on the paired-SD correction. Script `research/backtests/2026-09-17_is-MONOTONICITY-of-a-DIAL-a-PUBLISHABLE-STAMP-or-does-it-cost-a-run-a-second-grid_cloud.py`, 7 CSVs, console log, result memo, 3 LEADERBOARD rows. **NOTHING ENACTED**; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.

## 2026-09-17 — idea 1165 (cloud lane): do the RECORD's OTHER committed BLOCK BOOTSTRAP BANDS carry the same MACHINERY SHARE?

  **ANSWERED: THE TERM IS REAL, UNSTABLE ACROSS PANELS, AND IMMATERIAL TO EVERY PUBLISHED BAND.**

  **THIS IS A REAL CENSUS, NOT A FRESH POPULATION** — unusual for a "census the record" idea. All 36 committed `N_BLOCK` bands were **replayed bit for bit** off idea 1159's own committed `.objects.csv` using its `seed_of`, its L=63 and its 1,000 draws: observed values reproduce to **3.553e-15**, null medians to **3.553e-15**, and **36 of 36 INSIDE verdicts identically** (`G_REPLAY_OBS` / `_MED` / `_VER`). 1157's SMALL |MaxDD| 90% band (0.441, 0.881) is quoted from 1159's committed source. Everything below re-scores the record's own numbers.

  **HOW MANY PUBLISHED BANDS MOVE.** Under the parent's own order-destroyed control `X_SHUFFLE1`: **0 of 108** (36 bands x 3 confidence levels). Under `X_SHUFFLEPD`, a fresh permutation per draw with 1,000 of them against the parent's 5: **1 of 108**. 1157's ratio band still overlaps at **4 of 4** ladders under every control. Even `X_NONE` — the **maximal** correction, which removes the entire block-vs-iid gap rather than only the machinery part — flips **2 of 108**, both the same U56/H `OBJ_SPREAD` band at q=0.80 and q=0.90.

  **THE MECHANISM IS ARITHMETIC, AND IT SETTLES THE QUESTION.** The correction factor `phi_machinery` runs **0.828 to 1.147, median |phi−1| = 0.0496**, while the published 90% bands are **0.62 to 4.43 times their own null median wide** (OBJ_MAXDD 0.66-0.89, OBJ_SPREAD 0.62-2.05, OBJ_ARGMAX 0.67-4.43). A 5% recentring cannot move a verdict sitting deep inside a 70%-wide band. **A resample band is too blunt an instrument for the machinery term to reach.**

  **BUT THE SHARE ITSELF DOES NOT REPLICATE, AND THAT IS THE FINDING WORTH KEEPING.** On `OBJ_MAXDD` under the parent's own control: **SMALL +0.868 against its committed +0.841** (replicates), **U56 +0.063 against +0.165**, **B136 −0.046 against +0.164 — a sign flip**. Under the per-draw control the same three read **+0.133 / +0.101 / +0.258**. Across objects it is wilder still (U56 `OBJ_ARGMAX` −0.513, B136 `OBJ_SPREAD` −1.035). Pooled BLOCK/IID with order destroyed is **0.9759 here against the parent's committed 0.9696**. **Read plainly: the parent's POOLED number is solid and its PER-PANEL decomposition is not — only SMALL's 84% survives a control with 1,000 permutations instead of 5, and any future citation should narrow it to SMALL rather than repeat it as a three-panel fact.**

  **RULE 8 AND BOTH KEEP PATHS.** 81 rung books (base rates 4b full 16, 4b OOS 17, **4a 0 of 81**) plus 4 choosers x 4 ladders x 3 panels = **48 picks, all published**. IS 2009-2016 only, OOS 2017-2026 untouched. 4b full AND OOS: `K_NONULL` **0 of 12**, `K_BLOCK` **1 of 12**, `K_CORR` **1 of 12**, `K_IID` **1 of 12**; **4a 0 of 48**. **The machinery correction moves the pick at 4 of 12 ladders and is WORSE where it moves — mean OOS Sharpe −0.0318, better at only 2 of 4 — so it is not worth making as a selection device either.**

  **THE ONE 4b BOOK A CHOOSER REACHES** is `K_CORR` / U56 / **gross 0.60**: full CAGR **12.41%**, Sharpe **1.1376**, MaxDD **−15.51%**; OOS **13.49% / 1.1609 / −15.51%**; against SPY full 15.06% / 0.8814 / −33.72% and OOS 15.15% / 0.8684 / −33.72%, live RULES v2 8.60% / 1.1980 / −12.05%. **It is the same book idea 1162 reached by a different route the same day.** The memo points at 1162's exact RULES wording rather than duplicating it and **recommends PARK**: a de-grossing of the incumbent, Sharpe equal to four decimals, CAGR 3.1 pp/yr lower, 4a failed.

  **NO PROTOCOL CLAUSE PROPOSED, DELIBERATELY.** The obvious one — require an order-destroyed control beside every published moving-block band — was **priced by this run** and buys **0 of 108** verdict corrections while costing a third null on every banded run. A clause that corrects nothing is not worth a rule.

  **SURVIVORSHIP (rule 9), and it bites harder here than usual.** B136 and SMALL are current-constituent lists; SMALL is the current constituents of a sub-$2B screen (**664 columns after dropping 52 tickers with `max_1d_move >= 1.0` from `data/small_meta.csv`**, tape 2010-01-04 -> 2026-09-11). **Every object in this file is a drawdown**, so every delisted, zeroed or screened-out name being absent makes all three panels' drawdowns — and every band and share above — the shallowest the period could have produced. SMALL, the one panel whose machinery share replicates, is also the one most exposed to this.

  12 of 12 gates PASS. Script `research/backtests/2026-09-17_do-the-RECORD-s-OTHER-committed-BLOCK-BOOTSTRAP-BANDS-carry-the-same-MACHINERY-SHARE_cloud.py`, 7 CSVs, console log, result memo, 3 LEADERBOARD rows. **NOTHING ENACTED**; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.

## 2026-09-17 — idea 1175 (cloud lane, idea 1 of 2): is EVERY 4b-CONDITIONAL OUTCOME in the record UNFALSIFIABLE BY A CONTROLLED TAPE?

  **ANSWERED: IT DEPENDS ENTIRELY ON WHICH CONTROL, AND ONE OF THE TWO IN USE IS NOT A NULL AT ALL.**

  **THE CENSUS SEPARATES TWO LAYERS AND THEY DISAGREE BY TWO ORDERS OF MAGNITUDE.** 5,737 committed CSVs scanned, 1,265 carrying a 4b gate column, 29,267 (file, column) cells examined: only **158 columns (0.0054) are defined ONLY on 4b-pass rows**, across 18 files. The reader-facing layer is where the defect lives: **392 committed sentences restrict an outcome to the 4b-passing set**, in 140 files, **381 of them quoting a number** — O_SHARE 36, O_BINDING 17, O_MARGIN 10, O_CSTAR 2, O_OTHER 327. The machine record is nearly clean; the prose is not.

  **THE ANSWER, AND IT REVERSES THE QUEUE'S FRAMING.** Under `X_PERM` — a per-day permutation of the cross-section among the names observable that day, which preserves the daily equal-weight return path to **5.551e-17**, the daily cross-sectional multiset to **3.331e-16** and the listing mask exactly (0 cells) — the 4b pass rate collapses to **0.033 / 0.117 / 0.000** on U56 / B136 / SMALL against **0.292 / 0.083 / 0.000** on the real panels. That is idea 1171's degeneracy, reproduced. But under `X_ROT`, a per-name circular rotation, it goes the **other way**: **0.550 / 0.400 / 0.083**. The "control" passes 4b **almost twice as often as reality does on U56**. A claim is not unfalsifiable by a controlled tape; it is unfalsifiable by *one* of the two tapes the record calls controlled.

  **THE MECHANISM IS ARITHMETIC AND IT SETTLES WHICH TAPE IS LEGITIMATE.** Mean off-diagonal daily-return correlation, U56: REAL **0.3241**, X_PERM **0.2744**, X_ROT **−0.0002**. Rotation destroys the market factor outright, so a 40-name book on an X_ROT tape is a near-independent portfolio: median book Sharpe **2.0911** and median MaxDD **−5.93%**, against REAL 1.1008 / −18.70% and X_PERM 0.9465 / −21.31%. **A tape that hands every book a Sharpe of 2 and a 6% drawdown is a free lunch, not a null.** X_PERM keeps the factor (0.274 against 0.324) and is the only one of the two that controls selection without also removing the risk being controlled for.

  **THE CHEAPEST REPAIR IS DE-GATING, IT COSTS ZERO EXTRA DRAWS, AND BOTH "SOFTER BENCHMARK" REPAIRS ARE WORSE THAN THE DISEASE.** Two dials (outcome set × repair) = 20 grid points, all published. Definedness on the controlled tapes: **R_DEGATE 0.742 (c*) and 1.000 (margin, binding leg, share)** against **R_NONE 0.197**, **R_SOFT 0.128**, **R_MATCH 0.000**, **R_REFUSE 0.000**. Fidelity: the de-gated real-panel rho(turnover, c*) reads **−0.779 on U56 (n = 24) and −0.769 on B136 (n = 24)** against the gated **−0.930 (U56, n = 7)** and the record's committed **−0.65 / −0.6484 / −0.6154** — same sign, same order of magnitude, **3.4x the domain, zero extra runs**. **R_MATCH is the trap**: a panel-EW benchmark is not softer than SPY, it is harder (U56 EW Sharpe **1.1353** against SPY **0.8815**), and it takes definedness to **0.000 on every panel including the real one**. R_SOFT costs **+720 book runs** and buys 0.128. **c* stays partly undefined even de-gated (0.742) because it needs a positive margin at 0 bps, and SMALL has none: 0 of 24 books.**

  **RULE 8 AND BOTH KEEP PATHS.** 72 real books, every one published. 4b full **9 of 72**, 4b OOS **15 of 72**, **4a 0 of 72**. The best 4b book (full AND OOS) is `U56 / monthly / n = 40 / gross 1.00`: full CAGR **14.46%**, Sharpe **1.1880**, MaxDD **−17.96%** (H1 1.1924 / H2 1.1856); OOS **16.01% / 1.2866 / −17.96%**; against SPY full 15.06% / 0.8815 / −33.72% and OOS 15.15% / 0.8686 / −33.72%, and live RULES v2 8.60% / 1.1982 / −12.05% (OOS 9.42% / 1.2717 / −12.05%). **Memo written with exact RULES wording and recommending PARK**: it is **not IS-chooser-reachable** (CH_ISSHARPE picks U56/M/n=5/gross=1.00, OOS Sharpe 0.8290), it fails 4a, and it buys +0.0149 of OOS Sharpe over the live book for 5.9 pp more drawdown.

  **ONE POSITIVE SIGNAL, REPORTED AT ITS TRUE WEIGHT.** `CH_GATED` — the 4b-conditional gate used as an IS-only chooser — moves the pick at **2 of 3 panels**, gains **+0.1185 mean OOS Sharpe** (max +0.2636) and is the **only** chooser on this grid to reach a 4b passer at all (**2 of 12 picks**, against 0 for CH_ISSHARPE, CH_MATCH and CH_CSTAR). That is the opposite of the record's recent run of "the gate costs you nothing and buys you nothing" results. **n = 3 panels: this is a PARK-grade signal and a follow-up, not a finding.**

  **NO PROTOCOL CLAUSE PROPOSED.** The obvious one — require a controlled-tape claim to name its tape — is worth having, but this run priced only two tapes on one book family, and rule 6 restricts changes to Sunday review in any case.

  **SURVIVORSHIP (rule 9).** U56, B136 and SMALL are all current-constituent lists; SMALL is the current constituents of a sub-$2B screen, **664 columns after dropping the 52 tickers with `max_1d_move >= 1.0` from `data/small_meta.csv`** (tape 2010-01-04 → 2026-09-11). Every delisted, zeroed or screened-out name is absent, so every CAGR, drawdown and pass rate above — the 4b base rates and the memo's book included — is the most flattering the period could have produced.

  **11 of 11 gates PASS**, including the fast runner ≡ `engine.backtest` at **0.000e+00** on both cadences, live RULES v2 at 8.60%/1.1982/−12.05% against the committed 8.60%/1.1980/−12.05%, SPY at 15.06%/0.8815/−33.72%, and the standing CAND20 cell at 12.56%/1.0854/−18.31% — MaxDD exact and the Sharpe bracketed by the record's **two** committed values for that same book (1.0596 and 1.0921, 0.0325 apart, a spread 933's own G3 flagged). Script `research/backtests/2026-09-17_is-EVERY-4b-CONDITIONAL-OUTCOME-in-the-record-UNFALSIFIABLE-BY-A-CONTROLLED-TAPE_cloud.py`, 7 CSVs, console log, memo, 4 LEADERBOARD rows. **NOTHING ENACTED**; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.

## 2026-09-17 — idea 1179 (lane B): what does a ONE-SIDED agreement between a FREE bar and a PAID one buy the record?

  **ANSWERED: NOTHING. THE ONE-SIDEDNESS DOES NOT GENERALISE, IT REVERSES, AND 1170's OWN IS AN ARTEFACT OF TWO CONSTANTS.**

  **THE DIRECTION DOES NOT REPLICATE — IT FLIPS.** Of the three fresh paid-vs-free pairs the queue names, **0 of 3 are one-sided in 1170's direction**. `B_SHARPE` (moving-block Sharpe band vs Lo's analytic SE) is **+0 / −2** and `B_PERMT` (block sign-flip permutation vs paired t) is **+0 / −9** — one-sided the OTHER way, the free bar REFUSING what the paid bar admits — and `B_MEANP` (block bootstrap p vs normal p) is **+1 / −2, TWO-SIDED**. Agreement on the 87 POP+ANCHOR cells: 0.9655 / 0.9770 / 0.8966 against `B_TSTAT`'s 0.8161.

  **AND 1170's OWN PAIR IS A CONSTANT, NOT A METHOD.** Its free bar is 2 × the median SE and its paid bar 3 × the cross-seed spread with a 0.25 pp floor; the free constant that admits the SAME NUMBER of cells is **6.532, not 2.000 — 3.27x** — and at that matched level the committed **+16 / −0 becomes +1 / −1, TWO-SIDED**. `B_SHARPE` goes ONE-SIDED − → **IDENTICAL**, i.e. its disagreement was purely a level offset too. **A looser bar admits a superset BY CONSTRUCTION, and a superset is one-sided BY CONSTRUCTION.** H_ONESIDED, H_UNIVERSAL and H_CONSTANT are all REFUTED. This is a **correction to the record**: 1170's *"the cheap rule never refuses a cell the expensive rule admits, so it is a sound lower bound on refusals"* is arithmetically right and inferentially void, and future citations should quote the boundary number instead.

  **THE SCREEN WORKS ANYWAY, AND THAT IS THE FINDING WORTH KEEPING.** Two dials (bar pair × screen threshold) = **24 grid points, all published**. Flag a cell when |z_free − z\*| ≤ k and pay the full null only there. All four pairs reach **ZERO screened error** — verdicts identical to paying everywhere — at cost **B_SHARPE 0.0230, B_MEANP 0.0575, B_PERMT 0.2529, B_TSTAT 0.2989** of cells (free-only error 0.0230 / 0.0345 / 0.1034 / 0.1839; exact k\* 0.043 / 0.045 / 0.671 / 4.786). **The property that licenses a screen is BOUNDARY CONCENTRATION, not one-sidedness** — strictly weaker, strictly more useful, and present at 4 of 4 pairs: two-sided `B_MEANP` still reaches zero error at 0.0575 cost because its wrong-way disagreement also sits within 0.045 of the bar. H_SCREEN SUPPORTED 4 of 4.

  **THE CENSUS SAYS THE RECORD CANNOT AUDIT ANY OF THIS RETROSPECTIVELY.** 5,747 committed CSVs scanned, 816 carry a bar column, and only **35 (0.0429) publish BOTH a paid and a free bar** — PAID-only 94 (0.1152), FREE-only 687 (0.8419); 0.1377 in a resampling context. Prose is worse: of **59,872 committed sentences**, 843 quote a paid bar and **23 (0.0273)** also quote its free counterpart. **H_AUDITABLE REFUTED.** A screen has to be adopted FORWARD, one extra column at a time.

  **RULE 8 AND BOTH KEEP PATHS — ZERO CAPITAL CONTENT.** 72 population books and 3 panels × 3 choosers × 4 pairs × 3 regimes = **108 picks, all published**; IS bars rebuilt on 2009–2016 ONLY, picks scored on the untouched 2017–2026 window. 4b full **2 of 72**, 4b OOS **3 of 72**, **4a 0 of 72**; picks 4b full+OOS **0 of 108**, 4a **0 of 108**. **The bar regime (G_FREE / G_PAID / G_SCREEN) moves the pick at 0 of 36 (panel, pair, chooser) cells and |ΔOOS Sharpe| is 0.0000 everywhere — H_CAPITAL REFUTED.** Best 4b book (full AND OOS) `U56 / N20/H126/W`: full **15.55% / 1.1381 / −19.13%** (H1 1.2049 / H2 1.0932), OOS **16.92% / 1.1615 / −19.13%**; against SPY full 15.06% / 0.8814 / −33.72% and OOS 15.15% / 0.8684, live RULES v2 8.60% / 1.1980 / −12.05% (OOS 9.42% / 1.2714). It is **PRIOR ART** — 1170's own incumbent cell — **not IS-chooser-reachable (0 of 108)** and it **fails 4a**, so **no memo and nothing enacted**. Published honestly: `B_PERMT`'s IS pool is nearly empty (mean 0.33 books) and the chooser falls back to the unrestricted 24-book set at 6 of 9 cells under every regime, which is why its three regimes cannot differ — a limitation of that arm, not a result.

  **CLAUSE PROPOSED, NOT ENACTED (PROTOCOL rule 6 — Sunday review only):** *a run reporting that a cheap bar agrees one-sidedly with an expensive one must also report the agreement at a LEVEL-MATCHED cheap constant, the one admitting the same number of cells; and a run publishing a resampled bar should publish its closed-form counterpart in the same row, which costs no draws and is the only way a screen can ever be audited from the record.*

  **A POPULATION CAVEAT PUBLISHED RATHER THAN BURIED.** 45 of the 132 cells are GROSS rungs of one anchor book, and Sharpe and the t on the mean are near scale-invariant, so those cells are near-duplicates that inflate any agreement rate. Every headline is reported on all 132 AND on the 87 non-GROSS cells, and the 87 is the number quoted.

  **SURVIVORSHIP (rule 9).** U56 and B136 are current-constituent lists; SMALL is the current constituents of a sub-$2B screen (**664 columns after dropping 52 tickers with `max_1d_move >= 1.0` from `data/small_meta.csv`**, tape 2010-01-04 → 2026-09-11). Every CAGR, drawdown and 4b base rate above is the most flattering the period could have produced. It largely cancels out of the headline (an agreement count between two bars on the same cell) and does NOT cancel out of the 4a / 4b legs.

  **12 of 12 gates PASS**, including the fast runner ≡ `engine.backtest` at 1.4e-17, live RULES v2 and SPY against their committed anchors, Lo's analytic SE ≡ the iid-bootstrap SD of the Sharpe on gaussian data, sign-flip ≡ normal p at L=1, and **1170's committed pair replayed bit for bit off its own `.cells.csv`: 0 of 132 verdicts differing on EACH of its two rules and its committed 0.8788 / +16 / −0 reproduced at 0.000e+00**. Hypotheses 1 of 6 SUPPORTED. Script `research/backtests/2026-09-17_what-does-a-ONE-SIDED-agreement-between-a-FREE-bar-and-a-PAID-one-buy-the-record_B.py`, 8 CSVs, console log, result memo, 4 LEADERBOARD rows. **NOTHING ENACTED**; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.
## 2026-09-17 — idea 1180 (cloud lane, idea 2 of 2): is EVERY committed ESTIMABILITY or RESOLUTION VERDICT in the record itself SEED-STABLE?

  **ANSWERED: NO — AND 1170's 56 → 52 IS THE MILD END OF THE DEFECT, NOT THE ANOMALY.**

  **THE HARVEST CONFIRMS THE QUEUE'S PREMISE AND THEN OVERSHOOTS IT.** The queue said the record "has no seed-count convention". It is worse than that: in the machine layer there is no seed count **at all**. 166 committed CSVs carry an estimability / resolution verdict column — **333 (file, column) verdict cells**, 209 of them boolean — and **50 (0.150) carry a seed axis of any kind while 0 (0.000) state a seed COUNT**. The reader-facing layer is the same story at scale: **779 committed sentences publish an estimability, resolution or "clears its own noise" verdict**, across 180 files, and **16 (0.0205) name the seed count they were read at**.

  **THE ANSWER.** 72 cells (3 panels × 24 books, the same 72 as 1164) × **120 independent null tapes per panel** × 5 seed counts × 25 replicates × 4 verdict sets = **259,200 verdict evaluations, every one published**. The record's headline form is a count — "N of 72 ESTIMABLE". That count's **max − min spread across replicates AT THE SAME SEED COUNT** runs **23–36 cells at S = 3** and is **still 7–13 at S = 33**. The per-cell flip rate (share of the 72 whose verdict is not unanimous across replicates) runs **0.500–1.000 at S = 3** and **0.125–0.407 at S = 33**. Agreement with the S = 33 reference is **0.541–0.859 at S = 3**, and even S = 33 scored against itself reaches only **0.887–0.980**. **A 4-cell move on 72 is what this object does between two draws of the same seed count.** 1170 did not measure a seed-count effect; it measured noise that a 3-seed and a 5-seed reading share.

  **WHAT SEED COUNT WOULD FIX IT: NONE THAT ANYONE WILL RUN.** Fitting log(spread) = a + b·log(S) over the measured ladder, **8 of 12 (verdict set, statistic) pairs decay like sqrt-N** (b = −0.372 to −0.506) and imply **S ≈ 2,510 (V_BAND / CAGR) to 16,798 (V_TSTAT / MaxDD)** to hold the published count to within one cell — against the record's **3** and 1170's **5**, a 500x to 5,600x increase. The other **four pairs do not decay at all**: V_RESOLVE / CAGR **b = −0.092**, V_RESOLVE / MaxDD −0.252, V_SIGN / CAGR −0.261, V_TSTAT / CAGR −0.321. For those **no seed count fixes the verdict**. This is an **EXTRAPOLATION, not a measurement**, and the companion script `2026-09-17_estimability-verdict-seed-stable_cloud.FIT_seedcount.py` says so in its own output and refuses to quote an implied S where b is flat.

  **THE PRACTICAL READING.** An estimability verdict at 3 or 5 seeds is not a cheap version of the right answer; it is a different random variable. Either publish the seed count and the replicate spread beside the verdict, or stop publishing the verdict as a count. **No PROTOCOL clause is proposed** — rule 6 restricts changes to Sunday review, and this run priced one verdict family on one book family.

  **METHOD LIMIT, STATED BECAUSE IT BOUNDS EVERY NUMBER ABOVE.** The S-seed verdicts are scored on seed sets drawn **without replacement from a common pool of 120 null tapes per panel**, not on 120 × 25 freshly drawn tapes. Replicates at the same S are therefore mildly dependent, which makes every flip rate and spread reported here a **LOWER bound** on fully independent re-runs. A verdict already unstable under this construction is unstable a fortiori.

  **RULE 8 AND BOTH KEEP PATHS.** 72 real books and **378 chooser picks, all published**. 4b full **9 of 72**, 4b OOS **15 of 72**, **4a 0 of 72**, and **4b OOS 0 of 378 picks at every seed count**. The best 4b book (full AND OOS) is `U56 / monthly / n = 40 / gross 1.00`: full **14.46% / 1.1880 / −17.96%** (H1 1.1924 / H2 1.1856), OOS **16.01% / 1.2866 / −17.96%**; against SPY full 15.06% / 0.8815 / −33.72% and OOS 15.15% / 0.8686 / −33.72%, and live RULES v2 8.60% / 1.1982 / −12.05% (OOS 9.42% / 1.2717 / −12.05%). **It is the same book idea 1175 reached the same day on the same family, reproduced to the last digit** — a consistency check between the two runs, not a second piece of evidence for the book. Memo written with exact RULES wording and **recommending PARK**.

  **THE VERDICT IS WORTHLESS AS A SELECTION DEVICE AND ITS SIGN IS PANEL-DEPENDENT.** `CH_ESTIM`'s eligible set collapses monotonically as seeds are added (U56 24 → 6-15 of 24, B136 → 0-9, SMALL → 6), and its out-of-sample effect **splits by panel**: U56 mean OOS Sharpe 0.8290 → **1.1422** at S = 33, B136 1.0051 → 1.0958, but **SMALL 0.4373 → 0.3060**. Two panels up, one down, and never to a 4b pass. **KILL as a capital finding.**

  **SURVIVORSHIP (rule 9).** U56, B136 and SMALL are current-constituent lists; SMALL is the current constituents of a sub-$2B screen, **664 columns after dropping the 52 tickers with `max_1d_move >= 1.0` from `data/small_meta.csv`** (tape 2010-01-04 → 2026-09-11). Every delisted, zeroed or screened-out name is absent, so every CAGR, drawdown, base rate and the memo's book are the most flattering the period could have produced.

  **11 of 11 gates PASS**, including the fast runner ≡ `engine.backtest` at **0.000e+00** on both cadences, live RULES v2 at 8.60%/1.1982/−12.05%, SPY at 15.06%/0.8815/−33.72%, `X_PERM` preserving the daily equal-weight return path to **5.551e-17** and the listing mask exactly, and the standing CAND20 cell at 12.56%/1.0854/−18.31% with its Sharpe bracketed by the record's two committed values. Script `research/backtests/2026-09-17_is-EVERY-committed-ESTIMABILITY-or-RESOLUTION-VERDICT-in-the-record-itself-SEED-STABLE_cloud.py` plus the `FIT_seedcount` companion, 8 CSVs, two console logs, result memo, 4 LEADERBOARD rows. **NOTHING ENACTED**; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.

## 2026-09-17 — idea 1161 (lane B): why does a RESAMPLE NULL sit ABOVE the OBSERVED MAXIMUM at 108 of 108 cells?

  **ANSWERED: IT IS THE BOOK, NOT THE RESAMPLE — AND THEN THE QUEUE'S OWN RESIDUAL STORY DIES TOO.**

  **THE DECISIVE ARM PUTS A KNOWN ANSWER UNDER THE MACHINERY.** `S_PERM` re-orders the ladder's rungs by ONE common random permutation: every rung's marginal is exact, the cross-rung dependence is exact, and there is **no serial structure left to lose**, so a resample null of an S_PERM tape is correctly specified and the truth is ratio 1.000 / share-below-median 0.500. 25 independent pseudo-tapes x 400 draws x 3 nulls x 3 objects x 12 (panel, ladder) = **5,400 calibration cells, every one published**. The machinery reads **1,416 of 2,700 below its own median (0.5244)** against the real panels' **0.824**, with an anchor-object floor of **N_BLOCK 1.0096 / N_IID 0.9998 / N_STAT 1.0020**. **H_MACHINERY REFUTED.**

  **AND THE CHANNEL LADDER SAYS THE SAME THING ONE FACTOR AT A TIME.** Off the record's own N_BLOCK on OBJ_MAXDD, in log(observed / null median), against a total gap of **−0.0603**: **wrap-around +0.0026**, **the Jensen drift channel +0.0046** (each draw forced onto the observed total log return — gate G8, 0.000e+00), **marginal resampling −0.0066** (an EXACT permutation null — gate G7, 0.000e+00). Two of the three move the gap the WRONG WAY. Only `K4_IID` (L = 63 → 1) moves it at all, **−0.1128**, and that is not a repair but a different null: it deletes 63 days of the book's own order. **H_WRAP, H_DRIFT and H_MARGINAL all REFUTED; H_BOOK SUPPORTED — R_PERM, which preserves the observed multiset EXACTLY and destroys only order, still sits above the observed at 36 of 36.**

  **BUT THE RESIDUAL IS NOT THE RECOVERY / MEAN-REVERSION STORY EITHER.** An MA(1) filter carrying the book's **OWN** lag-1 autocorrelation (`S_MA1`) closes **0.278** of the S_PERM (1.0054) → REAL (0.9415) distance. **H_AC1 REFUTED.** The structure that shortens these drawdowns lives at **quarter horizons** — VR(63) 0.53–0.57 against VR(21) 0.67–0.91 — which is exactly why L = 63 → 1 is the one variant that moves anything. **And the sign is the opposite of the naive story:** the LEAST mean-reverting panel carries the LARGEST gap (SMALL ac1 −0.0301 / gap −0.1062; B136 −0.0378 / −0.0861; U56 −0.0501 / −0.0605). That is idea 1162's `H_MEANREV` refutation seen from the other side. **n = 3 distinct books, published as three, and NO rank correlation is reported** — all four anchor rows per panel are the same book, so a correlation over the 12 would be 3 points counted 4 times each.

  **WHAT A PUBLISHED BAND ACTUALLY NEEDS, AND IT IS OBJECT-DEPENDENT.** Machinery floor **OBJ_MAXDD 1.0054** — a LEVEL needs **no** correction and the whole ~6% gap is a fact about the book — against **OBJ_SPREAD 0.9505**, a genuine ~5% machinery bias, and OBJ_ARGMAX 0.9796. **H_OBJECT SUPPORTED.** The spread bias is **idea 1148's count inflation showing up INSIDE the null**, where nobody had checked it: a max-minus-min over k rungs of a *resampled* ladder grows with k too. **H_BAND REFUTED** and worth saying plainly: R_MACH does not improve coverage at all (inside-90% **0.9630 → 0.9630**), because the record's bands were never failing to CONTAIN the observed value — they were mis-centred, and a 1% recentring changes no verdict. Two dials (null type x correction) = **15 cells x 36 = 540 rows, all published**.

  **TWO DEFECTS IN THE PARENT, FOUND WHILE REBUILDING IT, REPORTED AND NOT ABSORBED.** (i) The queue publishes 1159's ratio triple as a bare *"0.936 / 0.851 / 0.964 by null type"* with **no labels**; only 1159's own CSV says which belongs to which (N_BLOCK 0.9358 / N_IID 0.8507 / N_STAT 0.9641), and the two readings are **0.085 apart on a 0.15 effect**. The first cut of this script mapped them in the other order and **gate G9c failed at 8.527e-02** — the gate is the only reason the error did not propagate. (ii) **Every ladder's ANCHOR rung is the SAME book** (N = 20, H = 126, gross 0.75, weekly; gate G12, observed identical at 0.000e+00), so OBJ_MAXDD's 36 cells are **9 distinct (panel x null) measurements replicated four times**, differing only by the null's seed. On distinct cells the parent's headline reads **62 of 81 (0.765)**, not 89 of 108 (0.824). The four replicates agree at **9 of 9** and the replicate spread of the null MEDIAN is **0.0182 (median) / 0.0256 (max)** against a headline gap of **0.0585**, so the "36 of 36" is **not** a seed artefact — but it is **9 measurements, not 36**, and both readings are quoted throughout.

  **RULE 8 AND BOTH KEEP PATHS.** 72 books per panel (N x H x cadence), **216 in all, every one published**; parameters chosen on 2009–2016 ONLY and read once on 2017–2026. **4b full 12 of 216, 4b OOS 13 of 216, 4a 0 of 216** (U56 11/11, B136 1/2, SMALL 0/0). Best 4b book (full AND OOS) `U56 / weekly / N = 12 / H = 126`: full **17.65% / 1.1658 / −20.17%** (H1 1.2741 / H2 1.0833), OOS **18.78% / 1.1701 / −20.17%**; against SPY full 15.06% / 0.8814 / −33.72% and OOS 15.15% / 0.8684 / −33.72%, and live RULES v2 8.60% / 1.1980 / −12.05% (OOS 9.42% / 1.2714 / −12.05%). **Memo written with exact RULES wording and recommending PARK**: it clears 4b's drawdown cap (0.60 x 33.72% = **20.232%**) by **0.06 pp**, its immediate neighbour `N = 15` has a **higher** OOS Sharpe (**1.1869**) and **fails** at 20.97%, **0 of 180 picks reach it**, and it fails 4a.

  **THE CAPITAL CONTENT, AT ITS TRUE WEIGHT.** `CH_NULLDD` uses the null itself as the selector. The correction moves the pick at **9 of 9 (panel, null) cells — but 3 of 9 once `R_PIVOT` is set aside**, and R_PIVOT is a *reflection*, not a repair: it maps the null through the observed value, turning "the null sits above" into "the null sits below" by construction without pricing why. Mean OOS Sharpe of the pick: **R_NONE 0.7110, R_MACH 0.7110, R_CENTER 0.7241, R_PERM 0.7471** (R_PIVOT 0.9103, n = 9, a curiosity). **H_CAPITAL is recorded SUPPORTED against its declared bar and reported as CAPITAL-IRRELEVANT** — the bar was written before the repair/reflection distinction was visible and this run does not move a bar after seeing the result. Picks clearing 4b full AND OOS 27 of 180, **4a 0 of 180**; by chooser CH_ISCAGR 0 of 45, CH_ISSHARPE 0 of 45, **CH_ISDD 15 of 45**, CH_NULLDD 12 of 45 — a plain IS drawdown chooser reaches a 4b passer more often than the null-normalised one.

  **NO PROTOCOL CLAUSE PROPOSED.** The obvious one — require a max-minus-min null to be count-matched — is worth having, but this run priced one null family on one book family, and rule 6 reserves every PROTOCOL edit to a Sunday review in any case.

  **SURVIVORSHIP (rule 9).** U56, B136 and SMALL are current-constituent lists; SMALL is the current constituents of a sub-$2B screen, **663 columns after dropping the 52 tickers with `max_1d_move >= 1.0` from `data/small_meta.csv`** (tape 2010-01-04 → 2026-09-11). It bears on this run's headline **directly and in the direction of the finding**: a survivor-only panel has shallower observed drawdowns, which pushes the observed value further BELOW its own null. **The Arm C machinery floor is immune** (a ratio read on the same panel, so the bias divides out); **the residual book gap is NOT**, an unknown part of the ~6% is survivorship, and this run cannot separate them without a point-in-time panel. The comparative conclusion — machinery ≈ 1.00 on a level, 0.95 on a spread — survives regardless, because both are read on the same tapes.

  **15 of 15 gates PASS**, including the fast runner ≡ `engine.backtest` at **1.388e-17**, live RULES v2 MaxDD ≡ −12.05% at 4.949e-05, SPY OOS at 2.894e-03, SMALL determinism 0.000e+00, **`N_BLOCK` at L = 1 ≡ `N_IID` bit for bit (1162's identity, 0.000e+00)**, **`perm_index` rows exact permutations of 0..T−1 for all three nulls (0.000e+00)**, **R_CENTER forcing every draw's total log return onto the observed (0.000e+00)**, `boot_maxdd` on the identity index ≡ the observed |MaxDD| (1.421e-14), and — the two the whole run turns on — **`R_NONE` reproducing 1159's committed OBSERVED values and NULL MEDIANS on 1159's own seeds at 3.553e-15 (G9, G10)**, so every correction is a strict re-processing of the record's own draws and not a different experiment. G2 carries 1163's price-vintage defect openly (this run 0.155520/1.138079/−0.191276 against the committed 0.155787/1.139701/−0.191276, bar declared at 5e-3 and the deviation printed). **HYPOTHESES 3 of 9 SUPPORTED.** Script `research/backtests/2026-09-17_why-does-a-RESAMPLE-NULL-sit-ABOVE-the-OBSERVED-MAXIMUM-at-108-of-108-cells_B.py`, 13 CSVs, console log, result memo, PARK memo, 6 LEADERBOARD rows. **NOTHING ENACTED**; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.

## 2026-09-17 — idea 1158 (lane B): should a REGIME-to-LENGTH RATIO fix its FRACTION LADDER or count-match its BETWEEN TERM?

  **ANSWERED: NEITHER AS A MEASUREMENT — FREEZE, BECAUSE COUNT-MATCHING PROVABLY CANNOT.**

  **THE DECISIVE ARM HAS NO DATA IN IT.** Gate G11 evaluates each candidate between term on an **exactly log-linear m[f]**, where the right answer is known and identical at every fraction ladder. Over the run's five ladders the between term's max/min is **endpoint range 2.2623x, ladder-SD 1.5298x, COUNT-MATCHED 1.4202x, span-normalised EXACTLY 1.0000x (6.66e-16)**. The queue's repair 2 shrinks 1148's defect by a third and **cannot remove it at any sample size**: a max-minus-min over k ladder points and a mean over its k(k−1)/2 pairs both scale with the ladder's **LOG-SPAN**, and that is the quantity neither named repair divides out. Repair 1 — freezing the ladder in the clause — is comparable **by fiat**, which this run states at every occurrence (gate G9, 0.000e+00) and never counts as evidence.

  **ON THE OBJECT THE RECORD ACTUALLY COMMITS, NOT A CONVENIENT ONE.** 1148's and 1157's cells are a POOLED construction — the per-part median across the rungs of each of the four rung ladders as the within groups, the median over every (rung, part) value as the level. The first cut of this script priced the repairs on a per-book ratio instead, **and gate G7 caught it**. The pooled replica is gated to 1157's own committed `cells.csv`. Median ladder sensitivity / verdict-flip rate, pooled (216 cells): **P_RAW 2.0841 / 0.1759, P_CMATCH 1.8612 / 0.0556, P_CMNORM 2.0174 / 0.0509, P_ELAST 1.9801 / 0.0370**, P_FREEZE 1.0000 / 0.0000 by construction. The independent per-book reading (2,916 cells) gives 2.5232 / 1.7334 / 1.8630 / 4.2004 and is published beside it. **H_CMATCH REFUTED and H_NORM REFUTED** against their declared 1.10 / 0.05 bars. **145,800 per-book and 10,800 pooled ratio readings, every one published**; 2 tuned dials (repair × fraction ladder) = 25 combinations, all reported.

  **WHY EVEN THE EXACTLY-INVARIANT ESTIMATORS FAIL ON DATA, AND WHERE THEY DO NOT.** **H_CURVE REFUTED**: the pooled median R² of m[f] on log(length) is **0.6959** against a declared 0.80, and by statistic **SHARPE 0.1137, CAGR 0.3421, VOL 0.6467** against **MAXDD 0.9284, ULCER 0.8509**. For the moment statistics there is **no length effect to normalise** — the ratio is noise over noise whatever the between term, and H_SPAN is REFUTED (0.1806 of cells predicted within 10%) for the same reason. **Where a real, log-linear length effect exists the repair works and works well**: pooled MAXDD LSENS **1.997 → 1.295 (count-matched) → 1.182 (span-normalised) → 1.168 (elasticity)**, ULCER 3.157 → 1.648 → 1.321. **The clause is a PATH-STATISTIC clause or it is nothing** — which is exactly the statistic 1148's own sub-1 headline cell was about.

  **THE DEFECT ITSELF IS REAL, AND FREEZING IS NOT FREE.** **H_DEFECT SUPPORTED** — the pooled ratio falls **0.5547x (U56) / 0.4894x (B136) / 0.5905x (SMALL)** from F_1140 to F_FINER, reproducing 1157's committed 0.39x–0.59x. **H_BETWEEN SUPPORTED** — median LSENS between **2.2812** against within **1.0167**. **H_NOISE SUPPORTED** — a 60-draw moving-block bootstrap with ONE COMMON index across all 27 rung books puts the finer reading outside the marginal 5–95% band at **4 of 6** cells, and the finer ladder is lower on the SAME resampled tape at ≥0.95 of draws in **5 of 6**. **H_CALIB SUPPORTED** — on an S_PERM tape (one common permutation, every regime ordering destroyed) the repair ranking by LSENS is **identical**, so ladder-stability is a property of the ESTIMATOR and not of this tape. **What freezing costs**: against the count-matched reading on the finest ladder the frozen number restates **0.6782 of 432 pooled cells by more than 25%** and flips the sub-1 verdict at 0.0532.

  **A DEFECT THE GATES FOUND, LABELLED EVERYWHERE AS NOT PRE-DECLARED.** G7 failed on its first run because the committed price cache has **gained one trading day** since 1157 (U56 4,705 rows to 2026-09-15 → 4,706 to 2026-09-16); B136, SMALL and every T_MATCHED panel are unchanged row for row. On the moved panel the record's own committed ratio shifts by a **median 0.0104 and a maximum 0.7762** in relative terms, with **0.1944 of the 72 deterministic cells moving more than 10%**, and **putting the single day back removes 97.57% of the deviation** (G7c). The mechanism is plain: every sub-tape boundary is `n // f`, so one extra row re-cuts every stretch on every rung of the rung ladder and the fraction ladder at once. **Before any ladder or between-term question arises, two runs of this object are not comparable unless both state the tape's LAST DATE and ROW COUNT** — a cheaper and stricter requirement than either repair the queue named. **CENSUS**: 5,975 committed CSVs scanned by header, **164 carry a sub-tape fraction column over 331,218 committed rows**, 120 carry a within/between/ratio column; of **90 committed prose sentences** naming a sub-tape or regime-to-length object, only **30 (0.3333)** name a fraction ladder at all and none names a row count.

  **THE GATE BAR, AND WHY IT IS NOT ZERO.** G2 measures a **price-ADJUSTMENT drift of 1.62e-03** in the book's own CAGR between 1157's cache vintage and today's — the same nominal dates, re-adjusted closes (this run 0.155520/1.138079/−0.191276 against the committed 0.155787/1.139701/−0.191276). A ratio derived from those returns cannot reproduce to machine precision on any tape, so a 1e-9 bar would gate the data vintage rather than this run's arithmetic. G7/G8 are set at **1e-3 relative — three significant figures, tighter than the drift measured in the input** — and read **2.38e-05 / 4.64e-05**. R_MATCHED, which carries 1148's own SAMPLED 200-pair spread, is gated separately at 1.68e-01 (G7b); the two headline gates use the two **deterministic** within-definitions, where no sampler enters.

  **RULE 8 AND BOTH KEEP PATHS.** 162 books and **264 chooser picks, all published**; parameters chosen on 2009–2016 ONLY and read once on 2017–2026. **4b full 31 of 162, 4b OOS 32 of 162, 4a 0 of 162**; 33 of 264 picks clear 4b full and **4a 0 of 264**. Best 4b book (full AND OOS) `T_MATCHED / U56 / CADENCE = Q`: full **14.33% / 1.0693 / −19.94%** (H1 1.0456 / H2 1.1043), OOS **17.38% / 1.1781 / −19.94%**; against SPY full 15.06% / 0.8814 / −33.72% and OOS 15.15% / 0.8684 / −33.72%, and live RULES v2 8.60% / 1.1980 / −12.05% (OOS 9.42% / 1.2714 / −12.05%). **Memo written with exact RULES wording and recommending PARK**: it clears 4b's drawdown cap (0.60 × 33.72% = **20.232%**) by **0.29 pp**, fails 4a on all three legs, is **0 of 11 choosers reachable on its own family** (every chooser picks D, W or M, never Q), and is a quarterly-cadence rung the record has priced many times.

  **THE CAPITAL CONTENT, AT ITS TRUE WEIGHT.** `CH_RATIO` uses the repaired ratio as the selector, the only route by which a fraction ladder could touch capital. **H_CAPITAL REFUTED**: every CH_RATIO mean OOS Sharpe lies between **0.8146 and 0.8632** against a plain IS-Sharpe chooser's **0.8534**, a maximum move of **0.0486** against the declared 0.05 bar. The repair does move the PICK — 18 or 19 of 24 families on the Sharpe ratio — and buys **nothing** for it. **KILL as a capital finding.**

  **NO PROTOCOL CLAUSE PROPOSED.** The memo drafts the wording a Sunday review would need, and recommends against enacting it: rule 6 reserves every PROTOCOL edit to a Sunday review, and this run priced one ratio family on one book family.

  **SURVIVORSHIP (rule 9).** U56 and B136 are current-constituent lists; SMALL is the current constituents of a sub-$2B screen, **663 columns after dropping the 52 tickers with `max_1d_move >= 1.0` from `data/small_meta.csv`** (tape 2010-01-04 → 2026-09-11). Every delisted, zeroed or screened-out name is absent, so every drawdown here is the shallowest the period could have produced. Every headline in this run is a **ratio of ratios read on one tape** (LSENS = R at one ladder over R at another), so the bias very largely divides out of it; it does **not** divide out of the 4b legs, measured against SPY, a real index, so every 4b pass counted above is an **UPPER bound**.

  **14 of 14 gates PASS, HYPOTHESES 5 of 10 SUPPORTED.** Script `research/backtests/2026-09-17_should-a-REGIME-to-LENGTH-RATIO-FIX-its-FRACTION-LADDER-or-COUNT-MATCH-its-BETWEEN-TERM_B.py`, 19 CSVs, console log, PARK memo, 7 LEADERBOARD rows. **NOTHING ENACTED**; RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.

## 2026-09-17 — RECORD REPAIR (lane B, while resolving the idea-1158 lane collision)

  Commit `3d056ec` (lane cloud, idea 1158) **replaced `research/CHANGELOG.md` wholesale** instead of appending to it: the file went from 1,099 lines to 22, dropping every entry before it. The loss was caught when this lane's rebase conflicted on the same file. `research/CHANGELOG.md` has been rebuilt here as **merge base `405a5b7` + lane B's idea-1158 entry + lane cloud's idea-1158 entry, in that order**, so the full history and BOTH runs' entries are present and nothing of `3d056ec`'s own text is discarded. No other file was affected — `LEADERBOARD.md` (+5) and `QUEUE.md` (+5/-1) were clean appends in that commit. This is a second symptom of defect 932 (two lanes claiming one idea in the same hour) and is recorded here so the next lane can see it happened.

  Lane cloud's idea-1158 entry, preserved verbatim from `3d056ec`, follows.

- 2026-09-17 (lane cloud, idea 1158 should-a-REGIME-to-LENGTH-RATIO-FIX-its-FRACTION-LADDER-or-COUNT-MATCH-its-BETWEEN-TERM) — **ANSWERED = NEITHER, AND THE TWO REPAIRS FIX DIFFERENT THINGS.** No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, engine.py, scan.py, bot.py and baseline.py untouched. SELECTION: this lane takes the LAST eligible open idea; 1158 ended '## Open' and is not EDGAR / Form 4 / 8-K / options / spin-off / live-data. Price leg of 81 rung books (78 distinct) and 27 rule-8 picks, so it carries this run's mandatory rule-8 walk-forward and both KEEP paths.

  **THE TWO DIALS AND NO MORE (rule 4, and the queue names both):** `REPAIR` {R_ASIS, R_FROZEN, R_COUNT} x `FRACTION LADDER` {L2, L3, L4, L6, L8} = **15 cells, EVERY ONE PUBLISHED** in `.grid.csv` at every (panel, statistic, construction, partition), 1,080 rows. NOT dials: PANEL {U56, B136, SMALL}; the four DIAL LADDERS that are the ratio's groups (CADENCE, GROSS, H, N = 27 rung books per panel, 1148's grid); STATS {CAGR, VOL, SHARPE, MAXDD, ULCER, CALMAR} (headline MAXDD); PARTITIONS {ALIGNED, OFFSET}; CONSTRUCTIONS {C_POOLED, C_BOOK}; WITHIN terms {R_SPREAD, R_SD, R_MATCHED} (headline R_MATCHED); the jackknife; 3 rule-8 choosers. Tape PINNED at 2026-09-15, the vintage 1148/1157's committed anchors saw.

  **THE ANSWER, SCORED IN THE DECLARED ORDER: (D) NEITHER — because the queue's two candidate repairs are answers to two DIFFERENT questions.** `R_FROZEN` buys COMPARABILITY (**18 of 18** (panel, stat) cells, by construction) and pays for it with the LEVEL and the RESOLUTION: it **misses the resolved value at 11 of 18 cells** (median relative gap **0.3221**), including **all three** cells of the record's headline statistic, and its leave-one-ladder-out jackknife SE of log(ratio) is **0.6723 against R_COUNT-at-L8's 0.1722 — 3.9x wider**. `R_COUNT` is **not a comparability repair at all**: comparable at **2 of 18**, median max/min **2.083x** over the five ladders, worst **17.246x** — removing the endpoint inflation does not stop the medians moving with the ladder — **but it IS a resolution repair (SE ratio 0.2561).** `R_ASIS`, the record's as-committed between term, is comparable at **0 of 18**, median **3.715x**, worst **77.956x**. **The ladder is not only a comparability dial, it is a RESOLUTION dial, and freezing it spends resolution to buy comparability. That is the reading the queue's framing does not anticipate.**

  **THE COMBINATION IS THE CLAUSE THE HEADLINE STATISTIC NEEDS AND ONLY THAT ONE.** `R_BOTH` (frozen ladder AND count-matched between term) is computed and published because the pre-declared scoring lands on (D) and a reader will ask; it is **NOT a fourth dial value and is never selected on**. Comparable 18 of 18 by construction, and it **lands on the resolved value at all three MAXDD cells at gaps 0.0253 / 0.0886 / 0.0151** — an order of magnitude better than freezing alone — **but at only 4 of 18 cells overall, median gap 0.3843, WORSE than R_FROZEN's 0.3221**, because on ratio-valued statistics (SHARPE, CALMAR) the between term passes near zero and both repairs blow up together. **The three MAXDD rows are its best three, not a representative sample, and are not quoted alone.** Filed as 1187.

  **A CORRECTION TO 1148's OWN COMMITTED HEADLINE: its SMALL/MAXDD 0.794x is the FROZEN 3-rung reading; the resolved one on its own finer ladder is 1.174x — the record's number is 32% low against data the record itself later collected.** Filed as 1188.

  **THE CENSUS — THE RECORD CANNOT AUDIT THIS RETROSPECTIVELY.** **26 committed CSVs in research/backtests carry a sub-tape FRACTION column, 42,170 rows in total, and only 4 (0.1538) also publish the LADDER they walked.** Six distinct committed ladder labels exist and the max fraction actually walked runs 0..8 across 21 files. **PROSE: 66 committed sentences assert a regime-to-length reading; 0.1061 state a ladder and 0.2576 quote a number.** **On the two ladders the record ACTUALLY walked — L3 (1140/1148's) and L8 (1157's), both committed — R_ASIS disagrees with itself by a median 1.736x and up to 7.233x (within 1.25 at 3 of 18), R_COUNT by 1.559x (worst 12.675x, 4 of 18), R_FROZEN by 1.000x (18 of 18). Only freezing makes two committed runs comparable, and it does so by refusing to read the finer ladder at all.**

  **GATES 10 of 10 PASS, AND TWO ARE EXACT REPLAYS OF THE RECORD'S OWN NUMBERS.** **G6: 1148/1157's SMALL/MAXDD/R_MATCHED/F_1140 replayed BIT FOR BIT — 0.794259 against the committed 0.794259** (their `ratio_from` verbatim, their Monte-Carlo pair statistic, their seed, their loop order). **G9: 1157's premise REPRODUCED INDEPENDENTLY — R_ASIS(L8)/R_ASIS(L3) = 0.5615x / 0.5148x / 0.5916x against their committed 0.39x-0.59x.** G7 the EXACT all-pairs statistic recovers 0.794259 at 1.89e-07 (the departure removes noise, not signal). **G8 R_FROZEN's ladder-invariance is PROVED at 0.00e+00, not asserted**, because the comparability claim for that repair is definitional. G1 1.39e-17, G5 1157's committed MaxDD levels 7.17e-06, G10 determinism 0.00e+00. **THE VINTAGE, PUBLISHED NOT ABSORBED: G2/G3 read 4.12e-05 / 1.70e-04 on the PINNED tape against 1.62e-03 / 2.89e-03 unpinned** (idea 1163's defect). **NO BOOTSTRAP, AND THE REASON IS DECLARED UP FRONT: a moving-block resample destroys the very object a regime-to-length ratio measures — it would leave the within term almost unchanged while making the between term meaningless, i.e. manufacture this run's own answer — so the resolution measure is a LEAVE-ONE-LADDER-OUT JACKKNIFE over the ratio's own four groups. A stated departure from 1157, not a quiet substitution.**

  **RULE 8 AND BOTH KEEP PATHS — 81 RUNG BOOKS (78 DISTINCT), ALL PUBLISHED.** Benchmarks (pinned): **U56 SPY 15.10% / 0.8829 / -33.72% (halves 0.9588/0.8207), OOS 15.21% / 0.8711; U56 RULES v2 (live) @10 bps 8.62% / 1.2008 / -12.05%, OOS 9.46% / 1.2763; B136 SPY 15.16% / 0.8861 / -33.72%, OOS 15.33% / 0.8767; B136 LIVE 7.98% / 1.0993 / -12.24%, OOS 7.88% / 1.1059; SMALL SPY 14.06% / 0.8581 / -33.72%, OOS 15.33% / 0.8767; SMALL LIVE 4.30% / 0.6637 / -13.89%, OOS 3.75% / 0.5600.** Base rates at 10 bps: **4b full 16 of 81, 4b OOS 17, BOTH 15, 4a 0 of 81** (U56 10/27, B136 6/27, **SMALL 0 of 27 on every path**). **NO NEW CANDIDATE AND THE REASON IS ARITHMETIC: 11 of the 16 passers are the standing incumbent read at a LOWER GROSS RUNG, and their Sharpe runs 1.1389 / 1.1391 / 1.1392 / 1.1394 / 1.1396 / 1.1397 across gross 0.50 -> 0.75 — a spread of 8e-04 over a 1.5x change in gross. That is idea 1177's degenerate Sharpe-vs-gross ladder arriving from a third direction, a cross-run confirmation and not a discovery.** Filed as 1189. The rest are prior art (U56 N=12, B136 N=15, B136 N=10). The one uncommitted cell, **U56 CADENCE=Q (15.40% / 1.1389 / -19.94%, OOS 17.28% / 1.1727)**, is **dominated by the incumbent on every full-sample axis and ahead only out of sample — preferring it would be selecting on the OOS window, which rule 8 exists to forbid, so it is recorded and NOT promoted. No memo.** **THE FREE PARAMETER HAS CAPITAL REACH AND NO CAPITAL VALUE: the repair MOVES the pick at 5 of 6 (panel, ratio-chooser) cells, yet 0 of 27 picks clear 4a and exactly 1 of 27 clears 4b — and that one is U56 / R_FROZEN / CH_HIGHRATIO landing on CADENCE=W, the standing incumbent, reached by the FALSIFICATION CONTROL under the repair that discards data.** CH_LOWRATIO, the reading the record's language implies a run should prefer, loses to CH_ISSHARPE on OOS Sharpe at B136 and SMALL under every repair.

  **PROTOCOL CLAUSE PROPOSED NOT ENACTED (rule 6):** *"no sub-tape ratio without its fraction ladder" — any committed sentence quoting a regime-to-length, within-to-between or sub-tape spread ratio SHALL name the fraction ladder it was computed on and state whether its between term is an endpoint range or a count-matched pairwise mean; a ratio quoted without both is not comparable to any other run's.*

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists; SMALL is the current output of a sub-$2B screen less the documented `max_1d_move >= 1.0` exclusion (52 of 715 dropped, 663 names plus SPY as benchmark). Every LEVEL is optimistic and every 4a/4b count is an UPPER bound. A RATIO of two spreads in the statistic's own units is far less exposed but **not immune** — a survivorship-flattered panel has a shallower drawdown path and MAXDD is this family's headline statistic — so the levels are published beside every ratio.

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_should-a-REGIME-to-LENGTH-RATIO-FIX-its-FRACTION-LADDER-or-COUNT-MATCH-its-BETWEEN-TERM_cloud.py`, 9 CSVs, console log, result note, 5 LEADERBOARD rows. Follow-ups filed 1187 (does a sub-tape ratio need a statistic class before a clause can be written), 1188 (how many committed sub-tape figures move once the ladder is resolved rather than frozen) and 1189 (is the gross-rung degeneracy of Sharpe universal).

## 2026-09-17 — Research cloud, idea 1199: should a SATURATED PERCENTILE be PUBLISHABLE as a POINT at all?

**ANSWERED = NO, AND THE COST IS NOT RHETORICAL: THE POINT FORM'S ARGMAX IS ITS TIE-BREAK.** Two dials and no more (CLAIM SET {C_STRICT, C_PROX, C_ALL}, 1197's harvester verbatim; BOUND FORM {B_POINT, B_ONE, B_TWO, B_CP}), every rung published at every cell, nothing selected on. **DATA-FREE FIRST:** a percentile over K draws lives on {0, 1/K, ..., 1}, so at K=10 the point form prints 1.000 where the one-sided 95% Clopper-Pearson statement is **0.741134** — the point **OVERSTATES the K=10 evidence by 0.259 in the statistic's own units**, and 1197 found 0.963-1.000 of U56/B136 cells sitting there. **CENSUS of 32,887 committed text units:** C_STRICT **104** / 26 ceiling-quoted / **10 FLOOR-quoted** / 40 state K / **23 ADJUDICATED AT THE CEILING**; of 31 saturated units only **21 are re-expressible at all** because **0.3226 state no draw count** and a bound form is a function of K — a loss the point form **hid rather than avoided**. **PRICE ARM:** 36 books x 3 panels, 14,400 gross-matched nulls, nested K ladder. The point form separates 12 books into **1.75 (U56) / 1.40 (B136) distinct values at K=10** with **10.95 / 11.60 of 12 tied at its maximum**, against **CH_Z at 12.00 of 12 at every one of 360 cells**; SMALL de-saturates to 0.000 at K=400, so **saturation is a statistic-meets-easy-panel property, not a machinery one**. **RULE 8 (chosen on 2009-2016 only, 2017-2026 read once, 20 seeds x 18 (panel,K) cells): B_POINT 0.8897 mean OOS Sharpe and 0 of 18 4b-clearing picks; B_ONE = B_TWO = B_CP 0.9568 (+0.0671) and 7 of 18.** **THE THREE BOUND FORMS ARE DECISION-IDENTICAL TO SIX DECIMALS AT ALL 54 CELLS — every basis point comes from DECLARING THE TIE, none from how the bound is written.** **MECHANISM: B_POINT lands on N=5/M, the FIRST cell in the sort order, at 6 of 6 U56 and 5 of 6 B136 rungs — a saturated point percentile does not choose, the alphabet does, silently** — and carries OOS MaxDD -28.7% against the bound form's -19.7%.

  **KEEP paths on all 36 books: 4a 0 of 36; 4b full 6, 4b OOS 7, BOTH 6** (U56 5/12, B136 1/12, **SMALL 0/12 on every path**). **NO NEW CANDIDATE AND THE REASON IS THE RECORD'S OWN: U56 N=20/W IS the standing 2026-09-04 incumbent, and the five U56 passers span Sharpe 1.1520 -> 1.2111 over an N ladder from 15 to 40 — 0.059 across a 2.7x change in holding count, with the walk-forward's reach (N=15/W, OOS 17.58% / 1.1795 / -19.69%) ahead of the incumbent by 0.0130 of full Sharpe and 0.0090 of OOS Sharpe. That is idea 1189's degenerate-ladder reading arriving from a fourth direction, a cross-run confirmation and not a discovery; preferring it would be selecting inside a ladder this record has already measured flat. RECORDED AND NOT PROMOTED. No memo.**

  **PROTOCOL CLAUSE PROPOSED NOT ENACTED (rule 6):** *"no saturated percentile as a point" — any committed sentence quoting a null percentile at an attainable extreme (>= 1 - 1/(K+1) or <= 1/(K+1)) SHALL publish it as a BOUND carrying its own draw count, and SHALL name the statistic used to break the resulting tie. A ceiling percentile quoted as a point, or quoted without K, is not an adjudication and may not be cited as one.*

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists; SMALL is the current output of a sub-$2B screen less the documented `max_1d_move >= 1.0` exclusion (52 of 715 dropped, 663 names plus SPY as benchmark only). Every LEVEL is optimistic and every 4a/4b count is an UPPER bound. The Arm C **comparison** runs two choosers over the same book population and is far less exposed, but the levels are published beside it.

  **GATES 11 of 11,** including G1 fast runner == `engine.backtest` at 2.08e-17, G1b reproducing 1191's **two** NaN rows in `engine.backtest` `returns` on ndarray and confirming both sit inside the 260-row warm-up, G3 live RULES v2 U56 MaxDD **-12.0549%** against the record's -12.05%, G4 CH_PCT distinct <= K+1 at every rung with 0 violations, G5 CH_Z tying at its maximum 1.00 of 12 always, and G6b reproducing 1197's committed census (104/26/23/22 against 103/26/23/22 — the corpus grew by one unit).

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_should-a-SATURATED-PERCENTILE-be-PUBLISHABLE-as-a-POINT-at-all_cloud.py`, 8 CSVs, console log, result note, 5 LEADERBOARD rows. Follow-ups filed 1202 (is FIRST-WINS the record's actual habit or only 1199's assumption), 1203 (does the tie-break statistic matter once the tie is declared) and 1204 (how many committed saturated claims sit on a panel that de-saturates).

## 2026-09-17 — Research cloud, idea 1155: is the COUNT INFLATION of max-minus-min a GENERAL DEFECT in the record's SPREAD and BAND claims?

**ANSWERED = YES, AND IT IS EXACT RATHER THAN STATISTICAL: the count-matched spread is STRICTLY SMALLER at 54 of 54 cells where the counts differ and EXACTLY EQUAL at 18 of 18 where they do not (every CADENCE cell and only those). There is no cell in between.** Two dials and no more (CLAIM SET {C_STRICT, C_PROX, C_ALL}; MATCHING RULE {M_NONE, M_SUBSAMPLE, M_PAIRWISE, M_D2}), every rung scored at every cell, nothing selected on. **DATA-FREE FIRST:** for k iid N(0,1) draws E[max-min] = d2(k), reproduced by Monte Carlo to 2.13e-3; **the record's ladders are 2 / 4 / 6 / 10 rungs long, so a GROSS ladder's max-min is 2.7274x a CADENCE ladder's ON PURE NOISE**, and **1140's committed "1.7x - 61x band" pools k=2 with k=3 where d2(3)/d2(2) = 1.5000 exactly — its FLOOR is 1.133x the count difference alone.** **CENSUS of 32,930 committed text units:** C_STRICT **313** spread/band/range claims, **0.2875 comparing two DIFFERENT counts in the same unit** and **0.3898 adjudicating with NO count stated at all**; re-expressed against the inflation their own counts buy, **the SMALLEST quoted multiple survives at only 54 of 90 (0.6000) — two in five committed spread multiples are the two counts and not an effect** (median inflation carried 1.8999). **PRICE ARM, 3 panels x 4 ladders x 6 statistics:** measured inflation **N 2.2467 against the prediction 2.2461 (agreement to 6e-04)** and **H 1.8524 against 1.8245** — on those dials the published spread is, to measurement precision, **entirely the rung count** — while **GROSS comes in BELOW its prediction (2.4545 vs 2.7274) because d2 assumes independent rungs and a near-monotone ladder realises LESS range than noise: the gap from d2 is a free measurement of idea 1189's degenerate gross ladder.** **1148's committed 5-of-6 / 1.137x understates the defect 1.73x** because it matched to a larger k_min than the record's shortest ladder.

  **THE LIMIT OF THE REPAIR IS STATED AS A GATE (G7): M_D2's t-statistic EQUALS M_NONE's at every cell to 3.6e-15** — d2 is a constant divisor at fixed k and cannot change a WITHIN-ladder verdict. Decisiveness at 2 block-bootstrap SE runs **0.3472 (M_NONE, M_D2) / 0.3333 (M_SUBSAMPLE, M_PAIRWISE)**: **two thirds of the record's ladder spreads do not clear 2 SE under ANY convention.** **RULE 8 WALK-FORWARD (widest-spread dial chosen on 2009-2016 ONLY, then that ladder's IS argmax, 2017-2026 read ONCE): C_ANCHOR — NEVER MOVE — reads 0.8934 mean OOS Sharpe and is the ONLY chooser of five reaching a 4b book, against M_D2 0.8865, M_NONE 0.8672 and M_SUBSAMPLE = M_PAIRWISE 0.8198.** On U56 the status-quo chooser's widest-dial pick is N=5 (OOS 17.55% / 0.9512 / -22.87%) while the anchor it walked off reads OOS 16.13% / 1.1042 / -19.47% and clears 4b. **THE CAPITAL FINDING IS NOT THE REPAIR: THE RECORD'S 'TUNE THE WIDEST DIAL' HABIT LOSES TO DOING NOTHING UNDER EVERY CONVENTION, AND COUNT MATCHING IS THE TWO WORST RULES OF THE FIVE.**

  **KEEP paths on all 66 books: 4a 0 of 66; 4b full 16, 4b OOS 17, BOTH 16** (U56 10/22, B136 6/22, **SMALL 0/22 on every path**). **NO NEW CANDIDATE, AND THE PASSER LIST IS AN INDEPENDENT CONFIRMATION OF 1189: TEN OF THE 16 PASSERS ARE ONE BOOK READ AT A DIFFERENT GROSS RUNG** (U56 g=0.55..0.75 spanning Sharpe 1.102047 -> 1.102906, **8.6e-04 over a 1.36x change in gross**; B136 g=0.50..0.70 spanning 1.6e-03 over 1.40x); the rest are the anchor and its N=15 / H=21 neighbours. **This run was not looking for the degenerate gross ladder — it fell out of a census of RANGE statistics, which is the strongest form the confirmation can take. RECORDED AND NOT PROMOTED. No memo.**

  **THE GATE THAT FAILED FIRST, PRINTED RATHER THAN PATCHED OVER.** G1 read **1.977e-02** on the first cut: `build` writes its targets on the ALREADY-SHIFTED rebalance rows (the application row, where `nrun` starts each segment) while `engine.backtest` shifts BOTH the weights frame and the mask, so an application-time frame is lagged a second time; the comparand must be the decision-time frame Wdec[t] = W[t+1]. **No arithmetic changed — the first cut compared two books one trading day apart. G1 now reads 2.78e-17, and every other gate had passed silently through the defect.**

  **PROTOCOL CLAUSE PROPOSED NOT ENACTED (rule 6):** *"no range without its count" — any committed sentence quoting a spread, band or range SHALL state the number of points it was taken over; a sentence comparing two such quantities over DIFFERENT counts SHALL either count-match them or divide each by d2(k); and **a published multiple below d2(k_max)/d2(k_min) SHALL be reported as no effect**.*

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists; SMALL is the current output of a sub-$2B screen less the documented `max_1d_move >= 1.0` exclusion (52 of 715 dropped, 663 names plus SPY as benchmark only). Every LEVEL is optimistic and every 4a/4b count is an UPPER bound. Arm B's quantity is a RATIO of two spreads of the same statistic on the same panel and is far less exposed, but MaxDD is one of its six statistics and a survivorship-flattered panel has a shallower drawdown path, so the levels are published beside every ratio.

  **GATES 9 of 9** (G0 d2 Monte Carlo, G0b 1140's band floor, G1 2.78e-17, G1b engine's two NaN rows inside the warm-up, G3 live v2 U56 MaxDD -12.0549% vs -12.05%, G4/G5 the two order relations at 0 violations, G6 claim-set nesting, G7 the constant-divisor identity).

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_is-the-COUNT-INFLATION-of-max-minus-min-a-GENERAL-DEFECT-in-the-record-s-SPREAD-and-BAND-claims_cloud.py`, 7 CSVs, console log, result note, 5 LEADERBOARD rows. Follow-ups filed 1205 (does the gap from d2 measure ladder degeneracy better than the spread itself), 1206 (does the widest-dial habit lose to doing nothing on a rolling window and other anchors) and 1207 (how many committed verdicts rest on a multiple below its own count inflation).

## 2026-09-17 — Research lane B, idea 1154: does ANY committed REACH or CHOOSER claim survive its OWN P_boot BAR?

**ANSWERED = ALMOST NONE, AND THE FEW THAT DO ARE CERTIFIED BY AN IDENTITY RATHER THAN BY EVIDENCE.** Two dials and no more (CLAIM SET {C_STRICT, C_PROX, C_ALL}, 1197/1199/1155's nesting; BLOCK LENGTH {21, 63, 126, 252} — 1101 used 63 and only 63), every one of the 12 dial cells published, all 72 decisions scored at every cell, nothing selected on. **DATA-FREE FIRST:** a 0.90 bar is a function of the rung count and nothing else at the null — a pick on the 10-rung GROSS ladder must beat its own uniform null **9.00x**, on the 4-rung H and CADENCE ladders only **3.60x**, so *"resolved" is not comparable across ladders unless the rung count travels with it*, which is idea 1155's count-inflation defect arriving on PICKS instead of on SPREADS. **CENSUS of 32,979 committed text units: C_STRICT 1,455 sentences NAME a pick and ADJUDICATE on it and only 229 (0.1574) STATE ANY RESOLUTION FOR IT** (0.1141 state a margin, 0.6392 a rung count); C_PROX 1,661 / 0.1505, C_ALL 6,414 / 0.1085. **THE PRICE ARM, 288 published rows at 1000 joint moving-block redraws each: 1 OF 12 committed decisions clears the 0.90/0.10 bar at the headline C_STRICT x L63; 17 of 72 over the whole population — and 15 OF THOSE 17 ARE GROSS-LADDER DECISIONS, WHERE THE LADDER IS EXACTLY MONOTONE IN IS SHARPE AT 6 OF 6 (panel, anchor) CELLS (Spearman rho = 1.000000, gate G10) OVER AN IS-SHARPE SPREAD OF ONLY 0.001395-0.009050.** argmax-of-monotone is an identity, so the bootstrap re-picks the endpoint on nearly every draw however small the difference; **outside GROSS, 2 of 54 resolve** (N 2/18, H 0/18, CADENCE 0/18). **THE UNSTATED DIAL MOVES THE HEADLINE 57%: resolution runs 0.1944 -> 0.2361 -> 0.2500 -> 0.3056 monotonically as L goes 21 -> 252 on the SAME 72 decisions — a run free to pick L is free to pick its own resolution count, and 1101 named L = 63 but not why.**

  **WHY THE BAR CANNOT DO THE JOB IT WAS ASKED TO DO. P_boot MEASURES ORDER STABILITY, NOT EFFECT SIZE.** The smallest-margin RESOLVED pick (B136/A/GROSS) carries an IS margin of **3.33e-04** and is CERTIFIED at 0.9410; the largest-margin UNRESOLVED pick (SMALL/B/H) carries **2.28e-01, 685x larger**, and is REJECTED at 0.8120. **Stated at its true strength, which is NOT "inverted": median IS margin 9.739e-03 among the resolved against 9.369e-03 among the unresolved (ratio 1.0395), Spearman 0.1551 over all 72 — the two are NEAR-ORTHOGONAL, which is WORSE for the bar than an inversion would be, because it means the bar carries NO effect-size information at all.** The hypothesis this arm was written to test — that idea 1120's cheap MARGIN column is a free substitute for the bootstrap — is REFUTED, and so is its converse: a run publishing one has not published the other.

  **RULE 8 WALK-FORWARD (picks chosen on 2009-2016 ALONE, 2017-2026 read ONCE; R_RAW = publish the IS argmax always, the record's habit; R_BAR = publish only if P_pick >= 0.90 else stay at the anchor; R_ANCHOR = never move, 1155's control): mean OOS Sharpe 0.7996 / 0.7887 / 0.7901 on C_ALL — inside 0.011, a distinction worth nothing. THE CAPITAL FINDING IS THE 4b COUNT: R_RAW reaches 6 of 72 capital-worthy books against the DO-NOTHING control's 24 of 72, and the bar recovers 19 purely by refusing to move 59 of 72 times.** On C_STRICT the anchor wins outright (0.8750 vs 0.8496 OOS Sharpe, 4 vs 2 4b books, -0.2523 vs -0.2709 OOS MaxDD). **A BAR WHOSE ONLY DEMONSTRATED VALUE IS THAT IT STOPS YOU MOVING IS AN EXPENSIVE WAY TO DO NOTHING, AND DOING NOTHING IS FREE.** This is idea 1155's "the widest-dial habit loses to doing nothing" arriving from a second direction, on a different statistic and a different population.

  **KEEP paths on all 144 distinct rung books: 4a 0 of 144; 4b full 20, 4b OOS 24, BOTH 19** (U56 14/48, B136 6/48, **SMALL 0 of 48 on every path**; SMALL leg rates L_H1 0.021, L_H2 0.000, L_OOS 0.000, L_DD 0.104, L_CAGR 0.021). **NO NEW CANDIDATE. FIVE OF THE U56 PASSERS ARE ONE BOOK READ AT A DIFFERENT GROSS RUNG — N=20/H=126/W at g = 0.55..0.75 spans Sharpe 1.137390 -> 1.138079, 6.89e-04 over a 1.36x change in gross** (B136 g = 0.55..0.70 spans 1.27e-03). That is idea 1189's degenerate gross ladder arriving from a fifth direction, and here it is **not a side observation but THE MECHANISM OF THE HEADLINE RESULT**, since it is exactly that degeneracy which lets the bootstrap certify the GROSS picks. U56 N=12/H=126/g0.75/W reads full 17.65% / 1.1658 / -20.17%, OOS 18.78% / 1.1701 / -20.17%, ahead of the standing incumbent — **and this run's own machinery calls the U56 N-ladder pick UNRESOLVED (P_pick 0.4290), so preferring it would be selecting on the OOS window rule 8 exists to forbid. RECORDED AND NOT PROMOTED. No memo.**

  **THE GATE THAT FAILED FIRST, PRINTED RATHER THAN PATCHED OVER.** G6 read **5.975e-02** against a hand-set 0.05. That was a **mis-specified bar, not broken machinery**: draws inside one exchangeable ladder share that ladder's realisation, so the effective sample is the LADDER count M, and at M = 40 the position-mean's own SE is 0.063 — the "failure" sat at 0.95 SE. The bar is now **derived** (3 x sqrt(p(1-p)/M) at p = 1/k) and M raised to 200 until the gate can discriminate. **A gate that cannot reject is not a gate.** **A SECOND DEFECT WAS CAUGHT BY G2 BEFORE ANY RESULT WAS READ:** the first cut built selections on the ALREADY-SHIFTED rebalance rows (1155's convention) and missed the record's committed U56 anchor triple by **3.69e-02** — decision and application landing on the same row. Corrected to 1161's decide-at-t / apply-at-t+1 frame, which is what protocol rule 2 requires and which reproduces the committed triple at **1.622e-03**.

  **PROTOCOL CLAUSE PROPOSED NOT ENACTED (rule 6):** *"no resolution claim without its ladder shape" — any committed sentence calling a pick, reach or argmax RESOLVED, STABLE or SIGNIFICANT under a resampling bar SHALL state (i) the rung count k, since the bar's own uniform null is 1/k, (ii) the block length the bar was computed at, since the resolution rate on this record's own ladders moves 0.1944 -> 0.3056 across L in {21, 252} with no other change, and (iii) the pick's IS MARGIN beside the probability, since the two are near-orthogonal (Spearman 0.1551). **A pick on a ladder that is monotone in the chooser's own statistic SHALL NOT be reported as resolved at all: there the bar certifies an identity and is silent on the effect.***

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT lists; SMALL is the current output of a sub-$2B screen less the documented `max_1d_move >= 1.0` exclusion (52 of 715 dropped, 663 names plus SPY as benchmark only, excluded from SMALL's eligible set). Every LEVEL is optimistic and every 4a/4b count is an UPPER bound. P_boot is a ratio of resampled argmax counts on one panel's own ladder and is far less exposed, but the levels are published beside it. **DECLARED APPROXIMATION (1101's, restated):** P_boot resamples ONE tape, so every value is CLOSER TO 0 OR 1 than the truth and "resolved" is scored in the direction that FAVOURS the record — a decision this run calls unresolved is unresolved *a fortiori*.

  **GATES 10 of 10** (G1 fast runner == `engine.backtest` 1.388e-17; G2 cross-run 1101's committed U56 anchor-A triple 1.622e-03; G3 live RULES v2 U56 MaxDD -12.0549% vs -12.05%; G4 SPY OOS triple 2.894e-03; G5 claim sets nest 0; G6 exchangeable ladders' position-mean P_boot == uniform null 1/5 at 1.848e-02 against a derived 3-SE bar of 0.0849; G7 degenerate ladder puts all mass on the FIRST rung at 0, re-gating 1199's first-wins; G8 bootstrap determinism 0; G9 cross-run 1101's U56 anchor-A reach set {GROSS, CADENCE} 0; G10 GROSS exactly monotone at 6 of 6 cells 0).

  **KILL as a capital finding.** Script `research/backtests/2026-09-17_does-any-committed-REACH-or-CHOOSER-claim-survive-its-OWN-P_boot-BAR_B.py`, 11 CSVs, console log, result note, 5 LEADERBOARD rows. Follow-ups filed 1208 (is the block length the record's largest unstated dial across every resampling claim it publishes), 1209 (does a monotone-ladder exclusion leave the record any resolvable pick at all) and 1210 (how many committed picks would change if the chooser had to beat the anchor by its own bootstrap SE rather than merely outrank it).

## 2026-09-17 — cloud lane, idea 1208: is-the-SMALL-PANEL-REVERSAL-of-the-DO-NOTHING-ADVANTAGE-a-PANEL-FACT-or-a-TURNOVER-FACT — KILL (capital) / ANSWERED = NEITHER

1206 committed a large panel split as "the largest real effect in the run": the do-nothing control
C_ANCHOR top on U56 (1.2165) and B136 (1.1708) and BEHIND on SMALL (0.5298 vs 0.6718-0.6783). The
queue read it as a cost rebate the count-matched rules collect on a higher-turnover panel. Re-walking
all 472 picks per rule over 12 grid points (COST {0,10,25,50} bps x MATCH {T_NONE, T_BAND, T_CHARGE}),
39,648 pick-cells, 1206's 21 BY PANEL cells reproduced to 4.69e-05:

- THE COST CHANNEL IS DEAD. SMALL reversal +0.1455 / +0.1463 / +0.1451 / +0.1744 across the four cost
  rungs — 0.99x of its 10 bps size at ZERO bps, and not monotone. Charging every book the anchor's
  own selection turnover moves it -0.0046 (3.1% of its level, the WRONG SIGN for the hypothesis).
- THE EXACT ARITHMETIC. The count-matched picks turn over 4.85/yr against the anchor's 5.32/yr; at
  16.32% vol that 0.47/yr rebate is worth +0.0029 of Sharpe = 1.96% of the reversal at 10 bps and
  9.80% at 50 bps. The mechanism 1206 pointed at is absent: the count-matched rules land on
  CADENCE=M at 0.2014 of SMALL picks, LESS often than the anchor set's own 0.2500.
- WHAT IT IS INSTEAD. The whole effect is a REACH channel: restricting the chooser to rungs within
  +/-25% of the anchor's turnover moves it -0.2175, and reads -0.2103 at ZERO cost, so it prices
  nothing. The rules' SMALL advantage is that they reach books far from the anchor in turnover whose
  GROSS return is higher — not that their moves are cheap.
- AND IT DOES NOT CLEAR ITS OWN ERROR BAR. Paired on (anchor, window, fold) with SE clustered by the
  14 non-overlapping calendar folds: +0.1463, SE 0.0985, t +1.48. 0 of 12 grid points clear 2 SE on
  SMALL, 0 of 12 on U56, 0 of 12 on B136. Resolution needs 25 folds against the 14 this tape
  supplies. 1206's "largest real effect" is the largest UNRESOLVED effect.
- A DEFECT THE GATES CAUGHT FIRST. Both matches were written on the natural reading that turnover is
  LINEAR IN GROSS (turn(g) = g*turn(1)). G2 rejected it at 0.0173 of one-way turnover on the record's
  anchor before any result was read: drifted weights are renormalised by a portfolio value carrying a
  1-g CASH SLEEVE, so the restoring trade depends on the rung. The gross ladder is the selection frame
  scaled in WEIGHTS (1206's G2) but NOT in TURNOVER. Any committed claim that re-prices a gross ladder
  by scaling its turnover is wrong by up to this much.
- RULE 8. The reversal read separately IS (2013-2016) vs OOS (2017-2026): SMALL +0.3103 / +0.1135,
  U56 -0.0303 / -0.0505, B136 +0.0054 / -0.0618 at the reference cell. T_CHARGE tracks T_NONE in both
  windows (+0.3205 / +0.1059); T_BAND FLIPS SIGN out of sample (+0.0436 / -0.0942) and is not monotone
  in its own band (-0.0451 / -0.0712 / +0.0191 at 0.10 / 0.25 / 0.50), so it is reported, not relied on.
- BOTH KEEP PATHS. Books: 4a 0/228 at 0 bps, 1/228 at 10/25/50; 4b full+OOS 51/43/36/25 of 228.
  Stitched curves: 4a 0 of 252 at EVERY one of the 12 grid points; 4b full+OOS 276 of 3,024 — U56 240,
  B136 36, SMALL 0. The 43 book-level passers at 10 bps are the same 43 1206 recorded and declined to
  promote. NOT PROMOTED, NO MEMO, NO RULES CHANGE.
- SURVIVORSHIP (rule 9): B136 and SMALL are CURRENT constituents; SMALL is the sub-$2B screen (663
  names after dropping every max_1d_move >= 1.0 ticker) starting 2010.
- Gates 11 of 11 after G2 failed at 0.0173 on the first cut. Runtime 57s, offline, deterministic.

## 2026-09-17 — cloud lane, idea 1210: should-a-CHOOSER-have-to-BEAT-THE-ANCHOR-BY-ITS-OWN-BOOTSTRAP-SE-rather-than-merely-OUTRANK-IT — KILL (capital) / ANSWERED = NO

1154 published R_RAW (publish the IS argmax) at mean OOS Sharpe 0.7996 and 6 of 72 4b books,
R_BAR (publish only if P_pick >= 0.90) at 0.7887 / 19, R_ANCHOR (never move) at 0.7901 / 24, and
read it as "the publish-the-argmax habit loses three quarters of its capital-worthy books to doing
nothing". The queue proposed a third rule, R_SE(k, basis): move only when the IS argmax beats the
ANCHOR by k bootstrap SE of the DIFFERENCE. Tested at 7 k x 3 SE bases, every cell published, with
1154's construction and SEED BASE inherited whole so the reproductions are bit-level (all three
mean OOS Sharpes to 0.0, Spearman 0.1551 to 2.06e-05, resolved count 17 of 72 exactly):

- THE TRAP, DECLARED BEFORE ANY NUMBER. R_SE is monotone in k (G9) and at large k IS R_ANCHOR, so
  "R_SE recovers the 18 books R_RAW loses" is true by construction and worth nothing. The only
  test that means anything is at a MATCHED MOVE COUNT, against R_BAR and against a count-matched
  RANDOM bar (500 seeded uniform draws of the same size from R_RAW's 59 movers).
- NEITHER BAR CARRIES INFORMATION. Of the 18 R_SE cells with k > 0, 0 clear the 90th percentile of
  their own count-matched null on 4b count and 0 on mean OOS Sharpe; the best sits at 0.815 and
  k=0.5 SE_JOINT sits at 0.006 on Sharpe.
- AND THE RANK BAR IS WORSE THAN A COIN. R_BAR moves 13 and books 19 where a random bar moving 13
  books a median of 20 (p10 18, p90 22): percentile 0.263 on 4b, 0.248 on Sharpe. 1154's "19 books
  against R_RAW's 6" is bought entirely by moving 13 times instead of 59.
- THE LAW, FITTED TO NO DATA. The line through the record's own two endpoints (R_ANCHOR 24 at 0
  moves, R_RAW 6 at 59) is 4b(m) = 24.0 - 0.3051*m. It tracks the count-matched null median to a
  worst deviation of 0.66 of a book over m = 0-59, and across all 20 published rules move count
  alone explains R^2 = 0.9798 of the 4b count. Mean OOS Sharpe over those same 20 rules spans only
  0.7828-0.7996. THE 4b COUNT IN THIS FAMILY IS A THRESHOLD CENSUS OF HOW OFTEN A RULE DECLINED TO
  MOVE, NOT A RETURN.
- BYCATCH 1: A CORRELATION-BLIND ERROR BAR OVERSTATES A LADDER DIFFERENCE BY ~3.2x. The pick's and
  the anchor's bootstrap statistics co-move at median correlation 0.9247 (min 0.6777), so the SD of
  the difference is far below the legs added in quadrature: median SE_MARGINAL / SE_JOINT = 3.1580
  (G10). SE_MARGINAL moves 6 times at k=1 where SE_JOINT moves 28.
- BYCATCH 2: THE BOOTSTRAP SEED IS AN UNSTATED DIAL IN EVERY P_boot CLAIM. P_pick is a mean of
  B=1000 Bernoulli draws with MC SE up to 0.0158, so a decision within that of the 0.90 bar is
  called by the seed: the same 72 decisions at L=63 under four seed bases give resolved 16-17 of
  72, Spearman 0.1551-0.1749 and R_BAR move count 12-13, nothing else changed. 1154 stated its seed
  base; no other P_boot claim in the record does.
- THE SE BAR IS NOT MERELY A RE-SCALED MARGIN BAR. Spearman(delta, delta/SE) +0.5560 over the 59
  movers, SE max/min 158.6 (G8): it varies, it just does not vary in a way that predicts which
  moves keep a book.
- RULE 8 TWICE. Picks use 2009-2016 only (1154's split); this run's own two dials were chosen on
  2017-2021 and 2022-2026 read ONCE. IS-chosen cell k=1.5 SE_JOINT (19 moves), IS 1.0100 ->
  OOS-B 0.5580 / 7.17% / -17.23%, against R_BAR 0.5517, R_ANCHOR 0.5543 and R_RAW 0.6042 — the rule
  that moved all 59 times is the best of the four out of sample.
- BOTH KEEP PATHS on 144 distinct rung books: 4a 0 of 144; 4b full 20, 4b OOS 24, BOTH 19 — U56
  14/48, B136 6/48, SMALL 0/48. These are 1154's own rung books; this run adds none. NOT PROMOTED,
  NO MEMO, NO RULES CHANGE.
- SURVIVORSHIP (rule 9): B136 and SMALL are CURRENT constituents; SMALL is the sub-$2B screen with
  52 of 715 tickers dropped for max_1d_move >= 1.0 (663 names), SPY excluded from its eligible set.
- Gates 12 of 12. Runtime 105s, offline, deterministic.

## 2026-09-17 — lane B, idea 1219: should-every-committed-t-carry-its-SE-BASIS-and-its-L — KILL (capital) / ANSWERED = YES for the clause, NO for the money

1212 published "210 committed t-values recovered; 10 classify as fold-clustered" from a scan of
the record's TEXT, and stated its own limitation: most committed t's do not name their SE basis
within 200 characters. This run reads the EMITTING SCRIPT instead. Two dials, CLAIM SET
{C_1212, C_HEAD, C_MEMO, C_ALL} x RECOVERY {R_TEXT200, R_TEXTWIDE, R_SCRIPT, R_SCRIPT_STRICT},
16 cells all published. 1212's census reproduces at EXACTLY 210 hits and EXACTLY 10 fold at its
own commit 0743d51 (gates G1, G9), so the comparison is like for like.

- THE RECOVERY. Script 86 of 210 (0.410) against text 18 of 210 (0.086) — 4.78x. But
  R_SCRIPT_STRICT, which is what "carries its basis AND its L" actually means, is 4 of 210
  (0.019). All 210 claims map to a script and all 210 scripts are on disk, so the mapping is not
  the bottleneck: 201 of 210 sit in a script that declares NO L at all, 9 in one that pins a
  unique L. Basis mix S_IID 82, S_BLOCK 4, AMBIGUOUS 6, NO NAMED BASIS 118.
- THE CORRECTION TO 1212, AND IT POINTS THE HARDER WAY. 0 of its 10 fold-clustered t's have an
  emitting script containing ANY fold-SE machinery, and R_SCRIPT finds 0 fold-SE t's in the whole
  210. The 10 were keyword collisions, chiefly on the word "year". 1212's census should be read
  as 0, not 10, and its limitation note understated the problem rather than overstating it.
  NO_SE is reported as a FLOOR and not a proof: 47 of the 53 NO_SE scripts contain "SE", "std(",
  ".sem(" or "np.sqrt(" somewhere, so the rule means "no NAMED basis it recognises".
- THE BARS, RE-MEASURED NOT QUOTED. 90 disjoint gross-matched null pairs (mean dSharpe -0.0025,
  pair corr 0.9037, exchangeability gated before anything was read). 1212's 15-cell crit95 table
  reproduces to a worst deviation of 0.1636 on independent seeds and its central claim survives:
  L is the dial, not the basis — S_FOLD 1.6375 -> 2.5605 (x1.56 against 1212's x1.54), S_BLOCK
  1.9530 -> 2.3266 (x1.19 against x1.24), S_IID x1.03, S_NW x1.23.
- THE EXPOSURE. Ambiguity band [1.6375, 2.5605]: 27 of 210 committed t's (0.1286), and 27 of the
  126 that clear 1.96 (0.2143), are significant under some (basis, L) the record leaves open and
  not under others. 0 of the 86 RESOLVABLE t's are lost at their own bar — they are overwhelmingly
  S_IID, whose honest bar already IS 1.96. The exposure is entirely in the 124 that cannot be
  resolved. C_MEMO 44 of 358 in band, C_ALL 64 of 480.
- BYCATCH, MEASURED FIRST AND NOT REPAIRED QUIETLY. A Sharpe-scale HAC SE's DENOMINATOR is an
  unstated dial of its own: dividing by sd(ra - rb) instead of the pooled BOOK volatility
  under-rejects by ~2x (crit95 0.9219-1.2778 against a nominal 1.96), because two books correlated
  at 0.90 have a difference whose SD is far below either book's. Published as S_NW_DIFFSD; it is
  not a rung and enters nothing downstream.
- RULE 8 AND THE CAPITAL CONTENT. 216 books chosen on 2009-2016 with 2017-2026 read ONCE, plus 960
  publish decisions (24 contexts x 4 bases x 5 L x 2 bars). HONEST bar minus QUOTED 1.96 over the
  20 (basis, L) cells: +0.0010 of mean OOS Sharpe (SE 0.0014, t +0.71), moves -0.10, 4b +0.00, and
  the two bars make IDENTICAL decisions at 13 of 20 cells. Doing nothing returns 0.8575; 10 of 40
  rules beat it and 5 of 40 clear their own count-matched random bar against the 4.0 of 40 chance
  predicts — 1210's and 1221's finding arriving a third time.
- BOTH KEEP PATHS. 4a 0 of 216; 4b full 12, 4b OOS 13, BOTH 12 over 12 DISTINCT books (U56 11,
  B136 1, SMALL 0). Best U56 / W / N=12 / H=126: full 17.65% / 1.1658 / -20.17% (halves 1.2741 /
  1.0833), OOS 18.78% / 1.1701 / -20.17% — the book 1183 and 1212 already committed.
  CONFIRMATORY, NOT GENERATIVE. NOT PROMOTED, NO MEMO, NO RULES CHANGE.
- SURVIVORSHIP (rule 9). B136 and SMALL are CURRENT constituents; SMALL is the sub-$2B screen with
  52 of 715 tickers dropped for max_1d_move >= 1.0 (663 names), SPY excluded from its eligible set.
- Gates 11 of 11. Runtime 103s, offline, deterministic.

## 2026-09-17 — lane B, idea 1226: does-any-committed-SE-in-the-record-state-what-it-DIVIDED-BY — KILL (capital) / ANSWERED = NO

1219 published as declared bycatch that the SAME Newey-West SE of the same daily difference
calibrates at crit95 2.0161-2.4754 when the pooled BOOK volatility puts it on the Sharpe scale
and 0.9219-1.2778 when the SD of the DIFFERENCE does, stated its own choice in a source comment
and stopped. An SE on a ratio scale is a quotient; 1212 and 1219 censused the numerator's basis
and its L, and nobody has ever censused the bottom half. Two dials, CLAIM SET {C_1219, C_HEAD,
C_MEMO, C_ALL} x DENOMINATOR SET {D_PAIR, D_VOL3, D_VOL4, D_ALL5}, 16 cells all published. The
SE NUMERATOR is held FIXED at one HAC/Bartlett construction for the whole run and the L ladder
is 1212's, reported at every rung and never chosen, so the spread below is the denominator and
nothing else. 1219's two committed bands reproduce to 3.3e-05 and 4.7e-05 on its own null pairs
(gates G7, G8), so this measures the record's object rather than a lookalike.

- THE ANSWER, AND IT IS NO. 0 of 113 (C_1219), 0 of 115 (C_HEAD) and 0 of 264 (C_MEMO)
  committed SEs name their denominator in the committed sentence; the emitting script pins one
  for 3, 3 and 4. C_ALL rises to 43 of 316 (0.1361) ONLY because every one of its 38
  text-stating hits is in QUEUE.md — the entries that ASK this question, 1219's bycatch note
  and idea 1226 itself. NOT ONE result sentence in LEADERBOARD.md or CHANGELOG.md names what
  its SE divided by, at any claim set. Same on the t population: 0 of 211 in text, 7 in script.
  NOT_RATIO (38-81 per set) is published separately and folded into neither pass nor fail.
- THE SIZE OF THE DIAL. D_PAIR band [0.9219, 2.4754] = x2.69 with numerator, basis and L held
  fixed; D_ALL5 [0.9219, 4.0721] = x4.42; at a SINGLE L it is x2.80-x3.35, so L cannot be
  blamed. THE EXPOSURE IS ONE DISTINCTION, NOT FIVE: D_BOOKVOL, D_ANCHOR and D_MAXVOL agree to
  0.4% — any book-volatility reading gives the same bar — and the whole factor is difference-SD
  against any book vol, because two books correlated at 0.9037 have a difference whose SD is
  0.4381 of theirs. D_SPYVOL (the information-ratio scale) 2.9064-4.0721. The iid-bootstrap SE
  of the Sharpe DIFFERENCE calibrates at 1.9697, which is where the quoted 1.96 comes from.
- THE EXPOSURE, 16 CELLS. On the narrowest honest reading 55 of 211 C_HEAD committed t's
  (0.2607), and 0.4365 of the ones published as significant, have a verdict that is a function
  of a choice nobody wrote down; 15 are significant at 1.96 and not at the strictest bar in the
  pair. C_1219 54 of 210, C_MEMO 95 of 359, C_ALL 131 of 482. D_ALL5 takes C_HEAD to 96 of 211.
- BYCATCH, MEASURED FIRST AND NOT REPAIRED QUIETLY. The crit95 is itself a measurement: per
  cell its Monte-Carlo SE (resampling the 90 PAIRS, 400 draws) is 0.1120-0.2028 on the book-vol
  rungs and 0.2634-0.4863 on D_SPYVOL, and an independent seed base moves the same bands by up
  to 0.4035 — about 2 SE, and larger than several differences the record has published as
  findings. The headline deliberately uses 1219's OWN pairs so nothing rests on this run's
  draw; the 16 exposure cells move by at most 30 claims on the replicated bands and do not
  reorder.
- RULE 8 AND THE CAPITAL CONTENT. 216 books chosen on 2009-2016 with 2017-2026 read ONCE, plus
  1,200 publish decisions (24 contexts x 5 denominators x 5 L x 2 bars). HONEST bar minus
  QUOTED 1.96: +0.0014 of mean OOS Sharpe (SE 0.0014, t +1.05), moves -1.72, 4b +0.00, and the
  two bars make IDENTICAL decisions at 10 of 25 cells. But the DIAL is not nothing: at the
  quoted bar D_DIFFSD moves 0 of 24 contexts at EVERY L while D_BOOKVOL moves 7.0, D_ANCHOR
  7.2, D_MAXVOL 8.2 and D_SPYVOL 10.6 — had the record divided by the difference SD, no publish
  decision in this whole family would ever have been made. Every reading still lands at or
  below doing nothing (0.8505-0.8575 against 0.8575). 9 of 50 rules clear their own
  count-matched random bar against 5.0 by chance (one-sided binomial p 0.0579) — not
  separation, and H_ANCHOR's declared "at least one rule" bar was too weak; read it as no.
  1210's, 1219's, 1221's and 1227's finding arriving a fifth time.
- BOTH KEEP PATHS. 4a 0 of 216; 4b full AND OOS 12 of 216 over 12 DISTINCT books (U56 11, B136
  1, SMALL 0). Best U56 / W / N=12 / H=126: full 17.65% / 1.1658 / -20.17% (halves 1.2741 /
  1.0833), OOS 18.78% / 1.1701 / -20.17% — the book 1183, 1212 and 1219 already committed.
  CONFIRMATORY, NOT GENERATIVE. NOT PROMOTED, NO MEMO, NO RULES CHANGE.
- SURVIVORSHIP (rule 9). B136 and SMALL are CURRENT constituents; SMALL is the sub-$2B screen
  with 52 of 715 tickers dropped for max_1d_move >= 1.0 (663 names), SPY excluded from its
  eligible set. The census arms scan committed text and source and carry no market bias; the
  bias largely cancels out of a critical-value RATIO, which is the whole headline, and does NOT
  cancel out of the OOS levels or the 4b legs, so any pass there is an upper bound.
- Gates 12 of 12. Runtime 76s, offline, deterministic.

## 2026-09-17 — lane C, idea 1230: how-many-committed-MOVE-RATES-count-a-pick-onto-an-ANCHOR-EQUIVALENT-KEY-as-a-MOVE — KILL (capital) / ANSWERED = 3 of 70

1227 gated that ("N",20), ("H",126), ("GROSS",0.75) and ("CADENCE","W") are ONE book bit for
bit, so a chooser "moving" onto one of them has not moved and a null drawing one has drawn a
NO-OP. It measured the damage on its own grid and stopped. This run censuses the record for the
same error and re-prices what turns on it. Two dials, CLAIM CLASS {C_HEAD, C_MEMO, C_ALL,
C_SCRIPT} x EQUIVALENCE TEST {E_KEY, E_EXACT, E_FP12, E_RET8, E_DEC6, E_DEC3}, 24 cells all
published. The BOOKS are frozen at 1207/1214/1223/1227's construction and do not change with
either dial (gate G12, max deviation exactly 0.0), so every Sharpe and every delta below is
identical across the equivalence dial; what moves is the move rate, the null, and the publish
decision. 1223's committed move rates and mean OOS Sharpes reproduce to 3.810e-04 (G8) and
1227's committed 0.0280 overstatement to 3.857e-05 (G15), so this measures the record's object
rather than a lookalike.

- THE ANSWER, AND IT IS 3 of 70. Of 179 committed MOVE RATES (from 447 candidate text units;
  268 NOT_MOVE_RATE are published separately and folded into neither pass nor fail), 3 of 70
  C_HEAD units and 11 of 107 C_MEMO units are EXPOSED — key-based on a grid whose anchor book
  appears under two or more keys. 46 of 70 C_HEAD units sit on a grid that is NOT degenerate and
  are therefore correct as written; 17 are UNRECOVERABLE. THE BIGGER NUMBER IS THE SILENCE: only
  3 of 70 committed move rates (0.0429) state their equivalence basis in the committed sentence,
  and 0 of 118 move-computing SCRIPTS name one. Attribution reaches 49 of 49 LEADERBOARD rows,
  because rule 5 puts the script in the last column, and 3 of 21 CHANGELOG ones, because this
  file does not.
- THE EQUIVALENCE TEST IS NOT THE DIAL. E_EXACT, E_FP12 (1e-12), E_RET8 (1e-8), E_DEC6 and
  E_DEC3 all return the IDENTICAL class of four keys: the nearest non-equivalent book is
  8.453e-03 away in max |Delta r|, four orders above the loosest tolerance, so no borderline
  case exists for a tolerance to adjudicate. The whole exposure is the KEY/VALUE distinction.
  Bycatch: the GROSS rungs are NOT decision-equivalent per fold (max |Delta fold Sharpe|
  4.915e-02) — the record's gross-Sharpe degeneracy is a FULL-SAMPLE fact, not a per-fold one.
- THE SIZE OF THE ERROR. Move rate 0.3817 on the KEY definition against 0.3537 on every value
  test over 14,520 pick-cells: 0.0280 of all pick-cells are "moves" onto a book that IS the
  anchor. The null's destination pool is 21 books of which 3 are no-ops, against a clean 18.
- WHAT IT COSTS THE NULL, AND WHAT IT DOES NOT. Individual p-values move by up to 0.2695
  (NL_COUNT) and 0.3310 (NL_PERM), and the shift is NOT one-signed: mean p(KEY) - p(EXACT) is
  -0.0141 on NL_COUNT, where no-op destinations shrink the null toward zero and the contaminated
  pool OVER-publishes, and +0.0143 on NL_PERM, where dropping no-ops from the chooser's own
  destination multiset makes the re-dealt null more aggressive. 0 of 120 publish decisions flip
  at 0.05 — but exactly 1 cell publishes at all at 0.05 under either pool, so that zero is a
  fact about this family's emptiness and not about the dial. The bar is walked rather than
  assumed: 4 of 120 cells flip at some alpha (2 at 0.15-0.20 on NL_COUNT, 4 at 0.50 on NL_PERM).
  INERT HERE, NOT INERT IN GENERAL.
- RULE 8 AND THE CAPITAL CONTENT. 360 deploy/hold decisions per equivalence test, chosen on the
  folds closing before 2017-01-01 and read once on 2017-2026: deploy-iff-the-IS-gain-clears-its-
  own-matched-null returns mean OOS fold Sharpe 1.1157 (E_KEY, 18 deploys), 1.1164 (E_FP12 /
  E_RET8), 1.1167 (E_EXACT, 16) and 1.1169 (E_DEC6 / E_DEC3, 15), against 1.1178 for ALWAYS-HOLD.
  EVERY POOL LOSES TO DOING NOTHING; cleaning the pool is worth +0.0010 of mean OOS fold Sharpe
  and removes 2 of 18 deployments that were bought with no-op destinations. It improves the loss
  without reversing it. 1206's, 1221's, 1226's and 1227's finding arriving a fifth time.
- BOTH KEEP PATHS, AND THE BYCATCH SCORED AGAINST THIS RUN ITSELF. 4a 0 of 66 rung books and 0
  of 360 stitched curves. 4b full 17, 4b OOS 16, BOTH 16 — but those 16 key-rows are 13 DISTINCT
  books, because the U56 anchor passes four times under N=20, H=126, GROSS=0.75 and CADENCE=W.
  This run's own headline pass count carries the exact error it censuses, and the honest figures
  are 14 / 13 / 13; that is idea 1194's gross-free key confirmed by VALUE rather than argued.
  Best 4b book U56 / N=15: full 16.70% / 1.1349 / -20.14% (halves 1.2022 / 1.0910), OOS 18.87% /
  1.1894 / -20.14% against SPY OOS 15.15% / 0.8686 / -33.72%. Its drawdown leg clears by 9 bp
  (-20.14% against the 0.60 x SPY cap of -20.23%), so read it as marginal. CONFIRMATORY, NOT
  GENERATIVE — the books pre-date this idea. NOT PROMOTED, NO MEMO, NO RULES CHANGE.
- THE CENSUS IS REFLEXIVE, AND RE-RUNNING IT IS HOW THAT WAS CAUGHT. A census of the record,
  committed to the record, changes the record it censused. Measured, not argued, on three
  executions of this one script: C_ALL read 179, then 185 once the run's OWN console log was on
  disk for the next pass to scan; C_HEAD read 70 on the tree AS READ and 81 once this entry and
  this run's five LEADERBOARD rows landed. THE FINDING IS INVARIANT AND THE DENOMINATOR IS NOT:
  EXPOSED (1 + 2), NOT_DEGENERATE (46) and UNRECOVERABLE (17) are bit-identical across both
  C_HEAD readings; all 11 new units are CLEAN, because this run's own sentences state their
  basis, and states_basis rises 3 -> 10 for the same reason. The headline 3 of 70 is the census
  of the record as it stood BEFORE this run; the same script on the post-commit tree reads 3 of
  81. The committed .console.txt carries the second reading. NO FIXED POINT EXISTS for the
  console leg, because each execution overwrites the input the next one reads — 1088's rotating
  -window problem in a second place, and a standing hazard for every census the record runs.
  7 of the 32 QUEUE units sit on this very 1223/1227/1230 thread, i.e. are entries that ASK the
  question; they are counted where they fall and named here so the population is reproducible.
- SURVIVORSHIP (rule 9). B136 and SMALL are CURRENT constituents; SMALL is the sub-$2B screen
  with 51 of 715 tickers dropped for max_1d_move >= 1.0 (664 names), SPY excluded from its
  eligible set. The census arms scan committed text and source and carry no market bias; the
  bias does NOT cancel out of the OOS levels or the 4b legs, so any pass there is an upper bound.
- Gates 20 of 20. Runtime 32s, offline, deterministic.

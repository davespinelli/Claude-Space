- 2026-09-16 (cloud lane, idea 1071 does-the-0.97pp-DRAWDOWN-MISS-close-under-a-NAME-CAP-instead-of-a-GROSS-CUT) — **ANSWERED = NO, AND THE n DIAL RUNS THE WRONG WAY. KILL of both dials the queue named; KILL of the "diversification cuts drawdown" premise the idea rests on; CONFIRM, with its own arithmetic, of 1064's finding that the gross rung is the only dial that moves the miss. No RULES change, no book promoted, no PROTOCOL edit (rule 6); RULES.md, PROTOCOL.md, scan.py, bot.py and baseline.py untouched.** SELECTION: this lane takes the FIRST eligible open idea; 1071 mentions no EDGAR / Form 4 / 8-K / options / spin-offs / live data and carries a real capital book.

  **THE TWO DIALS AND NO MORE (PROTOCOL rule 4):** N {20, 25, 30, 40} x CAPMULT k {1.00, 1.25, 1.50, 2.00, INF}, per-name target weight `w_i = min(g/n_sel, k*g/N)` with the residual in CASH. All 20 points published on BOTH panels, for the book AND for the null. Fixed, NOT dials: CAND20 legs, max_vol 0.60, gross 0.75, 10 bps, LAG 1, weekly cadence, min hold 126 — 936/1064's construction verbatim. 1064's 5-rung GROSS ladder is REPRODUCED as the comparand, not tuned.

  **DECLARED BEFORE ANY NUMBER, so it could not be presented later as a finding:** k < 1 IS a gross cut (in a book that fills all N slots it binds uniformly) and is therefore NOT on the ladder; for k >= 1 the cap binds only through BREADTH, because the eligibility gate leaves n_sel < N on thin days and the incumbent construction answers by CONCENTRATING. The cap is thus a de-gross that fires exactly when breadth thins, and thin breadth is when drawdowns happen. That was the refutable claim, and gate G5 tests whether the dial is live at all before any result is read.

  **THE ANSWER.** MISS = max(|MaxDD| - |live -12.05%|, 0.70*SPY_CAGR - CAGR) in pp. **The window is open at 0 of 20 points on BOTH panels.** Best grid MISS **7.049 pp** (U56, N=20/k=1.00) and **6.538 pp** (B136) against the plain gross ladder's **1.270 pp** (U56, g=0.45) and **1.012 pp** (B136) — the reweighting dials land **5.5x further from the window than de-grossing**, and the grid's own best point is the SMALLEST n and the TIGHTEST cap, i.e. it points back down the ladder it came from. **H_NDIAL FAILS IN THE DECLARED-OPPOSITE DIRECTION**: MaxDD worsens monotonically in n (U56 -19.13% / -21.16% / -22.07% / -22.98%; B136 -20.74% -> -25.38%) while CAGR falls monotonically too (U56 15.58% / 14.89% / 14.01% / 13.46%). The premise the queue wrote the idea on is false in both of its clauses: **this book's drawdowns are COMMON-FACTOR, not idiosyncratic**, so extra names buy no protection and only dilute. The cap dial is LIVE (k=1.00 binds 9.0% of U56 rebalance dates at N=20, 21.9% at N=40, mean realised gross 0.710 -> 0.702) but its whole effect on drawdown is **<= 0.92 pp** within any N, bought with cash at 0.31 pp of CAGR — a breadth-triggered micro-de-gross, not a third mechanism.

  **THE NULL AT MATCHED REALISED DRAWDOWN (the queue's own comparand, 20 seeds x 40 points).** Each gross- and holding-count-matched null path is scaled to the book's own |MaxDD|. **H_NULL PASS at 1.000** — the book beats the null's median CAGR at **40 of 40** points. But the EDGE COLLAPSES ALONG THE n DIAL: U56 **+5.07 pp** at n=20 (15.58% vs 10.51%, percentile 1.00) -> **+0.49 pp** at n=40 (13.46% vs 12.97%) -> **+0.13 pp** at n=40/k=1.00 (12.99% vs 12.87%, **percentile 0.65, inside its own null**). Widening n does not merely fail to cut drawdown; it spends the selection edge that justifies holding the book at all.

  **RULE 8 (IS 2009-2016 alone, OOS 2017-2026 read ONCE).** 2 IS-only choosers x 2 panels = 4 picks, all published: U56 C_ISSHARPE and C_ISDD both take N=40/k=2.00 -> OOS 13.89% / 1.0925 / -22.98%; B136 C_ISSHARPE N=40/k=1.00 -> 15.47% / 1.0660 / -23.17%; B136 C_ISDD N=30/k=2.00 -> 16.61% / 1.0696 / -24.35%. **4b OOS 0 of 4, every one failing the DD leg, and every pick a WIDE book** — IS Sharpe and IS MaxDD both prefer width in the first half and are both wrong about it in the second. **4a 0 of 40 points.** 5 of 20 U56 points clear 4b full-sample AND OOS, but **all five are N=20**, the incumbent, reached by no dial. Benchmarks: SPY full 15.10% / 0.8829 / -33.72% (halves 0.9588 / 0.8207), OOS 15.21% / 0.8711 / -33.72%; RULES v2 live full 8.62% / 1.2007 / -12.05%, OOS 9.45% / 1.2762 / -12.05% (B136: SPY OOS 15.33% / 0.8767 / -33.72%, RULES v2 OOS 7.88% / 1.1059 / -12.24%).

  **GATES 6 of 7, printed before any result number.** G1 fast runner == `engine.backtest` 1.39e-17; G2 CROSS-RUN idea 936's committed U56/CAND20/W/H126/0.75 triple (15.5787% / 1.1397 / -19.1276%) rebuilt to 3.18e-07; G3 SPY OOS triple 1.70e-04; G5 the cap dial is live; G6 live RULES v2 MaxDD == committed -12.05% at 4.95e-05. **G4 FAILS at 1.40e-03, and the declaration that produced it was simply WRONG**: weights DRIFT between rebalances and the cash sleeve does not, so a book REBUILT at gross lambda*g is not lambda times the book at g — what the lambda match actually builds is the DAILY-REBALANCED blend of the null path with cash, a real book but a different convention. **The wrong declaration is published beside the failure rather than replaced**, and G4b prices the gap at **0.020 pp of CAGR, 0.011 pp of MaxDD and 0.0014 of Sharpe** at half gross, against a MISS measured in whole percentage points — small enough that the contrast stands, and stated so it can be checked.

  **SURVIVORSHIP (rule 9).** U56 and B136 are CURRENT-CONSTITUENT panels, so every level is optimistic and every 4b count an UPPER bound, most of all the CAGR floor and the DD cap, which are read against SPY, a real index. The headline contrast is a book against its own null drawn from the same pool over the same tape, where the bias very largely cancels; the MISS levels are not, and are flattered.

  Script `research/backtests/2026-09-16_does-the-0.97pp-DRAWDOWN-MISS-close-under-a-NAME-CAP-instead-of-a-GROSS-CUT_cloud.py`, 7 CSVs, 4 LEADERBOARD rows. Follow-ups filed: 1081 (does the common-factor reading hold on a small-cap panel), 1082 (walk n BELOW 20 — is the incumbent an optimum or the top of an unwalked slope), 1083 (is the 1.0-1.3 pp residual miss decidable at this sample length).

